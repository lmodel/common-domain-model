/**
* The enumerated values to specify the business centers.
*/
export enum BusinessCenterEnum {
    
    /** Abu Dhabi, Business Day (as defined in 2021 ISDA Definitions Section 2.1.10 (ii)) */
    AEAB = "AEAB",
    /** Abu Dhabi, Settlement Day (as defined in 2021 ISDA Definitions Section 2.1.10 (i)) */
    AEAD = "AEAD",
    /** Dubai, United Arab Emirates */
    AEDU = "AEDU",
    /** Yerevan, Armenia */
    AMYE = "AMYE",
    /** Luanda, Angola */
    AOLU = "AOLU",
    /** Buenos Aires, Argentina */
    ARBA = "ARBA",
    /** Vienna, Austria */
    ATVI = "ATVI",
    /** Adelaide, Australia */
    AUAD = "AUAD",
    /** Brisbane, Australia */
    AUBR = "AUBR",
    /** Canberra, Australia */
    AUCA = "AUCA",
    /** Darwin, Australia */
    AUDA = "AUDA",
    /** Melbourne, Australia */
    AUME = "AUME",
    /** Perth, Australia */
    AUPE = "AUPE",
    /** Sydney, Australia */
    AUSY = "AUSY",
    /** Baku, Azerbaijan */
    AZBA = "AZBA",
    /** Bridgetown, Barbados */
    BBBR = "BBBR",
    /** Dhaka, Bangladesh */
    BDDH = "BDDH",
    /** Brussels, Belgium */
    BEBR = "BEBR",
    /** Sofia, Bulgaria */
    BGSO = "BGSO",
    /** Manama, Bahrain */
    BHMA = "BHMA",
    /** Hamilton, Bermuda */
    BMHA = "BMHA",
    /** Bandar Seri Begawan, Brunei */
    BNBS = "BNBS",
    /** La Paz, Bolivia */
    BOLP = "BOLP",
    /** Brazil Business Day. */
    BRBD = "BRBD",
    /** Brasilia, Brazil. */
    BRBR = "BRBR",
    /** Rio de Janeiro, Brazil. */
    BRRJ = "BRRJ",
    /** Sao Paulo, Brazil. */
    BRSP = "BRSP",
    /** Nassau, Bahamas */
    BSNA = "BSNA",
    /** Gaborone, Botswana */
    BWGA = "BWGA",
    /** Minsk, Belarus */
    BYMI = "BYMI",
    /** Calgary, Canada */
    CACL = "CACL",
    /** Fredericton, Canada. */
    CAFR = "CAFR",
    /** Montreal, Canada */
    CAMO = "CAMO",
    /** Ottawa, Canada */
    CAOT = "CAOT",
    /** Toronto, Canada */
    CATO = "CATO",
    /** Vancouver, Canada */
    CAVA = "CAVA",
    /** Winnipeg, Canada */
    CAWI = "CAWI",
    /** Basel, Switzerland */
    CHBA = "CHBA",
    /** Geneva, Switzerland */
    CHGE = "CHGE",
    /** Zurich, Switzerland */
    CHZU = "CHZU",
    /** Abidjan, Cote d'Ivoire */
    CIAB = "CIAB",
    /** Santiago, Chile */
    CLSA = "CLSA",
    /** Yaounde, Cameroon */
    CMYA = "CMYA",
    /** Beijing, China */
    CNBE = "CNBE",
    /** Shanghai, China */
    CNSH = "CNSH",
    /** Bogota, Colombia */
    COBO = "COBO",
    /** San Jose, Costa Rica */
    CRSJ = "CRSJ",
    /** Willemstad, Curacao */
    CWWI = "CWWI",
    /** Nicosia, Cyprus */
    CYNI = "CYNI",
    /** Prague, Czech Republic */
    CZPR = "CZPR",
    /** Cologne, Germany */
    DECO = "DECO",
    /** Dusseldorf, Germany */
    DEDU = "DEDU",
    /** Frankfurt, Germany */
    DEFR = "DEFR",
    /** Hannover, Germany */
    DEHA = "DEHA",
    /** Hamburg, Germany */
    DEHH = "DEHH",
    /** Leipzig, Germany */
    DELE = "DELE",
    /** Mainz, Germany */
    DEMA = "DEMA",
    /** Munich, Germany */
    DEMU = "DEMU",
    /** Stuttgart, Germany */
    DEST = "DEST",
    /** Copenhagen, Denmark */
    DKCO = "DKCO",
    /** Santo Domingo, Dominican Republic */
    DOSD = "DOSD",
    /** Algiers, Algeria */
    DZAL = "DZAL",
    /** Guayaquil, Ecuador */
    ECGU = "ECGU",
    /** Tallinn, Estonia */
    EETA = "EETA",
    /** Cairo, Egypt */
    EGCA = "EGCA",
    /** ESAS Settlement Day (as defined in 2006 ISDA Definitions Section 7.1 and Supplement Number 15 to the 2000 ISDA Definitions) */
    ESAS = "ESAS",
    /** Barcelona, Spain */
    ESBA = "ESBA",
    /** Madrid, Spain */
    ESMA = "ESMA",
    /** San Sebastian, Spain */
    ESSS = "ESSS",
    /** Addis Ababa, Ethiopia */
    ETAA = "ETAA",
    /** Publication dates for ICE Swap rates based on EUR-EURIBOR rates */
    EUR_ICESWAP = "EUR_ICESWAP",
    /** TARGET Settlement Day */
    EUTA = "EUTA",
    /** Helsinki, Finland */
    FIHE = "FIHE",
    /** Paris, France */
    FRPA = "FRPA",
    /** Edinburgh, Scotland */
    GBED = "GBED",
    /** London, United Kingdom */
    GBLO = "GBLO",
    /** Publication dates for GBP ICE Swap rates */
    GBP_ICESWAP = "GBP_ICESWAP",
    /** Tbilisi, Georgia */
    GETB = "GETB",
    /** Saint Peter Port, Guernsey */
    GGSP = "GGSP",
    /** Accra, Ghana */
    GHAC = "GHAC",
    /** Gibraltar, Gibraltar */
    GIGI = "GIGI",
    /** Banjul, Gambia */
    GMBA = "GMBA",
    /** Conakry, Guinea */
    GNCO = "GNCO",
    /** Athens, Greece */
    GRAT = "GRAT",
    /** Guatemala City, Guatemala */
    GTGC = "GTGC",
    /** Guatemala City, Guatemala [DEPRECATED, to be removed in 2024. Replaced by GTGC.] */
    GUGC = "GUGC",
    /** Hong Kong, Hong Kong */
    HKHK = "HKHK",
    /** Tegucigalpa, Honduras */
    HNTE = "HNTE",
    /** Zagreb, Republic of Croatia */
    HRZA = "HRZA",
    /** Budapest, Hungary */
    HUBU = "HUBU",
    /** Jakarta, Indonesia */
    IDJA = "IDJA",
    /** Dublin, Ireland */
    IEDU = "IEDU",
    /** Jerusalem, Israel */
    ILJE = "ILJE",
    /** Publication dates of the ILS-SHIR index. */
    ILS_SHIR = "ILS_SHIR",
    /** Publication dates of the ILS-TELBOR index. */
    ILS_TELBOR = "ILS_TELBOR",
    /** Tel Aviv, Israel */
    ILTA = "ILTA",
    /** Ahmedabad, India */
    INAH = "INAH",
    /** Bangalore, India */
    INBA = "INBA",
    /** Chennai, India */
    INCH = "INCH",
    /** Hyderabad, India */
    INHY = "INHY",
    /** Kolkata, India */
    INKO = "INKO",
    /** Mumbai, India */
    INMU = "INMU",
    /** New Delhi, India */
    INND = "INND",
    /** Baghdad, Iraq */
    IQBA = "IQBA",
    /** Teheran, Iran */
    IRTE = "IRTE",
    /** Reykjavik, Iceland */
    ISRE = "ISRE",
    /** Milan, Italy */
    ITMI = "ITMI",
    /** Rome, Italy */
    ITRO = "ITRO",
    /** Turin, Italy */
    ITTU = "ITTU",
    /** St. Helier, Channel Islands, Jersey */
    JESH = "JESH",
    /** Kingston, Jamaica */
    JMKI = "JMKI",
    /** Amman, Jordan */
    JOAM = "JOAM",
    /** Tokyo, Japan */
    JPTO = "JPTO",
    /** Nairobi, Kenya */
    KENA = "KENA",
    /** Phnom Penh, Cambodia */
    KHPP = "KHPP",
    /** Seoul, Republic of Korea */
    KRSE = "KRSE",
    /** Kuwait City, Kuwait */
    KWKC = "KWKC",
    /** George Town, Cayman Islands */
    KYGE = "KYGE",
    /** Almaty, Kazakhstan */
    KZAL = "KZAL",
    /** Vientiane, Laos */
    LAVI = "LAVI",
    /** Beirut, Lebanon */
    LBBE = "LBBE",
    /** Colombo, Sri Lanka */
    LKCO = "LKCO",
    /** Luxembourg, Luxembourg */
    LULU = "LULU",
    /** Riga, Latvia */
    LVRI = "LVRI",
    /** Casablanca, Morocco */
    MACA = "MACA",
    /** Rabat, Morocco */
    MARA = "MARA",
    /** Monaco, Monaco */
    MCMO = "MCMO",
    /** Ulan Bator, Mongolia */
    MNUB = "MNUB",
    /** Macau, Macao */
    MOMA = "MOMA",
    /** Valletta, Malta */
    MTVA = "MTVA",
    /** Port Louis, Mauritius */
    MUPL = "MUPL",
    /** Male, Maldives */
    MVMA = "MVMA",
    /** Lilongwe, Malawi */
    MWLI = "MWLI",
    /** Mexico City, Mexico */
    MXMC = "MXMC",
    /** Kuala Lumpur, Malaysia */
    MYKL = "MYKL",
    /** Labuan, Malaysia */
    MYLA = "MYLA",
    /** Maputo, Mozambique */
    MZMA = "MZMA",
    /** Windhoek, Namibia */
    NAWI = "NAWI",
    /** Abuja, Nigeria */
    NGAB = "NGAB",
    /** Lagos, Nigeria */
    NGLA = "NGLA",
    /** Amsterdam, Netherlands */
    NLAM = "NLAM",
    /** Rotterdam, Netherlands */
    NLRO = "NLRO",
    /** Oslo, Norway */
    NOOS = "NOOS",
    /** Kathmandu, Nepal */
    NPKA = "NPKA",
    /** New York Fed Business Day (as defined in 2006 ISDA Definitions Section 1.9, 2000 ISDA Definitions Section 1.9, and 2021 ISDA Definitions Section 2.1.7) */
    NYFD = "NYFD",
    /** New York Stock Exchange Business Day (as defined in 2006 ISDA Definitions Section 1.10, 2000 ISDA Definitions Section 1.10, and 2021 ISDA Definitions Section 2.1.8) */
    NYSE = "NYSE",
    /** Auckland, New Zealand */
    NZAU = "NZAU",
    /** New Zealand Business Day (proposed effective date: 2025-10-06) */
    NZBD = "NZBD",
    /** Wellington, New Zealand */
    NZWE = "NZWE",
    /** Muscat, Oman */
    OMMU = "OMMU",
    /** Panama City, Panama */
    PAPC = "PAPC",
    /** Lima, Peru */
    PELI = "PELI",
    /** Manila, Philippines */
    PHMA = "PHMA",
    /** Makati, Philippines */
    PHMK = "PHMK",
    /** Karachi, Pakistan */
    PKKA = "PKKA",
    /** Warsaw, Poland */
    PLWA = "PLWA",
    /** San Juan, Puerto Rico */
    PRSJ = "PRSJ",
    /** Lisbon, Portugal */
    PTLI = "PTLI",
    /** Doha, Qatar */
    QADO = "QADO",
    /** Bucharest, Romania */
    ROBU = "ROBU",
    /** Belgrade, Serbia */
    RSBE = "RSBE",
    /** Moscow, Russian Federation */
    RUMO = "RUMO",
    /** Abha, Saudi Arabia */
    SAAB = "SAAB",
    /** Jeddah, Saudi Arabia */
    SAJE = "SAJE",
    /** Riyadh, Saudi Arabia */
    SARI = "SARI",
    /** Stockholm, Sweden */
    SEST = "SEST",
    /** Singapore, Singapore */
    SGSI = "SGSI",
    /** Ljubljana, Slovenia */
    SILJ = "SILJ",
    /** Bratislava, Slovakia */
    SKBR = "SKBR",
    /** Freetown, Sierra Leone */
    SLFR = "SLFR",
    /** Dakar, Senegal */
    SNDA = "SNDA",
    /** San Salvador, El Salvador */
    SVSS = "SVSS",
    /** Bangkok, Thailand */
    THBA = "THBA",
    /** Tunis, Tunisia */
    TNTU = "TNTU",
    /** Ankara, Turkey */
    TRAN = "TRAN",
    /** Istanbul, Turkey */
    TRIS = "TRIS",
    /** Port of Spain, Trinidad and Tobago */
    TTPS = "TTPS",
    /** Taipei, Taiwan */
    TWTA = "TWTA",
    /** Dar es Salaam, Tanzania */
    TZDA = "TZDA",
    /** Dodoma, Tanzania */
    TZDO = "TZDO",
    /** Kiev, Ukraine */
    UAKI = "UAKI",
    /** Kampala, Uganda */
    UGKA = "UGKA",
    /** Boston, Massachusetts, United States */
    USBO = "USBO",
    /** Chicago, United States */
    USCH = "USCH",
    /** Charlotte, North Carolina, United States */
    USCR = "USCR",
    /** Washington, District of Columbia, United States */
    USDC = "USDC",
    /** Publication dates for ICE Swap rates based on USD-LIBOR rates */
    USD_ICESWAP = "USD_ICESWAP",
    /** Publication dates for the USD-Municipal Swap Index */
    USD_MUNI = "USD_MUNI",
    /** Denver, United States */
    USDN = "USDN",
    /** Detroit, Michigan, United States */
    USDT = "USDT",
    /** U.S. Government Securities Business Day (as defined in 2006 ISDA Definitions Section 1.11 and 2000 ISDA Definitions Section 1.11) */
    USGS = "USGS",
    /** Honolulu, Hawaii, United States */
    USHL = "USHL",
    /** Houston, United States */
    USHO = "USHO",
    /** Los Angeles, United States */
    USLA = "USLA",
    /** Mobile, Alabama, United States */
    USMB = "USMB",
    /** Minneapolis, United States */
    USMN = "USMN",
    /** New York, United States */
    USNY = "USNY",
    /** Portland, Oregon, United States */
    USPO = "USPO",
    /** Sacramento, California, United States */
    USSA = "USSA",
    /** Seattle, United States */
    USSE = "USSE",
    /** San Francisco, United States */
    USSF = "USSF",
    /** Wichita, United States */
    USWT = "USWT",
    /** Montevideo, Uruguay */
    UYMO = "UYMO",
    /** Tashkent, Uzbekistan */
    UZTA = "UZTA",
    /** Caracas, Venezuela */
    VECA = "VECA",
    /** Road Town, Virgin Islands (British) */
    VGRT = "VGRT",
    /** Hanoi, Vietnam */
    VNHA = "VNHA",
    /** Ho Chi Minh (formerly Saigon), Vietnam */
    VNHC = "VNHC",
    /** Aden, Yemen */
    YEAD = "YEAD",
    /** Johannesburg, South Africa */
    ZAJO = "ZAJO",
    /** Lusaka, Zambia */
    ZMLU = "ZMLU",
    /** Harare, Zimbabwe */
    ZWHA = "ZWHA",
};

export enum CommodityBusinessCalendarEnum {
    
    /** Abu Dhabi Securities Exchange https://www.adx.ae/ */
    ADSM = "ADSM",
    /** Argus Media Fertilizer Reports. http://www.argusmedia.com/Fertilizer */
    AGRUS_FMB = "AGRUS_FMB",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    APPI = "APPI",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ARGUS_CRUDE = "ARGUS_CRUDE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ARGUS_EUROPEAN_GAS = "ARGUS_EUROPEAN_GAS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ARGUS_EUROPEAN_PRODUCTS = "ARGUS_EUROPEAN_PRODUCTS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ARGUS_INTERNATIONAL_LPG = "ARGUS_INTERNATIONAL_LPG",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ARGUS_MCCLOSKEYS_COAL_REPORT = "ARGUS_MCCLOSKEYS_COAL_REPORT",
    /** The Argus US Products report. http://www.argusmedia.com/Petroleum/Petroleum-Products/Argus-US-Products */
    ARGUS_US_PRODUCTS = "ARGUS_US_PRODUCTS",
    /** Australian Securities Exchange http://www.asx.com.au/ */
    ASX = "ASX",
    /** Australian Wheat Board. www.awb.com.au */
    AWB = "AWB",
    /** Australian Wool Exchange. http://www.awex.com.au/home.html */
    AWEX = "AWEX",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    BALTIC_EXCHANGE = "BALTIC_EXCHANGE",
    /** The business calendar of the Bank Negara Malaysia Policy Committee. */
    BANK_NEGARA_MALAYSIA_POLICY_COMMITTEE = "BANK_NEGARA_MALAYSIA_POLICY_COMMITTEE",
    /** The business calendar for the Belpex power exchange (www.belpex.be). */
    BELPEX = "BELPEX",
    /** BlueNext Power Market. */
    BLUENEXT = "BLUENEXT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    BM_F = "BM_F",
    /** The settlement business calendar for Bursa Malaysia. */
    BURSA_MALAYSIA_SETTLEMENT = "BURSA_MALAYSIA_SETTLEMENT",
    /** The trading business calendar for Bursa Malaysia. */
    BURSA_MALAYSIA_TRADING = "BURSA_MALAYSIA_TRADING",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CANADIAN_GAS_PRICE_REPORTER = "CANADIAN_GAS_PRICE_REPORTER",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CBOT_SOFT = "CBOT_SOFT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CMAI_AROMATICS_MARKET_REPORT = "CMAI_AROMATICS_MARKET_REPORT",
    /** CMAI Global Plastics and Polymers Market Report. http://www.ihs.com/products/chemical/index.aspx?pu=1&rd=cmai */
    CMAI_GLOBAL_PLASTICS_AND_POLYMERS_MARKET_REPORT = "CMAI_GLOBAL_PLASTICS_AND_POLYMERS_MARKET_REPORT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CMAI_METHANOL_MARKET_REPORT = "CMAI_METHANOL_MARKET_REPORT",
    /** CMAI Monomers Market Report. http://www.ihs.com/products/chemical/index.aspx?pu=1&rd=cmai */
    CMAI_MONOMERS_MARKET_REPORT = "CMAI_MONOMERS_MARKET_REPORT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CME_DAIRY = "CME_DAIRY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CME_NON_DAIRY_SOFT = "CME_NON_DAIRY_SOFT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    COMEX = "COMEX",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CRU = "CRU",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    CRU_LONG = "CRU_LONG",
    /** The business calendar for statistical publications by the by the United States Department of Energy (DOE). */
    DEPARTMENT_OF_ENERGY = "DEPARTMENT_OF_ENERGY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    DEWITT_BENZENE_DERIVATIVES = "DEWITT_BENZENE_DERIVATIVES",
    /** Dubai Mercantile Exchange. http://www.dubaimerc.com/ */
    DME = "DME",
    /** Dow Jones US Calendar. http://www.dowjones.com/ */
    DOW_JONES = "DOW_JONES",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    DOW_JONES_ENERGY_SERVICE = "DOW_JONES_ENERGY_SERVICE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    DowJonesPower = "DowJonesPower",
    /** European Energy Exchange-Coal */
    EEX_COAL = "EEX_COAL",
    /** European Energy Exchange-Emissions Rights */
    EEX_EMISSIONS = "EEX_EMISSIONS",
    /** European Energy Exchange-Gas */
    EEX_GAS = "EEX_GAS",
    /** European Energy Exchange-Power */
    EEX_POWER = "EEX_POWER",
    /** TBD. */
    EURONEX_MATIF = "EURONEX_MATIF",
    /** FERTECON Limited Information Services. http://fertecon.com/current_information_services.asp */
    FERTECON = "FERTECON",
    /** Fertilizer Week. http://www.crugroup.com/market-analysis/products/fertilizerweek */
    FERTILIZER_WEEK = "FERTILIZER_WEEK",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    GAS_DAILY = "GAS_DAILY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    GAS_DAILY_PRICE_GUIDE = "GAS_DAILY_PRICE_GUIDE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    GLOBALCOAL = "GLOBALCOAL",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    HEREN_REPORT = "HEREN_REPORT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ICE_10X_DAILY = "ICE_10X_DAILY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ICE_10X_MONTHLY = "ICE_10X_MONTHLY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ICE_CANADA = "ICE_CANADA",
    /** European Climate Exchange. */
    ICE_ECX = "ICE_ECX",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ICE_GAS = "ICE_GAS",
    /** The business calendar oil and refined product contracts on ICE Futures Europe. */
    ICE_OIL = "ICE_OIL",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    ICE_US_AGRICULTURAL = "ICE_US_AGRICULTURAL",
    /** The business calendar for publication of ICIS Benzene (Europe) data. */
    ICIS_PRICING_BENZENE__EUROPE_ = "ICIS_PRICING_BENZENE__EUROPE_",
    /** The business calendar for publication of ICIS Ethylene (Europe) data. */
    ICIS_PRICING_ETHYLENE__EUROPE_ = "ICIS_PRICING_ETHYLENE__EUROPE_",
    /** The business calendar for publication of ICIS Polyproylene (Europe) data. */
    ICIS_PRICING_POLYPROPYLENE__EUROPE_ = "ICIS_PRICING_POLYPROPYLENE__EUROPE_",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    INSIDE_FERC = "INSIDE_FERC",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    JAPAN_MOF_TSRR = "JAPAN_MOF_TSRR",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    KCBOT = "KCBOT",
    /** The banking business calendar in Kuala Lumpur. */
    KUALA_LUMPUR_BANK = "KUALA_LUMPUR_BANK",
    /** The business calendar for the Labuan Bank (Malaysia). */
    LABUAN_BANK = "LABUAN_BANK",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    LIFFE_LONDON_SOFT = "LIFFE_LONDON_SOFT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    LME = "LME",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    LONDON_BULLION_MARKET = "LONDON_BULLION_MARKET",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    LONDON_BULLION_MARKET_GOLD_A_M_ONLY = "LONDON_BULLION_MARKET_GOLD_A_M_ONLY",
    /** The London Platinum and Palladium Market in London on which members quote prices for the buying and selling of Platinum and Palladium. */
    LONDON_PLATINUM_PALLADIUM_MARKET = "LONDON_PLATINUM_PALLADIUM_MARKET",
    /** Minneapolis Grain Exchange http://www.mgex.com/ */
    MGEX = "MGEX",
    /** The business calendar for the N2EX UK power exchange (https://www.n2ex.com/aboutn2ex). */
    N2EX = "N2EX",
    /** NASDAQ-OMX (Formerly known as Nordpool). http://www.nasdaqomx.com/commodities */
    NASDAQ_OMX = "NASDAQ_OMX",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NATURAL_GAS_WEEK = "NATURAL_GAS_WEEK",
    /** Per 2005 ISDA Commodity Definitions, Article XIV. */
    NERC = "NERC",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NGI = "NGI",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NGX = "NGX",
    /** The Nuclear Market Review report as published by Trade tech. http://www.uranium.info/nuclear_market_review.php */
    NUCLEAR_MARKET_REVIEW = "NUCLEAR_MARKET_REVIEW",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NYMEX_ELECTRICITY = "NYMEX_ELECTRICITY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NYMEX_GAS = "NYMEX_GAS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NYMEX_NATURAL_GAS = "NYMEX_NATURAL_GAS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    NYMEX_OIL = "NYMEX_OIL",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    OFFICIAL_BOARD_MARKETS = "OFFICIAL_BOARD_MARKETS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    OPIS_LP_GAS = "OPIS_LP_GAS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    OPIS_PROPANE = "OPIS_PROPANE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PAPER_PACKAGING_MONITOR = "PAPER_PACKAGING_MONITOR",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PAPER_TRADER = "PAPER_TRADER",
    /** Pertamina-Indonesia. http://www.pertamina.com/ */
    PERTAMINA = "PERTAMINA",
    /** PetroChemWire Publication Calendar. http://www.petrochemwire.com/ */
    PETROCHEMWIRE = "PETROCHEMWIRE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PIX_PULP_BENCHMARK_INDICES = "PIX_PULP_BENCHMARK_INDICES",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_APAG_MARKETSCAN = "PLATTS_APAG_MARKETSCAN",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_BUNKERWIRE = "PLATTS_BUNKERWIRE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_CLEAN_TANKERWIRE = "PLATTS_CLEAN_TANKERWIRE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_CRUDE_OIL_MARKETWIRE = "PLATTS_CRUDE_OIL_MARKETWIRE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_DIRTY_TANKERWIRE = "PLATTS_DIRTY_TANKERWIRE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_EUROPEAN_GAS = "PLATTS_EUROPEAN_GAS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_EUROPEAN_MARKETSCAN = "PLATTS_EUROPEAN_MARKETSCAN",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_METALS_ALERT = "PLATTS_METALS_ALERT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_OILGRAM = "PLATTS_OILGRAM",
    /** The Steel Index Iron Ore Service. http://www.thesteelindex.com/en/iron-ore */
    PLATTS_TSI_IRON_ORE = "PLATTS_TSI_IRON_ORE",
    /** The Steel Index Scrap Reference Prices. http://www.thesteelindex.com/en/scrapprices */
    PLATTS_TSI_SCRAP = "PLATTS_TSI_SCRAP",
    /** The Steel Index. http://www.thesteelindex.com/en/price-specifications */
    PLATTS_TSI_STEEL = "PLATTS_TSI_STEEL",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PLATTS_US_MARKETSCAN = "PLATTS_US_MARKETSCAN",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PULP_AND_PAPER_INTERNATIONAL = "PULP_AND_PAPER_INTERNATIONAL",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    PULP_AND_PAPER_WEEK = "PULP_AND_PAPER_WEEK",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    RIM_PRODUCTS_INTELLIGENCE_DAILY = "RIM_PRODUCTS_INTELLIGENCE_DAILY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    SAFEX_SOFT = "SAFEX_SOFT",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    SFE_SOFT = "SFE_SOFT",
    /** Singapore Exchange. www.sgx.com */
    SGX = "SGX",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    SICOM = "SICOM",
    /** Standard and Poor's GSCI. http://us.spindices.com/index-family/commodities/sp-gsci */
    SP_GSCI = "SP_GSCI",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    STATISTICHES_BUNDESAMT = "STATISTICHES_BUNDESAMT",
    /** Tokyo Grain Exchange. www.tge.or.jp */
    TGE = "TGE",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    TOCOM_OIL = "TOCOM_OIL",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    TOCOM_PRECIOUS = "TOCOM_PRECIOUS",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    TOCOM_SOFT = "TOCOM_SOFT",
    /** The Ux Consulting Company. http://www.uxc.com/products/uxw_overview.aspx */
    UX_WEEKLY = "UX_WEEKLY",
    /** Per 2005 ISDA Commodity Definitions, Section 7.2 Certain Definitions Relating To Commodity Reference Prices. */
    WORLD_PULP_MONTHLY = "WORLD_PULP_MONTHLY",
};
/**
* The enumerated values to specify the convention for adjusting any relevant date if it would otherwise fall on a day that is not a valid business day.
*/
export enum BusinessDayConventionEnum {
    
    /** The non-business date will be adjusted to the first following day that is a business day */
    FOLLOWING = "FOLLOWING",
    /** Per 2000 ISDA Definitions, Section 4.11. FRN Convention; Eurodollar Convention. FRN is included here as a type of business day convention although it does not strictly fall within ISDA's definition of a Business Day Convention and does not conform to the simple definition given above. */
    FRN = "FRN",
    /** The non-business date will be adjusted to the first following day that is a business day unless that day falls in the next calendar month, in which case that date will be the first preceding day that is a business day. */
    MODFOLLOWING = "MODFOLLOWING",
    /** The non-business day will be adjusted to the first preceding day that is a business day. */
    PRECEDING = "PRECEDING",
    /** The non-business date will be adjusted to the first preceding day that is a business day unless that day falls in the previous calendar month, in which case that date will be the first following day that us a business day. */
    MODPRECEDING = "MODPRECEDING",
    /** The non-business date will be adjusted to the nearest day that is a business day - i.e. if the non-business day falls on any day other than a Sunday or a Monday, it will be the first preceding day that is a business day, and will be the first following business day if it falls on a Sunday or a Monday. */
    NEAREST = "NEAREST",
    /** The date will not be adjusted if it falls on a day that is not a business day. */
    NONE = "NONE",
    /** The date adjustments conventions are defined elsewhere, so it is not required to specify them here. */
    NotApplicable = "NotApplicable",
};
/**
* The enumerated values to specify a day of the seven-day week.
*/
export enum DayOfWeekEnum {
    
    /** Monday */
    MON = "MON",
    /** Tuesday */
    TUE = "TUE",
    /** Wednesday */
    WED = "WED",
    /** Thursday */
    THU = "THU",
    /** Friday */
    FRI = "FRI",
    /** Saturday */
    SAT = "SAT",
    /** Sunday */
    SUN = "SUN",
};
/**
* Lists the enumerated values to specify the day type classification used in counting the number of days between two dates.
*/
export enum DayTypeEnum {
    
    /** Applies when calculating the number of days between two dates the count includes only business days. */
    Business = "Business",
    /** Applies when calculating the number of days between two dates the count includes all calendar days. */
    Calendar = "Calendar",
    /** Applies when calculating the number of days between two dates the count includes only currency business days. */
    CurrencyBusiness = "CurrencyBusiness",
    /** Applies when calculating the number of days between two dates the count includes only stock exchange business days. */
    ExchangeBusiness = "ExchangeBusiness",
    /** Applies when calculating the number of days between two dates the count includes only scheduled trading days. */
    ScheduledTradingDay = "ScheduledTradingDay",
};
/**
* The enumerated values to specify the period, e.g. day, week.
*/
export enum PeriodEnum {
    
    /** Day */
    D = "D",
    /** Week */
    W = "W",
    /** Month */
    M = "M",
    /** Year */
    Y = "Y",
};
/**
* The enumerated values to specify a time period containing the additional value of Term.
*/
export enum PeriodExtendedEnum {
    
    /** Hour */
    H = "H",
    /** Day */
    D = "D",
    /** Week */
    W = "W",
    /** Month */
    M = "M",
    /** Year */
    Y = "Y",
    /** Term. The period commencing on the effective date and ending on the termination date. The T period always appears in association with periodMultiplier = 1, and the notation is intended for use in contexts where the interval thus qualified (e.g. accrual period, payment period, reset period, ...) spans the entire term of the trade. */
    T = "T",
    /** CalculationPeriod - the period corresponds to the calculation period   For example, used in the Commodity Markets to indicate that a reference contract is the one that corresponds to the period of the calculation period. */
    C = "C",
};
/**
* The enumeration values to specify a time period containing additional values such as Term.
*/
export enum PeriodTimeEnum {
    
    /** Period measured in hours. */
    Hour = "Hour",
    /** Period measured in minutes. */
    Minute = "Minute",
    /** Period measured in seconds. */
    Second = "Second",
};
/**
* The enumeration values to qualify the allowed units of time.
*/
export enum TimeUnitEnum {
    
    /** Second */
    Second = "Second",
    /** Minute */
    Minute = "Minute",
    /** Hour */
    Hour = "Hour",
    /** Day */
    Day = "Day",
    /** Week */
    Week = "Week",
    /** Month */
    Month = "Month",
    /** Year */
    Year = "Year",
};
/**
* The enumerated values to specify the period term as part of a periodic schedule, i.e. the calculation period end date within the regular part of the calculation period. The value could be a rule, e.g. IMM Settlement Dates, which is the 3rd Wednesday of the month, or it could be a specific day of the month, such as the first day of the applicable month.
*/
export enum RollConventionEnum {
    
    /** Rolls on month end dates irrespective of the length of the month and the previous roll day. */
    EOM = "EOM",
    /** Roll days are determined according to the FRN Convention or Euro-dollar Convention as described in ISDA 2000 definitions. */
    FRN = "FRN",
    /** IMM Settlement Dates. The third Wednesday of the (delivery) month. */
    IMM = "IMM",
    /** The last trading day/expiration day of the Canadian Derivatives Exchange (Bourse de Montreal Inc) Three-month Canadian Bankers' Acceptance Futures (Ticker Symbol BAX). The second London banking day prior to the third Wednesday of the contract month. If the determined day is a Bourse or bank holiday in Montreal or Toronto, the last trading day shall be the previous bank business day. Per Canadian Derivatives Exchange BAX contract specification. */
    IMMCAD = "IMMCAD",
    /** The last trading day of the Sydney Futures Exchange 90 Day Bank Accepted Bills Futures contract (see http://www.sfe.com.au/content/sfe/trading/con_specs.pdf). One Sydney business day preceding the second Friday of the relevant settlement. */
    IMMAUD = "IMMAUD",
    /** The last trading day of the Sydney Futures Exchange NZ 90 Day Bank Bill Futures contract (see http://www.sfe.com.au/content/sfe/trading/con_specs.pdf). The first Wednesday after the ninth day of the relevant settlement month. */
    IMMNZD = "IMMNZD",
    /** Sydney Futures Exchange 90-Day Bank Accepted Bill Futures Settlement Dates. The second Friday of the (delivery) month */
    SFE = "SFE",
    /** The roll convention is not required. For example, in the case of a daily calculation frequency. */
    NONE = "NONE",
    /** 13-week and 26-week U.S. Treasury Bill Auction Dates. Each Monday except for U.S. (New York) holidays when it will occur on a Tuesday. */
    TBILL = "TBILL",
    /** Rolls on the 1st day of the month. */
    _1 = "_1",
    /** Rolls on the 2nd day of the month. */
    _2 = "_2",
    /** Rolls on the 3rd day of the month. */
    _3 = "_3",
    /** Rolls on the 4th day of the month. */
    _4 = "_4",
    /** Rolls on the 5th day of the month. */
    _5 = "_5",
    /** Rolls on the 6th day of the month. */
    _6 = "_6",
    /** Rolls on the 7th day of the month. */
    _7 = "_7",
    /** Rolls on the 8th day of the month. */
    _8 = "_8",
    /** Rolls on the 9th day of the month. */
    _9 = "_9",
    /** Rolls on the 10th day of the month. */
    _10 = "_10",
    /** Rolls on the 11th day of the month. */
    _11 = "_11",
    /** Rolls on the 12th day of the month. */
    _12 = "_12",
    /** Rolls on the 13th day of the month. */
    _13 = "_13",
    /** Rolls on the 14th day of the month. */
    _14 = "_14",
    /** Rolls on the 15th day of the month. */
    _15 = "_15",
    /** Rolls on the 16th day of the month. */
    _16 = "_16",
    /** Rolls on the 17th day of the month. */
    _17 = "_17",
    /** Rolls on the 18th day of the month. */
    _18 = "_18",
    /** Rolls on the 19th day of the month. */
    _19 = "_19",
    /** Rolls on the 20th day of the month. */
    _20 = "_20",
    /** Rolls on the 21st day of the month. */
    _21 = "_21",
    /** Rolls on the 22nd day of the month. */
    _22 = "_22",
    /** Rolls on the 23rd day of the month. */
    _23 = "_23",
    /** Rolls on the 24th day of the month. */
    _24 = "_24",
    /** Rolls on the 25th day of the month. */
    _25 = "_25",
    /** Rolls on the 26th day of the month. */
    _26 = "_26",
    /** Rolls on the 27th day of the month. */
    _27 = "_27",
    /** Rolls on the 28th day of the month. */
    _28 = "_28",
    /** Rolls on the 29th day of the month. */
    _29 = "_29",
    /** Rolls on the 30th day of the month. */
    _30 = "_30",
    /** Rolling weekly on a Monday. */
    MON = "MON",
    /** Rolling weekly on a Tuesday */
    TUE = "TUE",
    /** Rolling weekly on a Wednesday */
    WED = "WED",
    /** Rolling weekly on a Thursday */
    THU = "THU",
    /** Rolling weekly on a Friday */
    FRI = "FRI",
    /** Rolling weekly on a Saturday */
    SAT = "SAT",
    /** Rolling weekly on a Sunday */
    SUN = "SUN",
};
/**
* The enumerated values to specify how the compounding calculation is done
*/
export enum CompoundingTypeEnum {
    
    /** Compounding is not applicable */
    None = "None",
    /** Compounding is done only on business days, i.e. not compounded each day on weekends or holidays. */
    Business = "Business",
    /** Compounding is done on each calendar day. */
    Calendar = "Calendar",
};
/**
* How often is rounding performed
*/
export enum RoundingFrequencyEnum {
    
    /** Rounding is done on each day */
    Daily = "Daily",
    /** Rounding is done only at the end of the period */
    PeriodEnd = "PeriodEnd",
};
/**
* The enumerated values to specify the day count fraction.
*/
export enum DayCountFractionEnum {
    
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (v), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (e) or Annex to the 2000 ISDA Definitions (June 2000 Version), Section 4.16. Day Count Fraction, paragraph (d). */
    ACT_360 = "ACT_360",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (ix), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (i). */
    ACT_365L = "ACT_365L",
    /** Per CFTC definitions. */
    ACT_364 = "ACT_364",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (iv), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (d) or Annex to the 2000 ISDA Definitions (June 2000 Version), Section 4.16. Day Count Fraction, paragraph (c). */
    ACT_365_FIXED = "ACT_365_FIXED",
    /** The Fixed/Floating Amount will be calculated in accordance with the 'BASE EXACT/EXACT' day count fraction, as defined in the 'Definitions Communes plusieurs Additifs Techniques' published by the Association Francaise des Banques in September 1994. */
    ACT_ACT_AFB = "ACT_ACT_AFB",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (iii), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (c). This day count fraction code is applicable for transactions booked under the 2021 ISDA Definitions and the 2006 ISDA Definitions. Transactions under the 2000 ISDA Definitions should use the ACT/ACT.ISMA code instead. */
    ACT_ACT_ICMA = "ACT_ACT_ICMA",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (ii), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (b) or Annex to the 2000 ISDA Definitions (June 2000 Version), Section 4.16. Day Count Fraction, paragraph (b). Note that going from FpML 2.0 Recommendation to the FpML 3.0 Trial Recommendation the code in FpML 2.0 'ACT/365.ISDA' became 'ACT/ACT.ISDA'. */
    ACT_ACT_ISDA = "ACT_ACT_ISDA",
    /** The Fixed/Floating Amount will be calculated in accordance with Rule 251 of the statutes, by-laws, rules and recommendations of the International Securities Market Association, as published in April 1999, as applied to straight and convertible bonds issued after December 31, 1998, as though the Fixed/Floating Amount were the interest coupon on such a bond. This day count fraction code is applicable for transactions booked under the 2000 ISDA Definitions. Transactions under the 2021 ISDA Definitions and the 2006 ISDA Definitions should use the ACT/ACT.ICMA code instead. */
    ACT_ACT_ISMA = "ACT_ACT_ISMA",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (x). Supercedes BUS/252, the number of Business Days in the Calculation Period or Compounding Period in respect of which payment is being made divided by 252. */
    CAL_252 = "CAL_252",
    /** Per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (a) or Annex to the 2000 ISDA Definitions (June 2000 Version), Section 4.16. Day Count Fraction, paragraph (a). */
    _1_1 = "_1_1",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (vii), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (g) or Annex to the 2000 ISDA Definitions (June 2000 Version), Section 4.16. Day Count Fraction, paragraph (f). Note that the algorithm defined for this day count fraction has changed between the 2000 ISDA Definitions and 2006 ISDA Definitions. See Introduction to the 2006 ISDA Definitions for further information relating to this change. */
    _30E_360 = "_30E_360",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (viii), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (h). Note the algorithm for this day count fraction under the 2006 ISDA Definitions is designed to yield the same results in practice as the version of the 30E/360 day count fraction defined in the 2000 ISDA Definitions. See Introduction to the 2006 ISDA Definitions for further information relating to this change. */
    _30E_360_ISDA = "_30E_360_ISDA",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (vi), per 2006 ISDA Definitions, Section 4.16. Day Count Fraction, paragraph (f) or Annex to the 2000 ISDA Definitions (June 2000 Version), Section 4.16. Day Count Fraction, paragraph (e). */
    _30_360 = "_30_360",
    /** Per 2021 ISDA Definitions, Section 4.6.1 Day Count Fraction, paragraph (xi), the calculation mechanics are driven deterministically by the Calculation Period Frequency (i.e. 0.25 if it is three months, 0.5 if it is 6 months, 1 if it is a year), except that if the first Calculation Period or the final Calculation Period is less than the Calculation Period Frequency, Actual/Actual (ISDA) shall apply to that Calculation Period */
    RBA_BOND_BASIS = "RBA_BOND_BASIS",
};
/**
* The enumerated values to specify the method of calculation to be used when averaging rates. Per ISDA 2000 Definitions, Section 6.2. Certain Definitions Relating to Floating Amounts.
*/
export enum AveragingWeightingMethodEnum {
    
    /** The arithmetic mean of the relevant rates for each reset date. */
    Unweighted = "Unweighted",
    /** The arithmetic mean of the relevant rates in effect for each day in a calculation period calculated by multiplying each relevant rate by the number of days such relevant rate is in effect, determining the sum of such products and dividing such sum by the number of days in the calculation period. */
    Weighted = "Weighted",
};
/**
* Specifies enumerations for the type of averaging calculation.
*/
export enum AveragingCalculationMethodEnum {
    
    /** Refers to the calculation of an average by taking the sum of observations divided by the count of observations. */
    Arithmetic = "Arithmetic",
    /** Refers to the calculation of an average by taking the nth root of the product of n observations. */
    Geometric = "Geometric",
    /** Refers to the calculation of an average by taking the reciprocal of the arithmetic mean of the reciprocals of the observations. */
    Harmonic = "Harmonic",
};
/**
* The enumerated values to specify the rounding direction and precision to be used in the rounding of a number.  Used by function cdm.base.math.RoundToPrecision.
*/
export enum RoundingDirectionEnum {
    
    /** A fractional number will be rounded up to the specified number of decimal places (the precision). For example, 5.21 and 5.25 rounded up to 1 decimal place are 5.3 and 5.3 respectively. */
    Up = "Up",
    /** A fractional number will be rounded down to the specified number of decimal places (the precision). For example, 5.29 and 5.25 rounded down to 1 decimal place are 5.2 and 5.2 respectively. */
    Down = "Down",
    /** A fractional number will be rounded either up or down to the specified number of decimal places (the precision) depending on its value. For example, 5.24 would be rounded down to 5.2 and 5.25 would be rounded up to 5.3 if a precision of 1 decimal place were specified. */
    Nearest = "Nearest",
};
/**
* The enumerated values to specify the rounding direction when rounding of a number to nearest.  Used by function cdm.base.math.RoundToNearest.
*/
export enum RoundingModeEnum {
    
    /** A number will be rounded down to the specified nearest number. For example, 529 rounded down to the nearest 10 is 520. */
    Down = "Down",
    /** A number will be rounded up to the specified nearest number. For example, 521 rounded up to the nearest 10 is 530. */
    Up = "Up",
};
/**
* Represents the enumerated values to specify a logical quantification, i.e. either All or Any.
*/
export enum QuantifierEnum {
    
    /** Specifies that the condition in the scope of the quantifier is true of every member of the domain i.e. every one of the elements in scope. */
    All = "All",
    /** Specifies that the condition in the scope of the quantifier is true of at least one member of the domain i.e. one or more of the elements in scope. */
    Any = "Any",
};

export enum CompareOp {
    
    GreaterThan = "GreaterThan",
    GreaterThanOrEquals = "GreaterThanOrEquals",
    Equals = "Equals",
    LessThanOrEquals = "LessThanOrEquals",
    LessThan = "LessThan",
};
/**
* Provides enumerated values for capacity units, generally used in the context of defining quantities for commodities.
*/
export enum CapacityUnitEnum {
    
    /** Denotes Allowances as standard unit. */
    ALW = "ALW",
    /** Denotes a Barrel as a standard unit. */
    BBL = "BBL",
    /** Denotes Billion Cubic Feet as a standard unit. */
    BCF = "BCF",
    /** Denotes Board Feet as a standard unit. */
    BDFT = "BDFT",
    /** Denotes Cubic Meters as a standard unit. */
    CBM = "CBM",
    /** Denotes Certified Emissions Reduction as a standard unit. */
    CER = "CER",
    /** Denotes Climate Reserve Tonnes as a standard unit. */
    CRT = "CRT",
    /** Denotes 10 grams as a standard unit used in precious metals contracts (e.g MCX). */
    DAG = "DAG",
    /** Denotes a single day as a standard unit used in time charter trades. */
    DAY = "DAY",
    /** Denotes Dry Metric Ton (Tonne) Units - Consists of a metric ton of mass excluding moisture. */
    DMTU = "DMTU",
    /** Denotes Environmental Credit as a standard unit. */
    ENVCRD = "ENVCRD",
    /** Denotes Environmental Offset as a standard unit. */
    ENVOFST = "ENVOFST",
    /** Denotes a 40 ft. Equivalent Unit container as a standard unit. */
    FEU = "FEU",
    /** Denotes a Gram as a standard unit. */
    G = "G",
    /** Denotes a GB Bushel as a standard unit. */
    GBBSH = "GBBSH",
    /** Denotes a GB British Thermal Unit as a standard unit. */
    GBBTU = "GBBTU",
    /** Denotes a GB Hundredweight unit as standard unit. */
    GBCWT = "GBCWT",
    /** Denotes a GB Gallon unit as standard unit. */
    GBGAL = "GBGAL",
    /** Denotes a Thousand GB British Thermal Units as a standard unit. */
    GBMBTU = "GBMBTU",
    /** Denotes a Million GB British Thermal Units as a standard unit. */
    GBMMBTU = "GBMMBTU",
    /** Denotes a GB Ton as a standard unit. */
    GBT = "GBT",
    /** Denotes a GB Thermal Unit as a standard unit. */
    GBTHM = "GBTHM",
    /** Denotes a Gigajoule as a standard unit. */
    GJ = "GJ",
    /** Denotes a Gigawatt as a standard unit. */
    GW = "GW",
    /** Denotes a Gigawatt-hour as a standard unit. */
    GWH = "GWH",
    /** Denotes a Hectolitre as a standard unit. */
    HL = "HL",
    /** Denotes a 100-troy ounces Gold Bar as a standard unit. */
    HOGB = "HOGB",
    /** Denotes an ISO British Thermal Unit as a standard unit. */
    ISOBTU = "ISOBTU",
    /** Denotes a Thousand ISO British Thermal Unit as a standard unit. */
    ISOMBTU = "ISOMBTU",
    /** Denotes a Million ISO British Thermal Unit as a standard unit. */
    ISOMMBTU = "ISOMMBTU",
    /** Denotes an ISO Thermal Unit as a standard unit. */
    ISOTHM = "ISOTHM",
    /** Denotes a Joule as a standard unit. */
    J = "J",
    /** Denotes a Kilogram as a standard unit. */
    KG = "KG",
    /** Denotes a Kilolitre as a standard unit. */
    KL = "KL",
    /** Denotes a Kilowatt as a standard unit. */
    KW = "KW",
    /** Denotes a Kilowatt-day as a standard unit. */
    KWD = "KWD",
    /** Denotes a Kilowatt-hour as a standard unit. */
    KWH = "KWH",
    /** Denotes a Kilowatt-month as a standard unit. */
    KWM = "KWM",
    /** Denotes a Kilowatt-minute as a standard unit. */
    KWMIN = "KWMIN",
    /** Denotes a Kilowatt-year as a standard unit. */
    KWY = "KWY",
    /** Denotes a Litre as a standard unit. */
    L = "L",
    /** Denotes a Pound as a standard unit. */
    LB = "LB",
    /** Denotes a Thousand Barrels as a standard unit. */
    MB = "MB",
    /** Denotes a Thousand board feet, which are used in contracts on forestry underlyers as a standard unit. */
    MBF = "MBF",
    /** Denotes a Megajoule as a standard unit. */
    MJ = "MJ",
    /** Denotes a Million board feet, which are used in contracts on forestry underlyers as a standard unit. */
    MMBF = "MMBF",
    /** Denotes a Million Barrels as a standard unit. */
    MMBBL = "MMBBL",
    /** Denotes a Thousand square feet as a standard unit. */
    MSF = "MSF",
    /** Denotes a Metric Ton as a standard unit. */
    MT = "MT",
    /** Denotes a Megawatt as a standard unit. */
    MW = "MW",
    /** Denotes a Megawatt-day as a standard unit. */
    MWD = "MWD",
    /** Denotes a Megawatt-hour as a standard unit. */
    MWH = "MWH",
    /** Denotes a Megawatt-month as a standard unit. */
    MWM = "MWM",
    /** Denotes a Megawatt-minute as a standard unit. */
    MWMIN = "MWMIN",
    /** Denotes a Megawatt-year as a standard unit. */
    MWY = "MWY",
    /** Denotes a Troy Ounce as a standard unit. */
    OZT = "OZT",
    /** Denotes a Standard Gold Bar as a standard unit. */
    SGB = "SGB",
    /** Denotes a 20 ft. Equivalent Unit container as a standard unit. */
    TEU = "TEU",
    /** Denotes a US Bushel as a standard unit. */
    USBSH = "USBSH",
    /** Denotes a US British Thermal Unit as a standard unit. */
    USBTU = "USBTU",
    /** Denotes US Hundredweight unit as a standard unit. */
    USCWT = "USCWT",
    /** Denotes a US Gallon unit as a standard unit. */
    USGAL = "USGAL",
    /** Denotes a Thousand US British Thermal Units as a standard unit. */
    USMBTU = "USMBTU",
    /** Denotes a Million US British Thermal Units as a standard unit. */
    USMMBTU = "USMMBTU",
    /** Denotes a US Ton as a standard unit. */
    UST = "UST",
    /** Denotes a US Thermal Unit as a standard unit. */
    USTHM = "USTHM",
};
/**
* Provides enumerated values for weather units, generally used in the context of defining quantities for commodities.
*/
export enum WeatherUnitEnum {
    
    /** Denotes Cooling Degree Days as a standard unit. */
    CDD = "CDD",
    /** Denotes Critical Precipitation Day as a standard unit. */
    CPD = "CPD",
    /** Heating Degree Day as a standard unit. */
    HDD = "HDD",
};
/**
* Provides enumerated values for financial units, generally used in the context of defining quantities for securities.
*/
export enum FinancialUnitEnum {
    
    /** Denotes financial contracts, such as listed futures and options. */
    Contract = "Contract",
    /** Denotes a Contractual Product as defined in the CDM.  This unit type would be used when the price applies to the whole product, for example, in the case of a premium expressed as a cash amount. */
    ContractualProduct = "ContractualProduct",
    /** Denotes a price expressed in index points, e.g. for a stock index. */
    IndexUnit = "IndexUnit",
    /** Denotes a log normal volatility, expressed in %/month, where the percentage is represented as a decimal. For example, 0.15 means a log-normal volatility of 15% per month. */
    LogNormalVolatility = "LogNormalVolatility",
    /** Denotes the number of units of financial stock shares. */
    Share = "Share",
    /** Denotes a value (expressed in currency units) for a one day change in a valuation date, which is typically used for expressing sensitivity to the passage of time, also known as theta risk, or carry, or other names. */
    ValuePerDay = "ValuePerDay",
    /** Denotes a value (expressed in currency units) per percent change in the underlying rate which is typically used for expressing sensitivity to volatility changes, also known as vega risk. */
    ValuePerPercent = "ValuePerPercent",
    /** Denotes a quantity (expressed as a decimal value) represented the weight of a component in a basket. */
    Weight = "Weight",
};
/**
* An arithmetic operator that can be passed to a function
*/
export enum ArithmeticOperationEnum {
    
    /** Addition of 2 values */
    Add = "Add",
    /** Subtraction of 2nd value from 1st value */
    Subtract = "Subtract",
    /** Multiplication of 2 values */
    Multiply = "Multiply",
    /** Division of 1st value by 2nd value */
    Divide = "Divide",
    /** Maximum of 2 values */
    Max = "Max",
    /** Minimum of 2 values */
    Min = "Min",
};
/**
* Specifies whether a quantity change is an increase, a decrease or a replacement, whereby the quantity is always specified as a positive number.
*/
export enum QuantityChangeDirectionEnum {
    
    /** When the quantity should go up by the specified amount. */
    Increase = "Increase",
    /** When the quantity should go down by the specified amount. */
    Decrease = "Decrease",
    /** When the quantity should be replaced by the specified amount. */
    Replace = "Replace",
};
/**
* Identifies the grade of physical commodity product to be delivered.
*/
export enum ProductGradeEnum {
    
    /** Applies to Oil Product Type Fuel Oil. */
    _0_1_Percent = "_0_1_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    _0_5_Percent = "_0_5_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    _1_0_Percent = "_1_0_Percent",
    /** Applies to Oil Product Type Gasoline. */
    _10_PPM = "_10_PPM",
    /** Applies to Oil Product Type Gasoline. */
    _10_PPM_95_R = "_10_PPM_95_R",
    /** Applies to Oil Product Type Gasoline. */
    _10_PPM_Premium_Unleaded = "_10_PPM_Premium_Unleaded",
    /** Applies to Oil Product Type Gasoline. */
    _10_PPM_Regular_Unleaded = "_10_PPM_Regular_Unleaded",
    /** Applies to Oil Product Type Fuel Oil. */
    _180_CST = "_180_CST",
    /** Applies to Oil Product Type Fuel Oil. */
    _3_5_Percent = "_3_5_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    _380_CST = "_380_CST",
    /** Applies to Oil Product Type Fuel Oil. */
    _50_PPM = "_50_PPM",
    /** Applies to Oil Product Type Jet Fuel. */
    _54 = "_54",
    /** Applies to Oil Product Type Jet Fuel. */
    _55 = "_55",
    /** Applies to Oil Product Type Gasoline. */
    _87_M = "_87_M",
    /** Applies to Oil Product Type Gasoline. */
    _87_Unleaded = "_87_Unleaded",
    /** Applies to Oil Product Type Gasoline. */
    _87_Unleaded_ULS_30 = "_87_Unleaded_ULS_30",
    /** Applies to Oil Product Type Gasoline. */
    _92_Unleaded = "_92_Unleaded",
    /** Applies to Oil Product Type Gasoline. */
    _93_Unleaded = "_93_Unleaded",
    /** Applies to Oil Product Type Gasoline. */
    _95_Unleaded = "_95_Unleaded",
    /** Applies to Oil Product Type Gasoline. */
    _97_Unleaded = "_97_Unleaded",
    /** Applies to Oil Product Type Gasoline. */
    _98_Unleaded = "_98_Unleaded",
    /** Applies to Oil Product Type Oil. */
    Arabian_Light = "Arabian_Light",
    /** Applies to Oil Product Type Oil. */
    Brass_Blend = "Brass_Blend",
    /** Applies to Oil Product Type Oil. */
    Brent = "Brent",
    /** Applies to Oil Product Type Gasoline. */
    CARBOB = "CARBOB",
    /** Applies to Oil Product Type Gasoline. */
    CBOB = "CBOB",
    /** Applies to Oil Product Type Oil. */
    Cold_Lake = "Cold_Lake",
    /** Applies to Oil Product Type Oil. */
    Dubai = "Dubai",
    /** Applies to Oil Product Type Oil. */
    Edmonton_High_Sulphur_Sour = "Edmonton_High_Sulphur_Sour",
    /** Applies to Oil Product Type Gasoline. */
    EUROBOB = "EUROBOB",
    /** Applies to Oil Product Type Diesel Fuel. */
    German_10PPM = "German_10PPM",
    /** Applies to Oil Product Type Oil. */
    Gulf_Coast_Sweet = "Gulf_Coast_Sweet",
    /** Applies to Oil Product Type Oil. */
    Hardisty_Light = "Hardisty_Light",
    /** Applies to Oil Product Type Oil. */
    Iranian_Light_1 = "Iranian_Light_1",
    /** Applies to Oil Product Type Oil. */
    Iranian_Light = "Iranian_Light",
    /** Applies to Oil Product Type Jet Fuel. */
    Jet = "Jet",
    /** Applies to Oil Product Type Oil. */
    Kirkuk_Light = "Kirkuk_Light",
    /** Applies to Oil Product Type Oil. */
    Kuwait = "Kuwait",
    /** Applies to Oil Product Type Oil. */
    Light_Louisiana_Sweet = "Light_Louisiana_Sweet",
    /** Applies to Oil Product Type Diesel Fuel. */
    Low_Sulphur = "Low_Sulphur",
    /** Applies to Oil Product Type Jet Fuel. */
    Low_Sulphur_Jet = "Low_Sulphur_Jet",
    /** Applies to Oil Product Type Oil. */
    Mars = "Mars",
    /** Applies to Oil Product Type Oil. */
    Mixed_Sour_Blend = "Mixed_Sour_Blend",
    /** Applies to Oil Product Type Oil. */
    Mixed_Sweet_Blend = "Mixed_Sweet_Blend",
    /** Applies to Oil Product Type Oil. */
    Murban = "Murban",
    /** Applies to Oil Product Type Gasoline. */
    Natural_Gasoline = "Natural_Gasoline",
    /** Applies to Oil Product Type Diesel Fuel. */
    No__2 = "No__2",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_0_3_Percent = "No__6_0_3_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_0_3_Percent_HP = "No__6_0_3_Percent_HP",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_0_3_Percent_LP = "No__6_0_3_Percent_LP",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_0_7_Percent = "No__6_0_7_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_1_0_Percent = "No__6_1_0_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_2_2_Percent = "No__6_2_2_Percent",
    /** Applies to Oil Product Type Fuel Oil. */
    No__6_3_Percent = "No__6_3_Percent",
    /** Applies to Oil Product Type Butane. */
    Normal_Butane = "Normal_Butane",
    /** Applies to Oil Product Type Oil. */
    North_Dakota_Light = "North_Dakota_Light",
    /** Applies to Oil Product Type Oil. */
    Oman = "Oman",
    /** Applies to Oil Product Type Propylene. */
    Polymer_Grade = "Polymer_Grade",
    /** Applies to Oil Product Type Gasoline. */
    RBOB = "RBOB",
    /** Applies to Oil Product Type Oil. */
    Saharan = "Saharan",
    /** Applies to Oil Product Type Oil. */
    Sour = "Sour",
    /** Applies to Oil Product Type Oil. */
    Southern_Green_Canyon = "Southern_Green_Canyon",
    /** Applies to Oil Product Type Oil. */
    Tapis = "Tapis",
    /** Applies to Oil Product Type Oil. */
    Thunder_Horse = "Thunder_Horse",
    /** Applies to Oil Product Type Diesel Fuel. */
    Ultra_Low_Sulphur = "Ultra_Low_Sulphur",
    /** Applies to Oil Product Type Oil. */
    Urals = "Urals",
    /** Applies to Oil Product Type Oil. */
    Urals_Sour = "Urals_Sour",
    /** Applies to Oil Product Type Gasoline. */
    US_Regular_Unleaded = "US_Regular_Unleaded",
    /** Applies to Oil Product Type Oil. */
    Western_Canadian_Select = "Western_Canadian_Select",
    /** Applies to Oil Product Type Oil. */
    WTI = "WTI",
    /** Applies to Oil Product Type Oil. */
    WTS = "WTS",
    /** Applies to Oil Product Type Oil. */
    Zuetina = "Zuetina",
};
/**
* The enumerated values to specify the FpML asset class categorization.
*/
export enum AssetClassEnum {
    
    /** Commodity. */
    Commodity = "Commodity",
    /** Credit. */
    Credit = "Credit",
    /** Equity. */
    Equity = "Equity",
    /** ForeignExchange. */
    ForeignExchange = "ForeignExchange",
    /** InterestRate. */
    InterestRate = "InterestRate",
    /** Money Market Assets like CP and CD. */
    MoneyMarket = "MoneyMarket",
};
/**
* Extends product identifiers with additional identifier sources for Assets.
*/
export enum AssetIdTypeEnum {
    
    /** Used to identify the currency of a Cash Asset. */
    CurrencyCode = "CurrencyCode",
    /** The identifier follows the symbology set by the exchange which lists the asset. */
    ExchangeCode = "ExchangeCode",
    /** The identifier follows the symbology set by the clearing house which clears the asset. */
    ClearingCode = "ClearingCode",
};
/**
* Provides the enumerated values to specify the product identifier source.
*/
export enum ProductIdTypeEnum {
    
    /** Published by Bloomberg, the BBGID is a 12-digit alphanumeric randomly generated ID covering active and non-active securities. */
    BBGID = "BBGID",
    /** Published by Bloomberg as a short code to identify publicly trades shares of a particular stock on a specific exchange. */
    BBGTICKER = "BBGTICKER",
    /** Derived from the Committee on Uniform Security Identification Procedures, CUSIPs are 9-character identifiers that capture an issue’s important differentiating characteristics for issuers and their financial instruments in the U.S. and Canada. */
    CUSIP = "CUSIP",
    /** Issued under the guidelines of the Object Management Group, the Financial Instrument Global Identifier (FIGI) is a 12 character, alphanumeric, randomly generated ID covering hundreds of millions of active and inactive instruments. The identifier acts as a Uniform Resource Identifier (URI) to link to a set of metadata that uniquely and clearly describes the instrument. */
    FIGI = "FIGI",
    /** Issued by the International Swaps Dealers Association as a string representing a Commodity Reference Price used for purposes of determining a relevant price for an underlying commodity in an OTC derivatives contract. */
    ISDACRP = "ISDACRP",
    /** Issued by The International Securities Identification Number (ISIN) Organization, the ISIN is a 12-character alpha-numerical code used to uniformly identify a security for trading and settlement purposes. Securities with which ISINs can be used include debt securities, such as notes or bonds as well shares, such as common stock or shares of a fund, options, derivatives, and futures. The ISIN structure is defined in ISO 6166. */
    ISIN = "ISIN",
    /** The name of the product. */
    Name = "Name",
    /** Assigned by S&P Global Market Intelligence, REDID (Reference Entity Data Identifier) is a unique alphanumeric code to reference entities, primarily utilized within the credit derivatives market for the standardized identification of underlying entities. */
    REDID = "REDID",
    /** Issued by Refinitiv (formerly Reuters), the Reuters Instrument Codes(RIC) uniquely identifies financial instruments, including where they are traded. */
    RIC = "RIC",
    /** Used when the source is not otherwise in this enumerated list because it is internal or other reasons.  The source can be identified in the scheme which is part of the identifier attribute. */
    Other = "Other",
    /** Issued by the French Société Interprofessionnelle pour la Compensation des Valeurs Mobilières (SICOVAM) to identify French securities listed on French stock exchanges. */
    Sicovam = "Sicovam",
    /** Assigned by the London Stock Exchange, the Stock Exchange Daily Official List (SEDOL) is a list of security identifiers used in the United Kingdom and Ireland for clearing purposes.  SEDOLs serve as the National Securities Identifying Number for all securities issued in the United Kingdom and are therefore part of the security's ISIN as well. */
    SEDOL = "SEDOL",
    /** Assigned by the Derivatives Service Bureau Ltd (DSB), the Unique Product Identifier (UPI) is a unique code to describe an over-the-counter (OTC) derivatives product.  The UPI is used for identifying the product in transaction reporting data. */
    UPI = "UPI",
    /** Assigned by SIX Financial Information, Valoren is a unique numerical identification number typically ranging from six to nine digits primarily traded in Switzerland, Liechtenstein, and Belgium. This code enables the identification of securities such as stocks, bonds, and investment funds within these markets. */
    Valoren = "Valoren",
    /** Issued by the Institute for the Issuance and Administration of Securities in Germany (Securities Information), the Wertpapierkennnummer (WKN, WPKN, WPK or simply Wert) consists of six digits or capital letters (excluding I and O), and no check digit. It is used to identify German securities. */
    Wertpapier = "Wertpapier",
};
/**
* Represents the enumerated values to specify taxonomy sources.
*/
export enum TaxonomySourceEnum {
    
    /** Represents the ISO 10962 Classification of Financial Instruments code. */
    CFI = "CFI",
    /** Represents the ISDA product taxonomy. */
    ISDA = "ISDA",
    /** Represents the ISDA Collateral Asset Definition Identifier code. */
    ICAD = "ICAD",
    /** Represents the EMIR Article 9 Asset Definition Identifier code. */
    EMIR = "EMIR",
    /** Identifies European Union Eligible Collateral Assets classification categories based on EMIR Uncleared Margin Rules. */
    EU_EMIR_EligibleCollateralAssetClass = "EU_EMIR_EligibleCollateralAssetClass",
    /** Identifies United Kingdom Eligible Collateral Assets classification categories based on UK Onshored EMIR Uncleared Margin Rules Eligible Collateral asset classes for both initial margin (IM) and variation margin (VM) posted and collected between specified entities.Please note: UK EMIR regulation will detail which eligible collateral assets classes apply to each type of entity pairing (counterparty) and which apply to posting of IM and VM. */
    UK_EMIR_EligibleCollateralAssetClass = "UK_EMIR_EligibleCollateralAssetClass",
    /** Represents the Commodity Futures Trading Commission (CFTC) as a taxonomy source. */
    CFTC = "CFTC",
    /** Identifies US Eligible Collateral Assets classification categories based on Uncleared Margin Rules published by the CFTC and the US Prudential Regulator. Note: While the same basic categories exist in the CFTC and US Prudential Regulators margin rules, the precise definitions or application of those rules could differ between the two rules. */
    US_CFTC_PR_EligibleCollateralAssetClass = "US_CFTC_PR_EligibleCollateralAssetClass",
    /** Represents the Monetary Authority of Singapore (MAS) as a taxonomy source. */
    MAS = "MAS",
    /** Represents the Canadian Securities Administrators (CSA) as a taxonomy source. */
    CSA = "CSA",
    /** Denotes a user-specific scheme or taxonomy or other external sources not listed here. */
    Other = "Other",
};
/**
* The enumerated values to specify standard currency codes according to the International Standards Organization (ISO).  The set of codes in this enumerated list is sourced from ISO Standard 4217 (https://www.currency-iso.org/en/home/tables/table-a1.html), as of 29-Aug-18.
*/
export enum ISOCurrencyCodeEnum {
    
    /** UAE Dirham */
    AED = "AED",
    /** Afghani */
    AFN = "AFN",
    /** Lek */
    ALL = "ALL",
    /** Armenian Dram */
    AMD = "AMD",
    /** Kwanza */
    AOA = "AOA",
    /** Argentine Peso */
    ARS = "ARS",
    /** Australian Dollar */
    AUD = "AUD",
    /** Aruban Florin */
    AWG = "AWG",
    /** Azerbaijan Manat */
    AZN = "AZN",
    /** Convertible Mark */
    BAM = "BAM",
    /** Barbados Dollar */
    BBD = "BBD",
    /** Taka */
    BDT = "BDT",
    /** Bahraini Dinar */
    BHD = "BHD",
    /** Burundi Franc */
    BIF = "BIF",
    /** Bermudian Dollar */
    BMD = "BMD",
    /** Brunei Dollar */
    BND = "BND",
    /** Boliviano */
    BOB = "BOB",
    /** Mvdol */
    BOV = "BOV",
    /** Brazilian Real */
    BRL = "BRL",
    /** Bahamian Dollar */
    BSD = "BSD",
    /** Ngultrum */
    BTN = "BTN",
    /** Pula */
    BWP = "BWP",
    /** Belarusian Ruble */
    BYN = "BYN",
    /** Belize Dollar */
    BZD = "BZD",
    /** Canadian Dollar */
    CAD = "CAD",
    /** Congolese Franc */
    CDF = "CDF",
    /** WIR Euro */
    CHE = "CHE",
    /** Swiss Franc */
    CHF = "CHF",
    /** WIR Franc */
    CHW = "CHW",
    /** Unidad de Fomento */
    CLF = "CLF",
    /** Chilean Peso */
    CLP = "CLP",
    /** Yuan Renminbi */
    CNY = "CNY",
    /** Colombian Peso */
    COP = "COP",
    /** Unidad de Valor Real */
    COU = "COU",
    /** Costa Rican Colon */
    CRC = "CRC",
    /** Cuban Peso */
    CUP = "CUP",
    /** Cabo Verde Escudo */
    CVE = "CVE",
    /** Czech Koruna */
    CZK = "CZK",
    /** Djibouti Franc */
    DJF = "DJF",
    /** Danish Krone */
    DKK = "DKK",
    /** Dominican Peso */
    DOP = "DOP",
    /** Algerian Dinar */
    DZD = "DZD",
    /** Egyptian Pound */
    EGP = "EGP",
    /** Nakfa */
    ERN = "ERN",
    /** Ethiopian Birr */
    ETB = "ETB",
    /** Euro */
    EUR = "EUR",
    /** Fiji Dollar */
    FJD = "FJD",
    /** Falkland Islands Pound */
    FKP = "FKP",
    /** Pound Sterling */
    GBP = "GBP",
    /** Lari */
    GEL = "GEL",
    /** Ghana Cedi */
    GHS = "GHS",
    /** Gibraltar Pound */
    GIP = "GIP",
    /** Dalasi */
    GMD = "GMD",
    /** Guinean Franc */
    GNF = "GNF",
    /** Quetzal */
    GTQ = "GTQ",
    /** Guyana Dollar */
    GYD = "GYD",
    /** Hong Kong Dollar */
    HKD = "HKD",
    /** Lempira */
    HNL = "HNL",
    /** Gourde */
    HTG = "HTG",
    /** Forint */
    HUF = "HUF",
    /** Rupiah */
    IDR = "IDR",
    /** New Israeli Sheqel */
    ILS = "ILS",
    /** Indian Rupee */
    INR = "INR",
    /** Iraqi Dinar */
    IQD = "IQD",
    /** Iranian Rial */
    IRR = "IRR",
    /** Iceland Krona */
    ISK = "ISK",
    /** Jamaican Dollar */
    JMD = "JMD",
    /** Jordanian Dinar */
    JOD = "JOD",
    /** Yen */
    JPY = "JPY",
    /** Kenyan Shilling */
    KES = "KES",
    /** Som */
    KGS = "KGS",
    /** Riel */
    KHR = "KHR",
    /** Comorian Franc  */
    KMF = "KMF",
    /** North Korean Won */
    KPW = "KPW",
    /** Won */
    KRW = "KRW",
    /** Kuwaiti Dinar */
    KWD = "KWD",
    /** Cayman Islands Dollar */
    KYD = "KYD",
    /** Tenge */
    KZT = "KZT",
    /** Lao Kip */
    LAK = "LAK",
    /** Lebanese Pound */
    LBP = "LBP",
    /** Sri Lanka Rupee */
    LKR = "LKR",
    /** Liberian Dollar */
    LRD = "LRD",
    /** Loti */
    LSL = "LSL",
    /** Libyan Dinar */
    LYD = "LYD",
    /** Moroccan Dirham */
    MAD = "MAD",
    /** Moldovan Leu */
    MDL = "MDL",
    /** Malagasy Ariary */
    MGA = "MGA",
    /** Denar */
    MKD = "MKD",
    /** Kyat */
    MMK = "MMK",
    /** Tugrik */
    MNT = "MNT",
    /** Pataca */
    MOP = "MOP",
    /** Ouguiya */
    MRU = "MRU",
    /** Mauritius Rupee */
    MUR = "MUR",
    /** Rufiyaa */
    MVR = "MVR",
    /** Malawi Kwacha */
    MWK = "MWK",
    /** Mexican Peso */
    MXN = "MXN",
    /** Mexican Unidad de Inversion (UDI) */
    MXV = "MXV",
    /** Malaysian Ringgit */
    MYR = "MYR",
    /** Mozambique Metical */
    MZN = "MZN",
    /** Namibia Dollar */
    NAD = "NAD",
    /** Naira */
    NGN = "NGN",
    /** Cordoba Oro */
    NIO = "NIO",
    /** Norwegian Krone */
    NOK = "NOK",
    /** Nepalese Rupee */
    NPR = "NPR",
    /** New Zealand Dollar */
    NZD = "NZD",
    /** Rial Omani */
    OMR = "OMR",
    /** Balboa */
    PAB = "PAB",
    /** Sol */
    PEN = "PEN",
    /** Kina */
    PGK = "PGK",
    /** Philippine Peso */
    PHP = "PHP",
    /** Pakistan Rupee */
    PKR = "PKR",
    /** Zloty */
    PLN = "PLN",
    /** Guarani */
    PYG = "PYG",
    /** Qatari Rial */
    QAR = "QAR",
    /** Romanian Leu */
    RON = "RON",
    /** Serbian Dinar */
    RSD = "RSD",
    /** Russian Ruble */
    RUB = "RUB",
    /** Rwanda Franc */
    RWF = "RWF",
    /** Saudi Riyal */
    SAR = "SAR",
    /** Solomon Islands Dollar */
    SBD = "SBD",
    /** Seychelles Rupee */
    SCR = "SCR",
    /** Sudanese Pound */
    SDG = "SDG",
    /** Swedish Krona */
    SEK = "SEK",
    /** Singapore Dollar */
    SGD = "SGD",
    /** Saint Helena Pound */
    SHP = "SHP",
    /** Leone */
    SLE = "SLE",
    /** Somali Shilling */
    SOS = "SOS",
    /** Surinam Dollar */
    SRD = "SRD",
    /** South Sudanese Pound */
    SSP = "SSP",
    /** Dobra */
    STN = "STN",
    /** El Salvador Colon */
    SVC = "SVC",
    /** Syrian Pound */
    SYP = "SYP",
    /** Lilangeni */
    SZL = "SZL",
    /** Baht */
    THB = "THB",
    /** Somoni */
    TJS = "TJS",
    /** Turkmenistan New Manat */
    TMT = "TMT",
    /** Tunisian Dinar */
    TND = "TND",
    /** Pa’anga */
    TOP = "TOP",
    /** Turkish Lira */
    TRY = "TRY",
    /** Trinidad and Tobago Dollar */
    TTD = "TTD",
    /** New Taiwan Dollar */
    TWD = "TWD",
    /** Tanzanian Shilling */
    TZS = "TZS",
    /** Hryvnia */
    UAH = "UAH",
    /** Uganda Shilling */
    UGX = "UGX",
    /** US Dollar */
    USD = "USD",
    /** US Dollar (Next day) */
    USN = "USN",
    /** Uruguay Peso en Unidades Indexadas (UI) */
    UYI = "UYI",
    /** Peso Uruguayo */
    UYU = "UYU",
    /** Unidad Previsional */
    UYW = "UYW",
    /** Uzbekistan Sum */
    UZS = "UZS",
    /** Bolívar Soberano */
    VED = "VED",
    /** Bolívar Soberano */
    VES = "VES",
    /** Dong */
    VND = "VND",
    /** Vatu */
    VUV = "VUV",
    /** Tala */
    WST = "WST",
    /** Arab Accounting Dinar */
    XAD = "XAD",
    /** CFA Franc BEAC */
    XAF = "XAF",
    /** Silver */
    XAG = "XAG",
    /** Gold */
    XAU = "XAU",
    /** Bond Markets Unit European Composite Unit (EURCO) */
    XBA = "XBA",
    /** Bond Markets Unit European Monetary Unit (E.M.U.-6) */
    XBB = "XBB",
    /** Bond Markets Unit European Unit of Account 9 (E.U.A.-9) */
    XBC = "XBC",
    /** Bond Markets Unit European Unit of Account 17 (E.U.A.-17) */
    XBD = "XBD",
    /** East Caribbean Dollar */
    XCD = "XCD",
    /** Caribbean Guilder */
    XCG = "XCG",
    /** SDR (Special Drawing Right) */
    XDR = "XDR",
    /** CFA Franc BCEAO */
    XOF = "XOF",
    /** Palladium */
    XPD = "XPD",
    /** CFP Franc */
    XPF = "XPF",
    /** Platinum */
    XPT = "XPT",
    /** Sucre */
    XSU = "XSU",
    /** Codes specifically reserved for testing purposes */
    XTS = "XTS",
    /** ADB Unit of Account */
    XUA = "XUA",
    /** The codes assigned for transactions where no currency is involved */
    XXX = "XXX",
    /** Yemeni Rial */
    YER = "YER",
    /** Rand */
    ZAR = "ZAR",
    /** Zambian Kwacha */
    ZMW = "ZMW",
    /** Zimbabwe Gold */
    ZWG = "ZWG",
};
/**
* Union of the enumerated values defined by the International Standards Organization (ISO) and the FpML nonISOCurrencyScheme which consists of offshore and historical currencies (https://www.fpml.org/coding-scheme/non-iso-currency), as of 28-Oct-2016.
*/
export enum CurrencyCodeEnum {
    
    /** Offshore Chinese Yuan traded in Hong Kong. */
    CNH = "CNH",
    /** Offshore Chinese Yuan traded in Taiwan. */
    CNT = "CNT",
    /** Guernsey Pound. */
    GGP = "GGP",
    /** Isle of Man Pound. */
    IMP = "IMP",
    /** Jersey Pound. */
    JEP = "JEP",
    /** Tuvaluan Dollar. */
    KID = "KID",
    /** Monegasque Franc. */
    MCF = "MCF",
    /** Sammarinese Lira. */
    SML = "SML",
    /** Vatican Lira. */
    VAL = "VAL",
};
/**
* Represents an enumeration list to identify the type of entity issuing or guaranteeing the collateral asset.
*/
export enum CollateralEntityTypeEnum {
    
    /** Specifies international organisations and multilateral banks, entities constituted by treaties or with multiple sovereign members includes Multilateral development Banks. */
    SupraNational = "SupraNational",
    /** Specifies Sovereign, Government Debt Securities including Central Banks. */
    SovereignCentralBank = "SovereignCentralBank",
    /** Specifies institutions or bodies, typically constituted by statute, with a function mandated by the government and subject to government supervision inclusive of profit- and non-profit making bodies. Includes the US Agencies and GSEs and the EU concept of public sector entities. Excluding any entities which are also Regional Government. */
    QuasiGovernment = "QuasiGovernment",
    /** Specifies Regional Governments including states within countries, local authorities and municipalities. */
    RegionalGovernment = "RegionalGovernment",
    /** Specifies corporate bodies including Banks. */
    Corporate = "Corporate",
    /** Specifies a vehicle (with or without separate legal personality) designed for the purposes of collective investment towards a defined investment goal. */
    Fund = "Fund",
    /** Specifies a vehicle setup for the purpose of acquisition and financing of specific assets on a limited recourse basis. E.g. asset backed securities, including securitisations. */
    SpecialPurposeVehicle = "SpecialPurposeVehicle",
};
/**
* Represents an enumeration list to identify the type of supranational entity issuing the asset.
*/
export enum SupraNationalIssuerTypeEnum {
    
    /** Specifies International Financial Institution. */
    InternationalOrganisation = "InternationalOrganisation",
    /** Specifies Multilateral Bank or Multilateral Development Bank. */
    MultilateralBank = "MultilateralBank",
};
/**
* Represents an enumeration list to identify tranched or untranched credit risk.
*/
export enum CreditRiskEnum {
    
    /** Indicates tranched credit risk, including securitizations. */
    TranchedCreditRisk = "TranchedCreditRisk",
    /** Indicates tranched credit risk, including repackagings. */
    UntranchedCreditRisk = "UntranchedCreditRisk",
};
/**
* Represents an enumeration list to indentify the type of a security.
*/
export enum SecurityTypeEnum {
    
    /** Identifies a security as a fixed income security of debt issued and securitized as a tradable asset. */
    Debt = "Debt",
    /** Identifies a security as an Equity value of holding of shares in listed company. */
    Equity = "Equity",
    /** Identifies a security as representing holding in an investment fund. */
    Fund = "Fund",
    /** Identifies a security as a Warrant that give the right, but not the obligation, to buy or sell a security — most commonly an equity — at a certain price before expiration, or to receive the cash equivalent. */
    Warrant = "Warrant",
    /** Identifies a security as one that that offers a derivative-based economic return which is not structured as a bond, an equity or a warrant. Note that this security type is not a Certificate of Deposit (aka CD). */
    Certificate = "Certificate",
};
/**
* The enumerated values to specify the types of listed derivative options.
*/
export enum PutCallEnum {
    
    /** A put option gives the holder the right to sell the underlying asset by a certain date for a certain price. */
    Put = "Put",
    /** A call option gives the holder the right to buy the underlying asset by a certain date for a certain price. */
    Call = "Call",
};
/**
* Represents an enumeration list to identify the type of Equity.
*/
export enum EquityTypeEnum {
    
    /** Identifies an Equity of Common stocks and shares. */
    Ordinary = "Ordinary",
    /** Identifies an Equity of Non-Convertible Preference, Shares which hold priority to receive capital return in event of issuer liquidation. */
    NonConvertiblePreference = "NonConvertiblePreference",
    /** Identifies a negotiable depositary receipt certificate issued by a bank representing shares in a foreign company traded on a local stock exchange. */
    DepositaryReceipt = "DepositaryReceipt",
    /** Identifies an Equity of Convertible Preference, a stock which can be converted into a predetermined number of common shares and holds priority to receive capital return in event of issuer liquidation. */
    ConvertiblePreference = "ConvertiblePreference",
};

export enum DepositaryReceiptTypeEnum {
    
    /** American Depositary Receipts. Identifies a depositary receipt that are issued only by U.S. banks for foreign stocks that are traded on a U.S. exchange. */
    ADR = "ADR",
    /** Global Depositary Receipts. Identifies a depositary receipt that is listed on European and other international exchanges. */
    GDR = "GDR",
    /** Indian Depositary Receipts. Identifies a depositary receipt that are issued only by domestic depositories for foreign stocks that are traded on an Indian exchange. */
    IDR = "IDR",
    /** European Depositary Receipts. Identifies a depositary receipt that are issued only by European banks for foreign stocks that are traded on a European exchange. */
    EDR = "EDR",
};
/**
* Represents an enumeration list to identify the fund product type.
*/
export enum FundProductTypeEnum {
    
    /** Denotes a fund that invests only in highly liquid near-term instruments such as cash, cash equivalent securities, and high credit rating debt instruments with a short-term maturity. */
    MoneyMarketFund = "MoneyMarketFund",
    /** Denotes an investment fund consisting of stocks, bonds, and/or other assets that is passively managed and traded on a stock exchange. */
    ExchangeTradedFund = "ExchangeTradedFund",
    /** Denotes an investment fund consisting of stocks, bonds, and/or other assets that is actively managed and can only be purchased or sold through the investment manager. */
    MutualFund = "MutualFund",
    /** Denotes a fund that is not an Exchange Traded Fund, Money Market Fund or Mutual Fund. */
    OtherFund = "OtherFund",
};
/**
* Represents an enumeration list to identify the Maturity.
*/
export enum MaturityTypeEnum {
    
    /** Denotes a period from now until maturity date. */
    RemainingMaturity = "RemainingMaturity",
    /** Denotes a period from issuance until maturity date. */
    OriginalMaturity = "OriginalMaturity",
    /** Denotes a period from issuance date until now. */
    FromIssuance = "FromIssuance",
};
/**
* Specifies the type of secured debt.
*/
export enum SecuredTypeEnum {
    
    /** Debt instrument with periodic income payments and value derived from or backed by a specified pool of underlying assets. */
    AssetBacked = "AssetBacked",
    /** Complex product that is backed by a pool of loans and other assets and sold to institutional investors. */
    CollateralizedObligation = "CollateralizedObligation",
    /** Specifies a debt obligations issued by credit institutions which offer a so-called double-recourse protection to bondholders. */
    CoveredBonds = "CoveredBonds",
};
/**
* Specifies the underlying asset or asset pool securing an Asset Backed debt instrument.
*/
export enum AssetBackedEnum {
    
    /** When the asset(s) backing the debt are home equity loans, in which the borrower uses the equity of their home as collateral. */
    HomeEquity = "HomeEquity",
    /** When the asset(s) backing the debt are auto loans. */
    AutoLoan = "AutoLoan",
    /** When the asset(s) backing the debt are student loans. */
    StudentLoan = "StudentLoan",
    /** When the asset(s) backing the debt are credit card loans. */
    CreditCard = "CreditCard",
    /** When the asset(s) backing the debt are property, typically when the debt is a mortgage loan. */
    Property = "Property",
    /** When the asset(s) backing the debt instrument is a pool of mortgage loans, e.g. for a mortgage backed security. */
    Mortgage = "Mortgage",
    /** Any other asset which generates receivables for an Asset Backed Security. */
    Other = "Other",
};
/**
* Specifies different types of property.
*/
export enum PropertyTypeEnum {
    
    /** Commercial property. */
    Commercial = "Commercial",
    /** Residential property. */
    Residential = "Residential",
};
/**
* Specifies the type of Collateralized Obligation.
*/
export enum CollateralizedObligationEnum {
    
    /** Collateralized Debt Obligation. */
    CDO = "CDO",
    /** Collateralized Mortgage Obligation. */
    CMO = "CMO",
    /** Collateralized Loan Obligation. */
    CLO = "CLO",
    /** Collateralized Bond Obligation. */
    CBO = "CBO",
};
/**
* Specifies the type of Covered Bond.
*/
export enum CoveredBondEnum {
    
    /** German bank debenture collateralized by long-term assets. */
    Pfandbriefe = "Pfandbriefe",
    /** Pfandbriefe with an issuing syndicate and a minimum issuance volume of EUR 1 billion. */
    JumboPfandbriefe = "JumboPfandbriefe",
};
/**
* Specifies the order of repayment in the event of a sale or bankruptcy of the issuer or a related party (eg guarantor).
*/
export enum DebtSeniorityEnum {
    
    /** Denotes debt which is secured over assets of the issuer or a related party (eg guarantor). */
    Secured = "Secured",
    /** Denotes debt  which ranks pari passu with all other unsecured creditors of the issuer. */
    Senior = "Senior",
    /** Denotes debt  owed to an unsecured creditor that in the event of a liquidation can only be paid after the claims of secured and senior creditors have been met. */
    Subordinated = "Subordinated",
};
/**
* Represents an enumeration list that specifies the general rule for periodic interest rate payment.
*/
export enum DebtInterestEnum {
    
    /** Denotes payment calculated with reference to a fixed interest rate. */
    Fixed = "Fixed",
    /** Denotes payment calculated with reference to a floating interest rate. */
    Floating = "Floating",
    /** Denotes payment calculated with reference to one or more specified inflation rates. */
    InflationLinked = "InflationLinked",
    /** Denotes payment calculated with reference to one or more price or other indices (other than inflation rates). */
    IndexLinked = "IndexLinked",
    /** Denotes a stripped bond representing only the interest component. */
    InterestOnly = "InterestOnly",
    /** Denotes payment calculated with reference to other underlyings (not being floating interest rates, inflation rates or indices) or with a non-linear relationship to floating interest rates, inflation rates or indices. */
    OtherStructured = "OtherStructured",
    /** Denotes payment calculated with reference to the inverse of a floating interest rate. */
    InverseFloating = "InverseFloating",
    /** Denotes a zero coupon bond that does not pay intetrest. */
    ZeroCoupon = "ZeroCoupon",
};
/**
* Represents an enumeration list that specifies the general rule for repayment of principal.
*/
export enum DebtPrincipalEnum {
    
    /** Denotes that the principal is paid all at once on maturity of the debt insrument. Bullet debt instruments cannot be redeemed early by an issuer, which means they are non-callable. */
    Bullet = "Bullet",
    /** Denotes that the principal on the debt can be repaid early, in whole or in part, at the option of the issuer. */
    Callable = "Callable",
    /** Denotes that the principal on the debt can be repaid early, in whole or in part, at the option of the holder. */
    Puttable = "Puttable",
    /** Denotes that the principal on the debt is paid down regularly, along with its interest expense over the life of the debt instrument.  Includes amortising instruments with a bullet balance repayment at maturity. */
    Amortising = "Amortising",
    /** Denotes that the principal on the debt is calculated with reference to one or more specified inflation rates. */
    InflationLinked = "InflationLinked",
    /** Denotes that the  principal on the debt is calculated with reference to one or more price or other indices (other than inflation rates). */
    IndexLinked = "IndexLinked",
    /** Denotes that the  principal on the debt is calculated with reference to other underlyings (not being floating interest rates, inflation rates or indices) or with a non-linear relationship to floating interest rates, inflation rates or indices. */
    OtherStructured = "OtherStructured",
    /** Denotes a stripped bond representing only the principal component. */
    PrincipalOnly = "PrincipalOnly",
};
/**
* Specifies the contractual redemption or conversion features associated with a debt security.
*/
export enum RedemptionTypeEnum {
    
    /** Specifies a debt instrument that can be converted into common shares. */
    Convertible = "Convertible",
    /** Specifies a debt security that can be converted to equity in a company other than the issuing company. */
    Exchangeable = "Exchangeable",
    /** Specifies a debt security that can be converted into equity if a pre-defined trigger event occurs. */
    ContingentConvertible = "ContingentConvertible",
    /** Specifies a debt security that requires the issuer to periodically set aside money to buy back portions of the debt. */
    Sinkable = "Sinkable",
    /** Specifies a debt security that can be redeemed early if certain extraordinary events occur. */
    Extraordinary = "Extraordinary",
};
/**
* Specifies the redeeming party of a debt security.
*/
export enum RedemptionPartyEnum {
    
    /** Specifies the holder of a debt security. */
    Holder = "Holder",
    /** Specifies the issuer of a debt security. */
    Issuer = "Issuer",
};
/**
* Represents an enumeration list to identify the asset type.
*/
export enum AssetTypeEnum {
    
    /** Identifies negotiable financial instrument of monetary value with an issue ownership position. */
    Security = "Security",
    /** Identifies cash in a currency form. */
    Cash = "Cash",
    /** Identifies basic good used in commerce that is interchangeable with other goods of the same type. */
    Commodity = "Commodity",
    /** Identifies an asset as a listed derivative on an exchange. */
    ListedDerivative = "ListedDerivative",
    /** An asset that represents a loan or borrow obligation. */
    Loan = "Loan",
    /** Identifies an asset as a letter of credit or documentary credit/ bankers commercial credit.  A payment mechanism used in international trade to provide economic guarantee of payment by a creditworthy issuer for payment of exported goods. */
    LetterOfCredit = "LetterOfCredit",
    /** Identifies other asset types. */
    Other = "Other",
};

export enum MoneyMarketTypeEnum {
    
    CommercialPaper = "CommercialPaper",
    CertificateOfDeposit = "CertificateOfDeposit",
};

export enum DeliveryNearbyTypeEnum {
    
    /** Describes the reference contract as the one that pertains to the month-year of the calculation period. If used, the nearby count is expected to be 0. */
    CalculationPeriod = "CalculationPeriod",
    /** Specifies that the reference delivery date of the underlying Commodity shall be the expiration date of the futures contract in the nth nearby month. */
    NearbyMonth = "NearbyMonth",
    /** Specifies that the reference delivery date of the underlying Commodity shall be the expiration date of the futures contract in the nth nearby week. */
    NearbyWeek = "NearbyWeek",
};
/**
* Defines a publication in which the price can be found. (e.g Gas Daily, Platts Bloomberg.
*/
export enum CommodityInformationPublisherEnum {
    
    Argus = "Argus",
    ArgusMcCloskeys = "ArgusMcCloskeys",
    ArgusAmericasCrudeReport = "ArgusAmericasCrudeReport",
    ArgusBiofuelReport = "ArgusBiofuelReport",
    ArgusCrudeReport = "ArgusCrudeReport",
    ArgusEuropeanProductsReport = "ArgusEuropeanProductsReport",
    ArgusFMB = "ArgusFMB",
    ArgusInternationalLPGReport = "ArgusInternationalLPGReport",
    ArgusLPG = "ArgusLPG",
    ArgusNatGas = "ArgusNatGas",
    AssocBanksSingapore = "AssocBanksSingapore",
    BandD = "BandD",
    BalticExchange = "BalticExchange",
    BankOfCanada = "BankOfCanada",
    BankOfEngland = "BankOfEngland",
    BankOfJapan = "BankOfJapan",
    Bloomberg = "Bloomberg",
    BLUENEXT = "BLUENEXT",
    CAISO = "CAISO",
    CanadianGasPriceReporter = "CanadianGasPriceReporter",
    CanadianGasReporter = "CanadianGasReporter",
    ChemicalMarketsAssociation = "ChemicalMarketsAssociation",
    CMAIAromaticsMarketReport = "CMAIAromaticsMarketReport",
    CMAIWeeklyMethanolMarketReport = "CMAIWeeklyMethanolMarketReport",
    CRUSteelLongProductMonitor = "CRUSteelLongProductMonitor",
    CRUSteelSheetProductsMonitor = "CRUSteelSheetProductsMonitor",
    DowJonesEnergyService = "DowJonesEnergyService",
    DowJonesEnergyServiceScreen = "DowJonesEnergyServiceScreen",
    DowJonesNatGas = "DowJonesNatGas",
    EEX = "EEX",
    ERCOT = "ERCOT",
    EuroCentralBank = "EuroCentralBank",
    EURONEXMATIF = "EURONEXMATIF",
    FederalReserve = "FederalReserve",
    FERTECON = "FERTECON",
    FertilizerWeek = "FertilizerWeek",
    FHLBSF = "FHLBSF",
    GasDaily = "GasDaily",
    GasDailyPriceGuide = "GasDailyPriceGuide",
    GlobalCoale = "GlobalCoale",
    GME = "GME",
    HerenReport = "HerenReport",
    ICE = "ICE",
    ICE10XDailyNaturalGas = "ICE10XDailyNaturalGas",
    ICE10XDailyPower = "ICE10XDailyPower",
    ICE10XMonthly = "ICE10XMonthly",
    ICEDayAheadIndex = "ICEDayAheadIndex",
    ICEECX = "ICEECX",
    ICIS = "ICIS",
    InsideFERC = "InsideFERC",
    IPE = "IPE",
    ISDA = "ISDA",
    ISONewEngland = "ISONewEngland",
    JAPANMOFTSRR = "JAPANMOFTSRR",
    LEBA = "LEBA",
    LondonBullionMarketAssociation = "LondonBullionMarketAssociation",
    LONDONPLATINUMPALLADIUMMARKET = "LONDONPLATINUMPALLADIUMMARKET",
    MegawattDaily = "MegawattDaily",
    MetalBulletin = "MetalBulletin",
    MISO = "MISO",
    NaturalGasWeek = "NaturalGasWeek",
    NGIBidweekSurvey = "NGIBidweekSurvey",
    NYISO = "NYISO",
    OBM = "OBM",
    OMEL = "OMEL",
    OPIS = "OPIS",
    PaperTrader = "PaperTrader",
    PIX = "PIX",
    PJM = "PJM",
    PlattsAsiaPacific = "PlattsAsiaPacific",
    PlattsAsiaPacificArabMarketscan = "PlattsAsiaPacificArabMarketscan",
    PlattsCleanTankerwire = "PlattsCleanTankerwire",
    PlattsCoalTrader = "PlattsCoalTrader",
    PlattsCrudeOilMarketwire = "PlattsCrudeOilMarketwire",
    PlattsDirtyTakerwire = "PlattsDirtyTakerwire",
    PlattsENGR = "PlattsENGR",
    PlattsEuropean = "PlattsEuropean",
    PlattsEuropeanMarketscan = "PlattsEuropeanMarketscan",
    PlattsGasDaily = "PlattsGasDaily",
    PlattsGasDailyPriceGuide = "PlattsGasDailyPriceGuide",
    PlattsInsideFERC = "PlattsInsideFERC",
    PlattsLPG = "PlattsLPG",
    PlattsMarketwire = "PlattsMarketwire",
    PlattsMegawattDaily = "PlattsMegawattDaily",
    PlattsMetalsAlert = "PlattsMetalsAlert",
    PlattsOilgram = "PlattsOilgram",
    PlattsOilgramBunkerwire = "PlattsOilgramBunkerwire",
    PlattsPolymerscan = "PlattsPolymerscan",
    PlattsTSIIronOre = "PlattsTSIIronOre",
    TSIScrap = "TSIScrap",
    TSISteel = "TSISteel",
    PlattsUS = "PlattsUS",
    PlattsUSMarketscan = "PlattsUSMarketscan",
    PPM = "PPM",
    PPMEurope = "PPMEurope",
    PPW = "PPW",
    ReserveBankAustralia = "ReserveBankAustralia",
    ReserveBankNewZealand = "ReserveBankNewZealand",
    Reuters = "Reuters",
    ReutersScreen = "ReutersScreen",
    RIMIntelligenceProducts = "RIMIntelligenceProducts",
    SeaPac = "SeaPac",
    Telerate = "Telerate",
    TelerateScreen = "TelerateScreen",
    UXWEEKLY = "UXWEEKLY",
    WorldCrudeReport = "WorldCrudeReport",
    WorldPulpMonthly = "WorldPulpMonthly",
};
/**
* Identifies European Union Eligible Collateral Assets classification categories based on EMIR Uncleared Margin Rules. Eligible Collateral asset classes for both initial margin (IM) and variation margin (VM) posted and collected between specified entities. Please note: EMIR regulation will detail which eligible collateral assets classes apply to each type of entity pairing (counterparty) and which apply to posting of IM and VM.
*/
export enum EUEMIREligibleCollateralEnum {
    
    /** Denotes Cash in the form of money credited to an account in any currency, or similar claims for the repayment of money, such as money market deposits. */
    EU_EMIRTypeA = "EU_EMIRTypeA",
    /**  Denotes gold in the form of allocated pure gold bullion of recognised good delivery. */
    EU_EMIRTypeB = "EU_EMIRTypeB",
    /**  Denotes debt securities issued by Member States' central governments or central banks. */
    EU_EMIRTypeC = "EU_EMIRTypeC",
    /**  Denotes debt securities issued by Member States' regional governments or local authorities whose exposures are treated as exposures to the central government of that Member State in accordance with Article 115(2) of Regulation (EU) No 575/2013. */
    EU_EMIRTypeD = "EU_EMIRTypeD",
    /**  Denotes debt securities issued by Member States' public sector entities whose exposures are treated as exposures to the central government, regional government or local authority of that Member State in accordance with Article 116(4) of Regulation (EU) No 575/2013. */
    EU_EMIRTypeE = "EU_EMIRTypeE",
    /**  Denotes debt securities issued by Member States' regional governments or local authorities other than those referred to in (TypeD.) */
    EU_EMIRTypeF = "EU_EMIRTypeF",
    /**  Denotes debt securities issued by Member States' public sector entities other than those referred to in (TypeE). */
    EU_EMIRTypeG = "EU_EMIRTypeG",
    /**  Denotes debt securities issued by multilateral development banks listed in Article 117(2) of Regulation (EU) No 575/2013. */
    EU_EMIRTypeH = "EU_EMIRTypeH",
    /**  Denotes debt securities issued by the international organisations listed in Article 118 of Regulation (EU) No 575/2013. */
    EU_EMIRTypeI = "EU_EMIRTypeI",
    /**  Denotes debt securities issued by third countries' governments or central banks. */
    EU_EMIRTypeJ = "EU_EMIRTypeJ",
    /**  Denotes debt securities issued by third countries' regional governments or local authorities that meet the requirements of (TypeD) and (TypeE). */
    EU_EMIRTypeK = "EU_EMIRTypeK",
    /**  Denotes debt securities issued by third countries' regional governments or local authorities other than those referred to in (TypeD) and (TypeE). */
    EU_EMIRTypeL = "EU_EMIRTypeL",
    /**  Denotes debt securities issued by credit institutions or investment firms including bonds referred to in Article 52(4) of Directive 2009/65/EC of the European Parliament and of the Council. */
    EU_EMIRTypeM = "EU_EMIRTypeM",
    /**  Denotes corporate bonds. */
    EU_EMIRTypeN = "EU_EMIRTypeN",
    /**  Denotes the most senior tranche of a securitisation, as defined in Article 4(61) of Regulation (EU) No 575/2013, that is not a re-securitisation as defined in Article 4(63) of that Regulation. */
    EU_EMIRTypeO = "EU_EMIRTypeO",
    /**  Denotes convertible bonds provided that they can be converted only into equities which are included in an index specified pursuant to point (a) of Article 197 (8) of Regulation (EU) No 575/2013. */
    EU_EMIRTypeP = "EU_EMIRTypeP",
    /**  Denotes equities included in an index specified pursuant to point (a) of Article 197(8) of Regulation (EU) No 575/2013. */
    EU_EMIRTypeQ = "EU_EMIRTypeQ",
    /**  Denotes shares or units in undertakings for collective investments in transferable securities (UCITS), provided that the conditions set out in Article 5 of EU Regulation 2016/2251 are met. */
    EU_EMIRTypeR = "EU_EMIRTypeR",
};
/**
* Identifies United Kingdom Eligible Collateral Assets classification categories based on UK Onshored EMIR Uncleared Margin Rules. Eligible Collateral asset classes for both initial margin (IM) and variation margin (VM) posted and collected between specified entities. Please note: UK EMIR regulation will detail which eligible collateral assets classes apply to each type of entity pairing (counterparty) and which apply to posting of IM and VM.
*/
export enum UKEMIREligibleCollateralEnum {
    
    /** Denotes cash in the form of money credited to an account in any currency, or similar claims for the repayment of money, such as money market deposits. */
    UK_EMIRTypeA = "UK_EMIRTypeA",
    /** Denotes gold in the form of allocated pure gold bullion of recognised good delivery. */
    UK_EMIRTypeB = "UK_EMIRTypeB",
    /** Denotes debt securities issued by the central government of the United Kingdom or the Bank of England. */
    UK_EMIRTypeC = "UK_EMIRTypeC",
    /** Denotes debt securities issued by United Kingdom regional governments or local authorities whose exposures are treated as exposures to the central government of the United Kingdom in accordance with Article 115(2) of Regulation (EU) No 575/2013. */
    UK_EMIRTypeD = "UK_EMIRTypeD",
    /** Denotes debt securities issued by United Kingdom public sector entities whose exposures are treated as exposures to the central government, regional government or local authority of the United Kingdom in accordance with Article 116(4) of Regulation (EU) No 575/2013. */
    UK_EMIRTypeE = "UK_EMIRTypeE",
    /** Denotes debt securities issued by United Kingdom regional governments or local authorities other than those referred to in (TypeD). */
    UK_EMIRTypeF = "UK_EMIRTypeF",
    /** Denotes debt securities issued by United Kingdom public sector entities other than those referred to in (TypeE). */
    UK_EMIRTypeG = "UK_EMIRTypeG",
    /** Denotes debt securities issued by multilateral development banks listed in Article 117(2) of Regulation (EU) No 575/2013. */
    UK_EMIRTypeH = "UK_EMIRTypeH",
    /** Denotes debt securities issued by the international organisations listed in Article 118 of Regulation (EU) No 575/2013. */
    UK_EMIRTypeI = "UK_EMIRTypeI",
    /** Denotes debt securities issued by third countries' governments or central banks. */
    UK_EMIRTypeJ = "UK_EMIRTypeJ",
    /** Denotes debt securities issued by third countries' regional governments or local authorities that meet the requirements of (TypeD) and (TypeE). */
    UK_EMIRTypeK = "UK_EMIRTypeK",
    /** Denotes debt securities issued by third countries' regional governments or local authorities other than those referred to in (TypeD) and (TypeE). */
    UK_EMIRTypeL = "UK_EMIRTypeL",
    /** Denotes debt securities issued by credit institutions or investment firms including bonds admitted to the register of regulated covered bonds maintained under Regulation 7(1)(b) of the Regulated Covered Bonds Regulations 2008 (SI 2008/346). */
    UK_EMIRTypeM = "UK_EMIRTypeM",
    /** Denotes corporate bonds. */
    UK_EMIRTypeN = "UK_EMIRTypeN",
    /** Denotes the most senior tranche of a securitisation, as defined in Article 4(61) of Regulation (EU) No 575/2013, that is not a re-securitisation as defined in Article 4(63) of that Regulation. */
    UK_EMIRTypeO = "UK_EMIRTypeO",
    /** Denotes convertible bonds provided that they can be converted only into equities which are included in an index specified pursuant to point (a) of Article 197 (8) of Regulation (EU) No 575/2013. */
    UK_EMIRTypeP = "UK_EMIRTypeP",
    /** Denotes equities included in an index specified pursuant to point (a) of Article 197(8) of Regulation (EU) No 575/2013. */
    UK_EMIRTypeQ = "UK_EMIRTypeQ",
    /** Denotes shares or units in undertakings for UK UCITS, provided that the conditions set out in Article 5 of EU Regulation 2016/2251 (as modified by the Technical Standards (European Market Infrastructure) (EU Exit) (No. 3) Instrument 2019) are met. */
    UK_EMIRTypeR = "UK_EMIRTypeR",
};
/**
* Identifies US Eligible Collateral Assets classification categories based on Uncleared Margin Rules published by the CFTC and the US Prudential Regulator. Note: While the same basic categories exist in the CFTC and US Prudential Regulators margin rules, the precise definitions or application of those rules could differ between the two rules.
*/
export enum USCFTCPREligibleCollateralEnum {
    
    /** Denotes immediately available cash funds denominated in USD, a major currency, a currency of settlement for the uncleared swap. */
    US_CFTC_PRType1 = "US_CFTC_PRType1",
    /** Denotes a security that is issued by, or unconditionally guaranteed as to the timely payment of principal and interest by, the U.S. Department of the Treasury. */
    US_CFTC_PRType2 = "US_CFTC_PRType2",
    /** Denotes a security that is issued by, or unconditionally guaranteed as to the timely payment of principal and interest by, a U.S. government agency (other than the U.S. Department of Treasury) whose obligations are fully guaranteed by the full faith and credit of the United States government. */
    US_CFTC_PRType3 = "US_CFTC_PRType3",
    /** Denotes a security that is issued by, or fully guaranteed as to the payment of principal and interest by, the European Central Bank or a sovereign entity that is assigned no higher than a 20 percent risk weight under the capital rules applicable to swap dealers subject to regulation by a prudential regulator. */
    US_CFTC_PRType4 = "US_CFTC_PRType4",
    /** Denotes a publicly traded debt security issued by, or an asset-backed security fully guaranteed as to the timely payment of principal and interest by, a U.S. Government-sponsored enterprise that is operating with capital support or another form of direct financial assistance received from the U.S. government that enables the repayments of the U.S. Government-sponsored enterprise's eligible securities. */
    US_CFTC_PRType5A = "US_CFTC_PRType5A",
    /** Denotes a publicly traded debt security, but not an asset backed security, that is investment grade and issued by a U.S. Government-sponsored enterprise that is not operating with capital support or another form of direct financial assistance received from the U.S. government. */
    US_CFTC_PRType5B = "US_CFTC_PRType5B",
    /** Denotes a security that is issued by, or fully guaranteed as to the payment of principal and interest by, the Bank for International Settlements, the International Monetary Fund, or a multilateral development bank. */
    US_CFTC_PRType6 = "US_CFTC_PRType6",
    /** Denotes publicly-traded debt, but not an asset backed security, that is investment grade and is not a debt security issued by a  U.S. Government-sponsored enterprise. This category excludes a security issued by a non-bank financial institution supervised by the board of governors of the Federal Reserve System under Title I of the Dodd-Frank Wall Street Reform and Consumer Protection Act. This category also excludes a security issued by any of the following entities, by a company that would be any of the following entities if it were the organized under the laws of the United States or any State, or in either case by an affiliate of such an entity: the party posting the collateral, a bank holding company, a savings and loan holding company, a U.S. intermediate holding company, a foreign bank, a depositary institution, a securities holding company, a broker, a dealer, a futures commission merchant, a swap dealer, or a security-based swap dealer. */
    US_CFTC_PRType7 = "US_CFTC_PRType7",
    /** Denotes a publicly traded common equity security that is included in the Standard & Poor's Composite 500 Index or related indexes. This category excludes a security issued by a non-bank financial institution supervised by the board of governors of the Federal Reserve System under Title I of the Dodd-Frank Wall Street Reform and Consumer Protection Act. This category also excludes a security issued by any of the following entities, by a company that would be any of the following entities if it were the organized under the laws of the United States or any State, or in either case by an affiliate of such an entity: the party posting the collateral, a bank holding company, a savings and loan holding company, a U.S. intermediate holding company, a foreign bank, a depositary institution, a securities holding company, a broker, a dealer, a futures commission merchant, a swap dealer, or a security-based swap dealer. */
    US_CFTC_PRType8A = "US_CFTC_PRType8A",
    /**  Denotes a publicly traded common equity security that is included in the Standard & Poor's Composite 1500 Index or related indexes. This category excludes a security issued by a non-bank financial institution supervised by the board of governors of the Federal Reserve System under Title I of the Dodd-Frank Wall Street Reform and Consumer Protection Act. This category also excludes a security issued by any of the following entities, by a company that would be any of the following entities if it were the organized under the laws of the United States or any State, or in either case by an affiliate of such an entity: the party posting the collateral, a bank holding company, a savings and loan holding company, a U.S. intermediate holding company, a foreign bank, a depositary institution, a securities holding company, a broker, a dealer, a futures commission merchant, a swap dealer, or a security-based swap dealer. */
    US_CFTC_PRType8B = "US_CFTC_PRType8B",
    /** Denotes a publicly traded common equity security that is included in an index that a regulated swap entity's supervisor in a foreign jurisdiction recognizes for purposes of including publicly traded common equity as initial margin under applicable regulatory policy, if held in that foreign jurisdiction. This category excludes a security issued by a non-bank financial institution supervised by the board of governors of the Federal Reserve System under Title I of the Dodd-Frank Wall Street Reform and Consumer Protection Act. This category also excludes a security issued by any of the following entities, by a company that would be any of the following entities if it were the organized under the laws of the United States or any State, or in either case by an affiliate of such an entity: the party posting the collateral, a bank holding company, a savings and loan holding company, a U.S. intermediate holding company, a foreign bank, a depositary institution, a securities holding company, a broker, a dealer, a futures commission merchant, a swap dealer, or a security-based swap dealer. */
    US_CFTC_PRType8C = "US_CFTC_PRType8C",
    /** Denotes securities in the form of redeemable securities in a pooled investment fund representing the security-holder's proportional interest in the fund's net assets and that are issued and redeemed only on the basis of the market value of the fund's net assets prepared each business day after the security-holder makes its investment commitment or redemption request to the fund, if the fund's investments are limited to the following: (A) securities that are issued by, or unconditionally guaranteed as to the timely payment of principal and interest by, the U.S. Department of the Treasury, and immediately-available cash funds denominated in U.S. dollars; or (B) securities denominated in a common currency and issued by, or fully guaranteed as to the payment of principal and interest by, the European Central Bank or a sovereign entity that is assigned no higher than a 20 percent risk weight under the capital rules applicable to swap dealers subject to regulation by a prudential regulator, and immediately-available cash funds denominated in the same currency; and (C) assets of the fund may not be transferred through securities lending, securities borrowing, repurchase agreements, reverse repurchase agreements, or other means that involve the fund having rights to acquire the same or similar assets from the transferee. */
    US_CFTC_PRType9 = "US_CFTC_PRType9",
    /** Denotes Gold. */
    US_CTFC_PRType10 = "US_CTFC_PRType10",
};
/**
* The enumerated values to specify standard currency codes according to the International Standards Organization (ISO). The set of codes in this enumerated list is sourced from ISO Standard 3166 (ISO-3166-1alpha-2)(https://www.iso.org/iso-3166-country-codes.html) as at 14-Aug-23.
*/
export enum ISOCountryCodeEnum {
    
    /** Afghanistan */
    AF = "AF",
    /** Albania */
    AL = "AL",
    /** Algeria */
    DZ = "DZ",
    /** American Samoa */
    AS = "AS",
    /** Andorra */
    AD = "AD",
    /** Angola */
    AO = "AO",
    /** Anguilla */
    AI = "AI",
    /** Antarctica */
    AQ = "AQ",
    /** Antigua and Barbuda */
    AG = "AG",
    /** Argentina */
    AR = "AR",
    /** Armenia */
    AM = "AM",
    /** Aruba */
    AW = "AW",
    /** Australia */
    AU = "AU",
    /** Austria */
    AT = "AT",
    /** Azerbaijan */
    AZ = "AZ",
    /** Bahamas (the) */
    BS = "BS",
    /** Bahrain */
    BH = "BH",
    /** Bangladesh */
    BD = "BD",
    /** Barbados */
    BB = "BB",
    /** Belarus */
    BY = "BY",
    /** Belgium */
    BE = "BE",
    /** Belize */
    BZ = "BZ",
    /** Benin */
    BJ = "BJ",
    /** Bermuda */
    BM = "BM",
    /** Aland Islands */
    AX = "AX",
    /** Bhutan */
    BT = "BT",
    /** Bolivia (Plurinational State of) */
    BO = "BO",
    /** Bonaire, Sint Eustatius and Saba */
    BQ = "BQ",
    /** Bosnia and Herzegovina */
    BA = "BA",
    /** Botswana */
    BW = "BW",
    /** Bouvet Island */
    BV = "BV",
    /** Brazil */
    BR = "BR",
    /** British Indian Ocean Territory (the) */
    IO = "IO",
    /** Brunei Darussalam */
    BN = "BN",
    /** Bulgaria */
    BG = "BG",
    /** Burkina Faso */
    BF = "BF",
    /** Burundi */
    BI = "BI",
    /** Cabo Verde */
    CV = "CV",
    /** Cambodia */
    KH = "KH",
    /** Cameroon */
    CM = "CM",
    /** Canada */
    CA = "CA",
    /** Cayman Islands (the) */
    KY = "KY",
    /** Central African Republic (the) */
    CF = "CF",
    /** Chad */
    TD = "TD",
    /** Chile */
    CL = "CL",
    /** China */
    CN = "CN",
    /** Christmas Island */
    CX = "CX",
    /** Cocos (Keeling) Islands (the) */
    CC = "CC",
    /** Colombia */
    CO = "CO",
    /** Comoros (the) */
    KM = "KM",
    /** Congo (the Democratic Republic of the) */
    CD = "CD",
    /** Congo (the) */
    CG = "CG",
    /** Cook Islands (the) */
    CK = "CK",
    /** Costa Rica */
    CR = "CR",
    /** Croatia */
    HR = "HR",
    /** Cuba */
    CU = "CU",
    /** Curaao */
    CW = "CW",
    /** Cyprus */
    CY = "CY",
    /** Czechia */
    CZ = "CZ",
    /** Cte d'Ivoire */
    CI = "CI",
    /** Denmark */
    DK = "DK",
    /** Djibouti */
    DJ = "DJ",
    /** Dominica */
    DM = "DM",
    /** Dominican Republic (the) */
    DO = "DO",
    /** Ecuador */
    EC = "EC",
    /** Egypt */
    EG = "EG",
    /** El Salvador */
    SV = "SV",
    /** Equatorial Guinea */
    GQ = "GQ",
    /** Eritrea */
    ER = "ER",
    /** Estonia */
    EE = "EE",
    /** Eswatini */
    SZ = "SZ",
    /** Ethiopia */
    ET = "ET",
    /** Falkland Islands (the) [Malvinas] */
    FK = "FK",
    /** Faroe Islands (the) */
    FO = "FO",
    /** Fiji */
    FJ = "FJ",
    /** Finland */
    FI = "FI",
    /** France */
    FR = "FR",
    /** French Guiana */
    GF = "GF",
    /** French Polynesia */
    PF = "PF",
    /** French Southern Territories (the) */
    TF = "TF",
    /** Gabon */
    GA = "GA",
    /** Gambia (the) */
    GM = "GM",
    /** Georgia */
    GE = "GE",
    /** Germany */
    DE = "DE",
    /** Ghana */
    GH = "GH",
    /** Gibraltar */
    GI = "GI",
    /** Greece */
    GR = "GR",
    /** Greenland */
    GL = "GL",
    /** Grenada */
    GD = "GD",
    /** Guadeloupe */
    GP = "GP",
    /** Guam */
    GU = "GU",
    /** Guatemala */
    GT = "GT",
    /** Guernsey */
    GG = "GG",
    /** Guinea */
    GN = "GN",
    /** Guinea-Bissau */
    GW = "GW",
    /** Guyana */
    GY = "GY",
    /** Haiti */
    HT = "HT",
    /** Heard Island and McDonald Islands */
    HM = "HM",
    /** Holy See (the) */
    VA = "VA",
    /** Honduras */
    HN = "HN",
    /** Hong Kong */
    HK = "HK",
    /** Hungary */
    HU = "HU",
    /** Iceland */
    IS = "IS",
    /** India */
    IN = "IN",
    /** Indonesia */
    ID = "ID",
    /** Iran (Islamic Republic of) */
    IR = "IR",
    /** Iraq */
    IQ = "IQ",
    /** Ireland */
    IE = "IE",
    /** Isle of Man */
    IM = "IM",
    /** Israel */
    IL = "IL",
    /** Italy */
    IT = "IT",
    /** Jamaica */
    JM = "JM",
    /** Japan */
    JP = "JP",
    /** Jersey */
    JE = "JE",
    /** Jordan */
    JO = "JO",
    /** Kazakhstan */
    KZ = "KZ",
    /** Kenya */
    KE = "KE",
    /** Kiribati */
    KI = "KI",
    /** Korea (the Democratic People's Republic of) */
    KP = "KP",
    /** Korea (the Republic of) */
    KR = "KR",
    /** Kuwait */
    KW = "KW",
    /** Kyrgyzstan */
    KG = "KG",
    /** Lao People's Democratic Republic (the) */
    LA = "LA",
    /** Latvia */
    LV = "LV",
    /** Lebanon */
    LB = "LB",
    /** Lesotho */
    LS = "LS",
    /** Liberia */
    LR = "LR",
    /** Libya */
    LY = "LY",
    /** Liechtenstein */
    LI = "LI",
    /** Lithuania */
    LT = "LT",
    /** Luxembourg */
    LU = "LU",
    /** Macao */
    MO = "MO",
    /** Madagascar */
    MG = "MG",
    /** Malawi */
    MW = "MW",
    /** Malaysia */
    MY = "MY",
    /** Maldives */
    MV = "MV",
    /** Mali */
    ML = "ML",
    /** Malta */
    MT = "MT",
    /** Marshall Islands (the) */
    MH = "MH",
    /** Martinique */
    MQ = "MQ",
    /** Mauritania */
    MR = "MR",
    /** Mauritius */
    MU = "MU",
    /** Mayotte */
    YT = "YT",
    /** Mexico */
    MX = "MX",
    /** Micronesia (Federated States of) */
    FM = "FM",
    /** Moldova (the Republic of) */
    MD = "MD",
    /** Monaco */
    MC = "MC",
    /** Mongolia */
    MN = "MN",
    /** Montenegro */
    ME = "ME",
    /** Montserrat */
    MS = "MS",
    /** Morocco */
    MA = "MA",
    /** Mozambique */
    MZ = "MZ",
    /** Myanmar */
    MM = "MM",
    /** Namibia */
    NA = "NA",
    /** Nauru */
    NR = "NR",
    /** Nepal */
    NP = "NP",
    /** Netherlands (Kingdom of the) */
    NL = "NL",
    /** New Caledonia */
    NC = "NC",
    /** New Zealand */
    NZ = "NZ",
    /** Nicaragua */
    NI = "NI",
    /** Niger (the) */
    NE = "NE",
    /** Nigeria */
    NG = "NG",
    /** Niue */
    NU = "NU",
    /** Norfolk Island */
    NF = "NF",
    /** North Macedonia */
    MK = "MK",
    /** Northern Mariana Islands (the) */
    MP = "MP",
    /** Norway */
    NO = "NO",
    /** Oman */
    OM = "OM",
    /** Pakistan */
    PK = "PK",
    /** Palau */
    PW = "PW",
    /** Palestine, State of */
    PS = "PS",
    /** Panama */
    PA = "PA",
    /** Papua New Guinea */
    PG = "PG",
    /** Paraguay */
    PY = "PY",
    /** Peru */
    PE = "PE",
    /** Philippines (the) */
    PH = "PH",
    /** Pitcairn */
    PN = "PN",
    /** Poland */
    PL = "PL",
    /** Portugal */
    PT = "PT",
    /** Puerto Rico */
    PR = "PR",
    /** Qatar */
    QA = "QA",
    /** Romania */
    RO = "RO",
    /** Russian Federation (the) */
    RU = "RU",
    /** Rwanda */
    RW = "RW",
    /** Runion */
    RE = "RE",
    /** Saint Barthlemy */
    BL = "BL",
    /** Saint Helena, Ascension and Tristan da Cunha */
    SH = "SH",
    /** Saint Kitts and Nevis */
    KN = "KN",
    /** Saint Lucia */
    LC = "LC",
    /** Saint Martin (French part) */
    MF = "MF",
    /** Saint Pierre and Miquelon */
    PM = "PM",
    /** Saint Vincent and the Grenadines */
    VC = "VC",
    /** Samoa */
    WS = "WS",
    /** San Marino */
    SM = "SM",
    /** Sao Tome and Principe */
    ST = "ST",
    /** Saudi Arabia */
    SA = "SA",
    /** Senegal */
    SN = "SN",
    /** Serbia */
    RS = "RS",
    /** Seychelles */
    SC = "SC",
    /** Sierra Leone */
    SL = "SL",
    /** Singapore */
    SG = "SG",
    /** Sint Maarten (Dutch part) */
    SX = "SX",
    /** Slovakia */
    SK = "SK",
    /** Slovenia */
    SI = "SI",
    /** Solomon Islands */
    SB = "SB",
    /** Somalia */
    SO = "SO",
    /** South Africa */
    ZA = "ZA",
    /** South Georgia and the South Sandwich Islands */
    GS = "GS",
    /** South Sudan */
    SS = "SS",
    /** Spain */
    ES = "ES",
    /** Sri Lanka */
    LK = "LK",
    /** Sudan (the) */
    SD = "SD",
    /** Suriname */
    SR = "SR",
    /** Svalbard and Jan Mayen */
    SJ = "SJ",
    /** Sweden */
    SE = "SE",
    /** Switzerland */
    CH = "CH",
    /** Syrian Arab Republic (the) */
    SY = "SY",
    /** Taiwan (Province of China) */
    TW = "TW",
    /** Tajikistan */
    TJ = "TJ",
    /** Tanzania, the United Republic of */
    TZ = "TZ",
    /** Thailand */
    TH = "TH",
    /** Timor-Leste */
    TL = "TL",
    /** Togo */
    TG = "TG",
    /** Tokelau */
    TK = "TK",
    /** Tonga */
    TO = "TO",
    /** Trinidad and Tobago */
    TT = "TT",
    /** Tunisia */
    TN = "TN",
    /** Turkmenistan */
    TM = "TM",
    /** Turks and Caicos Islands (the) */
    TC = "TC",
    /** Tuvalu */
    TV = "TV",
    /** Trkiye */
    TR = "TR",
    /** Uganda */
    UG = "UG",
    /** Ukraine */
    UA = "UA",
    /** United Arab Emirates (the) */
    AE = "AE",
    /** United Kingdom of Great Britain and Northern Ireland (the) */
    GB = "GB",
    /** United States Minor Outlying Islands (the) */
    UM = "UM",
    /** United States of America (the) */
    US = "US",
    /** Uruguay */
    UY = "UY",
    /** Uzbekistan */
    UZ = "UZ",
    /** Vanuatu */
    VU = "VU",
    /** Venezuela (Bolivarian Republic of) */
    VE = "VE",
    /** Viet Nam */
    VN = "VN",
    /** Virgin Islands (British) */
    VG = "VG",
    /** Virgin Islands (U.S.) */
    VI = "VI",
    /** Wallis and Futuna */
    WF = "WF",
    /** Western Sahara* */
    EH = "EH",
    /** Yemen */
    YE = "YE",
    /** Zambia */
    ZM = "ZM",
    /** Zimbabwe */
    ZW = "ZW",
};
/**
* The enumerated values used in both the obligations and deliverable obligations of the credit default swap to represent a class or type of securities which apply.
*/
export enum ObligationCategoryEnum {
    
    /** ISDA term 'Payment'. */
    Payment = "Payment",
    /** ISDA term 'Borrowed Money'. */
    BorrowedMoney = "BorrowedMoney",
    /** ISDA term 'Reference Obligations Only'. */
    ReferenceObligationsOnly = "ReferenceObligationsOnly",
    /** ISDA term 'Bond'. */
    Bond = "Bond",
    /** ISDA term 'Loan'. */
    Loan = "Loan",
    /** ISDA term 'Bond or Loan'. */
    BondOrLoan = "BondOrLoan",
};
/**
* The enumerated values to specify the list of floating rate index.
*/
export enum FloatingRateIndexEnum {
    
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AED_EBOR_Reuters = "AED_EBOR_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    AED_EIBOR = "AED_EIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_AONIA = "AUD_AONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    AUD_AONIA_OIS_Compound_1 = "AUD_AONIA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_AONIA_OIS_COMPOUND = "AUD_AONIA_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_AONIA_OIS_COMPOUND_SwapMarker = "AUD_AONIA_OIS_COMPOUND_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_BBR_AUBBSW = "AUD_BBR_AUBBSW",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_BBR_BBSW = "AUD_BBR_BBSW",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_BBR_BBSW_Bloomberg = "AUD_BBR_BBSW_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_BBR_BBSY__BID_ = "AUD_BBR_BBSY__BID_",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_BBR_ISDC = "AUD_BBR_ISDC",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    AUD_BBSW = "AUD_BBSW",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    AUD_BBSW_Quarterly_Swap_Rate_ICAP = "AUD_BBSW_Quarterly_Swap_Rate_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    AUD_BBSW_Semi_Annual_Swap_Rate_ICAP = "AUD_BBSW_Semi_Annual_Swap_Rate_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    AUD_BBSY_Bid = "AUD_BBSY_Bid",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_LIBOR_BBA = "AUD_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_LIBOR_BBA_Bloomberg = "AUD_LIBOR_BBA_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_LIBOR_Reference_Banks = "AUD_LIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Quarterly_Swap_Rate_ICAP = "AUD_Quarterly_Swap_Rate_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Quarterly_Swap_Rate_ICAP_Reference_Banks = "AUD_Quarterly_Swap_Rate_ICAP_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "AUD_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Semi_Annual_Swap_Rate_BGCANTOR_Reference_Banks = "AUD_Semi_Annual_Swap_Rate_BGCANTOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Semi_annual_Swap_Rate_ICAP = "AUD_Semi_annual_Swap_Rate_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Semi_Annual_Swap_Rate_ICAP_Reference_Banks = "AUD_Semi_Annual_Swap_Rate_ICAP_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    AUD_Swap_Rate_Reuters = "AUD_Swap_Rate_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    BRL_CDI = "BRL_CDI",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_BA_CDOR = "CAD_BA_CDOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_BA_CDOR_Bloomberg = "CAD_BA_CDOR_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_BA_ISDD = "CAD_BA_ISDD",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_BA_Reference_Banks = "CAD_BA_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_BA_Reuters = "CAD_BA_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_BA_Telerate = "CAD_BA_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CAD_CDOR = "CAD_CDOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_CORRA = "CAD_CORRA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_CORRA_CanDeal_TMX_Term = "CAD_CORRA_CanDeal_TMX_Term",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_CORRA_Compounded_Index = "CAD_CORRA_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_CORRA_OIS_Compound_1 = "CAD_CORRA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_CORRA_OIS_COMPOUND = "CAD_CORRA_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_ISDA_Swap_Rate = "CAD_ISDA_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_LIBOR_BBA = "CAD_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_LIBOR_BBA_Bloomberg = "CAD_LIBOR_BBA_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_LIBOR_BBA_SwapMarker = "CAD_LIBOR_BBA_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_LIBOR_Reference_Banks = "CAD_LIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_REPO_CORRA = "CAD_REPO_CORRA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_TBILL_ISDD = "CAD_TBILL_ISDD",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_TBILL_Reference_Banks = "CAD_TBILL_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_TBILL_Reuters = "CAD_TBILL_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CAD_TBILL_Telerate = "CAD_TBILL_Telerate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_3M_LIBOR_SWAP_CME_vs_LCH_ICAP = "CHF_3M_LIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_3M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg = "CHF_3M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_3M_LIBOR_SWAP_EUREX_vs_LCH_ICAP = "CHF_3M_LIBOR_SWAP_EUREX_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_3M_LIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg = "CHF_3M_LIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_6M_LIBOR_SWAP_CME_vs_LCH_ICAP = "CHF_6M_LIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_6M_LIBORSWAP_CME_vs_LCH_ICAP_Bloomberg = "CHF_6M_LIBORSWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP = "CHF_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg = "CHF_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_Annual_Swap_Rate = "CHF_Annual_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_Annual_Swap_Rate_11_00_ICAP = "CHF_Annual_Swap_Rate_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_Annual_Swap_Rate_Reference_Banks = "CHF_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_Basis_Swap_3m_vs_6m_LIBOR_11_00_ICAP = "CHF_Basis_Swap_3m_vs_6m_LIBOR_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_ISDAFIX_Swap_Rate = "CHF_ISDAFIX_Swap_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_LIBOR = "CHF_LIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_LIBOR_BBA = "CHF_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_LIBOR_BBA_Bloomberg = "CHF_LIBOR_BBA_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_LIBOR_ISDA = "CHF_LIBOR_ISDA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_LIBOR_Reference_Banks = "CHF_LIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_OIS_11_00_ICAP = "CHF_OIS_11_00_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_SARON = "CHF_SARON",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_12M = "CHF_SARON_Average_12M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_1M = "CHF_SARON_Average_1M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_1W = "CHF_SARON_Average_1W",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_2M = "CHF_SARON_Average_2M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_3M = "CHF_SARON_Average_3M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_6M = "CHF_SARON_Average_6M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Average_9M = "CHF_SARON_Average_9M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_Compounded_Index = "CHF_SARON_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CHF_SARON_OIS_Compound_1 = "CHF_SARON_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_SARON_OIS_COMPOUND = "CHF_SARON_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_TOIS_OIS_COMPOUND = "CHF_TOIS_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CHF_USD_Basis_Swaps_11_00_ICAP = "CHF_USD_Basis_Swaps_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CL_CLICP_Bloomberg = "CL_CLICP_Bloomberg",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CLP_ICP = "CLP_ICP",
    /** Refers to the Indice Camara Promedio ('ICP') rate for Chilean Pesos which, for a Reset Date, is determined and published by the Asociacion de Bancos e Instituciones Financieras de Chile A.G. ('ABIF') in accordance with the 'Reglamento Indice de Camara Promedio' of the ABIF as published in the Diario Oficial de la Republica de Chile (the 'ICP Rules') and which is reported on the ABIF website by not later than 10:00 a.m., Santiago time, on that Reset Date. */
    CLP_TNA = "CLP_TNA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNH_HIBOR = "CNH_HIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNH_HIBOR_Reference_Banks = "CNH_HIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNH_HIBOR_TMA = "CNH_HIBOR_TMA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_7_Repo_Compounding_Date = "CNY_7_Repo_Compounding_Date",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_CNREPOFIX_CFXS_Reuters = "CNY_CNREPOFIX_CFXS_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNY_Deposit_Rate = "CNY_Deposit_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNY_Fixing_Repo_Rate = "CNY_Fixing_Repo_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNY_LPR = "CNY_LPR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_PBOCB_Reuters = "CNY_PBOCB_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_Quarterly_7_day_Repo_Non_Deliverable_Swap_Rate_TRADITION = "CNY_Quarterly_7_day_Repo_Non_Deliverable_Swap_Rate_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_Quarterly_7_day_Repo_Non_Deliverable_Swap_Rate_TRADITION_Reference_Banks = "CNY_Quarterly_7_day_Repo_Non_Deliverable_Swap_Rate_TRADITION_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNY_Quarterly_7D_Repo_NDS_Rate_Tradition = "CNY_Quarterly_7D_Repo_NDS_Rate_Tradition",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "CNY_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_Semi_Annual_Swap_Rate_Reference_Banks = "CNY_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNY_SHIBOR = "CNY_SHIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CNY_SHIBOR_OIS_Compound = "CNY_SHIBOR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CNY_Shibor_OIS_Compounding = "CNY_Shibor_OIS_Compounding",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction.. */
    CNY_SHIBOR_Reuters = "CNY_SHIBOR_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    COP_IBR_OIS_Compound_1 = "COP_IBR_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    COP_IBR_OIS_COMPOUND = "COP_IBR_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CZK_Annual_Swap_Rate_11_00_BGCANTOR = "CZK_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CZK_Annual_Swap_Rate_Reference_Banks = "CZK_Annual_Swap_Rate_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CZK_CZEONIA = "CZK_CZEONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CZK_CZEONIA_OIS_Compound = "CZK_CZEONIA_OIS_Compound",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    CZK_PRIBOR = "CZK_PRIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CZK_PRIBOR_PRBO = "CZK_PRIBOR_PRBO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    CZK_PRIBOR_Reference_Banks = "CZK_PRIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR = "DKK_CIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR2 = "DKK_CIBOR2",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR2_Bloomberg = "DKK_CIBOR2_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR2_DKNA13 = "DKK_CIBOR2_DKNA13",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR_DKNA13 = "DKK_CIBOR_DKNA13",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR_DKNA13_Bloomberg = "DKK_CIBOR_DKNA13_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_CIBOR_Reference_Banks = "DKK_CIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_CITA = "DKK_CITA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_CITA_DKNA14_COMPOUND = "DKK_CITA_DKNA14_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_DESTR = "DKK_DESTR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_DESTR_Compounded_Index = "DKK_DESTR_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_DESTR_OIS_Compound = "DKK_DESTR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    DKK_DKKOIS_OIS_COMPOUND = "DKK_DKKOIS_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    DKK_Tom_Next_OIS_Compound = "DKK_Tom_Next_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_3M_EURIBOR_SWAP_CME_vs_LCH_ICAP = "EUR_3M_EURIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_3M_EURIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg = "EUR_3M_EURIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_3M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP = "EUR_3M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_3M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg = "EUR_3M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_6M_EURIBOR_SWAP_CME_vs_LCH_ICAP = "EUR_6M_EURIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_6M_EURIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg = "EUR_6M_EURIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_6M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP = "EUR_6M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_6M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg = "EUR_6M_EURIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_10_00 = "EUR_Annual_Swap_Rate_10_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_10_00_BGCANTOR = "EUR_Annual_Swap_Rate_10_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_10_00_Bloomberg = "EUR_Annual_Swap_Rate_10_00_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_10_00_ICAP = "EUR_Annual_Swap_Rate_10_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_10_00_SwapMarker = "EUR_Annual_Swap_Rate_10_00_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_10_00_TRADITION = "EUR_Annual_Swap_Rate_10_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_11_00 = "EUR_Annual_Swap_Rate_11_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_11_00_Bloomberg = "EUR_Annual_Swap_Rate_11_00_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_11_00_ICAP = "EUR_Annual_Swap_Rate_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_11_00_SwapMarker = "EUR_Annual_Swap_Rate_11_00_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_3_Month = "EUR_Annual_Swap_Rate_3_Month",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_3_Month_SwapMarker = "EUR_Annual_Swap_Rate_3_Month_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_4_15_TRADITION = "EUR_Annual_Swap_Rate_4_15_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Annual_Swap_Rate_Reference_Banks = "EUR_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_Basis_Swap_EONIA_vs_3m_EUR_IBOR_Swap_Rates_A_360_10_00_ICAP = "EUR_Basis_Swap_EONIA_vs_3m_EUR_IBOR_Swap_Rates_A_360_10_00_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_CNO_TEC10 = "EUR_CNO_TEC10",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EONIA = "EUR_EONIA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_AVERAGE_1 = "EUR_EONIA_AVERAGE_1",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_Average = "EUR_EONIA_Average",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_10_00_BGCANTOR = "EUR_EONIA_OIS_10_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_10_00_ICAP = "EUR_EONIA_OIS_10_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_10_00_TRADITION = "EUR_EONIA_OIS_10_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_11_00_ICAP = "EUR_EONIA_OIS_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_4_15_TRADITION = "EUR_EONIA_OIS_4_15_TRADITION",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_Compound_1 = "EUR_EONIA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_COMPOUND = "EUR_EONIA_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_OIS_COMPOUND_Bloomberg = "EUR_EONIA_OIS_COMPOUND_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EONIA_Swap_Index = "EUR_EONIA_Swap_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR = "EUR_EURIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Act_365 = "EUR_EURIBOR_Act_365",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Act_365_Bloomberg = "EUR_EURIBOR_Act_365_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Annual_Bond_Swap_vs_1m_11_00_ICAP = "EUR_EURIBOR_Annual_Bond_Swap_vs_1m_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Basis_Swap_1m_vs_3m_Euribor_11_00_ICAP = "EUR_EURIBOR_Basis_Swap_1m_vs_3m_Euribor_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Basis_Swap_3m_vs_6m_11_00_ICAP = "EUR_EURIBOR_Basis_Swap_3m_vs_6m_11_00_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_ICE_Swap_Rate_11_00 = "EUR_EURIBOR_ICE_Swap_Rate_11_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_ICE_Swap_Rate_12_00 = "EUR_EURIBOR_ICE_Swap_Rate_12_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Reference_Banks = "EUR_EURIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Reuters = "EUR_EURIBOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURIBOR_Telerate = "EUR_EURIBOR_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EURONIA_OIS_Compound_1 = "EUR_EURONIA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EURONIA_OIS_COMPOUND = "EUR_EURONIA_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR = "EUR_EuroSTR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Average_12M = "EUR_EuroSTR_Average_12M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Average_1M = "EUR_EuroSTR_Average_1M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Average_1W = "EUR_EuroSTR_Average_1W",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Average_3M = "EUR_EuroSTR_Average_3M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Average_6M = "EUR_EuroSTR_Average_6M",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_COMPOUND = "EUR_EuroSTR_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Compounded_Index = "EUR_EuroSTR_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_FTSE_Term = "EUR_EuroSTR_FTSE_Term",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Compounded_Index = "EUR_EuroSTR_ICE_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Compounded_Index_0_Floor = "EUR_EuroSTR_ICE_Compounded_Index_0_Floor",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Compounded_Index_0_Floor_2D_Lag = "EUR_EuroSTR_ICE_Compounded_Index_0_Floor_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Compounded_Index_0_Floor_5D_Lag = "EUR_EuroSTR_ICE_Compounded_Index_0_Floor_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Compounded_Index_2D_Lag = "EUR_EuroSTR_ICE_Compounded_Index_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Compounded_Index_5D_Lag = "EUR_EuroSTR_ICE_Compounded_Index_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_ICE_Swap_Rate = "EUR_EuroSTR_ICE_Swap_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_OIS_Compound = "EUR_EuroSTR_OIS_Compound",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_EuroSTR_Term = "EUR_EuroSTR_Term",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_ISDA_EURIBOR_Swap_Rate_11_00 = "EUR_ISDA_EURIBOR_Swap_Rate_11_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_ISDA_EURIBOR_Swap_Rate_12_00 = "EUR_ISDA_EURIBOR_Swap_Rate_12_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_ISDA_LIBOR_Swap_Rate_10_00 = "EUR_ISDA_LIBOR_Swap_Rate_10_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_ISDA_LIBOR_Swap_Rate_11_00 = "EUR_ISDA_LIBOR_Swap_Rate_11_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    EUR_LIBOR = "EUR_LIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_LIBOR_BBA = "EUR_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_LIBOR_BBA_Bloomberg = "EUR_LIBOR_BBA_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_LIBOR_Reference_Banks = "EUR_LIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TAM_CDC = "EUR_TAM_CDC",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TEC10_CNO = "EUR_TEC10_CNO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TEC10_CNO_SwapMarker = "EUR_TEC10_CNO_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TEC10_Reference_Banks = "EUR_TEC10_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TEC5_CNO = "EUR_TEC5_CNO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TEC5_CNO_SwapMarker = "EUR_TEC5_CNO_SwapMarker",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TEC5_Reference_Banks = "EUR_TEC5_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_TMM_CDC_COMPOUND = "EUR_TMM_CDC_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    EUR_USD_Basis_Swaps_11_00_ICAP = "EUR_USD_Basis_Swaps_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_6M_LIBOR_SWAP_CME_vs_LCH_ICAP = "GBP_6M_LIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_6M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg = "GBP_6M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP = "GBP_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg = "GBP_6M_LIBOR_SWAP_EUREX_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_ISDA_Swap_Rate = "GBP_ISDA_Swap_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_LIBOR = "GBP_LIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_LIBOR_BBA = "GBP_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_LIBOR_BBA_Bloomberg = "GBP_LIBOR_BBA_Bloomberg",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_LIBOR_ICE_Swap_Rate = "GBP_LIBOR_ICE_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_LIBOR_ISDA = "GBP_LIBOR_ISDA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_LIBOR_Reference_Banks = "GBP_LIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_RONIA = "GBP_RONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_RONIA_OIS_Compound = "GBP_RONIA_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_Semi_Annual_Swap_Rate = "GBP_Semi_Annual_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_Semi_Annual_Swap_Rate_11_00_ICAP = "GBP_Semi_Annual_Swap_Rate_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_Semi_Annual_Swap_Rate_11_00_TRADITION = "GBP_Semi_Annual_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_Semi_Annual_Swap_Rate_4_15_TRADITION = "GBP_Semi_Annual_Swap_Rate_4_15_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_Semi_Annual_Swap_Rate_Reference_Banks = "GBP_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_Semi_Annual_Swap_Rate_SwapMarker26 = "GBP_Semi_Annual_Swap_Rate_SwapMarker26",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA = "GBP_SONIA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_COMPOUND = "GBP_SONIA_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_Compounded_Index = "GBP_SONIA_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_FTSE_Term = "GBP_SONIA_FTSE_Term",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Compounded_Index = "GBP_SONIA_ICE_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Compounded_Index_0_Floor = "GBP_SONIA_ICE_Compounded_Index_0_Floor",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Compounded_Index_0_Floor_2D_Lag = "GBP_SONIA_ICE_Compounded_Index_0_Floor_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Compounded_Index_0_Floor_5D_Lag = "GBP_SONIA_ICE_Compounded_Index_0_Floor_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Compounded_Index_2D_Lag = "GBP_SONIA_ICE_Compounded_Index_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Compounded_Index_5D_Lag = "GBP_SONIA_ICE_Compounded_Index_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Swap_Rate = "GBP_SONIA_ICE_Swap_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_ICE_Term = "GBP_SONIA_ICE_Term",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_OIS_11_00_ICAP = "GBP_SONIA_OIS_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_OIS_11_00_TRADITION = "GBP_SONIA_OIS_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_OIS_4_15_TRADITION = "GBP_SONIA_OIS_4_15_TRADITION",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_OIS_Compound = "GBP_SONIA_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_SONIA_Swap_Rate = "GBP_SONIA_Swap_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    GBP_UK_Base_Rate = "GBP_UK_Base_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_USD_Basis_Swaps_11_00_ICAP = "GBP_USD_Basis_Swaps_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_WMBA_RONIA_COMPOUND = "GBP_WMBA_RONIA_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GBP_WMBA_SONIA_COMPOUND = "GBP_WMBA_SONIA_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GRD_ATHIBOR_ATHIBOR = "GRD_ATHIBOR_ATHIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GRD_ATHIBOR_Reference_Banks = "GRD_ATHIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GRD_ATHIBOR_Telerate = "GRD_ATHIBOR_Telerate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GRD_ATHIMID_Reference_Banks = "GRD_ATHIMID_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    GRD_ATHIMID_Reuters = "GRD_ATHIMID_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR = "HKD_HIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR_HIBOR_ = "HKD_HIBOR_HIBOR_",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR_HIBOR_Bloomberg = "HKD_HIBOR_HIBOR_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR_HKAB = "HKD_HIBOR_HKAB",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR_HKAB_Bloomberg = "HKD_HIBOR_HKAB_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR_ISDC = "HKD_HIBOR_ISDC",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HIBOR_Reference_Banks = "HKD_HIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HONIA = "HKD_HONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    HKD_HONIA_OIS_Compound = "HKD_HONIA_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_HONIX_OIS_COMPOUND = "HKD_HONIX_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_ISDA_Swap_Rate_11_00 = "HKD_ISDA_Swap_Rate_11_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_ISDA_Swap_Rate_4_00 = "HKD_ISDA_Swap_Rate_4_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Annual_Swap_Rate_11_00_BGCANTOR = "HKD_Quarterly_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Annual_Swap_Rate_11_00_TRADITION = "HKD_Quarterly_Annual_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Annual_Swap_Rate_4_00_BGCANTOR = "HKD_Quarterly_Annual_Swap_Rate_4_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Annual_Swap_Rate_Reference_Banks = "HKD_Quarterly_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Quarterly_Swap_Rate_11_00_ICAP = "HKD_Quarterly_Quarterly_Swap_Rate_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Quarterly_Swap_Rate_4_00_ICAP = "HKD_Quarterly_Quarterly_Swap_Rate_4_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HKD_Quarterly_Quarterly_Swap_Rate_Reference_Banks = "HKD_Quarterly_Quarterly_Swap_Rate_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    HUF_BUBOR = "HUF_BUBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HUF_BUBOR_Reference_Banks = "HUF_BUBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    HUF_BUBOR_Reuters = "HUF_BUBOR_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    HUF_HUFONIA = "HUF_HUFONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    HUF_HUFONIA_OIS_Compound = "HUF_HUFONIA_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_IDMA_Bloomberg = "IDR_IDMA_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_IDRFIX = "IDR_IDRFIX",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    IDR_INDONIA = "IDR_INDONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    IDR_INDONIA_OIS_Compound = "IDR_INDONIA_OIS_Compound",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    IDR_JIBOR = "IDR_JIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_JIBOR_Reuters = "IDR_JIBOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_SBI_Reuters = "IDR_SBI_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "IDR_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_Semi_Annual_Swap_Rate_Non_deliverable_16_00_Tullett_Prebon = "IDR_Semi_Annual_Swap_Rate_Non_deliverable_16_00_Tullett_Prebon",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_Semi_Annual_Swap_Rate_Reference_Banks = "IDR_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_SOR_Reference_Banks = "IDR_SOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_SOR_Reuters = "IDR_SOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    IDR_SOR_Telerate = "IDR_SOR_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ILS_SHIR = "ILS_SHIR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ILS_SHIR_OIS_Compound = "ILS_SHIR_OIS_Compound",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ILS_TELBOR = "ILS_TELBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ILS_TELBOR01_Reuters = "ILS_TELBOR01_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ILS_TELBOR_Reference_Banks = "ILS_TELBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_BMK = "INR_BMK",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_CMT = "INR_CMT",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_FBIL_MIBOR_OIS_COMPOUND = "INR_FBIL_MIBOR_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_INBMK_REUTERS = "INR_INBMK_REUTERS",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_MIBOR = "INR_MIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    INR_MIBOR_OIS = "INR_MIBOR_OIS",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    INR_MIBOR_OIS_Compound_1 = "INR_MIBOR_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_MIBOR_OIS_COMPOUND = "INR_MIBOR_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_MIFOR = "INR_MIFOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_MIOIS = "INR_MIOIS",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_MITOR_OIS_COMPOUND = "INR_MITOR_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_Modified_MIFOR = "INR_Modified_MIFOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_Reference_Banks = "INR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_Semi_Annual_Swap_Rate_11_30_BGCANTOR = "INR_Semi_Annual_Swap_Rate_11_30_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_Semi_Annual_Swap_Rate_Non_deliverable_16_00_Tullett_Prebon = "INR_Semi_Annual_Swap_Rate_Non_deliverable_16_00_Tullett_Prebon",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_Semi_Annual_Swap_Rate_Reference_Banks = "INR_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_SORR = "INR_SORR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    INR_SORR_OIS_Compound = "INR_SORR_OIS_Compound",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ISK_REIBOR = "ISK_REIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ISK_REIBOR_Reference_Banks = "ISK_REIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ISK_REIBOR_Reuters = "ISK_REIBOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_Annual_Swap_Rate_11_00_TRADITION = "JPY_Annual_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_Annual_Swap_Rate_3_00_TRADITION = "JPY_Annual_Swap_Rate_3_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_BBSF_Bloomberg_10_00 = "JPY_BBSF_Bloomberg_10_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_BBSF_Bloomberg_15_00 = "JPY_BBSF_Bloomberg_15_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_Euroyen_TIBOR = "JPY_Euroyen_TIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_ISDA_Swap_Rate_10_00 = "JPY_ISDA_Swap_Rate_10_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_ISDA_Swap_Rate_15_00 = "JPY_ISDA_Swap_Rate_15_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR = "JPY_LIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_BBA = "JPY_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_BBA_Bloomberg = "JPY_LIBOR_BBA_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_FRASETT = "JPY_LIBOR_FRASETT",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_ISDA = "JPY_LIBOR_ISDA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_Reference_Banks = "JPY_LIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_TSR_10_00 = "JPY_LIBOR_TSR_10_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_LIBOR_TSR_15_00 = "JPY_LIBOR_TSR_15_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_LTPR_MHBK = "JPY_LTPR_MHBK",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LTPR_MHCB = "JPY_LTPR_MHCB",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_LTPR_TBC = "JPY_LTPR_TBC",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_MUTANCALL_TONAR = "JPY_MUTANCALL_TONAR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_OIS_11_00_ICAP = "JPY_OIS_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_OIS_11_00_TRADITION = "JPY_OIS_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_OIS_3_00_TRADITION = "JPY_OIS_3_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_Quoting_Banks_LIBOR = "JPY_Quoting_Banks_LIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_STPR_Quoting_Banks = "JPY_STPR_Quoting_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR = "JPY_TIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_17096 = "JPY_TIBOR_17096",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_17097 = "JPY_TIBOR_17097",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_DTIBOR01 = "JPY_TIBOR_DTIBOR01",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_TIBM = "JPY_TIBOR_TIBM",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_TIBM__10_Banks_ = "JPY_TIBOR_TIBM__10_Banks_",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_TIBM__5_Banks_ = "JPY_TIBOR_TIBM__5_Banks_",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_TIBM__All_Banks_ = "JPY_TIBOR_TIBM__All_Banks_",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_TIBM__All_Banks__Bloomberg = "JPY_TIBOR_TIBM__All_Banks__Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_TIBM_Reference_Banks = "JPY_TIBOR_TIBM_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TIBOR_ZTIBOR = "JPY_TIBOR_ZTIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA = "JPY_TONA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_Average_180D = "JPY_TONA_Average_180D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_Average_30D = "JPY_TONA_Average_30D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_Average_90D = "JPY_TONA_Average_90D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_Compounded_Index = "JPY_TONA_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_ICE_Compounded_Index = "JPY_TONA_ICE_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_ICE_Compounded_Index_0_Floor = "JPY_TONA_ICE_Compounded_Index_0_Floor",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_ICE_Compounded_Index_0_Floor_2D_Lag = "JPY_TONA_ICE_Compounded_Index_0_Floor_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_ICE_Compounded_Index_0_Floor_5D_Lag = "JPY_TONA_ICE_Compounded_Index_0_Floor_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_ICE_Compounded_Index_2D_Lag = "JPY_TONA_ICE_Compounded_Index_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_ICE_Compounded_Index_5D_Lag = "JPY_TONA_ICE_Compounded_Index_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    JPY_TONA_OIS_Compound_1 = "JPY_TONA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_OIS_COMPOUND = "JPY_TONA_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_TSR_10_00 = "JPY_TONA_TSR_10_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TONA_TSR_15_00 = "JPY_TONA_TSR_15_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TORF_QUICK = "JPY_TORF_QUICK",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TSR_Reference_Banks = "JPY_TSR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TSR_Reuters_10_00 = "JPY_TSR_Reuters_10_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TSR_Reuters_15_00 = "JPY_TSR_Reuters_15_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TSR_Telerate_10_00 = "JPY_TSR_Telerate_10_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_TSR_Telerate_15_00 = "JPY_TSR_Telerate_15_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    JPY_USD_Basis_Swaps_11_00_ICAP = "JPY_USD_Basis_Swaps_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    KRW_Bond_3222 = "KRW_Bond_3222",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    KRW_CD_3220 = "KRW_CD_3220",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    KRW_CD_91D = "KRW_CD_91D",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    KRW_CD_KSDA_Bloomberg = "KRW_CD_KSDA_Bloomberg",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    KRW_KOFR = "KRW_KOFR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    KRW_KOFR_OIS_Compound = "KRW_KOFR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    KRW_Quarterly_Annual_Swap_Rate_3_30_ICAP = "KRW_Quarterly_Annual_Swap_Rate_3_30_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    MXN_TIIE = "MXN_TIIE",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MXN_TIIE_Banxico = "MXN_TIIE_Banxico",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MXN_TIIE_Banxico_Bloomberg = "MXN_TIIE_Banxico_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MXN_TIIE_Banxico_Reference_Banks = "MXN_TIIE_Banxico_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    MXN_TIIE_ON = "MXN_TIIE_ON",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    MXN_TIIE_ON_OIS_Compound = "MXN_TIIE_ON_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MXN_TIIE_Reference_Banks = "MXN_TIIE_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    MYR_KLIBOR = "MYR_KLIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MYR_KLIBOR_BNM = "MYR_KLIBOR_BNM",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MYR_KLIBOR_Reference_Banks = "MYR_KLIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    MYR_MYOR = "MYR_MYOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    MYR_MYOR_OIS_Compound = "MYR_MYOR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MYR_Quarterly_Swap_Rate_11_00_TRADITION = "MYR_Quarterly_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    MYR_Quarterly_Swap_Rate_TRADITION_Reference_Banks = "MYR_Quarterly_Swap_Rate_TRADITION_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    NOK_NIBOR = "NOK_NIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NOK_NIBOR_NIBR = "NOK_NIBOR_NIBR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NOK_NIBOR_NIBR_Bloomberg = "NOK_NIBOR_NIBR_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NOK_NIBOR_NIBR_Reference_Banks = "NOK_NIBOR_NIBR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NOK_NIBOR_OIBOR = "NOK_NIBOR_OIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NOK_NIBOR_Reference_Banks = "NOK_NIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NOK_NOWA = "NOK_NOWA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    NOK_NOWA_OIS_Compound = "NOK_NOWA_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_BBR_BID = "NZD_BBR_BID",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_BBR_FRA = "NZD_BBR_FRA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_BBR_ISDC = "NZD_BBR_ISDC",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_BBR_Reference_Banks = "NZD_BBR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_BBR_Telerate = "NZD_BBR_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    NZD_BKBM_Bid = "NZD_BKBM_Bid",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    NZD_BKBM_FRA = "NZD_BKBM_FRA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    NZD_BKBM_FRA_Swap_Rate_ICAP = "NZD_BKBM_FRA_Swap_Rate_ICAP",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. NOTE: In accordance with Section 2.1.11(ii) (New Zealand Business Days), from the date on which the New Zealand Financial Markets Association's 'New Zealand Business Day Guidance' (proposed effective date of October 6, 2025) becomes effective, the reference to a 'Wellington and Auckland Business Day' will be deemed to be replaced with a reference to a 'New Zealand Business Day' for all Transactions entered into from (and including) that effective date. */
    NZD_NZIONA = "NZD_NZIONA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. NOTE: In accordance with Section 2.1.11(ii) (New Zealand Business Days), from the date on which the New Zealand Financial Markets Association's 'New Zealand Business Day Guidance' (proposed effective date of October 6, 2025) becomes effective, the reference to a 'Wellington and Auckland Business Day' will be deemed to be replaced with a reference to a 'New Zealand Business Day' for all Transactions entered into from (and including) that effective date. */
    NZD_NZIONA_OIS_Compound_1 = "NZD_NZIONA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_NZIONA_OIS_COMPOUND = "NZD_NZIONA_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "NZD_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_Semi_Annual_Swap_Rate_BGCANTOR_Reference_Banks = "NZD_Semi_Annual_Swap_Rate_BGCANTOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_Swap_Rate_ICAP = "NZD_Swap_Rate_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    NZD_Swap_Rate_ICAP_Reference_Banks = "NZD_Swap_Rate_ICAP_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PHP_ORR = "PHP_ORR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PHP_PHIREF = "PHP_PHIREF",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PHP_PHIREF_BAP = "PHP_PHIREF_BAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PHP_PHIREF_Bloomberg = "PHP_PHIREF_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PHP_PHIREF_Reference_Banks = "PHP_PHIREF_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PHP_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "PHP_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PHP_Semi_Annual_Swap_Rate_Reference_Banks = "PHP_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_POLONIA = "PLN_POLONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_POLONIA_OIS_Compound_1 = "PLN_POLONIA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PLN_POLONIA_OIS_COMPOUND = "PLN_POLONIA_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_POLSTR = "PLN_POLSTR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_POLSTR_OIS_Compound = "PLN_POLSTR_OIS_Compound",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_WIBID = "PLN_WIBID",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_WIBOR = "PLN_WIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PLN_WIBOR_Reference_Banks = "PLN_WIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PLN_WIBOR_WIBO = "PLN_WIBOR_WIBO",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_WIRON = "PLN_WIRON",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    PLN_WIRON_OIS_Compound = "PLN_WIRON_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PLZ_WIBOR_Reference_Banks = "PLZ_WIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    PLZ_WIBOR_WIBO = "PLZ_WIBOR_WIBO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    REPOFUNDS_RATE_FRANCE_OIS_COMPOUND = "REPOFUNDS_RATE_FRANCE_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    REPOFUNDS_RATE_GERMANY_OIS_COMPOUND = "REPOFUNDS_RATE_GERMANY_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    REPOFUNDS_RATE_ITALY_OIS_COMPOUND = "REPOFUNDS_RATE_ITALY_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RON_Annual_Swap_Rate_11_00_BGCANTOR = "RON_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RON_Annual_Swap_Rate_Reference_Banks = "RON_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RON_RBOR_Reuters = "RON_RBOR_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    RON_ROBID = "RON_ROBID",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    RON_ROBOR = "RON_ROBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_Annual_Swap_Rate_11_00_BGCANTOR = "RUB_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_Annual_Swap_Rate_12_45_TRADITION = "RUB_Annual_Swap_Rate_12_45_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_Annual_Swap_Rate_4_15_TRADITION = "RUB_Annual_Swap_Rate_4_15_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_Annual_Swap_Rate_Reference_Banks = "RUB_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_Annual_Swap_Rate_TRADITION_Reference_Banks = "RUB_Annual_Swap_Rate_TRADITION_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    RUB_Key_Rate_CBRF = "RUB_Key_Rate_CBRF",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    RUB_MosPrime = "RUB_MosPrime",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_MOSPRIME_NFEA = "RUB_MOSPRIME_NFEA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_MOSPRIME_Reference_Banks = "RUB_MOSPRIME_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    RUB_RUONIA = "RUB_RUONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    RUB_RUONIA_OIS_Compound_1 = "RUB_RUONIA_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    RUB_RUONIA_OIS_COMPOUND = "RUB_RUONIA_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    SAR_SAIBOR = "SAR_SAIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SAR_SRIOR_Reference_Banks = "SAR_SRIOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SAR_SRIOR_SUAA = "SAR_SRIOR_SUAA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_Annual_Swap_Rate = "SEK_Annual_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_Annual_Swap_Rate_SESWFI = "SEK_Annual_Swap_Rate_SESWFI",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SIOR_OIS_COMPOUND = "SEK_SIOR_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    SEK_STIBOR = "SEK_STIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_STIBOR_Bloomberg = "SEK_STIBOR_Bloomberg",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    SEK_STIBOR_OIS_Compound = "SEK_STIBOR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_STIBOR_Reference_Banks = "SEK_STIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_STIBOR_SIDE = "SEK_STIBOR_SIDE",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR = "SEK_SWESTR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_Average_1M = "SEK_SWESTR_Average_1M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_Average_1W = "SEK_SWESTR_Average_1W",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_Average_2M = "SEK_SWESTR_Average_2M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_Average_3M = "SEK_SWESTR_Average_3M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_Average_6M = "SEK_SWESTR_Average_6M",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_Compounded_Index = "SEK_SWESTR_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SEK_SWESTR_OIS_Compound = "SEK_SWESTR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Currency_Basis_Swap_Rate_11_00_Tullett_Prebon = "SGD_Semi_Annual_Currency_Basis_Swap_Rate_11_00_Tullett_Prebon",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Currency_Basis_Swap_Rate_16_00_Tullett_Prebon = "SGD_Semi_Annual_Currency_Basis_Swap_Rate_16_00_Tullett_Prebon",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "SGD_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_11_00_Tullett_Prebon = "SGD_Semi_Annual_Swap_Rate_11_00_Tullett_Prebon",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_11_00_TRADITION = "SGD_Semi_Annual_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_16_00_Tullett_Prebon = "SGD_Semi_Annual_Swap_Rate_16_00_Tullett_Prebon",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_ICAP = "SGD_Semi_Annual_Swap_Rate_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_ICAP_Reference_Banks = "SGD_Semi_Annual_Swap_Rate_ICAP_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_Reference_Banks = "SGD_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_Semi_Annual_Swap_Rate_TRADITION_Reference_Banks = "SGD_Semi_Annual_Swap_Rate_TRADITION_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    SGD_SIBOR = "SGD_SIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SIBOR_Reference_Banks = "SGD_SIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SIBOR_Reuters = "SGD_SIBOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SIBOR_Telerate = "SGD_SIBOR_Telerate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SONAR_OIS_COMPOUND = "SGD_SONAR_OIS_COMPOUND",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SONAR_OIS_VWAP_COMPOUND = "SGD_SONAR_OIS_VWAP_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    SGD_SOR = "SGD_SOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SORA = "SGD_SORA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SORA_COMPOUND = "SGD_SORA_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    SGD_SORA_OIS_Compound = "SGD_SORA_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SOR_Reference_Banks = "SGD_SOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SOR_Reuters = "SGD_SOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SOR_Telerate = "SGD_SOR_Telerate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SOR_VWAP = "SGD_SOR_VWAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SGD_SOR_VWAP_Reference_Banks = "SGD_SOR_VWAP_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SKK_BRIBOR_Bloomberg = "SKK_BRIBOR_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SKK_BRIBOR_BRBO = "SKK_BRIBOR_BRBO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SKK_BRIBOR_NBSK07 = "SKK_BRIBOR_NBSK07",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    SKK_BRIBOR_Reference_Banks = "SKK_BRIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "THB_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_Semi_Annual_Swap_Rate_Reference_Banks = "THB_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_SOR_Reference_Banks = "THB_SOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_SOR_Reuters = "THB_SOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_SOR_Telerate = "THB_SOR_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    THB_THBFIX = "THB_THBFIX",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_THBFIX_Reference_Banks = "THB_THBFIX_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_THBFIX_Reuters = "THB_THBFIX_Reuters",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_THOR = "THB_THOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    THB_THOR_COMPOUND = "THB_THOR_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    THB_THOR_OIS_Compound = "THB_THOR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_Annual_Swap_Rate_11_00_TRADITION = "TRY_Annual_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_Annual_Swap_Rate_11_15_BGCANTOR = "TRY_Annual_Swap_Rate_11_15_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_Annual_Swap_Rate_Reference_Banks = "TRY_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_Semi_Annual_Swap_Rate_TRADITION_Reference_Banks = "TRY_Semi_Annual_Swap_Rate_TRADITION_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    TRY_TLREF = "TRY_TLREF",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    TRY_TLREF_OIS_Compound_1 = "TRY_TLREF_OIS_Compound_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_TLREF_OIS_COMPOUND = "TRY_TLREF_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    TRY_TRLIBOR = "TRY_TRLIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_TRYIBOR_Reference_Banks = "TRY_TRYIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TRY_TRYIBOR_Reuters = "TRY_TRYIBOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_Quarterly_Annual_Swap_Rate_11_00_BGCANTOR = "TWD_Quarterly_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_Quarterly_Annual_Swap_Rate_Reference_Banks = "TWD_Quarterly_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_Reference_Dealers = "TWD_Reference_Dealers",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_Reuters_6165 = "TWD_Reuters_6165",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_TAIBIR01 = "TWD_TAIBIR01",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_TAIBIR02 = "TWD_TAIBIR02",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    TWD_TAIBOR = "TWD_TAIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_TAIBOR_Bloomberg = "TWD_TAIBOR_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_TAIBOR_Reuters = "TWD_TAIBOR_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_Telerate_6165 = "TWD_Telerate_6165",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    TWD_TWCPBA = "TWD_TWCPBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    UK_Base_Rate = "UK_Base_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_3M_LIBOR_SWAP_CME_vs_LCH_ICAP = "USD_3M_LIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_3M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg = "USD_3M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_6M_LIBOR_SWAP_CME_vs_LCH_ICAP = "USD_6M_LIBOR_SWAP_CME_vs_LCH_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_6M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg = "USD_6M_LIBOR_SWAP_CME_vs_LCH_ICAP_Bloomberg",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_AMERIBOR = "USD_AMERIBOR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_AMERIBOR_Average_30D = "USD_AMERIBOR_Average_30D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_AMERIBOR_Average_90D = "USD_AMERIBOR_Average_90D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_AMERIBOR_Term = "USD_AMERIBOR_Term",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_AMERIBOR_Term_Structure = "USD_AMERIBOR_Term_Structure",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Annual_Swap_Rate_11_00_BGCANTOR = "USD_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Annual_Swap_Rate_11_00_TRADITION = "USD_Annual_Swap_Rate_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Annual_Swap_Rate_4_00_TRADITION = "USD_Annual_Swap_Rate_4_00_TRADITION",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_AXI_Term = "USD_AXI_Term",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_BA_H_15 = "USD_BA_H_15",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_BA_Reference_Dealers = "USD_BA_Reference_Dealers",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_BMA_Municipal_Swap_Index = "USD_BMA_Municipal_Swap_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_BSBY = "USD_BSBY",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CD_H_15 = "USD_CD_H_15",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CD_Reference_Dealers = "USD_CD_Reference_Dealers",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CMS_Reference_Banks = "USD_CMS_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CMS_Reference_Banks_ICAP_SwapPX = "USD_CMS_Reference_Banks_ICAP_SwapPX",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CMS_Reuters = "USD_CMS_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CMS_Telerate = "USD_CMS_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_CMT = "USD_CMT",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_CMT_Average_1W = "USD_CMT_Average_1W",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CMT_T7051 = "USD_CMT_T7051",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CMT_T7052 = "USD_CMT_T7052",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_COF11_FHLBSF = "USD_COF11_FHLBSF",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_COF11_Reuters = "USD_COF11_Reuters",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_COF11_Telerate = "USD_COF11_Telerate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_COFI = "USD_COFI",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CP_H_15 = "USD_CP_H_15",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_CP_Money_Market_Yield = "USD_CP_Money_Market_Yield",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CP_Reference_Dealers = "USD_CP_Reference_Dealers",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_CRITR = "USD_CRITR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_Federal_Funds = "USD_Federal_Funds",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Federal_Funds_H_15 = "USD_Federal_Funds_H_15",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Federal_Funds_H_15_Bloomberg = "USD_Federal_Funds_H_15_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Federal_Funds_H_15_OIS_COMPOUND = "USD_Federal_Funds_H_15_OIS_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_Federal_Funds_OIS_Compound = "USD_Federal_Funds_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Federal_Funds_Reference_Dealers = "USD_Federal_Funds_Reference_Dealers",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_FFCB_DISCO = "USD_FFCB_DISCO",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_FXI_Term = "USD_FXI_Term",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_ISDAFIX3_Swap_Rate = "USD_ISDAFIX3_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_ISDAFIX3_Swap_Rate_3_00 = "USD_ISDAFIX3_Swap_Rate_3_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_ISDA_Swap_Rate = "USD_ISDA_Swap_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_ISDA_Swap_Rate_3_00 = "USD_ISDA_Swap_Rate_3_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_LIBOR = "USD_LIBOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_BBA = "USD_LIBOR_BBA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_BBA_Bloomberg = "USD_LIBOR_BBA_Bloomberg",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_ICE_Swap_Rate_11_00 = "USD_LIBOR_ICE_Swap_Rate_11_00",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_ICE_Swap_Rate_15_00 = "USD_LIBOR_ICE_Swap_Rate_15_00",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_ISDA = "USD_LIBOR_ISDA",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_LIBO = "USD_LIBOR_LIBO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_LIBOR_Reference_Banks = "USD_LIBOR_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_Municipal_Swap_Index = "USD_Municipal_Swap_Index",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Municipal_Swap_Libor_Ratio_11_00_ICAP = "USD_Municipal_Swap_Libor_Ratio_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Municipal_Swap_Rate_11_00_ICAP = "USD_Municipal_Swap_Rate_11_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_11_00_BGCANTOR = "USD_OIS_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_11_00_LON_ICAP = "USD_OIS_11_00_LON_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_11_00_NY_ICAP = "USD_OIS_11_00_NY_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_11_00_TRADITION = "USD_OIS_11_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_3_00_BGCANTOR = "USD_OIS_3_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_3_00_NY_ICAP = "USD_OIS_3_00_NY_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_OIS_4_00_TRADITION = "USD_OIS_4_00_TRADITION",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Overnight_Bank_Funding_Rate = "USD_Overnight_Bank_Funding_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_Prime = "USD_Prime",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Prime_H_15 = "USD_Prime_H_15",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Prime_Reference_Banks = "USD_Prime_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_S_P_Index_High_Grade = "USD_S_P_Index_High_Grade",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_SandP_Index_High_Grade = "USD_SandP_Index_High_Grade",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SIBOR_Reference_Banks = "USD_SIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SIBOR_SIBO = "USD_SIBOR_SIBO",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SIFMA_Municipal_Swap_Index = "USD_SIFMA_Municipal_Swap_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR = "USD_SOFR",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_Average_180D = "USD_SOFR_Average_180D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_Average_30D = "USD_SOFR_Average_30D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_Average_90D = "USD_SOFR_Average_90D",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_CME_Term = "USD_SOFR_CME_Term",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_COMPOUND = "USD_SOFR_COMPOUND",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_Compounded_Index = "USD_SOFR_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Compounded_Index = "USD_SOFR_ICE_Compounded_Index",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Compounded_Index_0_Floor = "USD_SOFR_ICE_Compounded_Index_0_Floor",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Compounded_Index_0_Floor_2D_Lag = "USD_SOFR_ICE_Compounded_Index_0_Floor_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Compounded_Index_0_Floor_5D_Lag = "USD_SOFR_ICE_Compounded_Index_0_Floor_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Compounded_Index_2D_Lag = "USD_SOFR_ICE_Compounded_Index_2D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Compounded_Index_5D_Lag = "USD_SOFR_ICE_Compounded_Index_5D_Lag",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix and 2006 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Swap_Rate = "USD_SOFR_ICE_Swap_Rate",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Swap_Rate_Spreads = "USD_SOFR_ICE_Swap_Rate_Spreads",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_SOFR_ICE_Term = "USD_SOFR_ICE_Term",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_SOFR_OIS_Compound = "USD_SOFR_OIS_Compound",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Swap_Rate_BCMP1 = "USD_Swap_Rate_BCMP1",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_TBILL_Auction_High_Rate = "USD_TBILL_Auction_High_Rate",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_TBILL_H_15 = "USD_TBILL_H_15",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_TBILL_H_15_Bloomberg = "USD_TBILL_H_15_Bloomberg",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_TBILL_Secondary_Market = "USD_TBILL_Secondary_Market",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    USD_TBILL_Secondary_Market_Bond_Equivalent_Yield = "USD_TBILL_Secondary_Market_Bond_Equivalent_Yield",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_TIBOR_ISDC = "USD_TIBOR_ISDC",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_TIBOR_Reference_Banks = "USD_TIBOR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_19901_3_00_ICAP = "USD_Treasury_19901_3_00_ICAP",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_Rate_BCMP1 = "USD_Treasury_Rate_BCMP1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_Rate_ICAP_BrokerTec = "USD_Treasury_Rate_ICAP_BrokerTec",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_Rate_SwapMarker100 = "USD_Treasury_Rate_SwapMarker100",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_Rate_SwapMarker99 = "USD_Treasury_Rate_SwapMarker99",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_Rate_T19901 = "USD_Treasury_Rate_T19901",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    USD_Treasury_Rate_T500 = "USD_Treasury_Rate_T500",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    VND_Semi_Annual_Swap_Rate_11_00_BGCANTOR = "VND_Semi_Annual_Swap_Rate_11_00_BGCANTOR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    VND_Semi_Annual_Swap_Rate_Reference_Banks = "VND_Semi_Annual_Swap_Rate_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_DEPOSIT_Reference_Banks = "ZAR_DEPOSIT_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_DEPOSIT_SAFEX = "ZAR_DEPOSIT_SAFEX",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ZAR_JIBAR = "ZAR_JIBAR",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_JIBAR_Reference_Banks = "ZAR_JIBAR_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_JIBAR_SAFEX = "ZAR_JIBAR_SAFEX",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ZAR_Prime_Average_1 = "ZAR_Prime_Average_1",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_PRIME_AVERAGE = "ZAR_PRIME_AVERAGE",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_PRIME_AVERAGE_Reference_Banks = "ZAR_PRIME_AVERAGE_Reference_Banks",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_Quarterly_Swap_Rate_1_00_TRADITION = "ZAR_Quarterly_Swap_Rate_1_00_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_Quarterly_Swap_Rate_5_30_TRADITION = "ZAR_Quarterly_Swap_Rate_5_30_TRADITION",
    /** Per 2006 ISDA Definitions or Annex to the 2000 ISDA Definitions, Section 7.1 Rate Options, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ZAR_Quarterly_Swap_Rate_TRADITION_Reference_Banks = "ZAR_Quarterly_Swap_Rate_TRADITION_Reference_Banks",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ZAR_ZARONIA = "ZAR_ZARONIA",
    /** Per 2021 ISDA Interest Rate Derivatives Definitions Floating Rate Matrix, as amended through the date on which parties enter into the relevant transaction. */
    ZAR_ZARONIA_OIS_Compound = "ZAR_ZARONIA_OIS_Compound",
};
/**
* The enumerated values to specify the list of inflation rate indices.
*/
export enum InflationRateIndexEnum {
    
    /** Australia: AUD - Non-revised Consumer Price Index (CPI) */
    AUD_CPI = "AUD_CPI",
    /** Austria: AUS - Non-revised Consumer Price Index (CPI) */
    AUS_CPI = "AUS_CPI",
    /** Austria: AUS - Non-revised Harmonised Indices of Consumer Prices (HICP) */
    AUS_HICP = "AUS_HICP",
    /** Belgium: BLG - Non-revised Consumer Price Index - General Index (CPI) */
    BLG_CPI_GI = "BLG_CPI_GI",
    /** Belgium: BLG - Non-revised Consumer Price Index - Health Index (CPI) */
    BLG_CPI_HI = "BLG_CPI_HI",
    /** Belgium: BLG - Non-revised Harmonised Consumer Price Index (HICP) */
    BLG_HICP = "BLG_HICP",
    /** Brazil: BRL - Non-revised Price Index (IGP-M) */
    BRL_IGPM = "BRL_IGPM",
    /** Brazil: BRL - Non-revised Consumer Price Index (IPCA) */
    BRL_IPCA = "BRL_IPCA",
    /** Canada: CAD - Non-revised Consumer Price Index (CPI) */
    CAD_CPI = "CAD_CPI",
    /** Chile: CLP - Non-revised Consumer Price Index (CPI) */
    CLP_CPI = "CLP_CPI",
    /** China: CNY - Non-revised Consumer Price Index (CPI) */
    CNY_CPI = "CNY_CPI",
    /** Czech Republic: CZK - Non-revised Consumer Price Index (CPI) */
    CZK_CPI = "CZK_CPI",
    /** Denmark: DEK - Non-revised Consumer Price Index (CPI) */
    DEK_CPI = "DEK_CPI",
    /** Denmark: DEK - Non-revised Harmonised Consumer Price Index (HICP) */
    DEK_HICP = "DEK_HICP",
    /** Germany: DEM - Non-revised Consumer Price Index (CPI) */
    DEM_CPI = "DEM_CPI",
    /** Germany: DEM - Non-revised Consumer Price Index for North Rhine-Westphalia */
    DEM_CPI_NRW = "DEM_CPI_NRW",
    /** Germany: DEM - Non-revised Harmonised Consumer Price Index (HICP) */
    DEM_HICP = "DEM_HICP",
    /** Spain: ESP - National-Non-revised Consumer Price Index (CPI) */
    ESP_CPI = "ESP_CPI",
    /** Spain: ESP - Harmonised-Non-revised Consumer Price Index (HICP) */
    ESP_HICP = "ESP_HICP",
    /** Spain: ESP - National-Revised Consumer Price Index (CPI). */
    ESP_R_CPI = "ESP_R_CPI",
    /** Spain: ESP - Harmonised-Revised Consumer Price Index (HICP) */
    ESP_R_HICP = "ESP_R_HICP",
    /** European Union: EUR - All Items-Non-revised Consumer Price Index */
    EUR_AI_CPI = "EUR_AI_CPI",
    /** European Union: EUR - All Items-Revised Consumer Price Index */
    EUR_AI_R_CPI = "EUR_AI_R_CPI",
    /** European Union: EUR - Excluding Tobacco-Non-revised Consumer Price Index */
    EUR_EXT_CPI = "EUR_EXT_CPI",
    /** European Union: EUR - Excluding Tobacco-Revised Consumer Price Index */
    EUR_EXT_R_CPI = "EUR_EXT_R_CPI",
    /** Finland: FIN - Non-revised Consumer Price Index (CPI) */
    FIN_CPI = "FIN_CPI",
    /** Finland: FIN - Harmonised-Non-revised Consumer Price Index (HICP) */
    FIN_HICP = "FIN_HICP",
    /** France: FRC - Excluding Tobacco-Non-Revised Consumer Price Index */
    FRC_EXT_CPI = "FRC_EXT_CPI",
    /** France: FRC - Harmonised-Non-revised Consumer Price Index (HICP) */
    FRC_HICP = "FRC_HICP",
    /** Greece: GRD - Non-revised Consumer Price Index (CPI) */
    GRD_CPI = "GRD_CPI",
    /** Greece: GRD - Harmonised-Non-revised Consumer Price Index (HICP) */
    GRD_HICP = "GRD_HICP",
    /** Hong Kong: HKD - Non-revised Consumer Price Index (CPI) */
    HKD_CPI = "HKD_CPI",
    /** Hungary: HUF - Non-revised Consumer Price Index (CPI) */
    HUF_CPI = "HUF_CPI",
    /** Indonesia: IDR - Non-revised Consumer Price Index (CPI) */
    IDR_CPI = "IDR_CPI",
    /** Israel: ILS - Non-revised Consumer Price Index (CPI) */
    ILS_CPI = "ILS_CPI",
    /** Ireland: IRL - Non-revised Consumer Price Index (CPI) */
    IRL_CPI = "IRL_CPI",
    /** Ireland: IRL - Harmonised-Non-revised Consumer Price Index (HICP) */
    IRL_HICP = "IRL_HICP",
    /** Iceland: ISK - Non-revised Consumer Price Index (CPI) */
    ISK_CPI = "ISK_CPI",
    /** Iceland: ISK - Harmonised Consumer Price Index (HICP) */
    ISK_HICP = "ISK_HICP",
    /** Italy: ITL - Inflation for Blue Collar Workers and Employees-Excluding Tobacco Consumer Price Index */
    ITL_BC_EXT_CPI = "ITL_BC_EXT_CPI",
    /** Italy: ITL - Inflation for Blue Collar Workers and Employees-Including Tobacco Consumer Price Index */
    ITL_BC_INT_CPI = "ITL_BC_INT_CPI",
    /** Italy: ITL - Non-revised Harmonised Consumer Price Index (HICP) */
    ITL_HICP = "ITL_HICP",
    /** Italy: ITL - Whole Community - Excluding Tobacco Consumer Price Index */
    ITL_WC_EXT_CPI = "ITL_WC_EXT_CPI",
    /** Italy: ITL - Whole Community - Including Tobacco Consumer Price Index */
    ITL_WC_INT_CPI = "ITL_WC_INT_CPI",
    /** Japan: JPY - Non-revised Consumer Price Index Nationwide General Excluding Fresh Food (CPI) */
    JPY_CPI_EXF = "JPY_CPI_EXF",
    /** South Korea: KRW - Non-revised Consumer Price Index (CPI) */
    KRW_CPI = "KRW_CPI",
    /** Luxembourg: LUX - Non-revised Consumer Price Index (CPI) */
    LUX_CPI = "LUX_CPI",
    /** Luxembourg: LUX - Harmonised-Non-revised Consumer Price Index (HICP) */
    LUX_HICP = "LUX_HICP",
    /** Mexico: MXN - Non-revised Consumer Price Index (CPI) */
    MXN_CPI = "MXN_CPI",
    /** Mexico: MXN - Unidad de Inversion Index (UDI) */
    MXN_UDI = "MXN_UDI",
    /** Malaysia: MYR - Non-revised Consumer Price Index (CPI) */
    MYR_CPI = "MYR_CPI",
    /** Netherlands: NLG - Non-revised Consumer Price Index (CPI) */
    NLG_CPI = "NLG_CPI",
    /** Netherlands: NLG - Harmonised-Non-revised Consumer Price Index (HICP) */
    NLG_HICP = "NLG_HICP",
    /** Norway: NOK - Non-revised Consumer Price Index (CPI) */
    NOK_CPI = "NOK_CPI",
    /** New Zealand: NZD - Non-revised Consumer Price Index (CPI) */
    NZD_CPI = "NZD_CPI",
    /** Peru: PER - Non-revised Consumer Price Index (CPI) */
    PER_CPI = "PER_CPI",
    /** Poland: PLN - Non-Revised Consumer Price Index (CPI) */
    PLN_CPI = "PLN_CPI",
    /** Portugal: POR - Non-revised Consumer Price Index (CPI) */
    POR_CPI = "POR_CPI",
    /** Portugal: POR - Harmonised-Non-revised Consumer Price Index (HICP) */
    POR_HICP = "POR_HICP",
    /** Russia: RUB - Non-revised Consumer Price Index (CPI) */
    RUB_CPI = "RUB_CPI",
    /** Sweden: SEK - Non-revised Consumer Price Index (CPI) */
    SEK_CPI = "SEK_CPI",
    /** Singapore: SGD - Non-revised Consumer Price Index (CPI) */
    SGD_CPI = "SGD_CPI",
    /** Switzerland: SWF - Non-revised Consumer Price Index (CPI) */
    SWF_CPI = "SWF_CPI",
    /** Turkey: TRY - Non-revised Consumer Price Index (CPI) */
    TRY_CPI = "TRY_CPI",
    /** Taiwan: TWD - Non-revised Consumer Price Index (CPI) */
    TWD_CPI = "TWD_CPI",
    /** United Kingdom: GBP - Non-revised Consumer Prices Index including Housing (UKCPIH) */
    UK_CPIH = "UK_CPIH",
    /** United Kingdom: GBP - Harmonised-Non-revised Consumer Price Index (HICP) */
    UK_HICP = "UK_HICP",
    /** United Kingdom: GBP - Non-revised Retail Price Index (UKRPI) */
    UK_RPI = "UK_RPI",
    /** United Kingdom: GBP - Non-revised Retail Price Index Excluding Mortgage Interest Payments (UKRPIX) */
    UK_RPIX = "UK_RPIX",
    /** United States: USA - Non-revised Consumer Price Index - Urban (CPI-U) */
    USA_CPI_U = "USA_CPI_U",
    /** South Africa: ZAR - Non-revised Consumer Price Index (CPI) */
    ZAR_CPI = "ZAR_CPI",
    /** South Africa: ZAR - Non-revised Consumer Price Index Excluding Mortgages (CPIX) */
    ZAR_CPIX = "ZAR_CPIX",
};
/**
* Defines the enumerated values to specify the nature of a location identifier.
*/
export enum CommodityLocationIdentifierTypeEnum {
    
    /** The physical or virtual point at which the commodity will be delivered. */
    DeliveryPoint = "DeliveryPoint",
    /** The physical or virtual point at which the commodity enters a transportation system. */
    EntryPoint = "EntryPoint",
    /** Identification of the border(s) or border point(s) of a transportation contract. */
    InterconnectionPoint = "InterconnectionPoint",
    /** The physical or virtual point at which the commodity is withdrawn from a transportation system. */
    WithdrawalPoint = "WithdrawalPoint",
    /** The zone covering potential delivery points for the commodity */
    DeliveryZone = "DeliveryZone",
    /** The hub code of the buyer. */
    BuyerHub = "BuyerHub",
    /** The hub code of the seller. */
    SellerHub = "SellerHub",
};
/**
* Defines the enumerated values to specify the nature of a trade identifier.
*/
export enum TradeIdentifierTypeEnum {
    
    UniqueTransactionIdentifier = "UniqueTransactionIdentifier",
    UniqueSwapIdentifier = "UniqueSwapIdentifier",
};
/**
* Defines the enumerated values to specify the two counterparties to the transaction.
*/
export enum CounterpartyRoleEnum {
    
    Party1 = "Party1",
    Party2 = "Party2",
};
/**
* The enumerated values to specify the reference entity types corresponding to a list of types defined in the ISDA First to Default documentation.
*/
export enum EntityTypeEnum {
    
    /** Entity Type of Asian. */
    Asian = "Asian",
    /** Entity Type of Australian and New Zealand. */
    AustralianAndNewZealand = "AustralianAndNewZealand",
    /** Entity Type of European Emerging Markets. */
    EuropeanEmergingMarkets = "EuropeanEmergingMarkets",
    /** Entity Type of Japanese. */
    Japanese = "Japanese",
    /** Entity Type of North American High Yield. */
    NorthAmericanHighYield = "NorthAmericanHighYield",
    /** Entity Type of North American Insurance. */
    NorthAmericanInsurance = "NorthAmericanInsurance",
    /** Entity Type of North American Investment Grade. */
    NorthAmericanInvestmentGrade = "NorthAmericanInvestmentGrade",
    /** Entity Type of Singaporean. */
    Singaporean = "Singaporean",
    /** Entity Type of Western European. */
    WesternEuropean = "WesternEuropean",
    /** Entity Type of Western European Insurance. */
    WesternEuropeanInsurance = "WesternEuropeanInsurance",
};
/**
* The enumerated values to specify an interest rate stream payer or receiver party.
*/
export enum PayerReceiverEnum {
    
    /** The party identified as the stream payer. */
    Payer = "Payer",
    /** The party identified as the stream receiver. */
    Receiver = "Receiver",
};
/**
* The enumeration values to qualify the type of account.
*/
export enum AccountTypeEnum {
    
    /** Aggregate client account, as defined under ESMA MiFIR. */
    AggregateClient = "AggregateClient",
    /** The account contains trading activity or positions that belong to a client of the firm that opened the account. */
    Client = "Client",
    /** The account contains proprietary trading activity or positions, belonging to the firm that is the owner of the account. */
    House = "House",
};
/**
* The enumerated values for the natural person's role.
*/
export enum NaturalPersonRoleEnum {
    
    /** The person who arranged with a client to execute the trade. */
    Broker = "Broker",
    /** Acquirer of the legal title to the financial instrument. */
    Buyer = "Buyer",
    /** The party or person with legal responsibility for authorization of the execution of the transaction. */
    DecisionMaker = "DecisionMaker",
    /** Person within the firm who is responsible for execution of the transaction. */
    ExecutionWithinFirm = "ExecutionWithinFirm",
    /** Person who is responsible for making the investment decision. */
    InvestmentDecisionMaker = "InvestmentDecisionMaker",
    /** Seller of the legal title to the financial instrument. */
    Seller = "Seller",
    /** The person who executed the trade. */
    Trader = "Trader",
};
/**
* The enumeration values associated with person identifier sources.
*/
export enum PersonIdentifierTypeEnum {
    
    /** Alien Registration Number, number assigned by a social security agency to identify a non-resident person. */
    ARNU = "ARNU",
    /** Passport Number, number assigned by an authority to identify the passport number of a person. */
    CCPT = "CCPT",
    /** Customer Identification Number, number assigned by an issuer to identify a customer. */
    CUST = "CUST",
    /** Drivers License Number, number assigned by an authority to identify a driver's license. */
    DRLC = "DRLC",
    /** Employee Identification Number, number assigned by a registration authority to an employee. */
    EMPL = "EMPL",
    /** National Identity Number, number assigned by an authority to identify the national identity number of a person.. */
    NIDN = "NIDN",
    /** Social Security Number, number assigned by an authority to identify the social security number of a person. */
    SOSE = "SOSE",
    /** Tax Identification Number, number assigned by a tax authority to identify a person. */
    TXID = "TXID",
    /** Natural Person Identifier. To identify the person who is acting as private individual, not as business entity. Used for regulatory reporting. */
    NPID = "NPID",
    /** Privacy Law Identifier. It refers to the DMO Letter No. 17-16, http://www.cftc.gov/idc/groups/public/@lrlettergeneral/documents/letter/17-16.pdf */
    PLID = "PLID",
};
/**
* The enumeration values associated with legal entity identifier sources.
*/
export enum EntityIdentifierTypeEnum {
    
    /** The Reference Entity Database identifier. */
    REDID = "REDID",
    /** The ISO 3166 Country Code. */
    CountryCode = "CountryCode",
    /** Used when the identifier type is not otherwise in this enumerated list because it is internal or other reasons.  The source can be identified in the scheme which is part of the identifier attribute. */
    Other = "Other",
};
/**
* The enumeration values associated with party identifier sources.
*/
export enum PartyIdentifierTypeEnum {
    
    /** The Bank Identifier Code. */
    BIC = "BIC",
    /** The ISO 17442:2012 Legal Entity Identifier. */
    LEI = "LEI",
    /** The ISO 10383 Market Identifier Code (MIC). */
    MIC = "MIC",
};
/**
* The enumerated values for the party role. The enumerated values go beyond the FpML partyRoleScheme as they also include elements that are part of the FpML Trade, such as the Barrier Determination Agent and the Hedging Party.
*/
export enum PartyRoleEnum {
    
    /** Organization responsible for preparing the accounting for the trade. */
    Accountant = "Accountant",
    /** An agent who lends securities of its principals under stock lending arrangements. */
    AgentLender = "AgentLender",
    /** The organization responsible for supplying the allocations for a trade to be allocated to multiple accounts/organizations. */
    AllocationAgent = "AllocationAgent",
    /** The organization that arranged the trade, i.e. brought together the counterparties.  Synonyms/Alternatives: Inter-dealer broker, agent. */
    ArrangingBroker = "ArrangingBroker",
    /** The party specified in the related confirmation as Barrier Determination Agent. */
    BarrierDeterminationAgent = "BarrierDeterminationAgent",
    /** The party that lends out securities under stock lending arrangements via an Agent Lender. */
    BeneficialOwner = "BeneficialOwner",
    /** Organization that suffers the economic benefit of the trade.  The beneficiary may be distinct from the principal/counterparty - an example occurs when a hedge fund trades via a prime broker; in this case the principal is the prime broker, but the beneficiary is the hedge fund.  This can be represented as a payer/receiver account in the name of the hedge fund, but it is also possible to add the party role of 'Beneficiary' at the partyTradeInformation level. */
    Beneficiary = "Beneficiary",
    /** The entity for which the organization supporting the trade's processing has booked/recorded the trade. This is used in non-reporting workflows situations in which the trade doesn\'t need to be reported but a firm still wants to specify their own side. */
    BookingParty = "BookingParty",
    /** The party that borrows securities under stock lending arrangements. */
    Borrower = "Borrower",
    /** Acquirer of the legal title to the financial instrument. In the case of an option, the buyer is the holder of the option. In the case of a swap or forward, the buyer will be determined by industry best practice.  This does not refer to an investor or investment manager or other organization on what is typically called  the 'Buy side'; for that, see the 'Client' role. Corresponds to 'Buyer' as defined in certain regulations such as ESMA MiFID II/MIFIR RTS 22 field 9. */
    Buyer = "Buyer",
    /** The party or person who, having legal authority to act on behalf of the trade counterparty acting as Buyer as defined in this coding scheme, made the decision to acquire the financial instrument. Corresponds to 'buyer decision maker' as defined in ESMA\'s MIFIR RTS 23 report. This does not refer to the decision maker for what is traditionally called the 'Buy side'; for that, see the 'Client Decision Maker' role. */
    BuyerDecisionMaker = "BuyerDecisionMaker",
    /** The party that provides credit support under English Law. */
    Chargor = "Chargor",
    /** An organization that clears trades through a clearing house, via a clearing broker (member of the clearing house) who acts as an agent on its behalf. The term 'client' refers to the organization\'s role in the clearing process in relation to its clearing broker, and not whether it is a price maker or taker in the execution process. */
    ClearingClient = "ClearingClient",
    /** A party to the trade that claims a clearing exception, such as an end-user exception under Dodd-Frank Act provisions. */
    ClearingExceptionParty = "ClearingExceptionParty",
    /** Organization that submits the trade to a clearing house on behalf of the principal. Synonyms/alternates: Futures Commission Merchant (FCM), Clearing Broker, Clearing Member Firm. Some implementations use 'Clearing Broker' as synonym. */
    ClearingFirm = "ClearingFirm",
    /** The organization that acts as a central counterparty to clear a derivatives contract.  This is used to represent the role of Central Counterparties (CCPs) or Derivative Clearing Organizations (DCOs).  Sometimes called 'ClearingService'. Some implementations also use the term 'Clearer'. */
    ClearingOrganization = "ClearingOrganization",
    /** Client as defined under ESMA MIFIR. This is generally the investor or other client of an investment firm, and is synonymous with the Beneficiary in many circumstances. */
    Client = "Client",
    /** The party or person who, having legal authority to act on behalf of a trade counterparty, made the decision to acquire or sell the financial instrument. */
    ClientDecisionMaker = "ClientDecisionMaker",
    /** Organization serving as a financial intermediary for the purposes of electronic confirmation or providing services for post-processing of transactional data. */
    ConfirmationPlatform = "ConfirmationPlatform",
    /** A party to a contractual document.  If the intended usage relates to the context of the trade lifecycle, more specific annotations have been defined which might be more appropriate. */
    ContractualParty = "ContractualParty",
    /** Organization officially attached to the counterparty. e.g. partner, branch, subsidiary. */
    CounterPartyAffiliate = "CounterPartyAffiliate",
    /** The topmost entity or organization, within the corporate hierarchy, responsible for the reporting party. */
    CounterPartyUltimateParent = "CounterPartyUltimateParent",
    /** An economic counterparty to the trade. Synonym: principal. */
    Counterparty = "Counterparty",
    /** Organization that enhances the credit of another organization (similar to guarantor, but may not fully guarantee the obligation). */
    CreditSupportProvider = "CreditSupportProvider",
    /** Organization that maintains custody of the asset represented by the trade on behalf of the owner/principal. */
    Custodian = "Custodian",
    /** Entity submitting the transaction report to the competent authority. */
    DataSubmitter = "DataSubmitter",
    /** The party referenced is specified in the contract confirmation as Determination Party. */
    DeterminingParty = "DeterminingParty",
    /** Organization that is disputing the trade or transaction. */
    DisputingParty = "DisputingParty",
    /** A marketplace organization which purpose is to maintain document records.  If the intended usage relates to the context of the trade lifecycle, more specific annotations have been defined which might be more appropriate. */
    DocumentRepository = "DocumentRepository",
    /** The (generally sell-side) organization that executed the trade; the price-making party. */
    ExecutingBroker = "ExecutingBroker",
    /** Entity executing the transaction.  If the transaction is executed directly by the reporting party, it will be the reporting party.  If it is executed by an execution agent or an affiliated party on behalf of the reporting party, it will be that affiliate or agent. */
    ExecutingEntity = "ExecutingEntity",
    /** The (generally buy-side) organization that acts to execute trades on behalf of an investor. Typically this is an investment manager or asset manager, and also makes the investment decisions for the investor. If required, a separate InvestmentDecision role can be specified to distinguish that the party making the investment decision is different. */
    ExecutionAgent = "ExecutionAgent",
    /** The facility, exchange, or market where the trade was executed. Synonym: Swap Execution Facility, Designated Contract Market, Execution Venue. */
    ExecutionFacility = "ExecutionFacility",
    /** Organization that backs (guarantees) the credit risk of the trade. */
    Guarantor = "Guarantor",
    /** The ISDA Hedging Party that is specified in the related confirmation as Hedging, or if no Hedging Party is specified, either party to the contract. */
    HedgingParty = "HedgingParty",
    /** The party that lends out securities under stock lending arrangements as principal. */
    Lender = "Lender",
    /** Margin affiliate as defined by U.S. margin and capital rules §23.151. */
    MarginAffiliate = "MarginAffiliate",
    /** The entity transmitting the order to the reporting firm. Synonym: Transmitting Firm. */
    OrderTransmitter = "OrderTransmitter",
    /** The party that provides credit support under New York Law. */
    Pledgor = "Pledgor",
    /** The organization that takes on or took on the credit risk for this trade by stepping in between the two economic parties (without a central counterparty clearing mechanism). */
    PrimeBroker = "PrimeBroker",
    /** The trade repository at which the trade was reported previous to the current trade repository. */
    PriorTradeRepository = "PriorTradeRepository",
    /** The party that acts as either a portfolio compression or portfolio rebalancing service provider, as defined under ESMA MIFIR */
    PTRRServiceProvider = "PTRRServiceProvider",
    /** The reporting service (whether trade repository, market data service, or exchange/facility/venue data distribution service) that published the report of this trade. */
    PublicationVenue = "PublicationVenue",
    /** The party with the regulatory responsibility to report this trade. */
    ReportingParty = "ReportingParty",
    /** Organization officially attached to the reporting party  e.g. partner, branch, subsidiary. */
    ReportingPartyAffiliate = "ReportingPartyAffiliate",
    /** The topmost entity or organization, within the corporate hierarchy, responsible for the reporting party. */
    ReportingPartyUltimateParent = "ReportingPartyUltimateParent",
    /** A counterparty in a trade, which performs in one of the following capacities: 1) it transfers or agrees to transfer in the future an instrument or title to that instrument in exchange for payment, 2) it writes a derivatives instrument such as an option or a swap in which it provides risk protection to the buyer. This does not refer to the broker/dealer or other organization on what is typically called  the 'Sell side'; for that, see the 'Executing Broker' role. Corresponds to 'Seller' as defined in certain regulations such as ESMA MiFID II/MIFIR RTS 22 field 16. */
    Seller = "Seller",
    /** The party or person who, having legal authority to act on behalf of the trade counterparty acting as Seller as defined in this coding scheme, made the decision to sell the financial instrument. Corresponds to 'seller decision maker' as defined in ESMA\'s MIFIR RTS 23 report. This does not refer to the decision maker for what is traditionally called the 'Sell side'; for that, see the 'Trader' person role. */
    SellerDecisionMaker = "SellerDecisionMaker",
    /** The party that receives credit support under New York or English Law. */
    SecuredParty = "SecuredParty",
    /** The organization that makes or receives payments on behalf of the given principal party. */
    SettlementAgent = "SettlementAgent",
    /** An organization that maintains records of the trade for regulatory reporting purposes. */
    TradeRepository = "TradeRepository",
    /** The organization that originally supplied the record of the trade. In the context of regulatory reporting, it is the submitter of the trade record to a regulator or TR. */
    TradeSource = "TradeSource",
    /** The entity responsible for managing the assets/investments of this party.  Synonym:  Asset Manager, Investment Manager, Trading Advisory. */
    TradingManager = "TradingManager",
    /** An entity with which this party trades from time to time, ie. with which it acts as a counterparty on some transactions.   This role is used for static reference data, not individual transactions. */
    TradingPartner = "TradingPartner",
    /** A third party entity that acts as an intermediary and provides automated clearing, settlement, allocation, valuation and reporting services. */
    TripartyAgent = "TripartyAgent",
    /** A third-party providing custody, settlement, segregation and reporting services. */
    ThirdPartyCustodian = "ThirdPartyCustodian",
};
/**
* Defines the enumerated values to specify the ancillary roles to the transaction. The product is agnostic to the actual parties involved in the transaction, with the party references abstracted away from the product definition and replaced by the AncillaryRoleEnum. The AncillaryRoleEnum can then be positioned in the product and the AncillaryParty type, which is positioned outside of the product definition, allows the AncillaryRoleEnum to be associated with an actual party reference.
*/
export enum AncillaryRoleEnum {
    
    /** Specifies the party which determines additional disruption events. */
    DisruptionEventsDeterminingParty = "DisruptionEventsDeterminingParty",
    /** Specifies the party which determines if dividends are extraordinary in relation to normal levels. */
    ExtraordinaryDividendsParty = "ExtraordinaryDividendsParty",
    /** Specifies the clearing organization (CCP, DCO) which the trade should be cleared. */
    PredeterminedClearingOrganizationParty = "PredeterminedClearingOrganizationParty",
    /** Specifies the party to which notice of a manual exercise should be given. */
    ExerciseNoticeReceiverPartyManual = "ExerciseNoticeReceiverPartyManual",
    /** Specifies the party to which notice of a optional early termination exercise should be given. */
    ExerciseNoticeReceiverPartyOptionalEarlyTermination = "ExerciseNoticeReceiverPartyOptionalEarlyTermination",
    /** Specifies the party to which notice of a cancelable provision exercise should be given. */
    ExerciseNoticeReceiverPartyCancelableProvision = "ExerciseNoticeReceiverPartyCancelableProvision",
    /** Specifies the party to which notice of a extendible provision exercise should be given. */
    ExerciseNoticeReceiverPartyExtendibleProvision = "ExerciseNoticeReceiverPartyExtendibleProvision",
    /** Specifies the party responsible for performing calculation agent duties as defined in the applicable product definition. */
    CalculationAgentIndependent = "CalculationAgentIndependent",
    /** Specifies the party responsible for performing calculation agent duties associated with an optional early termination. */
    CalculationAgentOptionalEarlyTermination = "CalculationAgentOptionalEarlyTermination",
    /** Specifies the party responsible for performing calculation agent duties associated with an mandatory early termination. */
    CalculationAgentMandatoryEarlyTermination = "CalculationAgentMandatoryEarlyTermination",
    /** Specifies the party responsible for deciding the fallback rate. */
    CalculationAgentFallback = "CalculationAgentFallback",
};
/**
* The enumerated values to specify the type of telephone number, e.g. work vs. mobile.
*/
export enum TelephoneTypeEnum {
    
    /** A number used primarily for work-related calls. Includes home office numbers used primarily for work purposes. */
    Work = "Work",
    /** A number on a mobile telephone that is often or usually used for work-related calls. This type of number can be used for urgent work related business when a work number is not sufficient to contact the person or firm. */
    Mobile = "Mobile",
    /** A number used primarily for work-related facsimile transmissions. */
    Fax = "Fax",
    /** A number used primarily for non work-related calls. (Normally this type of number would be used only as an emergency backup number, not as a regular course of business). */
    Personal = "Personal",
};
/**
* Enumeration for the different types of confirmation status.
*/
export enum ConfirmationStatusEnum {
    
    Confirmed = "Confirmed",
    Unconfirmed = "Unconfirmed",
};
/**
* Enumeration for the different types of affirmation status.
*/
export enum AffirmationStatusEnum {
    
    Affirmed = "Affirmed",
    Unaffirmed = "Unaffirmed",
};
/**
* The enumerated values to specify how a contract has been executed, e.g. electronically, verbally, ...
*/
export enum ExecutionTypeEnum {
    
    /** Execution via electronic execution facility, derivatives contract market, or other electronic message such as an instant message. */
    Electronic = "Electronic",
    /** Bilateral execution between counterparties not pursuant to the rules of a SEF or DCM. */
    OffFacility = "OffFacility",
    /** Execution via a platform that may or may not be covered by a regulatory defintion. OnVenue is intended to distinguish trades executed on a trading platform from those executed via phone, email or messaging apps. The role and details of the venue are included in the party attribute of the trade. The general rule is that if the parties utilitzed the services of the platform to execute the trade then it would be considered OnVenue. */
    OnVenue = "OnVenue",
};
/**
* The enumeration values to specify the actions associated with transactions.
*/
export enum ActionEnum {
    
    /** A new instance of a transaction event, which is also characterized by the fact that the eventIdentifier has an associated version 1. */
    New = "New",
    /** A correction of a prior instance of the transaction event. The eventIdentifier has an associated version greater than 1. */
    Correct = "Correct",
    /** A cancellation of a prior instance of the transaction event. The eventIdentifier has an associated version greater than 1. */
    Cancel = "Cancel",
};
/**
* The qualification of the type of asset transfer.
*/
export enum AssetTransferTypeEnum {
    
    /** The transfer of assets takes place without a corresponding exchange of payment. */
    FreeOfPayment = "FreeOfPayment",
};
/**
* The enumeration values to qualify the intent associated with a transaction event.
*/
export enum EventIntentEnum {
    
    /** The intent is to allocate one or more trades as part of an allocated block trade. */
    Allocation = "Allocation",
    /** The intent is to designate a stand-alone cash transfer as a result of Trade contracual terms e.g. incurred by payout for instance a Performance Amount or a Floating Rate Amount. The particular CashFlow at stake shall be further specified in priceTransferEnum or transferTypeEnum. For clarity, such intentEnum value shall not be used whenever a cash transfer is not stand-alone but is instead embedded in another Event as part of the composable modelling e.g. Decrease with Fees, Cross-Currency Notional Reset, etc. or any other Event whenever including a cash transfer with other features. For clarity, a principal payment related to a Principal Exhange is excluded as well, because a dedicated intentEnum value exists for this event i.e. PrincipalExchange value. */
    CashFlow = "CashFlow",
    /** The intent is to clear the contract. */
    Clearing = "Clearing",
    /** The intent is to compress multiple trades as part of a netting or compression event. */
    Compression = "Compression",
    /** The intent is to form a contract from an execution. */
    ContractFormation = "ContractFormation",
    /** The intent is to amend the terms of the contract through renegotiation. */
    ContractTermsAmendment = "ContractTermsAmendment",
    /** The intent is to take into effect the occurrence of a Corporate Action and the particular Corporate Action at stake shall be further specified in CorporateActionTypeEnum. */
    CorporateActionAdjustment = "CorporateActionAdjustment",
    /** The intent is to take into effect the occurrence of a Credit Event. */
    CreditEvent = "CreditEvent",
    /** The intent is to Decrease the quantity or notional of the contract. */
    Decrease = "Decrease",
    /** The intent is to fully unwind the Trade, as a result of the application of Trade contractual terms (e.g. an obligation to do so before Termination Date as part of any kind of Early Termination terms) as defined within the CDM EarlyTerminationProvision data type. Accordingly, increase and decrease of positions which result from negotiation by the parties shall not be designated by such intentEnum. For clarity, partial exercise of an option before its expiration date is excluded as well, though related to Trade contract terms, because a dedicated intentEnum value exists for this event i.e. OptionExercise value. */
    EarlyTerminationProvision = "EarlyTerminationProvision",
    /** The intent is to Increase the quantity or notional of the contract. */
    Increase = "Increase",
    /** The intent is to replace an interest rate index by another one during the life of a trade and add a transition spread on top of this index (and on top of the spreads already defined in the trade, if any).  */
    IndexTransition = "IndexTransition",
    /** The intent is to increase or to decrease the notional of the Trade, in accordance with Notional Reset features e.g. could apply for Cross Currency Swaps, Equity Performance Swaps, etc. */
    NotionalReset = "NotionalReset",
    /** The intent is to increase or to decrease the notional of the Trade, in accordance with Step features attached to a Payout Quantity. */
    NotionalStep = "NotionalStep",
    /** The intent is to novate the contract. */
    Novation = "Novation",
    /** The intent is to record any kind of stand-alone obervervations e.g. internal data recording, usage of CDM for recording and/or exchanging data as part of pricing 'consensus' processing, etc. For clarity, such intentEnum value shall not be used whenever an observation is not stand-alone but is instead embedded in another Event as part of the composable modelling e.g. CashFlow to which an observation of prices is associated, etc. */
    ObservationRecord = "ObservationRecord",
    /** The intent is to Exercise a contract that is made of one or several option payout legs. For clarity, such intentEnum value shall not be used whenever an optional right is exercised in relation with a Trade which composition includes other types of payout legs e.g. right to call or to cancel before Termination Date as part of any kind of Early Termination terms other than genuine bermuda or american style features described in option payout.  */
    OptionExercise = "OptionExercise",
    /** The intent is to extend the trade through exercise of an optional right as defined within the CDM OptionProvision data type. */
    OptionalExtension = "OptionalExtension",
    /** The intent is to cancel the trade through exercise of an optional right as defined within the CDM OptionProvision data type. */
    OptionalCancellation = "OptionalCancellation",
    /** The intent is to rebalance a portfolio, by inserting new derivatives transactions into portfolios of participants to reduce risks linked to those trades. These are offsetting trades that rebalance relationships between different counterparties when it comes to exposure of portfolios to certain types of risk, such as interest rate risk. */
    PortfolioRebalancing = "PortfolioRebalancing",
    /** The intent is to pay or to receive a cash transfer, in accordance with Principal Exchange features. */
    PrincipalExchange = "PrincipalExchange",
    /** The intent is to reallocate one or more trades as part of an allocated block trade. */
    Reallocation = "Reallocation",
    /** The intent is to close a repo transaction through repurchase. */
    Repurchase = "Repurchase",
};

export enum PositionEventIntentEnum {
    
    /** The intent is to form a position from a fully formed contract. */
    PositionCreation = "PositionCreation",
    /** The intent is to take into effect the occurrence of a Corporate Action and the particular Corporate Action at stake shall be further specified in CorporateActionTypeEnum. */
    CorporateActionAdjustment = "CorporateActionAdjustment",
    /** The intent is to Decrease the quantity of the position. */
    Decrease = "Decrease",
    /** The intent is to Increase the quantity of the position. */
    Increase = "Increase",
    /** The intent is to transfer the position to another clearing member. */
    Transfer = "Transfer",
    /** The intent is to Exercise a position or part of a position. */
    OptionExercise = "OptionExercise",
    /** The intent is to update the valuation of the position. */
    Valuation = "Valuation",
};
/**
* The enumeration of the account level for the billing summary.
*/
export enum RecordAmountTypeEnum {
    
    AccountTotal = "AccountTotal",
    GrandTotal = "GrandTotal",
    ParentTotal = "ParentTotal",
};
/**
* The enumeration values to specify the transfer status.
*/
export enum TransferStatusEnum {
    
    /** The transfer is disputed. */
    Disputed = "Disputed",
    /** The transfer has been instructed. */
    Instructed = "Instructed",
    /** The transfer is pending instruction. */
    Pending = "Pending",
    /** The transfer has been settled. */
    Settled = "Settled",
    /** The transfer has been netted into a separate Transfer. */
    Netted = "Netted",
};
/**
* The enumeration values indicating the BusinessEvent function associated input instructions.
*/
export enum InstructionFunctionEnum {
    
    Execution = "Execution",
    ContractFormation = "ContractFormation",
    QuantityChange = "QuantityChange",
    Renegotiation = "Renegotiation",
    Compression = "Compression",
};
/**
* Represents the enumeration values that indicate the intended status of message type, such as expected call, notification of a call or a margin call.
*/
export enum CallTypeEnum {
    
    /** Identifies an actionable Margin Call. */
    MarginCall = "MarginCall",
    /** Identifies a notification of a Margin Call for legal obligation to notify other party to initiate a margin call when notifying party is calculation or valuation agent. */
    Notification = "Notification",
    /** Identifies an expected Margin Call instruction for either party to notify the other or their service provider of an expected margin call movement. */
    ExpectedCall = "ExpectedCall",
};
/**
* Represents the enumeration values to identify the collateral action instruction.
*/
export enum MarginCallActionEnum {
    
    /** Indicates an instruction of a new collateral asset delivery. */
    Delivery = "Delivery",
    /** Indicates an instruction for a return of a principals collateral asset delivery. */
    Return = "Return",
};
/**
* Represents the enumeration list to identify the settlement status of the collateral.
*/
export enum CollateralStatusEnum {
    
    /** Indicates the collateral balance amount in full, inclusive of any pre-agreed collateral positions in transit for settlement. */
    FullAmount = "FullAmount",
    /** Indicates the collateral is settled and not an in transit pre-agreed collateral amount/s. */
    SettledAmount = "SettledAmount",
    /** Indicates collateral amount in transit settlement cycle only, excluding settled collateral amount/s. */
    InTransitAmount = "InTransitAmount",
};
/**
* Represents the enumeration values to define the response type to a margin call.
*/
export enum MarginCallResponseTypeEnum {
    
    /** Specifies a 'Full Agreement' to Margin Call. */
    AgreeinFull = "AgreeinFull",
    /** Specifies a 'Partial agreement' to Margin Call. */
    PartiallyAgree = "PartiallyAgree",
    /** Specifies a 'Full Dispute' to a Margin call. */
    Dispute = "Dispute",
};
/**
* Represents the enumeration values to specify the margin type in relation to bilateral or regulatory obligation.
*/
export enum RegMarginTypeEnum {
    
    /** Indicates Variation Margin */
    VM = "VM",
    /** Indicates Regulatory Initial Margin */
    RegIM = "RegIM",
    /** Indicates Non Regulatory Initial margin or independent amount */
    NonRegIM = "NonRegIM",
};
/**
* Represents the enumeration values to specify the role of the party in relation to a regulatory initial margin call.
*/
export enum RegIMRoleEnum {
    
    /** Indicates 'Pledgor' party of initial margin call. */
    Pledgor = "Pledgor",
    /** Indicates 'Secured' party of initial margin call. */
    Secured = "Secured",
};
/**
* Represents the enumeration indicators to specify if an asset or group of assets valuation is based on any valuation treatment haircut.
*/
export enum HaircutIndicatorEnum {
    
    /** Indicates Pre haircut value */
    PreHaircut = "PreHaircut",
    /** Indicates Post haircut value */
    PostHaircut = "PostHaircut",
};
/**
* The enumerated values to specify the origin of a performance transfer
*/
export enum PerformanceTransferTypeEnum {
    
    Commodity = "Commodity",
    Correlation = "Correlation",
    Dividend = "Dividend",
    Equity = "Equity",
    Interest = "Interest",
    Volatility = "Volatility",
    Variance = "Variance",
};
/**
* The enumerated values to specify the origin of a corporate action transfer.
*/
export enum CorporateActionTypeEnum {
    
    /** Corporate action triggered by the distribution of a cash dividend. */
    CashDividend = "CashDividend",
    /** Corporate action triggered by the distribution of a stock dividend. */
    StockDividend = "StockDividend",
    /** Corporate action triggered by a stock split. A stock split or stock divide increases the number of shares in a public company. The price is adjusted such that the before and after market capitalization of the company remains the same and dilutiondoes not occur. The value maps closely to the ISO code (SPLF) defined as a distribution of subsidiary stock to the shareholders of the parent company without a surrender of shares. */
    StockSplit = "StockSplit",
    /** Corporate action triggered by a reverse split. A reverse stock split or reverse split is a process by a company of issuing to each shareholder in that company a smaller number of new shares in proportion to that shareholder's original shares that are subsequently canceled. A reverse stock split is also called a stock merge. The reduction in the number of issued shares is accompanied by a proportional increase in the share price. The value maps closely to the ISO code (SPLR) defined as a decrease in a company's number of outstanding equities without any change in the shareholder's equity or the aggregate market value at the time of the split. Equity price and nominal value are increased accordingly. */
    ReverseStockSplit = "ReverseStockSplit",
    /** Corporate action triggered by a spin Off. A spin-out, also known as a spin-off or a starburst, refers to a type of corporate action where a company splits off sections of itself as a separate business. The value maps closely to the ISO code (SOFF) defined as a a distribution of subsidiary stock to the shareholders of the parent company without a surrender of shares. Spin-off represents a form of divestiture usually resulting in an independent company or in an existing company. For example, demerger, distribution, unbundling. */
    SpinOff = "SpinOff",
    /** Corporate action triggered by a merger. Mergers and acquisitions (abbreviated M&A) is an aspect of corporate strategy, corporate finance and management dealing with the buying, selling, dividing and combining of different companies and similar entities that can help an enterprise grow rapidly in its sector or location of origin, or a new field or new location, without creating a subsidiary, other child entity or using a joint venture. The distinction between a merger and an acquisition has become increasingly blurred in various respects (particularly in terms of the ultimate economic outcome), although it has not completely disappeared in all situations. The value maps closely to the ISO code (MRGR) defined as an offer made to shareholders, normally by a third party, requesting them to sell (tender) or exchange their equities. */
    Merger = "Merger",
    /** Corporate action triggered by the removal of a security from a stock exchange. */
    Delisting = "Delisting",
    /** Corporate action triggered by a change in the name used to trade the security. */
    StockNameChange = "StockNameChange",
    /** Corporate action triggered by a change in the code used to trade the security. */
    StockIdentifierChange = "StockIdentifierChange",
    /** Corporate action triggered by an issuance to shareholders of rights to purchase additional shares at a discount. */
    RightsIssue = "RightsIssue",
    /** Corporate action triggered by a takeover. A takeover is the purchase of onecompany (the target) by another (the acquirer, or bidder). The value maps to the ISO code (TEND) but is finer grained than TEND which emcompasses Tender/Acquisition/Takeover/Purchase Offer/Buyback. ISO defines the TEND code as an offer made to shareholders, normally by a third party, requesting them to sell (tender) or exchange their equities. */
    Takeover = "Takeover",
    /** Corporate action triggered by a Stock Reclassification. */
    StockReclassification = "StockReclassification",
    /** Corporate action triggered by a bonus issue. A bonus issue or bonus share is a free share of stock given to current shareholders in a company, based upon the number of shares that the shareholder already owns. While the issue of bonus shares increases the total number of shares issued and owned, it does not change the value of the company. The value maps closely to the ISO code (BONU) defined as a bonus, scrip or capitalisation issue. Security holders receive additional assets free of payment from the issuer, in proportion to their holding. */
    BonusIssue = "BonusIssue",
    /** Corporate action triggered by a Class Action. An action where an individual represents a group in a court claim. The judgment from the suit is for all the members of the group (class). The value maps closely to the ISO code (CLSA) defined as the situation where interested parties seek restitution for financial loss. The security holder may be offered the opportunity to join a class action proceeding and would need to respond with an instruction. */
    ClassAction = "ClassAction",
    /** Corporate action triggered by an early redemption. The value maps closely to the ISO code (MCAL) defined as the redemption of an entire issue outstanding of securities, for example, bonds, preferred equity, funds, by the issuer or its agent, for example, asset manager, before final maturity. */
    EarlyRedemption = "EarlyRedemption",
    /** Corporate action triggered by a liquidation. When a business or firm is terminated or bankrupt, its assets are sold (liquidated) and the proceeds pay creditors. Any leftovers are distributed to shareholders. The value maps closely to the ISO code (LIQU) defined as a distribution of cash, assets or both. Debt may be paid in order of priority based on preferred claims to assets specified by the security. */
    Liquidation = "Liquidation",
    /** Corporate action triggered by bankruptcy, insolvency filing or insolvency of the issuer of the Security. */
    BankruptcyOrInsolvency = "BankruptcyOrInsolvency",
    /** Corporate action triggered by the nationalization of the issuer of the Security. */
    IssuerNationalization = "IssuerNationalization",
    /** Corporate action triggered by the relisting of a security from the original stock exchange to another exchange. */
    Relisting = "Relisting",
    /** Corporate Action which is separately agreed between the parties and/or described elsewhere in the document (for instance in bespoke term described in string type objects present in some root attributes of Agreement type). */
    BespokeEvent = "BespokeEvent",
};
/**
* Represents the enumerated values to specify a credit event type.
*/
export enum CreditEventTypeEnum {
    
    /** The reference entity has been dissolved or has become insolvent. It also covers events that may be a precursor to insolvency such as instigation of bankruptcy or insolvency proceedings. Sovereign trades are not subject to Bankruptcy as 'technically' a Sovereign cannot become bankrupt. ISDA 2003 Term: Bankruptcy. */
    Bankruptcy = "Bankruptcy",
    /** Results from the fact that the rating of the reference obligation is downgraded to a distressed rating level. From a usage standpoint, this credit event is typically not applicable in case of RMBS trades. */
    DistressedRatingsDowngrade = "DistressedRatingsDowngrade",
    /** This credit event triggers, after the expiration of any applicable grace period, if the reference entity fails to make due payments in an aggregrate amount of not less than the payment requirement on one or more obligations (e.g. a missed coupon payment). ISDA 2003 Term: Failure to Pay. */
    FailureToPay = "FailureToPay",
    /** Corresponds to the failure by the Reference Entity to pay an expected interest amount or the payment of an actual interest amount that is less than the expected interest amount. ISDA 2003 Term: Failure to Pay Interest. */
    FailureToPayInterest = "FailureToPayInterest",
    /** Corresponds to the failure by the Reference Entity to pay an expected principal amount or the payment of an actual principal amount that is less than the expected principal amount. ISDA 2003 Term: Failure to Pay Principal. */
    FailureToPayPrincipal = "FailureToPayPrincipal",
    /** A governmental intervention is an event resulting from an action by a governmental authority that materially impacts the reference entity's obligations, such as an interest rate reduction, principal reduction, deferral of interest or principal, change in priority ranking, or change in currency or composition of payment. ISDA 2014 Term: Governmental Intervention. */
    GovernmentalIntervention = "GovernmentalIntervention",
    /** Results from the fact that losses occur to the underlying instruments that do not result in reductions of the outstanding principal of the reference obligation. */
    ImpliedWritedown = "ImpliedWritedown",
    /** Results from the fact that the underlier fails to make principal payments as expected. */
    MaturityExtension = "MaturityExtension",
    /** One or more of the obligations have been declared due and payable before they would otherwise have been due and payable as a result of, or on the basis of, the occurrence of a default, event of default or other similar condition or event other than failure to pay (preferred by the market over Obligation Default, because more definitive and encompasses the definition of Obligation Default - this is more favorable to the Seller). Subject to the default requirement amount. ISDA 2003 Term: Obligation Acceleration. */
    ObligationAcceleration = "ObligationAcceleration",
    /** One or more of the obligations have become capable of being declared due and payable before they would otherwise have been due and payable as a result of, or on the basis of, the occurrence of a default, event of default or other similar condition or event other than failure to pay. ISDA 2003 Term: Obligation Default. */
    ObligationDefault = "ObligationDefault",
    /** The reference entity, or a governmental authority, either refuses to recognise or challenges the validity of one or more obligations of the reference entity, or imposes a moratorium thereby postponing payments on one or more of the obligations of the reference entity. Subject to the default requirement amount. ISDA 2003 Term: Repudiation/Moratorium. */
    RepudiationMoratorium = "RepudiationMoratorium",
    /** A restructuring is an event that materially impacts the reference entity's obligations, such as an interest rate reduction, principal reduction, deferral of interest or principal, change in priority ranking, or change in currency or composition of payment. ISDA 2003 Term: Restructuring. */
    Restructuring = "Restructuring",
    /** Results from the fact that the underlier writes down its outstanding principal amount. */
    Writedown = "Writedown",
};
/**
* Method used for the valuation of the transaction by the valuation party.
*/
export enum ValuationTypeEnum {
    
    /** Mark-to-Market */
    MarkToMarket = "MarkToMarket",
    /** Mark-to-Model */
    MarkToModel = "MarkToModel",
};
/**
* Source for the valuation of the transaction by the valuation party.
*/
export enum ValuationSourceEnum {
    
    /** Central Counterparty's Valuation */
    CentralCounterparty = "CentralCounterparty",
};
/**
* Scope of the valuation i.e. what it applies to.
*/
export enum ValuationScopeEnum {
    
    /** Valuation of the collateral used against a trade. */
    Collateral = "Collateral",
    /** Valuation of the actual trade itself. */
    Trade = "Trade",
};

export enum PriceTimingEnum {
    
    /** The last price anyone paid for a share of a product during the business hours of the exchange where the product is traded on a business day. */
    ClosingPrice = "ClosingPrice",
    /** The first price anyone paid for a share of a product during the business hours of the exchange where the product is traded on a business day. */
    OpeningPrice = "OpeningPrice",
};
/**
* The enumerated values to specify what led to the contract or execution closure.
*/
export enum ClosedStateEnum {
    
    /** The execution or contract has been allocated. */
    Allocated = "Allocated",
    /** The execution or contract has been cancelled. */
    Cancelled = "Cancelled",
    /** The (option) contract has been exercised. */
    Exercised = "Exercised",
    /** The (option) contract has expired without being exercised. */
    Expired = "Expired",
    /** The contract has reached its contractual termination date. */
    Matured = "Matured",
    /** The contract has been novated. This state applies to the stepped-out contract component of the novation event. */
    Novated = "Novated",
    /** The contract has been subject of an early termination event. */
    Terminated = "Terminated",
};
/**
* Enumeration to describe the different (risk) states of a Position, whether executed, settled, matured...etc
*/
export enum PositionStatusEnum {
    
    /** The position has been executed, which is the point at which risk has been transferred. */
    Executed = "Executed",
    /** Contract has been formed, in case position is on a contractual product. */
    Formed = "Formed",
    /** The position has settled, in case product is subject to settlement after execution, such as securities. */
    Settled = "Settled",
    /** The position has been cancelled, in case of a cancellation event following an execution. */
    Cancelled = "Cancelled",
    /** The position has been closed, in case of a termination event. */
    Closed = "Closed",
};
/**
* Enumeration to describe the type of AvailableInventory
*/
export enum AvailableInventoryTypeEnum {
    
    /** Where a lender is broadcasting the securities that they have available to lend */
    AvailableToLend = "AvailableToLend",
    /** Where a party is asking a lender if they have specific securities available for them to borrow */
    RequestToBorrow = "RequestToBorrow",
};
/**
* The enumeration values to qualify the type of credit limits.
*/
export enum CreditLimitTypeEnum {
    
    /** The type of credit line expressed in CS01. The sensitivity with respect to changes in the CDS spread. */
    CS01 = "CS01",
    /** The type of credit line expressed in DV01. The dollar value of a one basis point decrease in interest rates. It shows the change in a bond's price compared to a decrease in the bond's yield. */
    DV01 = "DV01",
    /** The type of credit line expressed in Initial Margin value. */
    IM = "IM",
    /** The type of credit line expressed in Notional amount. */
    Notional = "Notional",
    /** The type of credit line expressed as a Net Present Value. */
    NPV = "NPV",
    /** The type of credit line expressed in PV01. The value of a one dollar or one basis point annuity. */
    PV01 = "PV01",
};
/**
* The enumeration values to qualify the timestamps that can be associated with a lifecycle event. The reason for such approach is that the experience of integrating the DTCC and CME data representations suggests that a wide set of timestamps are currently utilized among service providers, while there is not at present an objective set of criteria that could help suggest a defined set of timestamps as part of the CDM. Implementers are expected to evaluate the current enumeration values to determine whether those meet their requirements. If not, they are expected to engage with the CDM team to evaluate the addition of further value(s) to this enumeration, which will then participate to the development of a compendium for further evaluation at a later point in order to determine whether this modeling is appropriate.
*/
export enum EventTimestampQualificationEnum {
    
    /** The date and time on the trade was cleared. */
    clearingDateTime = "clearingDateTime",
    /** The date and time on which trade was confirmed as cleared. */
    clearingConfirmationDateTime = "clearingConfirmationDateTime",
    /** The date and time on which trade was received by Clearing Body. */
    clearingReceiptDateTime = "clearingReceiptDateTime",
    /** The date and time on which the event was submitted for clearing. */
    clearingSubmissionDateTime = "clearingSubmissionDateTime",
    /** The date and time on which the event was confirmed. */
    confirmationDateTime = "confirmationDateTime",
    /** The date and time on which the event was created. */
    eventCreationDateTime = "eventCreationDateTime",
    /** The date and time on which the event will be considered expired. */
    eventExpirationDateTime = "eventExpirationDateTime",
    /** The date and time on which the event was processed. */
    eventProcessingDateTime = "eventProcessingDateTime",
    /** The date and time on which the event was sent. */
    eventSentDateTime = "eventSentDateTime",
    /** The date and time on which the event was submitted. */
    eventSubmittedDateTime = "eventSubmittedDateTime",
    /** The date and time on which the trade execution was performed. */
    executionDateTime = "executionDateTime",
    /** The date and time on which the transaction has been created. This timestamp is specified as such by the CME ClearPort Matched IRS Trade submission API specification: 'The transaction date time of the trade. Represents the date & time on which the trade was initially generated either by CME Clearing or firm. The transaction date time may be assigned by CME Clearing at the point the trade is reported as cleared. Transaction date time can also be provided by an external submitter of the trade at the point the trade is submitted.' */
    transactionCreationDateTime = "transactionCreationDateTime",
};
/**
* The enumeration values to specify the level at which the limit is set: customer business, proprietary business or account level. This is part of the CME specification for clearing credit limits, although not specified as a set of enumerated values as part of the clearing confirmation specification.
*/
export enum LimitLevelEnum {
    
    /** The limit is set in relation to the proprietary business undertaken by the clearing counterparty. */
    Account = "Account",
    /** The limit is set in relation to the customer business undertaken by the clearing counterparty. */
    Customer = "Customer",
    /** The limit is set at the account level in relation to the clearing counterparty. */
    House = "House",
};

export enum WarehouseIdentityEnum {
    
    /** The DTCC Trade Information Warehouse Gold service */
    DTCC_TIW_Gold = "DTCC_TIW_Gold",
};

export enum WorkflowStatusEnum {
    
    Accepted = "Accepted",
    Alleged = "Alleged",
    Amended = "Amended",
    Cancelled = "Cancelled",
    Certain = "Certain",
    Cleared = "Cleared",
    Pending = "Pending",
    Rejected = "Rejected",
    Submitted = "Submitted",
    Terminated = "Terminated",
    Uncertain = "Uncertain",
    Unconfirmed = "Unconfirmed",
    Affirmed = "Affirmed",
    Confirmed = "Confirmed",
};
/**
* The enumerated values to specify a set of standard contract definitions relevant to the transaction.
*/
export enum ContractualDefinitionsEnum {
    
    /** ISDA 1991 Interest Rate Definitions */
    ISDA1991InterestRate = "ISDA1991InterestRate",
    /** ISDA 1993 Commodity Derivatives Definitions */
    ISDA1993CommodityDerivatives = "ISDA1993CommodityDerivatives",
    /** ISDA 1996 Equity Derivatives Definitions */
    ISDA1996EquityDerivatives = "ISDA1996EquityDerivatives",
    /** ISDA 1997 Bullion Definitions */
    ISDA1997Bullion = "ISDA1997Bullion",
    /** ISDA 1997 Government Bond Option Definitions */
    ISDA1997GovernmentBondOption = "ISDA1997GovernmentBondOption",
    /** ISDA 1998 FX and Currency Option Definitions */
    ISDA1998FxAndCurrencyOption = "ISDA1998FxAndCurrencyOption",
    /** ISDA 1999 Credit Derivatives Definitions */
    ISDA1999CreditDerivatives = "ISDA1999CreditDerivatives",
    /** ISDA 2000 Definitions */
    ISDA2000 = "ISDA2000",
    /** ISDA 2002 Equity Derivatives Definitions */
    ISDA2002EquityDerivatives = "ISDA2002EquityDerivatives",
    /** ISDA 2003 Credit Derivatives Definitions */
    ISDA2003CreditDerivatives = "ISDA2003CreditDerivatives",
    /** ISDA 2004 Novation Definitions */
    ISDA2004Novation = "ISDA2004Novation",
    /** ISDA 2005 Commodity Definitions */
    ISDA2005Commodity = "ISDA2005Commodity",
    /** ISDA 2006 Definitions */
    ISDA2006 = "ISDA2006",
    /** ISDA 2006 Inflation Derivatives Definitions */
    ISDA2006InflationDerivatives = "ISDA2006InflationDerivatives",
    /** ISDA 2008 Inflation Derivatives Definitions */
    ISDA2008InflationDerivatives = "ISDA2008InflationDerivatives",
    /** ISDA 2011 Equity Derivatives Definitions */
    ISDA2011EquityDerivatives = "ISDA2011EquityDerivatives",
    /** ISDA 2014 Credit Derivatives Definitions */
    ISDA2014CreditDerivatives = "ISDA2014CreditDerivatives",
    /** ISDA 2021 Interest Rate Derivatives Definitions */
    ISDA2021InterestRateDerivatives = "ISDA2021InterestRateDerivatives",
    /** ISDA 2022 Verified Carbon Credit Transactions Definitions */
    ISDA2022VerifiedCarbonCredit = "ISDA2022VerifiedCarbonCredit",
    /** ISDA 2023 Digital Asset Derivatives Definitions */
    ISDA2023DigitalAssetDerivatives = "ISDA2023DigitalAssetDerivatives",
};
/**
* The enumerated values to define the supplements to a base set of ISDA Definitions that are applicable to the transaction.
*/
export enum ContractualSupplementTypeEnum {
    
    /** Standard Terms Supplement for ABX Transactions. */
    ABX = "ABX",
    /** Standard Terms Supplement for Asset-Backed Tranche Transactions. */
    ABXTranche = "ABXTranche",
    /** ISDA Standard Terms Supplement for use with Credit Derivative Transactions on Leveraged Loans. */
    CDSonLeveragedLoans = "CDSonLeveragedLoans",
    /** ISDA Standard Terms Supplement for use with Credit Derivative Transactions on Mortgage-backed Security with Pay-As-You-Go or Physical Settlement. */
    CDSonMBS = "CDSonMBS",
    /** Standard Terms Supplement for CDX Untranched Transactions. */
    CDX = "CDX",
    /** Standard Terms Supplement for CDX Emerging Markets Untranched Transactions. */
    CDXEmergingMarkets = "CDXEmergingMarkets",
    /** Standard Terms Supplement for CDX Emerging Markets Diversified Untranched Transactions.. */
    CDXEmergingMarketsDiversified = "CDXEmergingMarketsDiversified",
    /** Standard Terms Supplement for CDX Swaption Transactions. */
    CDXSwaption = "CDXSwaption",
    /** Standard Terms Supplement for Dow Jones CDX Tranche Transactions. */
    CDXTranche = "CDXTranche",
    /** Standard Terms Supplement for CMBX Transactions. */
    CMBX = "CMBX",
    /** Standard Terms Supplement for Single Name European CMBS Transactions. */
    EuropeanCMBS = "EuropeanCMBS",
    /** Standard Terms Supplement for Single Name European RMBS Transactions. */
    EuropeanRMBS = "EuropeanRMBS",
    /** Standard Terms Supplement for IOS Transactions. */
    IOS = "IOS",
    /** Supplement to the 1999 ISDA Credit Derivatives Definitions Relating to Convertible, Exchangeable or Accreting Obligations dated November 9, 2001. */
    ISDA1999CreditConvertibleExchangeableAccretingObligations = "ISDA1999CreditConvertibleExchangeableAccretingObligations",
    /** Restructuring Supplement to the 1999 ISDA Credit Derivatives Definitions dated May 11, 2001. */
    ISDA1999CreditRestructuring = "ISDA1999CreditRestructuring",
    /** Supplement Relating to Successor and Credit Events to the 1999 ISDA Credit Derivatives Definitions dated November 28, 2001. */
    ISDA1999CreditSuccessorAndCreditEvents = "ISDA1999CreditSuccessorAndCreditEvents",
    /** Additional Provisions for LPN dated December 6, 2007. */
    ISDA2003AdditionalProvisionsLPN = "ISDA2003AdditionalProvisionsLPN",
    /** Additional Provisions for Contingent Credit Spread Transactions dated August 15, 2008. */
    ISDA2003ContingentCreditSpreadTransaction = "ISDA2003ContingentCreditSpreadTransaction",
    /** 2005 Matrix Supplement to the 2003 ISDA Credit Derivatives. */
    ISDA2003Credit2005MatrixSupplement = "ISDA2003Credit2005MatrixSupplement",
    /** Additional Provisions for the Argentine Republic: Excluded Obligations and Excluded Deliverable Obligations dated December 21, 2005. */
    ISDA2003CreditArgentineRepublic = "ISDA2003CreditArgentineRepublic",
    /** ISDA Credit Derivatives Determinations Committees and Auction Settlement Supplement to the 2003 ISDA Credit Derivatives Definitions (published on [TBD]). */
    ISDA2003CreditAuctionSupplement = "ISDA2003CreditAuctionSupplement",
    /** May 2003 Supplement to the 2003 ISDA Credit Derivatives Definitions. */
    ISDA2003CreditMay2003 = "ISDA2003CreditMay2003",
    /** Additional Provisions for Physically Settled Default Swaps Monoline Insurer as Reference Entity dated May 9, 2003. */
    ISDA2003CreditMonolineInsurers = "ISDA2003CreditMonolineInsurers",
    /** Additional Provisions for Physically Settled Default Swaps Monoline Insurer as Reference Entity dated January 21, 2005. */
    ISDA2003CreditMonolineInsurers2005 = "ISDA2003CreditMonolineInsurers2005",
    /** Additional Provisions for the Republic of Hungary: Obligation Characteristics and Deliverable Obligation Characteristics dated August 13, 2004. */
    ISDA2003CreditRepublicOfHungary = "ISDA2003CreditRepublicOfHungary",
    /** Additional Provisions for the Republic of Hungary: Obligation Characteristics and Deliverable Obligation Characteristics dated February 14, 2005.  */
    ISDA2003CreditRepublicOfHungary2005 = "ISDA2003CreditRepublicOfHungary2005",
    /** Additional Provisions for the Russian Federation: Obligation Characteristics and Deliverable Obligation Characteristics dated August 13, 2004. */
    ISDA2003CreditRussianFederation = "ISDA2003CreditRussianFederation",
    /** Additional Provisions for Credit Derivative Transactions - U.S. Municipal Entity as Reference Entity dated September 17, 2004. */
    ISDA2003CreditUSMunicipals = "ISDA2003CreditUSMunicipals",
    /** Additional Provisions for STMicroelectronics NV dated December 6, 2007. */
    ISDA2003STMicroelectronicsNV = "ISDA2003STMicroelectronicsNV",
    /** 2007 Full Lookthrough Depository Receipt Supplement to the 2002 Equity Derivatives Definitions. */
    ISDA2007FullLookthroughDepositoryReceiptSupplement = "ISDA2007FullLookthroughDepositoryReceiptSupplement",
    /** 2007 Partial Lookthrough Depository Receipt Supplement to the 2002 ISDA Equity Derivatives Definitions. */
    ISDA2007PartialLookthroughDepositoryReceiptSupplement = "ISDA2007PartialLookthroughDepositoryReceiptSupplement",
    /** Additional Provisions for Physically Settled Default Swaps Monoline Insurer. */
    ISDACreditMonolineInsurers = "ISDACreditMonolineInsurers",
    /** Additional Provisions for Fixed Recovery Credit Default Swap Transactions */
    ISDADeliveryRestrictions = "ISDADeliveryRestrictions",
    /** Additional Provisions for Fixed Recovery Credit Default Swap Transactions. */
    ISDAFixedRecovery = "ISDAFixedRecovery",
    /** Additional Provisions for LPN Reference Entities. */
    ISDALPNReferenceEntities = "ISDALPNReferenceEntities",
    /** Canadian Supplement to the 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement dated March 29, 2004. */
    ISDAMarch2004EquityCanadianSupplement = "ISDAMarch2004EquityCanadianSupplement",
    /** Additional Provisions for Recovery Lock Credit Default Swap Transactions. */
    ISDARecoveryLock = "ISDARecoveryLock",
    /** Additional Provisions for Secured Deliverable Obligation Characteristic. */
    ISDASecuredDeliverableObligationCharacteristic = "ISDASecuredDeliverableObligationCharacteristic",
    /** Standard Terms Supplement for Syndicated Secured Loan Credit Default Swap Index Transactions. */
    LCDX = "LCDX",
    /** Standard Terms Supplement for Syndicated Secured Loan Credit Default Swap Index Tranche Transactions. */
    LCDXTranche = "LCDXTranche",
    /** Standard Terms Supplement for MBX Transactions. */
    MBX = "MBX",
    /** Standard Terms Supplement for Municipal CDX Untranched Transactions. */
    MCDX = "MCDX",
    /** Standard Terms Supplement for PO Index Transactions. */
    PO = "PO",
    /** Standard Terms Supplement for PrimeX Transactions. */
    PrimeX = "PrimeX",
    /** Standard Terms Supplement for Standard CDX Tranche Transactions. */
    StandardCDXTranche = "StandardCDXTranche",
    /** Standard Syndicated Secured Loan Credit Default Swap Standard Terms Supplement. */
    StandardLCDS = "StandardLCDS",
    /** Standard Terms Supplement for Standard Syndicated Secured Loan Credit Default Swap Bullet Transactions. */
    StandardLCDSBullet = "StandardLCDSBullet",
    /** Standard Terms Supplement for Standard Syndicated Secured Loan Credit Default Swap Index Bullet Transactions. */
    StandardLCDXBullet = "StandardLCDXBullet",
    /** Standard Terms Supplement for Standard Syndicated Secured Loan Credit Default Swap Index Bullet Tranche Transactions. */
    StandardLCDXBulletTranche = "StandardLCDXBulletTranche",
    /** Standard Terms Supplement for Standard iTraxx Europe Tranched Transactions. */
    StandardiTraxxEuropeTranche = "StandardiTraxxEuropeTranche",
    /** Syndicated Secured Loan Credit Default Swap Standard Terms Supplement. */
    SyndicatedSecuredLoanCDS = "SyndicatedSecuredLoanCDS",
    /** Standard Terms Supplement for TRX Transactions. */
    TRX = "TRX",
    /** Standard Terms Supplement for TRX.II Transactions. */
    TRX_II = "TRX_II",
    /** Standard Terms Supplement for iTraxx Asia Excluding Japan. */
    iTraxxAsiaExJapan = "iTraxxAsiaExJapan",
    /** Standard Terms Supplement for iTraxx Asia Ex-Japan Swaption Transactions. */
    iTraxxAsiaExJapanSwaption = "iTraxxAsiaExJapanSwaption",
    /** Standard Terms Supplement for iTraxx Asia Excluding Japan Tranched Transactions. */
    iTraxxAsiaExJapanTranche = "iTraxxAsiaExJapanTranche",
    /** Standard Terms Supplement for iTraxx Australia. */
    iTraxxAustralia = "iTraxxAustralia",
    /** Standard Terms Supplement for iTraxx Australia Swaption Transactions. */
    iTraxxAustraliaSwaption = "iTraxxAustraliaSwaption",
    /** Standard Terms Supplement for iTraxx Australia Tranched Transactions. */
    iTraxxAustraliaTranche = "iTraxxAustraliaTranche",
    /** Standard Terms Supplement for iTraxx CJ. */
    iTraxxCJ = "iTraxxCJ",
    /** Standard Terms Supplement for iTraxx CJ Tranched Transactions. */
    iTraxxCJTranche = "iTraxxCJTranche",
    /** Standard Terms Supplement for iTraxx Europe Transactions. */
    iTraxxEurope = "iTraxxEurope",
    /** Standard Terms Supplement for iTraxx Europe Dealer Form. */
    iTraxxEuropeDealer = "iTraxxEuropeDealer",
    /** Standard Terms Supplement for iTraxx Europe Non-Dealer Form. */
    iTraxxEuropeNonDealer = "iTraxxEuropeNonDealer",
    /** Standard Terms Supplement for iTraxx Europe Swaption Transactions. */
    iTraxxEuropeSwaption = "iTraxxEuropeSwaption",
    /** Standard Terms Supplement for iTraxx Europe Tranched Transactions. */
    iTraxxEuropeTranche = "iTraxxEuropeTranche",
    /** Standard Terms Supplement for iTraxx Japan. */
    iTraxxJapan = "iTraxxJapan",
    /** Standard Terms Supplement for iTraxx Japan Swaption Transactions. */
    iTraxxJapanSwaption = "iTraxxJapanSwaption",
    /** Standard Terms Supplement for iTraxx Japan Tranched Transactions. */
    iTraxxJapanTranche = "iTraxxJapanTranche",
    /** Standard Terms Supplement for iTraxx LevX. */
    iTraxxLevX = "iTraxxLevX",
    /** Standard Terms Supplement for iTraxx SDI 75 Dealer Transactions. */
    iTraxxSDI75Dealer = "iTraxxSDI75Dealer",
    /** Standard Terms Supplement for iTraxx SDI 75 Non-Dealer Transactions. */
    iTraxxSDI75NonDealer = "iTraxxSDI75NonDealer",
    /** Standard Terms Supplement for iTraxx SovX. */
    iTraxxSovX = "iTraxxSovX",
};
/**
* The enumerated values to specify the law governing the contract or legal document.
*/
export enum GoverningLawEnum {
    
    /** The Governing Law is determined by reference to the relevant master agreement. */
    AsSpecifiedInMasterAgreement = "AsSpecifiedInMasterAgreement",
    /** Belgian law */
    BE = "BE",
    /** Alberta law */
    CAAB = "CAAB",
    /** British Columbia Law */
    CABC = "CABC",
    /** Manitoba law */
    CAMN = "CAMN",
    /** Ontario law */
    CAON = "CAON",
    /** Quebec law */
    CAQC = "CAQC",
    /** German law */
    DE = "DE",
    /** French law */
    FR = "FR",
    /** English law */
    GBEN = "GBEN",
    /** The law of the island of Guernsey */
    GBGY = "GBGY",
    /** The law of the Isle of Man */
    GBIM = "GBIM",
    /** The law of the island of Jersey */
    GBJY = "GBJY",
    /** Scottish law */
    GBSC = "GBSC",
    /** Irish law */
    IE = "IE",
    /** Japanese law */
    JP = "JP",
    /** Luxembourg law */
    LU = "LU",
    /** As agreed in the ISDA Master Agreement */
    RelatedMasterAgreement = "RelatedMasterAgreement",
    /** Californian law */
    USCA = "USCA",
    /** Delaware law */
    USDE = "USDE",
    /** Illinois law */
    USIL = "USIL",
    /** New York law */
    USNY = "USNY",
};
/**
* The enumerated values to specify the length unit in the Resource type.
*/
export enum LengthUnitEnum {
    
    Pages = "Pages",
    TimeUnit = "TimeUnit",
};
/**
* The enumerated values to specify a scheme of transaction types specified in the Equity Derivatives Settlement Matrix.
*/
export enum MatrixTermEnum {
    
    /** Matrix Transaction Type of ASIA CORPORATE. */
    AsiaCorporate = "AsiaCorporate",
    /** Matrix Transaction Type of ASIA FINANCIAL CORPORATE. */
    AsiaFinancialCorporate = "AsiaFinancialCorporate",
    /** Matrix Transaction Type of ASIA SOVEREIGN. */
    AsiaSovereign = "AsiaSovereign",
    /** Matrix Transaction Type of AUSTRALIA CORPORATE. */
    AustraliaCorporate = "AustraliaCorporate",
    /** Matrix Transaction Type of AUSTRALIA FINANCIAL CORPORATE. */
    AustraliaFinancialCorporate = "AustraliaFinancialCorporate",
    /** Matrix Transaction Type of AUSTRALIA SOVEREIGN. */
    AustraliaSovereign = "AustraliaSovereign",
    /** Matrix Transaction Type of EMERGING EUROPEAN AND MIDDLE EASTERN SOVEREIGN. */
    EmergingEuropeanAndMiddleEasternSovereign = "EmergingEuropeanAndMiddleEasternSovereign",
    /** Matrix Transaction Type of EMERGING EUROPEAN CORPORATE. */
    EmergingEuropeanCorporate = "EmergingEuropeanCorporate",
    /** Matrix Transaction Type of EMERGING EUROPEAN CORPORATE LPN. */
    EmergingEuropeanCorporateLPN = "EmergingEuropeanCorporateLPN",
    /** Matrix Transaction Type of EMERGING EUROPEAN FINANCIAL CORPORATE. */
    EmergingEuropeanFinancialCorporate = "EmergingEuropeanFinancialCorporate",
    /** Matrix Transaction Type of EMERGING EUROPEAN FINANCIAL CORPORATE LPN. */
    EmergingEuropeanFinancialCorporateLPN = "EmergingEuropeanFinancialCorporateLPN",
    /** Matrix Transaction Type of EUROPEAN COCO FINANCIAL CORPORATE. */
    EuropeanCoCoFinancialCorporate = "EuropeanCoCoFinancialCorporate",
    /** Matrix Transaction Type of EUROPEAN CORPORATE. */
    EuropeanCorporate = "EuropeanCorporate",
    /** Matrix Transaction Type of EUROPEAN FINANCIAL CORPORATE. */
    EuropeanFinancialCorporate = "EuropeanFinancialCorporate",
    /** Matrix Transaction Type of EUROPEAN SENIOR NON PREFERRED FINANCIAL CORPORATE. */
    EuropeanSeniorNonPreferredFinancialCorporate = "EuropeanSeniorNonPreferredFinancialCorporate",
    /** The ISDA-published 2011 Index Volatility Swap Agreement for Open Markets. */
    IVS1OpenMarkets = "IVS1OpenMarkets",
    /** Matrix Transaction Type of JAPAN CORPORATE. */
    JapanCorporate = "JapanCorporate",
    /** Matrix Transaction Type of JAPAN FINANCIAL CORPORATE. */
    JapanFinancialCorporate = "JapanFinancialCorporate",
    /** Matrix Transaction Type of JAPAN SOVEREIGN. */
    JapanSovereign = "JapanSovereign",
    /** Matrix Transaction Type of LATIN AMERICA CORPORATE. */
    LatinAmericaCorporate = "LatinAmericaCorporate",
    /** Matrix Transaction Type of LATIN AMERICA CORPORATE B. */
    LatinAmericaCorporateBond = "LatinAmericaCorporateBond",
    /** Matrix Transaction Type of LATIN AMERICA CORPORATE BL. */
    LatinAmericaCorporateBondOrLoan = "LatinAmericaCorporateBondOrLoan",
    /** Matrix Transaction Type of LATIN AMERICA FINANCIAL CORPORATE B. */
    LatinAmericaFinancialCorporateBond = "LatinAmericaFinancialCorporateBond",
    /** Matrix Transaction Type of LATIN AMERICA FINANCIAL CORPORATE BL. */
    LatinAmericaFinancialCorporateBondOrLoan = "LatinAmericaFinancialCorporateBondOrLoan",
    /** Matrix Transaction Type of LATIN AMERICA SOVEREIGN. */
    LatinAmericaSovereign = "LatinAmericaSovereign",
    /** Matrix Transaction Type of NEW ZEALAND CORPORATE. */
    NewZealandCorporate = "NewZealandCorporate",
    /** Matrix Transaction Type of NEW ZEALAND FINANCIAL CORPORATE. */
    NewZealandFinancialCorporate = "NewZealandFinancialCorporate",
    /** Matrix Transaction Type of NEW ZEALAND SOVEREIGN. */
    NewZealandSovereign = "NewZealandSovereign",
    /** Matrix Transaction Type of NORTH AMERICAN CORPORATE. */
    NorthAmericanCorporate = "NorthAmericanCorporate",
    /** Matrix Transaction Type of NORTH AMERICAN FINANCIAL CORPORATE. */
    NorthAmericanFinancialCorporate = "NorthAmericanFinancialCorporate",
    /** Matrix Transaction Type of SINGAPORE CORPORATE. */
    SingaporeCorporate = "SingaporeCorporate",
    /** Matrix Transaction Type of SINGAPORE FINANCIAL CORPORATE. */
    SingaporeFinancialCorporate = "SingaporeFinancialCorporate",
    /** Matrix Transaction Type of SINGAPORE SOVEREIGN. */
    SingaporeSovereign = "SingaporeSovereign",
    /** Matrix Transaction Type of STANDARD ASIA CORPORATE. */
    StandardAsiaCorporate = "StandardAsiaCorporate",
    /** Matrix Transaction Type of STANDARD ASIA FINANCIAL CORPORATE. */
    StandardAsiaFinancialCorporate = "StandardAsiaFinancialCorporate",
    /** Matrix Transaction Type of STANDARD ASIA SOVEREIGN. */
    StandardAsiaSovereign = "StandardAsiaSovereign",
    /** Matrix Transaction Type of STANDARD AUSTRALIA CORPORATE. */
    StandardAustraliaCorporate = "StandardAustraliaCorporate",
    /** Matrix Transaction Type of STANDARD AUSTRALIA FINANCIAL CORPORATE. */
    StandardAustraliaFinancialCorporate = "StandardAustraliaFinancialCorporate",
    /** Matrix Transaction Type of STANDARD AUSTRALIA SOVEREIGN. */
    StandardAustraliaSovereign = "StandardAustraliaSovereign",
    /** Matrix Transaction Type of STANDARD EMERGING EUROPEAN AND MIDDLE EASTERN SOVEREIGN. */
    StandardEmergingEuropeanAndMiddleEasternSovereign = "StandardEmergingEuropeanAndMiddleEasternSovereign",
    /** Matrix Transaction Type of STANDARD EMERGING EUROPEAN CORPORATE. */
    StandardEmergingEuropeanCorporate = "StandardEmergingEuropeanCorporate",
    /** Matrix Transaction Type of STANDARD EMERGING EUROPEAN CORPORATE LPN. */
    StandardEmergingEuropeanCorporateLPN = "StandardEmergingEuropeanCorporateLPN",
    /** Matrix Transaction Type of STANDARD EMERGING EUROPEAN FINANCIAL CORPORATE. */
    StandardEmergingEuropeanFinancialCorporate = "StandardEmergingEuropeanFinancialCorporate",
    /** Matrix Transaction Type of STANDARD EMERGING EUROPEAN FINANCIAL CORPORATE LPN. */
    StandardEmergingEuropeanFinancialCorporateLPN = "StandardEmergingEuropeanFinancialCorporateLPN",
    /** Matrix Transaction Type of STANDARD EUROPEAN COCO FINANCIAL CORPORATE. */
    StandardEuropeanCoCoFinancialCorporate = "StandardEuropeanCoCoFinancialCorporate",
    /** Matrix Transaction Type of STANDARD EUROPEAN CORPORATE. */
    StandardEuropeanCorporate = "StandardEuropeanCorporate",
    /** Matrix Transaction Type of STANDARD EUROPEAN FINANCIAL CORPORATE. */
    StandardEuropeanFinancialCorporate = "StandardEuropeanFinancialCorporate",
    /** Matrix Transaction Type of STANDARD EUROPEAN SENIOR NON PREFERRED FINANCIAL CORPORATE. */
    StandardEuropeanSeniorNonPreferredFinancialCorporate = "StandardEuropeanSeniorNonPreferredFinancialCorporate",
    /** Matrix Transaction Type of STANDARD JAPAN CORPORATE. */
    StandardJapanCorporate = "StandardJapanCorporate",
    /** Matrix Transaction Type of STANDARD JAPAN FINANCIAL CORPORATE. */
    StandardJapanFinancialCorporate = "StandardJapanFinancialCorporate",
    /** Matrix Transaction Type of STANDARD JAPAN SOVEREIGN. */
    StandardJapanSovereign = "StandardJapanSovereign",
    /** Matrix Transaction Type of STANDARD LATIN AMERICA CORPORATE B. */
    StandardLatinAmericaCorporateBond = "StandardLatinAmericaCorporateBond",
    /** Matrix Transaction Type of STANDARD LATIN AMERICA CORPORATE BL. */
    StandardLatinAmericaCorporateBondOrLoan = "StandardLatinAmericaCorporateBondOrLoan",
    /** Matrix Transaction Type of STANDARD LATIN AMERICA FINANCIAL CORPORATE B. */
    StandardLatinAmericaFinancialCorporateBond = "StandardLatinAmericaFinancialCorporateBond",
    /** Matrix Transaction Type of STANDARD LATIN AMERICA FINANCIAL CORPORATE BL. */
    StandardLatinAmericaFinancialCorporateBondOrLoan = "StandardLatinAmericaFinancialCorporateBondOrLoan",
    /** Matrix Transaction Type of STANDARD LATIN AMERICA SOVEREIGN. */
    StandardLatinAmericaSovereign = "StandardLatinAmericaSovereign",
    /** Matrix Transaction Type of STANDARD NEW ZEALAND CORPORATE. */
    StandardNewZealandCorporate = "StandardNewZealandCorporate",
    /** Matrix Transaction Type of STANDARD NEW ZEALAND FINANCIAL CORPORATE. */
    StandardNewZealandFinancialCorporate = "StandardNewZealandFinancialCorporate",
    /** Matrix Transaction Type of STANDARD NEW ZEALAND SOVEREIGN. */
    StandardNewZealandSovereign = "StandardNewZealandSovereign",
    /** Matrix Transaction Type of STANDARD NORTH AMERICAN CORPORATE. */
    StandardNorthAmericanCorporate = "StandardNorthAmericanCorporate",
    /** Matrix Transaction Type of STANDARD NORTH AMERICAN FINANCIAL CORPORATE. */
    StandardNorthAmericanFinancialCorporate = "StandardNorthAmericanFinancialCorporate",
    /** Matrix Transaction Type of STANDARD SINGAPORE CORPORATE. */
    StandardSingaporeCorporate = "StandardSingaporeCorporate",
    /** Matrix Transaction Type of STANDARD SINGAPORE FINANCIAL CORPORATE. */
    StandardSingaporeFinancialCorporate = "StandardSingaporeFinancialCorporate",
    /** Matrix Transaction Type of STANDARD SINGAPORE SOVEREIGN. */
    StandardSingaporeSovereign = "StandardSingaporeSovereign",
    /** Transaction Type of STANDARD SUBORDINATED EUROPEAN INSURANCE CORPORATE. */
    StandardSubordinatedEuropeanInsuranceCorporate = "StandardSubordinatedEuropeanInsuranceCorporate",
    /** Matrix Transaction Type of STANDARD SUKUK FINANCIAL CORPORATE. */
    StandardSukukFinancialCorporate = "StandardSukukFinancialCorporate",
    /** Matrix Transaction Type of STANDARD U.S. MUNICIPAL FULL FAITH AND CREDIT. */
    StandardUSMunicipalFullFaithAndCredit = "StandardUSMunicipalFullFaithAndCredit",
    /** Matrix Transaction Type of STANDARD U.S. MUNICIPAL GENERAL FUND. */
    StandardUSMunicipalGeneralFund = "StandardUSMunicipalGeneralFund",
    /** Matrix Transaction Type of STANDARD U.S. MUNICIPAL REVENUE. */
    StandardUSMunicipalRevenue = "StandardUSMunicipalRevenue",
    /** Matrix Transaction Type of STANDARD WESTERN EUROPEAN SOVEREIGN. */
    StandardWesternEuropeanSovereign = "StandardWesternEuropeanSovereign",
    /** Matrix Transaction Type of SUBORDINATED EUROPEAN INSURANCE CORPORATE. */
    SubordinatedEuropeanInsuranceCorporate = "SubordinatedEuropeanInsuranceCorporate",
    /** Matrix Transaction Type of SUKUK CORPORATE. */
    SukukCorporate = "SukukCorporate",
    /** Matrix Transaction Type of SUKUK FINANCIAL CORPORATE. */
    SukukFinancialCorporate = "SukukFinancialCorporate",
    /** Matrix Transaction Type of SUKUK SOVEREIGN. */
    SukukSovereign = "SukukSovereign",
    /** Matrix Transaction Type of U.S. MUNICIPAL FULL FAITH AND CREDIT. */
    USMunicipalFullFaithAndCredit = "USMunicipalFullFaithAndCredit",
    /** Matrix Transaction Type of U.S. MUNICIPAL GENERAL FUND. */
    USMunicipalGeneralFund = "USMunicipalGeneralFund",
    /** Matrix Transaction Type of U.S. MUNICIPAL REVENUE. */
    USMunicipalRevenue = "USMunicipalRevenue",
    /** Matrix Transaction Type of WESTERN EUROPEAN SOVEREIGN. */
    WesternEuropeanSovereign = "WesternEuropeanSovereign",
};
/**
* The enumerated values to specify the identification the form of applicable matrix.
*/
export enum MatrixTypeEnum {
    
    /** The ISDA-published Credit Derivatives Physical Settlement Matrix. */
    CreditDerivativesPhysicalSettlementMatrix = "CreditDerivativesPhysicalSettlementMatrix",
    /** The ISDA-published Equity Derivatives Matrix. */
    EquityDerivativesMatrix = "EquityDerivativesMatrix",
    /** The ISDA-published 2000 ISDA Definitions Settlement Matrix for Early Terminations and Swaptions. */
    SettlementMatrix = "SettlementMatrix",
};
/**
* The enumerated values to specify the type of a resource (e.g. document).
*/
export enum ResourceTypeEnum {
    
    /** Document describing the legal terms of a transaction. */
    Confirmation = "Confirmation",
    /** Document providing supplemental material economic terms to the FpML data representation. The initial intended usage is to fulfill the CFTC Part 45 rule requirement to report ‘Any other terms(s) of the swap matched or affirmed by the counterparties in verifying the swap’ when the reporting is done via the generic FpML representation. */
    SupplementalMaterialEconomicTerms = "SupplementalMaterialEconomicTerms",
    /** Document describing the economic characteristics of a transaction. */
    TermSheet = "TermSheet",
};
/**
* The enumerated values to specify the legal agreement type.
*/
export enum LegalAgreementTypeEnum {
    
    /** A Broker Confirmation. */
    BrokerConfirmation = "BrokerConfirmation",
    /** A Transaction Confirmation. */
    Confirmation = "Confirmation",
    /** A Credit Support Agreement. */
    CreditSupportAgreement = "CreditSupportAgreement",
    /** A Master Agreement. */
    MasterAgreement = "MasterAgreement",
    /** A Master Confirmation. */
    MasterConfirmation = "MasterConfirmation",
    /** A Security Agreement related to a Collateral Transfer Agreement (CTA). */
    SecurityAgreement = "SecurityAgreement",
    /** Another type of agreement. */
    Other = "Other",
};
/**
* The enumerated values to specify the legal agreement publisher.
*/
export enum LegalAgreementPublisherEnum {
    
    /** Association Française des Banques. */
    AFB = "AFB",
    /** BNY Mellon */
    BNYM = "BNYM",
    /** ISDA and Clearstream */
    ISDAClearstream = "ISDAClearstream",
    /** Emerging Markets Traders Association */
    EMTA = "EMTA",
    /** The Foreign Exchange Committee */
    TheFXCommittee = "TheFXCommittee",
    /** ISDA and Euroclear */
    ISDAEuroclear = "ISDAEuroclear",
    /** International Swaps and Derivatives Association, Inc. */
    ISDA = "ISDA",
    /** International Securities Lending Association */
    ISLA = "ISLA",
    /** JP Morgan */
    JPMorgan = "JPMorgan",
    /** International Capital Markets Association */
    ICMA = "ICMA",
};
/**
* The enumerated values to specify the Execution Location of a Security Agreement
*/
export enum ExecutionLocationEnum {
    
    /** The Agreement was executed outside of Belgium */
    ExecutedOutsideBelgium = "ExecutedOutsideBelgium",
    /** The Agreement was executed outside of Belgium */
    ExecutedInBelgium = "ExecutedInBelgium",
    /** An alternative approach is described in the document as follows. */
    OtherLocation = "OtherLocation",
};
/**
* The enumerated values to specify the Credit Support Document Terms
*/
export enum CreditSupportDocumentTermsEnum {
    
    /** A specified Credit Support Document is provided */
    Specified = "Specified",
    /** Any guarantee, collateral arrangement and/or other agreement or arrangement which provides for credit support with respect to the party’s obligations under this Agreement. */
    Any = "Any",
    /** No Credit Support Document is specified. */
    None = "None",
};
/**
* The enumerated values to specify the Credit Support Provider Terms
*/
export enum CreditSupportProviderTermsEnum {
    
    /** A specified Credit Support Provider is provided */
    Specified = "Specified",
    /** Any party or parties who now or in the future may provide a Credit Support Document or other form of credit support. */
    Any = "Any",
    /** No Credit Support Provider is specified. */
    None = "None",
};
/**
* The enumerated values to specify the Event of Default or Termination event for which Specified Entities terms are being defined.
*/
export enum SpecifiedEntityClauseEnum {
    
    DefaultUnderSpecifiedTransaction = "DefaultUnderSpecifiedTransaction",
    CrossDefault = "CrossDefault",
    Bankruptcy = "Bankruptcy",
    CreditEventUponMerger = "CreditEventUponMerger",
};
/**
* The enumerated values to specify the specified entity terms for the Event of Default or Termination Event specified.
*/
export enum SpecifiedEntityTermsEnum {
    
    /** No Specified Entity is provided */
    None = "None",
    /** Any Affiliate is a Specified Entity. */
    AnyAffiliate = "AnyAffiliate",
    /** The Specified Entity is provided. */
    NamedSpecifiedEntity = "NamedSpecifiedEntity",
    /** Any Material Subsidiary. */
    MaterialSubsidiary = "MaterialSubsidiary",
    /** Non standard Specified Entity terms are provided. */
    OtherSpecifiedEntity = "OtherSpecifiedEntity",
};

export enum TerminationCurrencyConditionEnum {
    
    /** A currency in which payments would be due under one or more Transactions. */
    PaymentsDue = "PaymentsDue",
    /** A currency that is freely available. */
    FreelyAvailable = "FreelyAvailable",
    /** A currency in which payments would be due under one or more Transactions and that is freely available. */
    PaymentsDueAndFreelyAvailable = "PaymentsDueAndFreelyAvailable",
    /** Termination Currency Conditions are specified. */
    Specified = "Specified",
};

export enum UmbrellaPartyRoleEnum {
    
    /** Represents a Contractual Party who has authority to negotiate, execute and deliver confirmations on behalf of each unique party to the agreement that is linked to the Agent. */
    Agent = "Agent",
    /** Represents a Contractual Party who has been authorised to act as a centralised authority empowered to negotiate, execute and manage transactions on behalf of multiple affiliated funds or accounts. */
    InvestmentManager = "InvestmentManager",
    /** Represents a Contractual Party that enters into and assumes direct responsibility for transactions. */
    Principal = "Principal",
    /** Represents an individual trading party, fund, portfolio or managed account associated to a principal, Investment Manager or Agent Contractual Party. */
    SubAccount = "SubAccount",
    /** Represents a distinct trading strategy, portfolio, or sub account managed within a broader legal entity or fund. It is not a legal party to the agreement. */
    Sleeve = "Sleeve",
};
/**
* Values to specify the procedure under which the market value of posted collateral will be recalculated.
*/
export enum RecalculationOfValueElectionEnum {
    
    /** The parties agree to consult. */
    ConsulationProcedure = "ConsulationProcedure",
    /** The procedures specified in an Other Regulatory CSA. */
    OtherRegulatoryCSAProcedure = "OtherRegulatoryCSAProcedure",
    /** Description to be added. */
    NotApplicable = "NotApplicable",
    /** Bespoke Recalculation of value terms are specified in the agreement. */
    Specified = "Specified",
};
/**
* Values to specify the SIMM normalized exception approaches.
*/
export enum SimmExceptionApplicableEnum {
    
    /** The ISDA Standard Initial Margin Model exception is applicable as a Fallback to Mandatory Method. */
    FallBackToMandatoryMethod = "FallBackToMandatoryMethod",
    /** The ISDA Standard Initial Margin Model exception is applicable as a Mandatory Method. */
    MandatoryMethod = "MandatoryMethod",
    /** An alternative approach is described in the document. */
    OtherMethod = "OtherMethod",
};
/**
* Values to specify the normalized exceptions applicable to an Initial Margin CSA.
*/
export enum ExceptionEnum {
    
    /** The election is applicable. */
    Applicable = "Applicable",
    /** The election is not applicable. */
    NotApplicable = "NotApplicable",
    /** An alternative approach is described in the document as follows. */
    Other = "Other",
};
/**
* Values to specify the methodology according to which sensitivities to (i) equity indices, funds and ETFs, and (ii) commodity indices are computed.
*/
export enum SensitivitiesEnum {
    
    /** The parties agree that in respect of the relevant sensitivities, the delta is allocated back to individual constituents. */
    Alternative = "Alternative",
    /** The relevant sensitivities are addressed by the standard preferred approach where the entire delta is put into the applicable asset class/category. */
    Standard = "Standard",
};
/**
* Values to specify the regulatory regimes.
*/
export enum RegulatoryRegimeEnum {
    
    /** Australian Prudential Standard CPS 226 Margining and risk mitigation for non-centrally cleared derivatives. */
    AustraliaMarginRules = "AustraliaMarginRules",
    /** Guideline E-22, Margin Requirements for Non-Centrally Cleared Derivatives issued by the Canadian Office of the Superintendent of Financial Institutions in February 2016. */
    CanadaMarginRules = "CanadaMarginRules",
    /** Margin requirements adopted by the U.S. Commodity Futures Trading Commission pursuant to CEA  4s(e). */
    CFTC_MarginRules = "CFTC_MarginRules",
    /** Regulation (EU) No 648/2012 of the European Parliament and of the Council of 4 July 2012 on OTC derivatives, central counterparties and trade repositories (including the EMIR RTS, which means the published regulatory technical standards on risk-mitigation techniques for OTC-derivative contracts not cleared by a CCP under Article 11(15) of EMIR). */
    EMIR_MarginRules = "EMIR_MarginRules",
    /** Chapter CR-G-14 'Non-centrally Cleared OTC Derivatives Transactions  Margin and Other Risk Mitigation Standards' in the Banking Supervisory Policy Manual issued by the Hong Kong Monetary Authority. */
    HongKongMarginRules = "HongKongMarginRules",
    /** Margin rules adopted by the Financial Services Agency of Japan pursuant to Article 40, Item 2 of the Financial Instruments and Exchange Act (kinyuu shouhin torihiki hou) (Act No. 25 of 1948) and by the Ministry of Agriculture, Forestry and Fisheries and the Ministry of Economy, Trade and Industry pursuant to the Commodity Derivatives Act (shouhin sakimono torihiki hou) (Act No. 239 of 1950) (including their subordinated regulations and the related supervisory guidelines). */
    JapanMarginRules = "JapanMarginRules",
    /** Margin requirements adopted by a 'prudential regulator' (as defined in CEA  1a(39)) pursuant to CEA  4s(e) and Exchange Act  15F(e). */
    US_PrudentialMarginRules = "US_PrudentialMarginRules",
    /** Margin requirements adopted by the U.S. Securities and Exchange Commission pursuant to Exchange Act  15F(e). */
    SEC_MarginRules = "SEC_MarginRules",
    /** Guidelines on Margin Requirements for Non-centrally Cleared OTC Derivatives Contracts issued by the Monetary Authority of Singapore (MAS) pursuant to section 321 of the Securities and Futures Act, Chapter 289 of Singapore. */
    SingaporeMarginRules = "SingaporeMarginRules",
    /** Margin rules adopted by the Swiss Federal Council pursuant to Article 110-111 of the Financial Market Infrastructure Act as well as Articles 100 to 107 and Annexes 3 to 5 of the Financial Market Infrastructure Ordinance. */
    SwitzerlandMarginRules = "SwitzerlandMarginRules",
    /** The Central Bank of Brazil margin requirements adopted pursuant to Resolution no. 4,662, of 25 May 2018, enacted by the National Monetary Council. */
    BrazilMarginRules = "BrazilMarginRules",
    /** EMIR (including, for the avoidance of doubt, the EMIR RTS) as it forms part of UK domestic law by virtue of section 3 of the European Union (Withdrawal) Act 2018 (as amended) (the EUWA) (including any amendments made to such legislation when it is brought into UK domestic law pursuant to section 8 of the EUWA or any regulations made thereunder), and which, for the avoidance of doubt, shall be subject to the interpretation provision in Paragraph [11(g)].3. */
    UnitedKingdomMarginRules = "UnitedKingdomMarginRules",
    /** the requirements contained in Joint Standard 2 of 2020 made in terms of the South African Financial Sector Regulation Act, 2017. */
    SouthAfricaMarginRules = "SouthAfricaMarginRules",
    /** Margin requirements adopted by the Korean Financial Services Commission and Financial Supervisory Service pursuant to the Guidelines on Margin Requirements for Non-Centrally Cleared OTC Derivatives Transactions, which are expected to be incorporated into the Financial Investment Services and Capital Markets Act. */
    SouthKoreaMarginRules = "SouthKoreaMarginRules",
    /** Part III of Schedule 10 to Code of Conduct for Persons Licensed by or Registered with the Securities and Futures Commission of Hong Kong. */
    HongKongSFCMarginRules = "HongKongSFCMarginRules",
};
/**
* Values to specify the margin approach specific to Initial Margin agreements.
*/
export enum MarginApproachEnum {
    
    /** Distinct Margin Flow (IM) Approach. */
    Distinct = "Distinct",
    /** Allocated Margin Flow (IM/IA) Approach. */
    Allocated = "Allocated",
    /** Greater of Margin Flow (IM/IA) Approach. */
    GreaterOf = "GreaterOf",
};
/**
* Values to specify the interest adjustment periodicity election through standard language.
*/
export enum InterestAdjustmentPeriodicityEnum {
    
    /** The interest adjustment takes place on the last local business day of each calendar month. */
    LastLocalBusinessDayOfMonth = "LastLocalBusinessDayOfMonth",
    /** The interest adjustment takes place each day. */
    EachDay = "EachDay",
};
/**
* The enumerated values to specify the instances where the independent amount eligible collateral is not defined as a set of eligible collateral assets.
*/
export enum IndependentAmountEligibilityEnum {
    
    /** None. */
    None = "None",
    /** None, unless otherwise specified in a Confirmation. */
    NoneUnlessSpecifiedInConfirmation = "NoneUnlessSpecifiedInConfirmation",
};
/**
* The enumerated values to specify condition(s) required by a party from the other party to hold its posted collateral.
*/
export enum HoldingPostedCollateralEnum {
    
    /** The custodian is acceptable to the other party to the agreement. */
    AcceptableCustodian = "AcceptableCustodian",
};

export enum CollateralAssetDefinitionsEnum {
    
    /** Australian Dollar (AUD) Cash. */
    AU_CASH = "AU_CASH",
    /** Australian Government Securities Capital-Indexed Bonds. */
    AU_CIB = "AU_CIB",
    /** Australian Semi-Government Securities Fixed Interest Bonds. */
    AU_FIB = "AU_FIB",
    /** Australian Government Securities Fixed Rate Bonds. */
    AU_FRB = "AU_FRB",
    /** Australian Semi-Government Securities Index Linked Bonds. */
    AU_ILB = "AU_ILB",
    /** Australian Government Securities Treasury Notes. */
    AU_NOTE = "AU_NOTE",
    /** Australian Semi-Government Securities Treasury Notes. */
    AU_STATENOTE = "AU_STATENOTE",
    /** Australian Government Securities Treasury Adjustable Rate Bonds. */
    AU_TAB = "AU_TAB",
    /** BEL20 Equity Securities. */
    BE_BEL20 = "BE_BEL20",
    /** Belgian Treasury Certificates. */
    BE_CERT = "BE_CERT",
    /** Belgian Linear Obligations. */
    BE_LINEAR = "BE_LINEAR",
    /** Belgian Treasury notes. */
    BE_NOTE = "BE_NOTE",
    /** Public sector issues guaranteed by Regional Authorities. */
    BE_REGIONGT = "BE_REGIONGT",
    /** Public sector issues guaranteed by the Belgian State. */
    BE_STATEGT = "BE_STATEGT",
    /** Belgian State Loans. */
    BE_STATELOAN = "BE_STATELOAN",
    /** Canada Bonds. */
    CA_BOND = "CA_BOND",
    /** Canadian Dollar (CAD) Cash. */
    CA_CASH = "CA_CASH",
    /** Government of Canada Real Return Bonds. */
    CA_RRB = "CA_RRB",
    /** Government of Canada Treasury Bills. */
    CA_TBILL = "CA_TBILL",
    /** Public Authority Bond. */
    CH_CANTON = "CH_CANTON",
    /** Swiss Franc (CHF) Cash. */
    CH_CASH = "CH_CASH",
    /** Federal Bond. */
    CH_FEDBOND = "CH_FEDBOND",
    /** Unverzinsliche Schatzanweisungen (Bills). */
    DE_BILL = "DE_BILL",
    /** Bundesanleihen (Bonds). */
    DE_BOND = "DE_BOND",
    /** Negotiable Debt Obligations issued by or taken over and since serviced and managed by the Erblasttilgungsfond (Redemption Fund for Inherited Liabilities) backed by Federal Republic of Germany, including but not limited to former issues of the Treuhandanstalt, the Bundesbahn, the Bundespost, the Economic Recovery Program (ERP), the privatised Federal Railway (Bahn AG), the telecommunications element of the Federal Post Office (Telekom) and the German Unity Fund. */
    DE_ERBLAST = "DE_ERBLAST",
    /** Kommunalschuldverschreib\tungen (Municipal Bonds). */
    DE_MUNI = "DE_MUNI",
    /** Bundesschatzanweisungen (Notes). */
    DE_NOTE2 = "DE_NOTE2",
    /** Bundesobligationen (Notes). */
    DE_NOTE5_5 = "DE_NOTE5_5",
    /** Hypothekenpfandbriefe (Mortgage Bonds). */
    DE_PFAND = "DE_PFAND",
    /** Skatkammerbeviser (Treasury Bills). */
    DK_BILL = "DK_BILL",
    /** BoligX obligationer. */
    DK_BOLIGX = "DK_BOLIGX",
    /** Statsobligationer (Government Bonds). */
    DK_BOND = "DK_BOND",
    /** Callable Mortgage Bonds. */
    DK_CALLMORT = "DK_CALLMORT",
    /** Danish Krone (DKK) Cash. */
    DK_CASH = "DK_CASH",
    /** KFX Equity Securities. */
    DK_KFX = "DK_KFX",
    /** Non-callable Mortgage Bonds. */
    DK_MORT = "DK_MORT",
    /** Statsgaeldsbeviser (Treasury Notes). */
    DK_NOTE = "DK_NOTE",
    /** Treasury Bills - Letras del Tesoro. */
    ES_BILL = "ES_BILL",
    /** Public Government Debt. */
    ES_BOND = "ES_BOND",
    /** Cedulas. */
    ES_CEDULAS = "ES_CEDULAS",
    /** Corporate Bonds. */
    ES_CORP = "ES_CORP",
    /** Equity securities issued by a Spanish company, and listed as an IBEX 35 constituent company as reported by the Sociedad de Bolsas, each share representing the minimum unit of participation of a shareholder in the stock capital of the company. */
    ES_EQUITY = "ES_EQUITY",
    /** Euro (EUR) Cash. */
    EU_CASH = "EU_CASH",
    /** FTSE Euro 100 Index Equity Securities. */
    EU_EURO100 = "EU_EURO100",
    /** FTSE Eurotop 300 Index Equity Securities. */
    EU_EUROTOP300 = "EU_EUROTOP300",
    /** EuroSTOXX 50 Index Equity Securities. */
    EU_STOXX50 = "EU_STOXX50",
    /** STOXX 600 Index Equity Securities. */
    EU_STOXX600 = "EU_STOXX600",
    /** Treasury bills. */
    FI_BILL = "FI_BILL",
    /** Serial bonds (Finnish Government Bond). */
    FI_BOND = "FI_BOND",
    /** HEX Equity Securities. */
    FI_HEX = "FI_HEX",
    /** Commercial Paper: (Billet de Trsorerie). */
    FR_BDT = "FR_BDT",
    /** Treasury Notes: Bons du Trsor  Taux Annuel (BTAN). */
    FR_BTAN = "FR_BTAN",
    /** Treasury Bills: Bons du Trsor  Taux Fixe (BTF). */
    FR_BTF = "FR_BTF",
    /** Government bonds: Obligations Assimilables du Trsor (OAT). */
    FR_OAT = "FR_OAT",
    /** STRIPS. */
    FR_STRIP = "FR_STRIP",
    /** Generally Accepted Australian Government Obligations. */
    GA_AU_GOV = "GA_AU_GOV",
    /** Generally Accepted Belgian Government Obligations. */
    GA_BE_GOV = "GA_BE_GOV",
    /** Generally Accepted Canadian Government Obligations. */
    GA_CA_GOV = "GA_CA_GOV",
    /** Generally Accepted Swiss Government Obligations. */
    GA_CH_GOV = "GA_CH_GOV",
    /** Generally Accepted German Government Obligations. */
    GA_DE_GOV = "GA_DE_GOV",
    /** Generally Accepted Danish Government Obligations. */
    GA_DK_GOV = "GA_DK_GOV",
    /** Generally Accepted Spanish Government Obligations. */
    GA_ES_GOV = "GA_ES_GOV",
    /** Generally Accepted Euro Zone Government Securities. */
    GA_EUROZONE_GOV = "GA_EUROZONE_GOV",
    /** Generally Accepted EU Member State Government Securities. */
    GA_EU_GOV = "GA_EU_GOV",
    /** Generally Accepted Finnish Government Obligations. */
    GA_FI_GOV = "GA_FI_GOV",
    /** Generally Accepted French Government Obligations. */
    GA_FR_GOV = "GA_FR_GOV",
    /** Generally Accepted G5 Government Obligations. */
    GA_G5_GOV = "GA_G5_GOV",
    /** Generally Accepted British Government Obligations. */
    GA_GB_GOV = "GA_GB_GOV",
    /** Generally Accepted Hong Kong Government Obligations. */
    GA_HK_GOV = "GA_HK_GOV",
    /** Generally Accepted Italian Government Obligations. */
    GA_IT_GOV = "GA_IT_GOV",
    /** Generally Accepted Japanese Government Obligations. */
    GA_JP_GOV = "GA_JP_GOV",
    /** Generally Accepted Korean Government Obligations. */
    GA_KR_GOV = "GA_KR_GOV",
    /** Generally Accepted Netherlands Government Obligations. */
    GA_NL_GOV = "GA_NL_GOV",
    /** Generally Accepted Norwegian Government Obligations. */
    GA_NO_GOV = "GA_NO_GOV",
    /** Generally Accepted New Zealand Government Obligations. */
    GA_NZ_GOV = "GA_NZ_GOV",
    /** Generally Accepted Swedish Government Obligations. */
    GA_SE_GOV = "GA_SE_GOV",
    /** Generally Accepted Singaporean Government Obligations. */
    GA_SG_GOV = "GA_SG_GOV",
    /** Generally Accepted US Agency Obligations. */
    GA_US_AGENCY = "GA_US_AGENCY",
    /** Generally Accepted US Government Obligations. */
    GA_US_GOV = "GA_US_GOV",
    /** Generally Accepted US Mortgage-Backed Obligations. */
    GA_US_MORTGAGES = "GA_US_MORTGAGES",
    /** British Pound Sterling (GBP) Cash. */
    GB_CASH = "GB_CASH",
    /** Double-dated Gilts. */
    GB_DDGILT = "GB_DDGILT",
    /** FTSE 100 Equity Securities. */
    GB_FT100 = "GB_FT100",
    /** FTSE 250 Equity Securities. */
    GB_FT250 = "GB_FT250",
    /** FTSE 350 Equity Securities. */
    GB_FT350 = "GB_FT350",
    /** Conventional Gilts. */
    GB_GILT = "GB_GILT",
    /** Index-Linked Gilts. */
    GB_INDEXGILT = "GB_INDEXGILT",
    /** Undated or Perpetual Gilts. */
    GB_PERPGILT = "GB_PERPGILT",
    /** Rump Stock. */
    GB_RUMPGILT = "GB_RUMPGILT",
    /** Bank of England Euro Bills. */
    GB_SUPR1 = "GB_SUPR1",
    /** Bank of England Euro Notes. */
    GB_SUPR2 = "GB_SUPR2",
    /** UK Treasury Bills. */
    GB_TBILL = "GB_TBILL",
    /** Gilt Strips or Zero Coupon Gilts. */
    GB_ZEROGILT = "GB_ZEROGILT",
    /** Hong Kong Government Exchange Fund Bills. */
    HK_BILL = "HK_BILL",
    /** Hong Kong Dollar (HKD) Cash. */
    HK_CASH = "HK_CASH",
    /** Hong Kong Government Exchange Fund Notes. */
    HK_NOTE = "HK_NOTE",
    /** Botbuoni Ordinari del Tesoro (BOT) zero coupon debt securities issued by the Italian Treasury with maturities up to 365 days. */
    IT_BOT = "IT_BOT",
    /** Buoni del Tesoro Poliennali fixed interest semi-annual debt securities issued by the Italian Treasury with original maturities between 3 and 30 years. */
    IT_BTP = "IT_BTP",
    /** Certificati di Credito del Tesoro a Cedola Variable (CCT) or floating rate interest bearing debt securities issued by the Italian Treasury. */
    IT_CCT = "IT_CCT",
    /** Corporate bonds. */
    IT_CORP = "IT_CORP",
    /** Certificati del Tesoro zero coupon debt securities issued by the Italian Treasury with maturities between 18 and 24 months. */
    IT_CTZ = "IT_CTZ",
    /** MIB30 Equity Securities. */
    IT_MIB30 = "IT_MIB30",
    /** Debt securities issued and marketed by the Republic of Italy outside the Italian market, traded as Eurobonds. */
    IT_REP = "IT_REP",
    /** Japanese Yen (JPY) Cash. */
    JP_CASH = "JP_CASH",
    /** Corporate bonds including straight bonds. */
    JP_CORPORATE = "JP_CORPORATE",
    /** Commercial Paper. */
    JP_CP = "JP_CP",
    /** Equity securities issued by a Japanese company, each share representing the minimum unit of participation of a partner in the stock capital of the company. */
    JP_EQUITY = "JP_EQUITY",
    /** Yen-denominated foreign bonds. */
    JP_EUROBOND = "JP_EUROBOND",
    /** Japanese Government Bonds. */
    JP_JGB = "JP_JGB",
    /** Korean Treasury Bonds. */
    KR_BOND = "KR_BOND",
    /** Korean Won (KRW) Cash. */
    KR_CASH = "KR_CASH",
    /** Non Korean Won denominated Export-Import Bank of Korea bonds. */
    KR_EXIM = "KR_EXIM",
    /** Korean Development Insurance Corporation Bonds (Korean Won denominated). */
    KR_KDICKRW = "KR_KDICKRW",
    /** Non-Korean Won denominated Korea Development Bank bonds (KDBs). */
    KR_KDR = "KR_KDR",
    /** KEPCO bonds. */
    KR_KEPCO = "KR_KEPCO",
    /** Monetary Stabilisation Bonds. */
    KR_MSB = "KR_MSB",
    /** Non Korean Won denominated Korea National Housing Corporation bonds (KNHCs). */
    KR_NHC = "KR_NHC",
    /** Non-Korean Won denominated Republic of Korea bonds (ROKs). */
    KR_ROK = "KR_ROK",
    /** AEX Equity Securities. */
    NL_AEX = "NL_AEX",
    /** Dutch Treasury Certificates. */
    NL_BILL = "NL_BILL",
    /** Dutch State Loans. */
    NL_BOND = "NL_BOND",
    /** Norwegian Government Bonds. */
    NO_BOND = "NO_BOND",
    /** Norwegian Krone (NOK) Cash. */
    NO_CASH = "NO_CASH",
    /** OBX Equity Securities. */
    NO_OBX = "NO_OBX",
    /** Norwegian T-Bills. */
    NO_TBILL = "NO_TBILL",
    /** New Zealand Government Bonds. */
    NZ_BOND = "NZ_BOND",
    /** New Zealand Dollar (NZD) Cash. */
    NZ_CASH = "NZ_CASH",
    /** New Zealand Government Treasury Bills. */
    NZ_TBILL = "NZ_TBILL",
    /** Swedish Krona (SEK) Cash. */
    SE_CASH = "SE_CASH",
    /** Swedish Government Bonds (SGB). */
    SE_GOVT = "SE_GOVT",
    /** Swedish Index Linked Government bonds. */
    SE_ILGOVT = "SE_ILGOVT",
    /** Swedish Mortgage Bonds. */
    SE_MORT = "SE_MORT",
    /** OMX Equity Securities. */
    SE_OMX = "SE_OMX",
    /** Swedish Treasury Bills (STB). */
    SE_TBILL = "SE_TBILL",
    /** Singapore Government (SGS) Bonds. */
    SG_BOND = "SG_BOND",
    /** Singapore Dollar (SGD) Cash. */
    SG_CASH = "SG_CASH",
    /** Singapore Government T-Bills (T-Bills). */
    SG_TBILL = "SG_TBILL",
    /** Inter-American Development Bank Bonds. */
    SU_IADB = "SU_IADB",
    /** International Bank for Reconstruction and Development (World Bank) Discount Notes. */
    SU_IBRDDN = "SU_IBRDDN",
    /** International Bank for Reconstruction and Development (World Bank or IBRD) Global Benchmark Bonds. */
    SU_IBRDGB = "SU_IBRDGB",
    /** Adjustable Rate Mortgage (ARM) Bonds. */
    US_ARM = "US_ARM",
    /** United States of America Dollar (USD) Cash. */
    US_CASH = "US_CASH",
    /** REMICs, CMOs and other derivative structures. */
    US_DERIV = "US_DERIV",
    /** Dow Jones Industrial Average Equity Securities. */
    US_DOW = "US_DOW",
    /** Dow Jones Composite Average Equity Securities. */
    US_DOW_COMP = "US_DOW_COMP",
    /** Dow Jones Transportation\tAverage Equity Securities. */
    US_DOW_TRAN = "US_DOW_TRAN",
    /** Dow Jones Utilities Average Equity Securities. */
    US_DOW_UTIL = "US_DOW_UTIL",
    /** Federal Agricultural Mortgage Corp (Farmer Mac) Bonds. */
    US_FAMC = "US_FAMC",
    /** Farm Credit System (FCS) Bonds. */
    US_FCS = "US_FCS",
    /** Farm Credit System Financial Assistance Corporation (FCSFAC) Bonds. */
    US_FCSFAC = "US_FCSFAC",
    /** Callable Agency Debt  Federal Home Loan Bank (FHLB). */
    US_FHLB = "US_FHLB",
    /** Non-Callable Federal Home Loan Bank Debt. */
    US_FHLBNC = "US_FHLBNC",
    /** Non-Callable Federal Home Loan Bank Discount Notes. */
    US_FHLBNCDN = "US_FHLBNCDN",
    /** Callable Agency Debt  the Federal Home Loan Mortgage Corporation (FHLMC or Freddie Mac). */
    US_FHLMC = "US_FHLMC",
    /** Federal Home Loan Mortgage Corporation Certificates  Mortgage Backed Securities. */
    US_FHLMCMBS = "US_FHLMCMBS",
    /** Financing Corp (FICO) Bonds. */
    US_FICO = "US_FICO",
    /** Callable Agency Debt  Federal National Mortgage Association (FNMA or Fannie Mae). */
    US_FNMA = "US_FNMA",
    /** Federal National Mortgage Association Certificates  Mortgage Backed Securities. */
    US_FNMAMBS = "US_FNMAMBS",
    /** Callable Agency Debt  Government National Mortgage Association (GNMA). */
    US_GNMA = "US_GNMA",
    /** Government National Mortgage Association Certificates  Mortgage Backed Securities (GNMA or Ginnie Mae). */
    US_GNMAMBS = "US_GNMAMBS",
    /** Lehman Brothers Credit Bond Index Debt Securities. */
    US_LEHM_BOND = "US_LEHM_BOND",
    /** NASDAQ-100 Index Equity Securities. */
    US_NAS_100 = "US_NAS_100",
    /** NASDAQ Composite Index Equity Securities. */
    US_NAS_COMP = "US_NAS_COMP",
    /** Non-Callable Agency Debt  Various Issuers. */
    US_NCAD = "US_NCAD",
    /** Non-Callable Agency Discount Notes  Various Issuers. */
    US_NCADN = "US_NCADN",
    /** NYSE Composite Index Equity Securities. */
    US_NYSE_COMP = "US_NYSE_COMP",
    /** Resolution Funding Corp (REFCorp) Bonds. */
    US_REFCORP = "US_REFCORP",
    /** Student Loan Marketing Association (Sallie Mae) Bonds. */
    US_SLMA = "US_SLMA",
    /** US Treasury Strips. */
    US_STRIP = "US_STRIP",
    /** Standard & Poors 100 Index Equity Securities. */
    US_S_P100 = "US_S_P100",
    /** Standard & Poors Midcap 400 Equity Securities. corporations that are included within the Standard And Poor's Midcap 400 Index published by Standard And Poor's, a division of The McGraw-Hill Companies, Inc. */
    US_S_P400 = "US_S_P400",
    /** Standard & Poors 500 Index Equity Securities. */
    US_S_P500 = "US_S_P500",
    /** Standard & Poors Smallcap 600 Index Equity Securities. */
    US_S_P600 = "US_S_P600",
    /** US Treasury Bills. */
    US_TBILL = "US_TBILL",
    /** US Treasury Bonds. */
    US_TBOND = "US_TBOND",
    /** US Treasury Inflation Protected Issues (TIPS). */
    US_TIPS = "US_TIPS",
    /** US Treasury Notes. */
    US_TNOTE = "US_TNOTE",
    /** Tennessee Valley Authority (TVA) Bonds. */
    US_TVA = "US_TVA",
};
/**
* The enumerated values to specify the effective date of the Amendment to Termination Currency when specified as a specific date (e.g. the annex date).
*/
export enum AmendmentEffectiveDateEnum {
    
    /** The effective date corresponds to the Annex date. */
    AnnexDate = "AnnexDate",
    /** The effective date corresponds to the Deed date. */
    DeedDate = "DeedDate",
    /** The effective date corresponds to the Agreement date. */
    AgreementDate = "AgreementDate",
    /** The effective date corresponds to the Amendment Effective Date (IM). */
    AmendmentEffectiveDate = "AmendmentEffectiveDate",
};
/**
* The enumerated values to specify the Additional Type of transaction that can require the collection or delivery of initial margin under a given regulatory regime for the purposes of Covered Transactions.
*/
export enum AdditionalTypeEnum {
    
    /** No Additional Type of transaction is applicable to the regulatory regulatory regime. */
    NotApplicable = "NotApplicable",
    /** Single stock equity option or index option transaction as referred to in the transitional provisions (if any) of the EMIR RTS. */
    EquityOptionOrIndexOption = "EquityOptionOrIndexOption",
    Other = "Other",
};
/**
* The enumerated values to specify an elective amount.
*/
export enum ElectiveAmountEnum {
    
    /** The elective amount is zero. */
    Zero = "Zero",
    /** The elective amount has no upper limit. */
    Unlimited = "Unlimited",
};
/**
* The enumerated values applicable for Security Interest for Obligations.
*/
export enum SecurityInterestObligationsEnum {
    
    /** Security Interest for Obligations is not applicable to the Party. */
    NotApplicable = "NotApplicable",
    /** All Obligations of Party A. */
    AllObligationsOfPartyA = "AllObligationsOfPartyA",
    /** All Obligations of Party B. */
    AllObligationsOfPartyB = "AllObligationsOfPartyB",
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    Other = "Other",
};
/**
* The enumerated values applicable for Security Interest for Obligations Obligee.
*/
export enum SecurityInterestObligeeEnum {
    
    /** Party A. */
    PartyA = "PartyA",
    /** Party A or any Affiliate of Party A. */
    PartyAOrAffiliate = "PartyAOrAffiliate",
    /** Party B. */
    PartyB = "PartyB",
    /** Party B or any Affiliate of Party B. */
    PartyBOrAffiliate = "PartyBOrAffiliate",
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    Other = "Other",
};
/**
* The relevant rating type.
*/
export enum RatingTypeEnum {
    
    /** Long Term Ratings are applicable for the stated Rating Agency. */
    LongTerm = "LongTerm",
    /** Short Term Ratings are applicable for the stated Rating Agency. */
    ShortTerm = "ShortTerm",
};
/**
* The enumerated values applicable to define the what conditions apply to trigger a Not Rated condition.
*/
export enum NotRatedByEnum {
    
    /** No rating is available for the Party for any of the stated Rating Agencies. */
    All = "All",
    /** No rating is available for the Party for any one of the stated Rating Agencies. */
    One = "One",
    /** No rating is available for the Party for any two of the stated Rating Agencies. */
    Two = "Two",
};
/**
* The enumerated values to define the number of Rating Agencies that must be considered to meet the rating condition.
*/
export enum NumberOfRatingAgenciesEnum {
    
    /** Ratings for all defined Rating Agencies will be considered. */
    All = "All",
    /** Ratings for Any 1 stated Rating Agency will be applicable. */
    AnyOne = "AnyOne",
    /** Ratings for Any 2 stated Rating Agencies will be applicable. */
    AnyTwo = "AnyTwo",
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    Other = "Other",
};
/**
* In respect of a Valuation Date, the amount of collateral which a party is entitled to demand to be transferred to it as a Delivery or Return Amount.
*/
export enum DeliveryReturnAmountEnum {
    
    /** Standard definition applies. */
    Standard = "Standard",
    /** Election includes non-standard ratings-based language. */
    NonStandardRatings = "NonStandardRatings",
    /** Election includes non-standard bespoke language. */
    NonStandardOther = "NonStandardOther",
};
/**
* The type of time specified for the Valuation Time.
*/
export enum ValuationTimeEnum {
    
    /** The valuation time should be at close of business. */
    CloseOfBusiness = "CloseOfBusiness",
    /** The valuation time should be at a specific time. */
    SpecificTime = "SpecificTime",
    /** The valuation time should be as selected. */
    AsSelected = "AsSelected",
};
/**
* The day at which the value of the collateral and obligations to transfer or return collateral are to be calculated relative to the Valuation Date.
*/
export enum ValuationTimeDayEnum {
    
    /** Should be calculated relative to the previous local business day. */
    PreviousLocalBusinessDay = "PreviousLocalBusinessDay",
    /** Should be calculated relative to the previous local business day in the Relevant Market). */
    PreviousLocalBusinessDayMarket = "PreviousLocalBusinessDayMarket",
    /** Should be calculated relative to the previous local business day in the city of the Valuation Agent. */
    PreviousLocalBusinessDayCityOfAgent = "PreviousLocalBusinessDayCityOfAgent",
    /** Should be calculated on the Valuation Date. */
    ValuationDate = "ValuationDate",
    /** Should be calculated relative to the previous local business day as selected. */
    AsSelected = "AsSelected",
};
/**
* Defines whether the date of a Valuation is defined by a certain day of the week or a particular date in the month.
*/
export enum ValuationDateDateEnum {
    
    /** Specific Valuation date frequency. */
    Each = "Each",
    /** Specific Valuation date frequency. */
    Last = "Last",
    /** Specific Valuation date frequency. */
    Secondlast = "Secondlast",
    /** Specific Valuation date frequency. */
    Thirdlast = "Thirdlast",
    /** Specific Valuation date frequency. */
    Fourthlast = "Fourthlast",
    /** A specific day of the calendar, defined. */
    CalendarDay = "CalendarDay",
    /** As Selected. */
    AsSelected = "AsSelected",
};
/**
* Defines the conditions for the day for a Valuation Date.
*/
export enum ValuationDateDayEnum {
    
    /** The Date must be a day on which commercial banks are open for general business in the local market. */
    LocalBusinessDay = "LocalBusinessDay",
    /** The Date can be a given day on the regular calendar. */
    CalendarDay = "CalendarDay",
    /** The Date must be a New York Banking Day, that is a day, other than a Saturday or Sunday, on which banks are open for general commercial business in New York, USA. */
    NewYorkBankingDay = "NewYorkBankingDay",
};
/**
* The regularity of the event for the Valuation Date.
*/
export enum ValuationDateFrequencyEnum {
    
    Daily = "Daily",
    Weekly = "Weekly",
    Fortnightly = "Fortnightly",
    Monthly = "Monthly",
    AsAgreed = "AsAgreed",
    AsSelected = "AsSelected",
};
/**
* Specification of which party is calculating the value of collateral to be delivered or returned.
*/
export enum CalculationValuationAgentPartyEnum {
    
    /** Party 1 will calculate the value of collateral to be delivered or returned. */
    Party1 = "Party1",
    /** Party 2 will calculate the value of collateral to be delivered or returned. */
    Party2 = "Party2",
    /** The party making the demand will calculate the value of collateral to be delivered or returned. */
    PartyMakingDemand = "PartyMakingDemand",
};
/**
* Specifies how the Valuation Agent is determined.
*/
export enum ValuationAgentDeterminationEnum {
    
    /** There is only a Sole Valuation Agent. */
    SoleValuationAgent = "SoleValuationAgent",
    /** Switch of Valuation Agent can occur upon Default. */
    SwitchUponDefault = "SwitchUponDefault",
    /** Switch of Valuation Agent can occur upon failure to perform. */
    SwitchUponFailureToPerform = "SwitchUponFailureToPerform",
    /** There is a fixed party for disputes. */
    FixedPartyForDisputes = "FixedPartyForDisputes",
};
/**
* Details of how the Custodian is determined.
*/
export enum LegacyVMCustodianEnum {
    
    /** Not Applicable. */
    NotApplicable = "NotApplicable",
    /** A named entity is specified as the Custodian. */
    NamedEntity = "NamedEntity",
    /** The Custodian as advised by Party 1 to Party 2. */
    AsAdvised1to2 = "AsAdvised1to2",
    /** The Custodian as advised by Party 2 to Party 1. */
    AsAdvised2to1 = "AsAdvised2to1",
    /** Party 1 will notify Party 2 of the custodian. */
    Party1toParty2 = "Party1toParty2",
    /** Party 2 will notify Party 1 of the custodian. */
    Party2toParty1 = "Party2toParty1",
};
/**
* Details of the consent requirements (if any) for Substitution.
*/
export enum LegacyConsentEnum {
    
    /** Consent is required for Substitution. */
    ConsentRequired = "ConsentRequired",
    /** No consent is required for Substitution. */
    NoConsent = "NoConsent",
    /** Consent is required, with no unreasonable delay, for Substitution. */
    ConsentRequiredNoDelay = "ConsentRequiredNoDelay",
    /** Times adjusted. */
    TimesAdjusted = "TimesAdjusted",
    /** Notice conditions apply for Substitution. */
    NoticeConditions = "NoticeConditions",
    /** Costs apportioned for Substitution. */
    CostsApportioned = "CostsApportioned",
};
/**
* Description of the relevant derivative transactions excluded from the calculation of exposure.
*/
export enum ExposureScopeProductEnum {
    
    /** FX Transactions. */
    FXTransactions = "FXTransactions",
    /** FX Transactions with Settlement date. */
    FXSettlementDate = "FXSettlementDate",
    /** Currency Option Transactions. */
    CurrencyOptionTransactions = "CurrencyOptionTransactions",
};
/**
* Specifies whether the meaning of credit support amount follows the pre-print definition or deviates from the standard (either based on a rating or some other methodology).
*/
export enum CreditSupportAmountEnum {
    
    /** The standard definition of the credit support amount applies. */
    Standard = "Standard",
    /** Non-Standard - IA Floor (Less GiverIA) */
    IAFloorGIA = "IAFloorGIA",
    /** Non-Standard - IA Floor (Less GiverIA and TakerIA) */
    IAFloorGIATIA = "IAFloorGIATIA",
    /** Non-Standard - Ratings */
    NonStandardRatings = "NonStandardRatings",
    /** Non-Standard - Other */
    NonStandardOther = "NonStandardOther",
};
/**
* Details of the alternative dispute resolution procedure (if any).
*/
export enum LegacyResolutionAlternativeEnum {
    
    /** No Alternative dispute resolution procedure is defined. */
    NoAlternative = "NoAlternative",
    /** A split the difference resolution procedure applies. */
    SplitDifference = "SplitDifference",
    /** Additional Conditions are defined. */
    AdditionalConditions = "AdditionalConditions",
};
/**
* Details of how cash collateral is valued when resolving disputes.
*/
export enum ValueCashEnum {
    
    /** Cash - Amount thereof. */
    CashAmount = "CashAmount",
    /** Cash - amount thereof multiplied by Valuation Percentage. */
    CashPercentage = "CashPercentage",
    /** Cash - Amount Thereof multiplied by (Valuation Percentage - FX Haircut). */
    CashPercentageLessHaircut = "CashPercentageLessHaircut",
    /** Exception value. */
    Other = "Other",
};
/**
* Details of how securities collateral is valued when resolving disputes.
*/
export enum ValueSecuritiesEnum {
    
    /** Securities collateral is valued using the bid price multiplied by the valuation percentage. */
    BidPricePercentage = "BidPricePercentage",
    /** Securities collateral is valued using the bid price multiplied by the valuation percentage less the FX haircut. */
    BidPricePercentageLessHaircut = "BidPricePercentageLessHaircut",
    /** Securities collateral is valued using the mid price multiplied by the valuation percentage. */
    MidPricePercentage = "MidPricePercentage",
    /** Securities collateral is valued using the mid price multiplied by the valuation percentage less the FX haircut. */
    MidPricePercentageLessHaircut = "MidPricePercentageLessHaircut",
    /** Exception value. */
    Other = "Other",
};
/**
* Details the day on which cash collateral is required to be transferred relative to the Notification Time.
*/
export enum CashCTSTimeEnum {
    
    /** The cash collateral should be transferred on the same day. */
    Same = "Same",
    /** The cash collateral should be transferred on the next day. */
    Next = "Next",
    /** The cash collateral should be transferred on the first local business day. */
    FirstLBD = "FirstLBD",
    /** The cash collateral should be transferred on the second local business day. */
    SecondLBD = "SecondLBD",
    /** Exception value. */
    Other = "Other",
};
/**
* Details the day on which securities collateral is required to be transferred relative to the Notification Time.
*/
export enum SecuritiesCTSTimeEnum {
    
    /** The securities collateral should be transferred on the same day. */
    Same = "Same",
    /** The securities collateral should be transferred on the next day. */
    Next = "Next",
    /** The securities collateral should be transferred on the first local business day. */
    FirstDay = "FirstDay",
    /** The securities collateral should be transferred on the second local business day. */
    SecondDay = "SecondDay",
    /** The securities collateral should be transferred on the third local business day. */
    ThirdDay = "ThirdDay",
    /** Exception value. */
    Other = "Other",
};
/**
* Specifies the types of events in the related Master Agreement that, when triggered, could temporarily or permanently suspend rights to rehypothecate, transfer or substitute collateral.
*/
export enum CSASpecifiedOrAccessConditionEnum {
    
    /** Specifies an event where a party is unable to comply with its obligations under the Master Agreement because to do so would be unlawful. */
    Illegality = "Illegality",
    /** Specifies an event where a party experiences changes in tax laws incurring further tax liability. */
    TaxEvent = "TaxEvent",
    /** Specifies an event where a party incurs tax liability due to a merger. */
    TaxEventUponMerger = "TaxEventUponMerger",
    /** Specifies an event where a party merges and the new entity becomes materially less creditworthy than the original. */
    CreditEventUponMerger = "CreditEventUponMerger",
    /** Specifies events which could trigger rights to early termination. */
    AdditionalTerminationEvents = "AdditionalTerminationEvents",
    /** Specifies an event beyond the control of one or both of the parties which prevents or renders it impossible to fulfill their obligations to the other party. */
    ForceMajeureEvent = "ForceMajeureEvent",
    /** Specifies a potential event that could trigger rights to early termination. */
    PotentialTerminationEvents = "PotentialTerminationEvents",
};
/**
* Specifies the types of events found in the related Master Agreement that may lead to the early termination of the Master Agreement, including suspension of the affected party's rights to rehypothecate, transfer, or substitute collateral.
*/
export enum AdditionalTerminationEventEnum {
    
    /** Specifies an event where a party fails to notify the other of a decline in Net Asset Value by a specified amount over a specified period of time. */
    FailureToNotifyNAV = "FailureToNotifyNAV",
    /** Specifies an event where the investment advisor of a party ceases to act for the party. */
    InvestmentAdvisor = "InvestmentAdvisor",
    /** Specifies an event where a person deemed important to a party has departed. */
    KeyPersons = "KeyPersons",
    /** Specifies an event in which a partys Net Asset Value (NAV) has declined beyond a specified percentage or amount within a defined time period. */
    NAVDeclineTrigger = "NAVDeclineTrigger",
    /** Specifies an event in which a partys Net Asset Value (NAV) falls below a predetermined absolute value, regardless of the timeframe over which the decline occurs. This value sets a minimum acceptable NAV threshold expressed as either a percentage or amount. */
    NAVFloor = "NAVFloor",
    /** Specifies an event where a party delivers operative documents that are invalid, untrue or unenforceable. */
    OperativeDocs = "OperativeDocs",
    /** Specifies an event where Party 1 experiences a downgrade beyond a predetermined level, or has their credit rating withdrawn or suspended. */
    RatingsDowngradeOrWithdrawalParty1 = "RatingsDowngradeOrWithdrawalParty1",
    /** Specifies an event where Party 2 experiences a downgrade beyond a predetermined level, or has their credit rating withdrawn or suspended. */
    RatingsDowngradeOrWithdrawalParty2 = "RatingsDowngradeOrWithdrawalParty2",
    /** Represents the Additional Termination Event(s) in the related Master Agreement. */
    AsApplicable = "AsApplicable",
    /** Represents any bespoke Additional Termination Event(s) that are set out in the related Specified Condition clause. */
    AsSpecified = "AsSpecified",
};
/**
* Specifies how multiple credit ratings are compared when determining the applicable Independent Amount, and whether that measure is highest, lowest, or a comparison of the ratings.
*/
export enum IndependentAmountCompareEnum {
    
    /** Represents that the credit ratings across multiple credit rating agencies will be compared against one another. */
    Compare = "Compare",
};
/**
* The enumerated values applicable where a Rated Party is defined within the relevant clause.
*/
export enum RatedPartyEnum {
    
    /** Party. */
    Party = "Party",
    /** Credit Support Provider. */
    CreditSupportProvider = "CreditSupportProvider",
    /** A Named Entity. */
    NamedEntity = "NamedEntity",
    /** The Party or Credit Support Provider. */
    PartyOrCreditSupportProvider = "PartyOrCreditSupportProvider",
    /** A Named Affiliate of the Party. */
    NamedAffiliate = "NamedAffiliate",
    /** All Affiliates to the Party. */
    AllAffiliates = "AllAffiliates",
};
/**
* The enumerated values for defining the relevant trigger(s) for the Minimum Transfer Amount (MTA) to fall to zero.
*/
export enum ZeroEventEnum {
    
    /** An Event of Default. */
    EventOfDefault = "EventOfDefault",
    /** A Termination Event. */
    TerminationEvent = "TerminationEvent",
    /** A Termination Event in respect of which all Transactions are Affected Transactions. */
    TerminationEventAllAffectedTransactions = "TerminationEventAllAffectedTransactions",
    /** A Potential Event of Default. */
    PotentialEventOfDefault = "PotentialEventOfDefault",
    /** An Additional Termination Event (ATE). */
    AdditionalTerminationEvent = "AdditionalTerminationEvent",
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    Other = "Other",
};
/**
* The enumerated values to specify the type of the master agreement governing the transaction.
*/
export enum MasterAgreementTypeEnum {
    
    /** AFB Master Agreement for Foreign Exchange and Derivatives Transactions */
    AFB = "AFB",
    /** A Bespoke (custom) Master Agreement, including one-off agreements for transactions */
    Bespoke = "Bespoke",
    /** Clearing Master Agreement */
    CMA = "CMA",
    /** Contrato Marco de Operaciones Financieras */
    CMOF = "CMOF",
    /** EEI Master Power Purchase and Sale Agreement */
    EEIPower = "EEIPower",
    /** EFET General Agreement Concerning the Delivery and Acceptance of Electricity */
    EFETElectricity = "EFETElectricity",
    /** EFET General Agreement Concerning The Delivery And Acceptance of Natural Gas */
    EFETGas = "EFETGas",
    /** European Master Agreement and the Derivatives Annex (Banking Federation of the European Union) */
    EMA = "EMA",
    /** Master Agreement Relating to transactions on Forward Financial Instruments (Federation Bancaire Francaise) */
    FBF = "FBF",
    /** GasEDI Base Contract for Short-term Sale and Purchase of Natural Gas */
    GasEDI = "GasEDI",
    /** German Master Agreement for Financial derivatives and Addendum for Options on Stock Exchange Indices or Securities */
    German = "German",
    /** ICMA Global Master Agreement for REPO Trades */
    GMRA = "GMRA",
    /** ISLA Global Master Agreement for Securities Lending */
    GMSLA = "GMSLA",
    /** FOA Grid Trade Master Agreement */
    GTMA = "GTMA",
    /** International Currency Options Market Master Agreement */
    ICOM = "ICOM",
    /** International Emissions Trading Association Emissions Reduction Purchase Agreement */
    IETA_ERPA = "IETA_ERPA",
    /** International Emissions Trading Association Emissions Trading Master Agreement */
    IETA_ETMA = "IETA_ETMA",
    /** International Emissions Trading Association International Emissions Trading Master Agreement */
    IETA_IETMA = "IETA_IETMA",
    /** International Foreign Exchange Master Agreement */
    IFEMA = "IFEMA",
    /** International Foreign Exchange and Options Master Agreement */
    IFEOMA = "IFEOMA",
    /** ISDA Master Agreement */
    ISDAMaster = "ISDAMaster",
    /** ISDA-FIA Cleared Derivatives Execution Agreement */
    ISDAFIA_CDEA = "ISDAFIA_CDEA",
    /** ISDA/IIFM Tahawwut (Hedging) Master Agreement (TMA) */
    ISDAIIFM_TMA = "ISDAIIFM_TMA",
    /** Master agreement of Japan Securities Clearing Corporation */
    JSCC = "JSCC",
    /** International Bullion Master Agreement Terms published by the London Bullion Market Association */
    LBMA = "LBMA",
    /** Leadership in Energy Automated Processing */
    LEAP = "LEAP",
    /** CTA Master Coal Purchase and Sales Agreement */
    MCPSA = "MCPSA",
    /** NAESB Base Contract for Sale and Purchase of Natural Gas */
    NAESBGas = "NAESBGas",
    /** Short Term Flat NBP Trading Terms and Conditions */
    NBP = "NBP",
    /** Standard Documentation for Derivative Transactions on the Russian Financial Markets */
    RussianDerivatives = "RussianDerivatives",
    /** Master Agreement and Contractual Terms for Repurchase Agreements on the Russian Financial Market */
    RussianRepo = "RussianRepo",
    /** globalCOAL Standard Coal Trading Agreement */
    SCoTA = "SCoTA",
    /** Swiss Master Agreement for OTC Derivatives Instruments */
    Swiss = "Swiss",
    /** TTF Hub Natural Gas Trading Terms and Conditions */
    TTF = "TTF",
    /** Zeebrugge Hub Natural Gas Trading Terms and Conditions */
    ZBT = "ZBT",
};
/**
* The enumerated values to specify the type of annex to be used with master confirmation agreement governing the transaction.
*/
export enum MasterConfirmationAnnexTypeEnum {
    
    /** The Index Variance Swap 2004 Annex to the ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement and to the Revised ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2004IndexVarianceSwapAmericasInterdealer = "ISDA2004IndexVarianceSwapAmericasInterdealer",
    /** The Share Variance Swap 2004 Annex to the ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement and to the Revised ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2004ShareVarianceSwapAmericasInterdealer = "ISDA2004ShareVarianceSwapAmericasInterdealer",
    /** The Dispersion Variance Swap Annex to the Revised 2007 ISDA European Variance Swap Master Confirmation Agreement applies. */
    ISDA2007DispersionVarianceSwapEuropean = "ISDA2007DispersionVarianceSwapEuropean",
    /** The EFS (Equity Share Finance Swap) 2007 Annex to the ISDA 2007 European Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2007EquityFinanceSwapEuropean = "ISDA2007EquityFinanceSwapEuropean",
    /** The Index Variance Swap 2007 Annex to the Revised ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2007IndexVarianceSwapAmericasInterdealer = "ISDA2007IndexVarianceSwapAmericasInterdealer",
    /** The Share Variance Swap 2007 Annex to the Revised ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2007ShareVarianceSwapAmericasInterdealer = "ISDA2007ShareVarianceSwapAmericasInterdealer",
    /** The Variance Option Standard Terms Appendix to the Revised ISDA 2007 European Variance Swap Master Confirmation Agreement applies. */
    ISDA2007VarianceOptionEuropean = "ISDA2007VarianceOptionEuropean",
    /** The Cash-settled Open Market EFS (Equity Finance Share Swap) 2008 Annex to the ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityFinanceSwapAsiaExcludingJapan = "ISDA2008EquityFinanceSwapAsiaExcludingJapan",
    /** The Cash-settled Open Market EFS (Equity Finance Share Swap) Annex to the Revised ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityFinanceSwapAsiaExcludingJapanRev1 = "ISDA2008EquityFinanceSwapAsiaExcludingJapanRev1",
    /** The Open Market Equity Option 2008 Annex to the ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityOptionAsiaExcludingJapan = "ISDA2008EquityOptionAsiaExcludingJapan",
    /** The Open Market Equity Option Annex to the Revised ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityOptionAsiaExcludingJapanRev1 = "ISDA2008EquityOptionAsiaExcludingJapanRev1",
    /** The Equity Option 2008 Annex to the ISDA 2008 Japanese Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityOptionJapan = "ISDA2008EquityOptionJapan",
    /** The Cash-settled Closed Market Index and Share Options 2009 Annex to the Revised ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009ClosedMarketsOptionsAsiaExcludingJapan = "ISDA2009ClosedMarketsOptionsAsiaExcludingJapan",
    /** The Interdealer Share Swap 2009 Annex to the ISDA 2009 European Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009EquityEuropeanInterdealerSS = "ISDA2009EquityEuropeanInterdealerSS",
    /** The Index Swap 2009 Annex to the ISDA 2007 European Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009EquityEuropeanIS = "ISDA2009EquityEuropeanIS",
    /** The Index and Share Options 2009 Annex to the ISDA 2009 Americas Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009IndexShareOptionAmericas = "ISDA2009IndexShareOptionAmericas",
    /** The Interdealer Index Swap 2009 Annex to the ISDA 2009 European Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009IndexSwapEuropeanInterdealer = "ISDA2009IndexSwapEuropeanInterdealer",
    /** The Index Swap 2009 Annex to the ISDA 2009 Pan-Asia Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009IndexSwapPanAsiaInterdealer = "ISDA2009IndexSwapPanAsiaInterdealer",
    /** The Share Swap 2009 Annex to the ISDA 2009 Pan-Asia Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009ShareSwapPanAsia = "ISDA2009ShareSwapPanAsia",
    /** The Fair Value Interdealer Share Swap 2010 Annex to the ISDA 2009 European Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2010FairValueShareSwapEuropeanInterdealer = "ISDA2010FairValueShareSwapEuropeanInterdealer",
    /** The Cash-settled Index Option/Cash/Physically-settled Share Option 2010 Annex to the ISDA 2010 EMEA EM Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2010IndexShareOptionEMEAInterdealer = "ISDA2010IndexShareOptionEMEAInterdealer",
};
/**
* Defines the consequences of nationalization, insolvency and delisting events relating to the underlying.
*/
export enum NationalizationOrInsolvencyOrDelistingEventEnum {
    
    /** The parties may, but are not obliged, to terminate the transaction on mutually acceptable terms and if the terms are not agreed then the transaction continues. */
    NegotiatedCloseout = "NegotiatedCloseout",
    /** The trade is terminated. */
    CancellationAndPayment = "CancellationAndPayment",
};
/**
* The enumerated values to specify the type of master confirmation agreement governing the transaction. While FpML positions the date a prefix, the CDM positions it as the suffix to handle grammar type constraints.
*/
export enum MasterConfirmationTypeEnum {
    
    /** Used for CDS Index trades. Relevant Master Confirmation determined by the contents of the creditDefaultSwap element. Best practice is to use the most specific code that applies. */
    _2003CreditIndex = "_2003CreditIndex",
    /** A privately negotiated European Interdealer Master Confirmation Agreement applies. */
    _2004EquityEuropeanInterdealer = "_2004EquityEuropeanInterdealer",
    /** A privately negotiated European Interdealer Master Confirmation Agreement applies. */
    _2005VarianceSwapEuropeanInterdealer = "_2005VarianceSwapEuropeanInterdealer",
    /** A European Interdealer Master Confirmation Agreement not defined by ISDA, and modified by the parties to the transaction applies. */
    _2006DividendSwapEuropean = "_2006DividendSwapEuropean",
    /** A European Interdealer Master Confirmation Agreement not defined by ISDA applies. */
    _2006DividendSwapEuropeanInterdealer = "_2006DividendSwapEuropeanInterdealer",
    /** Dummy MCA value mirroring the matrix term value AsiaCorporate. */
    _2014CreditAsia = "_2014CreditAsia",
    /** Dummy MCA value mirroring the matrix term value AsiaFinancialCorporate. */
    _2014CreditAsiaFinancial = "_2014CreditAsiaFinancial",
    /** Dummy MCA value mirroring the matrix term value AustraliaCorporate/NewZealandCorporate. */
    _2014CreditAustraliaNewZealand = "_2014CreditAustraliaNewZealand",
    /** Dummy MCA value mirroring the matrix term value AustraliaFinancialCorporate/NewZealandFinancialCorporate. */
    _2014CreditAustraliaNewZealandFinancial = "_2014CreditAustraliaNewZealandFinancial",
    /** Dummy MCA value mirroring the matrix term value EuropeanCorporate. */
    _2014CreditEuropean = "_2014CreditEuropean",
    /** Dummy MCA value mirroring the matrix term value EuropeanCoCoFinancialCorporate. */
    _2014CreditEuropeanCoCoFinancial = "_2014CreditEuropeanCoCoFinancial",
    /** Dummy MCA value mirroring the matrix term value EuropeanFinancialCorporate. */
    _2014CreditEuropeanFinancial = "_2014CreditEuropeanFinancial",
    /** Dummy MCA value mirroring the matrix term value JapanCorporate. */
    _2014CreditJapan = "_2014CreditJapan",
    /** Dummy MCA value mirroring the matrix term value JapanFinancialCorporate. */
    _2014CreditJapanFinancial = "_2014CreditJapanFinancial",
    /** Dummy MCA value mirroring the matrix term value NorthAmericanCorporate. */
    _2014CreditNorthAmerican = "_2014CreditNorthAmerican",
    /** Dummy MCA value mirroring the matrix term value NorthAmericanFinancialCorporate. */
    _2014CreditNorthAmericanFinancial = "_2014CreditNorthAmericanFinancial",
    /** Dummy MCA value mirroring the matrix term values SingaporeCorporate. */
    _2014CreditSingapore = "_2014CreditSingapore",
    /** Dummy MCA value mirroring the matrix term values SingaporeFinancialCorporate. */
    _2014CreditSingaporeFinancial = "_2014CreditSingaporeFinancial",
    /** Dummy MCA value mirroring the matrix term value AsiaSovereign. */
    _2014CreditSovereignAsia = "_2014CreditSovereignAsia",
    /** Dummy MCA value mirroring the matrix term value EmergingEuropeanAndMiddleEasternSovereign. */
    _2014CreditSovereignEmergingEuropeanAndMiddleEastern = "_2014CreditSovereignEmergingEuropeanAndMiddleEastern",
    /** Dummy MCA value mirroring the matrix term value JapanSovereign. */
    _2014CreditSovereignJapan = "_2014CreditSovereignJapan",
    /** Dummy MCA value mirroring the matrix term value LatinAmericaSovereign. */
    _2014CreditSovereignLatinAmerican = "_2014CreditSovereignLatinAmerican",
    /** Dummy MCA value mirroring the matrix term value WesternEuropeanSovereign. */
    _2014CreditSovereignWesternEuropean = "_2014CreditSovereignWesternEuropean",
    /** Dummy MCA value mirroring the matrix term values StandardAsiaCorporate. */
    _2014StandardCreditAsia = "_2014StandardCreditAsia",
    /** Dummy MCA value mirroring the matrix term values StandardAsiaFinancialCorporate. */
    _2014StandardCreditAsiaFinancial = "_2014StandardCreditAsiaFinancial",
    /** Dummy MCA value mirroring the matrix term values StandardAustraliaCorporate and StandardNewZealandCorporate. */
    _2014StandardCreditAustraliaNewZealand = "_2014StandardCreditAustraliaNewZealand",
    /** Dummy MCA value mirroring the matrix term values StandardAustraliaFinancialCorporate and StandardNewZealandFinancialCorporate. */
    _2014StandardCreditAustraliaNewZealandFinancial = "_2014StandardCreditAustraliaNewZealandFinancial",
    /** Dummy MCA value mirroring the matrix term value StandardEuropeanCorporate. */
    _2014StandardCreditEuropean = "_2014StandardCreditEuropean",
    /** Dummy MCA value mirroring the matrix term value StandardEuropeanCoCoFinancialCorporate. */
    _2014StandardCreditEuropeanCoCoFinancial = "_2014StandardCreditEuropeanCoCoFinancial",
    /** Dummy MCA value mirroring the matrix term value StandardEuropeanFinancialCorporate. */
    _2014StandardCreditEuropeanFinancial = "_2014StandardCreditEuropeanFinancial",
    /** Dummy MCA value mirroring the matrix term values StandardJapanCorporate. */
    _2014StandardCreditJapan = "_2014StandardCreditJapan",
    /** Dummy MCA value mirroring the matrix term value StandardJapanFinancialCorporate. */
    _2014StandardCreditJapanFinancial = "_2014StandardCreditJapanFinancial",
    /** Dummy MCA value mirroring the matrix term value StandardNorthAmericanCorporate. */
    _2014StandardCreditNorthAmerican = "_2014StandardCreditNorthAmerican",
    /** Dummy MCA value mirroring the matrix term value standardNorthAmericanFinancialCorporate. */
    _2014StandardCreditNorthAmericanFinancial = "_2014StandardCreditNorthAmericanFinancial",
    /** Dummy MCA value mirroring the matrix term values StandardSingaporeCorporate. */
    _2014StandardCreditSingapore = "_2014StandardCreditSingapore",
    /** Dummy MCA value mirroring the matrix term value StandardSingaporeFinancialCorporate. */
    _2014StandardCreditSingaporeFinancial = "_2014StandardCreditSingaporeFinancial",
    /** Dummy MCA value mirroring the matrix term value StandardAsiaSovereign. */
    _2014StandardCreditSovereignAsia = "_2014StandardCreditSovereignAsia",
    /** Dummy MCA value mirroring the matrix term value StandardEmergingEuropeanAndMiddleEasternSovereign. */
    _2014StandardCreditSovereignEmergingEuropeanAndMiddleEastern = "_2014StandardCreditSovereignEmergingEuropeanAndMiddleEastern",
    /** Dummy MCA value mirroring the matrix term values StandardJapanSovereign. */
    _2014StandardCreditSovereignJapan = "_2014StandardCreditSovereignJapan",
    /** Dummy MCA value mirroring the matrix term value StandardLatinAmericaSovereign. */
    _2014StandardCreditSovereignLatinAmerican = "_2014StandardCreditSovereignLatinAmerican",
    /** Dummy MCA value mirroring the matrix term value StandardWesternEuropeanSovereign. */
    _2014StandardCreditSovereignWesternEuropean = "_2014StandardCreditSovereignWesternEuropean",
    /** Used for CDS Index trades executed under the Dow Jones CDX Emerging Markets Master Confirmation. */
    DJ_CDX_EM = "DJ_CDX_EM",
    /** Used for CDS Index trades executed under the Dow Jones CDX Emerging Markets Diversified Master Confirmation. */
    DJ_CDX_EM_DIV = "DJ_CDX_EM_DIV",
    /** Used for CDS Index trades executed under the Dow Jones CDX Master Confirmation that covers CDX.NA.IG, CDX.NA.HY, and CDX.NA.XO. */
    DJ_CDX_NA = "DJ_CDX_NA",
    /** Used for CDS Index trades executed under the Dow Jones iTraxx Europe Master Confirmation Agreement. */
    DJ_iTraxx_Europe = "DJ_iTraxx_Europe",
    /** A general reference to the types of Americas Master Confirmation Agreements. Use the more specific values to reference a specific type of Americas Master Confirmation Agreement. */
    EquityAmericas = "EquityAmericas",
    /** A general reference to the types of Asia Master Confirmation Agreements. Use the more specific values to reference a specific type of Asia Master Confirmation Agreement. */
    EquityAsia = "EquityAsia",
    /** A general reference to the types of European Master Confirmation Agreements. Use the more specific values to reference a specific type of European Master Confirmation Agreement. */
    EquityEuropean = "EquityEuropean",
    /** ISDA 1999 Master Credit Derivatives Confirmation Agreement */
    ISDA1999Credit = "ISDA1999Credit",
    /** ISDA 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Asia had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2003CreditAsia = "ISDA2003CreditAsia",
    /** ISDA 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Australia and New Zealand had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2003CreditAustraliaNewZealand = "ISDA2003CreditAustraliaNewZealand",
    /** ISDA 2003 Master Credit Derivatives Confirmation Agreement interpreted as if European had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2003CreditEuropean = "ISDA2003CreditEuropean",
    /** ISDA 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Japan had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2003CreditJapan = "ISDA2003CreditJapan",
    /** ISDA 2003 Master Credit Derivatives Confirmation Agreement interpreted as if North American had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2003CreditNorthAmerican = "ISDA2003CreditNorthAmerican",
    /** ISDA 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Singapore had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2003CreditSingapore = "ISDA2003CreditSingapore",
    /** ISDA Sovereign 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Asia had been specified as the relevant Transaction Type in the Transaction Supplement. The 2003 Sovereign Master Confirmation has been superceded by the 2004. */
    ISDA2003CreditSovereignAsia = "ISDA2003CreditSovereignAsia",
    /** ISDA Sovereign 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Central and Eastern Europe had been specified as the relevant Transaction Type in the Transaction Supplement. The 2003 Sovereign Master Confirmation has been superceded by the 2004. */
    ISDA2003CreditSovereignCentralAndEasternEurope = "ISDA2003CreditSovereignCentralAndEasternEurope",
    /** ISDA Sovereign 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Japan had been specified as the relevant Transaction Type in the Transaction Supplement. The 2003 Sovereign Master Confirmation has been superceded by the 2004. */
    ISDA2003CreditSovereignJapan = "ISDA2003CreditSovereignJapan",
    /** ISDA Sovereign 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Latin America had been specified as the relevant Transaction Type in the Transaction Supplement. The 2003 Sovereign Master Confirmation has been superceded by the 2004. */
    ISDA2003CreditSovereignLatinAmerica = "ISDA2003CreditSovereignLatinAmerica",
    /** ISDA Sovereign 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Middle East had been specified as the relevant Transaction Type in the Transaction Supplement. The 2003 Sovereign Master Confirmation has been superceded by the 2004. */
    ISDA2003CreditSovereignMiddleEast = "ISDA2003CreditSovereignMiddleEast",
    /** ISDA Sovereign 2003 Master Credit Derivatives Confirmation Agreement interpreted as if Western Europe had been specified as the relevant Transaction Type in the Transaction Supplement. The 2003 Sovereign Master Confirmation has been superceded by the 2004. */
    ISDA2003CreditSovereignWesternEurope = "ISDA2003CreditSovereignWesternEurope",
    /** Dummy MCA value mirroring the matrix term values StandardAsiaCorporate. */
    ISDA2003StandardCreditAsia = "ISDA2003StandardCreditAsia",
    /** Dummy MCA value mirroring the matrix term values StandardAustraliaCorporate/Sovereign and StandardNewZealandCorporate/Sovereign. */
    ISDA2003StandardCreditAustraliaNewZealand = "ISDA2003StandardCreditAustraliaNewZealand",
    /** Dummy MCA value mirroring the matrix term value StandardEuropeanCorporate. */
    ISDA2003StandardCreditEuropean = "ISDA2003StandardCreditEuropean",
    /** Dummy MCA value mirroring the matrix term values StandardJapanCorporate. */
    ISDA2003StandardCreditJapan = "ISDA2003StandardCreditJapan",
    /** Dummy MCA value mirroring the matrix term value StandardNorthAmericanCorporate. */
    ISDA2003StandardCreditNorthAmerican = "ISDA2003StandardCreditNorthAmerican",
    /** Dummy MCA value mirroring the matrix term values StandardSingaporeCorporate/Sovereign. */
    ISDA2003StandardCreditSingapore = "ISDA2003StandardCreditSingapore",
    /** ISDA Sovereign 2004 Master Credit Derivatives Confirmation Agreement interpreted as if Asia had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2004CreditSovereignAsia = "ISDA2004CreditSovereignAsia",
    /** ISDA Sovereign 2004 Master Credit Derivatives Confirmation Agreement interpreted as if Emerging European and Middle Eastern had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2004CreditSovereignEmergingEuropeanAndMiddleEastern = "ISDA2004CreditSovereignEmergingEuropeanAndMiddleEastern",
    /** ISDA Sovereign 2004 Master Credit Derivatives Confirmation Agreement interpreted as if Japan had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2004CreditSovereignJapan = "ISDA2004CreditSovereignJapan",
    /** ISDA Sovereign 2004 Master Credit Derivatives Confirmation Agreement interpreted as if Latin American had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2004CreditSovereignLatinAmerican = "ISDA2004CreditSovereignLatinAmerican",
    /** ISDA Sovereign 2004 Master Credit Derivatives Confirmation Agreement interpreted as if Western European had been specified as the relevant Transaction Type in the Transaction Supplement. */
    ISDA2004CreditSovereignWesternEuropean = "ISDA2004CreditSovereignWesternEuropean",
    /** The ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2004EquityAmericasInterdealer = "ISDA2004EquityAmericasInterdealer",
    /** The Revised ISDA 2004 Americas Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2004EquityAmericasInterdealerRev1 = "ISDA2004EquityAmericasInterdealerRev1",
    /** Dummy MCA value mirroring the matrix term values StandardAsiaSovereign. */
    ISDA2004StandardCreditSovereignAsia = "ISDA2004StandardCreditSovereignAsia",
    /** Dummy MCA value mirroring the matrix term value StandardEmergingEuropeanAndMiddleEasternSovereign. */
    ISDA2004StandardCreditSovereignEmergingEuropeanAndMiddleEastern = "ISDA2004StandardCreditSovereignEmergingEuropeanAndMiddleEastern",
    /** Dummy MCA value mirroring the matrix term values StandardJapanSovereign. */
    ISDA2004StandardCreditSovereignJapan = "ISDA2004StandardCreditSovereignJapan",
    /** Dummy MCA value mirroring the matrix term value StandardLatinAmericaSovereign. */
    ISDA2004StandardCreditSovereignLatinAmerican = "ISDA2004StandardCreditSovereignLatinAmerican",
    /** Dummy MCA value mirroring the matrix term value StandardWesternEuropeanSovereign. */
    ISDA2004StandardCreditSovereignWesternEuropean = "ISDA2004StandardCreditSovereignWesternEuropean",
    /** ISDA 2005 AEJ (Asia Excluding Japan) Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2005EquityAsiaExcludingJapanInterdealer = "ISDA2005EquityAsiaExcludingJapanInterdealer",
    /** Second Revised ISDA 2005 AEJ (Asia Excluding Japan) Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2005EquityAsiaExcludingJapanInterdealerRev2 = "ISDA2005EquityAsiaExcludingJapanInterdealerRev2",
    /** The ISDA 2005 Japanese Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2005EquityJapaneseInterdealer = "ISDA2005EquityJapaneseInterdealer",
    /** ISDA 2006 Variance Swap Japanese Confirmation Agreement applies. */
    ISDA2006VarianceSwapJapanese = "ISDA2006VarianceSwapJapanese",
    /** ISDA 2006 Variance Swap Japanese Interdealer Confirmation Agreement applies. */
    ISDA2006VarianceSwapJapaneseInterdealer = "ISDA2006VarianceSwapJapaneseInterdealer",
    /** The ISDA 2007 European Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2007EquityEuropean = "ISDA2007EquityEuropean",
    /** The ISDA 2007 Americas Master Variance Swap Confirmation Agreement applies. */
    ISDA2007VarianceSwapAmericas = "ISDA2007VarianceSwapAmericas",
    /** The ISDA 2007 AEJ Master Variance Swap Confirmation Agreement applies. */
    ISDA2007VarianceSwapAsiaExcludingJapan = "ISDA2007VarianceSwapAsiaExcludingJapan",
    /** The Revised ISDA 2007 AEJ Master Variance Swap Confirmation Agreement applies. */
    ISDA2007VarianceSwapAsiaExcludingJapanRev1 = "ISDA2007VarianceSwapAsiaExcludingJapanRev1",
    /** The Second Revised ISDA 2007 AEJ Master Variance Swap Confirmation Agreement applies. */
    ISDA2007VarianceSwapAsiaExcludingJapanRev2 = "ISDA2007VarianceSwapAsiaExcludingJapanRev2",
    /** The ISDA 2007 European Variance Swap Master Confirmation Agreement applies. */
    ISDA2007VarianceSwapEuropean = "ISDA2007VarianceSwapEuropean",
    /** The Revised ISDA 2007 European Variance Swap Master Confirmation Agreement applies. */
    ISDA2007VarianceSwapEuropeanRev1 = "ISDA2007VarianceSwapEuropeanRev1",
    /** The ISDA 2008 Japanese Dividend Swap Master Confirmation Agreement applies. */
    ISDA2008DividendSwapJapan = "ISDA2008DividendSwapJapan",
    /** The Revised ISDA 2008 Japanese Dividend Swap Master Confirmation Agreement applies. */
    ISDA2008DividendSwapJapaneseRev1 = "ISDA2008DividendSwapJapaneseRev1",
    /** The ISDA 2008 Americas Master Designated/Exchange-Traded Contract Option Confirmation Agreement applies. */
    ISDA2008EquityAmericas = "ISDA2008EquityAmericas",
    /** The ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityAsiaExcludingJapan = "ISDA2008EquityAsiaExcludingJapan",
    /** The Revised ISDA 2008 AEJ (Asia Excluding Japan) Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityAsiaExcludingJapanRev1 = "ISDA2008EquityAsiaExcludingJapanRev1",
    /** The ISDA 2008 Japanese Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2008EquityJapan = "ISDA2008EquityJapan",
    /** The ISDA 2009 Americas Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009EquityAmericas = "ISDA2009EquityAmericas",
    /** The ISDA 2009 European Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009EquityEuropeanInterdealer = "ISDA2009EquityEuropeanInterdealer",
    /** 2009 Pan-Asia Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2009EquityPanAsia = "ISDA2009EquityPanAsia",
    /** The ISDA 2010 EMEA EM Interdealer Master Equity Derivatives Confirmation Agreement applies. */
    ISDA2010EquityEMEAInterdealer = "ISDA2010EquityEMEAInterdealer",
    /** The ISDA 2013 Americas Master Volatility Swap Confirmation Agreement applies. */
    ISDA2013VolatilitySwapAmericas = "ISDA2013VolatilitySwapAmericas",
    /** The ISDA 2013 AEJ Master Volatility Swap Confirmation Agreement applies. */
    ISDA2013VolatilitySwapAsiaExcludingJapan = "ISDA2013VolatilitySwapAsiaExcludingJapan",
    /** The ISDA 2013 European Volatility Swap Master Confirmation Agreement applies. */
    ISDA2013VolatilitySwapEuropean = "ISDA2013VolatilitySwapEuropean",
    /** The ISDA 2013 Volatility Swap Japanese Confirmation Agreement applies. */
    ISDA2013VolatilitySwapJapanese = "ISDA2013VolatilitySwapJapanese",
};

export enum MasterAgreementClauseIdentifierEnum {
    
    /** Date of Agreement */
    ISLA_GMSLA_001 = "ISLA_GMSLA_001",
    /** Parties */
    ISLA_GMSLA_002 = "ISLA_GMSLA_002",
    /** Specific Roles */
    ISLA_GMSLA_003 = "ISLA_GMSLA_003",
    /** Eligible Collateral */
    ISLA_GMSLA_004 = "ISLA_GMSLA_004",
    /** Margin */
    ISLA_GMSLA_005 = "ISLA_GMSLA_005",
    /** Aggregation */
    ISLA_GMSLA_006 = "ISLA_GMSLA_006",
    /** Collateral Disapplication */
    ISLA_GMSLA_007 = "ISLA_GMSLA_007",
    /** Settlement Netting */
    ISLA_GMSLA_008 = "ISLA_GMSLA_008",
    /** Notification Time */
    ISLA_GMSLA_009 = "ISLA_GMSLA_009",
    /** Indemnity */
    ISLA_GMSLA_010 = "ISLA_GMSLA_010",
    /** Base Currency */
    ISLA_GMSLA_011 = "ISLA_GMSLA_011",
    /** Places of Business */
    ISLA_GMSLA_012 = "ISLA_GMSLA_012",
    /** Value */
    ISLA_GMSLA_013 = "ISLA_GMSLA_013",
    /** Automatic Early Termination */
    ISLA_GMSLA_014 = "ISLA_GMSLA_014",
    /** Designated Offices */
    ISLA_GMSLA_015 = "ISLA_GMSLA_015",
    /** Address for Notices */
    ISLA_GMSLA_016 = "ISLA_GMSLA_016",
    /** Process Agent */
    ISLA_GMSLA_017 = "ISLA_GMSLA_017",
    /** Party Acting as Agent */
    ISLA_GMSLA_018 = "ISLA_GMSLA_018",
    /** Pooled Principal Transactions  */
    ISLA_GMSLA_019 = "ISLA_GMSLA_019",
    /** Party Preparing the Agreement  */
    ISLA_GMSLA_020 = "ISLA_GMSLA_020",
    /** Default Interest Rate */
    ISLA_GMSLA_021 = "ISLA_GMSLA_021",
    /** Existing Transactions */
    ISLA_GMSLA_022 = "ISLA_GMSLA_022",
    /** Automation */
    ISLA_GMSLA_023 = "ISLA_GMSLA_023",
    /** Act of Insolvency */
    ISLA_GMSLA_024 = "ISLA_GMSLA_024",
    /** Buy-In */
    ISLA_GMSLA_025 = "ISLA_GMSLA_025",
    /** Currency Conversions */
    ISLA_GMSLA_026 = "ISLA_GMSLA_026",
    /** Scope */
    ISLA_GMSLA_027 = "ISLA_GMSLA_027",
    /** Collateral Delivery Timings */
    ISLA_GMSLA_028 = "ISLA_GMSLA_028",
    /** Delivery */
    ISLA_GMSLA_029 = "ISLA_GMSLA_029",
    /** Substitution of Collateral */
    ISLA_GMSLA_030 = "ISLA_GMSLA_030",
    /** Manufactured Payments */
    ISLA_GMSLA_031 = "ISLA_GMSLA_031",
    /** Corporate Actions */
    ISLA_GMSLA_032 = "ISLA_GMSLA_032",
    /** Payment of Rates */
    ISLA_GMSLA_033 = "ISLA_GMSLA_033",
    /** Rate Applicable to Loaned Securities */
    ISLA_GMSLA_034 = "ISLA_GMSLA_034",
    /** Lender's Right to Terminate a Loan */
    ISLA_GMSLA_035 = "ISLA_GMSLA_035",
    /** Borrower's Right to Terminate a Loan */
    ISLA_GMSLA_036 = "ISLA_GMSLA_036",
    /** Failure to Deliver Event of Default */
    ISLA_GMSLA_037 = "ISLA_GMSLA_037",
    /** Failure to Redeliver */
    ISLA_GMSLA_038 = "ISLA_GMSLA_038",
    /** Assets Transferred to a Trustee */
    ISLA_GMSLA_039 = "ISLA_GMSLA_039",
    /** Suspension Event of Default */
    ISLA_GMSLA_040 = "ISLA_GMSLA_040",
    /** Costs and Expenses */
    ISLA_GMSLA_041 = "ISLA_GMSLA_041",
    /** Set-Off */
    ISLA_GMSLA_042 = "ISLA_GMSLA_042",
    /** Default Market Value Fallbacks */
    ISLA_GMSLA_043 = "ISLA_GMSLA_043",
    /** Assignment */
    ISLA_GMSLA_044 = "ISLA_GMSLA_044",
    /** Telephone Recordings */
    ISLA_GMSLA_045 = "ISLA_GMSLA_045",
    /** Waiver of Immunity */
    ISLA_GMSLA_046 = "ISLA_GMSLA_046",
    /** Agreement to Deliver Documents */
    ISLA_GMSLA_047 = "ISLA_GMSLA_047",
    /** Collateral Transfer Details */
    ISLA_GMSLA_048 = "ISLA_GMSLA_048",
    /** Confidentiality */
    ISLA_GMSLA_049 = "ISLA_GMSLA_049",
    /** Correction */
    ISLA_GMSLA_050 = "ISLA_GMSLA_050",
    /** Minimum Collateral Transfer Amount */
    ISLA_GMSLA_051 = "ISLA_GMSLA_051",
    /** Non-Reliance Representation */
    ISLA_GMSLA_052 = "ISLA_GMSLA_052",
    /** Records and Statements */
    ISLA_GMSLA_053 = "ISLA_GMSLA_053",
    /** Recovery and Resolution */
    ISLA_GMSLA_054 = "ISLA_GMSLA_054",
    /** Security Agreement Details */
    ISLA_GMSLA_055 = "ISLA_GMSLA_055",
    /** Triparty Services */
    ISLA_GMSLA_056 = "ISLA_GMSLA_056",
};

export enum MasterAgreementVariantIdentifierEnum {
    
    /** Agreement is Undated */
    ISLA_GMSLA_001_01 = "ISLA_GMSLA_001_01",
    /** Agreement is Dated */
    ISLA_GMSLA_001_02 = "ISLA_GMSLA_001_02",
    /** Name and Place of Incorporation */
    ISLA_GMSLA_002_01 = "ISLA_GMSLA_002_01",
    /** Names and Place of Incorporation plus Additional Information */
    ISLA_GMSLA_002_02 = "ISLA_GMSLA_002_02",
    /** Defining the Party's Role as Lender or Borrower */
    ISLA_GMSLA_002_03 = "ISLA_GMSLA_002_03",
    /** Non-specific Roles */
    ISLA_GMSLA_003_01 = "ISLA_GMSLA_003_01",
    /** Specific Roles */
    ISLA_GMSLA_003_02 = "ISLA_GMSLA_003_02",
    /** GMSLA Schedule */
    ISLA_GMSLA_004_01 = "ISLA_GMSLA_004_01",
    /** Outside of GMSLA */
    ISLA_GMSLA_004_02 = "ISLA_GMSLA_004_02",
    /** Additional Criteria */
    ISLA_GMSLA_004_03 = "ISLA_GMSLA_004_03",
    /** GMSLA Schedule */
    ISLA_GMSLA_005_01 = "ISLA_GMSLA_005_01",
    /** Outside of GMSLA */
    ISLA_GMSLA_005_02 = "ISLA_GMSLA_005_02",
    /** Aggregation Applies */
    ISLA_GMSLA_006_01 = "ISLA_GMSLA_006_01",
    /** Aggregation Does Not Apply */
    ISLA_GMSLA_006_02 = "ISLA_GMSLA_006_02",
    /** Aggregation Applies Separately to Loan Groups */
    ISLA_GMSLA_006_03 = "ISLA_GMSLA_006_03",
    /** Aggregation Applies to Some but Not All Loans */
    ISLA_GMSLA_006_04 = "ISLA_GMSLA_006_04",
    /** Neither Aggregation nor Loan by Loan Applies */
    ISLA_GMSLA_006_05 = "ISLA_GMSLA_006_05",
    /** Standard */
    ISLA_GMSLA_007_01 = "ISLA_GMSLA_007_01",
    /** Collateral Disapplied */
    ISLA_GMSLA_007_02 = "ISLA_GMSLA_007_02",
    /** Netting of Collateral Shall Apply */
    ISLA_GMSLA_008_01 = "ISLA_GMSLA_008_01",
    /** Netting of Collateral Shall Not Apply */
    ISLA_GMSLA_008_02 = "ISLA_GMSLA_008_02",
    /** Netting of Collateral Shall Apply with Multiple Payments or Delivery Options */
    ISLA_GMSLA_008_03 = "ISLA_GMSLA_008_03",
    /** Netting of Collateral Shall Apply Separately per Group of Loans */
    ISLA_GMSLA_008_04 = "ISLA_GMSLA_008_04",
    /** Specified Time */
    ISLA_GMSLA_009_01 = "ISLA_GMSLA_009_01",
    /** Notification Time by Collateral Type */
    ISLA_GMSLA_009_02 = "ISLA_GMSLA_009_02",
    /** Notification Time as Agreed */
    ISLA_GMSLA_009_03 = "ISLA_GMSLA_009_03",
    /** No Notification Time */
    ISLA_GMSLA_009_04 = "ISLA_GMSLA_009_04",
    /** Indemnity Applies */
    ISLA_GMSLA_010_01 = "ISLA_GMSLA_010_01",
    /** Indemnity does not Apply */
    ISLA_GMSLA_010_02 = "ISLA_GMSLA_010_02",
    /** Single Base Currency */
    ISLA_GMSLA_011_01 = "ISLA_GMSLA_011_01",
    /** Single Base Currency with Fallback */
    ISLA_GMSLA_011_02 = "ISLA_GMSLA_011_02",
    /** Single Base Currency with Multiple Fallback Options */
    ISLA_GMSLA_011_03 = "ISLA_GMSLA_011_03",
    /** Locations are Specified Without Reference to Party */
    ISLA_GMSLA_012_01 = "ISLA_GMSLA_012_01",
    /** Locations are Specified Separately per Party */
    ISLA_GMSLA_012_02 = "ISLA_GMSLA_012_02",
    /** Not all Places of Business Have to be Open */
    ISLA_GMSLA_012_03 = "ISLA_GMSLA_012_03",
    /** Standard Bid Price */
    ISLA_GMSLA_013_01 = "ISLA_GMSLA_013_01",
    /** Standard Mid Price */
    ISLA_GMSLA_013_02 = "ISLA_GMSLA_013_02",
    /** 2018 Standard */
    ISLA_GMSLA_013_03 = "ISLA_GMSLA_013_03",
    /** Borrowers Agreement to Pricing Source */
    ISLA_GMSLA_013_04 = "ISLA_GMSLA_013_04",
    /** Pre-agreed Pricing Source */
    ISLA_GMSLA_013_05 = "ISLA_GMSLA_013_05",
    /** Time Variation */
    ISLA_GMSLA_013_06 = "ISLA_GMSLA_013_06",
    /** Automatic Early Termination does not Apply */
    ISLA_GMSLA_014_01 = "ISLA_GMSLA_014_01",
    /** Automatic Early Termination Applies */
    ISLA_GMSLA_014_02 = "ISLA_GMSLA_014_02",
    /** Automatic Early Termination Applies in Modified Form) */
    ISLA_GMSLA_014_03 = "ISLA_GMSLA_014_03",
    /** Automatic Early Termination is specified separately for each Principal */
    ISLA_GMSLA_014_04 = "ISLA_GMSLA_014_04",
    /** Automatic Early Termination is not applicable unless required due to the systems of law */
    ISLA_GMSLA_014_05 = "ISLA_GMSLA_014_05",
    /** Party Specifies a Single Designated Office */
    ISLA_GMSLA_015_01 = "ISLA_GMSLA_015_01",
    /** Party Specifies Multiple Designated Offices */
    ISLA_GMSLA_015_02 = "ISLA_GMSLA_015_02",
    /** 2000 Standard */
    ISLA_GMSLA_016_01 = "ISLA_GMSLA_016_01",
    /** 2010 Standard */
    ISLA_GMSLA_016_02 = "ISLA_GMSLA_016_02",
    /** 2018 Standard */
    ISLA_GMSLA_016_03 = "ISLA_GMSLA_016_03",
    /** Plus Email */
    ISLA_GMSLA_016_04 = "ISLA_GMSLA_016_04",
    /** Separate Address for Legal and Operational Notices */
    ISLA_GMSLA_016_05 = "ISLA_GMSLA_016_05",
    /** Special Instructions */
    ISLA_GMSLA_016_06 = "ISLA_GMSLA_016_06",
    /** No Process Agent */
    ISLA_GMSLA_017_01 = "ISLA_GMSLA_017_01",
    /** Process Agent Specified */
    ISLA_GMSLA_017_02 = "ISLA_GMSLA_017_02",
    /** Process Agent to be Appointed */
    ISLA_GMSLA_017_03 = "ISLA_GMSLA_017_03",
    /** A Party will not act as Agent */
    ISLA_GMSLA_018_01 = "ISLA_GMSLA_018_01",
    /** A Party may act as Agent */
    ISLA_GMSLA_018_02 = "ISLA_GMSLA_018_02",
    /** A Party will always act as Agent */
    ISLA_GMSLA_018_03 = "ISLA_GMSLA_018_03",
    /** Pooled Principal Transactions Shall Not Apply */
    ISLA_GMSLA_019_01 = "ISLA_GMSLA_019_01",
    /** Pooled Principal Transactions Shall  Apply */
    ISLA_GMSLA_019_02 = "ISLA_GMSLA_019_02",
    /** Pooled Principal Transactions May Apply */
    ISLA_GMSLA_019_03 = "ISLA_GMSLA_019_03",
    /** Simple Election */
    ISLA_GMSLA_020_01 = "ISLA_GMSLA_020_01",
    /** Election with Modifications */
    ISLA_GMSLA_020_02 = "ISLA_GMSLA_020_02",
    /** Term Rate */
    ISLA_GMSLA_021_01 = "ISLA_GMSLA_021_01",
    /** Overnight Rate */
    ISLA_GMSLA_021_02 = "ISLA_GMSLA_021_02",
    /** Risk Free Rate */
    ISLA_GMSLA_021_03 = "ISLA_GMSLA_021_03",
    /** Non-Defaulting Party Election */
    ISLA_GMSLA_021_04 = "ISLA_GMSLA_021_04",
    /** Spread */
    ISLA_GMSLA_021_05 = "ISLA_GMSLA_021_05",
    /** Agreement Covers Existing Loans */
    ISLA_GMSLA_022_01 = "ISLA_GMSLA_022_01",
    /** Agreement Does Not Cover Existing Loans */
    ISLA_GMSLA_022_02 = "ISLA_GMSLA_022_02",
    /** Automation Does Not Apply */
    ISLA_GMSLA_023_01 = "ISLA_GMSLA_023_01",
    /** Automation May Apply */
    ISLA_GMSLA_023_02 = "ISLA_GMSLA_023_02",
    /** Standard Pre-Print */
    ISLA_GMSLA_024_01 = "ISLA_GMSLA_024_01",
    /** Grace Period Amendment */
    ISLA_GMSLA_024_02 = "ISLA_GMSLA_024_02",
    /** Jurisdictional Amendments */
    ISLA_GMSLA_024_03 = "ISLA_GMSLA_024_03",
    /** Transferor Pays Costs and Expenses */
    ISLA_GMSLA_025_01 = "ISLA_GMSLA_025_01",
    /** Transferor Pays Costs and Expenses other than those arising from Negligence */
    ISLA_GMSLA_025_02 = "ISLA_GMSLA_025_02",
    /** Transferor only Liable for Cost and Expenses if Reasonable Notice of Buy-in */
    ISLA_GMSLA_025_03 = "ISLA_GMSLA_025_03",
    /** Buy-in Expanded to Cover Buy-in Exercised by an Exchange */
    ISLA_GMSLA_025_04 = "ISLA_GMSLA_025_04",
    /** Standard */
    ISLA_GMSLA_026_01 = "ISLA_GMSLA_026_01",
    /** Selecting Party other than Lender */
    ISLA_GMSLA_026_02 = "ISLA_GMSLA_026_02",
    /** Variation of Exchange Rate Source */
    ISLA_GMSLA_026_03 = "ISLA_GMSLA_026_03",
    /** Standard Scope */
    ISLA_GMSLA_027_01 = "ISLA_GMSLA_027_01",
    /** Limited Scope */
    ISLA_GMSLA_027_02 = "ISLA_GMSLA_027_02",
    /** Same Day */
    ISLA_GMSLA_028_01 = "ISLA_GMSLA_028_01",
    /** Alternative Delivery Time */
    ISLA_GMSLA_028_02 = "ISLA_GMSLA_028_02",
    /** Same Day with Notification Time */
    ISLA_GMSLA_028_03 = "ISLA_GMSLA_028_03",
    /** Alternative Delivery Time with Notification Time */
    ISLA_GMSLA_028_04 = "ISLA_GMSLA_028_04",
    /** Asset Dependent */
    ISLA_GMSLA_028_05 = "ISLA_GMSLA_028_05",
    /** Simultaneous delivery of securities and collateral */
    ISLA_GMSLA_029_01 = "ISLA_GMSLA_029_01",
    /** Collateral Delivery as specified in the Security Agreement */
    ISLA_GMSLA_029_02 = "ISLA_GMSLA_029_02",
    /** Lender to Deliver Securities once Collateral is Delivered */
    ISLA_GMSLA_029_03 = "ISLA_GMSLA_029_03",
    /** Borrower Request */
    ISLA_GMSLA_030_01 = "ISLA_GMSLA_030_01",
    /** Borrower Request/Lender Consent */
    ISLA_GMSLA_030_02 = "ISLA_GMSLA_030_02",
    /** Lender or Borrower Request */
    ISLA_GMSLA_030_03 = "ISLA_GMSLA_030_03",
    /** Pre-approval of Alternative Collateral */
    ISLA_GMSLA_030_04 = "ISLA_GMSLA_030_04",
    /** Manufactured Payment of Amount Such Party Would Be Entitled to Receive */
    ISLA_GMSLA_031_01 = "ISLA_GMSLA_031_01",
    /** Manufactured Payment of Amount Such Lender Would Be Entitled to Receive */
    ISLA_GMSLA_031_02 = "ISLA_GMSLA_031_02",
    /** Manufactured Payment Only in Relation to Loaned Securities */
    ISLA_GMSLA_031_03 = "ISLA_GMSLA_031_03",
    /** Additional Sum to Be Paid to Cover Tax Relief */
    ISLA_GMSLA_031_04 = "ISLA_GMSLA_031_04",
    /** Notice Requirement */
    ISLA_GMSLA_031_05 = "ISLA_GMSLA_031_05",
    /** Standard */
    ISLA_GMSLA_032_01 = "ISLA_GMSLA_032_01",
    /** Reasonable Notice Defined */
    ISLA_GMSLA_032_02 = "ISLA_GMSLA_032_02",
    /** No Right to Instruct */
    ISLA_GMSLA_032_03 = "ISLA_GMSLA_032_03",
    /** Payment Within a Week */
    ISLA_GMSLA_033_01 = "ISLA_GMSLA_033_01",
    /** Payment Within 10 Days */
    ISLA_GMSLA_033_02 = "ISLA_GMSLA_033_02",
    /** Payment Upon Maturity */
    ISLA_GMSLA_033_03 = "ISLA_GMSLA_033_03",
    /** Such Rate as Agreed */
    ISLA_GMSLA_034_01 = "ISLA_GMSLA_034_01",
    /** VAT Added */
    ISLA_GMSLA_034_02 = "ISLA_GMSLA_034_02",
    /** No Deduction */
    ISLA_GMSLA_034_03 = "ISLA_GMSLA_034_03",
    /** No Rate Payable */
    ISLA_GMSLA_034_04 = "ISLA_GMSLA_034_04",
    /** Lender May Terminate a Loan at any Time */
    ISLA_GMSLA_035_01 = "ISLA_GMSLA_035_01",
    /** Lender May Not Terminate a Loan */
    ISLA_GMSLA_035_02 = "ISLA_GMSLA_035_02",
    /** Borrower May Terminate a Loan at Any Time */
    ISLA_GMSLA_036_01 = "ISLA_GMSLA_036_01",
    /** Borrower May Terminate a Loan Subject to Notice */
    ISLA_GMSLA_036_02 = "ISLA_GMSLA_036_02",
    /** Borrower May Terminate a Loan Subject to Limitations Concerning Corporate Actions */
    ISLA_GMSLA_036_03 = "ISLA_GMSLA_036_03",
    /** Borrower May Terminate a Loan Subject to Paying the Rate for the Full Term */
    ISLA_GMSLA_036_04 = "ISLA_GMSLA_036_04",
    /** Failure to Deliver Event of Default Applies */
    ISLA_GMSLA_037_01 = "ISLA_GMSLA_037_01",
    /** Failure to Deliver Event of Default does not Apply */
    ISLA_GMSLA_037_02 = "ISLA_GMSLA_037_02",
    /** Failure to Deliver Event of Default does not Apply to Lender */
    ISLA_GMSLA_037_03 = "ISLA_GMSLA_037_03",
    /** 2000 Standard */
    ISLA_GMSLA_038_01 = "ISLA_GMSLA_038_01",
    /** 2010 Standard */
    ISLA_GMSLA_038_02 = "ISLA_GMSLA_038_02",
    /** 2018 Standard */
    ISLA_GMSLA_038_03 = "ISLA_GMSLA_038_03",
    /** 2000 Modified No Lender Close Out */
    ISLA_GMSLA_038_04 = "ISLA_GMSLA_038_04",
    /** 2000 Standard */
    ISLA_GMSLA_039_01 = "ISLA_GMSLA_039_01",
    /** 2010/2018 Standard */
    ISLA_GMSLA_039_02 = "ISLA_GMSLA_039_02",
    /** Hybrid */
    ISLA_GMSLA_039_03 = "ISLA_GMSLA_039_03",
    /** 2000 Standard */
    ISLA_GMSLA_040_01 = "ISLA_GMSLA_040_01",
    /** 2010/2018 Standard */
    ISLA_GMSLA_040_02 = "ISLA_GMSLA_040_02",
    /** Hybrid */
    ISLA_GMSLA_040_03 = "ISLA_GMSLA_040_03",
    /** Standard Costs and Expenses */
    ISLA_GMSLA_041_01 = "ISLA_GMSLA_041_01",
    /** Limitation of Costs and Expenses */
    ISLA_GMSLA_041_02 = "ISLA_GMSLA_041_02",
    /** Expansion of Costs and Expenses */
    ISLA_GMSLA_041_03 = "ISLA_GMSLA_041_03",
    /** No Contractual Set-Off */
    ISLA_GMSLA_042_01 = "ISLA_GMSLA_042_01",
    /** Simple Contractual Set-Off */
    ISLA_GMSLA_042_02 = "ISLA_GMSLA_042_02",
    /** Set-Off with Unascertained Obligations Amendment */
    ISLA_GMSLA_042_03 = "ISLA_GMSLA_042_03",
    /** Standard Paragraph 11.2(a) */
    ISLA_GMSLA_043_01 = "ISLA_GMSLA_043_01",
    /** Amended Paragraph 11.2,(a) applies */
    ISLA_GMSLA_043_02 = "ISLA_GMSLA_043_02",
    /** Consent */
    ISLA_GMSLA_044_01 = "ISLA_GMSLA_044_01",
    /** Consent with Standard Exclusions */
    ISLA_GMSLA_044_02 = "ISLA_GMSLA_044_02",
    /** Consent with Additional Exclusions */
    ISLA_GMSLA_044_03 = "ISLA_GMSLA_044_03",
    /** Pre-approved Assignments */
    ISLA_GMSLA_044_04 = "ISLA_GMSLA_044_04",
    /** Parties May Record All Conversations */
    ISLA_GMSLA_045_01 = "ISLA_GMSLA_045_01",
    /** Parties Agree to Obtain Consent */
    ISLA_GMSLA_045_02 = "ISLA_GMSLA_045_02",
    /** Parties Limit the Conversations that May be Recorded */
    ISLA_GMSLA_045_03 = "ISLA_GMSLA_045_03",
    /** Submission as Evidence */
    ISLA_GMSLA_045_04 = "ISLA_GMSLA_045_04",
    /** Standard Waiver of Immunity Applies */
    ISLA_GMSLA_046_01 = "ISLA_GMSLA_046_01",
    /** Waiver of Immunity may Not Apply */
    ISLA_GMSLA_046_02 = "ISLA_GMSLA_046_02",
    /** No Additional Documentation Required */
    ISLA_GMSLA_047_01 = "ISLA_GMSLA_047_01",
    /** Additional Documentation Required */
    ISLA_GMSLA_047_02 = "ISLA_GMSLA_047_02",
    /** Collateral Transfer Details not included */
    ISLA_GMSLA_048_01 = "ISLA_GMSLA_048_01",
    /** Collateral Transfer Details included */
    ISLA_GMSLA_048_02 = "ISLA_GMSLA_048_02",
    /** Confidentiality Clause */
    ISLA_GMSLA_049_01 = "ISLA_GMSLA_049_01",
    /** Permitted Disclosure Clause */
    ISLA_GMSLA_049_02 = "ISLA_GMSLA_049_02",
    /** Paragraph 20.1 Amended to Refer  Paragraph 6 */
    ISLA_GMSLA_050_01 = "ISLA_GMSLA_050_01",
    /** Paragraph 27.2 Amended to refer to the 2010 GMSLA */
    ISLA_GMSLA_050_02 = "ISLA_GMSLA_050_02",
    /** MCTA  Delivery only */
    ISLA_GMSLA_051_01 = "ISLA_GMSLA_051_01",
    /** MCTA  Delivery and Re-Delivery */
    ISLA_GMSLA_051_02 = "ISLA_GMSLA_051_02",
    /** MCTA  Drops to Zero for a Defaulting Party */
    ISLA_GMSLA_051_03 = "ISLA_GMSLA_051_03",
    /** No Non-Reliance Representation */
    ISLA_GMSLA_052_01 = "ISLA_GMSLA_052_01",
    /** Non-Reliance Representation Added */
    ISLA_GMSLA_052_02 = "ISLA_GMSLA_052_02",
    /** No Records and Statements Clause */
    ISLA_GMSLA_053_01 = "ISLA_GMSLA_053_01",
    /** Records and Statements Clause Added */
    ISLA_GMSLA_053_02 = "ISLA_GMSLA_053_02",
    /** Recovery and Resolution not Included */
    ISLA_GMSLA_054_01 = "ISLA_GMSLA_054_01",
    /** Recovery and Resolution Included in GMSLA */
    ISLA_GMSLA_054_02 = "ISLA_GMSLA_054_02",
    /** Recovery and Resolution Included by Protocol */
    ISLA_GMSLA_054_03 = "ISLA_GMSLA_054_03",
    /** Recovery and Resolution Incorporated by Reference */
    ISLA_GMSLA_054_04 = "ISLA_GMSLA_054_04",
    /** Security Agreement Details Included */
    ISLA_GMSLA_055_01 = "ISLA_GMSLA_055_01",
    /** Triparty Services Not Referenced */
    ISLA_GMSLA_056_01 = "ISLA_GMSLA_056_01",
    /** Triparty Services May Apply */
    ISLA_GMSLA_056_02 = "ISLA_GMSLA_056_02",
};
/**
* The potential options for specifying whether a particular election applies in a GMSLA.
*/
export enum ApplicabilityOfElectionEnum {
    
    /** The election is applicable. */
    Applicable = "Applicable",
    /** The election is not applicable. */
    NotApplicable = "NotApplicable",
    /** The applicability of the election has not been specified. */
    NotSpecified = "NotSpecified",
};
/**
* The potential options for specifying whether mark to market value of collateral is determined on an aggregated or loan by loan basis.
*/
export enum AggregationSpecificationEnum {
    
    /** The market value is determined on an aggregated basis. */
    Aggregated = "Aggregated",
    /** The market value is determined on a loan by loan basis. */
    LoanByLoan = "LoanByLoan",
    /** The market value is determined on an aggregated basis for some loans but on a Loan by Loan basis for others. */
    AggregatedAndLoanByLoan = "AggregatedAndLoanByLoan",
    /** It is not specified how the market value is determined. */
    NotSpecified = "NotSpecified",
};
/**
* The enumerated values to specify the type of Broker Confirm that the FpML trade represents.
*/
export enum BrokerConfirmationTypeEnum {
    
    /** Broker Confirmation Type representing ABX index trades. */
    ABX = "ABX",
    /** Broker Confirmation Type of Asia Corporate. */
    AsiaCorporate = "AsiaCorporate",
    /** Broker Confirmation Type of Asia Sovereign. */
    AsiaSovereign = "AsiaSovereign",
    /** Broker Confirmation Type of Australia Corporate. */
    AustraliaCorporate = "AustraliaCorporate",
    /** Broker Confirmation Type of Australia Sovereign. */
    AustraliaSovereign = "AustraliaSovereign",
    /** Broker Confirmation Type for use with Credit Derivative Transactions on Leveraged Loans. */
    CDSonLeveragedLoans = "CDSonLeveragedLoans",
    /** Broker Confirmation Type for use with Credit Derivative Transactions on Mortgage-backed Security with Pay-As-You-Go or Physical Settlement. */
    CDSonMBS = "CDSonMBS",
    /** Broker Confirmation Type for CDX Emerging Markets Untranched Transactions. */
    CDXEmergingMarkets = "CDXEmergingMarkets",
    /** Broker Confirmation Type for CDX Emerging Markets Diversified Untranched Transactions. */
    CDXEmergingMarketsDiversified = "CDXEmergingMarketsDiversified",
    /** Broker Confirmation Type for CDX Swaption Transactions. */
    CDXSwaption = "CDXSwaption",
    /** Broker Confirmation Type for Dow Jones CDX Tranche Transactions. */
    CDXTranche = "CDXTranche",
    /** Broker Confirmation Type representing CMBX index trades. */
    CMBX = "CMBX",
    /** Broker Confirmation Type for CDS Index trades relating to Dow Jones CDX.EM index series. */
    DJ_CDX_EM_ = "DJ_CDX_EM_",
    /** Broker Confirmation Type for CDS Index trades relating to Dow Jones CDX.NA.IG and Dow Jones CDX.NA.HY index series. */
    DJ_CDX_NA = "DJ_CDX_NA",
    /** Broker Confirmation Type of Emerging European and Middle Eastern Sovereign. */
    EmergingEuropeanAndMiddleEasternSovereign = "EmergingEuropeanAndMiddleEasternSovereign",
    /** Broker Confirmation Type for EMERGING EUROPEAN CORPORATE. */
    EmergingEuropeanCorporate = "EmergingEuropeanCorporate",
    /** Broker Confirmation Type for EMERGING EUROPEAN CORPORATE LPN. */
    EmergingEuropeanCorporateLPN = "EmergingEuropeanCorporateLPN",
    /** Broker Confirmation Type for Single Name European CMBS Transactions. */
    EuropeanCMBS = "EuropeanCMBS",
    /** Broker Confirmation Type of European Corporate. */
    EuropeanCorporate = "EuropeanCorporate",
    /** Broker Confirmation Type for Single Name European RMBS Transactions. */
    EuropeanRMBS = "EuropeanRMBS",
    /** Broker Confirmation Type of Japan Corporate. */
    JapanCorporate = "JapanCorporate",
    /** Broker Confirmation Type of Japan Sovereign. */
    JapanSovereign = "JapanSovereign",
    /** Broker Confirmation Type of Latin America Corporate. */
    LatinAmericaCorporate = "LatinAmericaCorporate",
    /** Broker Confirmation Type for LATIN AMERICA CORPORATE B. */
    LatinAmericaCorporateBond = "LatinAmericaCorporateBond",
    /** Broker Confirmation Type for LATIN AMERICA CORPORATE BL. */
    LatinAmericaCorporateBondOrLoan = "LatinAmericaCorporateBondOrLoan",
    /** Broker Confirmation Type of Latin America Sovereign. */
    LatinAmericaSovereign = "LatinAmericaSovereign",
    /** Broker Confirmation Type for MBX Transactions. */
    MBX = "MBX",
    /** Broker Confirmation Type for Municipal CDX Untranched Transactions. */
    MCDX = "MCDX",
    /** Broker Confirmation Type of New Zealand Corporate. */
    NewZealandCorporate = "NewZealandCorporate",
    /** Broker Confirmation Type of New Zealand Sovereign. */
    NewZealandSovereign = "NewZealandSovereign",
    /** Broker ConfirmationType of North American Corporate. */
    NorthAmericanCorporate = "NorthAmericanCorporate",
    /** Broker Confirmation Type for PO Index Transactions. */
    PO = "PO",
    /** Broker Confirmation Type of Singapore Corporate. */
    SingaporeCorporate = "SingaporeCorporate",
    /** Broker Confirmation Type of Singapore Sovereign. */
    SingaporeSovereign = "SingaporeSovereign",
    /** Broker Confirmation Type of STANDARD ASIA CORPORATE. */
    StandardAsiaCorporate = "StandardAsiaCorporate",
    /** Broker Confirmation Type of STANDARD ASIA SOVEREIGN. */
    StandardAsiaSovereign = "StandardAsiaSovereign",
    /** Broker Confirmation Type of STANDARD AUSTRALIA CORPORATE. */
    StandardAustraliaCorporate = "StandardAustraliaCorporate",
    /** Broker Confirmation Type of STANDARD AUSTRALIA SOVEREIGN. */
    StandardAustraliaSovereign = "StandardAustraliaSovereign",
    /** Broker Confirmation Type for Standard CDX Tranche Transactions. */
    StandardCDXTranche = "StandardCDXTranche",
    /** Broker Confirmation Type of STANDARD EMERGING EUROPEAN AND MIDDLE EASTERN SOVEREIGN. */
    StandardEmergingEuropeanAndMiddleEasternSovereign = "StandardEmergingEuropeanAndMiddleEasternSovereign",
    /** Broker Confirmation Type of STANDARD EMERGING EUROPEAN CORPORATE. */
    StandardEmergingEuropeanCorporate = "StandardEmergingEuropeanCorporate",
    /** Broker Confirmation Type of STANDARD EMERGING EUROPEAN CORPORATE LPN. */
    StandardEmergingEuropeanCorporateLPN = "StandardEmergingEuropeanCorporateLPN",
    /** Broker Confirmation Type for STANDARD EUROPEAN CORPORATE. */
    StandardEuropeanCorporate = "StandardEuropeanCorporate",
    /** Broker Confirmation Type of STANDARD JAPAN CORPORATE. */
    StandardJapanCorporate = "StandardJapanCorporate",
    /** Broker Confirmation Type of STANDARD JAPAN SOVEREIGN. */
    StandardJapanSovereign = "StandardJapanSovereign",
    /** Standard Syndicated Secured Loan Credit Default Swap Broker Confirmation Type. */
    StandardLCDS = "StandardLCDS",
    /** Broker Confirmation Type for Standard Syndicated Secured Loan Credit Default Swap Bullet Transactions. */
    StandardLCDSBullet = "StandardLCDSBullet",
    /** Broker Confirmation Type for Standard Syndicated Secured Loan Credit Default Swap Index Bullet Transactions. */
    StandardLCDXBullet = "StandardLCDXBullet",
    /** Broker Confirmation Type for Standard Syndicated Secured Loan Credit Default Swap Index Bullet Tranche Transactions. */
    StandardLCDXBulletTranche = "StandardLCDXBulletTranche",
    /** Broker Confirmation Type of STANDARD LATIN AMERICA CORPORATE B. */
    StandardLatinAmericaCorporateBond = "StandardLatinAmericaCorporateBond",
    /** Broker Confirmation Type of STANDARD LATIN AMERICA CORPORATE BL. */
    StandardLatinAmericaCorporateBondOrLoan = "StandardLatinAmericaCorporateBondOrLoan",
    /** Broker Confirmation Type of STANDARD LATIN AMERICA SOVEREIGN. */
    StandardLatinAmericaSovereign = "StandardLatinAmericaSovereign",
    /** Broker Confirmation Type of STANDARD NEW ZEALAND CORPORATE. */
    StandardNewZealandCorporate = "StandardNewZealandCorporate",
    /** Broker Confirmation Type of STANDARD NEW ZEALAND SOVEREIGN. */
    StandardNewZealandSovereign = "StandardNewZealandSovereign",
    /** Broker Confirmation Type for STANDARD NORTH AMERICAN CORPORATE. */
    StandardNorthAmericanCorporate = "StandardNorthAmericanCorporate",
    /** Broker Confirmation Type of STANDARD SINGAPORE CORPORATE. */
    StandardSingaporeCorporate = "StandardSingaporeCorporate",
    /** Broker Confirmation Type of STANDARD SINGAPORE SOVEREIGN. */
    StandardSingaporeSovereign = "StandardSingaporeSovereign",
    /** Broker Confirmation Type for STANDARD SUBORDINATED EUROPEAN INSURANCE CORPORATE. */
    StandardSubordinatedEuropeanInsuranceCorporate = "StandardSubordinatedEuropeanInsuranceCorporate",
    /** Broker Confirmation Type for STANDARD WESTERN EUROPEAN SOVEREIGN. */
    StandardWesternEuropeanSovereign = "StandardWesternEuropeanSovereign",
    /** Broker Confirmation Type for Standard iTraxx Europe Tranched Transactions. */
    StandardiTraxxEuropeTranche = "StandardiTraxxEuropeTranche",
    /** Broker Confirmation Type of Subordinated European Insurance Corporate. */
    SubordinatedEuropeanInsuranceCorporate = "SubordinatedEuropeanInsuranceCorporate",
    /** Broker Confirmation Type of SUKUK CORPORATE. */
    SukukCorporate = "SukukCorporate",
    /** Broker Confirmation Type of SUKUK SOVEREIGN. */
    SukukSovereign = "SukukSovereign",
    /** Syndicated Secured Loan Credit Default Swap Broker Confirmation Type. */
    SyndicatedSecuredLoanCDS = "SyndicatedSecuredLoanCDS",
    /** Broker Confirmation Type for TRX Transactions. */
    TRX = "TRX",
    /** Broker Confirmation Type for TRX.II Transactions. */
    TRX_II = "TRX_II",
    /** Broker Confirmation Type for U.S. MUNICIPAL FULL FAITH AND CREDIT. */
    USMunicipalFullFaithAndCredit = "USMunicipalFullFaithAndCredit",
    /** Broker Confirmation Type for U.S. MUNICIPAL GENERAL FUND. */
    USMunicipalGeneralFund = "USMunicipalGeneralFund",
    /** Broker Confirmation Type for U.S. MUNICIPAL REVENUE. */
    USMunicipalRevenue = "USMunicipalRevenue",
    /** Broker Confirmation Type of Western European Sovereign. */
    WesternEuropeanSovereign = "WesternEuropeanSovereign",
    /** Broker Confirmation Type for iTraxx Asia Excluding Japan. */
    iTraxxAsiaExJapan = "iTraxxAsiaExJapan",
    /** Broker Confirmation Type for iTraxx Asia Ex-Japan Swaption Transactions. */
    iTraxxAsiaExJapanSwaption = "iTraxxAsiaExJapanSwaption",
    /** Broker Confirmation Type for iTraxx Asia Excluding Japan Tranched Transactions. */
    iTraxxAsiaExJapanTranche = "iTraxxAsiaExJapanTranche",
    /** Broker Confirmation Type for iTraxx Australia. */
    iTraxxAustralia = "iTraxxAustralia",
    /** Broker Confirmation Type for iTraxx Australia Swaption Transactions. */
    iTraxxAustraliaSwaption = "iTraxxAustraliaSwaption",
    /** Broker Confirmation Type for iTraxx Australia Tranched Transactions. */
    iTraxxAustraliaTranche = "iTraxxAustraliaTranche",
    /** Broker Confirmation Type for iTraxx CJ. */
    iTraxxCJ = "iTraxxCJ",
    /** Broker Confirmation Type for iTraxx CJ Tranched Transactions. */
    iTraxxCJTranche = "iTraxxCJTranche",
    /** Broker Confirmation Type for iTraxx Europe Transactions */
    iTraxxEurope = "iTraxxEurope",
    /** Broker Confirmation Type for iTraxx Europe Swaption Transactions. */
    iTraxxEuropeSwaption = "iTraxxEuropeSwaption",
    /** Broker Confirmation Type for iTraxx Europe Tranched Transactions. */
    iTraxxEuropeTranche = "iTraxxEuropeTranche",
    /** Broker Confirmation Type for iTraxx Japan. */
    iTraxxJapan = "iTraxxJapan",
    /** Broker Confirmation Type for iTraxx Japan Swaption Transactions. */
    iTraxxJapanSwaption = "iTraxxJapanSwaption",
    /** Broker Confirmation Type for iTraxx Japan Tranched Transactions. */
    iTraxxJapanTranche = "iTraxxJapanTranche",
    /** Broker Confirmation Type for iTraxx LevX. */
    iTraxxLevX = "iTraxxLevX",
    /** Broker Confirmation Type for iTraxx SDI 75 Transactions. */
    iTraxxSDI75 = "iTraxxSDI75",
    /** Broker Confirmation Type for iTraxx SovX. */
    iTraxxSovX = "iTraxxSovX",
};
/**
* Defines the enumerated values to specify the determination roles to the transaction. Such roles mostly address any determination that would be required when some Extraordinary Events would happen, for instance qualifying the effectiveness of such event, or when a calculation is required, etc. else any other kind of determination as need be.
*/
export enum DeterminationRoleEnum {
    
    /** Specifies the party responsible for performing calculation agent duties as defined in the applicable product definition. As an indication, the role of Calculation Agent is key in regards of other roles, for mainly two reasons : first is that it is a fallback role i.e. whenever another role is not defined, then such role would be assumed by the Calculation Agent ; second is that related determination are not limited to Extraordinary Event per se i.e. any determinationr required in regards of Payout calculations for instance would fall on Calculation Agent, unless otherwise specified in Calculation Agent attached to Economic Terms. */
    CalculationAgent = "CalculationAgent",
    /** Specifies the party responsible for performing related HedgingParty duties as defined in the applicable product definition, notably in regards of particular Extraordinary Events or Price Determination Methods which involve hedging considerations. */
    HedgingParty = "HedgingParty",
    /** Specifies the party responsible for performing related HedgingParty duties as defined in the applicable product definition, notably in regards of particular Disruption Events or Early Termination Terms which may involve the calculation of a liquidation or compensation value amount. */
    DeterminingParty = "DeterminingParty",
};
/**
* The enumerated values to specify whether such terms i.e. Illegality or Impossibility shall be treated either as defined per the documents applicable to the FX transaction.
*/
export enum FxIllegalityOrImpossibilityEnum {
    
    ToBeTreatedAsDisruptionEvent = "ToBeTreatedAsDisruptionEvent",
    ToBeTreatedAsIllegalityOrImpossibility = "ToBeTreatedAsIllegalityOrImpossibility",
};
/**
* The enumerated values to specify whether such terms i.e. Force Majeure or Act Of State shall be treated either as defined per the documents applicable to the FX transaction.
*/
export enum FxForceMajeureOrActOfSStateEnum {
    
    ToBeTreatedAsDisruptionEvent = "ToBeTreatedAsDisruptionEvent",
    ToBeTreatedAsForceMajeureOrActOfSState = "ToBeTreatedAsForceMajeureOrActOfSState",
};

export enum SpecifiedValueEnum {
    
    OutstandingPrincipalBalance = "OutstandingPrincipalBalance",
    StatedPrincipalBalance = "StatedPrincipalBalance",
    FaceValue = "FaceValue",
    MarketValue = "MarketValue",
};

export enum FxSubstitutionProvisionTypeEnum {
    
    LocalAssetSubstituteGross = "LocalAssetSubstituteGross",
    LocalAssetSubstituteNet = "LocalAssetSubstituteNet",
    LocalCurrencySubstitute = "LocalCurrencySubstitute",
};

export enum StandardizedScheduleAssetClassEnum {
    
    InterestRates = "InterestRates",
    Credit = "Credit",
    ForeignExchange = "ForeignExchange",
    Equity = "Equity",
    Commodity = "Commodity",
};

export enum StandardizedScheduleProductClassEnum {
    
    Swap = "Swap",
    CrossCurrencySwap = "CrossCurrencySwap",
    Swaption = "Swaption",
    SwaptionStraddle = "SwaptionStraddle",
    SwapWithCallableBermudanRightToEnterExitSwaps = "SwapWithCallableBermudanRightToEnterExitSwaps",
    IRExoticSwapWithAnExoticCouponAgainstAFloatingLeg = "IRExoticSwapWithAnExoticCouponAgainstAFloatingLeg",
    Option = "Option",
    ForwardRateAgreement = "ForwardRateAgreement",
    SingleNameCreditDefaultSwap = "SingleNameCreditDefaultSwap",
    IndexCDS = "IndexCDS",
    IndexTranche = "IndexTranche",
    CreditNthToDefault = "CreditNthToDefault",
    CreditTotalReturnSwapOnABond = "CreditTotalReturnSwapOnABond",
    DeliverableSwap = "DeliverableSwap",
    NonDeliverableCrossCurrencySwap = "NonDeliverableCrossCurrencySwap",
    DeliverableForward = "DeliverableForward",
    NonDeliverableForward = "NonDeliverableForward",
    DeliverableOption = "DeliverableOption",
    NonDeliverableOption = "NonDeliverableOption",
    VarianceSwap = "VarianceSwap",
    VolatilitySwap = "VolatilitySwap",
    CorrelationSwap = "CorrelationSwap",
    Forward = "Forward",
    DividendSwap = "DividendSwap",
    DeliverableOptionF = "DeliverableOptionF",
    ContractForDifference = "ContractForDifference",
    SwapsAndPortfolioSwaps = "SwapsAndPortfolioSwaps",
    FixedFloatSwap = "FixedFloatSwap",
    BasisSwap = "BasisSwap",
};
/**
* The enumerated values to specify the list of information providers.
*/
export enum InformationProviderEnum {
    
    /** The Association of Banks in Singapore. */
    AssocBanksSingapore = "AssocBanksSingapore",
    /** The central bank of Chile. */
    BancoCentralChile = "BancoCentralChile",
    /** The central bank of Canada. */
    BankOfCanada = "BankOfCanada",
    /** The Bank Of England. */
    BankOfEngland = "BankOfEngland",
    /** The central bank of Japan. */
    BankOfJapan = "BankOfJapan",
    /** Bloomberg LP. */
    Bloomberg = "Bloomberg",
    /** The European Central Bank. */
    EuroCentralBank = "EuroCentralBank",
    /** The Federal Reserve, the central bank of the United States. */
    FederalReserve = "FederalReserve",
    /** The Federal Home Loan Bank of San Francisco, or its successor. */
    FHLBSF = "FHLBSF",
    /** ICESWAP Rate Administrator which means ICE Benchmark Administration, or any successor thereto, as administrator of the ICE Swap Rate. */
    ICESWAP = "ICESWAP",
    /** International Swaps and Derivatives Association, Inc. */
    ISDA = "ISDA",
    /** Refinitiv, formerly Thomson Reuters Financial & Risk. */
    Refinitiv = "Refinitiv",
    /** The Reserve Bank of Australia. */
    ReserveBankAustralia = "ReserveBankAustralia",
    /** The Reserve Bank of New Zealand. */
    ReserveBankNewZealand = "ReserveBankNewZealand",
    /** Reuters Group Plc. */
    Reuters = "Reuters",
    /** South African Futures Exchange, or its successor. */
    SAFEX = "SAFEX",
    /** Telerate, Inc. */
    Telerate = "Telerate",
    /** The Tokyo Swap Reference Rate (or TSR) Administrator, which means Refinitiv Asia Pacific Limited, or any successor thereto, as administrator of the TSR. */
    TOKYOSWAP = "TOKYOSWAP",
};
/**
* Enumerated values to specify whether the price is expressed in absolute or relative terms.
*/
export enum PriceExpressionEnum {
    
    /** The price is expressed as an absolute amount. */
    AbsoluteTerms = "AbsoluteTerms",
    /** The price is expressed in percentage of the notional amount. */
    PercentageOfNotional = "PercentageOfNotional",
    /** Denotes a price expressed in percentage of face value with fractions which is used for quoting bonds, e.g. 101 3/8 indicates that the buyer will pay 101.375 of the face value. */
    ParValueFraction = "ParValueFraction",
    /** Denotes a price expressed per number of options. */
    PerOption = "PerOption",
};
/**
* The enumerated values to specify the side from which perspective a value is quoted.
*/
export enum QuotationSideEnum {
    
    /** Denotes a value as the Afternoon fixing reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Afternoon = "Afternoon",
    /** Denotes a value 'asked' by a seller for an asset, i.e. the value at which a seller is willing to sell. */
    Ask = "Ask",
    /** Denotes a value 'bid' by a buyer for an asset, i.e. the value a buyer is willing to pay. */
    Bid = "Bid",
    /** Denotes a value as the Closing price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Closing = "Closing",
    /** Denotes a value as the High price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    High = "High",
    /** Denotes a value as the Index price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Index = "Index",
    /** Denotes a value as the Average of the Bid and Ask prices reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    MeanOfBidAndAsk = "MeanOfBidAndAsk",
    /** Denotes a value as the Locational Marginal price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    LocationalMarginal = "LocationalMarginal",
    /** Denotes a value as the Low price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Low = "Low",
    /** Denotes a value as the Marginal Hourly price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    MarginalHourly = "MarginalHourly",
    /** Denotes a value as the Market Clearing price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    MarketClearing = "MarketClearing",
    /** Denotes a value as the Average of the High and Low prices reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    MeanOfHighAndLow = "MeanOfHighAndLow",
    /** Denotes a value as the Morning fixing reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Morning = "Morning",
    /** Denotes a value as the Official price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Official = "Official",
    /** Denotes a value as the Opening price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Opening = "Opening",
    /** Denotes a value as the Official Settlement Price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    OSP = "OSP",
    /** Denotes a value as the Settlement price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Settlement = "Settlement",
    /** Denotes a value as the Spot price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Spot = "Spot",
    /** Denotes a value as the Average of the Midpoint of prices reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    Mid = "Mid",
    /** Denotes a value as the National Single price reported in or by the relevant Price Source as specified in the relevant Confirmation. */
    NationalSingle = "NationalSingle",
    /** Denotes a value as the Volume Weighted Average of prices effective on the Pricing Date. */
    WeightedAverage = "WeightedAverage",
    /** Denotes a value as the Non-volume Weighted Average of prices effective on the Pricing Date. */
    UnWeightedAverage = "UnWeightedAverage",
};
/**
* The enumerated values to specify the actual quotation style (e.g. PointsUpFront, TradedSpread) used to quote a credit default swap fee leg.
*/
export enum QuotationStyleEnum {
    
    /** When quotation style is 'PointsUpFront', the initialPoints element of the Credit Default Swap feeLeg should be populated */
    PointsUpFront = "PointsUpFront",
    /** When quotation style is 'TradedSpread', the marketFixedRate element of the Credit Default Swap feeLeg should be populated */
    TradedSpread = "TradedSpread",
    /** When quotation style is 'Price', the marketPrice element of the Credit Default Swap feeLeg should be populated */
    Price = "Price",
};
/**
* The enumerated values to specify the ISDA defined methodology for determining the final price of the reference obligation for purposes of cash settlement.
*/
export enum ValuationMethodEnum {
    
    Market = "Market",
    Highest = "Highest",
    AverageMarket = "AverageMarket",
    AverageHighest = "AverageHighest",
    BlendedMarket = "BlendedMarket",
    BlendedHighest = "BlendedHighest",
    AverageBlendedMarket = "AverageBlendedMarket",
    AverageBlendedHighest = "AverageBlendedHighest",
};
/**
* The enumeration values to specify the Commodity Reference Prices specified in the Annex to the 2005 ISDA Commodity Definitions.
*/
export enum CommodityReferencePriceEnum {
    
    /** Per 2005 ISDA Commodity Definitions, Sub-Annex A, Section 7.1 Commodity Reference Prices, as amended and supplemented through the date on which parties enter into the relevant transaction. */
    ALUMINIUM_ALLOY_LME_15_MONTH = "ALUMINIUM_ALLOY_LME_15_MONTH",
    /** A code for the NYMEX Central Appalachian Coal commodity */
    COAL_CENTRAL_APPALACHIAN_NYMEX = "COAL_CENTRAL_APPALACHIAN_NYMEX",
    /** A code for the ICE Futures U.S. (‘ICUS’) Cocoa commodity */
    COCOA_ICE = "COCOA_ICE",
    /** A code for the ICUS Coffee C commodity */
    COFFEE_ARABICA_ICE = "COFFEE_ARABICA_ICE",
    /** A code for the ICUS Coffee C commodity */
    COFFEE_ROBUSTA_ICE = "COFFEE_ROBUSTA_ICE",
    /** A code for the COMEX (‘CMX’) Copper Grade #1 commodity */
    COPPER_COMEX = "COPPER_COMEX",
    /** A code for the Chicago Board of Trade (‘CBOT’) Corn commodity */
    CORN_CBOT = "CORN_CBOT",
    /** A code for the ICUS Cotton No. 2 commodity */
    COTTON_NO__2_ICE = "COTTON_NO__2_ICE",
    /** A code for the CBOT Ethanol commodity */
    ETHANOL_CBOT = "ETHANOL_CBOT",
    /** A code for the CME Feeder Cattle commodity */
    FEEDER_CATTLE_CME = "FEEDER_CATTLE_CME",
    /** A code for the ICUS Frozen Concentrated Orange Juice commodity */
    FROZEN_CONCENTRATED_ORANGE_JUICE_NO__1_ICE = "FROZEN_CONCENTRATED_ORANGE_JUICE_NO__1_ICE",
    /** A code for the NYMEX Gasoline Blendstock (RBOB) commodity */
    GASOLINE_RBOB_NEW_YORK_ICE = "GASOLINE_RBOB_NEW_YORK_ICE",
    /** A code for the NYMEX Gasoline Blendstock (RBOB) commodity */
    GASOLINE_RBOB_NEW_YORK_NYMEX = "GASOLINE_RBOB_NEW_YORK_NYMEX",
    /** A code for the CMX Gold commodity */
    GOLD_COMEX = "GOLD_COMEX",
    /** A code for the NYMEX No. 2 Heating Oil, New York Harbor commodity */
    HEATING_OIL_NEW_YORK_NYMEX = "HEATING_OIL_NEW_YORK_NYMEX",
    /** A code for the CME Lean Hogs commodity */
    LEAN_HOGS_CME = "LEAN_HOGS_CME",
    /** A code for the CME Live Cattle commodity */
    LIVE_CATTLE_CME = "LIVE_CATTLE_CME",
    /** A code for the CME Random Length Lumber commodity */
    LUMBER_CME = "LUMBER_CME",
    /** A code for the CME Milk Class III commodity */
    MILK_CLASS_III_CME = "MILK_CLASS_III_CME",
    /** A code for the CME Non Fat Dry Milk commodity */
    MILK_NONFAT_DRY_CME = "MILK_NONFAT_DRY_CME",
    /** A code for the NYMEX Natural Gas commodity */
    NATURAL_GAS_NYMEX = "NATURAL_GAS_NYMEX",
    /** A code for the NYMEX Panhandle Basis Swap commodity */
    NATURAL_GAS_PEPL__TEXOK_MAINLINE__INSIDE_FERC = "NATURAL_GAS_PEPL__TEXOK_MAINLINE__INSIDE_FERC",
    /** A code for the NYMEX Waha Basis Swap commodity */
    NATURAL_GAS_W__TEXAS__WAHA__INSIDE_FERC = "NATURAL_GAS_W__TEXAS__WAHA__INSIDE_FERC",
    /** A code for the CBOT Oats commodity */
    OATS_CBOT = "OATS_CBOT",
    /** A code for the NYMEX Crude Oil, Light Sweet commodity */
    OIL_WTI_NYMEX = "OIL_WTI_NYMEX",
    /** A code for the NYMEX Palladium commodity */
    PALLADIUM_NYMEX = "PALLADIUM_NYMEX",
    /** A code for the NYMEX Platinum commodity */
    PLATINUM_NYMEX = "PLATINUM_NYMEX",
    /** A code for the CBOT Rough Rice commodity */
    RICE_CBOT = "RICE_CBOT",
    /** A code for the CMX Silver commodity */
    SILVER_COMEX = "SILVER_COMEX",
    /** A code for the CBOT Soybeans commodity */
    SOYBEANS_CBOT = "SOYBEANS_CBOT",
    /** A code for the CBOT Soybean Meal commodity */
    SOYBEAN_MEAL_CBOT = "SOYBEAN_MEAL_CBOT",
    /** A code for the CBOT Soybean Oil commodity */
    SOYBEAN_OIL_CBOT = "SOYBEAN_OIL_CBOT",
    /** A code for the ICUS Sugar No. 11 commodity */
    SUGAR___11__WORLD__ICE = "SUGAR___11__WORLD__ICE",
    /** A code for the ICUS Sugar No. 16 commodity */
    SUGAR___16__US__ICE = "SUGAR___16__US__ICE",
    /** A code for the CBOT Wheat commodity */
    WHEAT_CBOT = "WHEAT_CBOT",
    /** A code for the Kansas City Board of Trade (‘KCBT’)Wheat commodity */
    WHEAT_HRW_KCBOT = "WHEAT_HRW_KCBOT",
    /** A code for the Wheat commodity */
    WHEAT_RED_SPRING_MGE = "WHEAT_RED_SPRING_MGE",
};
/**
* The enumerated values to specify how an exchange rate is quoted.
*/
export enum QuoteBasisEnum {
    
    /** The amount of currency1 for one unit of currency2 */
    Currency1PerCurrency2 = "Currency1PerCurrency2",
    /** The amount of currency2 for one unit of currency1 */
    Currency2PerCurrency1 = "Currency2PerCurrency1",
};
/**
* The enumerated values to specify the type of quotation rate to be obtained from each cash settlement reference bank.
*/
export enum QuotationRateTypeEnum {
    
    /** A bid rate. */
    Bid = "Bid",
    /** An ask rate. */
    Ask = "Ask",
    /** A mid-market rate. */
    Mid = "Mid",
    /** If optional early termination is applicable to a swap transaction, the rate, which may be a bid or ask rate, which would result, if seller is in-the-money, in the higher absolute value of the cash settlement amount, or, is seller is out-of-the-money, in the lower absolute value of the cash settlement amount. */
    ExercisingPartyPays = "ExercisingPartyPays",
};
/**
* Represents the enumerated values to specify the rating agencies.
*/
export enum CreditRatingAgencyEnum {
    
    /** A. M. Best */
    AMBest = "AMBest",
    /** Canadian Bond Rating Service */
    CBRS = "CBRS",
    /** Dominion Bond Rating Service */
    DBRS = "DBRS",
    /** Fitch */
    Fitch = "Fitch",
    /** Japan Credit Rating Agency, Ltd. */
    Japanagency = "Japanagency",
    /** Moody's */
    Moodys = "Moodys",
    /** Rating And Investment Information, Inc. */
    RatingAndInvestmentInformation = "RatingAndInvestmentInformation",
    /** Standard And Poor's */
    StandardAndPoors = "StandardAndPoors",
};
/**
* Represents the enumerated values to specify the credit rating outlook.
*/
export enum CreditRatingOutlookEnum {
    
    /** Denotes a rating may be raised. */
    Positive = "Positive",
    /** Denotes a rating may be lowered. */
    Negative = "Negative",
    /** Denotes a rating is not likely to change. */
    Stable = "Stable",
    /** Denotes a rating may be raised, lowered, or affirmed. */
    Developing = "Developing",
};
/**
* Represents the enumerated values to specify the credit watch rating.
*/
export enum CreditRatingCreditWatchEnum {
    
    /** Denotes a rating may be raised. */
    Positive = "Positive",
    /** Denotes a rating may be lowered. */
    Negative = "Negative",
    /** Denotes a rating may be raised, lowered, or affirmed. */
    Developing = "Developing",
};
/**
* Represents an enumeration list to identify the characteristics of the rating if there are several agency issue ratings but not equivalent, reference will be made to label characteristics of the rating such as the lowest/highest available.
*/
export enum CreditNotationMismatchResolutionEnum {
    
    /** Denotes the lowest credit notation if several notations are listed. */
    Lowest = "Lowest",
    /** Denotes the highest credit notation if several notations are listed. */
    Highest = "Highest",
    /** Denotes that a credit notation issued from a defined reference agency is used if several notations are listed. */
    ReferenceAgency = "ReferenceAgency",
    /** Denotes the average credit notation if several notations are listed. */
    Average = "Average",
    /** Denotes the second best credit notation if several notations are listed. */
    SecondBest = "SecondBest",
    /** Utilised where bespoke language represents the label characteristics of the rating. */
    Other = "Other",
};
/**
* Identifies an agency rating as a simple scale boundary of minimum or maximum.
*/
export enum CreditNotationBoundaryEnum {
    
    /** Denotes a minumum boundary */
    Minimum = "Minimum",
    /** Denotes a maxiumum boundary */
    Maximum = "Maximum",
};
/**
* The enumerated values to specify the interpolation method, e.g. linear.
*/
export enum InterpolationMethodEnum {
    
    /** Linear Interpolation applicable. */
    Linear = "Linear",
    /** Linear Interpolation applicable. */
    LinearZeroYield = "LinearZeroYield",
    /** No Interpolation applicable. */
    None = "None",
};
/**
* The enumerated values to specify the settlement rate options as specified in the Annex A to the 1998 FX and Currency Options Definitions.
*/
export enum SettlementRateOptionEnum {
    
    /** The Spot Rate for a Rate Calculation Date will be the Argentine Peso/U.S. Dollar Specified Rate, expressed as the amount of Argentine Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Buenos Aires and New York) which appears on the Reuters Screen BNAR Page at the close of business in Buenos Aires on that Rate Calculation Date. */
    ARS_BNAR_ARS01 = "ARS_BNAR_ARS01",
    /** The Spot Rate for a Rate Calculation Date will be the Argentine Peso/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Argentine Pesos per one U.S. Dollar, for settlement on the same day, as published on EMTA's web site (www.emta.org) at approximately 1:00 p.m. (Buenos Aires time), or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA ARS Indicative Survey Methodology (which means a methodology, dated as of January 2, 2003, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Argentine Peso/U.S. Dollar markets for the purpose of determining the EMTA ARS Indicative Survey Rate). */
    ARS_EMTA_INDICATIVE_SURVEY_RATE_ARS04 = "ARS_EMTA_INDICATIVE_SURVEY_RATE_ARS04",
    /** The Spot Rate for a Rate Calculation Date will be the Argentine Peso/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Argentine Pesos per one U.S. Dollar, for settlement on the same day, as published on EMTA's web site (www.emta.org) at approximately 1:00 p.m. (Buenos Aires time), or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA ARS Industry Survey Methodology (which means a methodology, dated as of January 2, 2003, as amended from time to time, for a centralized industry-wide survey of financial institutions in Buenos Aires that are active participants in the Argentine Peso/U.S. Dollar spot markets for the purpose of determining the EMTA ARS Industry Survey Rate). */
    ARS_EMTA_INDUSTRY_SURVEY_RATE_ARS03 = "ARS_EMTA_INDUSTRY_SURVEY_RATE_ARS03",
    /** The Spot Rate for a Rate Calculation Date will be the volume weighted average Argentine Peso/U.S. Dollar Rate of all trades executed in the electronic market for a Rate Calculation Day expressed as the amount of Argentine Pesos per one U.S. Dollar, for settlement on the same day, reported by the Mercado Abierto Electronico (the 'MAE') at approximately 3:00 pm, Buenos Aires time, and published on the FOREX-MAE Page as the 'PPN' rate ('Promedio Ponderado Noticiado') on www.mae.com.ar on that Rate Calculation Date. */
    ARS_MAE_ARS05 = "ARS_MAE_ARS05",
    /** The Spot Rate for a Rate Calculation Date will be the Argentine Peso/U.S. Dollar offered rate for U.S. Dollars, expressed as the amount of Argentine Pesos per one U.S. Dollar, for settlement on the same day quoted by Banco de la Nacion (in accordance with the Convertibility Law of March 27, 1991 and Regulatory Decree No. 529/91 of April 1, 1991, as may be amended from time to time) for that Rate Calculation Date. */
    ARS_OFFICIAL_RATE_ARS02 = "ARS_OFFICIAL_RATE_ARS02",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar Specified Rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) which appears on the Reuters Screen BRBY Page under the caption 'INTBK FLTING (LAST)' at approximately 11:00 a.m., Sao Paulo time, on that Rate Calculation Date. */
    BRL_BRBY_BRL01 = "BRL_BRBY_BRL01",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days, as published on EMTA's web site (www.emta.org) at approximately 12:00 p.m. (Sao Paulo time), or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA BRL Indicative Survey Methodology (which means a methodology, dated as of March 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Brazilian Real/U.S. Dollar markets for the purpose of determining the EMTA BRL Indicative Survey Rate). */
    BRL_EMTA_INDICATIVE_SURVEY_RATE_BRL13 = "BRL_EMTA_INDICATIVE_SURVEY_RATE_BRL13",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days, as published on EMTA's web site (www.emta.org) at approximately 3:45 p.m. (Sao Paulo time), or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA BRL Industry Survey Methodology (which means a methodology, dated as of March 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions in Brazil that are active participants in the Brazilian Real/U.S. Dollar spot markets for the purpose of determining the EMTA BRL Industry Survey Rate). */
    BRL_EMTA_INDUSTRY_SURVEY_RATE_BRL12 = "BRL_EMTA_INDUSTRY_SURVEY_RATE_BRL12",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar Specified Rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil in the 'Diario Oficial da Uniao' on the first Business Day following that Rate Calculation Date. */
    BRL_OFFICIAL_RATE_BRL02 = "BRL_OFFICIAL_RATE_BRL02",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar commercial rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil on SISBACEN Data System under transaction code PCOT- 390, Option 3, at the Specified Time, if any, on that Rate Calculation Date. */
    BRL_PCOT_COMMERCIAL_BRL03 = "BRL_PCOT_COMMERCIAL_BRL03",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar floating rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil on SISBACEN Data System under transaction code PCOT- 390, Option 3, at the Specified Time, if any, on that Rate Calculation Date. */
    BRL_PCOT_FLOATING_BRL04 = "BRL_PCOT_FLOATING_BRL04",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar offered rate for U.S. Dollars, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days reported by the Banco Central do Brasil on SISBACEN Data System under transaction code PTAX-800 ('Consulta de Cambio' or Exchange Rate Inquiry), Option 5 ('Cotacoes para Contabilidade' or 'Rates for Accounting Purposes') by approximately 6:00 p.m., Sao Paulo time, on that Rate Calculation Date. */
    BRL_PTAX_BRL09 = "BRL_PTAX_BRL09",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar commercial rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil on SISBACEN Data System under transaction code PTAX- 800 ('Consultas de Cambio' or Exchange Rate Inquiry), Option 5 ('Cotacoes para Contabilidad' or Rates for Accounting Purposes) market type 'L' (corresponding to U.S. Dollars traded in the foreign exchange market segment officially denominated 'Livre' and commonly known as 'Comercial') as of 7:30 p.m., Sao Paulo time, on that Rate Calculation Date. */
    BRL_PTAX_COMMERCIAL_BRL05 = "BRL_PTAX_COMMERCIAL_BRL05",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar commercial rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil which appears on the Reuters Screen BRFR Page at PTAX-800 as of 11:00 a.m., Sao Paulo time, on the first Business Day following that Rate Calculation Date. 23 */
    BRL_PTAX_COMMERCIAL_BRFR_BRL06 = "BRL_PTAX_COMMERCIAL_BRFR_BRL06",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar floating rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil on SISBACEN Data System under transaction code PTAX- 800 ('Consultas de Cambio' or Exchange Rate Inquiry), Option 5 ('Cotacoes para Contabilidad' or Rates for Accounting Purposes) market type 'F' (corresponding to U.S. Dollars traded in the foreign exchange market segment officially denominated 'Flutuante') as of 7:30 p.m., Sao Paulo time, on that Rate Calculation Date. */
    BRL_PTAX_FLOATING_BRL07 = "BRL_PTAX_FLOATING_BRL07",
    /** The Spot Rate for a Rate Calculation Date will be the Brazilian Real/U.S. Dollar floating rate, expressed as the amount of Brazilian Reais per one U.S. Dollar, for settlement in two Business Days (where such days are Business Days in both Sao Paulo and New York) reported by the Banco Central do Brasil on the SISBACEN Data System which appears on the Reuters Screen BRFR Page at PTAX-800 as of 11:00 a.m., Sao Paulo time, on the first Business Day following that Rate Calculation Date. */
    BRL_PTAX_FLOATING_BRFR_BRL08 = "BRL_PTAX_FLOATING_BRFR_BRL08",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar observado rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) reported by the Banco Central de Chile which appears on the Reuters Screen BCCH Page under the caption 'OBSERVADO' at 10:00 a.m., Santiago time, on the first Business Day following that Rate Calculation Date. */
    CLP_BCCH_CLP01 = "CLP_BCCH_CLP01",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar informal rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) of the informal exchange market which appears on the Reuters Screen CHILD Page at the Specified Time, if any, on that Rate Calculation Date. */
    CLP_CHILD_INFORMAL_CLP02 = "CLP_CHILD_INFORMAL_CLP02",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar interbank rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) reported by the Banco Central de Chile for the formal exchange market which appears on the Reuters Screen CHILD Page at the Specified Time, if any, on that Rate Calculation Date. */
    CLP_CHILD_INTERBANK_CLP03 = "CLP_CHILD_INTERBANK_CLP03",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar observado rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) reported by the Banco Central de Chile which appears on the Reuters Screen CHILD Page on the first Business Day following that Rate Calculation Date. */
    CLP_CHILD_OBSERVADO_CLP04 = "CLP_CHILD_OBSERVADO_CLP04",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar informal rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) of the informal exchange market which appears on the Reuters Screen CHILG Page at the Specified Time, if any, on that Rate Calculation Date. */
    CLP_CHILG_INFORMAL_CLP05 = "CLP_CHILG_INFORMAL_CLP05",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar interbank rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) reported by the Banco Central de Chile for the formal exchange market which appears on the Reuters Screen CHILG Page at the Specified Time, if any, on that Rate Calculation Date. */
    CLP_CHILG_INTERBANK_CLP06 = "CLP_CHILG_INTERBANK_CLP06",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar observado rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) reported by the Banco Central de Chile which appears on the Reuters Screen CHILG Page under 'OBSERVADO' at the Specified Time, if any, on the first Business Day following that Rate Calculation Date. */
    CLP_CHILG_OBSERVADO_CLP07 = "CLP_CHILG_OBSERVADO_CLP07",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar 'observado' rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement in one Business Day reported by the Banco Central de Chile (www.bcentral.cl) as the 'Dolar Observado' (Dollar Observado) rate by not later than 10:30 a.m., Santiago time, on the first Business Day following that Rate Calculation Date. */
    CLP_DOLAR_OBS_CLP10 = "CLP_DOLAR_OBS_CLP10",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day, as published on EMTA's web site (www.emta.org) at approximately 11:00 a.m., Santiago time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA CLP Indicative Survey Methodology (which means a methodology, dated as of August 1, 2006, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Chilean Peso/U.S. Dollar markets for the purpose of determining the EMTA CLP Indicative Survey Rate). */
    CLP_EMTA_INDICATIVE_SURVEY_RATE_CLP11 = "CLP_EMTA_INDICATIVE_SURVEY_RATE_CLP11",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar Specified Rate, expressed as the amount of Chilean Pesos per one U.S. Dollar (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York), calculated in accordance with Title I, Chapter 1 Number 6 of the Compendium of International Exchange Norms of the Banco Central de Chile and published by the Banco Central de Chile at the Specified Time, if any, on the first Business Day following that Rate Calculation Date. */
    CLP_OFFICIAL_RATE_CLP08 = "CLP_OFFICIAL_RATE_CLP08",
    /** The Spot Rate for a Rate Calculation Date will be the Chilean Peso/U.S. Dollar observado rate, expressed as the amount of Chilean Pesos per one U.S. Dollar, for settlement on the same day (or, if such day is not a Business Day in New York, for settlement on the first succeeding day that is a Business Day in both Santiago and New York) reported by the Banco Central de Chile which appears on the Telerate Page 38942 opposite the caption 'Observado' at the Specified Time, if any, on the first Business Day following the Rate Calculation Date. */
    CLP_TELERATE_38942_CLP09 = "CLP_TELERATE_38942_CLP09",
    /** The Spot Rate for a Rate Calculation Date will be the Chinese Renminbi/U.S. Dollar official fixing rate, expressed as the amount of Chinese Renminbi per one U.S. Dollar, for settlement in two Business Days reported by the People's Bank of China, Beijing, People's Republic of China, which appears on the Reuters Screen 'SAEC' Page opposite the symbol 'USDCNY=' at approximately 9:15 a.m., Beijing time, on that Rate Calculation Date. */
    CNY_SAEC_CNY01 = "CNY_SAEC_CNY01",
    /** The Spot Rate for a Rate Calculation Date will be the Chinese Renminbi/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Chinese Renminbi per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m. (Singapore time), or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC CNY Indicative Survey Methodology (which means a methodology, dated as of December 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Chinese Renminbi/U.S. Dollar markets for the purpose of determining the SFEMC CNY Indicative Survey Rate). */
    CNY_SFEMC_INDICATIVE_SURVEY_RATE_CNY02 = "CNY_SFEMC_INDICATIVE_SURVEY_RATE_CNY02",
    /** The Spot Rate for a Rate Calculation Date will be the Colombian Peso/U.S. Dollar fixing rate, expressed as the amount of Colombian Pesos per one U.S. Dollar, for settlement on the same day (unless such day is not a Business Day in New York, then for settlement on the first succeeding day that is a Business Day in Bogota and New York) reported by the Colombian Banking Superintendency which appears on the Reuters Screen CO/COL03 Page opposite the caption 'TRCM' ('Tasa de Cierre Representative del Mercado' or closing market price) at 12:00 noon, Bogota time, on the first Business Day following that Rate Calculation Date. */
    COP_CO_COL03_COP01 = "COP_CO_COL03_COP01",
    /** The Spot Rate for a Rate Calculation Date will be the Colombian Peso/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Colombian Pesos per one U.S. Dollar, for settlement on the same day, as published on EMTA's web site (www.emta.org) at approximately 11:30 a.m., Bogota time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA COP Indicative Survey Methodology (which means a methodology, dated as of August 1, 2006, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Colombian Peso/U.S. Dollar markets for the purpose of determining the EMTA COP Indicative Survey Rate). */
    COP_EMTA_INDICATIVE_SURVEY_RATE_COP03 = "COP_EMTA_INDICATIVE_SURVEY_RATE_COP03",
    /** The Spot Rate for a Rate Calculation Date will be the Colombian Peso/U.S. Dollar fixing rate, expressed as the amount of Colombian Pesos per one U.S. Dollar, for settlement on the same day reported by the Colombian Financial Superintendency (www.banrep.gov.co) as the 'Tasa Representativa del Mercado (TRM)' (also referred to as the 'Tasa de Cambio Representativa del Mercado' (TCRM)) by not later than 10:30 a.m., Bogota time, on the first Business Day following that Rate Calculation Date. */
    COP_TRM_COP02 = "COP_TRM_COP02",
    /** the Spot Rate for a Rate Calculation Date will be the Reference Currency/U.S. Dollar exchange rate, expressed as the amount of Reference Currency per one U.S. Dollar, determined on the basis of quotations provided by Reference Dealers on that Rate Calculation Date of that day's price of a Specified Company's American Depositary Receipt or American Depositary Receipts (the 'ADR' or 'ADRs', as appropriate) and the price of the local share or shares of such Specified Company of the same type and in the same quantity represented by such ADR or ADRs, as the case may be (the 'Share' or 'Shares', as appropriate). The Calculation Agent will request each of the Reference Dealers to provide a firm quotation of (A) in the case where one ADR represents less than one Share, its bid and offer price (in the Reference Currency) for one Share and its bid and offer price (in U.S. Dollars) for the number of ADRs which represent such Share and (B) in all other cases, its bid and offer price (in the Reference Currency) for the Share or Shares, as the case may be, and its bid and offer price (in U.S. Dollars) for one ADR. If one or more quotations are provided, the rate for a Rate Calculation Date will equal the ratio of (1) the arithmetic mean of the midpoint of the bid and offer prices quoted in the Reference Currency by each Reference Dealer for such Share or Shares, as the case may be, and (2) the arithmetic mean of the midpoint of the bid and offer prices quoted in U.S. Dollars by each Reference Dealer for such ADR or ADRs, as the case may be, subject to an adjustment, if any, by the Calculation Agent to reduce the effect of momentary disparities in the prices of the Share or Shares and the ADR or ADRs, as appropriate. The quotations used to determine the Spot Rate for a Rate Calculation Date will be determined in each case at the Specified Time on the Rate Calculation Date or, if no such time is specified, the time chosen by the Calculation Agent. */
    CURRENCY_IMPLIED_RATE__ADR__CURA1 = "CURRENCY_IMPLIED_RATE__ADR__CURA1",
    /** The Spot Rate for a Rate Calculation Date will be the Reference Currency/Settlement Currency exchange rate, expressed as the amount of Reference Currency per one unit of Settlement Currency, determined on the basis of quotations provided by Reference Dealers on that Rate Calculation Date for that day's price of Local Assets. The Calculation Agent will request each of the Reference Dealers to provide a firm quotation of its bid and offer price (in both the Reference Currency and the Settlement Currency) for an amount of Local Assets whose face value equals the Specified Amount. If one or more quotations are provided, the rate for a Rate Calculation Date will equal the ratio of (A) the arithmetic mean of the midpoint of the bid and offer prices quoted in the Reference Currency by each Reference Dealer for such Local Assets and (B) the arithmetic mean of the midpoint of the bid and offer prices quoted in the Settlement Currency by each Reference Dealer for such Local Assets. The quotations used to determine the Spot Rate for a Rate Calculation Date will be determined in each case at the Specified Time on the Rate Calculation Date or, if no such time is specified, the time chosen by the Calculation Agent. */
    CURRENCY_IMPLIED_RATE__LOCAL_ASSET__CURA2 = "CURRENCY_IMPLIED_RATE__LOCAL_ASSET__CURA2",
    /** The Spot Rate for a Rate Calculation Date will be the Reference Currency/Settlement Currency Specified Rate, expressed as the amount of the Reference Currency per one unit of Settlement Currency, for settlement on the Settlement Date agreed upon by the parties on or prior to that Rate Calculation Date (or, if different, the day on which rates for that date would, in the ordinary course, be published or announced). */
    CURRENCY_MUTUAL_AGREEMENT_CURA3 = "CURRENCY_MUTUAL_AGREEMENT_CURA3",
    /** The Spot Rate for a Rate Calculation Date will be determined on the basis of quotations provided by Reference Dealers on that Rate Calculation Date of that day's Specified Rate, expressed as the amount of Reference Currency per one unit of Settlement Currency, for settlement on the Settlement Date. The Calculation Agent will request the Specified Office of each of the Reference Dealers to provide a firm quotation of its Specified Rate for a transaction where the amount of Reference Currency equals the Specified Amount. If four quotations are provided, the rate for a Rate Calculation Date will be the arithmetic mean of the Specified Rates, without regard to the Specified Rates having the highest and lowest value. If exactly three quotations are provided, the rate for a Rate Calculation Date will be the Specified Rate provided by the Reference Dealer that remains after disregarding the Specified Rates having the highest and lowest values. For this purpose, if more than one quotation has the same highest value or lowest value, then the Specified Rate of one of such quotations shall be disregarded. If exactly two quotations are provided, the rate for a Rate Calculation Date will be the arithmetic mean of the Specified Rates. If only one quotation is provided, the rate for a Rate Calculation Date will be the Specified Rate quoted by that Reference Dealer. The quotations used to determine the Spot Rate for a Rate Calculation Date will be determined in each case at the Specified Time on that Rate Calculation Date or, if no such time is specified, the time chosen by the Calculation Agent. */
    CURRENCY_REFERENCE_DEALERS_CURA4 = "CURRENCY_REFERENCE_DEALERS_CURA4",
    /** The Spot Rate for a Rate Calculation Date will be determined by the Calculation Agent on the basis of that day's Specified Rate, expressed as the amount of Reference Currency per one unit of Settlement Currency, in a legal and customary wholesale market in which there is no, or minimal, Governmental Authority controls or interference, except as a participant in such market. */
    CURRENCY_WHOLESALE_MARKET_CURA5 = "CURRENCY_WHOLESALE_MARKET_CURA5",
    /** The Spot Rate for a Rate Calculation Date will be the Ecuadorian Sucre/U.S. Dollar Specified Rate, expressed as the amount of Ecuadorian Sucres per one U.S. Dollar, for settlement in one Business Day (where such day is a Business Day in Guayaquil and New York) which appears on Reuters Screen DNRP Page at 12:00 noon, Guayaquil time, on that Rate Calculation Date. */
    ECS_DNRP_ECS01 = "ECS_DNRP_ECS01",
    /** The Spot Rate for a Rate Calculation Date will be the Indonesian Rupiah/U.S. Dollar spot rate at 11:00 a.m., Singapore time, expressed as the amount of Indonesian Rupiah per one U.S. Dollar, for settlement in two Business Days, reported by the Association of Banks in Singapore which appears on the Telerate Page 50157 to the right of the caption 'Spot' under the column 'IDR' at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    IDR_ABS_IDR01 = "IDR_ABS_IDR01",
    /** The Spot Rate for a Rate Calculation Date will be the Indonesian Rupiah/U.S. Dollar weighted average spot rate in the interbank market based on traded IDR/USD spot foreign exchange transactions during a specified time period which are captured on a real time basis, expressed as the amount of Indonesian Rupiah per one U.S. Dollar, for settlement in two Business Days, published by Bank Indonesia at approximately 10:00 a.m., Jakarta time, on that Rate Calculation Date as the Jakarta Interbank Spot Dollar Rate USD - IDR on Bank Indonesia's website or otherwise made available by Bank Indonesia (or its successor as administrator). */
    IDR_JISDOR_IDR04 = "IDR_JISDOR_IDR04",
    /** The Spot Rate for a Rate Calculation Date will be the Indonesian Rupiah/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Indonesian Rupiah per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m., Singapore time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC IDR Indicative Survey Methodology (which means a methodology, dated as of December 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Indonesian Rupiah/U.S. Dollar markets for the purpose of determining the SFEMC IDR Indicative Survey Rate). */
    IDR_SFEMC_INDICATIVE_SURVEY_RATE_IDR02 = "IDR_SFEMC_INDICATIVE_SURVEY_RATE_IDR02",
    /** The Spot Rate for a Rate Calculation Date will be the Indonesian Rupiah/U.S. Dollar implied spot rate expressed as the amount of Indonesian Rupiah per one U.S. Dollar, for settlement in two Business Days, reported by ABS Benchmarks Administration Co Pte. Ltd. (or its successor as administrator or sponsor of that rate), which appears on Thomson Reuters Screen ABSFIX01 Page at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    IDR_VWAP_IDR03 = "IDR_VWAP_IDR03",
    /** The Spot Rate for a Rate Calculation Date will be the Israeli Shekel/U.S. Dollar Specified Rate, expressed as the amount of Israeli Shekels per one U.S. Dollar, for settlement in two Business Days which appears on the Reuters Screen BOIJ Page as of 1:00 p.m., Tel Aviv time, on that Rate Calculation Date. */
    ILS_BOIJ_ILS01 = "ILS_BOIJ_ILS01",
    /** The Spot Rate for a Rate Calculation Date will be the Israeli Shekel/U.S. Dollar Specified Rate, expressed as the amount of Israeli Shekels per one U.S. Dollar, for settlement in two Business Days which appears on the Reuters Screen FXIL Page as of 1:00 p.m., Tel Aviv time, on that Rate Calculation Date. */
    ILS_FXIL_ILS02 = "ILS_FXIL_ILS02",
    /** The Spot Rate for a Rate Calculation Date will be the Indian Rupee/U.S. Dollar reference rate, expressed as the amount of Indian Rupee per one U.S. Dollar, for settlement in two Business Days, reported by Financial Benchmarks India Pvt. Ltd. (www.fbil.org.in) at approximately 1:30 p.m., Mumbai time, or as soon thereafter as practicable, on that Rate Calculation Date. */
    INR_FBIL_INR01 = "INR_FBIL_INR01",
    /** The Spot Rate for a Rate Calculation Date will be the Indian Rupee/U.S. Dollar reference rate, expressed as the amount of Indian Rupee per one U.S. Dollar, for settlement in two Business Days reported by the Reserve Bank of India which appears on the Reuters Screen RBIB Page at approximately 12:30 p.m., Mumbai time, or as soon thereafter as practicable, on that Rate Calculation Date. */
    INR_RBIB_INR01 = "INR_RBIB_INR01",
    /** The Spot Rate for a Rate Calculation Date will be the Indian Rupee/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Indian Rupee per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m. (Singapore time), or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC INR Indicative Survey Methodology (which means a methodology, dated as of December 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Indian Rupee/U.S. Dollar markets for the purpose of determining the SFEMC INR Indicative Survey Rate). */
    INR_SFEMC_INDICATIVE_SURVEY_RATE_INR02 = "INR_SFEMC_INDICATIVE_SURVEY_RATE_INR02",
    /** The Spot Rate for a Rate Calculation Date will be the Korean Won/U.S. Dollar Specified Rate, expressed as the amount of Korean Won per one U.S. Dollar, for settlement in two Business Days which appears on the Reuters Screen KEBEY Page at the Specified Time, if any, on that Rate Calculation Date. */
    KRW_KEBEY_KRW01 = "KRW_KEBEY_KRW01",
    /** The Spot Rate for a Rate Calculation Date will be the Korean Won/U.S. Dollar market average rate, expressed as the amount of Korean Won per one U.S. Dollar, for settlement in two Business Days reported by the Korea Financial Telecommunications and Clearing Corporation which appears on the Reuters Screen KFTC18 Page to the right of the caption 'USD Today' that is available at approximately 3:30 p.m., Seoul time, on the Rate Calculation Date or as soon thereafter as practicable. */
    KRW_KFTC18_KRW02 = "KRW_KFTC18_KRW02",
    /** The Spot Rate for a Rate Calculation Date will be the Korean Won/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Korean Won per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m., Singapore time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC KRW Indicative Survey Methodology (which means a methodology, dated as of December 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Korean Won/U.S. Dollar markets for the purpose of determining the SFEMC KRW Indicative Survey Rate). */
    KRW_SFEMC_INDICATIVE_SURVEY_RATE_KRW04 = "KRW_SFEMC_INDICATIVE_SURVEY_RATE_KRW04",
    /** The Spot Rate for a Rate Calculation Date will be the Korean Won/U.S. Dollar market average rate, expressed as the amount of Korean Won per one U.S. Dollar, for settlement in two Business Days reported by the Korea Financial Telecommunications and Clearing Corporation which appears on Telerate Page 45644 to the right of the caption 'USD Today' that is available at approximately 3:30 p.m., Seoul time, on the Rate Calculation Date or as soon thereafter as practicable. */
    KRW_TELERATE_45644_KRW03 = "KRW_TELERATE_45644_KRW03",
    /** The Spot Rate for a Rate Calculation Date will be the Kazakhstan Tenge / U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Kazakhstan Tenge per one U.S. Dollar, for settlement on the same Business Day, as published on EMTA's website (www.emta.org) at approximately 1:00 p.m., Almaty time, or as soon thereafter as practicable, on that Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA KZT Indicative Survey Methodology (which means a methodology, dated as of March 16, 2009, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Kazakhstan Tenge/U.S. Dollar markets for the purpose of determining the EMTA KZT Indicative Survey Rate). */
    KZT_EMTA_INDICATIVE_SURVEY_RATE_KZT02 = "KZT_EMTA_INDICATIVE_SURVEY_RATE_KZT02",
    /** The Spot Rate for a Rate Calculation Date will be the Kazakhstan Tenge / U.S. Dollar weighted average rate, expressed as the amount of Kazakhstan Tenge per one U.S. Dollar, for settlement on the same Business Day reported by the Kazakhstan Stock Exchange (www.kase.kz) at approximately 11:00 am, Almaty time, on that Rate Calculation Date. */
    KZT_KASE_KZT01 = "KZT_KASE_KZT01",
    /** The Spot Rate for a Rate Calculation Date will be the Lebanese Pound/U.S. Dollar Specified Rate, expressed as the amount of Lebanese Pounds per one U.S. Dollar, for settlement in two Business Days which appears on the Reuters Screen BDLX Page as of 12:00 noon, Beirut time, on that Rate Calculation Date. */
    LBP_BDLX_LBP01 = "LBP_BDLX_LBP01",
    /** The Spot Rate for a Rate Calculation Date will be the Moroccan Dirham/U.S. Dollar Specified Rate, expressed as the amount of Moroccan Dirham per one U.S. Dollar, for settlement in two Business Days reported by the Central Bank of Morocco as of 1:00 p.m., Rabat time, on that Rate Calculation Date. */
    MAD_OFFICIAL_RATE_MAD01 = "MAD_OFFICIAL_RATE_MAD01",
    /** The Spot Rate for a Rate Calculation Date will be the Mexican Pesos/U.S. Dollar Specified rate, expressed as the amount of Mexican Pesos per one U.S. Dollar, for settlement in two Business Days reported by Banco de Mexico which appears on the Reuters Screen BNMX Page opposite the caption 'Fix' at the close of business in Mexico City on that Rate Calculation Date. */
    MXP_BNMX_MXP01 = "MXP_BNMX_MXP01",
    /** The Spot Rate for a Rate Calculation Date will be the Mexican Peso/U.S. Dollar fixing rate, expressed as the amount of Mexican Pesos per one U.S. Dollar, for settlement in two Business Days which is published by Banco de Mexico in the Official Gazette of the Federation pursuant to the 'Disposiciones aplicables a la determinacion del tipo de Cambio para solventar obligaciones denominadas en moneda extranjera pagaderas en la Republica Mexicana' (Rules applicable to determine the exchange rate to pay obligations denominated in foreign currency payable in Mexico) on the first Business Day following that Rate Calculation Date. */
    MXP_FIXING_RATE_MXP02 = "MXP_FIXING_RATE_MXP02",
    /** The Spot Rate for a Rate Calculation Date will be the Mexican Peso/U.S. Dollar fixing rate, expressed as the amount of Mexican Pesos per one U.S. Dollar, for settlement in two Business Days reported by Banco de Mexico which appears on Reuters Screen MEX01 Page under the heading 'MXNFIX=RR', at the close of business in Mexico City on that Rate Calculation Date. */
    MXP_MEX01_MXP03 = "MXP_MEX01_MXP03",
    /** The Spot Rate for a Rate Calculation Date will be the Mexican Peso/U.S. Dollar fixing rate, expressed as the amount of Mexican Pesos per one U.S. Dollar, for settlement in two Business Days which is published by the Bolsa Mexicana de Valores, S.A. de C.V. (as established in Section 2 of the 'Resolution concerning the exchange rate applicable for calculating the Mexican Peso equivalent of principal and interest of Mexican Treasury Notes denominated in foreign currency and payable in Mexican Pesos' published in the Diario Oficial de la Federacion on November 11, 1991) in the Movimiento Diario del Mercado de Valores de la Bolsa Mexicana de Valores, S.A. de C.V. under the heading 'Movimiento Diario del Mercado de Valores' on that Rate Calculation Date. */
    MXP_PUBLISHED_MXP04 = "MXP_PUBLISHED_MXP04",
    /** The Spot Rate for a Rate Calculation Date will be the Malaysian Ringgit/U.S. Dollar spot rate at 11:00 a.m., Singapore time, expressed as the amount of Malaysian Ringgit per one U.S. Dollar, for settlement in two Business Days, reported by the Association of Banks in Singapore, which appears on the Telerate Page 50157 to the right of the caption 'Spot' under the column 'MYR' at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    MYR_ABS_MYR01 = "MYR_ABS_MYR01",
    /** The Spot Rate for a Rate Calculation Date will be the Malaysian Ringgit/U.S. Dollar reference rate, expressed as the amount of Malaysian Ringgit per one U.S. Dollar, for settlement in two Business Days, calculated and reported by Bank Negara Malaysia as its Kuala Lumpur USD/MYR Reference Rate, which appears on Thomson Reuters Screen MYRFIX2 Page at approximately 3:30 p.m., Kuala Lumpur time, on that Rate Calculation Date. */
    MYR_KL_REF_MYR04 = "MYR_KL_REF_MYR04",
    /** The Spot Rate for a Rate Calculation Date will be the Malaysian Ringgit/U.S. Dollar spot rate expressed as the amount of Malaysian Ringgit per one U.S. Dollar, for settlement in two Business Days, reported by Persatuan Pasaran Kewangan Malaysia (ACI - Malaysia), which appears on Thomson Reuters Screen MYRFIX2 Page at approximately 11:10 a.m., Kuala Lumpur time, on that Rate Calculation Date. */
    MYR_PPKM_MYR03 = "MYR_PPKM_MYR03",
    /** The Spot Rate for a Rate Calculation Date will be the Malaysian Ringgit/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Malaysian Ringgit per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m., Singapore time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC MYR Indicative Survey Methodology (which means a methodology, dated as of July 15, 2005, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Malaysian Ringgit/U.S. Dollar markets for the purpose of determining the SFEMC MYR Indicative Survey Rate). */
    MYR_SFEMC_INDICATIVE_SURVEY_RATE_MYR02 = "MYR_SFEMC_INDICATIVE_SURVEY_RATE_MYR02",
    /** The Spot Rate for a Rate Calculation Date will be the Peruvian Sol/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Peruvian Soles per one U.S. Dollar, for settlement on the same day, as published on EMTA's web site (www.emta.org) at approximately 11:00 a.m., Lima time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA PEN Indicative Survey Methodology (which means a methodology, dated as of August 1, 2006, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Peruvian Sol/U.S. Dollar markets for the purpose of determining the EMTA PEN Indicative Survey Rate). */
    PEN_EMTA_INDICATIVE_SURVEY_RATE_PEN04 = "PEN_EMTA_INDICATIVE_SURVEY_RATE_PEN04",
    /** The Spot Rate for a Rate Calculation Date will be the Peruvian Sol/U.S. Dollar average exchange rate in the interbank market expressed as the amount of Peruvian New Soles per one U.S. Dollar for settlement on the same day reported by the Banco Central de Reserva del Peru (www.bcrp.gob.pe) as the 'Tipo de Cambio Interbancario Promedio' at approximately 2:00 p.m., Lima time, on that Rate Calculation Date. */
    PEN_INTERBANK_AVE_PEN05 = "PEN_INTERBANK_AVE_PEN05",
    /** The Spot Rate for a Rate Calculation Date will be the Peruvian Sol/U.S. Dollar fixing rate (mid market last), expressed as the amount of Peruvian Sols per one U.S. Dollar, for settlement on that same day which appears on the Reuters Screen PDSB Page opposite the caption 'PEN=' as of 12:00 noon, Lima time, on that Rate Calculation Date. */
    PEN_PDSB_PEN01 = "PEN_PDSB_PEN01",
    /** The Spot Rate for a Rate Calculation Date will be the midpoint of the Peruvian Sol/U.S. Dollar closing weighted average bid and offer ('compra y venta') exchange rates expressed as the amount of Peruvian New Soles per one U.S. Dollar for settlement on the same day, reported by the Superintendencia de Banca, Seguros y AFP (www.sbs.gob.pe) of the Republic of Peru at approximately 5:00 p.m., Lima time, on that Rate Calculation Date. */
    PEN_WT_AVE_PEN03 = "PEN_WT_AVE_PEN03",
    /** The Spot Rate for a Rate Calculation Date will be the Philippine Peso/U.S. Dollar morning weighted average rate for that Rate Calculation Date, expressed as the amount of Philippine Pesos per one U.S. Dollar, for settlement in one Business Day, sponsored by Bankers Association of the Philippines (www.bap.org.ph) as its 'BAP AM Weighted Average Rate' at approximately 11:30 a.m., Manila time, or as soon thereafter as practicable, on that Rate Calculation Date. */
    PHP_BAPPESO_PHP06 = "PHP_BAPPESO_PHP06",
    /** The Spot Rate for a Rate Calculation Date will be the Philippine Peso/U.S. Dollar morning weighted average rate for that Rate Calculation Date, expressed as the amount of Philippine Pesos per one U.S. Dollar, for settlement in one Business Day reported by the Philippine Dealing System PDEX which appears on the Reuters Screen PDSPESO Page to the right of the caption 'AM WT AVE' at approximately 11:30 a.m., Manila time, or as soon thereafter as practicable, on that Rate Calculation Date. */
    PHP_PDSPESO_PHP06 = "PHP_PDSPESO_PHP06",
    /** The Spot Rate for a Rate Calculation Date will be the Philippine Peso/U.S. Dollar tom rate (mid market), expressed as the amount of Philippine Pesos per one U.S. Dollar, for settlement in one Business Day which appears on the Reuters Screen PHPESO Page at approximately 11:00 a.m., Manila time, on that Rate Calculation Date. */
    PHP_PHPESO_PHP01 = "PHP_PHPESO_PHP01",
    /** The Spot Rate for a Rate Calculation Date will be the Philippine Peso/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Philippine Pesos per one U.S. Dollar, for settlement in one Business Day, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m., Singapore time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC PHP Indicative Survey Methodology (which means a methodology, dated as of December 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Philippine Peso/U.S. Dollar markets for the purpose of determining the SFEMC PHP Indicative Survey Rate). */
    PHP_SFEMC_INDICATIVE_SURVEY_RATE_PHP05 = "PHP_SFEMC_INDICATIVE_SURVEY_RATE_PHP05",
    /** The Spot Rate for a Rate Calculation Date will be the Philippine Peso/U.S. Dollar tom rate (mid market), expressed as the amount of Philippine Pesos per one U.S. Dollar, for settlement in one Business Day which appears on the Telerate Page 15439 at approximately 11:00 a.m., Manila time, on that Rate Calculation Date. */
    PHP_TELERATE_15439_PHP03 = "PHP_TELERATE_15439_PHP03",
    /** The Spot Rate for a Rate Calculation Date will be the Philippine Peso/U.S. Dollar Specified Rate, expressed as the amount of Philippine Pesos per one U.S. Dollar, for settlement in one Business Day which appears on the Telerate Page 2920 at the Specified Time, if any, on that Rate Calculation Date. */
    PHP_TELERATE_2920_PHP02 = "PHP_TELERATE_2920_PHP02",
    /** The Spot Rate for a Rate Calculation Date will be the Pakistani Rupee/U.S. Dollar reference rate expressed as the amount of Pakistani Rupees per one U.S. Dollar, for settlement in two Business Days reported by the State Bank of Pakistan (www.sbp.org.pk) at approximately 2:30 pm, Karachi time, on that Rate Calculation Date. */
    PKR_SBPK_PKR01 = "PKR_SBPK_PKR01",
    /** The Spot Rate for a Rate Calculation Date will be the Pakistani Rupee/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Pakistani Rupees per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m. Singapore time, or as soon thereafter as practicable, on that Rate Calculation Date. The Spot Rate shall be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC PKR Indicative Survey Methodology (which means a methodology, dated as of July 14, 2008, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Pakistani Rupee/U.S. Dollar markets for the purpose of determining the SFEMC PKR Indicative Survey Rate). */
    PKR_SFEMC_INDICATIVE_SURVEY_RATE_PKR02 = "PKR_SFEMC_INDICATIVE_SURVEY_RATE_PKR02",
    /** The Spot Rate for a Rate Calculation Date will be the Polish Zloty/U.S. Dollar Specified Rate, expressed as the amount of Polish Zloty per one U.S. Dollar, for settlement in two Business Days reported by the National Bank of Poland which appears on the Reuters Screen NBPQ Page at the Specified Time, if any, on that Rate Calculation Date. */
    PLZ_NBPQ_PLZ01 = "PLZ_NBPQ_PLZ01",
    /** The Spot Rate for a Rate Calculation Date will be the Polish Zloty/U.S. Dollar fixing rate, expressed as the amount of Polish Zloty per one U.S. Dollar, for settlement in two Business Days reported by the National Bank of Poland which appears on the Reuters Screen NBPR Page at the Specified Time, if any, on that Rate Calculation Date. */
    PLZ_NBPR_PLZ02 = "PLZ_NBPR_PLZ02",
    /** The Spot Rate for a Rate Calculation Date will be the Russian Ruble/U.S. Dollar Specified Rate, expressed as the amount of Russian Rubles per one U.S. Dollar, for settlement in one Business Day, calculated by the Chicago Mercantile Exchange ('CME') and as published on CME's website, which appears on the Reuters Screen EMTA Page, at approximately 1:30 p.m., Moscow time, on that Rate Calculation Date. The Spot Rate shall be calculated by the CME pursuant to the Chicago Mercantile Exchange / EMTA, Inc. Daily Russian Ruble Per U.S. Dollar Reference Rate Methodology (which means a methodology, effective as of June 16, 2005, as amended from time to time, for a centralized industry-wide survey of financial institutions in Russia that are active participants in the Russian Ruble/U.S. Dollar spot market for the purpose of determining the RUB CME-EMTA Rate). */
    RUB_CME_EMTA_RUB03 = "RUB_CME_EMTA_RUB03",
    /** The Spot Rate for a Rate Calculation Date will be the Russian Ruble/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Russian Rubles per one U.S. Dollar, for settlement in one Business Day, as published on EMTA's web site (www.emta.org) at approximately 2:45 p.m., Moscow time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA RUB Indicative Survey Methodology (which means a methodology dated as of June 16, 2005, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Russian Ruble/U.S. Dollar spot market for the purpose of determining the EMTA RUB Indicative Survey Rate). */
    RUB_EMTA_INDICATIVE_SURVEY_RATE_RUB04 = "RUB_EMTA_INDICATIVE_SURVEY_RATE_RUB04",
    /** The Spot Rate for a Rate Calculation Date will be the Russian Ruble/U.S. Dollar Specified Rate, expressed as the amount of Russian Rubies per one U.S. Dollar, for settlement on the same day reported by the Moscow Interbank Currency Exchange which appears on the Reuters Screen MICEXFRX Page as of 10:30 a.m., Moscow time, on that Rate Calculation Date. */
    RUB_MICEXFRX_RUB01 = "RUB_MICEXFRX_RUB01",
    /** The Spot Rate for a Rate Calculation Date will be the Russian Ruble/U.S. Dollar Specified Rate, expressed as the amount of Russian Rubies per one U.S. Dollar, for settlement on the same day reported by the Moscow Interbank Currency Exchange which appears on the Reuters Screen MMVB Page as of 10:30 a.m., Moscow time, on that Rate Calculation Date. */
    RUB_MMVB_RUB02 = "RUB_MMVB_RUB02",
    /** The Spot Rate for a Rate Calculation Date will be the Singapore Dollar/U.S. Dollar spot rate expressed as the amount of Singapore Dollar per one U.S. Dollar for settlement in two Business Days, reported by ABS Benchmarks Administration Co Pte. Ltd. (or its successor as administrator or sponsor of the rate), which appears on Thomson Reuters Screen ABSFIX01 Page at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    SGD_VWAP_SGD3 = "SGD_VWAP_SGD3",
    /** The Spot Rate for a Rate Calculation Date will be the Slovak Koruna/U.S. Dollar Specified Rate, expressed as the amount of Slovak Koruna per one U.S. Dollar, for settlement in two Business Days reported by the National Bank of Slovakia which appears on the Reuters Screen NBSB Page as of 11:40 a.m., Bratislava time, on that Rate Calculation Date. */
    SKK_NBSB_SKK01 = "SKK_NBSB_SKK01",
    /** The Spot Rate for a Rate Calculation Date will be the Thai Baht/U.S. Dollar spot rate at 11:00 a.m., Singapore time, expressed as the amount of Thai Bhaht per one U.S. Dollar, for settlement in two Business Days, reported by the Association of Banks in Singapore which appears on the Reuters Screen ABSIRFIX01 Page to the right of the caption 'Spot' under the column 'THB' at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    THB_ABS_THB01 = "THB_ABS_THB01",
    /** The Spot Rate for a Rate Calculation Date will be the Thai Baht / U.S. Dollar spot rate expressed as the amount of Thai Baht per one U.S. Dollar for settlement in two Business Days, reported by ABS Benchmarks Administration Co Pte. Ltd. (or its successor as administrator or sponsor of the rate), which appears on Thomson Reuters Screen ABSFIX01 Page at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    THB_VWAP_THB01 = "THB_VWAP_THB01",
    /** The Spot Rate for a Rate Calculation Date will be the Taiwanese Dollar/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Taiwanese Dollars per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m., Singapore time, or as soon thereafter as practicable, on such Rate Calculation Date. The Spot Rate will be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC TWD Indicative Survey Methodology (which means a methodology, dated as of December 1, 2004, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Taiwanese Dollar/U.S. Dollar markets for the purpose of determining the SFEMC TWD Indicative Survey Rate). */
    TWD_SFEMC_INDICATIVE_SURVEY_RATE_TWD04 = "TWD_SFEMC_INDICATIVE_SURVEY_RATE_TWD04",
    /** The Spot Rate for a Rate Calculation Date will be the Taiwanese Dollar/U.S. Dollar spot rate, expressed as the amount of Taiwanese Dollars per one U.S. Dollar, for settlement in two Business Days, reported by the Taipei Forex Inc. which appears on the Reuters Screen TAIFX1 Page under the heading 'Spot' as of 11:00 a.m. Taipei time, on that Rate Calculation Date, or if no rate appears as of 11:00 a.m., Taipei time, the rate that first appears in any of the next succeeding 15 minute intervals after such time, up to and including 12:00 noon, Taipei time on that Rate Calculation Date. */
    TWD_TAIFX1_TWD03 = "TWD_TAIFX1_TWD03",
    /** The Spot Rate for a Rate Calculation Date will be the Taiwanese Dollar/U.S. Dollar spot rate, expressed as the amount of Taiwanese Dollars per one U.S. Dollar, for settlement in two Business Days, reported by the Taipei Forex Inc. which appears on the Telerate Page 6161 under the heading 'Spot' as of 11:00 a.m., Taipei time, on that Rate Calculation Date, or if no rate appears as of 11:00 a.m., Taipei time, the rate that first appears in any of the next succeeding 15 minute intervals after such time, up to and including 12:00 noon, Taipei time, on that Rate Calculation Date. */
    TWD_TELERATE_6161_TWD01 = "TWD_TELERATE_6161_TWD01",
    /** The Spot Rate for a Rate Calculation Date will be the Taiwanese Dollar/U.S. Dollar Specified Rate, expressed as the amount of Taiwanese Dollars per one U.S. Dollar, for settlement in two Business Days which appears on the Reuters Screen TFEMA Page as of 11:00 a.m., Taipei time, on that Rate Calculation Date. */
    TWD_TFEMA_TWD02 = "TWD_TFEMA_TWD02",
    /** The Spot Rate for a Rate Calculation Date will be the Ukrainian Hryvnia/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Ukrainian Hryvnia per one U.S. Dollar, for settlement on the same Business Day, as published on EMTA's website (www.emta.org) at approximately 2:00 p.m., Kiev time, or as soon thereafter as practicable, on that Rate Calculation Date. The Spot Rate shall be calculated by EMTA (or a service provider EMTA may select in its sole discretion) pursuant to the EMTA UAH Indicative Survey Methodology (which means a methodology, dated as of March 16, 2009, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Ukrainian Hryvnia / U.S. Dollar markets for the purpose of determining the EMTA UAH Indicative Survey Rate). */
    UAH_EMTA_INDICATIVE_SURVEY_RATE_UAH03 = "UAH_EMTA_INDICATIVE_SURVEY_RATE_UAH03",
    /** The Spot Rate for a Rate Calculation Date will be the Ukrainian Hryvnia/U.S. Dollar Specified Rate for U.S. Dollars expressed as the amount of Ukrainian Hryvnia per one U.S. Dollar, for settlement on the same Business Day calculated by Thomson Reuters pursuant to the EMTA UAH Industry Survey Methodology, which rate appears on EMTA's website (www.emta.org) and on Thomson Reuters Page EMTAUAHFIX at approximately 11:30 am, Kiev time, on that Rate Calculation Date. The 'EMTA UAH Industry Survey Methodology' as used herein means the methodology dated as of March 16, 2009, for a centralized industry wide survey of financial institutions in the Ukrainian Hryvnia/U.S. Dollar spot market for the purposes of determining the EMTA UAH Industry Survey Rate. */
    UAH_EMTA_INDUSTRY_SURVEY_RATE_UAH02 = "UAH_EMTA_INDUSTRY_SURVEY_RATE_UAH02",
    /** The Spot Rate for a Rate Calculation Date will be the Ukrainian Hryvnia/U.S. Dollar spot rate, expressed as the amount of Ukrainian Hryvnia per one U.S. Dollar, for settlement on the same Business Day reported by GFI Brokers on Thomson Reuters Page GFIU by 9:30 am, London time, on that Rate Calculation Date. */
    UAH_GFI_UAH01 = "UAH_GFI_UAH01",
    /** The Spot Rate for a Rate Calculation Date will be the midpoint of the Venezuelan Bolivar /U.S. Dollar Tipo de Cambio De Referencia buying and selling rates, expressed as the amount of Venezuelan Bolivar per one U.S. Dollar, for settlement in two Business Days reported by the Banco Central de Venezuela (www.bcv.org.ve) at approximately 5:00 p.m., Caracas time, on that Rate Calculation Date. */
    VEF_FIX_VEF01 = "VEF_FIX_VEF01",
    /** The Spot Rate for a Rate Calculation Date will be the Vietnamese Dong/U.S. Dollar spot rate at 11:00 a.m., Singapore time, expressed as the amount of Vietnamese Dong per one U.S. Dollar, for settlement in two Business Days reported by the Association of Banks in Singapore, which appears on the Reuters Screen ABSIRFIX01 Page to the right of the caption 'Spot' under the column 'VND' at approximately 11:30 a.m., Singapore time, on that Rate Calculation Date. */
    VND_ABS_VND01 = "VND_ABS_VND01",
    /** The Spot Rate for a Rate Calculation Date will be the Vietnamese Dong/U.S. Dollar spot rate expressed as the amount of Vietnamese Dong per one U.S. Dollar, for settlement in two Business Days which appears on Reuters Screen VNDFIX=VN Page under the caption 'Spot' and to the right of the caption 'Average' at approximately 11:00 am, Hanoi time, on that Rate Calculation Date. */
    VND_FX_VND02 = "VND_FX_VND02",
    /** The Spot Rate for a Rate Calculation Date will be the Vietnamese Dong/U.S. Dollar Specified Rate for U.S. Dollars, expressed as the amount of Vietnamese Dong per one U.S. Dollar, for settlement in two Business Days, as published on SFEMC's website (www.sfemc.org) at approximately 3:30 p.m., Singapore time, or as soon as thereafter as practicable, on that Rate Calculation Date. The Spot Rate shall be calculated by SFEMC (or a service provider SFEMC may select in its sole discretion) pursuant to the SFEMC VND Indicative Survey Methodology (which means a methodology, dated as of July 14, 2008, as amended from time to time, for a centralized industry-wide survey of financial institutions that are active participants in the Vietnamese Dong/U.S. Dollar markets for the purpose of determining the SFEMC VND Indicative Survey Rate). */
    VND_SFEMC_INDICATIVE_SURVEY_RATE_VND03 = "VND_SFEMC_INDICATIVE_SURVEY_RATE_VND03",
};
/**
* The enumerated values to specify how a calculation agent will be determined.
*/
export enum PartyDeterminationEnum {
    
    /** The party that gives notice of exercise. Per 2000 ISDA Definitions, Section 11.1. Parties, paragraph (d). */
    ExercisingParty = "ExercisingParty",
    /** The party that is given notice of exercise. Per 2000 ISDA Definitions, Section 11.1. Parties, paragraph (e). */
    NonExercisingParty = "NonExercisingParty",
    /** The Calculation Agent is determined by reference to the relevant master agreement. */
    AsSpecifiedInMasterAgreement = "AsSpecifiedInMasterAgreement",
    /** The Calculation Agent is determined by reference to the relevant standard terms supplement. */
    AsSpecifiedInStandardTermsSupplement = "AsSpecifiedInStandardTermsSupplement",
    /** Both parties with joined rights to be a calculation agent. */
    Both = "Both",
};
/**
* Provides enumerated values for types of prices in the Price data type in order to explain how to interpret the amount and use it in calculations.
*/
export enum PriceTypeEnum {
    
    /** Denotes a price expressed as a cash amount in a given currency to purchase a unit of an asset (e.g. a security or a commodity). */
    AssetPrice = "AssetPrice",
    /** Denotes the amount payable by the buyer to the seller for an option. The premium is paid on the specified premium payment date or on each premium payment date if specified. */
    Premium = "Premium",
    /** Denotes a price expressed as the weighted average of all pairwise correlation coefficients. */
    Correlation = "Correlation",
    /** Denotes a price expressed as the dividend payment from a index or share. */
    Dividend = "Dividend",
    /** Denotes a rate to convert one currency or other measure of value to another. Foreign Exchange rates are represented in decimals, e.g. {amount, unitOfAmount, PerUnitOfAmount} = [1.23, USD, GBP] = USD 1.23 for every 1 GBP. */
    ExchangeRate = "ExchangeRate",
    /** Denotes a price expressed as a rate to be applied to quantity/notional amount and represented as decimal, e.g. {amount, unitOfAmount, PerUnitOfAmount} = [0.08, EUR, EUR] = 8%  of the EUR notional quantity/amount or 8 cents for every EUR of notional amount. */
    InterestRate = "InterestRate",
    /** Denotes a price expressed as the the arithmetic average of the squared differences from the mean value of an observable price. */
    Variance = "Variance",
    /** Denotes a price expressed as the the square root of the arithmetic average of the squared differences from the mean value of an observable price. */
    Volatility = "Volatility",
};
/**
* Provides a list of possible sub types of the chosen price type, used in conjunction with the PriceTypeEnum.
*/
export enum PriceSubTypeEnum {
    
    /** The lending fee associated with borrowing a security. */
    Fee = "Fee",
    /** Denotes any rebate that is attributed to the cashflow. For example, where a lender will provide a rebate on the cash given to them by the borrower as collateral on a securities lending trade.  */
    Rebate = "Rebate",
};
/**
* The enumerated values to specify the premium type for forward start options.
*/
export enum PremiumTypeEnum {
    
    PrePaid = "PrePaid",
    PostPaid = "PostPaid",
    Variable = "Variable",
    Fixed = "Fixed",
};

export enum PriceOperandEnum {
    
    AccruedInterest = "AccruedInterest",
    Commission = "Commission",
    ForwardPoint = "ForwardPoint",
};
/**
* How is the Creadit Support Annex defined for this transaction as defined in the 2021 ISDA Definitions, section 18.2.1 
*/
export enum CsaTypeEnum {
    
    /** There is no CSA applicable */
    NoCSA = "NoCSA",
    /** Thre is an existing Credit Support Annex */
    ExistingCSA = "ExistingCSA",
    /** There is a bilateral Credit Support Annex specific to the transaction */
    ReferenceVMCSA = "ReferenceVMCSA",
};
/**
* What calculation type is required, averaging or compounding. This enumeration is used to represent the definitions of modular calculated rates as described in the 2021 ISDA Definitions, section 7.
*/
export enum CalculationMethodEnum {
    
    /** Averaging, i.e. arithmetic averaging. */
    Averaging = "Averaging",
    /** Compounding, i.e. geometric averaging following an ISDA defined formula. */
    Compounding = "Compounding",
    /** A rate based on an index that is computed by a rate administrator.  The user is responsible for backing out the rate by applying a simple formula. */
    CompoundedIndex = "CompoundedIndex",
};
/**
* Indicates how to use the inflation index to calculate the payment (e.g. Ratio, Return, Spread). Added for Inflation Asset Swap
*/
export enum InflationCalculationMethodEnum {
    
    /** (Inflation Index Final / Inflation Index Base). Inflation Index Final is inflation index for Reference Month that is the Lag number of months prior to Payment Date (subject to interpolation). Inflation Index Base subject to the Calculation Style. Used in inflation asset swaps to calculate the inflation coupons and principal exchange. */
    Ratio = "Ratio",
    /** (Inflation Index Final / Inflation Index Base -1). Inflation Index Final is the inflation index for Reference Month that is the Lag number of months prior to Payment Date (subject to interp). Inflation Index Base subject to the Calculation Style. Used in market standard ZC Inflation swaps. */
    Return = "Return",
    /** Inflation Index Final - Inflation Index Base). Inflation Index Final is Index for Ref month the Lag months prior to Payment Date (subject to interp). Inflation Index Base subject to the Calculation Style. Typically used for fixing locks. */
    Spread = "Spread",
};
/**
* Indicates the style of how the inflation index calculates the payment (e.g. YearOnYear, ZeroCoupon).
*/
export enum InflationCalculationStyleEnum {
    
    /** YearOnYear means Inflation Index Base is the inflation index for Reference Month that is 12 months prior to Inflation Index Final (subject to interpolation). Inflation Index Base is cashflow dependent. */
    YearOnYear = "YearOnYear",
    /** ZeroCoupon means Inflation Index Base used in the CalculationMethod is the inflation index for the Reference Month that is the lag number of months prior to Effective Date in the case of a swap, or Bond Interest Accrual Date in the case of an Asset Swap (subject to interpolation). Inflation Index Base has the same value for each inflation cashflow and Principal Exchange calculation within the trade. */
    ZeroCoupon = "ZeroCoupon",
};
/**
* The enumerated values to specify whether rate calculations occur relative to the first or last day of a calculation period. Done in uppercase due to a bug in code generation. This enumeration is used to represent the definitions of modular calculated rates as described in the 2021 ISDA Definitions, section 7.
*/
export enum ObservationPeriodDatesEnum {
    
    /** Calculations occur relative to the first day of a calculation period. */
    SetInAdvance = "SetInAdvance",
    /** Calculations occur relative to the last day of a calculation period (set in arrears). */
    Standard = "Standard",
    /** Calculations occur relative to a previously defined reset date, e.g. for a fallback rate. */
    FixingDate = "FixingDate",
};
/**
*  the specific calculation method, e.g. lookback. This enumeration is used to represent the definitions of modular calculated rates as described in the 2021 ISDA Definitions, section 7.
*/
export enum CalculationShiftMethodEnum {
    
    /** Calculations and weighting are done with respect to the calculation period, but observations are shifted back by several days. */
    Lookback = "Lookback",
    /** the observation period is shifted by several days prior to rate setting, and weightings are done with respect to the obseration period. */
    ObservationPeriodShift = "ObservationPeriodShift",
    /** Calculations cut the rate off several business days prior to rate setting (Lockout). */
    RateCutOff = "RateCutOff",
    /** calculations occur without any shifting, e.g. OIS Compounding/Basic Averaging style. */
    NoShift = "NoShift",
};
/**
* Top level ISDA FRO category.
*/
export enum FloatingRateIndexCategoryEnum {
    
    /** The rate is observed directly from a screen. */
    ScreenRate = "ScreenRate",
    /** The rate is calculated by the calculation agents from multiple observations. */
    Calculated = "Calculated",
    /** The rate is obtained by polling several other banks. */
    ReferenceBanks = "ReferenceBanks",
};
/**
* Second level ISDA FRO category.
*/
export enum FloatingRateIndexStyleEnum {
    
    /** An ISDA-defined calculated rate done using arithmetic averaging. */
    AverageFRO = "AverageFRO",
    /** A published index calculated using compounding. */
    CompoundedIndex = "CompoundedIndex",
    /** A published index using a methodology defined by the publisher, e.g. S&P 500. */
    Index = "Index",
    /** Other */
    Other = "Other",
    /** Overnight Rate */
    Overnight = "Overnight",
    /**  A published rate computed using an averaging methodology. */
    PublishedAverage = "PublishedAverage",
    /** Specified Formula */
    SpecifiedFormula = "SpecifiedFormula",
    /** A rate representing the market rate for swaps of a given maturity. */
    SwapRate = "SwapRate",
    /** A rate specified over a given term, such as a libor-type rate. */
    TermRate = "TermRate",
};
/**
* 3rd level ISDA FRO category.
*/
export enum FloatingRateIndexCalculationMethodEnum {
    
    /** A calculation methodology using the ISDA-defined OIS compounding formula. */
    OISCompound = "OISCompound",
    /** A calculation methodology using the arithmetic mean. */
    Average = "Average",
    /** Compounded Index */
    Compounded = "Compounded",
    /** All-In Compounded Index */
    AllInCompounded = "AllInCompounded",
};
/**
* The enumerated values to specify points in the day when option exercise and valuation can occur.
*/
export enum TimeTypeEnum {
    
    /** The official closing time of the exchange on the valuation date. */
    Close = "Close",
    /** The official opening time of the exchange on the valuation date. */
    Open = "Open",
    /** The time at which the official settlement price is determined. */
    OSP = "OSP",
    /** The time specified in the element equityExpirationTime or valuationTime (as appropriate). */
    SpecificTime = "SpecificTime",
    /** The time at which the official settlement price (following the auction by the exchange) is determined by the exchange. */
    XETRA = "XETRA",
    /** The official closing time of the derivatives exchange on which a derivative contract is listed on that security underlier. */
    DerivativesClose = "DerivativesClose",
    /** The time is determined as provided in the relevant Master Confirmation. */
    AsSpecifiedInMasterConfirmation = "AsSpecifiedInMasterConfirmation",
};
/**
* The enumerated values to specify the method according to which an amount or a date is determined.
*/
export enum DeterminationMethodEnum {
    
    /** Agreed separately between the parties. */
    AgreedInitialPrice = "AgreedInitialPrice",
    /** As specified in Master Confirmation. */
    AsSpecifiedInMasterConfirmation = "AsSpecifiedInMasterConfirmation",
    /** Determined by the Calculation Agent. */
    CalculationAgent = "CalculationAgent",
    /** Official Closing Price. */
    ClosingPrice = "ClosingPrice",
    /** Determined by the Currency of Equity Dividends. */
    DividendCurrency = "DividendCurrency",
    /** The initial Index Level is the level of the Expiring Contract as provided in the Master Confirmation. */
    ExpiringContractLevel = "ExpiringContractLevel",
    /** Determined by the Hedging Party. */
    HedgeExecution = "HedgeExecution",
    /** Issuer Payment Currency. */
    IssuerPaymentCurrency = "IssuerPaymentCurrency",
    /** Net Asset Value. */
    NAV = "NAV",
    /** Opening Price of the Market. */
    OpenPrice = "OpenPrice",
    /** OSP Price. */
    OSPPrice = "OSPPrice",
    /** Settlement Currency. */
    SettlementCurrency = "SettlementCurrency",
    /** Date on which the strike is determined in respect of a forward starting swap. */
    StrikeDateDetermination = "StrikeDateDetermination",
    /** Official TWAP Price. */
    TWAPPrice = "TWAPPrice",
    /** TWAP VE Price. */
    TWAPVE = "TWAPVE",
    /** TWAP VE Calculated by Excluding Closing price. */
    TWAPVEExcludingClose = "TWAPVEExcludingClose",
    /** Price determined at valuation time. */
    ValuationTime = "ValuationTime",
    /** Official VWAP Price. */
    VWAPPrice = "VWAPPrice",
    /** VWAP VE Price. */
    VWAPVE = "VWAPVE",
    /** VWAP VE Calculated by Excluding Closing price. */
    VWAPVEExcludingClose = "VWAPVEExcludingClose",
};
/**
* The enumerated values to specify the consequences of Index Events.
*/
export enum IndexEventConsequenceEnum {
    
    /** Calculation Agent Adjustment. */
    CalculationAgentAdjustment = "CalculationAgentAdjustment",
    /** Negotiated Close Out. */
    NegotiatedCloseOut = "NegotiatedCloseOut",
    /** Cancellation and Payment. */
    CancellationAndPayment = "CancellationAndPayment",
    /** Related Exchange. */
    RelatedExchange = "RelatedExchange",
};
/**
* The enumerated values to specify the handling of an averaging date market disruption for an equity derivative transaction.
*/
export enum MarketDisruptionEnum {
    
    /** As defined in section 6.7 paragraph (c) sub-paragraph (iii) of the ISDA 2002 Equity Derivative definitions. */
    ModifiedPostponement = "ModifiedPostponement",
    /** As defined in section 6.7 paragraph (c) sub-paragraph (i) of the ISDA 2002 Equity Derivative definitions. */
    Omission = "Omission",
    /** As defined in section 6.7 paragraph (c) sub-paragraph (ii) of the ISDA 2002 Equity Derivative definitions. */
    Postponement = "Postponement",
};
/**
* The enumerated values to specify whether an option will trigger or expire depending upon whether the spot rate is above or below the barrier rate.
*/
export enum TriggerTypeEnum {
    
    /** The underlier price must be equal to or less than the Trigger level. */
    EqualOrLess = "EqualOrLess",
    /** The underlier price must be equal to or greater than the Trigger level. */
    EqualOrGreater = "EqualOrGreater",
    /** The underlier price must be equal to the Trigger level. */
    Equal = "Equal",
    /** The underlier price must be less than the Trigger level. */
    Less = "Less",
    /** The underlier price must be greater than the Trigger level. */
    Greater = "Greater",
};
/**
* The enumerated values to specify the time of day which would be considered for valuing the knock event.
*/
export enum TriggerTimeTypeEnum {
    
    /** The close of trading on a day would be considered for valuation. */
    Closing = "Closing",
    /** At any time during the Knock Determination period (continuous barrier). */
    Anytime = "Anytime",
};
/**
* The enumerated values to specify the consequences of extraordinary events relating to the underlying.
*/
export enum ShareExtraordinaryEventEnum {
    
    /** The trade continues such that the underlying now consists of the New Shares and/or the Other Consideration, if any, and the proceeds of any redemption, if any, that the holder of the underlying Shares would have been entitled to. */
    AlternativeObligation = "AlternativeObligation",
    /** The trade is cancelled and a cancellation fee will be paid by one party to the other. */
    CancellationAndPayment = "CancellationAndPayment",
    /** The trade will be adjusted by the Calculation Agent in accordance with the adjustments made by any exchange on which options on the underlying are listed. */
    OptionsExchange = "OptionsExchange",
    /** The Calculation Agent will determine what adjustment is required to offset any change to the economics of the trade. If the Calculation Agent cannot achieve this, the trade goes to Cancellation and Payment with the Calculation Agent deciding on the value of the cancellation fee. Adjustments may not be made to account solely for changes in volatility, expected dividends, stock loan rate or liquidity. */
    CalculationAgent = "CalculationAgent",
    /** The Calculation Agent will determine what adjustment is required to offset any change to the economics of the trade. If the Calculation Agent cannot achieve this, the trade goes to Cancellation and Payment with the Calculation Agent deciding on the value of the cancellation fee. Adjustments to account for changes in volatility, expected dividends, stock loan rate or liquidity are allowed. */
    ModifiedCalculationAgent = "ModifiedCalculationAgent",
    /** Applies to Basket Transactions. The portion of the Basket made up by the affected Share will be cancelled and a cancellation fee will be paid from one party to the other. The remainder of the trade continues. */
    PartialCancellationAndPayment = "PartialCancellationAndPayment",
    /** If this is a Share-for-Combined merger event (Shares are replaced with New Shares and Other Consideration), then different treatment can be applied to each component if the parties have specified this. */
    Component = "Component",
};
/**
* The enumerated values to specify the form of the restructuring credit event that is applicable to the credit default swap.
*/
export enum RestructuringEnum {
    
    /** Restructuring (Section 4.7) and Modified Restructuring Maturity Limitation and Conditionally Transferable Obligation (2014 Definitions: Section 3.31, 2003 Definitions: 2.32) apply. */
    ModModR = "ModModR",
    /** Restructuring (Section 4.7) and Restructuring Maturity Limitation and Fully Transferable Obligation (2014 Definitions: Section 3.31, 2003 Definitions: 2.32) apply. */
    ModR = "ModR",
    /** Restructuring as defined in the applicable ISDA Credit Derivatives Definitions. (2003 or 2014). */
    R = "R",
};
/**
* Defines whether the bank holidays are treated as weekdays or weekends in terms of delivery profile in the context of commodity products, in particular those with peak or off-peak delivery profiles.
*/
export enum BankHolidayTreatmentEnum {
    
    /** Bank holidays treated as weekdays. */
    AsWeekday = "AsWeekday",
    /** Bank holidays treated as weekends. */
    AsWeekend = "AsWeekend",
};
/**
* The enumerated values to specify the type of compounding, e.g. flat, straight.
*/
export enum CompoundingMethodEnum {
    
    /** Flat compounding. Compounding excludes the spread. Note that the first compounding period has it's interest calculated including any spread then subsequent periods compound this at a rate excluding the spread. */
    Flat = "Flat",
    /** No compounding is to be applied. */
    None = "None",
    /** Straight compounding. Compounding includes the spread. */
    Straight = "Straight",
    /** Spread Exclusive compounding. */
    SpreadExclusive = "SpreadExclusive",
};
/**
* Denotes the method by which the pricing days are distributed across the pricing period.
*/
export enum DayDistributionEnum {
    
    All = "All",
    First = "First",
    Last = "Last",
    Penultimate = "Penultimate",
};
/**
* The enumerated values to specify the method of calculating discounted payment amounts. This enumerations combines the FpML DiscountingTypeEnum and FraDiscountingEnum enumerations.
*/
export enum DiscountingTypeEnum {
    
    /** As specified by the 2006 ISDA Definitions, Section 8.4. Discounting, paragraph (a). */
    Standard = "Standard",
    /** As specified by the 2006 ISDA Definitions, Section 8.4. Discounting, paragraph (b). */
    FRA = "FRA",
    /** As specified by the 2006 ISDA Definitions, Section 8.4. Discounting, paragraph (e). */
    FRAYield = "FRAYield",
    /** As specified by the Australian Financial Markets Association (AFMA) OTC Financial Product Conventions. This discounting method should not be used for a trade documented under a legal framework where the 2006 ISDA Definitions have been incorporated. */
    AFMA = "AFMA",
};
/**
* The enumerated values to specify whether the dividend is paid with respect to the Dividend Period.
*/
export enum DividendAmountTypeEnum {
    
    /** The record date for a dividend occurs during a dividend period. */
    RecordAmount = "RecordAmount",
    /** The ex-date for a dividend occurs during a dividend period. */
    ExAmount = "ExAmount",
    /** The payment date for a dividend occurs during a dividend period. */
    PaidAmount = "PaidAmount",
    /** The Amount is determined as provided in the relevant Master Confirmation. */
    AsSpecifiedInMasterConfirmation = "AsSpecifiedInMasterConfirmation",
};
/**
* The enumerated values to specify how the composition of Dividends is to be determined.
*/
export enum DividendCompositionEnum {
    
    /** The Equity Amount Receiver determines the composition of dividends (subject to conditions). */
    EquityAmountReceiverElection = "EquityAmountReceiverElection",
    /** The Calculation Agent determines the composition of dividends (subject to conditions). */
    CalculationAgentElection = "CalculationAgentElection",
};
/**
* The enumerated values to specify the date by reference to which the dividend will be paid.
*/
export enum DividendDateReferenceEnum {
    
    /** The dividend date will be specified ad-hoc by the parties, typically on the dividend ex-date. */
    AdHocDate = "AdHocDate",
    /** If 'Dividend Payment Date(s)' is specified in the Transaction Supplement as 'Cash Settlement Payment Date', then the Dividend Payment Date in respect of a Dividend Amount shall be the Cash Settlement Payment Date relating to the end of the Dividend Period during which the Shares commenced trading 'ex' the relevant dividend on the Exchange. */
    CashSettlementPaymentDate = "CashSettlementPaymentDate",
    /** If 'Dividend Payment Date(s)' is specified in the Transaction Supplement as 'Cash Settlement Payment Date – Ex Dividend'', then the Dividend Payment Date in respect of a Dividend Amount shall be the Cash Settlement Payment Date relating to the end of the Dividend Period during which the Shares commenced trading 'ex' the relevant dividend on the Exchange. */
    CashSettlePaymentDateExDiv = "CashSettlePaymentDateExDiv",
    /** If 'Dividend Payment Date(s)' is specified in the Transaction Supplement as 'Cash Settlement Payment Date – Issuer Payment', then the Dividend Payment Date in respect of a Dividend Amount shall be the Cash Settlement Payment Date relating to the end of the Dividend Period during which the issuer pays the relevant dividend to a holder of record provided that in the case where the Equity Amount Payer is the party specified to be the sole Hedging Party and the Hedging Party has not received the Dividend Amount by such date, then the date falling a number of Currency Business Days as specified in the Cash Settlement Payment Date after actual receipt by the Hedging Party of the Received Ex Amount or Paid Ex Amount (as applicable). */
    CashSettlePaymentDateIssuerPayment = "CashSettlePaymentDateIssuerPayment",
    /** Total of dividends which go ex, paid on next following Equity Payment Date, which is immediately following the Dividend Period during which the Shares commence trading ex-dividend on the Exchange. */
    CumulativeEquityExDiv = "CumulativeEquityExDiv",
    /** Total of paid dividends, paid on next following Equity Payment Date, which is immediately following the Dividend Period during which the dividend is paid by the Issuer to the holders of record of a Share. */
    CumulativeEquityPaid = "CumulativeEquityPaid",
    /** Total of paid dividends, paid on next following Equity Payment Date, which is immediately following the Dividend Ex Date, unless the Dividend Ex Date is between the Equity Valuation and Payment Date in which case the dividend is deferred to the following Equity Payment Date */
    CumulativeEquityExDivBeforeReset = "CumulativeEquityExDivBeforeReset",
    /** Total of paid dividends, paid on next following Equity Payment Date, which is immediately following the Dividend Pay Date, unless the Dividend Pay Date is between the Equity Valuation and Payment Date (not including the Valuation Date) in which case the dividend is deferred to the following Equity Payment Date */
    CumulativeEquityPaidBeforeReset = "CumulativeEquityPaidBeforeReset",
    /** Total of paid dividends, paid on next following Equity Payment Date, which is immediately following the Dividend Pay Date, unless the Dividend Pay Date is between the Equity Valuation and Payment Date (including the Valuation Date) in which case the dividend is deferred to the following Equity Payment Date */
    CumulativeEquityPaidInclReset = "CumulativeEquityPaidInclReset",
    /** Total of dividends which go ex, paid on next following Interest Payment Date, which is immediately following the Dividend Period during which the Shares commence trading ex-dividend on the Exchange, or where the date on which the Shares commence trading ex-dividend is a Payment Date, such Payment Date. */
    CumulativeInterestExDiv = "CumulativeInterestExDiv",
    /** Total of paid dividends, paid on next following Interest Payment Date, which is immediately following the Dividend Period during which the dividend is paid by the Issuer to the holders of record of a Share. */
    CumulativeInterestPaid = "CumulativeInterestPaid",
    /** Total of paid dividends, paid on next following Interest Payment Date, which is immediately following the Dividend Pay Date, unless the Dividend Pay Date is between the Equity Valuation and Payment Date (including the Valuation Date) in which case the dividend is deferred to the following Interest Payment Date. */
    CumulativeInterestPaidInclReset = "CumulativeInterestPaidInclReset",
    /** Total of paid dividends, paid on next following Interest Payment Date, which is immediately following the Dividend Pay Date, unless the Dividend Pay Date is between the Equity Valuation and Payment Date (not including the Valuation Date) in which case the dividend is deferred to the following Interest Payment Date. */
    CumulativeInterestPaidBeforeReset = "CumulativeInterestPaidBeforeReset",
    /** Date on which the dividend will be paid by the issuer. */
    DividendPaymentDate = "DividendPaymentDate",
    /** In respect of each Dividend Period, the relevant Dividend Valuation Date. */
    DividendValuationDate = "DividendValuationDate",
    /** Equity payment date of the swap. */
    EquityPaymentDate = "EquityPaymentDate",
    /** Date on which a holder of the security is entitled to the dividend. */
    ExDate = "ExDate",
    /** If 'Dividend Payment Date(s)' is specified in the Transaction Supplement as 'Floating Amount Payment Date', then the Dividend Payment Date in respect of a Dividend Amount shall be the first Payment Date falling at least one Settlement Cycle after the date that the Shares have commenced trading 'ex' the relevant dividend on the Exchange. */
    FloatingAmountPaymentDate = "FloatingAmountPaymentDate",
    /** The next payment date of the swap. */
    FollowingPaymentDate = "FollowingPaymentDate",
    /** Date on which the dividend will be recorded in the books of the paying agent. */
    RecordDate = "RecordDate",
    /** If 'Dividend Payment Date(s)' is specified in the Transaction Supplement as 'Share Payment', then the Dividend Payment Date in respect of a Dividend Amount shall fall on a date on or before the date that is two (or any other number that is specified in the Transaction Supplement) Currency Business Days following the day on which the Issuer of the Shares pays the relevant dividend to holders of record of the Shares. */
    SharePayment = "SharePayment",
    /** Termination date of the swap. */
    TerminationDate = "TerminationDate",
    /** Trade date of the swap */
    TradeDate = "TradeDate",
    /** Pays a fraction of the Dividend Amount on each Unwind Trade Settlement Date which occurs after the Dividend Ex Date, until position is fully unwound OR on the next Equity Pay Date after the Dividend Pay Date. This will be whichever date comes first or a combination of both. */
    UnwindOrEquityExDiv = "UnwindOrEquityExDiv",
    /** Pays a fraction of the Dividend Amount on each Unwind Trade Settlement Date which occurs after the Dividend Pay Date, until position is fully unwound OR on the next Equity Pay Date after the Dividend Pay Date. This will be whichever date comes first or a combination of both. */
    UnwindOrEquityPaid = "UnwindOrEquityPaid",
    /** Pays a fraction of the Dividend Amount on each Unwind Trade Settlement Date which occurs after the Dividend Ex Date, until position is fully unwound OR on the next Interest Pay Date after the Dividend Ex Date. This will be whichever date comes first or a combination of both. */
    UnwindOrInterestExDiv = "UnwindOrInterestExDiv",
    /** Pays a fraction of the Dividend Amount on each Unwind Trade Settlement Date which occurs after the Dividend Pay Date, until position is fully unwound OR on the next Interest Pay Date after the Dividend Pay Date. This will be whichever date comes first or a combination of both. */
    UnwindOrInterestPaid = "UnwindOrInterestPaid",
    /** Pays a fraction of the total on each Unwind Trade Settlement Date which occurs after the Dividend Ex Date, until trade is fully unwound. */
    UnwindExDiv = "UnwindExDiv",
    /** Pays a fraction of the total on each Unwind Trade Settlement Date which occurs after the Dividend Pay Date, until trade is fully unwound. */
    UnwindPaid = "UnwindPaid",
};
/**
* The enumerated values to specify the date on which the receiver of the equity payout is entitled to the dividend.
*/
export enum DividendEntitlementEnum {
    
    /** Dividend entitlement is on the dividend ex-date. */
    ExDate = "ExDate",
    /** Dividend entitlement is on the dividend record date. */
    RecordDate = "RecordDate",
};
/**
* 2002 ISDA Equity Derivatives Definitions: First Period, Second Period |
*/
export enum DividendPeriodEnum {
    
    /** 2002 ISDA Equity Derivatives Definitions: First Period means each period from, and including, one Cash Settlement Payment Date or Settlement Date, as the case may be, to, but excluding, the next following Cash Settlement Payment Date or Settlement Date, as the case may be, except that (i) the initial Dividend Period will commence on, and include, the Clearance System Business Day that is one Settlement Cycle following the Trade Date and (ii) the final Dividend Period will end on, but exclude, the final Cash Settlement Payment Date or Settlement Date, as the case may be. */
    FirstPeriod = "FirstPeriod",
    /** 2002 ISDA Equity Derivatives Definitions: Second Period means each period from, but excluding, one Valuation Date to, and including, the next Valuation Date, except that (i) the initial Dividend Period will commence on, but exclude, the Trade Date and (ii) the final Dividend Period will end on, and include, the final Valuation Date or, in respect of a Physically-settled Forward Transaction to which Variable Obligation is not applicable, the date that is one Settlement Cycle prior to the Settlement Date. */
    SecondPeriod = "SecondPeriod",
};
/**
* The enumerated values to specify the CDX index annex source.
*/
export enum IndexAnnexSourceEnum {
    
    /** As defined in the relevant form of Master Confirmation applicable to the confirmation of Dow Jones CDX indices. */
    MasterConfirmation = "MasterConfirmation",
    /** As defined in the relevant form of Master Confirmation applicable to the confirmation of Dow Jones CDX indices. */
    Publisher = "Publisher",
};
/**
* The enumerated values to specify the interest shortfall cap, applicable to mortgage derivatives.
*/
export enum InterestShortfallCapEnum {
    
    Fixed = "Fixed",
    Variable = "Variable",
};
/**
* Specifies the load type of the delivery.
*/
export enum LoadTypeEnum {
    
    /** Base load */
    BaseLoad = "BaseLoad",
    /** Peak load */
    PeakLoad = "PeakLoad",
    /** Off-peak load */
    OffPeak = "OffPeak",
    /** Block Hours */
    BlockHours = "BlockHours",
    /** Shaped */
    Shaped = "Shaped",
    /** Gas Day */
    GasDay = "GasDay",
    /** Other */
    Other = "Other",
};
/**
* The enumerated values to specify the method of calculating payment obligations when a floating rate is negative (either due to a quoted negative floating rate or by operation of a spread that is subtracted from the floating rate).
*/
export enum NegativeInterestRateTreatmentEnum {
    
    /** Negative Interest Rate Method. Per 2000 ISDA Definitions, Section 6.4 Negative Interest Rates, paragraphs (b) and (c). */
    NegativeInterestRateMethod = "NegativeInterestRateMethod",
    /** Zero Interest Rate Method. Per 2000 ISDA Definitions, Section 6.4. Negative Interest Rates, paragraphs (d) and (e). */
    ZeroInterestRateMethod = "ZeroInterestRateMethod",
    /** Per 2021 ISDA Definitions section 6.8.6 */
    ZeroInterestRateExcludingSpreadMethod = "ZeroInterestRateExcludingSpreadMethod",
};
/**
* The enumerated values to specify the treatment of Non-Cash Dividends.
*/
export enum NonCashDividendTreatmentEnum {
    
    /** The treatment of any non-cash dividend shall be determined in accordance with the Potential Adjustment Event provisions. */
    PotentialAdjustmentEvent = "PotentialAdjustmentEvent",
    /** Any non-cash dividend shall be treated as a Declared Cash Equivalent Dividend. */
    CashEquivalent = "CashEquivalent",
};
/**
* The enumerated values to specify the methods for converting rates from one basis to another.
*/
export enum RateTreatmentEnum {
    
    /** Bond Equivalent Yield. Per Annex to the 2000 ISDA Definitions (June 2000 Version), Section 7.3. Certain General Definitions Relating to Floating Rate Options, paragraph (g). */
    BondEquivalentYield = "BondEquivalentYield",
    /** Money Market Yield. Per Annex to the 2000 ISDA Definitions (June 2000 Version), Section 7.3. Certain General Definitions Relating to Floating Rate Options, paragraph (h). */
    MoneyMarketYield = "MoneyMarketYield",
};
/**
* The enumerated values to specify the type of return associated the equity payout.
*/
export enum ReturnTypeEnum {
    
    /** Price return, i.e. excluding dividends. */
    Price = "Price",
    /** Total return, i.e. including dividend and price components. */
    Total = "Total",
};
/**
* The enumerated values to specify the relevant settled entity matrix source.
*/
export enum SettledEntityMatrixSourceEnum {
    
    /** The Relevant Settled Entity Matrix shall be the list agreed for this purpose by the parties. The list is not included as part of the electronic confirmation. */
    ConfirmationAnnex = "ConfirmationAnnex",
    /** The term is not applicable. */
    NotApplicable = "NotApplicable",
    /** The Settled Entity Matrix published by the Index Publisher. */
    Publisher = "Publisher",
};
/**
* The enumerated values to specify a long or short spread value.
*/
export enum SpreadScheduleTypeEnum {
    
    /** Represents a Long Spread Schedule. Spread schedules defined as 'Long' will be applied to Long Positions. */
    Long = "Long",
    /** Represents a Short Spread Schedule. Spread schedules defined as 'Short' will be applied to Short Positions. */
    Short = "Short",
};
/**
* Used in conjunction with an exchange-based pricing source. Identifies a date source calendar from which the pricing dates and thus roll to the next contract will be based off (e.g. pricing is based on the NYMEX WTI First Nearby Futures Contract, if Future is chosen, the pricing will roll to the next futures contract on expiration, if ListedOption is chosen, the pricing will roll to the next futures contract on the Option expiration date which is three business days before the expiration of the NYMEX WTI futures contract.) Omitting this element will result in the default behavior expected with the pricing source described within the commodity element.
*/
export enum RollSourceCalendarEnum {
    
    ListedOption = "ListedOption",
    Future = "Future",
};
/**
* Seniority of debt instruments comprising the index.
*/
export enum CreditSeniorityEnum {
    
    /** Other as defined under EMIR. */
    Other = "Other",
    /** Senior Loss Absorbing Capacity (RED Tier Code: SNRLAC). */
    SeniorLossAbsorbingCapacity = "SeniorLossAbsorbingCapacity",
    /** Senior domestic (RED Tier Code: SECDOM). */
    SeniorSec = "SeniorSec",
    /** Senior foreign (RED Tier Code: SNRFOR). */
    SeniorUnSec = "SeniorUnSec",
    /** Subordinate, Lower Tier 2 (RED Tier Code: SUBLT2). */
    SubLowerTier2 = "SubLowerTier2",
    /** Subordinate Tier 1 (RED Tier Code: PREFT1). */
    SubTier1 = "SubTier1",
    /** Subordinate, Tier 3. */
    SubTier3 = "SubTier3",
    /** Subordinate, Upper Tier 2 (RED Tier Code: JRSUBUT2). */
    SubUpperTier2 = "SubUpperTier2",
};
/**
* The contract specifies which price must satisfy the boundary condition.  Used for variance, volatility and correlation caps and floors.
*/
export enum RealisedVarianceMethodEnum {
    
    /** For a return on day T, the observed price on T-1 must be in range. */
    Previous = "Previous",
    /** For a return on day T, the observed price on T must be in range. */
    Last = "Last",
    /** For a return on day T, the observed prices on both T and T-1 must be in range */
    Both = "Both",
};
/**
* Specifies the fallback provisions in respect to the applicable Futures Price Valuation.
*/
export enum FPVFinalPriceElectionFallbackEnum {
    
    /** In respect of the Early Final Valuation Date, the provisions for FPV Close shall apply. */
    FPVClose = "FPVClose",
    /** In respect of the Early Final Valuation Date, the provisions for FPV Hedge Execution shall apply. */
    FPVHedgeExecution = "FPVHedgeExecution",
};
/**
* To be specified only for products that embed a redemption payment.
*/
export enum FinalPrincipalExchangeCalculationEnum {
    
    /** If Floored is set then Principal Exchange takes the form: Notional Amount * Max(1, Index Final/ Index Base). */
    Floored = "Floored",
    /** If NonFloored is set then the Principal Exchange takes the form: Notional Amount * Index Final / Index Base. */
    NonFloored = "NonFloored",
};
/**
* Method by which spread is calculated. For example on an asset swap: 'ParPar' or 'Proceeds' may be the method indicated.
*/
export enum SpreadCalculationMethodEnum {
    
    ParPar = "ParPar",
    Proceeds = "Proceeds",
};
/**
* The enumerated values to specify whether payments occur relative to the calculation period start date or end date, each reset date, valuation date or the last pricing date.
*/
export enum PayRelativeToEnum {
    
    /** Payments will occur relative to the first day of each calculation period. */
    CalculationPeriodStartDate = "CalculationPeriodStartDate",
    /** Payments will occur relative to the last day of each calculation period. */
    CalculationPeriodEndDate = "CalculationPeriodEndDate",
    /** Payments will occur relative to the last Pricing Date of each Calculation Period. */
    LastPricingDate = "LastPricingDate",
    /** Payments will occur relative to the reset date. */
    ResetDate = "ResetDate",
    /** Payments will occur relative to the valuation date. */
    ValuationDate = "ValuationDate",
};
/**
* The enumerated values to specify whether resets occur relative to the first or last day of a calculation period.
*/
export enum ResetRelativeToEnum {
    
    /** Resets occur relative to the first day of a calculation period. */
    CalculationPeriodStartDate = "CalculationPeriodStartDate",
    /** Resets occur relative to the last day of a calculation period. */
    CalculationPeriodEndDate = "CalculationPeriodEndDate",
};
/**
* The enumerated values to specify how to deal with a non standard calculation period within a swap stream.
*/
export enum StubPeriodTypeEnum {
    
    /** If there is a non regular period remaining it is left shorter than the streams calculation period frequency and placed at the start of the stream. */
    ShortInitial = "ShortInitial",
    /** If there is a non regular period remaining it is left shorter than the streams calculation period frequency and placed at the end of the stream. */
    ShortFinal = "ShortFinal",
    /** If there is a non regular period remaining it is placed at the start of the stream and combined with the adjacent calculation period to give a long first calculation period. */
    LongInitial = "LongInitial",
    /** If there is a non regular period remaining it is placed at the end of the stream and combined with the adjacent calculation period to give a long last calculation period. */
    LongFinal = "LongFinal",
};
/**
* The enumerated values to specify the weekly roll day.
*/
export enum WeeklyRollConventionEnum {
    
    /** 13-week and 26-week U.S. Treasury Bill Auction Dates. Each Monday except for U.S. (New York) holidays when it will occur on a Tuesday */
    TBILL = "TBILL",
};
/**
* This enumeration provides guidance on how to process a given floating rate index.  It's based on the ISDA Floating Rate Index information, but transforms it into the specific categories needed for calculation 
*/
export enum FloatingRateIndexProcessingTypeEnum {
    
    /** These values are just looked up from the screen and applied. */
    Screen = "Screen",
    /** A published index calculated using compounding; the implied rate must be backed out. */
    CompoundIndex = "CompoundIndex",
    /** These are calculated by the calculation agent based on a standard OIS FRO definition. */
    OIS = "OIS",
    /** These are calculated by the calculation agent based on a standard overnight averaging FRO definition. */
    OvernightAvg = "OvernightAvg",
    /** These are calculated by the calculation agent based on deal-specific parameters (e.g. lookback compound based on an RFR). */
    Modular = "Modular",
    /** These must be looked up using a manual process */
    RefBanks = "RefBanks",
};
/**
* Specifies the types of collateral that are accepted by the Lender
*/
export enum CollateralTypeEnum {
    
    /** Security Lending Trades against Cash collateral */
    Cash = "Cash",
    /** Security Lending Trades against NonCash collateral */
    NonCash = "NonCash",
    /** Security Lending Trades against CashPool collateral */
    CashPool = "CashPool",
};
/**
* Represents the enumerated values to identify where a concentration limit is applied in the eligible collateral schedule.
*/
export enum ConcentrationLimitTypeEnum {
    
    /** Specifies a limit on a single asset in the eligible collateral schedule */
    Asset = "Asset",
    /** Specifies a limit on all cash valued in the base currency of the eligible collateral schedule. */
    BaseCurrencyEquivalent = "BaseCurrencyEquivalent",
    /** Specifies a limit on a single issuer in the eligible collateral schedule. */
    Issuer = "Issuer",
    /** Specifies a limit on a single exchange in the eligible collateral schedule. */
    PrimaryExchange = "PrimaryExchange",
    /** Specifies a limit on a single industry sector in the eligible collateral schedule. */
    IndustrySector = "IndustrySector",
    /** Specifies a limit on a single issuer in the eligible collateral schedule at the ultimate parent institution level. */
    UltimateParentInstitution = "UltimateParentInstitution",
    /** Specifies a limit of the issue compared to the outstanding amount of the asset on the market. */
    IssueOutstandingAmount = "IssueOutstandingAmount",
    /** Specifies a limit of the issue calculated as a percentage of the market capitalisation of the asset on the market. */
    MarketCapitalisation = "MarketCapitalisation",
    /** Specifies a limit on the total outstanding balance for an asset in the portfolio. */
    OutstandingBalance = "OutstandingBalance",
};
/**
* Indicates the type of equity average trading volume (single) the highest amount on one exchange, or (consolidated) volumes across more than one exchange.
*/
export enum AverageTradingVolumeMethodologyEnum {
    
    /** Single, the highest amount on one exchange. */
    Single = "Single",
    /** Consolidated volume across more than one exchange. */
    Consolidated = "Consolidated",
};
/**
* The enumerated values to specify the type of Credit Support Agreement governing the transaction.
*/
export enum CreditSupportAgreementTypeEnum {
    
    /** A Credit Support Deed legal agreement. */
    CreditSupportDeed = "CreditSupportDeed",
    /** A Credit Support Annex legal agreement. */
    CreditSupportAnnex = "CreditSupportAnnex",
    /** A Collateral Transfer Agreement */
    CollateralTransferAgreement = "CollateralTransferAgreement",
};
/**
* If there is an alternative to interest amounts, how is it specified?
*/
export enum AlternativeToInterestAmountEnum {
    
    /** Interest amount is not transferred if transfer would create or increase a delivery amount. */
    Standard = "Standard",
    /** The standard calculation of the Interest Amount is replaced with the amount of interest the secured party actually receives in relation to the Cash collateral. */
    ActualAmountReceived = "ActualAmountReceived",
    /** Interest amount is not transferred if transfer would create or increase a delivery amount. (This is the 'Standard' provision). However, interest Amount will be transferred if Delivery Amount is below Minimum Transfer Amount. */
    TransferIfDeliveryAmountBelowMTA = "TransferIfDeliveryAmountBelowMTA",
    /** An other alternative option outside these choices that can be described as an alternative provision. */
    Other = "Other",
};
/**
* How is collateral interest to be handled?
*/
export enum CollateralInterestHandlingEnum {
    
    /**  Transfer the interest each period  */
    Transfer = "Transfer",
    /**  Adjust the collateral balance to include the interest amount  */
    Adjust = "Adjust",
    /**  Transfer the interest if it meets certain criteria, or otherwise adjust the collateral balance to reflect the interest amount   */
    Transfer_or_Adjust = "Transfer_or_Adjust",
};
/**
* The enumerated values to specify the application of Interest Amount with respect to the Delivery Amount through standard language.
*/
export enum DeliveryAmountElectionEnum {
    
    /** The delivery only includes `Transfer on last Local Business Day. */
    LastLocalBusinessDay = "LastLocalBusinessDay",
    /** The delivery includes both `Transfer on last Local Business Day` and `Transfer a Delivery Amount (IM) consisting of cash on any Local Business Day.` */
    LastAndAnyLocalBusinessDay = "LastAndAnyLocalBusinessDay",
};
/**
* The enumerated values to specify the type of margin for which a legal agreement is named.
*/
export enum CollateralMarginTypeEnum {
    
    /** Denotes a margin agreement that is identified for use with Variation Margin/VM. */
    VariationMargin = "VariationMargin",
    /** Denotes a margin agreement that is identified for use with Initial Margin/IM. */
    InitialMargin = "InitialMargin",
};
/**
* Represents an enumeration list to identify which Collateral Criteria type should have priority over others. If set to 'Issuer', the rating in the  Issuer Criteria has priority or is used if there is no Asset criteria. If set to 'Asset', the rating in the Asset Criteria has priority or is used if there is no Issuer rating.
*/
export enum RatingPriorityResolutionEnum {
    
    /** Denotes that the Issuer Criteria has priority. */
    Issuer = "Issuer",
    /** Denotes that the Asset Criteria has priority. */
    Asset = "Asset",
    /** Denotes that lowest rating should be used if several criteria apply. */
    Lowest = "Lowest",
    /** Denotes that highest rating should be used if several criteria apply. */
    Highest = "Highest",
    /** Denotes that average rating should be used if several criteria apply. */
    Average = "Average",
};
/**
* The enumerated values to specify an Equity index.
*/
export enum CollateralEquityIndexEnum {
    
    /** AEX INDEX (AEX) */
    AEX = "AEX",
    /** ALL ORDINARIES INDEX */
    ALLORD = "ALLORD",
    /** AMSTERDAM MIDKAP INDEX */
    AMSMID = "AMSMID",
    /** AMSTERDAM SE ALL SHARE INDEX */
    AASHR = "AASHR",
    /** AMSTERDAM SMALL CAP INDEX */
    ASCIX = "ASCIX",
    /** ARGENTINA BOLSA-G (NEW) INDEX */
    ARBG = "ARBG",
    /** ARGENTINA MERVAL INDEX */
    ARGMI = "ARGMI",
    /** ATHEX COMPOSITE INDEX */
    ATHEX = "ATHEX",
    /** ATX PRIME INDEX */
    ATX50 = "ATX50",
    /** AUSTRIAN TRADED INDEX */
    ATX = "ATX",
    /** BEL 20 INDEX */
    BEL20 = "BEL20",
    /** BEL ALL SHARE INDEX */
    BELAS = "BELAS",
    /** BEL MID INDEX */
    BELMD = "BELMD",
    /** BEL SMALL INDEX */
    BGBEL = "BGBEL",
    /** BELGIUM-DS MARKET INDEX */
    BELSM = "BELSM",
    /** BG REIT INDEX */
    BGREIT = "BGREIT",
    /** BG TR30 INDEX */
    BULG30 = "BULG30",
    /** BGBX40 INDEX */
    BGBX40 = "BGBX40",
    /** BIST NATIONAL 100 INDEX */
    TRKSTN = "TRKSTN",
    /** BIST NATIONAL 30 INDEX */
    TIN30 = "TIN30",
    /** BNY MELLON ADR INDEX */
    BKADR = "BKADR",
    /** BNY MELLON BRIC SELECT ADR INDEX */
    BKBRIC = "BKBRIC",
    /** BUMIX INDEX */
    BUMIX = "BUMIX",
    /** BUX INDEX */
    BUDPES = "BUDPES",
    /** BVL GENERAL INDEX */
    PBVGEN = "PBVGEN",
    /** CAC 40 INDEX */
    CAC40 = "CAC40",
    /** CAC ALL SHARES INDEX */
    FCASH = "FCASH",
    /** CAC ALL TRADABLE INDEX */
    SBF250 = "SBF250",
    /** CAC LARGE 60 INDEX */
    CACL60 = "CACL60",
    /** CAC MID & SMALL INDEX */
    CAC190 = "CAC190",
    /** CAC MID 60 INDEX */
    FCAM = "FCAM",
    /** CAC NEXT 20 INDEX */
    CAC20 = "CAC20",
    /** CAC SMALL INDEX */
    CSM90 = "CSM90",
    /** CDAX PERFORMANCE INDEX */
    CDAX = "CDAX",
    /** CENTRAL & EAST. EUROP.FIN.CECE INDEX */
    CECE = "CECE",
    /** CHILE INTER 10 INDEX */
    CINT10 = "CINT10",
    /** CHILE SANTIAGO SE GENERAL (IGPA) INDEX */
    CSMGL = "CSMGL",
    /** CHILE SANTIAGO SE SELECTIVE (IPSA) INDEX */
    CSMST = "CSMST",
    /** CNX 100 INDEX */
    CNX100 = "CNX100",
    /** COMIT GLOBAL INDEX */
    MILBCI = "MILBCI",
    /** CROATIA ZAGREB STOCK EXCHANGE CROBEX INDEX */
    CZSEC = "CZSEC",
    /** CSI 500 */
    CSI500 = "CSI500",
    /** CZECH TRADED INDEX */
    CZT = "CZT",
    /** DAX INDEX */
    DAX = "DAX",
    /** DOW JONES BASIC RESOURCES TITANS 30 INDEX */
    DJTB30 = "DJTB30",
    /** DOW JONES COMPOSITE INDEX */
    DOWCOM = "DOWCOM",
    /** DOW JONES INDUSTRIAL AVERAGE INDEX */
    DOWI = "DOWI",
    /** DOW JONES ITALY TITANS 30 INDEX */
    DJT30 = "DJT30",
    /** DOW JONES SECTOR TITANS COMPOSITE INDEX */
    DJTCOM = "DJTCOM",
    /** DOW JONES TRANSPORTATION AVERAGE INDEX */
    DOWTRA = "DOWTRA",
    /** DOW JONES UTILITY AVERAGE INDEX */
    DOWUTI = "DOWUTI",
    /** EURO STOXX 50 INDEX */
    DJES50 = "DJES50",
    /** EURO STOXX INDEX */
    DJEUR = "DJEUR",
    /** EURO STOXX LARGE INDEX */
    DJESTX = "DJESTX",
    /** EURONEXT 100 INDEX */
    EURNXT = "EURNXT",
    /** EX-TSE FIRST SECTION INDEX */
    TSEFSI = "TSEFSI",
    /** FTSE 100 INDEX */
    FT100 = "FT100",
    /** FTSE 250 INDEX */
    FTSE25 = "FTSE25",
    /** FTSE 350 INDEX */
    FT350 = "FT350",
    /** FTSE AIM ALL-SHARE INDEX */
    FTAIM = "FTAIM",
    /** FTSE ALL SHARE INDEX */
    FTSEAL = "FTSEAL",
    /** FTSE ALL WORLD INDEX */
    FALLW = "FALLW",
    /** FTSE BURSA MALAYSIA EMAS SHARIAH INDEX */
    BEMAS = "BEMAS",
    /** FTSE BURSA MALAYSIA HIJRAH SHARIAH INDEX */
    BHSH = "BHSH",
    /** FTSE BURSA MALAYSIA KLCI INDEX */
    KLCOMP = "KLCOMP",
    /** FTSE CHINA A50 */
    FCHA50 = "FCHA50",
    /** FTSE EURO 100 INDEX */
    FTE100 = "FTE100",
    /** FTSE EUROTOP 100 INDEX */
    EURTP1 = "EURTP1",
    /** FTSE FLEDGLING INDEX */
    FTSFLG = "FTSFLG",
    /** FTSE GWA SHARIAH DEVELOPED INDEX */
    GWADV = "GWADV",
    /** FTSE ITALIA ALL SHARE INDEX */
    FTIASI = "FTIASI",
    /** FTSE ITALIA MID CAP INDEX */
    FTIMCI = "FTIMCI",
    /** FTSE ITALIA SMALL CAP INDEX */
    FITSMC = "FITSMC",
    /** FTSE ITALIA STAR INDEX */
    ALLSTR = "ALLSTR",
    /** FTSE MIB INDEX */
    SPMIB = "SPMIB",
    /** FTSE SMALL CAP INDEX */
    FTSMCA = "FTSMCA",
    /** FTSE ST ALL SHARE INDEX */
    SNGSES = "SNGSES",
    /** FTSE TECHMARK FOCUS INDEX */
    FTSE = "FTSE",
    /** FTSE TWSE TAIWAN 50 INDEX */
    TAI50 = "TAI50",
    /** FTSE/ATHEX LARGE CAP INDEX */
    FSA20 = "FSA20",
    /** FTSE/ATHEX MID CAP INDEX */
    FTS40 = "FTS40",
    /** FTSE/JSE ALL SHARE INDEX */
    JSEAS = "JSEAS",
    /** FTSE/JSE INDUSTRIAL 25 INDEX */
    JOHT25 = "JOHT25",
    /** FTSE/JSE SHARIAH ALL SHARE INDEX */
    FTJAS = "FTJAS",
    /** FTSE/JSE SHARIAH TOP 40 INDEX */
    FTJ40 = "FTJ40",
    /** FTSE/JSE TOP 40 INDEX */
    JOHT40 = "JOHT40",
    /** FTSEUROFIRST 300 INDEX */
    EUR300 = "EUR300",
    /** GERMANY X-DAX INDEX */
    XDAX = "XDAX",
    /** HANG SENG CHINA AFFILIATED CORP INDEX */
    HKHS = "HKHS",
    /** HANG SENG CHINA ENTERPRISES INDEX */
    HSCEI = "HSCEI",
    /** HANG SENG COMPOSITE INDEX */
    HSCI = "HSCI",
    /** HANG SENG INDEX */
    HSENG = "HSENG",
    /** HANG SENG MAINLAND 100 INDEX */
    HSM100 = "HSM100",
    /** HANG SENG REIT INDEX */
    HSREIT = "HSREIT",
    /** HDAX (XETRA) INDEX */
    HDAX = "HDAX",
    /** HERMES FINANCIAL INDEX */
    EHRMS = "EHRMS",
    /** HUNGARIAN TRADED HTX (USD) INDEX */
    HUNTRI = "HUNTRI",
    /** IBEX 35 INDEX */
    IBEX35 = "IBEX35",
    /** IBEX MEDIUM CAP INDEX */
    IBEXM = "IBEXM",
    /** IBEX SMALL CAP INDEX */
    IBEXS = "IBEXS",
    /** IBOVESPA BRASIL SAO PAULO SE INDEX */
    BOVESP = "BOVESP",
    /** IDX COMPOSITE INDEX */
    JKCM = "JKCM",
    /** IDX LQ45 INDEX */
    LQ45 = "LQ45",
    /** IGBM INDEX */
    SPAMAD = "SPAMAD",
    /** IMC 30 INDEX */
    IMC30 = "IMC30",
    /** IRELAND SE OVERALL (ISEQ) INDEX */
    ISEQ = "ISEQ",
    /** ISEQ 20 INDEX */
    ISEQ20 = "ISEQ20",
    /** JPX-NIKKEI 400 INDEX */
    NK400 = "NK400",
    /** KFMX INDEX */
    DBMIDC = "DBMIDC",
    /** KOSDAQ INDEX */
    SKKD = "SKKD",
    /** KOSPI 100 INDEX */
    KO100 = "KO100",
    /** KOSPI 200 INDEX */
    KORK20 = "KORK20",
    /** KOSPI 50 INDEX */
    KO50 = "KO50",
    /** KOSPI INDEX */
    KSPI = "KSPI",
    /** KOSPI LARGECAP INDEX */
    KORLRG = "KORLRG",
    /** KOSPI MIDCAP INDEX */
    KOSPM = "KOSPM",
    /** KOSPI SMALLCAP INDEX */
    KOSPS = "KOSPS",
    /** KRX 100 INDEX */
    KRX100 = "KRX100",
    /** LUXEMBOURG SE LUXX INDEX */
    LXBORG = "LXBORG",
    /** MDAX INDEX */
    DAXMID = "DAXMID",
    /** MOEX RUSSIA INDEX (MICEX INDEX) */
    MICEX = "MICEX",
    /** MSCI AC ASIA PACIFIC ESG LEADERS */
    MSAPAC = "MSAPAC",
    /** MSCI AC EUROPE AND MIDDLE EAST INDEX */
    MSEME = "MSEME",
    /** MSCI AC WORLD EX JAPAN INDEX (AKA MSCI KOKUSAI INDEX) */
    MSACEJ = "MSACEJ",
    /** MSCI AC WORLD IMI INDEX */
    MSACW = "MSACW",
    /** MSCI AC WORLD INDEX */
    MSALL = "MSALL",
    /** MSCI ACWI ESG SCREENED */
    MSACES = "MSACES",
    /** MSCI ACWI EX JAPAN INDEX */
    MSWDEJ = "MSWDEJ",
    /** MSCI AUSTRALIA IMI INDEX */
    MAUIMI = "MAUIMI",
    /** MSCI AUSTRALIA LARGE CAP INDEX */
    MAUSLC = "MAUSLC",
    /** MSCI AUSTRALIA MID CAP INDEX */
    MAUSMC = "MAUSMC",
    /** MSCI AUSTRALIA */
    MSAUST = "MSAUST",
    /** MSCI AUSTRIA IMI INDEX */
    AUSIMI = "AUSIMI",
    /** MSCI BELGIUM INDEX */
    MSBELG = "MSBELG",
    /** MSCI BRAZIL INDEX */
    MSBRZ = "MSBRZ",
    /** MSCI CANADA */
    MSCNDA = "MSCNDA",
    /** MSCI CHINA A50 */
    MCHA50 = "MCHA50",
    /** MSCI CHINA INDEX */
    MSCHIN = "MSCHIN",
    /** MSCI CZECH REPUBLIC LARGE CAP INDEX */
    MSCRLC = "MSCRLC",
    /** MSCI CZECH REPUBLIC MID CAP INDEX */
    MSCRMC = "MSCRMC",
    /** MSCI DENMARK IMI INDEX */
    MSDIMI = "MSDIMI",
    /** MSCI EAFE IMI INDEX */
    MSIMI = "MSIMI",
    /** MSCI EAFE INDEX */
    MSEAFE = "MSEAFE",
    /** MSCI EM ASIA ESG LEADERS */
    MSEMAS = "MSEMAS",
    /** MSCI EMERGING MARKETS INDEX */
    MSCIEM = "MSCIEM",
    /** MSCI EURO INDEX */
    MSERO = "MSERO",
    /** MSCI EUROPE ESG LEADERS */
    MSEESG = "MSEESG",
    /** MSCI EUROPE ESG SCREENED */
    MSUREE = "MSUREE",
    /** MSCI EUROPE INDEX */
    MSCIE = "MSCIE",
    /** MSCI EUROPE SMALL CAP INDEX */
    MSESC = "MSESC",
    /** MSCI EUROPE SMID CAP INDEX */
    MESMID = "MESMID",
    /** MSCI FINLAND LARGE CAP INDEX */
    MSFDLC = "MSFDLC",
    /** MSCI FINLAND MID CAP INDEX */
    MSFDMC = "MSFDMC",
    /** MSCI FRANCE IMI INDEX */
    MSFIMI = "MSFIMI",
    /** MSCI FRANCE LARGE CAP INDEX */
    MSFRLP = "MSFRLP",
    /** MSCI HONG KONG IMI INDEX */
    MHKIMI = "MHKIMI",
    /** MSCI INDIA INDEX */
    MSIND = "MSIND",
    /** MSCI IRELAND IMI INDEX */
    MIRIMI = "MIRIMI",
    /** MSCI ITALY INDEX */
    MSITAL = "MSITAL",
    /** MSCI ITALY LARGE CAP INDEX */
    MSITLC = "MSITLC",
    /** MSCI ITALY MID CAP INDEX */
    MSITMC = "MSITMC",
    /** MSCI ITALY SMALL CAP INDEX */
    ITSMCI = "ITSMCI",
    /** MSCI JAPAN IMI INDEX */
    JAPIMI = "JAPIMI",
    /** MSCI JAPAN INDEX */
    MSCIJP = "MSCIJP",
    /** MSCI KOREA INDEX */
    MSKOR = "MSKOR",
    /** MSCI MEXICO INDEX */
    MSMEX = "MSMEX",
    /** MSCI NETHERLANDS INDEX */
    MSNETH = "MSNETH",
    /** MSCI NETHERLANDS MID CAP INDEX */
    MSNEMC = "MSNEMC",
    /** MSCI NEW ZEALAND IMI INDEX */
    MNZIMI = "MNZIMI",
    /** MSCI NORWAY INDEX */
    MSNORW = "MSNORW",
    /** MSCI PAN EURO INDEX */
    MSCIPE = "MSCIPE",
    /** MSCI POLAND LARGE CAP INDEX */
    MPOLLC = "MPOLLC",
    /** MSCI POLAND MID CAP INDEX */
    MPOLMC = "MPOLMC",
    /** MSCI PORTUGAL IMI INDEX */
    MSPIMI = "MSPIMI",
    /** MSCI SINGAPORE INDEX */
    MSSING = "MSSING",
    /** MSCI SOUTH AFRICA INDEX */
    MSCSA = "MSCSA",
    /** MSCI SPAIN IMI INDEX */
    MSSIMI = "MSSIMI",
    /** MSCI SWEDEN IMI INDEX */
    SWEIMI = "SWEIMI",
    /** MSCI SWEDEN LARGE CAP INDEX */
    MSWELC = "MSWELC",
    /** MSCI SWITZERLAND IMI INDEX */
    SWIIMI = "SWIIMI",
    /** MSCI SWITZERLAND LARGE CAP INDEX */
    MSSWLP = "MSSWLP",
    /** MSCI TAIWAN INDEX */
    MSTAI = "MSTAI",
    /** MSCI THAILAND */
    MSTHAI = "MSTHAI",
    /** MSCI UK IMI */
    UKIMI = "UKIMI",
    /** MSCI UK INDEX */
    MSCIUK = "MSCIUK",
    /** MSCI UK SMALL CAP INDEX */
    UKSMCI = "UKSMCI",
    /** MSCI USA ESG LEADERS */
    MSUSAL = "MSUSAL",
    /** MSCI USA ESG SCREENED */
    MSUSEE = "MSUSEE",
    /** MSCI USA INDEX */
    MSCIUS = "MSCIUS",
    /** MSCI WORLD ESG LEADERS */
    MSWESG = "MSWESG",
    /** MSCI WORLD ESG SCREENED */
    MSDWEE = "MSDWEE",
    /** MSCI WORLD INDEX */
    MSCIWD = "MSCIWD",
    /** MSCI WORLD LOW CARBON SRI LEADERS INDEX */
    MSLCBL = "MSLCBL",
    /** MSCI WORLD SMALL CAP ESG SCREENED */
    MSWSCS = "MSWSCS",
    /** MSCI WORLD SMID CAP INDEX */
    MWSMID = "MWSMID",
    /** MWIG40 INDEX */
    MWIG = "MWIG",
    /** NASDAQ 100 INDEX */
    NASDAQ = "NASDAQ",
    /** NASDAQ COMPOSITE INDEX */
    NASCOM = "NASCOM",
    /** NEXT 150 INDEX */
    NEXT15 = "NEXT15",
    /** NIKKEI 500 INDEX */
    NIK500 = "NIK500",
    /** NIKKEI STOCK AVERAGE (NIKKEI 225) INDEX */
    NIKKEI = "NIKKEI",
    /** NIKKEI STOCK INDEX 300 */
    NK300O = "NK300O",
    /** NYSE COMPOSITE INDEX */
    NYSE = "NYSE",
    /** OBX INDEX */
    OBX25 = "OBX25",
    /** OMX AFFARSVARLDENS GENERAL INDEX */
    OMXAFG = "OMXAFG",
    /** OMX COPENHAGEN (OMXC) INDEX */
    KAX = "KAX",
    /** OMX COPENHAGEN (OMXC20) INDEX */
    KFX = "KFX",
    /** OMX COPENHAGEN BENCHMARK (OMXCB) INDEX */
    KBX = "KBX",
    /** OMX COPENHAGEN MID CAP INDEX */
    OMXCPM = "OMXCPM",
    /** OMX COPENHAGEN SMALL CAP INDEX */
    OMXCPS = "OMXCPS",
    /** OMX HELSINKI (OMXH) INDEX */
    HEXGEN = "HEXGEN",
    /** OMX HELSINKI 25 (OMXH25) INDEX */
    HEX25 = "HEX25",
    /** OMX HELSINKI CAP (OMXHCAP) INDEX */
    HEXP = "HEXP",
    /** OMX HELSINKI MID CAP INDEX */
    OMHSM = "OMHSM",
    /** OMX HELSINKI SMALL CAP INDEX */
    OMHSS = "OMHSS",
    /** OMX HELSINKI TECHNOLOGY INDEX */
    NOMXT = "NOMXT",
    /** OMX ICELAND ALL SHARE INDEX */
    ICEX = "ICEX",
    /** OMX ICELAND MID CAP PI INDEX */
    ICEM = "ICEM",
    /** OMX ICELAND SMALL CAP INDEX */
    ICES = "ICES",
    /** OMX STOCKHOLM 30 INDEX */
    OMX = "OMX",
    /** OMX STOCKHOLM ALL SHARE OMXS INDEX */
    GENX = "GENX",
    /** OMX STOCKHOLM BENCHMARK INDEX */
    SOXP = "SOXP",
    /** OSLO BORS ALL SHARE INDEX */
    OBX = "OBX",
    /** OSLO BORS BENCHMARK INDEX */
    OEBI = "OEBI",
    /** OSLO BORS MUTUAL FUND INDEX */
    OSEMF = "OSEMF",
    /** OSLO BORS SMALL CAP INDEX */
    OESC = "OESC",
    /** OTOB RUSSIAN TRADED RTX (USD) INDEX */
    RUSTRA = "RUSTRA",
    /** PHILIPPINE STOCK EXCHANGE PSEI INDEX */
    PSECOM = "PSECOM",
    /** PORTUGAL PSI ALL-SHARE INDEX */
    PSIGI = "PSIGI",
    /** PORTUGAL PSI-20 INDEX */
    PSI = "PSI",
    /** PRAGUE STOCK EXCHANGE INDEX */
    PRAGE = "PRAGE",
    /** PRIME ALL SHARE INDEX */
    PRIMAL = "PRIMAL",
    /** RSF EE MT (RUR) INDEX */
    ASPMTI = "ASPMTI",
    /** RSF GENERAL INDEX */
    ASPGEN = "ASPGEN",
    /** RTS INDEX */
    RRTS = "RRTS",
    /** RUSSELL 1000 INDEX */
    RU1000 = "RU1000",
    /** RUSSELL 2000 INDEX */
    RU2000 = "RU2000",
    /** RUSSELL 2500 INDEX */
    RU250 = "RU250",
    /** RUSSELL 3000 INDEX */
    RU3000 = "RU3000",
    /** S&P 100 INDEX */
    SP100 = "SP100",
    /** S&P 1500 SUPERCOMPOSITE INDEX */
    SP1500 = "SP1500",
    /** S&P 500 FINANCIAL INDEX */
    SPFINA = "SPFINA",
    /** S&P 500 INDEX */
    SP500 = "SP500",
    /** S&P 500 INDUSTRIALS INDEX */
    SPINDU = "SPINDU",
    /** S&P 600 SMALLCAP INDEX */
    SP600 = "SP600",
    /** S&P ADR INDEX */
    SPADR = "SPADR",
    /** S&P BMI FRANCE INDEX */
    BMIFR = "BMIFR",
    /** S&P BSE (SENSEX) 30 SENSITIVE INDEX */
    INSPCN = "INSPCN",
    /** S&P BSE 100 INDEX */
    BS100 = "BS100",
    /** S&P BSE 200 INDEX */
    BS200 = "BS200",
    /** S&P BSE 500 INDEX */
    BOM500 = "BOM500",
    /** S&P EMERGING BMI INDEX */
    BMIEMG = "BMIEMG",
    /** S&P GLOBAL 1200 ENERGY INDEX */
    SPGNRG = "SPGNRG",
    /** S&P GLOBAL 1200 INDEX */
    SPGLBL = "SPGLBL",
    /** S&P LATIN AMERICA 40 INDEX */
    SPLT40 = "SPLT40",
    /** S&P MIDCAP 400 INDEX */
    SPMIDC = "SPMIDC",
    /** S&P TOPIX 150 INDEX */
    TX150 = "TX150",
    /** S&P/ASX 100 INDEX */
    ASX100 = "ASX100",
    /** S&P/ASX 20 INDEX */
    _20LEAD = "_20LEAD",
    /** S&P/ASX 200 INDEX */
    ASX200 = "ASX200",
    /** S&P/ASX 300 INDEX */
    ASX300 = "ASX300",
    /** S&P/ASX 50 INDEX */
    ASX50 = "ASX50",
    /** S&P/ASX MIDCAP 50 INDEX */
    ASXM50 = "ASXM50",
    /** S&P/ASX MIDCAP 50 INDUSTRIALS INDEX */
    ASPA = "ASPA",
    /** S&P/ASX SMALL ORDINARIES INDEX */
    SPASX = "SPASX",
    /** S&P/BMV INMEX INDEX */
    INMEX = "INMEX",
    /** S&P/BMV IPC COMPMX INDEX */
    MEXCM = "MEXCM",
    /** S&P/BMV IPC INDEX */
    IPC = "IPC",
    /** S&P/BMV IPC MIDCAP INDEX */
    IPCM = "IPCM",
    /** S&P/BVL PERU GENERAL INDEX */
    PEGEN = "PEGEN",
    /** S&P/CNX NIFTY (50) INDEX */
    INSENS = "INSENS",
    /** S&P/HKEX GEM INDEX */
    SPHG = "SPHG",
    /** S&P/HKEX LARGE CAP INDEX */
    SPLC = "SPLC",
    /** S&P/NZX 10 INDEX */
    NZX10 = "NZX10",
    /** S&P/NZX 50 INDEX */
    NZSE50 = "NZSE50",
    /** S&P/NZX ALL INDEX CAPITAL INDEX */
    NZSEAL = "NZSEAL",
    /** S&P/NZX MIDCAP INDEX */
    NZXMI = "NZXMI",
    /** S&P/NZX SMALLCAP INDEX */
    NZXSI = "NZXSI",
    /** S&P/TSX 60 INDEX */
    SP60 = "SP60",
    /** S&P/TSX COMPLETION INDEX */
    CSPT = "CSPT",
    /** S&P/TSX COMPOSITE INDEX */
    TSE300 = "TSE300",
    /** S&P/TSX PREFERRED SHARE INDEX */
    SNPPF = "SNPPF",
    /** S&P/TSX SMALLCAP INDEX */
    CSPS = "CSPS",
    /** S&P/TSX VENTURE COMPOSITE INDEX */
    TSXVEN = "TSXVEN",
    /** SBF 120 INDEX */
    SBF120 = "SBF120",
    /** SBI TOP INDEX */
    SBITOP = "SBITOP",
    /** SDAX PERFORMANCE INDEX */
    SDAX = "SDAX",
    /** SET100 INDEX */
    SET100 = "SET100",
    /** SET50 INDEX */
    BNGK50 = "BNGK50",
    /** SHANGHAI G SHARE INDEX */
    CHSHG = "CHSHG",
    /** SHANGHAI SE 180 A SHR INDEX */
    C180 = "C180",
    /** SHANGHAI SE A SHARE INDEX */
    CHSA = "CHSA",
    /** SHANGHAI SE B SHARE INDEX */
    CHSB = "CHSB",
    /** SHANGHAI SE COMPOSITE INDEX */
    CSSC = "CSSC",
    /** SHANGHAI SHENZHEN CSI 300 INDEX */
    S300 = "S300",
    /** SHANGHAI SHENZHEN CSI 300 INDEX */
    _S300 = "_S300",
    /** SHENZEN SE A SHARE INDEX */
    CHZA = "CHZA",
    /** SHENZEN SE B SHARE INDEX */
    CHZB = "CHZB",
    /** SHENZEN SE COMPOSITE INDEX */
    CSZC = "CSZC",
    /** SLI SWISS LEADER INDEX */
    SLISWS = "SLISWS",
    /** SMI EXPANDED INDEX */
    SMIEX = "SMIEX",
    /** SMI INDEX */
    SMI = "SMI",
    /** SMI MID INDEX */
    SMIM = "SMIM",
    /** SOFIX INDEX */
    BSOFIX = "BSOFIX",
    /** STOXX ASIA/PACIFIC 600 INDEX */
    PAC600 = "PAC600",
    /** STOXX EUROPE 50 INDEX */
    DJSE = "DJSE",
    /** STOXX EUROPE 600 BANKS INDEX */
    STXE6B = "STXE6B",
    /** STOXX EUROPE 600 INDEX */
    DJS600 = "DJS600",
    /** STOXX EUROPE LARGE 200 INDEX */
    DJSTXL = "DJSTXL",
    /** STOXX GLOBAL 1800 INDEX */
    SX1800 = "SX1800",
    /** STRAITS TIMES INDEX (STI) */
    SNGSTR = "SNGSTR",
    /** SWIG80 INDEX */
    SWIG = "SWIG",
    /** SWISS PERFORMANCE INDEX */
    SPI = "SPI",
    /** TA-125 INDEX */
    ISTMI = "ISTMI",
    /** TA-35 INDEX */
    TELAVI = "TELAVI",
    /** TADAWUL ALL SHARE INDEX */
    TDWTAS = "TDWTAS",
    /** TAIEX INDEX */
    TWSE = "TWSE",
    /** TAIWAN SE OTC INDEX */
    TAIOTC = "TAIOTC",
    /** TECDAX INDEX */
    TECDAX = "TECDAX",
    /** THAI SET INDEX */
    BNGKSE = "BNGKSE",
    /** TOPIX 100 INDEX */
    TPX100 = "TPX100",
    /** TOPIX 1000 INDEX */
    TS1000 = "TS1000",
    /** TOPIX 500 INDEX */
    JT500 = "JT500",
    /** TOPIX CORE 30 INDEX */
    TCOR30 = "TCOR30",
    /** TOPIX INDEX */
    TOPIX = "TOPIX",
    /** TOPIX LARGE 70 INDEX */
    TPXLRG = "TPXLRG",
    /** TOPIX MID 400 INDEX */
    TMID40 = "TMID40",
    /** TOPIX SMALL INDEX */
    TPXSMA = "TPXSMA",
    /** TSE GROWTH MARKET CORE INDEX */
    TSEGMC = "TSEGMC",
    /** TSE GROWTH MARKET INDEX */
    TSEGMI = "TSEGMI",
    /** TSE MOTHERS INDEX */
    TKMOTH = "TKMOTH",
    /** TSE PRIME MARKET COMPOSITE INDEX */
    TSEPMC = "TSEPMC",
    /** TSE PRIME MARKET INDEX */
    TSEPMI = "TSEPMI",
    /** TSE REIT INDEX */
    TSER = "TSER",
    /** TSE STANDARD MARKET INDEX */
    TSESMI = "TSESMI",
    /** TSE STANDARD MARKET TOP20 INDEX */
    TSES20 = "TSES20",
    /** VENEZUELA SE GENERAL INDEX */
    VENGEN = "VENGEN",
    /** WIENER BORSE INDEX */
    AVSE = "AVSE",
    /** WIG INDEX */
    PWWGI = "PWWGI",
    /** WIG20 INDEX */
    PWG20 = "PWG20",
    /** WIG30 INDEX */
    WIG30 = "WIG30",
    /** WORLD FEDERATION OF EXCHANGES INDEX */
    WFEXCH = "WFEXCH",
};
/**
* The enumerated values to specify the conditions that govern the adjustment to the number of units of the return swap.
*/
export enum NotionalAdjustmentEnum {
    
    /** The adjustments to the number of units are governed by an execution clause. */
    Execution = "Execution",
    /** The adjustments to the number of units are governed by a portfolio rebalancing clause. */
    PortfolioRebalancing = "PortfolioRebalancing",
    /** The adjustments to the number of units are not governed by any specific clause. */
    Standard = "Standard",
};
/**
* Specifies delivery methods for securities transactions. This coding-scheme defines the possible delivery methods for securities.
*/
export enum DeliveryMethodEnum {
    
    /** Indicates that a securities delivery must be made against payment in simultaneous transmissions and stipulate each other. */
    DeliveryVersusPayment = "DeliveryVersusPayment",
    /** Indicates that a securities delivery can be made without a simultaneous cash payment in exchange and not depending on if payment obligations are fulfilled or not and vice versa. */
    FreeOfPayment = "FreeOfPayment",
    /** Indicates that a securities delivery must be made in full before the payment for the securities; fulfillment of payment obligations depends on securities delivery obligations fulfillment. */
    PreDelivery = "PreDelivery",
    /** Indicates that a payment in full amount must be made before the securities delivery; fulfillment of securities delivery obligations depends on payment obligations fulfillment. */
    PrePayment = "PrePayment",
};
/**
* The qualification of the type of transfers and cash flows associated with contracts and their lifecycle events.
*/
export enum ScheduledTransferEnum {
    
    /** A cash flow corresponding to a corporate action event. */
    CorporateAction = "CorporateAction",
    /** A cash flow corresponding to the periodic accrued interests. */
    Coupon = "Coupon",
    /** A cashflow resulting from a credit event. */
    CreditEvent = "CreditEvent",
    /** A cash flow corresponding to the synthetic dividend of an equity underlier asset traded through a derivative instrument. */
    DividendReturn = "DividendReturn",
    /** A cash flow associated with an exercise lifecycle event. */
    Exercise = "Exercise",
    /** A cash flow corresponding to the return of the fixed interest rate portion of a derivative instrument that has different types of underlying assets, such as a total return swap. */
    FixedRateReturn = "FixedRateReturn",
    /** A cash flow corresponding to the return of the floating interest rate portion of a derivative instrument that has different types of underlying assets, such as a total return swap. */
    FloatingRateReturn = "FloatingRateReturn",
    /** A cash flow corresponding to the compensation for missing assets due to the rounding of digits in the original number of assets to be delivered as per payout calculation. */
    FractionalAmount = "FractionalAmount",
    /** A cash flow corresponding to the return of the interest rate portion of a derivative instrument that has different types of underlying assets, such as a total return swap. */
    InterestReturn = "InterestReturn",
    /** Net interest across payout components. Applicable to products such as interest rate swaps. */
    NetInterest = "NetInterest",
    /** A cash flow corresponding to a performance return.  The settlementOrigin attribute on the Transfer should point to the relevant Payout defining the performance calculation. */
    Performance = "Performance",
    /** An amount which corresponds to the notional of the contract for various business reasons. This could be associated to a cashflow (e.g. a principal payment) or a transfer (e.g. delivery of an asset). */
    Principal = "Principal",
};
/**
* The qualification of the type of ad-hoc transfers and cash flows that can be associated with contracts and their lifecycle events.
*/
export enum UnscheduledTransferEnum {
    
    /** A recall of securities by the lender who has lent them out. */
    Recall = "Recall",
    /** A return by the borrower to the lender of the full or partial quantity of securities on the contract. */
    Return = "Return",
    /** A transfer resulting from the assignment of a contract to a new counterparty. */
    Assignment = "Assignment",
    /** The brokerage commission. */
    BrokerageCommission = "BrokerageCommission",
    /** A transfer associated with an increase lifecycle event. */
    Increase = "Increase",
    /** The novation fee. */
    Novation = "Novation",
    /** A transfer associated with a partial termination lifecycle event. */
    PartialTermination = "PartialTermination",
    /** Denotes the amount payable by the buyer to the seller for an option. The premium is paid on the specified premium payment date or on each premium payment date if specified. */
    Premium = "Premium",
    /** A transfer associated with a renegotiation lifecycle event. */
    Renegotiation = "Renegotiation",
    /** A transfer associated with a termination lifecycle event. */
    Termination = "Termination",
    /** An upfront transfer associated to the swap to adjust for a difference between the swap price and the current market price. */
    Upfront = "Upfront",
    /** A transfer associated with a credit event. */
    CreditEvent = "CreditEvent",
    /** A transfer associated with a corporate action */
    CorporateAction = "CorporateAction",
};
/**
* The enumeration values to specify how the option is to be settled when exercised.
*/
export enum SettlementTypeEnum {
    
    /** The intrinsic value of the option will be delivered by way of a cash settlement amount determined, (i) by reference to the differential between the strike price and the settlement price; or (ii) in accordance with a bilateral agreement between the parties. */
    Cash = "Cash",
    /** The securities underlying the transaction will be delivered by (i) in the case of a call, the seller to the buyer, or (ii) in the case of a put, the buyer to the seller versus a settlement amount equivalent to the strike price per share. */
    Physical = "Physical",
    /** Allow Election of either Cash or Physical settlement. */
    Election = "Election",
    /** Allow use of either Cash or Physical settlement without prior Election. */
    CashOrPhysical = "CashOrPhysical",
};
/**
* The enumeration values to specify how the transfer will settle, e.g. DvP.
*/
export enum TransferSettlementEnum {
    
    /** Simultaneous transfer of two assets, typically securities, as a way to avoid settlement risk. */
    DeliveryVersusDelivery = "DeliveryVersusDelivery",
    /** Settlement in which the transfer of the asset and the cash settlement are simultaneous. */
    DeliveryVersusPayment = "DeliveryVersusPayment",
    /** Simultaneous transfer of cashflows. */
    PaymentVersusPayment = "PaymentVersusPayment",
    /** No central settlement. */
    NotCentralSettlement = "NotCentralSettlement",
};
/**
* The enumerated values to specify whether a trade is settling using standard settlement instructions as well as whether it is a candidate for settlement netting.
*/
export enum StandardSettlementStyleEnum {
    
    /** This trade will settle using standard predetermined funds settlement instructions. */
    Standard = "Standard",
    /** This trade is a candidate for settlement netting. */
    Net = "Net",
    /** This trade will settle using standard predetermined funds settlement instructions and is a candidate for settlement netting. */
    StandardAndNet = "StandardAndNet",
    /** These trades have been paired and are a candidate for settlement netting. */
    PairAndNet = "PairAndNet",
};
/**
* Defines the different cash settlement methods for a product where cash settlement is applicable.
*/
export enum CashSettlementMethodEnum {
    
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (a). */
    CashPriceMethod = "CashPriceMethod",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (b). */
    CashPriceAlternateMethod = "CashPriceAlternateMethod",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (c). */
    ParYieldCurveAdjustedMethod = "ParYieldCurveAdjustedMethod",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (d). */
    ZeroCouponYieldAdjustedMethod = "ZeroCouponYieldAdjustedMethod",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (e). */
    ParYieldCurveUnadjustedMethod = "ParYieldCurveUnadjustedMethod",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (f) (published in Supplement number 23). */
    CrossCurrencyMethod = "CrossCurrencyMethod",
    /** An ISDA defined cash settlement method (yield curve) used for the determination of the applicable cash settlement amount. The method is defined in the 2006 ISDA Definitions, Section 18.3. Cash Settlement Methods, paragraph (g) (published in Supplement number 28). The method is defined in the 2021 ISDA Definitions, section 18.2.6. */
    CollateralizedCashPriceMethod = "CollateralizedCashPriceMethod",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2021 ISDA Definitions, Section 18.2.1. */
    MidMarketIndicativeQuotations = "MidMarketIndicativeQuotations",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2021 ISDA Definitions, Section 18.2.2. */
    MidMarketIndicativeQuotationsAlternate = "MidMarketIndicativeQuotationsAlternate",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2021 ISDA Definitions, Section 18.2.3. */
    MidMarketCalculationAgentDetermination = "MidMarketCalculationAgentDetermination",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2021 ISDA Definitions, Section 18.2.4. */
    ReplacementValueFirmQuotations = "ReplacementValueFirmQuotations",
    /** An ISDA defined cash settlement method used for the determination of the applicable cash settlement amount. The method is defined in the 2021 ISDA Definitions, Section 18.2.5 */
    ReplacementValueCalculationAgentDetermination = "ReplacementValueCalculationAgentDetermination",
};
/**
* Defines the settlement centre for a securities transaction.
*/
export enum SettlementCentreEnum {
    
    /** Euroclear Bank */
    EuroclearBank = "EuroclearBank",
    /** ClearStream Banking Luxembourg */
    ClearstreamBankingLuxembourg = "ClearstreamBankingLuxembourg",
};
/**
* The enumerated values to specify the type of averaging used in an Asian option.
*/
export enum AveragingInOutEnum {
    
    /** The average price is used to derive the strike price. Also known as 'Asian strike' style option. */
    In = "In",
    /** The average price is used to derive the expiration price. Also known as 'Asian price' style option. */
    Out = "Out",
    /** The average price is used to derive both the strike and the expiration price. */
    Both = "Both",
};
/**
* The enumerated values to specify the type or strategy of the option.
*/
export enum OptionTypeEnum {
    
    /** A 'payer' option: If you buy a 'payer' option you have the right but not the obligation to enter into the underlying swap transaction as the 'fixed' rate/price payer and receive float. */
    Payer = "Payer",
    /** A 'receiver' option: If you buy a 'receiver' option you have the right but not the obligation to enter into the underlying swap transaction as the 'fixed' rate/price receiver and pay float. */
    Receiver = "Receiver",
    /** A straddle strategy, which involves the simultaneous buying of a put and a call of the same underlier, at the same strike and same expiration date */
    Straddle = "Straddle",
};
/**
* The enumerated values to specify the option exercise style. i.e., European, Bermuda or American.
*/
export enum OptionExerciseStyleEnum {
    
    /** Single Exercise */
    European = "European",
    /** Multiple specified exercise dates */
    Bermuda = "Bermuda",
    /** Continuous exercise over a range of dates */
    American = "American",
};
/**
* Identifies a party to the on-demand repo transaction that has a right to demand for termination of the Security Finance transaction.
*/
export enum CallingPartyEnum {
    
    /** Initial buyer to the repo transaction. */
    InitialBuyer = "InitialBuyer",
    /** Initial seller to the repo transaction. */
    InitialSeller = "InitialSeller",
    /** Either, Buyer or Seller to the repo transaction. */
    Either = "Either",
    /** As defined in Master Agreement. */
    AsDefinedInMasterAgreement = "AsDefinedInMasterAgreement",
};
/**
* Defines the principal party to the trade that has the right to exercise.
*/
export enum ExerciseNoticeGiverEnum {
    
    /** Specifies that only the option buyer has the right to exercise. */
    Buyer = "Buyer",
    /** Specifies that only the option seller has the right to exercise. */
    Seller = "Seller",
    /** Specifies that both the option buyer and option seller has the right to exercise. */
    Both = "Both",
    /** Specifies that the Master Agreement defines the principal party to the trade that has the right to exercise. */
    AsSpecifiedInMasterAgreement = "AsSpecifiedInMasterAgreement",
};
/**
* This indicator defines which type of assets (cash or securities) is specified to apply as margin to the repo transaction.
*/
export enum MarginTypeEnum {
    
    /** When the margin type is Cash, the margin factor is applied to the cash value of the transaction. */
    Cash = "Cash",
    /** When the margin type is Instrument, the margin factor is applied to the instrument value for the transaction. In the 'instrument' case, the haircut would be applied to the securities. */
    Instrument = "Instrument",
};
/**
* A duration code for a Repo (or Securities Lending) transaction. There are many business and market rules that are derived from the duration of the transaction.
*/
export enum RepoDurationEnum {
    
    /** Indicates that a contract is classified as overnight, meaning that there is one business day difference between the start and end date of the contract. Business rule: When the repo is overnight, the number of business days between the spot and forward value dates must be one. Forward leg must be specified. */
    Overnight = "Overnight",
    /** Indicates that a contract is a regular term contract, with a start date and an end date. Business rule: When the repo is 'Term', both spot and forward legs must be specified. */
    Term = "Term",
};
/**
* An enumerator to differentiate the different trade types used in securities finance and modelled on an AssetPayout.
*/
export enum AssetPayoutTradeTypeEnum {
    
    /** In the case of a repurchase transaction, an immediate and equal income payment (often call a manufactured payment) is made by the buyer to the seller. */
    Repo = "Repo",
    /** In the case of a buy/sell-back, there is no income payment between buyer and seller. Instead, the repurchase price to be paid on the repurchase date is reduced by the amount of the income payment on the collateral plus some extra interest to compensate the seller for the delay between the income payment date on the collateral and the repurchase date of the repo. */
    BuySellBack = "BuySellBack",
    /** Denotes this payout as being associated to the security being lent as part of a securities lending contract. */
    SecurityLending = "SecurityLending",
};
/**
* The time of day at which the equity option expires, for example the official closing time of the exchange.
*/
export enum ExpirationTimeTypeEnum {
    
    /** The official closing time of the exchange on the valuation date. */
    Close = "Close",
    /** The official opening time of the exchange on the valuation date. */
    Open = "Open",
    /** The time at which the official settlement price is determined. */
    OSP = "OSP",
    /** The time specified in the element equityExpirationTime or valuationTime (as appropriate) */
    SpecificTime = "SpecificTime",
    /** The time at which the official settlement price (following the auction by the exchange) is determined by the exchange. */
    XETRA = "XETRA",
    /** The official closing time of the derivatives exchange on which a derivative contract is listed on that security underlyer. */
    DerivativesClose = "DerivativesClose",
    /** The time is determined as provided in the relevant Master Confirmation. */
    AsSpecifiedInMasterConfirmation = "AsSpecifiedInMasterConfirmation",
};


/**
 * A class for defining a date that shall be subject to adjustment if it would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the date.
 */
export interface AdjustableDate {
    /** A date subject to adjustment. While in FpML this date is required, this cardinality constraint has been relaxed as part of the CDM in order to support the FRA representation, which effective and termination dates are specified in FpML as adjusted dates. */
    unadjustedDate?: date,
    /** The business day convention and financial business centers used for adjusting the date if it would otherwise fall on a day that is not a business date in the specified business centers. */
    dateAdjustments?: BusinessDayAdjustments,
    /** A pointer style reference to date adjustments defined elsewhere in the document. */
    dateAdjustmentsReference?: BusinessDayAdjustments,
    /** The date once the adjustment has been performed. (Note that this date may change if the business center holidays change). */
    adjustedDate?: date,
}


/**
 * A class for defining a series of dates that shall be subject to adjustment if they would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the dates.
 */
export interface AdjustableDates {
    /** A date subject to adjustment. */
    unadjustedDate?: date[],
    /** The business day convention and financial business centers used for adjusting the date if it would otherwise fall on a day that is not a business date in the specified business centers. */
    dateAdjustments?: BusinessDayAdjustments,
    /** The date(s) once the adjustment has been performed. (Note that this date may change if the business center holidays change). */
    adjustedDate?: date[],
}


/**
 * A class for defining a date that shall be subject to adjustment if it would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the date.
 */
export interface AdjustableOrAdjustedDate {
    /** A date subject to adjustment. */
    unadjustedDate?: date,
    /** The business day convention and financial business centers used for adjusting the date if it would otherwise fall on a day that is not a business date in the specified business centers. */
    dateAdjustments?: BusinessDayAdjustments,
    /** The date once the adjustment has been performed. (Note that this date may change if the business center holidays change). */
    adjustedDate?: date,
}


/**
 * This Rosetta class specifies the date as either an unadjusted, adjusted or relative date. It supplements the features of the AdjustableOrAdjustedDate to support the credit default swap option premium, which uses the relative date construct.
 */
export interface AdjustableOrAdjustedOrRelativeDate {
    /** A date subject to adjustment. */
    unadjustedDate?: date,
    /** The business day convention and financial business centers used for adjusting the date if it would otherwise fall on a day that is not a business date in the specified business centers. */
    dateAdjustments?: BusinessDayAdjustments,
    /** The date once the adjustment has been performed. (Note that this date may change if the business center holidays change). */
    adjustedDate?: date,
    /** A date specified as some offset to another date (the anchor date). */
    relativeDate?: RelativeDateOffset,
}


/**
 * A class giving the choice between defining a date as an explicit date together with applicable adjustments or as relative to some other (anchor) date.
 */
export interface AdjustableOrRelativeDate {
    /** A date that shall be subject to adjustment if it would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the date. */
    adjustableDate?: AdjustableDate,
    /** A date specified as some offset to another date (the anchor date). */
    relativeDate?: AdjustedRelativeDateOffset,
}


/**
 * A class giving the choice between defining a series of dates as an explicit list of dates together with applicable adjustments or as relative to some other series of (anchor) dates.
 */
export interface AdjustableOrRelativeDates {
    /** A series of dates that shall be subject to adjustment if they would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the date. */
    adjustableDates?: AdjustableDates,
    /** A series of dates specified as some offset to another series of dates (the anchor dates). */
    relativeDates?: RelativeDates,
}


/**
 * A type defining a date (referred to as the derived date) as a relative offset from another date (referred to as the anchor date) plus optional date adjustments.
 */
export interface AdjustedRelativeDateOffset extends RelativeDateOffset {
    /** The business day convention and financial business centers used for adjusting the relative date if it would otherwise fall on a day that is not a business date in the specified business centers. */
    relativeDateAdjustments?: BusinessDayAdjustments,
}


/**
 * A class for specifying the business day calendar location used in determining whether a day is a business day or not, either by specifying this business center by reference to an enumerated list that is maintained by the FpML standard, or by reference to such specification when it exists elsewhere as part of the instance document. This class corresponds to the FpML BusinessCentersOrReference.model.
 */
export interface BusinessCenters {
    /** A code identifying one or several business day calendar location(s). The set of business day calendar locations are specified by the business day calendar location enumeration which is maintained by the FpML standard. */
    businessCenter?: string[],
    commodityBusinessCalendar?: string,
    /** A reference to a financial business center location specified elsewhere in the instance document. */
    businessCentersReference?: BusinessCenters,
}


/**
 * A class for defining a time with respect to a business day calendar location. For example, 11:00:00 GBLO.
 */
export interface BusinessCenterTime {
    /** A time specified in hh:mm:ss format where the second component must be '00', e.g. 11am would be represented as 11:00:00. */
    hourMinuteTime: string,
    /** A code identifying a business day calendar location. A business day calendar location is drawn from the list identified by the business day calendar location enumeration. */
    businessCenter: string,
}


/**
 * A class defining a range of contiguous business days by defining an unadjusted first date, an unadjusted last date and a business day convention and business centers for adjusting the first and last dates if they would otherwise fall on a non business day in the specified business centers. The days between the first and last date must also be good business days in the specified centers to be counted in the range.
 */
export interface BusinessDateRange extends DateRange {
    /** The convention for adjusting a date if it would otherwise fall on a day that is not a business day, as specified by an ISDA convention (e.g. Following, Precedent). */
    businessDayConvention: string,
    /** The business center(s), specified either explicitly or by reference to those specified somewhere else in the instance document. */
    businessCenters?: BusinessCenters,
}


/**
 * A class defining the business day convention and financial business centers used for adjusting any relevant date if it would otherwise fall on a day that is not a business day in the specified business center.
 */
export interface BusinessDayAdjustments {
    /** The convention for adjusting a date if it would otherwise fall on a day that is not a business day. */
    businessDayConvention: string,
    /** The business center(s), specified either explicitly or by reference to those specified somewhere else in the instance document. */
    businessCenters?: BusinessCenters,
}


/**
 * A class defining a contiguous series of calendar dates. The date range is defined as all the dates between and including the start and the end date. The start date must fall on or before the end date.
 */
export interface DateRange {
    /** The first date of a date range. */
    startDate: date,
    /** The last date of a date range. */
    endDate: date,
}


/**
 * List of dates.
 */
export interface DateList {
    date: date[],
}


/**
 * List of dateTimes.
 */
export interface DateTimeList {
    /** The CDM specifies that the zoned date time is to be expressed in accordance with ISO 8601, either as UTC as an offset to UTC. */
    dateTime: string[],
}


/**
 * A class for defining a date frequency, e.g. one day, three months, through the combination of an integer value and a standardized period value that is specified as part of an enumeration.
 */
export interface Frequency {
    /** A time period multiplier, e.g. 1, 2, or 3. If the period value is T (Term) then period multiplier must contain the value 1. */
    periodMultiplier: number,
    /** A time period, e.g. a day, week, month, year or term of the stream. */
    period: string,
}


/**
 * A class defining an offset used in calculating a new date relative to a reference date, e.g. calendar days, business days, commodity Business days, etc.
 */
export interface Offset extends Period {
    /** In the case of an offset specified as a number of days, this element defines whether consideration is given as to whether a day is a good business day or not. If a day type of business days is specified then non-business days are ignored when calculating the offset. The financial business centers to use for determination of business days are implied by the context in which this element is used. This element must only be included when the offset is specified as a number of days. If the offset is zero days then the dayType element should not be included. */
    dayType?: string,
}


/**
 * A class to specify an offset either as a normalized [multiplier, period, dayType] or as a custom provision of type string.
 */
export interface CustomisableOffset {
    offset?: Offset,
    customProvision?: string,
}


/**
 * A class to define recurring periods or time offsets.
 */
export interface Period {
    /** A time period multiplier, e.g. 1, 2 or 3 etc. A negative value can be used when specifying an offset relative to another date, e.g. -2 days. */
    periodMultiplier: number,
    /** A time period, e.g. a day, week, month or year of the stream. If the periodMultiplier value is 0 (zero) then period must contain the value D (day). */
    period: string,
}


/**
 * A class describing a set of dates defined as relative to another set of dates.
 */
export interface RelativeDates extends RelativeDateOffset {
    /** The number of periods in the referenced date schedule that are between each date in the relative date schedule. Thus a skip of 2 would mean that dates are relative to every second date in the referenced schedule. If present this should have a value greater than 1. */
    periodSkip?: number,
    /** The first and last dates of a schedule. This can be used to restrict the range of values in a reference series of dates. */
    scheduleBounds?: DateRange,
}


/**
 * A class defining a date (referred to as the derived date) as a relative offset from another date (referred to as the anchor date). If the anchor date is itself an adjustable date then the offset is assumed to be calculated from the adjusted anchor date. A number of different scenarios can be supported, namely; 1) the derived date may simply be a number of calendar periods (days, weeks, months or years) preceding or following the anchor date; 2) the unadjusted derived date may be a number of calendar periods (days, weeks, months or years) preceding or following the anchor date with the resulting unadjusted derived date subject to adjustment in accordance with a specified business day convention, i.e. the derived date must fall on a good business day; 3) the derived date may be a number of business days preceding or following the anchor date. Note that the businessDayConvention specifies any required adjustment to the unadjusted derived date. A negative or positive value in the periodMultiplier indicates whether the unadjusted derived precedes or follows the anchor date. The businessDayConvention should contain a value NONE if the day type element contains a value of Business (since specifying a negative or positive business days offset would already guarantee that the derived date would fall on a good business day in the specified business centers).
 */
export interface RelativeDateOffset extends Offset {
    /** The convention for adjusting a date if it would otherwise fall on a day that is not a business day, as specified by an ISDA convention (e.g. Following, Precedent). */
    businessDayConvention: string,
    /** The business center(s), specified either explicitly or by reference to those specified somewhere else in the instance document. */
    businessCenters?: BusinessCenters,
    /** A pointer style reference to a set of financial business centers defined elsewhere in the document. This set of business centers is used to determine whether a particular day is a business day or not. */
    businessCentersReference?: BusinessCenters,
    /** Specifies the anchor as an href attribute. The href attribute value is a pointer style reference to the element or component elsewhere in the document where the anchor date is defined. */
    dateRelativeTo?: date,
    /** The date once the adjustment has been performed. (Note that this date may change if the business center holidays change). */
    adjustedDate?: date,
}


/**
 * The time alongside with the timezone location information. This class makes use of the FpML TimezoneLocation construct.
 */
export interface TimeZone {
    /** The observation time. */
    time: string,
    /** FpML specifies the timezoneLocationScheme by reference to the Time Zone Database (a.k.a. tz database) maintained by IANA, the Internet Assigned Numbers Authority. */
    location?: string,
}


/**
 * Represents a time definition that can be either an absolute (direct) time or a time calculated relative to another referenced time. Either one directTime or one relativeTime should be provided
 */
export interface DirectOrRelativeTime {
    /** An absolute time value expressed in a specific time zone. */
    directTime?: TimeZone,
    /** A time value calculated relative to another reference time. */
    relativeTime?: RelativeTime,
}


/**
 * Defines a time value calculated relative to a reference time. The final value is derived by applying any offset and rounding rules inherited from RelativeTimeOffset to the reference time.
 */
export interface RelativeTime extends RelativeTimeOffset {
    /** The reference time zone or base time from which the relative time is calculated. */
    timeRelativeTo?: TimeZone,
}


/**
 * Describes how a time is offset from a reference point, including the unit, magnitude, and rounding behavior.
 */
export interface RelativeTimeOffset {
    /** The unit of time for the offset (e.g., seconds, minutes, hours, days).  */
    offsetTimeUnit?: string,
    /** A time offset multiplier, e.g. 1, 2 or 3 etc. A negative value can be used when specifying a negative offset, e.g. -2 hours. */
    offsetMultiplier?: number,
    /** Optional rules that define how the calculated time should be rounded after the offset has been applied. */
    rounding?: TimeRounding,
}


/**
 * Specifies how a calculated time should be rounded, including both the direction of rounding and the unit to which the time is rounded. Rounding is applied after all offsets have been calculated.
 */
export interface TimeRounding {
    /** The direction in which rounding is performed (e.g., up, down, or to the nearest value). */
    roundingDirection: string,
    /** The time unit to which the value should be rounded (e.g., minute, hour, day). */
    roundToUnit?: string,
}


/**
 * Indicates The period range defined as either a lower and upper period bound, or both.
 */
export interface PeriodRange {
    /** Specifies the lower bound of a period range, e.g. greater than or equal to 5Y. */
    lowerBound?: PeriodBound,
    /** Specifies the upper bound of a period range, e.g. less than to 10Y. */
    upperBound?: PeriodBound,
}


/**
 * Indicator to specify if the period bound is defined as a period and whether the bound is inclusive.
 */
export interface PeriodBound {
    /** Specifies the period is to be used as the bound, e.g. 5Y. */
    period: Period,
    /** Specifies whether the period bound is inclusive, e.g. for a lower bound, false would indicate greater than, whereas true would indicate greater than or equal to. */
    inclusive: boolean,
}


/**
 * Class to representing a method for generating a series of dates.
 */
export interface AveragingSchedule {
    /** Date on which this period begins. */
    startDate: date,
    /** Date on which this period ends. */
    endDate: date,
    /** The frequency at which averaging period occurs with the regular part of the valuation schedule and their roll date convention. */
    averagingPeriodFrequency: CalculationPeriodFrequency,
}


/**
 * A class giving the choice between defining a series of dates as an explicit list of dates together with applicable adjustments or as relative to some other series of (anchor) dates, or as a calculation period schedule.
 */
export interface AdjustableRelativeOrPeriodicDates {
    /** A series of dates that shall be subject to adjustment if they would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the date. */
    adjustableDates?: AdjustableDates,
    /** A series of dates specified as some offset to another series of dates (the anchor dates). */
    relativeDates?: RelativeDates,
    /** A calculation period schedule. */
    periodicDates?: PeriodicDates,
}


/**
 * A class for specifying a calculation period schedule.
 */
export interface PeriodicDates {
    /** The start date of the calculation period. FpML specifies that for interest rate swaps this date must only be specified if it is not equal to the effective date. It is always specified in the case of equity swaps and credit default swaps with periodic payments. This date may be subject to adjustment in accordance with a business day convention. */
    startDate?: AdjustableOrRelativeDate,
    /** The end date of the calculation period. FpML specifies that for interest rate swaps this date must only be specified if it is not equal to the termination date. It is always specified in the case of equity swaps with periodic payments. This date may be subject to adjustment in accordance with a business day convention. */
    endDate?: AdjustableOrRelativeDate,
    /** The frequency at which calculation period end dates occur with the regular part of the calculation period schedule and their roll date convention. */
    periodFrequency?: CalculationPeriodFrequency,
    /** The specification of the business day convention and financial business centers used for adjusting any calculation period date if it would otherwise fall on a day that is not a business day in the specified business center. */
    periodDatesAdjustments?: BusinessDayAdjustments,
    /** Denotes the enumerated values to specify the day type classification used in counting the number of days between two dates. */
    dayType?: string,
}


/**
 * A class to specify the frequency at which calculation period end dates occur within the regular part of the calculation period schedule and their roll date convention.
 */
export interface CalculationPeriodFrequency extends Frequency {
    /** The roll convention specifies the period term as part of a periodic schedule, i.e. the calculation period end date within the regular part of the calculation period. The value could be a rule, e.g. IMM Settlement Dates, which is the 3rd Wednesday of the month, or it could be a specific day of the month, such as the first day of the applicable month. It is used in conjunction with a frequency and the regular period start date of a calculation period. */
    rollConvention: string,
    /** Indicates, when true, that that the first Calculation Period should run from the Effective Date to the end of the calendar period in which the Effective Date falls, e.g. Jan 15 - Jan 31 if the calculation periods are one month long and Effective Date is Jan 15. If false, the first Calculation Period should run from the Effective Date for one whole period, e.g. Jan 15 to Feb 14 if the calculation periods are one month long and Effective Date is Jan 15. Mostly used in Commmodity Swaps. */
    balanceOfFirstPeriod?: boolean,
}


/**
 * Represents the parameters for describing how often something (such as collateral interest) is to be calculated.
 */
export interface CalculationFrequency {
    /** Specifies the time period at which calculation is performed, e.g. 1 month. */
    period: Period,
    /** Specifies the month of the year if used. */
    monthOfYear?: string,
    /** Specifies the day of the month if used. */
    dayOfMonth?: string,
    /** Specifies the day of the week if used. */
    dayOfWeek?: string,
    /** Specifies the week of the month if used. */
    weekOfMonth?: string,
    /** Specifies how many days from the trigger event should the payment occur. */
    offsetDays: string,
    /** Specifies where is the time measured. */
    dateLocation: BusinessCenterTime,
    /** Specifies the business center for adjustment of calculation period. */
    businessCenter?: string[],
}


/**
 * Defines the unit to be used for price, quantity, or other purposes
 */
export interface UnitType {
    /** Provides an enumerated value for a capacity unit, generally used in the context of defining quantities for commodities. */
    capacityUnit?: string,
    /** Provides an enumerated values for a weather unit, generally used in the context of defining quantities for commodities. */
    weatherUnit?: string,
    /** Provides an enumerated value for financial units, generally used in the context of defining quantities for securities. */
    financialUnit?: string,
    /** Defines the currency to be used as a unit for a price, quantity, or other purpose. */
    currency?: string,
}


/**
 * Provides an abstract type to define a measure as a number associated to a unit. This type is abstract because all its attributes are optional. The types that extend it can specify further existence constraints.
 */
export interface MeasureBase {
    /** Specifies the value of the measure as a number. Optional because in a measure vector or schedule, this single value may be omitted. */
    value?: string,
    /** Qualifies the unit by which the amount is measured. Optional because a measure may be unit-less (e.g. when representing a ratio between amounts in the same unit). */
    unit?: UnitType,
}


/**
 * Defines a concrete measure as a number associated to a unit. It extends MeasureBase by requiring the value attribute to be present. A measure may be unit-less so the unit attribute is still optional.
 */
export interface Measure extends MeasureBase {
}


/**
 * A set of measures, all in the same unit, where the values are defined through a schedule of steps. The initial value may be defined either as part of the steps, or using the single amount attribute.
 */
export interface MeasureSchedule extends MeasureBase {
    /** A schedule of step date and value pairs. On each step date the associated step value becomes effective. The step dates are used to order the steps by ascending order. This attribute is optional so the data type may be used to define a schedule with a single value. */
    datedValue?: DatedValue[],
}


/**
 * Specifies a quantity schedule to be associated to a financial product to represent a trade amount. This data type extends MeasureSchedule with several unit or multiplier attributes that are used to define financial quantities. This data type is generically based on a schedule and can also be used to represent a quantity as a single value.
 */
export interface QuantitySchedule extends MeasureSchedule {
    /** Defines an optional number that the quantity should be multiplied by to derive a total quantity. This number is associated to a unit. For example in the case of the Coal (API2) CIF ARA (ARGUS-McCloskey) Futures Contract on the CME, where the unit would be contracts, the multiplier value would 1,000 and the mulitiplier unit would be 1,000 MT (Metric Tons). */
    multiplier?: Measure,
    /** Defines the frequency to be used when defining a quantity. For example a quantity may be specified as a number of barrels of oil per day, which needs multiplying by the number of days in the relevant period to get the total quantity as a number of barrels. */
    frequency?: Frequency,
}


/**
 * Specifies a quantity as a single value to be associated to a financial product, for example a transfer amount resulting from a trade. This data type extends QuantitySchedule and requires that only the single amount value exists.
 */
export interface Quantity extends QuantitySchedule {
}


/**
 * Specifies a quantity as a non-negative number, which condition is enforced through a data rule that only applies to the extending class.
 */
export interface NonNegativeQuantity extends Quantity {
}


/**
 * A class defining a step date and non-negative step value pair. This step definitions are used to define varying rate or amount schedules, e.g. a notional amortisation or a step-up coupon schedule.
 */
export interface NonNegativeStep {
    /** The date on which the associated stepValue becomes effective. This day may be subject to adjustment in accordance with a business day convention. */
    stepDate: date,
    /** The non-negative rate or amount which becomes effective on the associated stepDate. A rate of 5% would be represented as 0.05. */
    stepValue: string,
}



export interface NonNegativeQuantitySchedule extends QuantitySchedule {
}


/**
 * Defines rounding rules and precision to be used in the rounding of a number.
 */
export interface Rounding {
    /** Specifies the rounding rounding rule as up, down, or nearest. */
    roundingDirection: string,
    /** Specifies the rounding precision in terms of a number of decimal places when the number is evaluated in decimal form (not percentage), e.g. 0.09876543 rounded to the nearest 5 decimal places is  0.0987654. */
    precision?: number,
}


/**
 * A class defining a schedule of rates or amounts in terms of an initial value and then a series of step date and value pairs. On each step date the rate or amount changes to the new step value. The series of step date and value pairs are optional. If not specified, this implies that the initial value remains unchanged over time.
 */
export interface Schedule {
    /** The initial rate or amount, as the case may be. An initial rate of 5% would be represented as 0.05. */
    value: string,
    /** The schedule of step date and value pairs. On each step date the associated step value becomes effective. A list of steps may be ordered in the document by ascending step date. An FpML document containing an unordered list of steps is still regarded as a conformant document. */
    datedValue?: DatedValue[],
}


/**
 * Defines a date and value pair. This definition is used for varying rate or amount schedules, e.g. a notional amortisation or a step-up coupon schedule.
 */
export interface DatedValue {
    /** The date on which the associated step value becomes effective. This day may be subject to adjustment in accordance with a business day convention. */
    date: date,
    /** The rate of amount which becomes effective on the associated step date. A rate of 5% would be represented as 0.05. */
    value: string,
}


/**
 * The number range defined as either a lower and upper number bound, or both.
 */
export interface NumberRange {
    /** The lower bound of a number range, e.g. greater than or equal to 5. */
    lowerBound?: NumberBound,
    /** The upper bound of a number range, e.g. less than 10. */
    upperBound?: NumberBound,
}


/**
 * The number bound is defined as a number and whether the bound is inclusive.
 */
export interface NumberBound {
    /** The number to be used as the bound, e.g. 5. */
    number: string,
    /** Whether the number bound is inclusive, e.g. for a lower bound, false would indicate greater than, whereas true would indicate greater than or equal to. */
    inclusive: boolean,
}


/**
 * The money range defined as either a lower and upper money bound, or both.
 */
export interface MoneyRange {
    /** The lower bound of a money range, e.g. greater than or equal to 1,000 USD. */
    lowerBound?: MoneyBound,
    /** The upper bound of a money range, e.g. less than 10,000 USD. */
    upperBound?: MoneyBound,
}


/**
 * The money bound is defined as a money amount and whether the bound is inclusive.
 */
export interface MoneyBound {
    /** The money amount to be used as the bound, e.g. 1,000 USD. */
    money: Money,
    /** Whether the money amount bound is inclusive, e.g. for a lower bound, false would indicate greater than, whereas true would indicate greater than or equal to. */
    inclusive: boolean,
}


/**
 * Defines the ways in which multiple values can be aggregated into a single value.
 */
export interface AveragingCalculationMethod {
    /** Identifies whether the average values will be weighted or unweighted. */
    isWeighted: boolean,
    /** Identifies which of the Pythagorean means is being used to compute an average value. */
    calculationMethod: string,
}


/**
 * The base data type to specify common attributes for all Assets.
 */
export interface AssetBase {
    /** Asset Identifiers are used to uniquely identify an Asset, using a specified Asset Identifier Type. */
    identifier: AssetIdentifier[],
    /** Defines the taxonomy of an object by combining a taxonomy source (i.e. the rules to classify the object) and a value (i.e. the output of those rules on the object. */
    taxonomy?: Taxonomy[],
    /** Defines whether the Asset is listed on a public exchange. */
    isExchangeListed?: boolean,
    /** If the Asset is listed, defines the public exchange of the listing. */
    exchange?: LegalEntity,
    /** Provides the related Exchanges, if applicable. */
    relatedExchange?: LegalEntity[],
    /** Specifies the type of asset. */
    assetType: string,
}


/**
 * The unique identifier for an Asset, specified using an Asset Identifier Type enumerator.
 */
export interface AssetIdentifier {
    /** The identifier value. */
    identifier: string,
    /** Defines the symbology source of the Asset Identifier, eg CUSIP, ISIN, etc. */
    identifierType: string,
}


/**
 * Defines the common attributes for all Instrument data types.
 */
export interface InstrumentBase extends AssetBase {
}


/**
 * Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the ProductIdentifier instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface ProductIdentifier {
    /** Provides an identifier associated with a specific product.  The identifier is unique within the public source specified in the source attribute. */
    identifier: string,
    /** Defines the source of the identifier. */
    source: string,
}


/**
 * Defines a taxonomy value as either a simple string or a more granular expression with class names and values for each class.
 */
export interface TaxonomyValue {
    /** Specifies the taxonomy value as a simple string, which may be associated to an external scheme. */
    name?: string,
    /** Specifies the taxonomy value as a set of class names and values for each class. */
    classification?: TaxonomyClassification[],
}


/**
 * Defines the taxonomy of an object by combining a taxonomy source (i.e. the rules to classify the object) and a value (i.e. the output of those rules on the object).
 */
export interface Taxonomy {
    /** The source of the taxonomy that defines the rules for classifying the object. The taxonomy source is taken from a enumerated list of taxonomy names. Optional as the taxonomy source may not be provided. */
    source?: string,
    /** The value according to that taxonomy. Optional as it may not be possible to classify the object in that taxonomy. */
    value?: TaxonomyValue,
}


/**
 * Specifies the product taxonomy, which is composed of a taxonomy value and a taxonomy source.
 */
export interface ProductTaxonomy extends Taxonomy {
    /** Classifies the most important risk class of the trade. */
    primaryAssetClass?: string,
    /**  Classifies additional risk classes of the trade, if any. */
    secondaryAssetClass?: string,
    /** Derived from the product payout features using a CDM product qualification function that determines the product type based on the product payout features. */
    productQualifier?: string,
}


/**
 * An Asset that consists solely of a monetary holding in a currency. The currency of the Cash asset is held in the string Identifier (from AssetBase) and the AssetIdTypeEnum must be set to define that a CurrencyCode is set.  The function SetCashCurrency can be used to create (or update) a Cash object and the function GetCashCurrency can be used to retrieve the currency of a Cash object.
 */
export interface Cash extends AssetBase {
}


/**
 * Identifies a specific commodity by referencing a product identifier or by a product definition.
 */
export interface Commodity extends AssetBase {
    /** Specifies the commodity underlier in the event that no ISDA Commodity Reference Benchmark exists. */
    commodityProductDefinition?: CommodityProductDefinition,
    /** Describes the required quote type of the underlying price that will be observed. Example values include 'Bid, 'Ask', 'Settlement' (for a futures contract) and 'WeightedAverage' (for some published prices and indices). */
    priceQuoteType: string,
    /** Specifies the parameters for identifying the relevant contract date when the commodity reference price is a futures contract. */
    deliveryDateReference?: DeliveryDateParameters,
    /** Provides additional information about the commodity underlier. */
    description?: string,
}


/**
 * Specifies the commodity underlier in the event that no ISDA Commodity Reference Price exists.
 */
export interface CommodityProductDefinition {
    /** Specifies the type of commodity. */
    referenceFramework: CommodityReferenceFramework,
    /** Specifies a publication that provides the commodity price, including, where applicable the details of where in the publication the price is published.  Applicable when the commodity reference price is not a futures contract */
    priceSource?: PriceSource,
    /** Specifies the publication where the commodity prices can be found. */
    commodityInfoPublisher?: string,
    /**  Identifies the exchange from which the reference price should be sourced, using the scheme at the following url: http://www.fpml.org/coding-scheme/external/exchange-id-MIC-1-0 */
    exchangeId: string,
}


/**
 * Specifies a specific date or the parameters for identifying the relevant contract date when the commodity reference price is a futures contract.
 */
export interface DeliveryDateParameters {
}


/**
 * Specifies the type of commodity.
 */
export interface CommodityReferenceFramework {
    /** Identifies the commodity more specifically. Where possible, this should follow the naming convention used in the 2005 ISDA Commodity Definitions SubAnnex A, including the subCommodity and additional qualifiers, but should be limited to 256 characters or less. */
    commodityName: string,
    /** Provides an enumerated value for a capacity unit, generally used in the context of defining quantities for commodities. */
    capacityUnit?: string,
    /** Provides an enumerated values for a weather unit, generally used in the context of defining quantities for commodities. */
    weatherUnit?: string,
    /** Defines the currency in which the commodity is priced. */
    currency: string,
}


/**
 * An Asset that exists only in digital form, eg Bitcoin or Ethereum, that is not backed by other Assets; excludes the digital representation of other Assets, eg coins or Tokenised assets.
 */
export interface DigitalAsset extends AssetBase {
}


/**
 * A securitized derivative on another asset.
 */
export interface ListedDerivative extends InstrumentBase {
    /** Also called contract month or delivery month. However, it's not always a month. It is usually expressed using a code, e.g. Z23 would be the Dec 2023 contract, (Z = December). For crude oil, the corresponding contract might be called CLZ23. Optional as this can be uniquely identified in the identifier. */
    deliveryTerm?: string,
    /** The type of option, ie Put or Call. Left empty if it is a Future. */
    optionType?: string,
    /** Specifies the strike of the option. */
    strike?: string,
}


/**
 * Identifies a loan by referencing an asset identifier and through an optional set of attributes.
 */
export interface Loan extends InstrumentBase {
    /** Specifies the borrower. There can be more than one borrower. It is meant to be used in the event that there is no Bloomberg Id or the Secured List isn't applicable. */
    borrower?: LegalEntity[],
    /** Specifies the seniority level of the lien. */
    lien?: string,
    /** Specifies the type of loan facility (letter of credit, revolving, ...). */
    facilityType?: string,
    /** Specifies the credit agreement date is the closing date (the date where the agreement has been signed) for the loans in the credit agreement. Funding of the facilities occurs on (or sometimes a little after) the Credit Agreement date. This underlier attribute is used to help identify which of the company's outstanding loans are being referenced by knowing to which credit agreement it belongs. ISDA Standards Terms Supplement term: Date of Original Credit Agreement. */
    creditAgreementDate?: date,
    /** Denotes the loan tranche that is subject to the derivative transaction. It will typically be referenced as the Bloomberg tranche number. ISDA Standards Terms Supplement term: Bloomberg Tranche Number. */
    tranche?: string,
}


/**
 * Specifies the collateral taxonomy, which is composed of a taxonomy value and a taxonomy source.
 */
export interface CollateralTaxonomy {
    /** Specifies the taxonomy value. */
    taxonomyValue: CollateralTaxonomyValue,
    /** Specifies the taxonomy source. */
    taxonomySource: string,
}


/**
 * Specifies the collateral taxonomy value, either as a specified enumeration or as a string.
 */
export interface CollateralTaxonomyValue {
    /** Identifies European Union Eligible Collateral Assets classification categories based on EMIR Uncleared Margin Rules. Eligible Collateral asset classes for both initial margin (IM) and variation margin (VM) posted and collected between specified entities. Please note: EMIR regulation will detail which eligible collateral assets classes apply to each type of entity pairing (counterparty) and which apply to posting of IM and VM */
    eu_EMIR_EligibleCollateral?: string,
    /** Identifies United Kingdom Eligible Collateral Assets classification categories based on UK Onshored EMIR Uncleared Margin Rules Eligible Collateral asset classes for both initial margin (IM) and variation margin (VM) posted and collected between specified entities. Please note: UK EMIR regulation will detail which eligible collateral assets classes apply to each type of entity pairing (counterparty) and which apply to posting of IM and VM. */
    uk_EMIR_EligibleCollateral?: string,
    /** Identifies US Eligible Collateral Assets classification categories based on Uncleared Margin Rules published by the CFTC and the US Prudential Regulator. Note: While the same basic categories exist in the CFTC and US Prudential Regulators’ margin rules, the precise definitions or application of those rules could differ between the two rules. */
    us_CFTC_PR_EligibleCollateral?: string,
    /** Identifies the taxonomy value when not specified as an enumeration. */
    nonEnumeratedTaxonomyValue?: string[],
}


/**
 * Identifies a security by referencing an identifier and by specifying the sector.
 */
export interface Security extends InstrumentBase {
    /** Identifies the type of security using an enumerated list. */
    securityType: string,
    /** Identifies the type of debt and selected debt economics. */
    debtType?: DebtType,
    /** Identifies the type of equity. */
    equityType?: EquityType,
    /** Identifies the type of fund. */
    fundType?: string,
}



export interface EquityType {
    /** Identifies the type of equity. */
    equityType?: string,
    depositaryReceipt?: string,
}


/**
 * Specifies the type of debt security.
 */
export interface DebtType {
    /** Specifies selected financial terms of a debt security. */
    debtEconomics?: DebtEconomics[],
}


/**
 * Specifies selected economics of a debt security.
 */
export interface DebtEconomics {
    /** Specifies the order of repayment in the event of a sale or bankruptcy of the issuer or a related party (eg guarantor). */
    seniority?: string,
    /** Specifies the general rule for periodic interest rate payment. */
    interest?: string,
    /** Specifies the general rule for repayment of principal. */
    principal?: string,
    /** Specifies the type of secured debt. */
    secured?: SecuredDebt,
    /** Specifies the general rule for redemption of a debt security. */
    redemption?: DebtRedemption,
}


/**
 * Specifies the redemption features and whether the redemption right is exercised at the discretion of the issuer or the holder.
 */
export interface DebtRedemption {
    /** Specifies the contractual redemption or conversion features associated with a debt security. */
    redemptionType?: string,
    /** Specifies the embedded option feature of a debt security. */
    putCall?: string,
    /** Specifies whether the debt security is redeemed at the election of the Issuer/Holder. */
    party?: string,
}



export interface SecuredDebt {
    /** Specifies the type of secured debt product. */
    securedType?: string,
    /** Specifies the type of Asset Backed Security. */
    assetBacked?: string,
    /** Specifies the type of Collateralized Obligation. */
    collateralizedObligation?: string,
    /** Specifies the type of Covered Bond. */
    coveredBond?: string,
    /** Specifies the type of property when the security is linked to a property asset. */
    propertyType?: string,
}


/**
 * Specifies the origin of entity guaranteeing the collateral.
 */
export interface CollateralGuarantorType {
    /** Specifies the origin of entity guaranteeing the collateral. */
    guarantorType: string,
}


/**
 * Represents a class to allow specification of the type of entity issuing the collateral.
 */
export interface CollateralIssuerType {
    /** Specifies the origin of entity issuing the collateral. */
    issuerType: string,
    /** Specifies debt issued by international organisations and multilateral banks. */
    supraNationalType?: string,
    /** Specifies debt issues by institutions or bodies, typically constituted by statute, with a function mandated by the government and subject to government supervision inclusive of profit- and non-profit making bodies. Includes the US Agencies and GSEs and the EU concept of public sector entities. Excluding any entities which are also Regional Government. */
    quasiGovernmentType?: QuasiGovernmentIssuerType,
    /** Specifies Regional government, local authority or municipal. */
    regionalGovernmentType?: RegionalGovernmentIssuerType,
    /** Specifies a subsidiary company that is formed to undertake a specific business purpose of acquisition and financing of specific assets on a potentially limited recourse basis dependent of how it is designed. E.g. asset backed securities, including securitisations. */
    specialPurposeVehicleType?: SpecialPurposeVehicleIssuerType,
}


/**
 * Represents a class to allow specification of different types of Quasi Government collateral.
 */
export interface QuasiGovernmentIssuerType {
    /** True if sovereign entity (e.g. not separate legal personality from sovereign) or false if non-sovereign entity (e.g. separate legal personality from sovereign). */
    sovereignEntity: boolean,
    /** Applies to non-sovereign entity (e.g. separate legal personality from sovereign).  True if entity has recourse to sovereign (e.g. debt guaranteed by government).  False if entity does not have recourse to sovereign. */
    sovereignRecourse?: boolean,
}


/**
 * Represents a class to allow specification of different type of Regional government collateral.
 */
export interface RegionalGovernmentIssuerType {
    /** Applies to regional governments, local authorities or municipals.  True if entity has recourse to sovereign (e.g. debt guaranteed by government).  False if entity does not have recourse to sovereign. */
    sovereignRecourse: boolean,
}


/**
 * Represents a class to allow specification of different types of special purpose vehicle (SPV) collateral.
 */
export interface SpecialPurposeVehicleIssuerType {
    /** Indicates tranched or untranched credit risk. */
    creditRisk?: string,
}



export interface TaxonomyClassification {
    /** The name defined by the classification system for a specific attribute in the taxonomy */
    className?: string,
    /** The value set by the taxonomy that is specific to the className attribute. */
    value: string,
    /** A description of the class. */
    description?: string,
    /** In the case of multi-layered hierarchical classification systems such as commodity classification, the layer the value and className occupy in the classification hierarchy, where 1 represents the top-layer. */
    ordinal?: number,
}


/**
 * Represents a class to allow specification of the asset product type.
 */
export interface AssetType {
    /** Represents a filter based on the type of collateral asset. */
    assetType: string,
    /** Represents a filter based on the type of security. */
    securityType?: string,
    /** Represents a filter based on the type of bond. */
    debtType?: DebtType,
    /** Represents a filter based on the type of equity. */
    equityType?: EquityType,
    /** Represents a filter based on the type of fund. */
    fundType?: string,
    /** Specifies the eligible asset type when not enumerated. */
    otherAssetType?: string[],
}


/**
 * Specifies a publication that provides the commodity price, including, where applicable, the details of where in the publication the price is published.
 */
export interface PriceSource {
    /** Defines a publication in which the price can be found. (e.g Gas Daily, Platts Bloomberg. Commodity publishers can be found at this URL:  http://www.fpml.org/coding-scheme/commodity-information-provider> */
    pricePublisher: string,
    /** Specifies the location of the price which may be a specific page, electornic screen name, or a code (e.g. a RIC code) where the price can be found. */
    priceSourceLocation?: string,
    /** Specifies the heading or field name for the price  on a given page or screen, where applicable. */
    priceSourceHeading?: string,
    /** Specifies the time at which the price should be observed. */
    priceSourceTime?: string,
}


/**
 * An Asset is defined as something that can be owned and transferred in the financial markets. As a choice data type, one and only one of the attributes must be used.
 */
export interface Asset {
}


/**
 * A type of Asset that is issued by one party to one or more others.
 */
export interface Instrument {
}


/**
 * A class to specify the ISDA 2003 Term: Not Domestic Currency.
 */
export interface NotDomesticCurrency {
    /** Indicates whether the Not Domestic Currency provision is applicable. */
    applicable: boolean,
    /** An explicit specification of the domestic currency. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
}


/**
 * A class to specify the underlying obligations of the reference entity on which protection is purchased or sold through the Credit Default Swap.
 */
export interface Obligations {
    /** Used in both obligations and deliverable obligations to represent a class or type of securities which apply. ISDA 2003 Term: Obligation Category/Deliverable Obligation Category. */
    category: string,
    /** An obligation and deliverable obligation characteristic. An obligation that ranks at least equal with the most senior Reference Obligation in priority of payment or, if no Reference Obligation is specified in the related Confirmation, the obligations of the Reference Entity that are senior. ISDA 2003 Term: Not Subordinated. */
    notSubordinated?: boolean,
    /** An obligation and deliverable obligation characteristic. The currency or currencies in which an obligation or deliverable obligation must be payable. ISDA 2003 Term: Specified Currency. */
    specifiedCurrency?: SpecifiedCurrency,
    /** An obligation and deliverable obligation characteristic. Any obligation that is not primarily (majority) owed to a Sovereign or Supranational Organisation. ISDA 2003 Term: Not Sovereign Lender. */
    notSovereignLender?: boolean,
    /** An obligation and deliverable obligation characteristic. Any obligation that is payable in any currency other than the domestic currency. Domestic currency is either the currency so specified or, if no currency is specified, the currency of (a) the reference entity, if the reference entity is a sovereign, or (b) the jurisdiction in which the relevant reference entity is organised, if the reference entity is not a sovereign. ISDA 2003 Term: Not Domestic Currency. */
    notDomesticCurrency?: NotDomesticCurrency,
    /** An obligation and deliverable obligation characteristic. If the reference entity is a Sovereign, this means any obligation that is not subject to the laws of the reference entity. If the reference entity is not a sovereign, this means any obligation that is not subject to the laws of the jurisdiction of the reference entity. ISDA 2003 Term: Not Domestic Law. */
    notDomesticLaw?: boolean,
    /** An obligation and deliverable obligation characteristic. Indicates whether or not the obligation is quoted, listed or ordinarily purchased and sold on an exchange. ISDA 2003 Term: Listed. */
    listed?: boolean,
    /** An obligation and deliverable obligation characteristic. Any obligation other than an obligation that was intended to be offered for sale primarily in the domestic market of the relevant Reference Entity. This specifies that the obligation must be an internationally recognised bond. ISDA 2003 Term: Not Domestic Issuance. */
    notDomesticIssuance?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published additional provisions for U.S. Municipal as Reference Entity. ISDA 2003 Term: Full Faith and Credit Obligation Liability. */
    fullFaithAndCreditObLiability?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published additional provisions for U.S. Municipal as Reference Entity. ISDA 2003 Term: General Fund Obligation Liability. */
    generalFundObligationLiability?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published additional provisions for U.S. Municipal as Reference Entity. ISDA 2003 Term: Revenue Obligation Liability. */
    revenueObligationLiability?: boolean,
    /** OTE: Only allowed as an obligation characteristic under ISDA Credit 1999. In essence Not Contingent means the repayment of principal cannot be dependent on a formula/index, i.e. to prevent the risk of being delivered an instrument that may never pay any element of principal, and to ensure that the obligation is interest bearing (on a regular schedule). ISDA 2003 Term: Not Contingent. */
    notContingent?: boolean,
    /** A free format string to specify any excluded obligations or deliverable obligations, as the case may be, of the reference entity or excluded types of obligations or deliverable obligations. ISDA 2003 Term: Excluded Obligations/Excluded Deliverable Obligations. */
    excluded?: string,
    /** This element is used to specify any other obligations of a reference entity in both obligations and deliverable obligations. The obligations can be specified free-form. ISDA 2003 Term: Other Obligations of a Reference Entity. */
    othReferenceEntityObligations?: string,
    /** Applies to Loan CDS, to indicate what lien level is appropriate for a deliverable obligation. Applies to European Loan CDS, to indicate the Ranking of the obligation. Example: a 2nd lien Loan CDS would imply that the deliverable obligations are 1st or 2nd lien loans. */
    designatedPriority?: string,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published Standard Terms Supplement for use with CDS Transactions on Leveraged Loans. ISDA 2003 Term: Cash Settlement Only. */
    cashSettlementOnly?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published Standard Terms Supplement for use with CDS Transactions on Leveraged Loans. ISDA 2003 Term: Delivery of Commitments. */
    deliveryOfCommitments?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published Standard Terms Supplement for use with CDS Transactions on Leveraged Loans. ISDA 2003 Term: Continuity. */
    continuity?: boolean,
}



export interface SpecifiedCurrency {
    /** Indicates whether the specified currency provision is applicable. */
    applicable: boolean,
    /** The currency in which the specified currency is denominated. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
}


/**
 * A type hold a list of codes for business data validation
 */
export interface CodeList {
    /** Identification information about the code list */
    identification: CodeListIdentification,
    /** Code values */
    codes?: CodeValue[],
}


/**
 * A type to identify the version of a codelist
 */
export interface CodeListIdentification {
    /** A description  of the code list, e.g. A list of codes identifying business centers */
    description?: string,
    /** The most recent date the code list was published, e.g. 2021-10-05 */
    publicationDate?: string,
    /** A short name (without whitespace) for the code list, e.g. businessCenterScheme */
    shortName: string,
    /** The the most recent version of the codelist, eg. 9-1 */
    version: string,
    /** The name of the code list, e.g. business-center */
    codeShortId?: string,
    /** Canonical URI which uniquely identifies all versions (collectively) of the referenced code list. Must be an absolute URI. Must not be used as a de facto location URI. */
    canonicalUri: string,
    /** Canonical URI which uniquely identifies a specific version of the referenced code list. Must be an absolute URI. Must not be used as a de facto location URI. */
    canonicalVersionUri?: string,
    /** Suggested retrieval location for this version, in genericode format. An application must signal an error to the user if a document retrieved using a LocationUri is not in genericode format. */
    locationUri?: string,
}


/**
 * A type to hold the value of the code in a codelist
 */
export interface CodeValue {
    /** The value of the code, e.g. USNY */
    value: string,
    /** A description of the meaning of the code, e.g. New York City, USA */
    description?: string,
    /** Where the code was defined,e.g. ISDA, FpML, Bloomberg */
    source?: string,
    /** When the code became/becomes effective */
    effectiveDate?: date,
    /** When the code ceased to be valid for new uses */
    deprecatedDate?: date,
}


/**
 * A class to specify the identifier value and its associated version.
 */
export interface AssignedIdentifier {
    /** The identifier value. */
    identifier: string,
    /** The identifier version, which is specified as an integer and is meant to be incremented each time the transaction terms (whether contract or event) change. This version is made option to support the use case where the identifier is referenced without the version. The constraint that a contract and a lifecycle event need to have an associated version is enforced through data rules. */
    version?: number,
}


/**
 * A class to specify a generic identifier, applicable to CDM artefacts such as executions, contracts, lifecycle events and legal documents. An issuer can be associated with the actual identifier value as a way to properly qualify it.
 */
export interface Identifier {
    /** The identifier issuer, when specified by reference to a party specified as part of the transaction. */
    issuerReference?: Party,
    /** The identifier issuer, when specified explicitly alongside the identifier value (instead of being specified by reference to a party). */
    issuer?: string,
    /** The identifier value. This level of indirection between the issuer and the identifier and its version provides the ability to associate multiple identifiers to one issuer, consistently with the FpML PartyTradeIdentifier. */
    assignedIdentifier: AssignedIdentifier[],
}


/**
 * Attaches an identifier to a collection of objects, when those objects themselves can each be represented by an identifier. One use case is the representation of package transactions, where each component is a separate trade with its own identifier, and those trades are linked together as a package with its own identifier. The data type has been named generically rather than referring to 'packages' as it may have a number of other uses.
 */
export interface IdentifiedList {
    /** The identifier for the list. In the case of a package transaction, this would be the package identifier. This attribute is mandatory to allow the list itself to be identified. */
    listId: Identifier,
    /** Identifiers for each component of the list. Since the data type is used to link multiple identified objects together, at least 2 components are required in the list. Creating an identified list with only 1 identified component has been deemed unnecessary, because it would just create a redundant identifier. */
    componentId: Identifier[],
    /** The price of the package. */
    price?: Price,
}


/**
 * Specifies a location identifier. An issuer and an identifier type can be associated with the actual identifier value as a way to properly qualify it.
 */
export interface LocationIdentifier extends Identifier {
    /** Specifies the nature of a location identifier. */
    locationIdentifierType?: string,
}


/**
 * Defines a counterparty enumerated value, e.g. Party1 or Party2, with an associated party reference. The product is agnostic to the actual parties to the transaction, with the party references abstracted away from the product definition and replaced by the CounterpartyEnum (e.g. values Party1 or Party2). The CounterpartyEnum can then be positioned in the product (e.g. to specify which counterparty is the payer, receiver etc) and this Counterparty type, which is positioned outside of the product definition, allows the CounterpartyEnum to be associated with an actual party reference.
 */
export interface Counterparty {
    /** Specifies the CounterpartyEnum, e.g. either Party1 or Party2, that is associated to the partyReference. */
    role: string,
    /** Specifies the party that is associated to the counterparty. */
    partyReference: Party,
}


/**
 * Defines an ancillary role enumerated value with an associated party reference. The product is agnostic to the actual parties involved in the transaction, with the party references abstracted away from the product definition and replaced by the AncillaryRoleEnum. The AncillaryRoleEnum can then be positioned in the product and this AncillaryParty type, which is positioned outside of the product definition, allows the AncillaryRoleEnum to be associated with an actual party reference.
 */
export interface AncillaryParty {
    /** Specifies the AncillaryRoleEnum that is associated to the party reference. An ancillary party is any involved party that is not one of the two principal parties to the transaction. */
    role: string,
    /** Specifies the party, or parties, associated to the ancillary role. */
    partyReference: Party[],
    /** Optionally specifies the counterparty that the ancillary party is acting on behalf of. */
    onBehalfOf?: string,
}


/**
 * This class corresponds to the FpML BuyerSeller.model construct.
 */
export interface BuyerSeller {
    /** Buyer party that can be resolved as one of the two principal parties to the transaction. The party that buys this instrument, i.e. pays for this instrument and receives the rights defined by it. ISDA 2002 Equity Definitions section 1.18: `Buyer` means the party specified as such in the related Confirmation. | ISDA 2006 Definitions article 12.1 (b)(i) relating to a Swaption: 'Buyer' means the party that will, on each Premium Payment Date, pay to Seller the Premium | ISDA 2006 Definitions article 12.1 (b)(ii) relating to Swap Transactions with applicable Early Termination: the party specified as such in the related Confirmation, or the Exercising Party if neither party is specified | ISDA 2006 Definitions article 12.1 (b)(iii) relating to any other Option Transaction: the party specified as such in the related Confirmation. | ISDA 2014 Credit Definition article 1.4: `Buyer` means the Fixed Rate Payer. */
    buyer: string,
    /** Seller party that can be resolved as one of the two principal parties to the transaction. The party that sells ('writes') this instrument, i.e. that grants the rights defined by this instrument and in return receives a payment for it. ISDA 2002 Equity Definitions section 1.19: `Seller` means the party specified as such in the related Confirmation. | ISDA 2006 Definitions article 12.1 (a)(i) relating to a Swaption: 'Seller' means the party the party specified as such or as writer in the related Confirmation | ISDA 2006 Definitions article 12.1 (a)(ii) relating to Swap Transactions with applicable Early Termination: the party specified as such or as writer in the related Confirmation or, if neither party is specified as such, the Non-exercising Party | ISDA 2006 Definitions article 12.1 (a)(iii) relating to any other Option Transaction: the party specified as such in the related Confirmation. | ISDA 2014 Credit Definition article 1.4: `Seller` means the Floating Rate Payer. */
    seller: string,
}


/**
 * Specifies the parties responsible for making and receiving payments defined by this structure.
 */
export interface PayerReceiver {
    /** Specifies the counterparty responsible for making the payments defined by this structure.  The party is one of the two principal parties to the transaction. */
    payer: string,
    /** Specifies the party that receives the payments corresponding to this structure.  The party is one of the two counterparties to the transaction. */
    receiver: string,
}


/**
 * Specifies the parties responsible for making and receiving payments defined by this structure.
 */
export interface PartyReferencePayerReceiver {
    /** The party responsible for making the payments defined by this structure. */
    payerPartyReference: Party,
    /** A reference to the account responsible for making the payments defined by this structure. */
    payerAccountReference?: Account,
    /** The party that receives the payments corresponding to this structure. */
    receiverPartyReference: Party,
    /** A reference to the account that receives the payments corresponding to this structure. */
    receiverAccountReference?: Account,
}


/**
 * A class to describe an institution (party) identified by means of a coding scheme and an optional name.
 */
export interface ReferenceBank {
    /** An institution (party) identifier, e.g. a bank identifier code (BIC). FpML specifies a referenceBankIdScheme. */
    referenceBankId: string,
    /** The name of the institution (party). A free format string. FpML does not define usage rules for the element. */
    referenceBankName?: string,
}


/**
 * A class defining the list of reference institutions polled for relevant rates or prices when determining the cash settlement amount for a product where cash settlement is applicable.
 */
export interface ReferenceBanks {
    /** An institution (party) identified by means of a coding scheme and an optional name. */
    referenceBank: ReferenceBank[],
}



export interface RelatedParty {
    /** Reference to a party. */
    partyReference: Party,
    /** Reference to an account. */
    accountReference?: Account,
    /** The category of the relationship. The related party performs the role specified in this field for the base party. For example, if the role is ,Guarantor, the related party acts as a guarantor for the base party. */
    role: string,
}


/**
 * A class to specify an account as an account number alongside, optionally. an account name, an account type, an account beneficiary and a servicing party.
 */
export interface Account {
    /** A reference to the party to which the account refers to. */
    partyReference?: Party,
    /** The account number. */
    accountNumber: string,
    /** The name by which the account is known. */
    accountName?: string,
    /** The type of account, e.g. client, house. */
    accountType?: string,
    /** A reference to the party beneficiary of the account. */
    accountBeneficiary?: Party,
    /** The reference to the legal entity that services the account, i.e. in the books of which the account is held. */
    servicingParty?: Party,
}


/**
 * A class to specify a post or street address.
 */
export interface Address {
    /** The set of street and building number information that identifies a postal address within a city. */
    street: string[],
    /** The city component of the postal address. */
    city?: string,
    /** A country subdivision used in postal addresses in some countries. For example, US states, Canadian provinces, Swiss cantons, ... */
    state?: string,
    /** The ISO 3166 standard code for the country within which the postal address is located. */
    country?: string,
    /** The code, required for computerized mail sorting systems, that is allocated to a physical address by a national postal authority. */
    postalCode?: string,
}


/**
 * A class to specify an organizational unit.
 */
export interface BusinessUnit {
    /** A name used to describe the organizational unit */
    name: string,
    /** An identifier used to uniquely identify the organizational unit */
    identifier?: Identifier,
    /** The contact information for such business unit, when different from the contact information associated with the party. */
    contactInformation?: ContactInformation,
}


/**
 * A class to specify contact information associated with a party: telephone, postal/street address, email and web page.
 */
export interface ContactInformation {
    /** The telephone number. */
    telephone?: TelephoneNumber[],
    /** The street/postal address. */
    address?: Address[],
    /** The email address. */
    email?: string[],
    /** The web page. This attribute is not specified as part of the FpML ContactInformation complex type. */
    webPage?: string[],
}


/**
 * A class to specify a legal entity, with a required name and an optional entity identifier (such as the LEI).
 */
export interface LegalEntity {
    /** The legal entity name. */
    name: string,
    /** A legal entity identifier with a source (e.g. RED entity code). */
    entityIdentifier?: EntityIdentifier[],
}


/**
 * A class to represent the attributes that are specific to a natural person.
 */
export interface NaturalPerson {
    /** The identifier associated with a person, e.g. the internal identification code. */
    personId?: PersonIdentifier[],
    /** An honorific title, such as Mr., Ms., Dr. etc. */
    honorific?: string,
    /** The natural person's first name. It is optional in FpML. */
    firstName?: string,
    /** The natural person's middle name(s). If a middle name is provided then an initial should be absent. */
    middleName?: string[],
    /** The natural person's middle initial(s). If a middle initial is provided then a name should be absent. */
    initial?: string[],
    /** The natural person's surname. */
    surname?: string,
    /** Name suffix, such as Jr., III, etc. */
    suffix?: string,
    /** The natural person's date of birth. */
    dateOfBirth?: date,
    /** The contact information for such person, when different from the contact information associated with the party. */
    contactInformation?: ContactInformation,
}


/**
 * A class to specify the role(s) that natural person(s) may have in relation to the contract.
 */
export interface NaturalPersonRole {
    /** A reference to the natural person to whom the role refers to. */
    personReference: NaturalPerson,
    /** FpML specifies a person role that is distinct from the party role. */
    role?: string,
}


/**
 * A class to specify a party, without a qualification as to whether this party is a legal entity or a natural person, although the model provides the ability to associate a person (or set of persons) to a party, which use case would imply that such party would be a legal entity (even if not formally specified as such). 
 */
export interface Party {
    /** The identifier associated with a party, e.g. the 20 digits LEI code. */
    partyId: PartyIdentifier[],
    /** The party name. */
    name?: string,
    /** Optional organization unit information used to describe the organization units (e.g. trading desks) involved in a transaction or business process, incl. the contact information (when relevant). */
    businessUnit?: BusinessUnit[],
    /** The person(s) who might be associated with the party as part of the execution, contract or legal document. */
    person?: NaturalPerson[],
    /** The role of the person(s)  */
    personRole?: NaturalPersonRole[],
    /** The account that might be associated with the party. At most one account can be specified, as it is expected that this information is used in the context of a contract or legal document where only one account per party can be associated with such object. */
    account?: Account,
    /** The postal/street address, telephone number, email address and/or web page. If the contact information is specific to the associated business unit(s) or person (s), it should be associated with those. */
    contactInformation?: ContactInformation,
}


/**
 * Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the PartyIdentifier instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface PartyIdentifier {
    /** Provides an identifier associated with a party. The identifier is unique within the public source specified in the source attribute. */
    identifier: string,
    /** Defines the source of the identifier. */
    identifierType?: string,
}


/**
 * Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the EntityIdentifier instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface EntityIdentifier {
    /** Provides an identifier associated with a legal entity. */
    identifier: string,
    /** Defines the source of the identifier. */
    identifierType?: string,
}


/**
 * Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the PersonIdentifier instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface PersonIdentifier {
    /** Provides an identifier associated with a person. The identifier is unique within the public source specified in the source attribute. */
    identifier: string,
    /** Defines the source of the identifier. */
    identifierType?: string,
    /** The ISO 3166 standard code for the country issuing the identifier. */
    country?: string,
}


/**
 * A class to specify the role(s) that party(ies) may have in relation to the execution, contract or other legal agreement.
 */
export interface PartyRole {
    /** A reference to the party to which the role refers to. */
    partyReference: Party,
    /** The party role. */
    role: string,
    /** A reference to the party that has ownership of this party role information. FpML specifies that For shared trade information, this attribute will reference the originator of the data (for example, an execution facility or clearing house). */
    ownershipPartyReference?: Party,
}


/**
 * A class to specify a telephone number as a type of phone number (e.g. work, personal, ...) alongside with the actual number.
 */
export interface TelephoneNumber {
    /** The type of telephone number, e.g. work, mobile. */
    telephoneNumberType?: string,
    /** The actual telephone number. */
    number: string,
}


/**
 * Holds an identifier for an ancillary entity, either identified directly via its ancillary role or directly as a legal entity.
 */
export interface AncillaryEntity {
    /** Identifies a party via its ancillary role on a transaction (e.g. CCP or DCO through which the trade should be cleared.) */
    ancillaryParty?: string,
    legalEntity?: LegalEntity,
}


/**
 * Specifies instructions to create a fully formed contract, with optional legal agreements.
 */
export interface ContractFormationInstruction {
    /** Optional legal agreements associated to the contract being formed, for instance a master agreement. */
    legalAgreement?: LegalAgreement[],
}


/**
 * Instruction to a function that will be used to perform a business event
 */
export interface Instruction {
    /** Specifies the primitive instructions that will be used to call primitive event functions. */
    primitiveInstruction?: PrimitiveInstruction,
    /** Specifies the trade state that will be acted on by the primitive event functions. */
    before?: TradeState,
}


/**
 * A Primitive Instruction describes the inputs required to pass into the corresponding PrimitiveEvent function.
 */
export interface PrimitiveInstruction {
    /** Specifies instructions describing an contract formation primitive event. */
    contractFormation?: ContractFormationInstruction,
    /** Specifies instructions describing an execution primitive event. */
    execution?: ExecutionInstruction,
    /** Specifies instructions describing an exercise primitive event. */
    exercise?: ExerciseInstruction,
    /** Specifies instructions describing a party change primitive event. */
    partyChange?: PartyChangeInstruction,
    /** Specifies instructions describing an quantity change primitive event. */
    quantityChange?: QuantityChangeInstruction,
    /** Specifies instructions describing a reset event. */
    reset?: ResetInstruction,
    /** Specifies instructions to split a trade into multiple branches. */
    split?: SplitInstruction,
    /** Specifies instructions describing a terms change primitive event. */
    termsChange?: TermsChangeInstruction,
    /** Specifies instructions describing a transfer primitive event. */
    transfer?: TransferInstruction,
    /** Specifies inputs needed to process a Index Transition business event. */
    indexTransition?: IndexTransitionInstruction,
    /** Specifies inputs needed to process a Stock Split business event. */
    stockSplit?: StockSplitInstruction,
    /** Specifies inputs needed to process an observation. */
    observation?: ObservationInstruction,
    /** Specifies inputs needed to process an update of a valuation. */
    valuation?: ValuationInstruction,
}


/**
 * A business event represents a life cycle event of a trade. The combination of the state changes results in a qualifiable life cycle event. An example of a Business Event is a PartialTermination which is a defined by a quantity change primitive event.
 */
export interface BusinessEvent extends EventInstruction {
    /** The CDM event qualifier, which corresponds to the outcome of the isEvent qualification logic which qualifies the lifecycle event as a function of its features (e.g. PartialTermination, ClearingSubmission, Novation, ...). */
    eventQualifier?: string,
    /** Specifies the after trade state(s) created. */
    after?: TradeState[],
}


/**
 * A business event represents a life cycle event of a position. The combination of the state changes results in a qualifiable life cycle event.
 */
export interface CounterpartyPositionBusinessEvent {
    /** The intent attribute is meant to be specified when the event qualification cannot be programmatically inferred from the event features. As a result it is only associated with those primitives that can give way to such ambiguity, the quantityChange being one of those. */
    intent: string,
    /** The intent of a corporate action on the position. */
    corporateActionIntent?: string,
    /** Specifies the date on which the event is taking place. This date is equal to the trade date in the case of a simple execution.  However it can be different from the trade date, for example in the case of a partial termination. */
    eventDate?: date,
    /** The date on which the event contractually takes effect, when different from the event date. */
    effectiveDate?: date,
    /** Specifies the package information in case the business event represents several trades executed as a package (hence this attribute is optional). The package information is only instantiated once at the business event level to preserve referential integrity, whereas individual trades make reference to it to identify that they are part of a package. */
    packageInformation?: IdentifiedList,
    /** Specifies the after position state(s) created. */
    after?: CounterpartyPositionState[],
}


/**
 * Specifies inputs needed to process an observation.
 */
export interface ObservationInstruction {
    /** Contains all information related to an observation. */
    observationEvent: ObservationEvent,
}


/**
 * Specifies inputs needed to process a valuation.
 */
export interface ValuationInstruction {
    /** Contains all information related to a valuation. */
    valuation: Valuation[],
    /** Specifies whether the previous valuation tracks in the valuation history are removed (True) or kept (False). */
    replace: boolean,
}


/**
 * Specifies instructions for execution of a transaction, consisting of a product, price, quantity, parties, trade identifier, execution details, and settlement terms.
 */
export interface ExecutionInstruction {
    /** Defines the financial product to be executed and contract formed. */
    product: NonTransferableProduct,
    /** Defines the prices (e.g. spread, equity price, FX rate), quantities (e.g. currency amount, no. shares) and settlement terms (e.g. initial fee, broker fee, up-front cds payment or option premium settlement) associated with the constituents of the transacted product. */
    priceQuantity: PriceQuantity[],
    /** Maps two defined parties to counterparty enums for the transacted product. */
    counterparty: Counterparty[],
    /** Maps any ancillary parties, e.g. parties involved in the transaction that are not one of the two principal parties. */
    ancillaryParty?: AncillaryParty[],
    /** Defines all parties to that execution, including agents and brokers. */
    parties: Party[],
    /** Defines the role(s) that party(ies) may have in relation to the execution. */
    partyRoles?: PartyRole[],
    /** Specifies the type and venue of execution, e.g. via voice, or electronically. */
    executionDetails: ExecutionDetails,
    /** Denotes the trade/execution date. */
    tradeDate: date,
    /** Denotes the trade time and timezone as agreed by the parties to the trade. */
    tradeTime?: TimeZone,
    /** Denotes one or more identifiers associated with the transaction. */
    tradeIdentifier: TradeIdentifier[],
    /** Detail the collateral requirement anticipated with the transaction. */
    collateral?: Collateral,
    /** Lot Identifier associated with the transaction. */
    lotIdentifier?: Identifier,
}


/**
 * Specifies the information required to communicate the choices made by the exercising party, in a financial product endowing the party with at least one option.
 */
export interface ExerciseInstruction {
    /** Contains instructions for exercising the option including a quantity change, and optionally a transfer. */
    exerciseQuantity: PrimitiveInstruction,
    /** Specifies the Option Payout being exercised on the trade. */
    exerciseOption?: OptionPayout,
    /** Specifies the date on which an option contained within the financial product would be exercised. The date may be omitted if the contractual product allows for only a single date of exercise (European exercise). */
    exerciseDate?: AdjustableOrAdjustedDate,
    /** Specifies the time at which an option contained within the financial product woulld be exercised. The time may be omitted if the contractual product allows for only a single time of exercise (European exercise).  */
    exerciseTime?: BusinessCenterTime,
    /** Specifies the trade identifier to apply to the replacement trade for physical exercise. */
    replacementTradeIdentifier?: TradeIdentifier[],
}


/**
 * Defines the information needed to create a Reset Business Event. 
 */
export interface ResetInstruction {
    payout: Payout[],
    /** Specifies the 'Rate Record Day' for a Fallback rate.  Fallback rate fixing processes typically set the fixing rate in arrears, i.e., the Fallback Rate corresponding to a Rate Record Date is set at the end of the interest accural period.  When this applies, Reset->resetDate occurs at the end of the interest period, and the Reset->rateRecordDate occurs near the start of the interest period.  The Reset->rateRecordDate and Reset->observations->observationIdentifier->observationDate will differ if a Fallback rate is unavailable on the Rate Record Date, and the latest previous available rate is used as the observation. */
    rateRecordDate?: date,
    /** Specifies the date on which the reset is occuring. */
    resetDate: date,
}


/**
 * Defines the tradeState or payout on which to create a Transfer along with all necessary resets.
 */
export interface CalculateTransferInstruction {
    tradeState: TradeState,
    payout: Payout,
    resets?: Reset[],
    payerReceiver?: PayerReceiver,
    /** Specifies quantity amount returned if not the full amount from the TradeState, e.g. partial return */
    quantity?: Quantity,
    date?: date,
}


/**
 * Defines the payout on which to create a Transfer along with all necessary resets.
 */
export interface TransferInstruction {
    /** Specifies the terms and state of a transfers. */
    transferState?: TransferState[],
}


/**
 * Instructions required to create a Quantity Change Primitive Event, which can be either an increase, a decrease or a replacement. An increase adds a new trade lot to the original trade, whereas a decrease subtracts from an existing trade lot's quantity. A replacement updates the quantity of an existing trade lot to the new value.
 */
export interface QuantityChangeInstruction {
    /** Quantity by which the trade is being increased, decreased or replaced, and the price at which such quantity change is agreed. The quantity change should always be specified as a positive number, with the direction (increase/decrease/replacement) being specified by the direction enumeration. A fee can also be associated to the quantity change by specifying a Price component of type CashPrice, including the corresponding settlement date and direction. */
    change: PriceQuantity[],
    /** Direction of the quantity change specified as either an increase, decrease or replacement. */
    direction: string,
    /** Identifier for the new lot (in case of increase) or for the existing lot to be changed(in case of decrease or replacement). This optional attribute is mandatory in case of a decrease or replacement if the initial trade state contains multiple trade lots. */
    lotIdentifier?: Identifier[],
}


/**
 * Defines the information needed to create a Index Transition Business Event.
 */
export interface IndexTransitionInstruction {
    /** Specifies both new floating rate index and spread adjustment for each leg to be updated.  The spread adjustment accounts for the difference between the old floating rate index relative to the new one. This spread amount is added to the existing spread to determine the new spread, which is applied from the specified effective date forward. In the case of the IBOR Fallback Rate Adjustments, the adjustment spread (also known as the Fallback Adjustment) accounts for two distinctions: i) the fact that the replacement Risk-Free Rate is an overnight rate while IBORs have term structures (e.g., 1, 3, 6-month LIBOR); and (ii) the historical spread differential between IBORs and their term equivalent Overnight Risk-Free Rate compounded rates. */
    priceQuantity: PriceQuantity[],
    /** Specifies the effective date of the index transition event. This is first date on which the floating rate calculation will use the new floating rate index and adjusted spread in the floating rate calculation. */
    effectiveDate: date,
    /** Specifies the cash transfer that can optionally be tied to an index transition event. */
    cashTransfer?: Transfer,
}


/**
 * Specifies instructions for terms change consisting of the new transaction terms, and the renegotiation fee.
 */
export interface TermsChangeInstruction {
    /** product to be changed */
    product?: NonTransferableProduct,
    /** ancillary party to be changed */
    ancillaryParty?: AncillaryParty[],
    adjustment?: string,
}


/**
 * Specifies instructions for a split, consisting of a breakdown of instructions to be applied to each branch of the split. This instruction can be used to duplicate a trade, as in a clearing scenario, or to split a trade into smaller quantities (in which case each breakdown instruction needs to include a quantity change), as in an allocation.
 */
export interface SplitInstruction {
    /** Each split breakdown specifies the set of primitive instructions to be applied to a single branch of that split. N split breakdowns result in N output trades, which include the original trade. Instructions for how to handle the original trade (e.g. if it must be closed) must be specified in one of the breakdowns. */
    breakdown: PrimitiveInstruction[],
}


/**
 * Specifies instruction to change the party on a trade. This primitive instruction is used in a number of scenarios including: clearing, allocation and novation. The instrution must include a trade identifier, because a change of party effectively results in a different trade.
 */
export interface PartyChangeInstruction {
    /** The new counterparty who is stepping into the trade. The stepping out counterparty is inferred based on the counterparty role that is being updated. */
    counterparty: Counterparty,
    /** Specifies an ancillary party to be added onto the new transaction, e.g. the original executing party in an allocation. */
    ancillaryParty?: AncillaryParty,
    /** Specifies an additional party roles to be added on to the new transaction. */
    partyRole?: PartyRole,
    /** The identifier to be assigned to the new trade post change of party. */
    tradeId: TradeIdentifier[],
}


/**
 * Defines the fundamental financial information that can be changed by a Primitive Event and by extension any business or life-cycle event. Each TradeState specifies where a Trade is in its life-cycle. TradeState is a root type and as such, can be created independently to any other CDM data type, but can also be used as part of the CDM Event Model.
 */
export interface TradeState {
    /** Represents the Trade that has been effected by a business or life-cycle event. */
    trade: Trade,
    /** Represents the State of the Trade through its life-cycle. */
    state?: State,
    /** Represents the updated Trade attributes which can change as the result of a reset event. Only the changed values are captured, leaving the remaining data attributes empty. See Create_Reset function for further details on how TradeState is used in the Reset event. The TradeState data type is used to maintain backwards compatibility with the current Reset mechanism. */
    resetHistory?: Reset[],
    /** Represents the updated Trade attributes which can change as the result of a transfer event. */
    transferHistory?: TransferState[],
    /** Represents the observed events related to a particular product or process, such as credit events or corporate actions. */
    observationHistory?: ObservationEvent[],
    valuationHistory?: Valuation[],
}


/**
 * Defines the fundamental financial information that can be changed by a Primitive Event and by extension any business or life-cycle event. Each PositionState specifies where a Position is in its life-cycle. PositionState is a root type and as such, can be created independently to any other CDM data type, but can also be used as part of the CDM Event Model.
 */
export interface CounterpartyPositionState {
    /** Represents the Position that has been effected by a business or life-cycle event. */
    counterpartyPosition: CounterpartyPosition,
    /** Represents the State of the Position through its life-cycle. */
    state?: State,
    /** Represents the observed events related to a particular product or process, such as credit events or corporate actions. */
    observationHistory?: ObservationEvent[],
    valuationHistory?: Valuation[],
}


/**
 * Specifies the necessary information to create any observation event.
 */
export interface ObservationEvent {
    /** Specifies the necessary information to create a credit event. */
    creditEvent?: CreditEvent,
    /** Specifies the necessary information to create a corporate action. */
    corporateAction?: CorporateAction,
}


/**
 * Defines the reset value or fixing value produced in cashflow calculations, during the life-cycle of a financial instrument. The reset process defined in Create_Reset function joins product definition details with observations to compute the reset value.
 */
export interface Reset {
    /** Specifies the reset or fixing value. The fixing value could be a cash price, interest rate, or other value. */
    resetValue: Price,
    /** Specifies the date on which the reset occurred. */
    resetDate: date,
    /** Specifies the 'Rate Record Day' for a Fallback rate.  Fallback rate fixing processes typically set the fixing rate in arrears, i.e., the Fallback Rate corresponding to a Rate Record Date is set at the end of the interest accural period.  When this applies, Reset->resetDate occurs at the end of the interest period, and the Reset->rateRecordDate occurs near the start of the interest period.  The Reset->rateRecordDate and Reset->observations->observationIdentifier->observationDate will differ if a Fallback rate is unavailable on the Rate Record Date, and the latest previous available rate is used as the observation. */
    rateRecordDate?: date,
    /** Represents an audit of the observations used to produce the reset value. If multiple observations were necessary to produce the reset value, the aggregation method should be defined on the payout. */
    observations: Observation[],
    /** Identifies the aggregation method to use in the case where multiple observations are used to compute the reset value and the method is not defined in a payout. */
    averagingMethodology?: AveragingCalculation,
}


/**
 * Defines the state of a trade at a point in the Trade's life cycle. Trades have many state dimensions, all of which are represented here. For example, states useful for position keeping are represented alongside those needed for regulatory reporting.
 */
export interface State {
    /** Represents the qualification of what led to the trade's closure alongside the dates on which this closure took effect. */
    closedState?: ClosedState,
    /** Identifies the state of the position, to distinguish if just executed, formed, already settled, closed, etc. */
    positionState?: string,
}


/**
 * Defines the fundamental financial information associated with a Transfer event. Each TransferState specifies where a Transfer is in its life-cycle. TransferState is a root type and as such, can be created independently to any other CDM data type, but can also be used as part of the CDM Event Model.
 */
export interface TransferState {
    /** Represents the Transfer that has been effected by a business or life-cycle event. */
    transfer: Transfer,
    /** Represents the State of the Transfer through its life-cycle. */
    transferStatus?: string,
}


/**
 * An ad-hoc transfer instigated by a party to the contract.
 */
export interface UnscheduledTransfer extends TransferBase {
    /** Specifies the type of unscheduled transfer. */
    transferType?: string,
}


/**
 * A scheduled transfer built into the contract.
 */
export interface ScheduledTransfer extends TransferBase {
    /** Specifies the type of scheduled transfer. */
    transferType: string,
    corporateActionTransferType?: string,
    /** Represents the origin to the transfer as a reference for lineage purposes, whether it originated from trade level settlement terms or from payment terms on an economic payout. */
    payoutReference?: Payout,
}


/**
 * Defines the movement of an Asset (eg cash, securities or commodities) between two parties on a date.
 */
export interface TransferBase extends AssetFlowBase {
    /** Represents a unique reference to the transfer. */
    identifier?: Identifier[],
    /** Represents the parties to the transfer and their role. */
    payerReceiver: PartyReferencePayerReceiver,
}


/**
 * Defines a trade identifier as a special case of the generic identifier type, that also includes the trade identifier class.
 */
export interface TradeIdentifier extends Identifier {
    /** The enumerated classification of the identifier. Optional as a trade identifier may be party-specific, in which case it may not correspond to any established classification. */
    identifierType?: string,
}


/**
 * Defines a position identifier as a special case of the generic identifier type, that also includes the position identifier class.
 */
export interface PositionIdentifier extends Identifier {
    /** The enumerated classification of the identifier. Optional as a position identifier may be party-specific, in which case it may not correspond to any established classification. */
    identifierType?: string,
}


/**
 * Defines the value of an investment, asset, or security
 */
export interface Valuation {
    /** Current value of the outstanding contract */
    amount: Money,
    /** Date and time of the last valuation marked to market, provided by the central counterparty (CCP) or calculated using the current or last available market price of the inputs. */
    timestamp: string,
    /** Method used for the valuation of the transaction by the valuation party. */
    method?: string,
    /** Source of the valuation of the transaction by the valuation party. */
    source?: string,
    /**  Defines the scope of the valuation, what it applies to e.g. the collateral or the trade */
    scope: string,
    /** The ratio of the change in the price of a derivative transaction to the change in the price of the underlying. This field is applicable only to options and swaptions. */
    delta?: string,
    /** Denotes when the valuation was sourced during a business day. */
    valuationTiming?: string,
    /** Denotes the price used to compute the valuation. */
    priceComponent?: Price,
}


/**
 * Defines the output of a financial transaction between parties - a Business Event. A Trade impacts the financial position (i.e. the balance sheet) of involved parties.
 */
export interface Trade extends TradableProduct {
    /** Represents the identifier(s) that uniquely identify a trade for an identity issuer. A trade can include multiple identifiers, for example a trade that is reportable to both the CFTC and ESMA, and then has an associated USI (Unique Swap Identifier) UTI (Unique Trade Identifier). */
    tradeIdentifier: TradeIdentifier[],
    /** Specifies the date which the trade was agreed. */
    tradeDate: date,
    /** Denotes the trade time and timezone as agreed by the parties to the trade. */
    tradeTime?: TimeZone,
    /** Represents the parties to the trade. The cardinality is optional to address the case where the trade is defined within a BusinessEvent data type, in which case the party is specified in BusinessEvent. */
    party?: Party[],
    /** Represents the role each specified party takes in the trade. further to the principal roles, payer and receiver. */
    partyRole?: PartyRole[],
    /** Represents information specific to trades that arose from executions. */
    executionDetails?: ExecutionDetails,
    /** Represents information specific to trades involving contractual products. */
    contractDetails?: ContractDetails,
    /** Specifies the date on which a trade is cleared (novated) through a central counterparty clearing service. */
    clearedDate?: date,
    /** Represents the collateral obligations of a party. */
    collateral?: Collateral,
    /** Represents a party's granular account information, which may be used in subsequent internal processing. */
    account?: Account[],
}


/**
 * Defines specific attributes that relate to trade executions.
 */
export interface ExecutionDetails {
    /** Identifies the type of execution, e.g. via voice, electronically... */
    executionType: string,
    /** Represents the venue on which a trade was executed. */
    executionVenue?: LegalEntity,
    /** A reference to the package linking the trade with other trades, in case the trade was executed as part of a package (hence this attribute is optional). */
    packageReference?: IdentifiedList,
}


/**
 * Defines specific attributes that relate to contractual details of trades.
 */
export interface ContractDetails {
    /** Represents the legal document(s) that governs a trade and associated contractual product terms, either as a reference to such documents when specified as part of the CDM, or through identification of some of the key terms of those documents, such as the type of document, the document identifier, the publisher, the document vintage and the agreement date. */
    documentation?: LegalAgreement[],
    /** Represents the law governing the trade and associated contractual product terms. */
    governingLaw?: string,
}


/**
 * A class to provide lineage information across lifecycle events through a pointer or set of pointers into the event(s), contract(s) and, possibly, payout components that the event is dependent on or relates to. As an example, if an contractFormation event is corrected, the correction event will have a lineage into the initial event, which takes the form of a globalKey into that initial contract formation event. Two referencing mechanisms are provided as part of the CDM: either the globalKey, which corresponds to the hash value of the CDM class which is referred to, or a reference qualifier which is meant to provide support for the ingestion of xml documents with id/href mechanisms. The CDM recommends the use of the globalKey and provides a default implementation which is accessible in the generated code through org.isda.cdm.globalKey.GlobalKeyHashCalculator. If implementers want to use an alternative hashing mechanism, the API in which they need to plug it is com.rosetta.model.lib.HashFunction.
 */
export interface Lineage {
    tradeReference?: Trade[],
    /** The reference to the instantiation of an Event object, either through a globalKey or an xml-derived id/href mechanism. The definition associated to the Lineage class provides more details with respect to those referencing approaches, their expected usage and available implementation. */
    eventReference?: WorkflowStep[],
    /** The reference to the previous state of a Portfolio, in a chain of Events leading up to a build of that Portfolio as the holding of Product(s) in specific Quantity(ies). As part of the PortfolioState object, a pointer to the previous PortfolioState is provided through a Lineage object, together with pointer(s) to the Event or set of Events leading up to the current (new) state. */
    portfolioStateReference?: PortfolioState[],
}


/**
 * Data required to perform a stock split business event.
 */
export interface StockSplitInstruction {
    /** The number that denotes the cumulative quantity of post-split shares issued to shareholders versus the quantity of pre-split shares previously issued to shareholders.  This number will be multiplied by existing shares in an equity derivative contract or other positions to determine the post-split number of shares.  With regard to any reference to price, the pre-split reference price will be divided by this number to determine the post-split reference price. */
    adjustmentRatio: string,
    /** The effective date of the stock split, also known as the ex-date. This is the date on which the additional shares are paid to the shareholders, or in the case of a reverse stock split, the number shares held by each shareholder is proportionally reduced.  Equity derivative transactions can be amended in firms' internal systems on such date.   In most markets, the listed stock price is reduced (or increased for a reverse stock split) to account for the split on the same date, but in some markets the price adjustment occurs on a later date.  In either case, equity derivative transactions should be amended on the date that the stocks are paid to the shareholders (or consolidated). */
    effectiveDate: date,
}


/**
 * Specifies the relevant data regarding a credit event.
 */
export interface CreditEvent {
    /** The type of credit event taking place. */
    creditEventType: string,
    /** The date in which the credit event is determined by the Credit Derivatives Determinations Comitee. */
    eventDeterminationDate: date,
    /** The date on which the auction is scheduled to occur. */
    auctionDate?: date,
    /** The final price resulting from the auction. */
    finalPrice?: Price,
    /** The percentage of the original value of the asset affected by the credit event that can be recovered. */
    recoveryPercent?: string,
    /** A public information source, e.g. a particular newspaper or electronic news service, that may publish relevant information used in the determination of whether or not a credit event has occurred. */
    publiclyAvailableInformation?: Resource[],
    /** The reference entity, part of a credit basket, impacted by the credit event. */
    referenceInformation: ReferenceInformation,
}


/**
 * Specifies the relevant data regarding a corporate action
 */
export interface CorporateAction {
    /** The type of corporate action taking place. */
    corporateActionType: string,
    /** The date on which the corporate action is known to have taken place. */
    exDate: date,
    /** The date on which resulting from the corporate action are delivered. */
    payDate: date,
    /** The date on which the account phyical balance and related underlier ownership is recorded by the custodian. */
    recordDate?: date,
    /** The date on which the corporate action is announced by the issuer. */
    announcementDate?: date,
    /** The underlier impacted by the corporate action. */
    underlier: Underlier,
    /** Specifies any additional details e.g. further descriptions depending on the particular type of Corporate Action, adjustmentFactor, calculations */
    adjustmentFactor?: AdjustmentFactor,
    /** A class defining the source of data used in an Observation for any purposes. The data is either sourced from a Provider, or from a Party. */
    informationSource?: InformationSource,
    /** To record observations of dividends. */
    dividendObservation?: PriceSchedule,
    /** Corporate action triggered by the occurrence of an event which description is seperatly agreed between the parties and/or described in any referenced agreement (for instance bespoke term described in any string type objects present in most root attributes of Agreement type). */
    bespokeEventDescription?: string,
}


/**
 * Describes the terms involved for adjusting the price of the underlier impacted by a Corporate Action.
 */
export interface AdjustmentFactor {
    /** The multiplier value applied to the price of the underlier impacted by a Corporate Action. */
    value: string,
    /** Describes the input terms involved in the calculation of the adjustment factor applied to the price of the underlier impacted by a Corporate Action. */
    calculationTerms?: AdjustmentFactorCalculationTerms,
}


/**
 * Describes the input terms involved in the calculation of the adjustment factor applied to the price of the underlier impacted by a Corporate Action.
 */
export interface AdjustmentFactorCalculationTerms {
    /** Multiple value, say 'M/N' where 'M' is the number of shares after the event and 'N' is the number of shares prior to the event.  */
    shareForShareRatio?: string,
    /** Multiple value, say 'M/N' where 'M' is the number of rights after the event, and 'N' of shares prior to the event.  */
    shareForRightsRatio?: string,
    /** The price to pay per each right. */
    rightsSubscriptionPrice?: Price,
    /** Multiplier value (e.g. 85 %, etc.) to calculate net dividend amount, given gross amount before taking into account the relevant fiscal features. */
    dividendRatio?: string,
    /** Defines the underlier for both the Parent Company and the Child Compagny involved in Spin Off corporate action, as well as the respective price observation for each. */
    spinOff?: SpinOff,
    /** Defines the underlier for both the Purchaser Company and the Acquired Compagny involved in Spin Off corporate action, as well as the respective price observation for each. */
    merger?: Merger,
    /** The accrual rate and related terms, to adjust the price of an underlier impacted by a Corporate Action when economic impact consists in freezing the underlier price to last fixing and applying accruals for each remaining underlier price fixing date required i.e. for each, adjustedPrice = lastFixingPrice x (1 + accrualRate x DCF). */
    accrualFactor?: AccrualFactor,
    /** The last fixing price of the Underlier affected by the CorporateAction, before any adjustment with regards to the CorporateAction. */
    lastFixingPriceBeforeAdjustment?: Price,
    /** To record observations of dividends. */
    dividendObservation?: PriceSchedule,
    /** To describe the formula used to calculate the Adjustment Factor. */
    bespokeCalculationFormula?: string,
}


/**
 * The accrual rate and related terms, to adjust the price of an underlier impacted by a Corporate Action when economic impact consists in freezing the underlier price to the last fixing then applying accruals per each remaining underlier price fixing date required i.e. for each fixing date, adjustedPrice = lastFixingPrice x accrualFactor, where: accrualFactor = (1 + accrualRateValue x DCF).
 */
export interface AccrualFactor {
    /** The rate to be applied to the last fixing price, for price accrual calculation purposes. DatedValue may be used for the purpose of representing Price series if such calculation is required for multiple dates. */
    value: PriceSchedule,
    /** Describes the input terms involved in the calculation of the accrual factor. */
    calculationTerms?: AccrualFactorCalculationTerms,
}


/**
 * Describes the input terms involved in the calculation of the accrual factor. Optionnally, long and short stub interpolation rates can be specified.
 */
export interface AccrualFactorCalculationTerms {
    /** The duration between last fixing date and the payment date of accruals, calculated in accordance with the appropriate DayCountFraction. */
    tenorTillMaturity: string,
    /** The enumerated values to specify the day count fraction. */
    dayCountFraction: string,
    /** Describes the rate, tenor, period duration for the short and long stubs, when the accrualRate optionnaly results from an interpolation method. */
    interpolationTerms?: StubValue,
}


/**
 * Defines the underlier for both the Parent Company and the Child Compagny involved in Spin Off corporate action, as well as th respective price observation for each.
 */
export interface SpinOff {
    /** Defines the underlier for the Parent Company involved in Spin Off corporate action. */
    parentSecurity: Security,
    /** The price observation that relates to underlier for the Parent Company. */
    parentSecurityPrice: Price,
    /** Defines the underlier for the Child Company involved in Spin Off corporate action. */
    childSecurity: Security,
    /** The price observation that relates to underlier for the Child Company. */
    childSecurityPrice: Price,
}


/**
 * Defines the underlier for both the Purchaser Company and the Acquired Compagny involved in Merger corporate action, as well as th respective price observation for each.
 */
export interface Merger {
    /** Defines the underlier for the Purchaser Company involved in Merger corporate action. */
    purchaserSecurity: Security,
    /** The price observation that relates to underlier for the Purchaser Company. */
    purchaserSecurityPrice: Price,
    /** Defines the underlier for the Acquired Company involved in Merger corporate action. */
    acquiredSecurity: Security,
    /** The price observation that relates to underlier for the Acquired Company. */
    acquiredSecurityPrice: Price,
}


/**
 * All information required to perform the clear life cycle event; the clearing party (CCP), the two parties facing each other on the alpha contract, and optionally the parties acting as clearing members.
 */
export interface ClearingInstruction {
    /** The contract that will be submitted to the clearing house for clearing. The contract should indicate that it should be cleared by assigning a clearing organisation as a party role. */
    alphaContract: TradeState,
    /** The Central Counter party (CCP) that the contract will be submitted to for clearing. */
    clearingParty: Party,
    /** First party facing the CCP if it is clearing for its own account. */
    party1: Party,
    /** Second party facing the CCP if it is clearing for its own account. */
    party2: Party,
    /** Optional party facing the CCP, acting as clearing member for party1. */
    clearerParty1?: Party,
    /** Optional party facing the CCP, acting as clearing member for party2. */
    clearerParty2?: Party,
    /** Open Offer */
    isOpenOffer?: boolean,
}


/**
 * A data defining:  the adjusted dates associated with a particular exercise event.
 */
export interface ExerciseEvent {
    /** The date on which the option exercise takes place. This date should already be adjusted for any applicable business day convention. */
    adjustedExerciseDate: date,
    /** The effective date of the underlying swap associated with a given exercise date. This date should already be adjusted for any applicable business day convention. */
    adjustedRelevantSwapEffectiveDate: date,
    /** The date by which the cash settlement amount must be agreed. This date should already be adjusted for any applicable business day convention. */
    adjustedCashSettlementValuationDate?: date,
    /** The date on which the cash settlement amount is paid. This date should already be adjusted for any applicable business day convention. */
    adjustedCashSettlementPaymentDate?: date,
    /** The date on which the exercise fee amount is paid. This date should already be adjusted for any applicable business day convention. */
    adjustedExerciseFeePaymentDate?: date,
}


/**
 * Specifies the information required to create the return of a Security Finance Transaction.
 */
export interface ReturnInstruction {
    /** Specifies the quantity of shares and cash to be returned in a partial return event. */
    quantity: Quantity[],
}


/**
 * Specifies the information required for inclusion in a securities lending billing invoice.
 */
export interface SecurityLendingInvoice {
    /** The party issuing the invoice */
    sendingParty: Party,
    /** The party receiving the invoice */
    receivingParty: Party,
    /** The starting date of the period described by this invoice */
    billingStartDate: date,
    /** The ending date of the period described by this invoice */
    billingEndDate: date,
    /** The billing records contained within the invoice */
    billingRecord: BillingRecord[],
    /** The billing summaries contained within the invoice */
    billingSummary: BillingSummary[],
}


/**
 * Specifies the instructions for creation of a Security Lending billing invoice.
 */
export interface BillingInstruction {
    /** The party issuing the invoice */
    sendingParty: Party,
    /** The party receiving the invoice */
    receivingParty: Party,
    /** The starting date of the period described by this invoice */
    billingStartDate: date,
    /** The ending date of the period described by this invoice */
    billingEndDate: date,
    /** Instructions for creating the billing records contained within the invoice */
    billingRecordInstruction: BillingRecordInstruction[],
    /** The billing summaries contained within the invoice */
    billingSummary?: BillingSummaryInstruction[],
}


/**
 * Specifies the instructions for creation of a billing record.
 */
export interface BillingRecordInstruction {
    /** The trade for the individual billing record. */
    tradeState: TradeState,
    /** The observations used to calculate the billing amount. */
    observation: Observation[],
    /** The starting date of the period described by this record */
    recordStartDate: date,
    /** The ending date of the period described by this record */
    recordEndDate: date,
    /** The date for settlement of the transfer. */
    settlementDate: date,
}


/**
 * Specifies the instructions for creation of a billing summary.
 */
export interface BillingSummaryInstruction {
    /** The account level for the billing summary. */
    summaryAmountType: string,
}


/**
 * Specifies individual records within a billing invoice.
 */
export interface BillingRecord {
    /** The trade for the individual billing record. */
    tradeState: TradeState,
    /** The settlement terms for the billing record */
    recordTransfer: Transfer,
    /** The starting date of the period described by this record */
    recordStartDate: date,
    /** The ending date of the period described by this record */
    recordEndDate: date,
    /** Indicates the minimum fee amount applied to the billing record, if any. */
    minimumFee?: Money,
}


/**
 * Specifies individual summaries within a billing invoice.
 */
export interface BillingSummary {
    /** The settlement terms for the billing summary */
    summaryTransfer?: Transfer,
    /** The account level for the billing summary. */
    summaryAmountType: string,
}


/**
 * The attributes that are specific for consensus based pricing reporting.
 */
export interface TradePricingReport {
    /** Represents the cosensus based pricing parameters on a trade basis. */
    trade: Trade,
    /** The regional exchange close time for the underlying contract,including time zone, at which the trades should be priced. This provides an indication for which regional snapshot should be used for pricing primarily for Global markets where there are multiple regional close times. */
    pricingTime: TimeZone,
    /** It specifies the interest payable on collateral delivered under a CSA covering the trade. */
    discountingIndex?: string,
}


/**
 * Represents common attributes required for Issuance and Response to a Margin Call action as a result of a demand for delivery or return of collateral determined under a legal agreement such as a credit support document or equivalent.
 */
export interface MarginCallBase {
    /** Identifies the enumeration values to specify the call notification type, direction, specific action type. */
    instructionType: MarginCallInstructionType,
    /** Represents the parties to the margin call. The cardinality is optional to address the case where both parties of the event are specified and a third party if applicable. */
    party?: Party[],
    /** Represents the role each specified party takes in the margin call. further to the principal roles, payer and receiver. */
    partyRole?: PartyRole[],
    /** Indicates the name of the Clearing Broker FCM/DCM. */
    clearingBroker?: Party,
    /** Represents a unique Identifier for a margin call message, that can be referenced throughout all points of the process. */
    callIdentifier?: Identifier,
    /** Specifies the legal agreement type the margin call is generated from and governed by. */
    callAgreementType: AgreementName,
    /** Specifies the collateral legal agreement minimum transfer amount in base currency. */
    agreementMinimumTransferAmount?: Money,
    /** Specifies the collateral legal agreement threshold amount in base currency. */
    agreementThreshold?: Money,
    /** Specifies the collateral legal agreement rounding in base currency. */
    agreementRounding?: Money,
    /** Identifies margin type and if related regulatory mandate */
    regMarginType: string,
    /** Indicates the role of the party in an regulatory initial margin call instruction (i.e Pledgor party or Secured party). */
    regIMRole?: string,
    /** Represents the current mark to market value or IM calculation value of the trade portfolio as recorded by the principle (in base currency), to be referenced in a margin call. */
    baseCurrencyExposure?: MarginCallExposure,
    /** Represents attributes to define the details of collateral assets within a collateral portfolio to be used in margin call messaging and contribute to collateral balances e.g securities in a collateral account recorded by the principal as held or posted. */
    collateralPortfolio?: CollateralPortfolio,
    /** Represents additional credit support amount over and above mark to market value. */
    independentAmountBalance?: CollateralBalance,
}


/**
 * Represents the current mark to market value or IM calculation value of the trade portfolio as recorded by the principle (in base currency).
 */
export interface Exposure {
    /** Represents a Portfolio that describes all the positions held at a given time, in various states which can be either traded, settled, etc., with lineage information to the previous state. */
    tradePortfolio: PortfolioState,
    /** Represents the aggregate value of the portfolio in base currency. */
    aggregateValue: Money,
    /** Indicates the date when the exposure is calculated if different from valuation date. */
    calculationDateTime?: string,
    /** Indicates the valuation date of the exposure underlying the calculation. */
    valuationDateTime: string,
}


/**
 * Represents attributes required for mark to market value or IM calculation value of the trade portfolio as recorded by the principle (in base currency).
 */
export interface MarginCallExposure extends MarginCallBase {
    /** Represents the whole overall mark to market value or IM calculation value of the trade portfolio as recorded by the principle (in base currency). */
    overallExposure: Exposure,
    /** Represents Initial Margin (IM) exposure derived from ISDA SIMM calculation. */
    simmIMExposure?: Exposure,
    /** Represents Initial Margin (IM) exposure derived from schedule or Grid calculation. */
    scheduleGridIMExposure?: Exposure,
}


/**
 * Represents common attributes to define a collateral balance recorded by the principal as held or posted.
 */
export interface CollateralBalance {
    /** Defines the collateral balance breakdown of settlement status. */
    collateralBalanceStatus?: string,
    /** Indicates if the collateral balance amount is based on pre or post haircut, if a haircut is associated with the collateral asset */
    haircutIndicator?: string,
    /** Specifies the collateral balance amount in base currency determined within a collateral legal agreement, or defined for reporting purposes. */
    amountBaseCurrency: Money,
    /** Specifies each of the parties in the collateral balance and its perspective with regards to the direction of the collateral balance, posted or received. */
    payerReceiver: PartyReferencePayerReceiver,
}


/**
 * Represents common attributes to define the details of collateral assets, to be used in margin call messaging and contribute to collateral balances e.g securities in a collateral account.
 */
export interface CollateralPortfolio {
    /** Specifies a unique identifier for a set of collateral positions in a portfolio. */
    portfolioIdentifier?: Identifier,
    /** Specifies the individual components of the collateral positions in the collateral portfolio. */
    collateralPosition?: CollateralPosition[],
    /** Represents the populated or calculated collateral aggregate balance amount for the collateral portfolio. */
    collateralBalance?: CollateralBalance[],
    /**  The specification of a legal agreement between two parties governing the collateral relationship such as Credit Support Agreement or Collateral Transfer Agreement etc. (NB: this can be provided by reference to a global key for each LegalAgreement object). */
    legalAgreement?: LegalAgreement,
}


/**
 * Specifies the individual components of collateral positions.
 */
export interface CollateralPosition extends PositionBase {
    /** Specifies if there is any treatment to be applied to collateral, such as percentage discount which will impact collateral value. */
    treatment?: CollateralTreatment,
    /** Indicates the collateral positions settlement status. */
    collateralPositionStatus?: string,
}


/**
 * Represents common attributes required for a Margin Call Issuance under a legal agreement such as a credit support document or equivalent.
 */
export interface MarginCallIssuance extends MarginCallBase {
    /** Specifies the amount of margin being called for which accounts for margin calculation inclusive of exposure, independent amount,threshold,collateral balance, MTA, rounding increments (in base currency detailed in supporting collateral agreement). */
    callAmountInBaseCurrency: Money,
    /** Specifies the details to describe or identify non-cash collateral eligible assets for recall purposes. */
    recallNonCashCollateralDescription?: EligibleCollateralCriteria[],
}


/**
 * Represents enumeration values to specify the call notification type, direction, specific action type.
 */
export interface MarginCallInstructionType {
    /** Indicates the status of the call message type, such as expected call, notification of a call or an actionable margin call. */
    callType: string,
    /** Indicates the choice if the call instruction is visible or not to the other party. */
    visibilityIndicator?: boolean,
}


/**
 * Specifies the margin call action details, including collateral to be moved and its direction.
 */
export interface MarginCallResponseAction {
    /** Specifies the collateral to be moved and its direction. */
    collateralPositionComponent: CollateralPosition[],
    /** Specifies the margin call action details, specified as either Delivery or Return. */
    marginCallAction: string,
}


/**
 * Represents common attributes required for a Margin Call Response under a legal agreement such as a credit support document or equivalent.
 */
export interface MarginCallResponse extends MarginCallBase {
    /** Specifies the margin call action details, including collateral to be moved and direction. */
    marginCallResponseAction: MarginCallResponseAction[],
    /** Indicates the response type, such as, is the margin call response a 'full' 'part' agreement or 'dispute'. */
    marginResponseType: string,
    /** Indicates the amount that posting entity agrees to remit in response to margin call (in base currency). */
    agreedAmountBaseCurrency: Money,
}


/**
 *  A class to qualify the closed state of an execution or a contract through the combination or a state (e.g. terminated, novated) and a set of dates: activity date, effective date and, when relevant, last payment date.
 */
export interface ClosedState {
    /** The qualification of what gave way to the contract or execution closure, e.g. allocation, termination, ... */
    state: string,
    /** The activity date on which the closing state took place, i.e. either the event date of the closing event (e.g. option exercise, contract early termination) or the contractual termination date. */
    activityDate: date,
    /** The date on which the closing event contractually takes effect, when different from the activity date. When an explicit event effective date attribute is associated with the closing event, it will be that date. In the case of a cancellation event, it will be the date on which the cancelled event took place. */
    effectiveDate?: date,
    /** The date associated with the last payment in relation to the artefact (e.g. contract) to which this closed state applies. As an example, in the case of an early termination event, it would be the settlement date of the associated fee, if applicable. */
    lastPaymentDate?: date,
}


/**
 * The type of transfer.
 */
export interface Transfer {
}


/**
 * Encapsulates data features common to trade and position.
 */
export interface ContractBase {
    /** Represents information specific to trades or positions involving contractual products. */
    contractDetails?: ContractDetails,
    /** Defines specific attributes that relate to trade or position executions. */
    executionDetails?: ExecutionDetails,
    /** Represents the collateral obligations of a party. */
    collateral?: Collateral,
}


/**
 * A Position describes the accumulated effect of a set of securities or financial transactions.
 */
export interface CounterpartyPosition extends ContractBase {
    /** Represents the identifier(s) that uniquely identify a position for an identity issuer. A position can include multiple identifiers, for example an internal position identifer and a UTI (Unique Trade Identifier). */
    positionIdentifier?: PositionIdentifier[],
    /** The date and time when the position was opened. */
    openDateTime?: string,
    /** Reference to all the trades that constitute the position. */
    tradeReference?: TradeState[],
    /** The parties involved in the position, including the Clearing Organization. */
    party?: Party[],
    /** Represents the role each specified party takes in the position. */
    partyRole?: PartyRole[],
    /** Encapsulates the core constituents that characterize a position. */
    positionBase: TradableProduct,
}


/**
 * Base class to describe a Position, which could be a group of products (e.g. trade portfolio) or a group of assets (e.g. collateral portfolio).
 */
export interface PositionBase {
    /** Position with many price quantities. */
    priceQuantity: PriceQuantity[],
    /** The product underlying the position. */
    product?: Product,
    /** The asset that underlies the position. */
    asset?: Asset,
    /** The aggregate cost of proceeds. */
    cashBalance?: Money,
    /** Reference to the Contract, in case product is contractual and the contract has been formed. */
    tradeReference?: TradeState,
}


/**
 * A Position describes how much of a given Product is being held and constitutes the atomic element of a Portfolio.
 */
export interface Position extends PositionBase {
}


/**
 * State-full representation of a Portfolio that describes all the positions held at a given time, in various states which can be either traded, settled, etc., with lineage information to the previous state
 */
export interface PortfolioState {
    /** The list of positions, each containing a Quantity and a Product. */
    positions?: Position[],
    /** Pointer to the previous PortfolioState and new Event(s) leading to the current (new) state. Previous PortfolioState in the Lineage can be Null in case this is the start of the chain of Events. */
    lineage: Lineage,
}


/**
 *  Parameters to be used to filter events that are relevant to a given portfolio in order to calculate the state of this portfolio. The attributes correspond to all the possible aggregation criteria that can be used and these criteria can be combined. All the attributes are optional.
 */
export interface AggregationParameters {
    /** To aggregate as of a particular date */
    dateTime: string,
    /** Specifies whether to calculate total position to given date, or only daily position for the given date. */
    totalPosition?: boolean,
    /** To aggregate based on position status (EXECUTED, SETTLED etc) */
    positionStatus?: string,
    /** To aggregate based on a selection of party(ies) / legal entity(ies). */
    party?: Party[],
    /** To aggregate based on a selection of products. */
    product?: NonTransferableProduct[],
    /** To aggregate based on a selection of product type(s). */
    productQualifier?: string[],
    tradeReference?: Trade[],
}


/**
 *  A Portfolio represents an aggregation of multiple Positions, by describing the parameters that this Portfolio should be aggregated based on. The resulting PortfolioState is calculated using these aggregation parameters as inputs, by aggregating all the Events that are relevant to this Portfolio. The concept of Portfolio works at all levels in the model: from the highest for a given LegalEntity for instance, to the lowest to account for security substitutions in a secutity financing transaction. As such, Portfolio can be used either above or below the Contract level.
 */
export interface Portfolio {
    /** Describes the portfolio by describing how to aggregate all its relevant Events. */
    aggregationParameters: AggregationParameters,
    /** Describes the state of the Portfolio as a list of Positions resulting from the aggregation. */
    portfolioState: PortfolioState,
}


/**
 * A data type that can be used to describe an inventory of securities.
 */
export interface Inventory {
    /** An array holding the list of inventory being described. Each element in the inventoryRecord array represents an individual piece of inventory i.e. a security. */
    inventoryRecord?: InventoryRecord[],
}


/**
 * An individual piece of inventory. This represents a single security.
 */
export interface InventoryRecord {
    /** Unique identifier for this record. This can be used to uniquely identify a specific piece of inventory. */
    identifer: AssignedIdentifier,
    /** The security details. */
    security: Security,
}


/**
 * A data type that can be used to describe the inventory of securities that a party holds. The securities are held in the AvailableInventoryRecord, with each item in the array being an individual security and its associated criteria. Criteria can include the quantity available, the rate at which the security is available to borrow at, as well as other details that can affect the decision as to whether a party wants to utilise the securities listed.
 */
export interface AvailableInventory {
    /** Defines the purpose of this inventory. */
    availableInventoryType: string,
    /** Unique identifier for this group. This can be used to uniquely identify a group of inventory records. */
    identifer?: AssignedIdentifier,
    /** Optional comments for this group of inventory records. */
    comment?: string,
    /** Allows details related to the availability messaging use case to be defined */
    messageInformation?: MessageInformation,
    /** Defines all parties involved for the list of inventory records in this set of inventory. For example, when used to describe securities lending availability, this could hold the sender of the availability, the intended recipient, the beneficial owner(s), the lender (which may differ from the sender as the lender may have the same piece of availability going through multiple agents), an agent or a venue. */
    party?: Party[],
    /** Defines the role(s) that party(ies) may have in relation to the inventory. */
    partyRole?: PartyRole[],
    /** An array holding the list of inventory being described. Each element in the inventoryRecord array represents an individual piece of inventory i.e. a security. */
    availableInventoryRecord?: AvailableInventoryRecord[],
}


/**
 * An individual piece of available inventory. This represents a single security and its associated criteria. The criteria are used to describe any restrictions on the securities.
 */
export interface AvailableInventoryRecord extends InventoryRecord {
    /** There may be a set period/time restriction associated to the security. */
    expirationDateTime?: string,
    /** The type of collateral can often be required when determining if the piece of availability being described is suitable for a party. */
    collateral?: CollateralProvisions[],
    /** An individual security may be held by several agents. Including the party role at this level allows us to reference the party holding this specific item. */
    partyRole?: PartyRole[],
    /** The quantity of the security */
    quantity?: Quantity,
    /** An optional element which can be used to hold a rate associated to this piece of availability. */
    interestRate?: Price,
    /** Specifies the dividend requirements if applicable. */
    dividendTerms?: DividendTerms,
    /** Optional comment for this specific piece of inventory. */
    comment?: string,
}


/**
 * A locate is an approval from a broker that needs to be obtained prior to effecting a short sale in an equity security. Similar to security availability, a borrower can request a single or multiple securities, but at least one must be requested.
 */
export interface SecurityLocate extends AvailableInventory {
}


/**
 * A class to represent the credit limit utilisation information.
 */
export interface CreditLimitInformation {
    limitApplicable: LimitApplicableExtended[],
}


/**
 * Credit limit utilisation breakdown by executed trades and pending orders.
 */
export interface CreditLimitUtilisation {
    /** Credit limit utilisation attributable to executed trades. */
    executed?: CreditLimitUtilisationPosition,
    /** Credit limit utilisation attributable to pending unexecuted orders. */
    pending?: CreditLimitUtilisationPosition,
}



export interface CreditLimitUtilisationPosition {
    /** Credit limit utilisation attributable to short positions. */
    shortPosition?: string,
    /** Credit limit utilisation attributable to long positions. */
    longPosition?: string,
    /** Global credit limit utilisation amount, agnostic of long/short position direction. */
    global_?: string,
}


/**
 * In its initial iteration, this class is meant to support the DTCC TIW workflow information.
 */
export interface CustomisedWorkflow {
    /** In this initial iteration, this corresponds to the DTCC TIW element name. */
    itemName: string,
    /** In this initial iteration, this corresponds to the DTCC value. */
    itemValue: string,
}


/**
 * Party approvals associated to the current WorkflowStep. 
 */
export interface WorkflowStepApproval {
    /** Flag denoting whether the workflow step is approved or not */
    approved: boolean,
    /** Reference to the Party who is approving/rejecting this workflow step */
    party: Party,
    /** Optional reason for rejecting the workflow step */
    rejectedReason?: string,
    /** Timestamp of the approval */
    timestamp: EventTimestamp,
}


/**
 * A workflow step represents the state of a business event. The workflow step contains a reference to a previous WorkflowStep in order to preserve lineage. A workflow step is accepted if it contains a business event, proposed if proposedEvent is present and is rejected if the rejected flag is set.
 */
export interface WorkflowStep {
    /** Life cycle event for the step. */
    businessEvent?: BusinessEvent,
    /** Documents the life cycle event for a position. */
    counterpartyPositionBusinessEvent?: CounterpartyPositionBusinessEvent,
    /** The proposed event for a workflow step. */
    proposedEvent?: EventInstruction,
    /** Flags this step as rejected. */
    rejected?: boolean,
    /** Optional party approvals for the current workflow step. A workflow step can have any number of parties associated to it, thus this object is represented as a list. All parties that are expected to provide approval should have an item in this list that references them. */
    approval?: WorkflowStepApproval[],
    /** Optional previous workflow step that provides lineage to workflow steps that precedes it. */
    previousWorkflowStep?: WorkflowStep,
    /** The intended next event can be specified, even if the instructions are not known yet. */
    nextEvent?: EventInstruction,
    /** Contains all information pertaining the FpML messaging header  */
    messageInformation?: MessageInformation,
    /** The set of timestamp(s) associated with the event as a collection of [dateTime, qualifier]. */
    timestamp: EventTimestamp[],
    /** The identifier(s) that uniquely identify a lifecycle event. The unbounded cardinality is meant to provide the ability to associate identifiers that are issued by distinct parties. As an example, each of the parties to the event may choose to associate their own identifiers to the event. */
    eventIdentifier: Identifier[],
    /** Specifies whether the event is a new, a correction or a cancellation. */
    action?: string,
    /** The specification of the event parties. This attribute is optional, as not applicable to certain events (e.g. most of the observations). */
    party?: Party[],
    /** Optional account information that could be associated to the event. */
    account?: Account[],
    /** The lineage attribute provides a linkage among lifecycle events through the globalKey hash value. One example is when a given lifecycle event is being corrected or cancelled. In such case, each subsequent event will have lineage into the prior version of that event. The second broad use case is when an event has a dependency upon either another event (e.g. the regular payment associated with a fix/float swap will have a lineage into the reset event, which will in turn have a lineage into the observation event for the floating rate and the contract) or a contract (e.g. the exercise of an option has a lineage into that option). */
    lineage?: Lineage,
    creditLimitInformation?: CreditLimitInformation,
    /** The event workflow information, i.e. the workflow status, the associated comment and the partyCustomisedWorkflow which purpose is to provide the ability to associate custom workflow information to the CDM. */
    workflowState?: WorkflowState,
}


/**
 * A collection of workflow steps which together make up an entire workflow sequence.
 */
export interface Workflow {
    steps: WorkflowStep[],
}


/**
 * Specifies instructions to create a BusinessEvent.
 */
export interface EventInstruction {
    /** The intent attribute is meant to be specified when the event qualification cannot be programmatically inferred from the event features. As a result it is only associated with those primitives that can give way to such ambiguity, the quantityChange being one of those. An example of such is a reduction in the trade notional, which could be interpreted as either a trade correction (unless a maximum period of time post-event is specified as part of the qualification), a partial termination or a portfolio rebalancing in the case of an equity swap. On the other hand, an event such as the exercise is not expected to have an associated intent as there should not be ambiguity. */
    intent?: string,
    /** The intent of a corporate action on the position. */
    corporateActionIntent?: string,
    /** Specifies the date on which the event is taking place. This date is equal to the trade date in the case of a simple execution.  However it can be different from the trade date, for example in the case of a partial termination. */
    eventDate?: date,
    /** The time and zone of the date in which the event is taking place. */
    eventTime?: TimeZone,
    /** The date on which the event contractually takes effect, when different from the event date. */
    effectiveDate?: date,
    /** The time and zone of the date in which the event contractually takes effect. */
    effectiveTime?: TimeZone,
    /** Specifies the package information in case the business event represents several trades executed as a package (hence this attribute is optional). The package information is only instantiated once at the business event level to preserve referential integrity, whereas individual trades make reference to it to identify that they are part of a package. */
    packageInformation?: IdentifiedList,
    /** Specifies the instructions to create the Business Event. */
    instruction?: Instruction[],
}


/**
 * A class to represent the various set of timestamps that can be associated with lifecycle events, as a collection of [dateTime, qualifier].
 */
export interface EventTimestamp {
    /** The CDM specifies that the zoned date time is to be expressed in accordance with ISO 8601, either as UTC as an offset to UTC. */
    dateTime: string,
    /** The timestamp qualifier is specified through an enumeration because the experience of integrating the DTCC and CME data representations suggests that a wide set of timestamps are currently utilized among service providers, while there is not at present an objective set of criteria that could help suggest a defined set of timestamps as part of the CDM. At some future point, one possible baseline could be developed from the review of the set of timestamps specified across regulatory regimes and regulations (incl. regulations such as high frequency trading). Also, the integration with a further set of implementations and the specification of business workflows such as clearing as part of the CDM development should help confirm the implementation approach in this respect. */
    qualification: string,
}


/**
 * A class to specify workflow information, which is conceptually applicable to all lifecycle events.
 */
export interface WorkflowState {
    /** The workflow status indicator, e.g. Accepted, Rejected, ... */
    workflowStatus: string,
    /** A comment field to be associated with the workflow, e.g. to specify why a transaction event was rejected by a party. */
    comment?: string,
    /** Workflow data that is specific to certain market participants and is expressed as part of the CDM in a very generic manner, which can be party-specific. The initial use cases have been derived from the CME clearing and the DTCC TIW submissions. */
    partyCustomisedWorkflow?: PartyCustomisedWorkflow[],
    /** The identity of the warehouse, if any, that is executing that workflow step. */
    warehouseIdentity?: string,
}



export interface LimitApplicable {
    /** Standard code to indicate which type of credit line is being referred to - i.e. IM, DV01, PV01, CS01, Notional, Clip Size, Notional, maximumOrderQuantity. */
    limitType?: string,
    /** This element is required in FpML, optional in CDM for the purpose of accommodating the CME data representation while making reference to the FpML one. */
    clipSize?: number,
    /** The limit utilised by all the cleared trades for the limit level and limit type. While the attribute is of type integer in FpML and the CME schema, it has been specified to be of type number in the CDM to take into consideration java size limits as well as for consistency purposes with the way most monetary amounts are expressed. */
    amountUtilized?: string,
    utilization?: CreditLimitUtilisation,
    /** The limit remaining for the limit level and limit type. This does not take into account any pending trades. While the attribute is of type integer in FpML and the CME schema, it has been specified to be of type number in the CDM to take into consideration java size limits as well as for consistency purposes with the way most monetary amounts are expressed. */
    amountRemaining?: string,
    /** The currency in which the applicable limit is denominated. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
    velocity?: Velocity,
}


/**
 * A class to represent the CDM attributes that are not part of the FpML standard. Once broader usage is confirmed, it is expected that those two classes can be collapsed.
 */
export interface LimitApplicableExtended extends LimitApplicable {
    /** The level at which the limit is set: customer business, proprietary business or account level. This attribute is specified as a string as part of the CME clearing confirmation specification. */
    limitLevel?: string,
    /** The total limit available for the limit level and limit type. While the attribute is of type integer in the CME schema, it has been specified to be of type number in the CDM to take into consideration java size limits as well as for consistency purposes with the way most monetary amounts are expressed. */
    limitAmount?: string,
    /** The limit utilized by this specific trade. While the attribute is of type integer in the CME schema, it has been specified to be of type number in the CDM to take into consideration java size limits as well as for consistency purposes with the way most monetary amounts are expressed. */
    limitImpactDueToTrade?: string,
}


/**
 * This class corresponds to the components of the FpML MessageHeader.model.
 */
export interface MessageInformation {
    /** A unique identifier assigned to the message. */
    messageId: string,
    /** The identifier for the originator of a message instance. */
    sentBy?: string,
    /** The identifier(s) for the recipient(s) of a message instance. */
    sentTo?: string[],
    /** A unique identifier (within the specified coding scheme) giving the details of some party to whom a copy of this message will be sent for reference. */
    copyTo?: string[],
}


/**
 * A class to specify a party-related, non-standardized data in a generic form.
 */
export interface PartyCustomisedWorkflow {
    /** Reference to the party to which the workflow pertains to. */
    partyReference?: Party,
    /** The party name to which the workflow pertains to. */
    partyName?: string,
    /** Non-standardized data in a generic form. */
    customisedWorkflow: CustomisedWorkflow[],
}



export interface Velocity {
    /** A time period multiplier, e.g. 1, 2, or 3. If the period value is T (Term) then period multiplier must contain the value 1. */
    periodMultiplier?: number,
    /** A time period, e.g. a day, week, month, year or term of the stream. */
    period?: string,
}



export interface ContractualMatrix {
    /** Identifies the form of applicable matrix. */
    matrixType: string,
    /** Defines any applicable key into the relevant matrix. For example, the Transaction Type would be the single term required for the Credit Derivatives Physical Settlement Matrix. This element should be omitted in the case of the 2000 ISDA Definitions Settlement Matrix for Early Termination and Swaptions. */
    matrixTerm?: string,
}


/**
 * A contractual supplement (such as those published by ISDA) and its publication date that will apply to the trade.
 */
export interface ContractualTermsSupplement {
    /** Identifies the form of applicable contractual supplement. */
    contractualTermsSupplementType: string,
    /** Specifies the publication date of the applicable version of the contractual supplement. */
    publicationDate?: date,
}


/**
 * A class for defining an agreement executed between parties.
 */
export interface OtherAgreement {
    /** An identifier that has been created to identify the agreement. */
    identifier?: string,
    /** The agreement executed between the parties and intended to govern product-specific derivatives transactions between those parties. */
    otherAgreementType: string,
    /** The version of the agreement. */
    version?: string,
    /** The date on which the agreement was signed. */
    date?: date,
}


/**
 * Describes the resource that contains the media representation of a business event (i.e used for stating the Publicly Available Information). For example, can describe a file or a URL that represents the event. This type is an extended version of a type defined by RIXML (www.rixml.org).  Rosetta restricts the FpML implementation by not providing the ability to associated a document in hexadecimalBinary or base64Binary until such time that actual use cases will come up.
 */
export interface Resource {
    /** The unique identifier of the resource within the event. FpML specifies this element of type resourceIdScheme but with no specified value. */
    resourceId: string,
    /** A description of the type of the resource, e.g. a confirmation. */
    resourceType?: string,
    /** Indicates the language of the resource, described using the ISO 639-2/T Code. */
    language?: string,
    /** Indicates the size of the resource in bytes. It could be used by the end user to estimate the download time and storage needs. */
    sizeInBytes?: string,
    /** Indicates the length of the resource. For example, if the resource were a PDF file, the length would be in pages. */
    length?: ResourceLength,
    /** Indicates the type of media used to store the content. mimeType is used to determine the software product(s) that can read the content. MIME Types are described in RFC 2046. */
    mimeType?: string,
    /** The name of the resource.  It is specified as a NormalizedString in FpML. */
    name?: string,
    /** Any additional comments that are deemed necessary. For example, which software version is required to open the document? Or, how does this resource relate to the others for this event? */
    comments?: string,
    /** Provides extra information as string. In case the extra information is in XML format, a CDATA section must be placed around the source message to prevent its interpretation as XML content. */
    string?: string,
    /** Indicates where the resource can be found, as a URL that references the information on a web server accessible to the message recipient. */
    url?: string,
}


/**
 * A class to indicate the length of the resource.
 */
export interface ResourceLength {
    /** The length unit of the resource. For example, pages (pdf, text documents) or time (audio, video files). */
    lengthUnit: string,
    /** The length value of the resource. */
    lengthValue: string,
}


/**
 * Specification of the content of a legal agreement.
 */
export interface AgreementTerms {
    /** Specification of the standard set of terms that define a legal agreement. */
    agreement: Agreement,
    /** Specification of whether the agreement terms have been negotiated using the clause library methodology. */
    clauseLibrary?: boolean,
    /** Specification of the roles of the counterparties to the agreement. */
    counterparty: Counterparty[],
}


/**
 * The specification of a legal agreement between two parties, being negotiated or having been executed. This includes the baseline information and the optional specialised elections
 */
export interface LegalAgreement extends LegalAgreementBase {
    /** Specification of the content of the legal agreement. */
    agreementTerms?: AgreementTerms,
    /** Specifies the agreement(s) that govern the agreement, either as a reference to such agreements when specified as part of the CDM, or through identification of some of the key terms of those agreements, such as the type of agreement, the publisher, the vintage, the agreement identifier and the agreement date. */
    relatedAgreements?: LegalAgreement[],
    /** Specifies a set of legal entities which are part of a legal agreement beyond the two contracting parties to that agreement. */
    umbrellaAgreement?: UmbrellaAgreement,
}


/**
 * Specifies the legal agreement baseline information, being negotiated or having been executed. It excludes specialized elections
 */
export interface LegalAgreementBase {
    /** The date on which the legal agreement has been agreed between the parties. This corresponds to the Date of Deed in an English Law document. */
    agreementDate?: date,
    /** The date on which, or as of which, the agreement is effective, if different from the agreement date. It is expected that it will most often correspond to the agreement date, although there could be situations where the parties will explicitly agree on a distinct effective date. */
    effectiveDate?: date,
    /** The legal agreement identifier. Several identifiers can be specified. */
    identifier?: Identifier[],
    /** The type of legal agreement, identified via a set of composable attributes: agreementName, publisher, governing law and version, e.g. ISDA 2013 Standard Credit Support Annex English Law. */
    legalAgreementIdentification: LegalAgreementIdentification,
    /** The two contractual parties to the legal agreement, which reference information is positioned as part of the partyInformation attribute. */
    contractualParty: Party[],
    /** The role(s) that other party(ies) may have in relation to the legal agreement, further to the contractual parties. */
    otherParty?: PartyRole[],
    /** A human readable document, for example a confirmation. */
    attachment?: Resource[],
}


/**
 * Specifies the type of legal agreement, identified via a set of composable attributes: agreementName, publisher, governing law and version, e.g. ISDA 2013 Standard Credit Support Annex English Law.
 */
export interface LegalAgreementIdentification {
    /** The law governing the legal agreement, e.g. English Law, New York Law or Japanese Law. */
    governingLaw?: string,
    /** The legal agreement name, e.g. Credit Support Annex for Variation Margin. */
    agreementName: AgreementName,
    /** The legal agreement publisher, e.g. ISDA. */
    publisher?: string,
    /** In the case where successive definitions of the legal agreement have been developed, the vintage identification. This is typically (but not necessarily) done by referencing the year, e.g. 2013 in the case of the ISDA 2013 Standard Credit Support Annex. */
    vintage?: number,
}


/**
 * Specifies the agreement name through an agreement type and optional detailed sub agreement type.
 */
export interface AgreementName {
    /** Specification of the legal agreement type. */
    agreementType: string,
    /** Specification of the credit support agreement type. */
    creditSupportAgreementType?: string,
    /** specifies the type of margin for which a legal agreement is named. */
    creditSupportAgreementMarginType?: string,
    /** The definitions such as those published by ISDA that will define the terms of the trade. */
    contractualDefinitionsType?: string,
    /** A contractual supplement (such as those published by ISDA) that will apply to the trade. */
    contractualTermsSupplement?: ContractualTermsSupplement[],
    /** A reference to a contractual matrix of elected terms/values (such as those published by ISDA) that shall be deemed to apply to the trade. The applicable matrix is identified by reference to a name and optionally a publication date. Depending on the structure of the matrix, an additional term (specified in the matrixTerm element) may be required to further identify a subset of applicable terms/values within the matrix. */
    contractualMatrix?: ContractualMatrix[],
    /** Specification of the master agreement type. */
    masterAgreementType?: string,
    /** The type of master confirmation executed between the parties. */
    masterConfirmationType?: string,
    /** The type of master confirmation annex executed between the parties. */
    masterConfirmationAnnexType?: string,
    /** Definition of an agreement that is not enumerated in the CDM. */
    otherAgreement?: string,
    brokerConfirmationType?: string,
}


/**
 * Specifies the set of agreements, the parties of which are set within UmbrellaAgreementSet, beyond the two contracting parties to that agreement.
 */
export interface UmbrellaAgreement {
    /** The language associated with the umbrella agreement, and which applies to all the parties to the umbrella agreement. */
    agreementSet: UmbrellaAgreementSet[],
}


/**
 * Specifies the entities that are part of the umbrella agreement.
 */
export interface UmbrellaAgreementParty {
    /** Specifies the party to the umbrella agreement */
    party?: Party,
    /** Specifies the CounterpartyRoleEnum, e.g. either Party1 or Party2, that is associated to the party. */
    actingAs?: string,
    /** Represents the legal role that each defined entity to the agreement performs within the structure of that agreement. */
    partyRole?: string,
    /** Specifies a non-legal entity that exists in the agreement with a defined relationship to a Legal entity to the agreement. */
    nonLegalEntity?: NonLegalEntity,
    /** Represents the identification of a parent child relationship between two entities in the Umbrella agreement, allowing non-legal entities to be linked to the appropriate legal entity and allowing funds, portfolio or managed accounts to be linked to their applicable Investment Manager or Agent within the agreement. */
    parentParty?: Party,
}


/**
 * Specifies a non-legal entity that exists in the agreement with a defined relationship to a Legal entity to the agreement
 */
export interface NonLegalEntity {
    /** Specifies the unique identifier value for a non-legal entity such as a sleeve, or sub account, that is a party to the agreement and has a relationship with a legal entity defined on the agreement. */
    identifier: string,
    /** Specifies the Identifier type for a non-Legal entity to the agreement, for example the use of an alert code to uniquely identify a non-Legal entity. */
    identifierType: string,
}


/**
 * Represents the groups of agreement term elections that exist within the agreement and to which entities that particular group of elections apply. Where all entities under the umbrella have the same set of elections only one agreement set will exist. At least one agreement set must be defined for an Umbrella agreement
 */
export interface UmbrellaAgreementSet {
    /** Specification of the standard set of terms that define a legal agreement. */
    agreement: Agreement,
    /** Specifies the underlying parties to the agreement that either act as legal parties to the agreement or represent legal constructs that are managed by a party to the agreement but do not represent a Legal Entity as of themselves. */
    party: UmbrellaAgreementParty[],
    /** Represents any additional language that may need to be captured regarding the structure or application of terms that may exist for the agreement. */
    additionalLanguage?: string,
}


/**
 * Specifies the party reference and the associated contact information.
 */
export interface ContactInformationElection {
    /** The party to which the election and details apply. */
    partyReference: string,
    /** The main notice/address information of the party. */
    primaryContactInformation?: ContactInformation,
}


/**
 * Specifies the party reference and the associated notice and contact information.
 */
export interface NoticeInformationElection extends ContactInformationElection {
    /** Additional notice/address information of the party. */
    additionalContactInformation?: NoticeContactInformation[],
}


/**
 * Specifies notice information including address and, optionally, an associated person. This type also supports the ISDA CSA representation as a single string, through the address attribute in Contact Information.
 */
export interface NoticeContactInformation extends ContactInformation {
    /** Optional information about people involved in a transaction or business process. */
    naturalPerson?: NaturalPerson[],
    /** Additional information on the recipient. */
    additionalInformation?: string,
}


/**
 * Specifies the party reference and the associated transfer & contact information.
 */
export interface TransferInformationElection extends ContactInformationElection {
}


/**
 * Specifies transfer information including address and, optionally, an associated account. This type also supports the ISDA CSA representation as a single string, through the address attribute in Contact Information.
 */
export interface TransferContactInformation extends ContactInformation {
    /** Specifies the account infomation for transfers to a party. */
    account?: Account[],
    /** Additional transfer information. */
    additionalInformation?: string,
}


/**
 * A class to specify a related legal agreement. For example, ISDA 2016 Credit Support Annex for Initial Margin, paragraph 13, General Principles, (s): Other CSA and Japanese Law CSA (VM). | ISDA 2016 Credit Support Annex for Variation Margin, paragraph 13, (o): Other CSA.
 */
export interface OtherAgreementTerms {
    /** The qualification of whether some other related agreement is specified (True) or not (False). */
    isSpecified: boolean,
    /** The specification of this other agreement, when the qualification is True. */
    legalDocument?: string,
}


/**
 * Specification of the standard set of terms that define a legal agreement.
 */
export interface Agreement {
    /** Elections to specify a Credit Support Annex or Credit Support Deed for Intial or Variation Margin. */
    creditSupportAgreementElections?: CreditSupportAgreementElections,
    /** Elections to specify a Collateral Transfer Agreement. */
    collateralTransferAgreementElections?: CollateralTransferAgreementElections,
    /** Elections to specify a Security agreement. */
    securityAgreementElections?: SecurityAgreementElections,
    /** Elections to specify a Master Agreement Schedule. */
    masterAgreementSchedule?: MasterAgreementSchedule,
    /** Any additional terms which mainly intend to specify the extraordinary events that may affect a trade and the related contractual rights and obligation of the parties when this happens */
    transactionAdditionalTerms?: TransactionAdditionalTerms,
    /** A legal representation of the different possible master agreements */
    masterAgreementElections?: MasterAgreementElections,
}


/**
 * The set of elections which specify an Initial Margin Credit Support Annex or Deed.
 */
export interface CreditSupportAgreementInitialMarginElections extends CreditSupportAgreementElectionsBase {
    /** The Regime Table provision , which determines the regulatory regime(s) applicable to each of the parties to the agreement. */
    regime: Regime,
    /** The determination of whether the One Way Provisions are applicable (true) or not applicable (false). */
    oneWayProvisions: OneWayProvisions,
    /** The specification of the ISDA SIMM Method for all Covered Transactions with respect to all Regimes. */
    generalSimmElections?: GeneralSimmElections,
    /** The qualification of whether cross-currency swaps need to be identified in the Confirmation so that the obligations to exchange principal be disregarded for the purpose of determining the Delivery Amount or Return Amount. */
    identifiedCrossCurrencySwap?: boolean,
    /** The specification of methodologies to compute sensitivities specific to the agreement. */
    sensitivityMethodologies: SensitivityMethodologies,
    /** The reference currency for the purpose of specifying the FX Haircut relating to a posting obligation, as being either the Termination Currency or an FX Designated Currency. */
    fxHaircutCurrency?: FxHaircutCurrency,
    /** The security providers posting obligations. */
    postingObligations: PostingObligations,
    /** The specification of Additional regimes for purposes of determining whether a Regulatory Event has occurred. */
    substitutedRegime?: SubstitutedRegime[],
    /** The specification of transactions covered by the terms of the agreement. */
    coveredTransactions?: CoveredTransactions,
    /** The bespoke provisions that might be specified by the parties to the agreement to specify the rights of Security Taker and/or Security Provider when an Early Termination or Access Condition event has occurred. */
    rightsEvents: RightsEvents,
    /** The Custodian and Segregated Account details in respect of each party to the agreement. */
    custodyArrangements?: CustodyArrangements,
    /** The bespoke definition of other agreement terms as specified by the parties to the agreement. */
    otherAgreements?: OtherAgreements,
    /** The bespoke provision that might be specified by the parties to the agreement applicable to Termination Currency.  Unless specified the definition of Termination Currency has the meaning specified in the Schedule to the ISDA Master Agreement. */
    terminationCurrencyAmendment: TerminationCurrencyAmendment,
    /** The bespoke provision that might be specified by the parties to the agreement applicable to Minimum Transfer Amount.  Unless specified the definition of Minimum Transfer Amount in any Other Regulatory CSA has the meaning specified in such Other Regulatory CSA. */
    minimumTransferAmountAmendment: MinimumTransferAmountAmendment,
    /** The bespoke provision that might be specified by the parties to the agreement applicable to Interpretations. */
    interpretationTerms?: string,
    /** The Process Agent that might be appointed by the parties to the agreement. */
    processAgent: ProcessAgent,
    /** The election for the Valuation of Appropriate Collateral. */
    appropriatedCollateralValuation?: AppropriatedCollateralValuation,
    /** The jurisdiction specific terms relevant to the agreement. */
    jurisdictionRelatedTerms?: JurisdictionRelatedTerms,
    /** Any additional amendments that might be specified by the parties to the agreement. */
    additionalAmendments?: string,
    /** Any additional terms that might be specified applicable. */
    additionalBespokeTerms?: string,
    /** The qualification of whether Trust Scheme Addendum is applicable (True) or not applicable (False). */
    trustSchemeAddendum: boolean,
    /** Details of language relating to Client Asset and Money Rules (CASS). */
    clientAssetSourcebookAdditionalLanguage?: string,
    /** The Credit Support Obligations applicable to the agreement. */
    creditSupportObligations: CreditSupportObligationsInitialMargin,
    /** The set of elections for determining Valuation and Timing terms specific to the agreement. */
    calculationAndTiming: CalculationAndTimingInitialMargin,
}


/**
 * The set of elections which specify a Variation Margin Credit Support Annex or Deed.
 */
export interface CreditSupportAgreementVariationMarginElections extends CreditSupportAgreementElectionsBase {
    /** The reference currency for the purpose of specifying the FX Haircut relating to a posting obligation, as being either the Termination Currency or an FX Designated Currency. */
    fxHaircutCurrency?: FxHaircutCurrency,
    /** The bespoke exchange date terms that might be specified by the parties to the agreement. */
    exchangeDate?: string,
    /** The specification of transactions covered by the terms of the agreement. */
    coveredTransactions: CoveredTransactions,
    /** The specification of whether the standard Credit Support Offset provisions are applicable (true) or not applicable (false). */
    creditSupportOffsets: boolean,
    /** The Custodian and Segregated Account details in respect of each party to the agreement. */
    custodyArrangements?: CustodyArrangements,
    /** The optional specification of address for transfer as specified by the respective parties to the agreement. */
    addressesForTransfer?: AddressesForTransfer,
    /** The bespoke definition of other agreement terms as specified by the parties to the agreement. */
    otherAgreements?: OtherAgreements,
    /** The party to whom obligations under the Master Agreement are owed and secured by the credit support arrangements. Applicable to 1994 NY CSA, 1995 English Law CSD and 2016 New York Law VM CSA. */
    securityInterestForObligations: SecurityInterestForObligations,
    /** The Credit Support Obligations applicable to the agreement. */
    creditSupportObligations: CreditSupportObligationsVariationMargin,
    /** The set of elections for determining Valuation and Timing terms specific to the agreement. */
    calculationAndTiming: CalculationAndTimingVariationMargin,
}


/**
 * The set of elections which specify a Legacy (1994 or 1995) Credit Support Annex or Deed.
 */
export interface CreditSupportAgreementLegacyElections extends CreditSupportAgreementElectionsBase {
    /** The bespoke exchange date terms that might be specified by the parties to the agreement. */
    exchangeDate?: string,
    /** The optional specification of address for transfer as specified by the respective parties to the agreement. */
    addressesForTransfer?: AddressesForTransfer,
    /** The party to whom obligations under the Master Agreement are owed and secured by the credit support arrangements. Applicable to 1994 NY CSA, 1995 English Law CSD and 2016 New York Law VM CSA. */
    securityInterestForObligations?: SecurityInterestForObligations,
    /** Where only a single  party is explicitly designated to post collateral, the party which is specified as the sole poster of collateral. */
    singlePostingParty?: SinglePostingParty,
    /** The Credit Support Obligations applicable to the agreement. */
    creditSupportObligations: CreditSupportObligationsLegacy,
    /** The set of elections for determining Valuation and Timing terms specific to the agreement. */
    calculationAndTiming: CalculationAndTimingLegacy,
}


/**
 * A logical container designed to hold a set of related data. In this case, designed to hold a set of clauses that are common amongst Legacy (1994/95), Variation Margin and Initial Margin Credit Support Documents.
 */
export interface CreditSupportAgreementElectionsBase {
    /** The base and eligible currency(ies) for the document as specified by the parties to the agreement. */
    baseAndEligibleCurrency: BaseAndEligibleCurrency,
    /** The set of elections that may overwrite the default Condition Precedent provision, and the set of provisions that are deemed Access Condition. */
    conditionsPrecedent?: ConditionsPrecedent,
    /** The conditions under which the Security Provider can substitute posted collateral. */
    substitution?: Substitution,
    /** The election terms under which a party disputes (i) the Calculation Agents calculation of a Delivery Amount or a Return Amount, or (ii) the Value of any Transfer of Eligible Credit Support or Posted Credit Support. */
    disputeResolution: DisputeResolution,
    /** The elections for the holding and using of posted collateral by the respective parties to the Credit Support Annex for Variation Margin. */
    holdingAndUsingPostedCollateral?: HoldingAndUsingPostedCollateral,
    /** The Distributions and Interest Payment terms specified as part of the agreement. */
    distributionAndInterestPayment?: DistributionAndInterestPayment,
    /** The Other Eligible Support elections associated with margin agreements. */
    otherEligibleAndPostedSupport?: OtherEligibleAndPostedSupport,
    /** The optional specification of address where the demands, specifications and notices will be communicated to for each of the parties to the agreement. */
    demandsAndNotices?: DemandsAndNotices,
    /** The specification Additional Representations that may be applicable to the agreement. */
    additionalRepresentations?: AdditionalRepresentations,
    /** Defines the dates agreed by the parties as the date of the Credit Support Annex or Credit Support Deed. */
    masterAgreementDatedAsOfDate?: MasterAgreementDatedAsOfDate,
    /** A clause providing that final return collateral is to be provided without consideration of any Minimum Transfer Amount and collateral haircut. */
    finalReturns: FinalReturns,
}


/**
 * The set of elections which specify a Collateral Transfer Agreement.
 */
export interface CollateralTransferAgreementElections {
    /** The Regime Table provision , which determines the regulatory regime(s) applicable to each of the parties to the agreement. */
    regime: Regime,
    /** The determination of whether the One Way Provisions are applicable (true) or not applicable (false). */
    oneWayProvisions: OneWayProvisions,
    /** The specification of the ISDA SIMM Method for all Covered Transactions with respect to all Regimes. */
    generalSimmElections?: GeneralSimmElections,
    /** The qualification of whether cross-currency swaps need to be identified in the Confirmation so that the obligations to exchange principal be disregarded for the purpose of determining the Delivery Amount or Return Amount. */
    identifiedCrossCurrencySwap?: boolean,
    /** The specification of methodologies to compute sensitivities specific to the agreement. */
    sensitivityMethodologies: SensitivityMethodologies,
    /** The reference currency for the purpose of specifying the FX Haircut relating to a posting obligation, as being either the Termination Currency or an FX Designated Currency. */
    fxHaircutCurrency?: FxHaircutCurrency,
    /** The security providers posting obligations. */
    postingObligations: PostingObligations,
    /** The specification of Additional regimes for purposes of determining whether a Regulatory Event has occurred. */
    substitutedRegime?: SubstitutedRegime[],
    /** The base and eligible currency(ies) for the document as specified by the parties to the agreement. */
    baseAndEligibleCurrency: BaseAndEligibleCurrency,
    /** The Credit Support Obligations applicable to the agreement. */
    creditSupportObligations: CreditSupportObligationsCollateralTransferAgreement,
    /** The set of elections for determining Valuation and Timing terms specific to the agreement. */
    calculationAndTiming: CalculationAndTimingCollateralTransferAgreement,
    /** The set of elections that may overwrite the default Condition Precedent provision, and the set of provisions that are deemed Access Condition. */
    conditionsPrecedent?: ConditionsPrecedent,
    /** The conditions under which the Security Provider can substitute posted collateral. */
    substitution?: Substitution,
    /** The election terms under which a party disputes (i) the Calculation Agents calculation of a Delivery Amount or a Return Amount, or (ii) the Value of any Transfer of Eligible Credit Support or Posted Credit Support. */
    disputeResolution: DisputeResolution,
    /** The bespoke provisions that might be specified by the parties to the agreement to specify the rights of Security Taker and/or Security Provider when an Early Termination or Access Condition event has occurred. */
    rightsEvents?: RightsEvents,
    /** The Custodian and Segregated Account details in respect of each party to the agreement. */
    custodyArrangements?: CustodyArrangements,
    /** The specification Additional Representations that may be applicable to the agreement. */
    additionalRepresentations?: AdditionalRepresentations,
    /** The optional specification of address where the demands, specifications and notices will be communicated to for each of the parties to the agreement. */
    demandsAndNotices?: DemandsAndNotices,
    /** The optional specification of address for transfer as specified by the respective parties to the agreement. */
    addressesForTransfer?: AddressesForTransfer,
    /** The bespoke definition of other agreement terms as specified by the parties to the agreement. */
    otherAgreements?: OtherAgreements,
    /** The bespoke provision that might be specified by the parties to the agreement applicable to Termination Currency.  Unless specified the definition of Termination Currency has the meaning specified in the Schedule to the ISDA Master Agreement. */
    terminationCurrencyAmendment: TerminationCurrencyAmendment,
    /** The bespoke provision that might be specified by the parties to the agreement applicable to Minimum Transfer Amount.  Unless specified the definition of Minimum Transfer Amount in any Other Regulatory CSA has the meaning specified in such Other Regulatory CSA. */
    minimumTransferAmountAmendment: MinimumTransferAmountAmendment,
    /** The bespoke provision that might be specified by the parties to the agreement applicable to Interpretations. */
    interpretationTerms?: string,
    /** The Process Agent that might be appointed by the parties to the agreement. */
    processAgent?: ProcessAgent,
    /** The jurisdiction specific terms relevant to the agreement. */
    jurisdictionRelatedTerms?: JurisdictionRelatedTerms,
    /** Any additional amendments that might be specified by the parties to the agreement. */
    additionalAmendments?: string,
    /** Any additional terms that might be specified applicable. */
    additionalBespokeTerms?: string,
    /** The terms of the Rider for the ISDA Euroclear 2019 Collateral Transfer Agreement with respect to the use of a Pledgee Representative attached to this Agreement. */
    pledgeeRepresentativeRider?: PledgeeRepresentativeRider,
    /** A clause providing that final return collateral is to be provided without consideration of any Minimum Transfer Amount and collateral haircut. */
    finalReturns?: FinalReturns,
}


/**
 * The set of elections which specify a Security Agremeent.
 */
export interface SecurityAgreementElections {
    /** The pledged account associated with the agreement. */
    pledgedAccount?: Account,
    /** Enforcement Events specific to the agreement. */
    enforcementEvent?: EnforcementEvent,
    /** Delivery In Lieu rights. */
    deliveryInLieuRight?: boolean,
    /** Full Discharge condition. */
    fullDischarge?: boolean,
    /** The election for the Valuation of Appropriate Collateral. */
    appropriatedCollateralValuation?: AppropriatedCollateralValuation,
    /** The Process Agent that might be appointed by the parties to the agreement. */
    processAgent?: ProcessAgent,
    /** The jurisdiction specific terms. */
    jurisdictionRelatedTerms?: JurisdictionRelatedTerms,
    /** Any additional amendments that might be specified by the parties to the agreement. */
    additionalAmendments?: string,
    /** Any additional terms that might be specified applicable. */
    additionalBespokeTerms?: string,
    /** The location and language of execution to determine duty to be paid. */
    executionTerms?: ExecutionTerms,
}


/**
 * Posted Credit Support item with corresponding Valuation Percentage, FX Haircut Percentage and any related disputed Posted Credit Support valuation.
 */
export interface PostedCreditSupportItem {
    /** The Base Currency Equivalent of Cash or Security. */
    cashOrSecurityValue: Money,
    /** Valuation Percentage means, for any item of Eligible Collateral (IM), the percentage specified in accordance with Paragraph 13. */
    haircutPercentage: string,
    /** FX Haircut Percentage means, for any item of Eligible Collateral (IM), the percentage specified in accordance with Paragraph 13. */
    fxHaircutPercentage?: string,
    /** Percentage value of any additional haircut to be applied to a collateral asset,the percentage value is expressed as the discount haircut to the value of the collateral- as an example a 5% haircut would be expressed as 0.05. */
    additionalHaircutPercentage?: string,
    /** Paragraph 5. If a party (a 'Disputing Party') disputes the Value of any Posted Credit Support (IM). */
    disputedCashOrSecurityValue: Money,
}


/**
 * A class to specify the Additional Representation.
 */
export interface AdditionalRepresentation {
    /** A qualification as to whether the Additional Representation is applicable. */
    partyElection: AdditionalRepresentationElection[],
    /** A supplemental custom election that might be specified by the parties for the purpose of specifying the Additional Representation. */
    customElection?: string,
}


/**
 * A class to specify the parties' Additional Representation(s) election.
 */
export interface AdditionalRepresentationElection {
    /** The elective party. */
    party: string,
    /** The Additional Representation is applicable when True, and not applicable when False. */
    isApplicable: boolean,
    /** Details of any Additional Representations given by the relevant parties in relation to the collateral arrangement. */
    additionalLanguage?: string,
}


/**
 * A class to specify Additional Representations that may be applicable to an agreement.
 */
export interface AdditionalRepresentations {
    /** The specification of the Additional Representation that may be applicable to the agreement. */
    additionalRepresentation?: AdditionalRepresentation,
    /** The qualification of whether Additional Information related to Regulatory Compliance and Concentration Limits is applicable or not. */
    regulatoryComplianceRepresentation?: boolean,
}


/**
 * A class to specify the Pledgor/Obligor/Chargor Additional Rights Event election.
 */
export interface AdditionalRightsEvent {
    /** The Pledgor Additional Rights Event election is applicable when True, and not applicable when False. */
    isApplicable: boolean,
    /** The qualification of the Pledgor Additional Rights Event election, when specified. */
    qualification?: string,
}


/**
 * A class to specify an optional termination event, additional to the Termination Events that will be deemed an Access Condition (Initial Margin CSA) or a Specified Condition (Variation Margin CSA).
 */
export interface AdditionalTerminationEvent {
    /** The name of the additional termination event. */
    name: string,
    /** Whether the additional termination event is applicable for the relevant party. */
    applicableParty: string,
}


/**
 * The specification of the Additional Type of transaction that can require the collection or delivery of initial margin under a given regulatory regime for the purposes of Covered Transactions.
 */
export interface AdditionalType {
    /** The qualification of the Additional Type of transaction that can require the collection or delivery of initial margin when specified as a standard value. */
    standardValue: string,
    /** The qualification of the Additional Type of transaction that can require the collection or delivery of initial margin when specified as a custom value by the parties to the legal agreement. */
    customValue?: string,
}


/**
 * A class to specify the effective date of the Amendment to Termination Currency. This date can be specified as either an actual date, a specific date (e.g. the annex date) or as a custom provision.
 */
export interface AmendmentEffectiveDate {
    /** The effective date of the Amendment to Termination Currency when specified as an actual date. */
    date?: date,
    /** The effective date of the Amendment to Termination Currency when specified as relative to another date (e.g. the annex date). */
    specificDate?: string,
    /** The effective date of the Amendment to Termination Currency when specified as a non normalized custom provision. */
    customProvision?: string,
}


/**
 * A class to specify the applicable regulatory regime(s) that parties to a legal agreement, such as the ISDA 2016 and 2018 CSA for Initial Margin, might be subject to.
 */
export interface ApplicableRegime {
    /** The applicable regulatory regime, as specified through an enumeration. */
    regime?: string,
    /** The additional regulatory regime as specified by the parties. */
    additionalRegime?: string,
    /** A class that is used by the ApplicableRegime and the AdditionalRegime classes to specify the terms that are specific to each party and regime which are referred to in the Regime Table as part of certain legal agreements, such as the ISDA 2016 and 2018 CSA for Initial Margin. */
    regimeTerms: RegimeTerms[],
    additionalType?: string,
    /** The bespoke Additional Type for the purposes of Covered Transactions (IM). */
    additionalTerms?: string,
}


/**
 * A class to specify the Valuation of Appropriated Collateral.
 */
export interface AppropriatedCollateralValuation {
    /** The qualification of whether the Valuation of Appropriate Collateral provision is applicable (True) or not applicable (False). */
    isSpecified: boolean,
    /** The parties' election that qualify the Valuation of Appropriate Collateral in the case where it is deemed applicable. */
    election?: string,
}


/**
 * A class to specify bespoke Calculation Date terms for the purposes of Initial Margin.
 */
export interface BespokeCalculationDate {
    /** Additional Calculation Date terms are applicable when True and not applicable when False. */
    isApplicable: boolean,
    /** The Additional Calculation Date terms for the purposes of Initial Margin. */
    calculationDateImTerms?: string,
}


/**
 * A class to specify additional Calculation Time terms for the purposes of Initial Margin.
 */
export interface BespokeCalculationTime {
    /** If set to True, the Calculation Time for Initial Margin is the time as of which the Calculation Agent (IM) computes its end of day valuations of derivatives transactions. */
    asCalculationAgent: boolean,
    /** Additional Terms applicable to Calculation Time for Initial Margin. */
    bespokeCalculationTimeTerms?: string,
}


/**
 * A class to specify any bespoke Transfer Timing language by each party to the agreement.
 */
export interface BespokeTransferTiming {
    /** A boolean flag to specify whether bespoke transfer terms are applicable or not. */
    isApplicable: boolean,
    /** The bespoke transfer timing terms applicable to the agreement. */
    bespokeTransferTimingTerms?: string,
}


/**
 * Details of the party calculating the value of collateral to be delivered or returned.
 */
export interface CalculationAgentTerms {
    /** The party which is specified as Calculation Agent for Initial Margin. */
    party?: string,
    /** The Calculation Agent (IM) terms when specified. */
    bespokeCalculationAgentTerms?: string,
}


/**
 * Specification of the Calculation, Valuation and Timing terms specific to the Legacy (1994/95) Credit Support Annex.
 */
export interface CalculationAndTimingLegacy extends CalculationAndTimingBase {
    /** Details of the days on which calculations are to be made to determine the Delivery Amount and/or Return Amount. */
    valuationDate: CSAValuationDate,
    /** Specifies the time on which the value of collateral or exposure are calculated in accordance with the agreement. */
    valuationTime: ValuationTime,
    /** Details of the party calculating the value of collateral to be delivered or returned. */
    valuationAgent: ValuationAgent,
}


/**
 * Specification of the Calculation, Valuation and Timing terms specific to the Variation Margin Credit Support Annex.
 */
export interface CalculationAndTimingVariationMargin extends CalculationAndTimingBase {
    /** Details of the days on which calculations are to be made to determine the Delivery Amount and/or Return Amount. */
    valuationDate: CSAValuationDate,
    /** Specifies the time on which the value of collateral or exposure are calculated in accordance with the agreement. */
    valuationTime: ValuationTime,
    /** Details of the party valuating the collateral to be delivered or returned. */
    valuationAgent: ValuationAgent,
    /** The specified location where the credit exposure will be calculated by the respective parties. */
    valuationDateLocation: ValuationCalculationDateLocation,
}


/**
 * Specification of the Calculation, Valuation and Timing terms specific to the Initial Margin Credit Support Annex.
 */
export interface CalculationAndTimingInitialMargin extends CalculationAndTimingBase {
    /** Details of the party calculating the value of collateral to be delivered or returned. */
    calculationAgentTerms: CalculationAgentTerms,
    /** The specification of bespoke Calculation Date terms for the purposes of Initial or Variation Margin by the parties to the agreement. */
    bespokeCalculationDate?: BespokeCalculationDate,
    /** Bespoke terms to describe the time as of which such party (or the Calculation Agent (IM) (if applicale)) computes its end of day valuations of derivatives transactions. */
    bespokeCalculationTime?: BespokeCalculationTime,
    /** The specified location where the credit exposure will be calculated by the respective parties. */
    calculationDateLocation: ValuationCalculationDateLocation,
    cashSettlementDay?: string,
    securitiesSettlementDay?: string,
}


/**
 * Specification of the Calculation, Valuation and Timing terms specific to the Initial Margin Credit Support Annex.
 */
export interface CalculationAndTimingCollateralTransferAgreement extends CalculationAndTimingBase {
    /** Details of the party calculating the value of collateral to be delivered or returned. */
    calculationAgentTerms?: CalculationAgentTerms,
    /** The specification of bespoke Calculation Date terms for the purposes of Initial or Variation Margin by the parties to the agreement. */
    bespokeCalculationDate?: BespokeCalculationDate,
    /** Bespoke terms to describe the time as of which such party (or the Calculation Agent (IM) (if applicale)) computes its end of day valuations of derivatives transactions. */
    bespokeCalculationTime?: BespokeCalculationTime,
    /** The specified location where the credit exposure will be calculated by the respective parties. */
    calculationDateLocation?: ValuationCalculationDateLocation,
    cashSettlementDay?: string,
    securitiesSettlementDay?: string,
}


/**
 * A logical container designed to hold a set of related data. In this case, designed to hold a set of clauses that are common amongst Legacy (1994/95), Variation Margin and Initial Margin Credit Support Documents in regards to Calculation, Valuation and Timing terms.
 */
export interface CalculationAndTimingBase {
    /** The time by which a demand for the Transfer of Eligible Credit Support (IM) or Posted Credit Support (IM) needs to be made in order for the transfer to take place in accordance with the Transfer Timing provisions. */
    notificationTime: NotificationTime,
}


/**
 * A class to specify the ISDA SIMM Calculation Currency.
 */
export interface CalculationCurrencyElection {
    /** The party which the SIMM Calculation Currency qualification applies to. */
    party: string,
    /** The SIMM Calculation Currency (also known as SIMM Reporting Currency) means the Base Currency when True. It means a different currency when False. In that latter case, the SIMM Calculation Currency is specified as part of the currency attribute. */
    isBaseCurrency: boolean,
    /** The currency in which the ISDA SIMM Calculation is denominated, when different from the Base Currency. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
}


/**
 * Specification of the Calculation Date Location for the respective parties to the legal agreement.
 */
export interface ValuationCalculationDateLocation {
    /** The parties' calculation date location election. */
    partyElection: ValuationCalculationDateLocationElection[],
}


/**
 * A class to specify each of the party elections with respect to the Calculation Date Location.
 */
export interface ValuationCalculationDateLocationElection {
    /** The elective party. */
    party: string,
    /** The Calculation Date Location when specified as a business center which corresponds to the FpML list of business centers or can be mapped to it. */
    businessCenter?: string,
    /** The Calculation Date Location when specified a location which doesn't correspond to the FpML list of business centers or cannot be mapped to it. */
    customLocation?: string,
}


/**
 * A class to specify Collateral Access Breach language.
 */
export interface CollateralAccessBreach {
    /** Collateral Access Breach terms are applicable when True and not applicable when False. */
    isApplicable: boolean,
    /** Determination of whether the Collateral Access Breach end date is a number of days (True) or specified (False). */
    cabEndDateElection?: boolean,
    /** The business days following the related Collateral Access Breach when the additional terms end. */
    cabEndDate?: string,
    /** Specific terms for when Collateral Access Breach terms end. */
    cabEndDateTerms?: string,
}


/**
 * A class to specify the Collateral Management Agreement election.
 */
export interface CollateralManagementAgreement {
    /** The parties' Collateral Management Agreement election. */
    partyElection: CollateralManagementAgreementElection[],
}


/**
 * A class to specify the Collateral Management Agreement election.
 */
export interface CollateralManagementAgreementElection {
    /** The elective party. */
    party: string,
    /** The designated Collateral Management Agreement with respect to the elective party as the Obligee. */
    collateralManagementAgreement: string,
}


/**
 * A class to specify the rounding methodology applicable to the Delivery Amount and the Return Amount.
 */
export interface CollateralRounding {
    /** The nearest integral multiple of Base Currency units to which the Delivery Amount will be rounded in accordance with the specified rounding direction. */
    deliveryAmount: string,
    /** The rounding rule applicable to the Delivery Amount (which can be (i) up to nearest; (ii) down to nearest). */
    deliveryDirection: string,
    /** The nearest integral multiple of Base Currency units to which the Return Amount will be rounded in accordance with the specified rounding direction. */
    returnAmount: string,
    /** The rounding rule applicable to the Return Amount (which can be (i) up to nearest; (ii) down to nearest). */
    returnDirection: string,
    /** The currency in which the Delivery Amount and Return Amount rounding amounts are specified. */
    currency: string,
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    other?: string,
}


/**
 * A class to specify the two set of elections that may overwrite the default Condition Precedent provision.
 */
export interface ConditionsPrecedent {
    /** The election to specify whether the standard Conditions Precedent apply. */
    conditionsPrecedentElection?: string,
    /** The custom provisions that might be specified by the parties to the agreement for the purpose of overwriting the default Condition Precedent provision specified in the CSA. */
    customProvision?: string,
    /** Specifies the events elected by the parties that are deemed an Access Condition (for Initial Margin CSA) or a Specified Condition (for Variation Margin and Legacy CSA), which, if triggered, may temporarily or permanently suspend rights to rehypothecate, transfer, or substitute collateral. */
    specifiedConditionOrAccessCondition?: SpecifiedConditionOrAccessCondition,
}


/**
 * A class to specify the relationship between the Control Agreement and the Credit Support Agreement.
 */
export interface ControlAgreement {
    /** The party specific elections. */
    partyElection: ControlAgreementElections[],
}


/**
 * A class to specify the Control Agreement election sby each party to the agreement.
 */
export interface ControlAgreementElections {
    /** The elective party. */
    party: string,
    /** The identification of whether the Control Agreement is a Credit Support Document with respect to each party. */
    controlAgreementAsCsd: boolean,
    /** Unless specified as inapplicable in the event of any inconsistency between this Deed and the Control Agreement, this Deed will prevail over the Control Agreement. */
    consistencyWithControlAgreement?: boolean,
    /** Unless specified as inapplicable the parties recognise that the Control Agreement is a means by which the parties can perform their obligations. */
    relationshipWithControlAgreement?: boolean,
}


/**
 * A class to specify Control Agreement language related to delivery of a Notice of Exclusive Control.
 */
export interface ControlAgreementNecEvent {
    /** Indicates party specific Control Agreement language related to delivery of a Notice of Exclusive Control. */
    controlAgreementNecEventElection: ControlAgreementNecEventElection[],
}


/**
 * A class to specify party specific Control Agreement language related to delivery of a Notice of Exclusive Control.
 */
export interface ControlAgreementNecEventElection {
    /** The elective party. */
    party: string,
    /** Indicates Control Agreement language related to delivery of a Notice of Exclusive Control. */
    necEvent: boolean,
}


/**
 * A class to specify the custodian and custody account details for each party to the agreement.
 */
export interface Custodian {
    /** The party specific elections. */
    partyElection: CustodianElection[],
}


/**
 * A class to specify the custodian and custody account details for each party to the agreement.
 */
export interface CustodianElection {
    /** The elective party. */
    party: string,
    /** The custody agent. While CreateiQ only specifies the custodian's name, specifying the legal entity as part of the CDM is deemed more appropriate, while this will still provide the ability to accommodate situations where only the entity name is available, as the entityId attribute is optional as part of the LegalEntity class. */
    custodian?: LegalEntity,
    /** The identification of the segregated cash account for the purpose of holding cash collateral. */
    segregatedCashAccount?: Account,
    /** The identification of the segregated security account for the purpose of holding security collateral. */
    segregatedSecurityAccount?: Account,
    /** Details how the Custodian is determined. */
    legacyVMCustodian?: string,
    /** The identity of the relevant custodian where Named Entity is elected. */
    legacyNamedEntity?: LegalEntity,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * A class to specify the Custodian Event.
 */
export interface CustodianEvent {
    /** The qualification as to whether the Custodian Event (English Law & New York Law ISDA CSA) or the Collateral Manager Event (Japanese Law ISDA CSA) is applicable. */
    isApplicable: boolean,
    /** The qualification of the Custodian Event (English Law & New York Law ISDA CSA) or Collateral Manager Event (Japanese Law ISDA CSA) End Date. */
    endDate?: CustodianEventEndDate,
}


/**
 * A class to specify the Custodian Event.
 */
export interface CustodianEventEndDate {
    /** Election to specify the number of days after the occurrence of the Custodian Event/Collateral Management Event for the purpose of qualifying the CE/CME End Date. */
    daysAfterCustodianEvent: CustomisableOffset,
    /** Election to specify the number of days prior to the termination of the Control Agreement /Collateral Management Event for the purpose of qualifying the CE/CME End Date, in the case where advance notice is given. */
    releaseDate: CustomisableOffset,
    /** The parties' election to specify the number of days prior to the end of the safekeeping period (Clearstream CTA) purpose of qualifying the CE/CME End Date, in the case where advance notice is given. */
    safekeepingPeriodExpiry?: CustomisableOffset,
    /** The parties' election to specify the number of days one party has effectively provided the Timely Statement to the other party. */
    dateOfTimelyStatement: CustomisableOffset,
}


/**
 * A class to specify the Custodian Risk elections specific to a Credit Support Agreement.
 */
export interface CustodianRisk {
    /** The party specific elections. */
    partyElection: CustodianRiskElection[],
}


/**
 * A class to specify the Custodian Risk.
 */
export interface CustodianRiskElection {
    /** The elective party. */
    party?: string,
    /** The qualification as to whether the risk is deemed as Specified. */
    isSpecified: boolean,
    /** The Custodian Risk or Collateral Manager Risk qualification. */
    qualification?: string,
}


/**
 * A class to specify the requirements applicable to the custodian with respect to the holding of posted collateral.
 */
export interface CustodianTerms {
    /** The minimal level of assets requirement with respect to the custody agent. */
    minimumAssets?: Money,
    /** The minimal rating requirement with respect to the custody agent. */
    minimumRating?: CreditNotation,
    /** Election to specify the initial custodian. */
    initialDesignation?: LegalEntity,
}


/**
 * Describes how posted collateral is held, maintained, and safeguarded by a custodian or third-party agent, in accordance with the terms of the governing agreement.
 */
export interface CustodyArrangements {
    /** Unless specified as True, the Control Agreement is not a Credit Support Document under the agreement with respect to a party. */
    isCreditSupportDocument?: boolean,
    /** Control Agreement language is specified when True. */
    hasControlAgreementLanguage?: boolean,
    /** Other Custom Provisions of Custody Arrangements. */
    otherProvisions?: string,
    /** The elections specific to Collateral Access Breach language. */
    collateralAccessBreach?: CollateralAccessBreach,
    /** The qualification of the Custodian Risk. */
    custodianRisk?: CustodianRisk,
    /** The party-specific election with respect to the control agreement. */
    controlAgreement?: ControlAgreement,
    /** Specifies if and until when a Custodian Event clause applies. */
    custodianEvent?: CustodianEvent,
    /** The custodian and segregated account details for each party to the agreement. */
    custodian?: Custodian,
    /** The designated Collateral Management Agreement with respect to the elective party as the Obligee. */
    collateralManagementAgreement?: CollateralManagementAgreement,
}


/**
 * A class to specify the election terms under which a party disputes the Value of any Transfer of Eligible Credit Support or Posted Credit Support.
 */
export interface DisputeResolution {
    /** The time by which the dispute needs to be resolved, failure of which would trigger a recalculation alongside a process that is specified as part of the agreement. */
    resolutionTime?: BusinessCenterTime,
    /** The custom Resolution Time election that might be specified by the parties. */
    otherTerms?: string,
    /** The alternative dispute resolution procedure if specified. */
    alternativeTerms?: string,
    /** The elections to specify terms for recalculation of the market value of posted collateral. */
    recalculationOfValue?: RecalculationOfValue,
    /** Alternative procedures to those in the pre-print for resolving collateral related disputes. */
    legacyAlternative?: LegacyResolutionAlternative,
    /** Methodology for resolving disputes in respect of the determination of the value of collateral. */
    value?: ResolutionValue,
}


/**
 * A class to specify the party elective amounts which can be used for the purpose of specifying elections such as the ISDA CSA Threshold and Minimum Transfer Amount.
 */
export interface ElectiveAmountElection {
    /** The elective party. */
    party: string,
    /** Specifies an enumerated election to express the elective amount. */
    electiveAmount?: string,
    /** The elective amount when expressed as a currency amount. The associated PartyElectiveAmount_amount data rule enforces that the currency amount is actually greater than 0. */
    amount?: Money,
    /** The elective amount when expressed as a custom election by the party. */
    customElection?: string,
}


/**
 * A class to specify Enforcement Events specific to Security Agreements.
 */
export interface EnforcementEvent {
    /** The early termination election. */
    earlyTerminationDate: boolean,
    /** The failure to pay election. */
    failureToPay?: boolean,
}


/**
 * A class to specify execution language terms of a Security Agreement.
 */
export interface ExecutionLanguage {
    /** A boolean attribute to determine if standard language is applicable or not. */
    standardLanguage: boolean,
    /** The number of original documents. */
    numberOfOriginals?: string,
    /** Bespoke execution language to be included when specified. */
    otherLanguage?: string,
}


/**
 * A class to specify execution location terms of a Security Agreement.
 */
export interface ExecutionLocation {
    /** The execution location of the agreement. */
    executionLocation: string,
    /** Bespoke execution location language to be included when specified. */
    otherLanguage?: string,
    /** The payer of documentary duty. */
    dutyPayer?: string,
    /** Bespoke terms specific to the payment of documentary duty. */
    dutyPayerLanguage?: string,
    /** The date that documentary duty will be paid. */
    dutyPaymentDate?: date,
    /** Bespoke terms specific to the date that documentary duty will be paid. */
    dutyPaymentLanguage?: string,
}


/**
 * A class to specify execution location and language of execution to determine duty to be paid.
 */
export interface ExecutionTerms {
    /** The bespoke execution language election. */
    executionLanguage: ExecutionLanguage,
    /** The bespoke execution location election. */
    executionLocation: ExecutionLocation,
}


/**
 * A class to specify party specific elections when a Collateral Transfer Agreement is governed by French Law.
 */
export interface FrenchLawAddendum {
    /** The qualification of whether the French Law Addendum is deemed applicable by the parties (True) or not (False). */
    isApplicable: boolean,
    /** The parties French Law Addendum Elections. */
    partyElection?: FrenchLawAddendumElection[],
}


/**
 * A class to specify party specific French Law Addendum language.
 */
export interface FrenchLawAddendumElection {
    /** The elective party. */
    party: string,
    /** The qualification of whether the party elects specific language. */
    isApplicable: boolean,
    /** The party specific language to be included in the agreement. */
    addendumLanguage?: string,
}


/**
 * A class to specify the reference currency for the purpose of specifying the FX Haircut relating to a posting obligation, as being either the Termination Currency or an FX Designated Currency.
 */
export interface FxHaircutCurrency {
    /** The reference currency for the purpose of specifying the FX Haircut relating to a posting obligation is the Termination Currency when the Boolean value is set to True. */
    isTerminationCurrency: boolean,
    /** When specified, the reference currency for the purpose of specifying the FX Haircut relating to a posting obligation. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    fxDesignatedCurrency?: string,
}


/**
 * A class to specify the ISDA SIMM as the Method for all Covered Transactions with respect to all Regimes.
 */
export interface GeneralSimmElections {
    /** The qualification of the ISDA SIMM version that is specified for all Covered Transactions. */
    simmVersion?: SimmVersion,
    /** The SIMM Calculation Currency, as specified for each of the parties to the CSA Initial Margin. */
    simmCalculationCurrency: SimmCalculationCurrency,
}


/**
 * Specifies the conditions that need to be satisfied before the Secured Party or Custodian acting on its behalf can hold and use posted collateral.
 */
export interface HoldingAndUsingPostedCollateral {
    /** The parties' elections for the holding and using of posted collateral. */
    partyElection: HoldingAndUsingPostedCollateralElection[],
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Specifies the parties' elections related to the holding and using of posted collateral.
 */
export interface HoldingAndUsingPostedCollateralElection {
    /** The party to which the elections apply. */
    party: string,
    /** The specification of the conditions under which a party and its custodian(s) are entitled to hold posted collateral. */
    eligibilityToHoldCollateral: EligibilityToHoldCollateral,
    /** Indicates whether the party has the right to reuse, rehypothecate, or otherwise make use of the posted collateral. 'True' if the party has the right,'False' if the party does not have the right. */
    useOfPostedCollateral: boolean,
}


/**
 * Identifies collateral that has been designated as not meeting the eligibility criteria set out in the agreement.
 */
export interface IneligibleCreditSupport {
    /** Specifies the date from which all credit support provided by a party is treated as ineligible under the agreement. */
    totalIneligibilityDate?: string,
    /** Specifies the date from which the transfer of credit support by a party is treated as ineligible under the agreement. */
    transferIneligibilityDate?: string,
    /** Identifies a party designated in the agreement for the purpose of applying particular rights, obligations, or elections. */
    specifiedParty?: string,
}


/**
 * A class to specify whether the Interest Adjustment is applicable and what its periodicity is.
 */
export interface InterestAdjustment {
    /** The Interest Adjustment is applicable when True and not applicable when False. */
    isApplicable: boolean,
    /** The qualification of the Interest Adjustment periodicity. */
    periodicity: InterestAdjustmentPeriodicity,
}


/**
 * A class to specify the Interest Adjustment periodicity.
 */
export interface InterestAdjustmentPeriodicity {
    /** The Interest Adjustment periodicity when specified through a standardized election. */
    standardElection?: string,
    /** The Interest Adjustment periodicity when specified through a custom election. */
    customElection?: string,
}


/**
 * A class to specify the application of Interest Amount with respect to the Delivery Amount and the Return Amount.
 */
export interface InterestAmount {
    /** The application of Interest Amount with respect the Return Amount. */
    returnAmount: ReturnAmount,
    /** The application of Interest Amount with respect the Delivery Amount. */
    deliveryAmount: DeliveryAmount,
}


/**
 * A class to specify Japanese Securities Provision elections.
 */
export interface JapaneseSecuritiesProvisions {
    /** Japanese Securities Provisions are applicable when True and Not Applicable when False. */
    isApplicable: boolean,
    /** Recommended Japanese Securities Provisions are applicable when True, additional Provisions are specified when False. */
    relevantProvisionsElection?: boolean,
    /** Specific terms applicable to Recommended Japanese Securities Provisions. */
    relevantProvisionsTerms?: string,
    /** Additional Amendments to Japanese Securities Provisions are specified when True, and not specified when False. */
    amendmentsToJapaneseProvisions?: boolean,
    /** Specific terms applicable to Additional Amendments to Japanese Securities Provisions. */
    amendmentsToJapaneseProvisionsTerms?: string,
    /** Specification of whether Clearstream Event amendment language is included (true) or excluded (false). */
    clearstreamAmendmentToJapaneseProvisions?: boolean,
}


/**
 * A class to specify terms jurisdiction related terms.
 */
export interface JurisdictionRelatedTerms {
    /** The Japanese Securities Provisions election. */
    japaneseSecuritiesProvisions?: JapaneseSecuritiesProvisions,
    /** Classification of optional exclusive jurisdiction terms. */
    exclusiveJurisdiction?: boolean,
    /** The Jury Waiver conditions specific to the agreement. */
    juryWaiver?: boolean,
    /** The French Law Addendum Provisions specific to the agreement. */
    frenchLawAddendum?: FrenchLawAddendum,
    /** The qualification of whether the Belgian Law Security Agreement Addendum is deemed applicable by the parties (True) or not (False). */
    belgianLawSecurityAgreement?: boolean,
}


/**
 * A class for selection of Margin Approach.
 */
export interface MarginApproach {
    /** Specifies the margin approach specific to Initial Margin agreements. */
    marginApproach: string,
}


/**
 *  A class to specify whether Amendment to Minimum Transfer Amount language is applicable or not.
 */
export interface MinimumTransferAmountAmendment {
    /** The definition of Minimum Transfer Amount in any Other Regulatory CSA will be amended when applicable. */
    isApplicable: boolean,
    /** The effective date of the Amendment to Termination Currency. */
    effectiveDate?: AmendmentEffectiveDate,
    /** The party elective amounts. */
    partyElections?: ElectiveAmountElection[],
}


/**
 * Specifies the time by which a demand for the Transfer of Eligible Credit Support or Posted Credit Support needs to be made in order for the transfer to take place in accordance with the Transfer Timing provisions.
 */
export interface NotificationTime {
    /** The parties' Notification Time election. */
    partyElections: NotificationTimeElection[],
    /** The determination of whether reference is made to dispute resolution notification timing in the agreement. */
    disputeNotificationReference?: boolean,
    /** The determination of whether transfer timing language is applicable or not. */
    transferTimingProviso?: boolean,
}


/**
 * A class to specify the notification time election by the respective parties to the agreement.
 */
export interface NotificationTimeElection {
    /** The elective party. */
    party: string,
    /** The Notification Time as a time that is qualified as a standard business center. */
    notificationTime?: BusinessCenterTime,
    /** The Notification Time as a bespoke election. */
    customNotification?: string,
    /** Whether the Notification Time is precluded on the day being a local business day. */
    localBusinessDay?: boolean,
}


/**
 * A class to specify whether One Way Provisions apply.
 */
export interface OneWayProvisions {
    /** The determination of whether the One Way Provisions are applicable (true) or not applicable (false). */
    isApplicable: boolean,
    /** The Posting Party for the purposes of One Way Provisions. It is specified in the case where the One Way Provision is deemed applicable. */
    postingParty?: string,
}


/**
 * The bespoke definition of other agreements or other credit support annex terms as specified by the parties to the agreement.
 */
export interface OtherAgreements {
    /** The bespoke definition of Other CSA as specified by the parties to the agreement. */
    otherCSA?: OtherAgreementTerms,
    /** The bespoke definition of whether Japanese Law CSA (VM) are specified by the parties to the agreement. */
    japaneseLawCsa?: OtherAgreementTerms,
}


/**
 * Specifies the Other Eligible Support elections.
 */
export interface OtherEligibleAndPostedSupport {
    /** The definition of 'Value' with respect to Other Eligible Support and Other Posted Support. */
    applicableValue?: boolean,
    /** The definition of 'Transfer' with respect to Other Eligible Support and Other Posted Support. */
    applicableTransfer?: boolean,
    /** Details how particular types of collateral are valued (other than in respect of a default). */
    value?: CollateralValueMethod,
}


/**
 * A class defining a legal agreement identifier issued by the indicated party.
 */
export interface PartyAgreementIdentifier {
    /** Party that issued the document identifier. */
    partyReference: Party,
    /** While FpML specifies the document identifier with a value and an associated scheme, the CDM makes use of the Identifier, which has an explicit issuer. The issuer of this identifier is not necessarily the same as the party reference. */
    documentIdentifier: Identifier[],
}


/**
 * A class to specify the collateral posting obligations of the security provider or security providers as specified in the corresponding agreement.
 */
export interface PostingObligations {
    /** The security provider party(ies) to which the posting obligations apply to, which can be either one of the parties to the legal agreement, or both of those. */
    securityProvider: string,
    /** The specification of the collateral posting obligations for the security provider party(ies). */
    partyElection: PostingObligationsElection[],
}


/**
 * Specifies the collateral posting obligations for the security provider party(ies).
 */
export interface PostingObligationsElection {
    /** Specifies the party providing the security. */
    party: string,
    /** If set to True, the Control Agreement is a Credit Support Document with respect to the party(ies). */
    asPermitted: boolean,
    /** The eligible collateral as specified in relation to the pledgor/chargor/obligor(s) posting obligation. */
    eligibleCollateral?: EligibleCollateralCriteria[],
    /** The excluded collateral as specified in relation to the pledgor/chargor/obligor(s) posting obligation. */
    excludedCollateral?: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Specifies the Process Agent that might be appointed by the parties as part of a Credit Support Annex/Deed or Collateral Transfer Agreement.
 */
export interface ProcessAgent {
    /** The parties' Process Agent election. */
    partyElection: ProcessAgentElection[],
}


/**
 * Specifies the parties' respective elections with respect to the Process Agent.
 */
export interface ProcessAgentElection extends ContactInformationElection {
    /** The qualification of whether the Process Agent is applicable (True) or not applicable (False). */
    isApplicable: boolean,
    /** Specification of the process agent entity. */
    entity?: LegalEntity,
    /** Additional information relating to the Process Agent. */
    additionalInformation?: string,
}


/**
 * A class to specify terms for Recalculation of the Market Value of Posted Collateral when a dispute has been failed to be resolved by Resolution Time.
 */
export interface RecalculationOfValue {
    /** The parties' Recalculation of Value terms. */
    partyElection: RecalculationOfValueElection[],
}


/**
 * A class to specify Recalculation of Value terms that will be applicable.
 */
export interface RecalculationOfValueElection {
    /** The elective party. */
    party: string,
    /** The procedure for Recalculation of Value. */
    recalculationOfValueElection: string,
    /** Additional Recalculation of Value terms when specified. */
    recalculationOfValueTerms?: string,
}


/**
 * A class to specify one or more regimes that may be specified as relevant to a legal agreement.
 */
export interface Regime {
    /** A class to specify the regime(s) that parties to a legal agreement, such as the ISDA 2016 and 2018 CSA for Initial Margin, might agree to apply to one or both parties when acting as collateral taker, and specific terms associated with that application. */
    applicableRegime: ApplicableRegime[],
    /** The specification of the number of days after effective delivery of notice that Mandatory method fallback applies. Specification is only required when one or more Regimes have Fall Back to Mandatory Method elected as a SIMM exception. */
    fallbackToMandatoryMethodDays?: string,
}


/**
 * A class that is used by the ApplicableRegime and the AdditionalRegime classes to specify the regulatory regime terms.
 */
export interface RegimeTerms {
    /** The party for which the regime terms are being specified when acting as collateral taker. */
    party: string,
    /** The specification of whether the regime is elected as applicable to the party when acting as collateral taker. */
    isApplicable?: string,
    /** The bespoke party specific Regime term elections applicable when specified. */
    asSpecified?: string,
    /** The election for SIMM exception to the regulatory regime clause. */
    simmException?: SimmException,
    /** Specifies the retrospective effect exception to the regulatory regime clause of Initial Margin documents as either a normalized value specified as part of an enumeration or a customized value specified of type string. */
    retrospectiveEffect?: RetrospectiveEffect,
}


/**
 * The terms of the Rider for the ISDA Euroclear 2019 Collateral Transfer Agreement with respect to the use of a Pledgee Representative attached to this Agreement.
 */
export interface PledgeeRepresentativeRider {
    /** Identification of whether the representative CTA provisions are applicable (True) or not applicable (False). */
    isApplicable: boolean,
    /** Identification of the represented party. */
    party?: string,
    /** The specific representative terms applicable when specified. */
    representativeTerms?: string,
    /** The specification of whether the representative event terms are applicable. */
    representativeEvent?: string,
    /** The specific representative event terms applicable when specified. */
    representativeEventTerms?: string,
    /** The definition of representative end date in relation to a representative event. */
    representativeEndDate?: CustomisableOffset,
}


/**
 * A class to specify the retrospective effect exception to the regulatory regime clause of Initial Margin documents as either a normalized value specified as part of an enumeration or a customized value specified of type string.
 */
export interface RetrospectiveEffect {
    /** The Standard Initial Margin Model exception when specified by the party according to one of the enumerated values. */
    standardisedException?: string,
    /** The Standard Initial Margin Model exception when specified as a customized approach by the party. */
    asSpecified?: string,
}


/**
 * A class to specify the rights of Security Taker and/or Security Provider when an Early Termination or Access Condition event has occurred.
 */
export interface RightsEvents {
    /** The bespoke provisions that might be specified by the parties to the agreement applicable to a Security Taker Rights Event. */
    securityTakerRightsEvent: SecuredPartyRightsEvent,
    /** The bespoke provisions that might be specified by the parties to the agreement applicable to a Notice of Exclusive Control Event. */
    controlAgreementNecEvent?: ControlAgreementNecEvent,
    /** The bespoke provisions that might be specified by the parties to the agreement applicable to a Security Provider Rights Event. */
    securityProviderRightsEvent: SecurityProviderRightsEvent,
    /** The specification of whether Delivery In Lieu language is applicable to the agreement (true) or not (false). */
    deliveryInLieuRight?: boolean,
    /** The Additional Rights Event election. */
    additionalRightsEvent?: AdditionalRightsEvent,
}


/**
 * A class to specify Secured Party Rights Event language.
 */
export interface SecuredPartyRightsEvent {
    securedPartyRightsEventElection?: SecuredPartyRightsEventElection[],
    /** A boolean attribute to specify whether Failure to Pay Early Termination language is included (True) or excluded (False) from the agreement. */
    earlyTerminationDateOptionalLanguage: boolean,
    /** A boolean attribute to specify whether Failure to Pay Early Termination language in the agreement is deemed applicable or not. */
    failureToPayEarlyTermination?: boolean,
}


/**
 * A class to specify party specific Secured Party Rights Event language.
 */
export interface SecuredPartyRightsEventElection {
    /** The elective party. */
    party: string,
    /** A boolean attribute to specify whether a Secured Party Rights Event will only occur upon the occurrence of one or more of the event specified in a Control Agreement. */
    rightsEvent: boolean,
}


/**
 * A class to specify the Pledgor/Obligor/Chargor Rights Event election.
 */
export interface SecurityProviderRightsEvent {
    /** A qualification as to whether the Additional Representation is applicable. */
    partyElection?: SecurityProviderRightsEventElection[],
    /** The Pledgor/Obligor/Chargor Rights Event election includes cooling off language when the attribute is set of True. */
    includeCoolingOffLanguage: boolean,
    /** If specified as applicable here, a Pledgor/Obligor/Chargor Rights Event will not occur unless the Pledgor/Obligor/Chargor (A) has provided a statement to the Secured Party in respect of such Early Termination Date. */
    fullDischarge?: boolean,
    /** The Automatic Set-Off provision applies when the value is set to True. */
    automaticSetOff?: boolean,
    /** A custom Pledgor/Obligor/Chargor Rights Event election might be specified by the parties. */
    customElection?: string,
}


/**
 * A class to specify party specific Secured Party Rights Event language.
 */
export interface SecurityProviderRightsEventElection {
    /** The elective party. */
    party: string,
    /** If True, the Rights Event language applies for the specified Party. */
    rightsEvent: boolean,
}


/**
 * A class to specificy methodologies to compute sensitivities specific to the agreement.
 */
export interface SensitivityMethodologies {
    /** Party specific elections for Sensitivity Methodology for Equity and Commodities. */
    partyElection: SensitivityMethodologiesPartyElection[],
}


/**
 * A class to specificy methodologies to compute sensitivities specific to the agreement.
 */
export interface SensitivityMethodologiesPartyElection {
    /** The party to which the election applies. */
    party: string,
    /** The methodology to compute sensitivities to equity indices, funds and ETFs for the purpose of Initial Margin agreements. */
    sensitivityToEquity: SensitivityToEquity,
    /** The methodology to compute sensitivities to commodity indices for the purpose of Initial Margin agreements. */
    sensitivityToCommodity: SensitivityMethodology,
}


/**
 * Represents risk sensitivity calculated with respect to the value of an individual equity instrument.
 */
export interface SensitivityToEquity {
    /** Represents risk sensitivity calculated with respect to the level of an equity or market index. */
    sensitivityToIndices: SensitivityMethodology,
    /** Represents risk sensitivity calculated with respect to the value of a fund, such as a mutual fund or collective investment vehicle. */
    sensitivityToFunds: SensitivityMethodology,
    /** Represents risk sensitivity calculated with respect to the value of an exchange-traded fund (ETF). */
    sensitivityToETFs: SensitivityMethodology,
}


/**
 * A class to specify the methodology according to which sensitivities are computed.
 */
export interface SensitivityMethodology {
    /** The methodology according to which sensitivities will be computed, when specified through a normalized election. */
    specifiedMethodology?: string,
    /** The methodology according to which sensitivities will be computed, when specified through a custom election. */
    customMethodology?: string,
}


/**
 * A class to specify the SIMM Calculation Currency elections by each party to the agreement.
 */
export interface SimmCalculationCurrency {
    /** The parties' SIMM Calculation Currency election. */
    partyElection: CalculationCurrencyElection[],
}


/**
 * A class to specify the SIMM exception to the regulatory regime clause.
 */
export interface SimmException {
    /** The Standard Initial Margin Model exception when specified by the party according to one of the enumerated values. */
    standardisedException?: string,
    /** The Standard Initial Margin model exception approach applicable when specified by the party according to one of the enumerated values. */
    simmExceptionApplicable?: string,
    /** The Standard Initial Margin Model exception when specified as a customized approach by the party. */
    asSpecified?: string,
}


/**
 * A class to specify the ISDA SIMM version that applies to the ISDA 2018 CSA for Initial Margin.
 */
export interface SimmVersion {
    /** A boolean attribute to determine whether the SIMM version is specified for the purpose of the legal agreement. */
    isSpecified?: boolean,
    /** The party which the specified SIMM version applies to. */
    partyVersion?: string,
    /** The SIMM version exception when specified as a customized approach by the party. */
    asSpecified?: string,
}


/**
 * A class to specify each party's election with respect to the Substituted Regimes that will be applicable...
 */
export interface SubstitutedRegime {
    /** The applicable regulatory regime, as specified through an enumeration. */
    regime?: string,
    /** The additional regulatory regime as specified by the parties. */
    additionalRegime?: string,
    /** Specifies the applicability of the Substituted Regime as denoted in the Substituted Regime Table as part of certain legal agreements, such as the ISDA 2016 and 2018 CSA for Initial Margin. */
    regimeTerms: SubstitutedRegimeTerms[],
}


/**
 * Specifies the applicability of the Substituted Regime as denoted in the Substituted Regime Table as part of certain legal agreements, such as such as the ISDA 2016 and 2018 CSA for Initial Margin.
 */
export interface SubstitutedRegimeTerms {
    /** The party for which the regime terms are being specified when acting as collateral taker. */
    party: string,
    /** The specification of whether the regime is elected as applicable to the party when acting as collateral taker. */
    isApplicable: boolean,
}


/**
 * A class to specify the conditions under which the Security Provider can substitute posted collateral. The details required for 'Legacy' agreements are substantially different to those required for newer agreements; where the agreement is a 1994 or 1995 CSA, or 1995 CSD, the legacyConsent attribute needs to be used which contains additional values beyond a simple true/false value.
 */
export interface Substitution {
    /** Party specific elections for substitution of posted collateral. */
    partyElection?: SubstitutionPartyElection[],
}


/**
 * Party specific elections for substitution of posted collateral.
 */
export interface SubstitutionPartyElection {
    /** The party that is acting as the Pledgor/Obligor/Chargor/Security Provider. */
    party: string,
    /** The election as to whether the Pledgor/Obligor/Chargor/Security Provider must obtain the Secured Partys consent for any collateral substitution. */
    needsConsent: boolean,
    /** Substitution Date has the meaning specified in Paragraph4(d)(ii), unless otherwise specified. */
    substitutionDateLanguage?: string,
    /** Specific consent language might be specified by the parties. */
    specificConsentLanguage?: string,
    /** The process for substituting/exchanging one form of collateral held by a party for a different form of collateral, used with legacy agreements. */
    legacyConsent?: string,
}


/**
 * A class to specify the Amendment to Termination Currency elections by the parties to the agreement.
 */
export interface TerminationCurrencyAmendment {
    /** The qualification of whether the Amendment to Termination Currency is deemed applicable by the parties (True) or not (False). */
    isApplicable: boolean,
    /** The effective date of the Amendment to Termination Currency. This date can be specified as either an actual date, a specific date (e.g. the annex date) or as a custom provision. */
    effectiveDate?: AmendmentEffectiveDate,
    /** The parties' Amendment Currency election. */
    partyElection?: TerminationCurrencyElection[],
}


/**
 * A class to specify the Amendment to Termination Currency election by the parties to the agreement.
 */
export interface TerminationCurrencyElection {
    /** The elective party. */
    party: string,
    /** The qualification of whether the Termination Currency is specified in this document (True) or in an Eligible Support Credit Support (IM) Schedule (False). */
    isSpecified: boolean,
    /** The Termination Currency associated with the party that referenced as part of this class. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
}


/**
 * The election of party specific additional obligations applicable to the agreement.
 */
export interface AdditionalObligations {
    /** The party that the additional obligations apply to. */
    party: string,
    /** The party specific additional obligations applicable to the agreement. */
    additionalObligations: string,
}


/**
 * The base and eligible currency(ies) for the document as specified by the parties to the agreement.
 */
export interface BaseAndEligibleCurrency {
    /** The common agreed currency into which relevant amounts of all collateral arrangements between the parties are converted, or if not an actual currency, the process through which this is determined. Where hardcoded (e.g. under the 1994 ISDA Credit Support Annex (Security Interest  NY Law)), the currency that effectively performs this function. */
    baseCurrency: string,
    /** A flag detailing whether the Base Currency is set to  the Termination Currency as defined in the related  Master Agreement. */
    baseCurrencyTerminationCurrency: boolean,
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    baseCurrencyOther?: string,
    /** A definition of a currency agreed by the parties, typically to indicate the currencies of eligible cash collateral. */
    eligibleCurrency?: string,
    /** A flag detailing whether the Base Currency is included as an Eligible Currency. */
    eligibleCurrencyInclBaseCurrency: boolean,
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    eligibleCurrencyOther?: string,
}


/**
 * Specification of Transactions covered by the legal agreement.
 */
export interface CoveredTransactions {
    /** Includes any Transaction specified below that is entered into on or after the specified date. */
    inclusionDate?: date,
    /** Covered Transactions when expressed using the ISDA taxonomy. */
    coveredTransactions?: ProductTaxonomy[],
    /** Covered Transactions when not expressed using the ISDA taxonomy. */
    bespokeCoveredTransactions?: string[],
    /** The party specific additional obligations applicable to the document. */
    additionalObligations?: AdditionalObligations[],
    /** Specification of amendments to the calculation of Exposure in terms of the Transactions covered. */
    exposure?: ExposureScope,
}


/**
 * A logical container designed to hold a set of related data. In this case, designed to hold a set of clauses that are common amongst Legacy (1994/95), Variation Margin and Initial Margin Credit Support Documents in regards to Credit Support Obligations.
 */
export interface CreditSupportObligationsBase {
    /** In respect of a Valuation Date, the amount of collateral which a party is entitled to demand to be transferred to it as a Delivery Amount. */
    deliveryAmount: CSADeliveryAmount,
    /** In respect of a Valuation Date, the amount of collateral which a party is entitled to demand to be recalled to it as a Return Amount. */
    returnAmount: CSAReturnAmount,
    /** Represents the threshold specified in the agreement below which collateral need not be delivered, and represents the smallest incremental value of exposure that must be reached before the parties are obliged to transfer collateral. */
    minimumTransferAmount: MinimumTransferAmount,
    /** Specification of transfer / settlement timing for cash and securities collateral assuming that the notice is provided before the Notification Time (and with a 1LBD lag introduced if the notice is delivered after the Notification Time), and that securities allow for a further Local Business Day for transfer / settlement. */
    collateralTransferTiming?: CollateralTransferTiming,
    /** The time by which the transfer of collateral must take place when different from the Regular Settlement Day as a result of parties' election. */
    bespokeTransferTiming?: BespokeTransferTiming,
}


/**
 * Specification of the Credit Support Obligations applicable to the Legacy (1994/95) Credit Support Annex.
 */
export interface CreditSupportObligationsLegacy extends CreditSupportObligationsBase {
    /** Represents the amount of unsecured risk that a party is willing to tolerate without holding any posted collateral provided by the other party. */
    threshold: Threshold,
    /** The rounding methodology applicable to the Delivery Amount and the Return Amount in terms of nearest integral multiple of Base Currency units. */
    rounding: CollateralRounding,
    /** Specifies the additional amount of collateral that is required to be delivered by a party irrespective of the mark-to-market exposure under the related Transactions. It may take the form of a fixed amount or may be determined using ratings. */
    independentAmount: LegacyIndependentAmount,
    /** The total amount one counterparty must deliver to the other at any time: the combination of the Exposure to that party and the net Independent Amounts it must post, minus any agreed Threshold. */
    creditSupportAmount: CreditSupportAmount,
    /** Specification of the Eligible Collateral or Credit Support applicable to the Agreement. */
    eligibleCreditSupport: EligibleCreditSupport,
}


/**
 * Specification of the Credit Support Obligations applicable to the Initial Margin Credit Support Annex.
 */
export interface CreditSupportObligationsInitialMargin extends CreditSupportObligationsBase {
    /** The selection of Margin Approach applicable to the agreement. */
    marginApproach: MarginApproach,
    /** The Other Eligible Support election. If not specified, this election is deemed as not applicable. */
    otherEligibleSupportIM?: string,
    /** The rounding methodology applicable to the Delivery Amount and the Return Amount in terms of nearest integral multiple of Base Currency units. */
    rounding: CollateralRounding,
    /** Represents the amount of unsecured risk that a party is willing to tolerate without holding any posted collateral provided by the other party. */
    threshold: Threshold,
    /** The total amount one counterparty must deliver to the other at any time: the combination of the Exposure to that party and the net Independent Amounts it must post, minus any agreed Threshold. */
    creditSupportAmount: CreditSupportAmount,
}


/**
 * Specification of the Credit Support Obligations applicable to the Initial Margin Credit Support Annex.
 */
export interface CreditSupportObligationsCollateralTransferAgreement extends CreditSupportObligationsBase {
    /** The selection of Margin Approach applicable to the agreement. */
    marginApproach: MarginApproach,
    /** The Other Eligible Support election. If not specified, this election is deemed as not applicable. */
    otherEligibleSupportIM?: string,
    /** Represents the amount of unsecured risk that a party is willing to tolerate without holding any posted collateral provided by the other party. */
    threshold: Threshold,
    /** The total amount one counterparty must deliver to the other at any time: the combination of the Exposure to that party and the net Independent Amounts it must post, minus any agreed Threshold. */
    creditSupportAmount: CreditSupportAmount,
    /** The rounding methodology applicable to the Delivery Amount and the Return Amount in terms of nearest integral multiple of Base Currency units. */
    rounding?: CollateralRounding,
}


/**
 * Specification of the Credit Support Obligations applicable to the Variation Margin Credit Support Annex.
 */
export interface CreditSupportObligationsVariationMargin extends CreditSupportObligationsBase {
    /** The parties to which the provisions of the Credit Support Annex for Variation Margin will apply to. */
    ineligibleCreditSupport?: IneligibleCreditSupport,
    /** The additional currencies that are specified as Major Currency for the purpose of applying the FX Haircut Percentage. */
    majorCurrency?: string[],
    /** The alternative definition for FX haircut percentage that applies to each party and item of Eligible Collateral unless this item is denominated in a Major Currency or in the Base Currency. */
    fxHaircut?: string,
    /** The rounding methodology applicable to the Delivery Amount and the Return Amount in terms of nearest integral multiple of Base Currency units. */
    rounding: CollateralRounding,
    /** Specification of the Eligible Collateral or Credit Support applicable to the Agreement. */
    eligibleCreditSupport: EligibleCreditSupport,
}


/**
 * Specifies the collateral posting obligations of the security provider or security providers as specified in the corresponding agreement
 */
export interface EligibleCreditSupport {
    /** The specification of the collateral posting obligations for the security provider party(ies) */
    partyElection: EligibleCollateralElection[],
}


/**
 * Specifies the collateral posting obligations for the security provider party(ies).
 */
export interface EligibleCollateralElection extends PostingObligationsElection {
    /** Specifies the Other Eligible Support conditions. If not specified, this election is deemed as not applicable. */
    otherEligibleSupport: string,
}


/**
 * Security Interest for Obligations clause applicable to 1994 NY CSA, 1995 English Law CSA, 1995 English Law Credit Support Deed, 2016 English Law VM CSA and 2016 New York Law VM CSA.
 */
export interface SecurityInterestForObligations {
    /** The elective party to which the Security Interest for Obligations is applicable. */
    party: string,
    /** A description of any additional obligations secured by the credit support arrangements. */
    obligations: string,
    /** The party to whom obligations under the Master Agreement are owed and secured by the credit support arrangements. */
    obligee: string,
    /** Exception value. */
    other?: string,
}


/**
 * Where only a single  party is explicitly designated to post collateral, the party which is specified as the sole poster of collateral.
 */
export interface SinglePostingParty {
    /** The party acting as the Single Posting Party. */
    party?: string,
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    other?: string,
}


/**
 * Represents the amount of unsecured risk that a party is willing to tolerate without holding any posted collateral provided by the other party.
 */
export interface Threshold {
    /** The party specific election for the threshold clause. */
    partyElection: ThresholdElection[],
}


/**
 * A logical container designed to hold a set of related data. In this case, designed to hold a set of information required to specify conditions under which Threshold or Minimum Transfer Amount can fall to zero.
 */
export interface ThresholdMinimumTransferAmountBase {
    /** Indicates whether a trigger applies for the Threshold or Minimum Transfer Amount to fall to zero. If 'True' then such a trigger exists. */
    zeroEvent: boolean,
    /** Specifies the relevant trigger for the Threshold or Minimum Transfer Amount to fall to zero. */
    event?: string,
}


/**
 * Specifies whether the threshold is rating based, a fixed amount, or infinity.
 */
export interface ThresholdElection {
    /** The party to which the threshold applies. */
    party: string,
    /** Defines that the Threshold is a Fixed Amount. */
    fixedAmount?: ThresholdMinimumTransferAmountFixedAmount,
    /** Defines that the Threshold is based on a Ratings condition(s). */
    ratingsBased?: ThresholdRatingsBased,
    /** Specifies that the Threshold is Infinity. If 'True' then the threshold for the elective party is Infinity. */
    infinity?: boolean,
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    other?: string,
}


/**
 * Defines that the Threshold is based on Ratings condition(s).
 */
export interface ThresholdRatingsBased extends ThresholdMinimumTransferAmountBase {
    /** The threshold currency code. */
    currency: string,
    /** The relevant rating type. */
    ratingType: string,
    /** Defines a combination of Rating Agency, Rating Value, Threshold amount and Currency code. */
    variableSet?: CSAThresholdVariableSet[],
    /** The party to which a rating applies. */
    ratedParty?: string,
    /** Details the Named Entity where the Rated Party is Named Entity. */
    namedEntity?: string,
    /** Details the Named Affiliate where the Rated Party is Named Affiliate. */
    namedAffiliate?: string,
    /** Where two ratings are specified whether the higher or lower rating prevails. */
    compare?: string,
    /** What conditions apply where a party has no rating. */
    noRating: boolean,
    /** Defines where conditions apply if no Rating where ratings may not exist. */
    notRatedBy?: string,
    /** Defines the number of Rating Agencies that the Party must be rated by. */
    numberOfRatingAgencies?: string,
}


/**
 * Defines a combination of Rating Agency, Rating Value, Threshold amount and Currency code.
 */
export interface CSAThresholdVariableSet {
    /** The relevant rating agency. */
    name?: string,
    /** Rating applicable to the party from a given rating agency. */
    value?: string,
    /** The amount value applicable to the Threshold. */
    amount?: number,
}


/**
 * Represents the threshold specified in the agreement below which collateral need not be delivered, and represents the smallest incremental value of exposure that must be reached before the parties are obliged to transfer collateral.
 */
export interface MinimumTransferAmount {
    /** The party specific elections for the Minimum Transfer Amount clause. */
    partyElection: MinimumTransferAmountElection[],
}


/**
 * Specifies the method by which the minimum transfer amount is defined, such as a fixed amount, ratings based, or an alternative basis set out in the agreement.
 */
export interface MinimumTransferAmountElection {
    /** The party to which the minimum transfer amount applies. */
    party: string,
    /** Defines that the Minimum Transfer Amount (MTA) is a Fixed Amount. */
    fixedAmount?: ThresholdMinimumTransferAmountFixedAmount,
    /** Defines that the Minimum Transfer Amount (MTA) is based on a Ratings condition(s). */
    ratingsBased?: MinimumTransferAmountRatingsBased,
    /** Utilised where the clause data structure is not able to capture a material aspect of the clause. */
    other?: string,
}


/**
 * Defines the Threshold or the Minimum Transfer Amount (MTA) as a fixed amount.
 */
export interface ThresholdMinimumTransferAmountFixedAmount extends ThresholdMinimumTransferAmountBase {
    /** Specifies the monetary value of the Threshold or the Minimum Transfer Amount (MTA) applicable to collateral transfer obligations. */
    amount: Money,
}


/**
 * Defines that the Minimum Transfer Amount (MTA) is based on a Ratings condition(s).
 */
export interface MinimumTransferAmountRatingsBased extends ThresholdMinimumTransferAmountBase {
    /** The minimum transfer amount currency code. */
    currency: string,
    /** The relevant rating type. */
    ratingType: string,
    /** Defines a combination of Rating Agency, Rating Value, amount and Currency code. */
    variableSet?: CSAMinimumTransferAmountVariableSet[],
    /** The party to which a rating applies. */
    ratedParty?: string,
    /** Details the Named Entity where the Rated Party is Named Entity. */
    namedEntity?: string,
    /** Details the Named Affiliate where the Rated Party is Named Affiliate. */
    namedAffiliate?: string,
    /** Where two ratings are specified whether the higher or lower rating prevails. */
    compare?: string,
    /** What conditions apply where a party has no rating. */
    noRating: boolean,
    /** Defines where conditions apply if no Rating where ratings may not exist. */
    notRatedBy?: string,
    /** Defines the number of Rating Agencies that the Party must be rated by. */
    numberOfRatingAgencies?: string,
}


/**
 * Defines a combination of Rating Agency, Rating Value, amount and Currency code.
 */
export interface CSAMinimumTransferAmountVariableSet {
    /** The name of the Rating Agency. */
    name?: string,
    /** The value assigned to the rating. */
    value?: string,
    /** The Minimum Transfer Amount (MTA) applicable to the Rating. */
    amount?: number,
    /** A flag that defines the Minimum Transfer Amount (MTA) as Infinity. */
    amountIsInfinity?: boolean,
}


/**
 * Defines the dates agreed by the parties as the date of the Credit Support Annex or Credit Support Deed.
 */
export interface MasterAgreementDatedAsOfDate {
    /** The date agreed by the parties as the Dated as of Date for the Master Agreement. */
    masterAgreementDatedAsOfDate?: date,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * In respect of a Valuation Date, the amount of collateral which a party is entitled to demand to be transferred to it as a Delivery Amount.
 */
export interface CSADeliveryAmount {
    /** Specifies whether the meaning of Delivery Amount follows the pre-print definition or deviates from this (either based on a rating or some other methodology). */
    deliveryAmount: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * In respect of a Valuation Date, the amount of collateral which a party is entitled to demand to be recalled to it as a Return Amount.
 */
export interface CSAReturnAmount {
    /** Specifies whether the meaning of Return Amount follows the pre-print definition or deviates from this (either based on a rating or some other methodology). */
    returnAmount: string,
}


/**
 * Specifies the time by which the value of the collateral and obligations to transfer or return collateral are to be calculated relative to the Valuation Date.
 */
export interface ValuationTime {
    /** The day in respect of which the Valuation Time is being specified. If not specified, addional language must be used to define the methodology. */
    day?: string,
    /** The location relevant to the timezone for the Valuation Time. */
    location?: string[],
    /** The type of time specified for the Valuation Time. */
    time?: string,
    /** A time specified in a 24-hour notation, e.g. 3pm would be represented as 15:00. */
    hourminutetime?: string,
    /** Any relevant time zone where specified. */
    timezone?: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * A clause providing that final return collateral is to be provided without consideration of any Minimum Transfer Amount and collateral haircut.
 */
export interface FinalReturns {
    /** Defines if the clause is applicable to this agreement or any other exceptions. */
    applicable?: boolean,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Specifies the date on which the value of collateral or exposure are calculated in accordance with the agreement.
 */
export interface CSAValuationDate {
    /** Defines whether a date is defined by a certain day of the week or a particular date in the month. If not specified here, then additional language must be used. */
    date?: string,
    /** Specific day(s) or day of the week, if any, specified for Valuation Time. */
    day?: string,
    /** Defines the day of the calendar if this is specified for in the ValuationDateDateEnum. */
    calendarDay?: string,
    /** Defines the regularity of the date specified for the Valuation Date. */
    frequency?: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Details of the party valuating the collateral to be delivered or returned.
 */
export interface ValuationAgent {
    /** Specification of which party calculating the value of collateral to be delivered or returned. */
    party?: string,
    /** Specifies how the Valuation Agent is determined. */
    valuationAgent?: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Optionally, for each party, specifies the amendments to the included coverage in the calculation of Exposure.
 */
export interface ExposureScope {
    /** Defines the electice scope for a specific party. */
    partyElection?: LegacyExposureScopeElection[],
}


/**
 * Specification of amendments to the calculation of Exposure in terms of the Transactions and Products covered.
 */
export interface LegacyExposureScopeElection {
    /** The elective party. */
    party: string,
    /** A boolean flag to represent whether any transactions are excluded (ie True) or not (ie False). */
    excludedTransactions: boolean,
    /** Description of the relevant derivative transactions excluded from the calculation of exposure. */
    excludedProducts?: ExcludedProducts[],
    /** The bespoke/additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Description of the relevant derivative transactions excluded from the calculation of exposure.
 */
export interface ExcludedProducts {
    /** Description of the relevant derivative transactions excluded from the calculation of exposure. */
    product?: string,
    /** Description of the specific branch entered into by the relevant party. */
    branch?: string[],
    /** A flag to indicate whether transactions entered into by a specific branch with respect to the relevant party are excluded; set to True if excluded, false if included. */
    excluded: boolean,
}


/**
 * The total amount one counterparty must deliver to the other at any time: the combination of the Exposure to that party and the net Independent Amounts it must post, minus any agreed Threshold.
 */
export interface CreditSupportAmount {
    /** Specifies whether the meaning of credit support amount follows the pre-print definition or deviates from the standard (either based on a rating or some other methodology). */
    creditSupportAmount: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Alternative procedures to those in the pre-print for resolving collateral related disputes.
 */
export interface LegacyResolutionAlternative {
    /** Details of the alternative dispute resolution procedure (if any). */
    resolutionAlternative?: string,
    /** The maximum amount to which the difference between the calculations by the valuation agent and disputing party can be split, where Split the Difference is elected. */
    amount?: Money,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Methodology for resolving disputes in respect of the determination of the value of collateral.
 */
export interface ResolutionValue {
    /** Details of how cash collateral is valued when resolving disputes. */
    cash?: string,
    /** Details of how securities collateral is valued when resolving disputes. */
    securities?: string,
    /** Represents whether a fallback calculation method is applicable. 'True' if fallback calculation method is applicable. */
    fallback?: boolean,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Specification of transfer / settlement timing for cash and securities collateral assuming that the notice is provided before the Notification Time (and with a 1LBD lag introduced if the notice is delivered after the Notification Time), and that securities allow for a further Local Business Day for transfer / settlement.
 */
export interface CollateralTransferTiming {
    /** Specification of transfer / settlement timing for cash and securities collateral assuming that the notice is provided before the Notification Time (and with a 1LBD lag introduced if the notice is delivered after the Notification Time), and that securities allow for a further Local Business Day for transfer / settlement. */
    transferSettlementTiming?: TransferSettlementTiming,
    /** Bespoke language removing the pre-print requirement to transfer legal title of securities collateral through written instructions to the relevant depository institution or other securities intermediaries solely for the purposes of the 1994 New York Law Credit Support Annex. */
    collateralTransferTimingDefinition?: CollateralTransferTimingDefinition,
}


/**
 * Specification of transfer / settlement timing for cash and securities collateral assuming that the notice is provided before the Notification Time (and with a 1LBD lag introduced if the notice is delivered after the Notification Time), and that securities allow for a further Local Business Day for transfer / settlement.
 */
export interface TransferSettlementTiming {
    /** Details the day on which collateral is required to be transferred relative to the Notification Time. */
    cashCollateralTransferSettlementTime?: string,
    /** Details the day on which collateral is required to be transferred relative to the Notification Time. */
    securititesCollateralTransferSettlementTime?: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Bespoke language removing the pre-print requirement to transfer legal title of securities collateral through written instructions to the relevant depository institution or other securities intermediaries solely for the purposes of the 1994 New York Law Credit Support Annex.
 */
export interface CollateralTransferTimingDefinition {
    /** A boolean flag to represent bespoke amendment to the pre-print definition of Transfer. */
    isApplicable?: boolean,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Details how particular types of collateral are valued (other than in respect of a default).
 */
export interface CollateralValueMethod {
    /** Details of how cash collateral is valued. */
    cashCollateral?: string,
    /** Details of how securities collateral is valued. */
    securitiesCollateral?: string,
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * Identification of party specific Credit Support Documents applicable to the document.
 */
export interface CreditSupportDocument {
    /** The party election of Credit Support Document(s), if any. */
    creditSupportDocumentElection: CreditSupportDocumentElection[],
}


/**
 * The party election of Credit Support Provider(s), if any.
 */
export interface CreditSupportDocumentElection {
    /** The elective party. */
    party: Party,
    /** Specification of the Credit Support Document terms. */
    creditSupportDocumentTerms: string,
    /** If a Credit Support Document is specified but not yet in CDM representation, it can still be captured. This argument should be used if the creditSupportDocument argument cannot be used due to not being able to represent the document fully in CDM format. */
    creditSupportDocumentTypes?: LegalAgreementIdentification[],
    /** The specified Credit Support Document(s), if any. */
    creditSupportDocument?: LegalAgreement[],
    /** Specification of a document when not captured under RelatedAgreement. */
    bespokeCreditSuppportDocument?: string,
}


/**
 * Identification of party specific Credit Support Providers applicable to the document.
 */
export interface CreditSupportProvider {
    /** The party election of Credit Support Provider(s), if any. */
    creditSupportProviderElection: CreditSupportProviderElection[],
}


/**
 * The party election of Credit Support Provider(s), if any.
 */
export interface CreditSupportProviderElection {
    /** The elective party. */
    party: Party,
    /** Specification of the Credit Support Provider terms. */
    creditSupportProviderTerms: string,
    /** The specified Credit Support Provider(s), if any. */
    creditSupportProvider?: LegalEntity[],
    /** ... */
    bespokeCreditSuppportProvider?: string,
}


/**
 * Specifies the events elected by the parties that are deemed an Access Condition (for Initial Margin CSA) or a Specified Condition (for Variation Margin and Legacy CSA), which, if triggered, may temporarily or permanently suspend rights to rehypothecate, transfer, or substitute collateral.
 */
export interface SpecifiedConditionOrAccessCondition {
    /** Indicates whether events that could temporarily or permanently suspend rights to rehypothecate, transfer or substitute collateral are applicable. If applicable,'True'. If not applicable,'False'. */
    isApplicable: boolean,
    /** The party specific elections. */
    partyElection?: SpecifiedOrAccessConditionPartyElection[],
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * The party specific elections in respect to the Specified Condition and Access Condition clause.
 */
export interface SpecifiedOrAccessConditionPartyElection {
    /** Identifies the party making the election. */
    party: string,
    /** Specifies events that may temporarily or permanently suspend a partys rights to rehypothecate, transfer, or substitute collateral. */
    specifiedOrAccessCondition?: string,
    /** Specifies events that may lead to the early termination of the Master Agreement, including suspension of the affected party's rights to rehypothecate, transfer, or substitute collateral. */
    additionalTerminationEvent?: string,
    /** Specifies any bespoke Additional Termination Event(s) that are set out in the related Specified Condition clause. */
    specifiedAdditionalTerminationEvent?: string[],
}


/**
 * Sets out the additional amount of collateral that is required to be delivered by that party irrespective of the mark-to-market exposure under the related Transactions. It may take the form of a fixed amount, or be determined using credit ratings.
 */
export interface LegacyIndependentAmount {
    /** The party specific elections for the Independent Amount clause. */
    partyElection: LegacyIndependentAmountParty[],
    /** The additional language that might be specified by the parties to the legal agreement. */
    additionalLanguage?: string,
}


/**
 * The party specific elections for the Independent Amount clause.
 */
export interface LegacyIndependentAmountParty {
    /** Identifies the Party making the election. */
    party: string,
    /** Specifies whether Independent Amount is applicable to the elected party. If applicable, 'True', and if not applicable then 'False'. */
    isApplicable: boolean,
    /** Indicates the Independent Amount is specified as a fixed amount. */
    fixedAmount?: Money,
    /** Indicates the Independent Amount is determined using a credit rating. */
    ratingsBased?: LegacyIndependentAmountRatingsBased,
    /** Indicates the Independent Amount is determined using both credit rating and exposure metrics. */
    ratingsXExposure?: LegacyIndependentAmountRatingsXExposure,
}


/**
 * A logical container designed to hold a set of related data. In this case, designed to hold a set of information required when Independent Amount is determined using credit ratings.
 */
export interface IndependentAmountRatings {
    /** Specifies how multiple credit ratings are compared when determining the applicable Independent Amount, and whether that measure is highest, lowest, or a comparison of either. */
    compare: string,
    /** Specifies the party to whom the rating agency rating applies to for the purposes of determining the Independent Amount. */
    ratedParty: string,
    /** Details the Named Affiliate where the Rated Party is Named Affiliate. */
    namedAffiliate?: LegalEntity,
    /** Details the Named Entity where the Rated Party is Named Entity. */
    namedEntity?: LegalEntity,
    /** The relevant rating type. */
    ratingType: string,
}


/**
 * Specifies that the Independent Amount is determined using a credit rating.
 */
export interface LegacyIndependentAmountRatingsBased extends IndependentAmountRatings {
    /** A logical container designed to hold a defined set of related data. In this case the collation of Rating Information and an associated Independent Amount. */
    variableSet: RatingAgencyAmount[],
    /** Specifies the currency applicable to the relevant Independent Amount. */
    currency?: string,
}


/**
 * A logical container to hold a defined set of related data. In this case the collation of Rating Information and an associated Independent Amount.
 */
export interface RatingAgencyAmount {
    /** Allows a grouping of Rating agencies and Rating values. */
    ratingVariableSet: CreditNotation[],
    /** Specifies the notional or collateral Independent Amount. */
    amount?: string,
}


/**
 * Specifies that the Independent Amount is determined using both credit rating and exposure metrics.
 */
export interface LegacyIndependentAmountRatingsXExposure extends IndependentAmountRatings {
    /** A logical container to hold a defined set of related data. In this case the collation of Ratings Information and associated exposure multiplier where highest or lowest of the associated credit ratings are used when determining the applicable Independent Amount. */
    highestLowestVariableSet?: RatingMultiplier[],
    /** A logical container to hold a defined set of related data. In this case the collation of Ratings Information and associated exposure multiplier where multiple credit ratings are compared when determining the applicable Independent Amount. */
    compareVariableSet?: DirectionRatingMultiplier[],
}


/**
 * A logical container to hold a defined set of related data. In this case the collation of Ratings Information and associated exposure multiplier.
 */
export interface RatingMultiplier {
    /** Allows a grouping of Rating agencies and Rating values. */
    ratingVariableSet: CreditNotation[],
    /** Specifies the multiplier applied to exposure based on the credit rating of the rated party. */
    exposureMultiplier: string,
}


/**
 * A logical container to hold a defined set of related data. In this case the collation of Ratings Information and associated exposure multiplier.
 */
export interface DirectionRatingMultiplier {
    /** A logical container to hold a defined set of related data. In this case the collation of Ratings Agency, Rating value and an associated direction. */
    variableSet: DirectionRatingSet[],
    /** Specifies the multiplier applied to exposure based on the credit rating of the rated party. */
    exposureMultiplier: string,
}


/**
 * Used to combine two or more Ratings and associated direction using AND logic.
 */
export interface AllDirectionRating {
    allDirectionRating: DirectionRatingSet[],
}


/**
 * Used to combine two or more Ratings and associated direction using OR logic.
 */
export interface AnyDirectionRating {
    anyDirectionRating: DirectionRatingSet[],
}


/**
 * A logical container to hold a defined set of related data. In this case the collation of Ratings Agency, Rating value and an associated direction.
 */
export interface DirectionRating extends CreditNotation {
    /** Specifies the direction of measurement for the rated party. */
    direction: string,
}


/**
 * Specifies the conditions under which a party and its custodian(s) are entitled to hold collateral.
 */
export interface EligibilityToHoldCollateral {
    /** Specifies whether the party's custodian is eligible to hold the other party's posted collateral. If 'True' then the custodian is eligible to hold the Posted Collateral given that the specified criteria are met. */
    custodianEligibility: boolean,
    /** Election to specify the initial custodian. */
    initialCustodian?: LegalEntity,
    /** A logical container to hold a defined set of related data. In this case the collation of criteria required by the party and its custodian to Hold the other party's Posted Collateral. */
    criteria?: EligibilityToHoldCollateralCriteria,
    /** The restrictions that might be required by a party and its custodian in terms of country(ies) where the other party's collateral can be held. */
    eligibleCountry?: string[],
    /** Specifies any additional conditions relevant to the Custodian of the respective parties as set out in the Holding and Using Posted Collateral clause. */
    custodianCondition?: string,
}


/**
 * Used to combine two or more Criteria using AND logic.
 */
export interface AllEligibilityToHoldCollateralCriteria {
    allCriteria: EligibilityToHoldCollateralCriteria[],
}


/**
 * Used to combine two or more Criteria using OR logic.
 */
export interface AnyEligibilityToHoldCollateralCriteria {
    anyCriteria: EligibilityToHoldCollateralCriteria[],
}


/**
 * The amount of net capital.
 */
export interface AmountOfNetCapital {
    /** The amount of net capital. */
    amountOfNetCapital?: Money,
}


/**
 * A logical container designed to hold a set of related data. In this case the Rating Agency, Rating Value and Rating Type.
 */
export interface RatingAgencyValue extends CreditNotation {
    /** The relevant rating type. */
    ratingType: string,
}


/**
 * Specifies the address where the demands, specifications and notices will be communicated to for each of the parties to the agreement.
 */
export interface DemandsAndNotices {
    /** The parties' contact information election. */
    partyElection: NoticeInformationElection[],
    /** Specifies if notice shall be deemed to be effective at the time such notice is actually received unless such notice is received on a day which is not a Local Business Day or after the Notification Time on any Local Business Day in which event such notice shall be deemed to be effective on the next succeeding Local Business Day. If deemed effective,'True'. If not deemed effective, or not stated,'False'. */
    deemedEffectiveNextLBD: boolean,
}


/**
 * Specifies the address for transfer as specified by the respective parties to the agreement.
 */
export interface AddressesForTransfer {
    /** The parties' contact and transfer information election. */
    partyElection: TransferInformationElection[],
}


/**
 * The set of elections which specify a Credit Support Annex or Deed.
 */
export interface CreditSupportAgreementElections {
}


/**
 * A logical container to hold a defined set of related data. In this case the collation of Ratings Agency, Rating value and an associated direction that are grouped by either AND or OR logic.
 */
export interface DirectionRatingSet {
}


/**
 * Defines a set of Criteria required by the party and its custodian to hold the other party's Posted Collateral.
 */
export interface EligibilityToHoldCollateralCriteria {
}


/**
 * The set of elections which specify a Master Agreement.
 */
export interface MasterAgreementSchedule {
    /** Clauses that have had elections made against them in this Master Agreement. There must be at least one clause defined in the agreement. */
    clause: MasterAgreementClause[],
}


/**
 * Defines clauses that make up a Master Agreement.
 */
export interface MasterAgreementClause {
    /** Unique identifier for the clause. */
    identifer: string,
    /** Optional textual description of the clause. */
    name?: string,
    /** Optional counterparty role. This can be used where a clause needs to be assigned to a specific party on the agreement based upon their role i.e. Party A or Party B. */
    counterparty?: string,
    /** Optional party. This can be required for umbrella agreements where a clause may need to be assigned to a specific party who may or may not be on the agreement. */
    otherParty?: string,
    /** Allows multiple variants to be defined for a clause. This needs to be an array as some clauses can specify different variants for different parties. At least one variant must be specified for a clause. */
    variant: MasterAgreementClauseVariant[],
}


/**
 * Sets the details for a specific variant associated to a clause in a Master Agreement.
 */
export interface MasterAgreementClauseVariant {
    /** Unique identifier for this variant. */
    identifier: string,
    /** Optional textual description of the variant. */
    name?: string,
    /** Optional counterparty role. This can be used where a clause needs to assign a different variant to the different parties on the agreement based upon their role i.e. Party A or Party B. */
    counterparty?: string,
    /** Optional party. This can be used where a clause needs to assign different variants to different parties who may or may not be on the agreement. */
    otherParty?: string,
    /** For some variants of some clauses additional details are required to work out what has been elected. This array can be used to define the name and value of these variables. Please refer to the agreement documentation for more details of the variables that are available for any clause. */
    variableSet?: MasterAgreementVariableSet[],
}


/**
 * Defines a type where additional variables associated to clauses and their variants can be described.
 */
export interface MasterAgreementVariableSet {
    /** For some variants a table of variables is required. To support this use case we need to be able to specify variables within variables. Including a variable set here gives us infinite nesting opportunities - realistically we are only ever expecting that a table would need to be defined for any particular clause, so we would expect two levels of nesting as a maximum i.e. variableSet->variableSet->name/value. */
    variableSet?: MasterAgreementVariableSet[],
    /** The name of the variable. */
    name?: string,
    /** The value for this variable. */
    value?: string,
}


/**
 * A set of elections that can be shared across master agreement types - this should be built upon for specific contracts.
 */
export interface MasterAgreementBase {
}


/**
 * A representation of a master agreement extended from a base agreement of shared elections.
 */
export interface MasterAgreementElections {
}


/**
 * The set of elections that define a GMRA
 */
export interface GlobalMasterRepoAgreement extends MasterAgreementBase {
}


/**
 * The set of elections that define an ISDA Master Agreement.
 */
export interface MasterAgreement extends MasterAgreementBase {
    /** The specification of whether there is an automatic occurrence of an Early Termination Date in respect of Transactions upon the occurrence of certain bankruptcy / insolvency related events. */
    automaticEarlyTermination: AutomaticEarlyTermination,
    /** Specification of the currency in which the termination payment is made (including the process by which such currency is determined). */
    terminationCurrency: TerminationCurrency,
    /** Specification of the address and other details for notices. */
    addressForNotices: DemandsAndNotices,
    /** Specification of whether the Governing Law clause extends to Non-Contractual Obligations (True) or does not extend to Non-Contractual Obligations (False). */
    nonContractualObligations: boolean,
    /** Identification of party specific Credit Support Documents applicable to the document. */
    creditSupportDocument: CreditSupportDocument,
    /** Identification of party specific Credit Support Providers applicable to the document. */
    creditSupportProvider: CreditSupportProvider,
    /** A provision that allows each party to specify its Specified Entities for certain Events of Default and Termination Events. */
    specifiedEntities: SpecifiedEntities[],
}


/**
 * A class to specify the Automatic Early Termination provision applicable to a Master Agreement.
 */
export interface AutomaticEarlyTermination {
    /** Specifies whether automatic termination applies in the event of certain bankruptcy events. */
    fallbackAET: boolean,
    /** Specifies whether indemnity applies in the event AET is applicable to one or both of the parties. */
    indemnity: boolean,
    /** The party election specific to the Automatic Early Termination Clause. */
    partyElection?: AutomaticEarlyTerminationElection[],
}


/**
 * A class to specify the party elections specific to the Automatic Early Termination Clause.
 */
export interface AutomaticEarlyTerminationElection {
    /** The party for which the Automatic Early Termination provisions are being specified. */
    party: Party,
    /** A boolean election to specify whether the Automatic Early Termination provisions of Section 6(a) are applicable (True) or not applicable (False). */
    isApplicable: boolean,
}


/**
 * Specifies how the Termination Currency for the agreement will be determined.
 */
export interface TerminationCurrency {
    /** Allows for specific Termination Currency(ies) and a fallback Termination Currency to be selected. */
    statedTerminationCurrency?: TerminationCurrencySelection,
    /** Provides that the Termination Currency will be determined by reference to a contractual mechanism when closing out the Agreement. */
    partyOptionTerminationCurrency?: PartyOptionTerminationCurrency,
}


/**
 * Specifies Termination Currency where a currency is stated at the time the agreement is entered into.
 */
export interface TerminationCurrencySelection {
    /** Specifies a single Termination Currency for the agreement. */
    statedCurrency?: string,
    /** Specifies a single fallback Termination Currency should the stated currency not be freely available. */
    fallbackCurrency?: string,
    /** Specifies different termination currencies to apply depending on which party or parties are the Defaulting Party Affected Party(ies). */
    partyElection?: PartyTerminationCurrencySelection[],
    /** Specifies fallback Termination Currency where both parties are Affected Parties. */
    bothAffected?: string,
}


/**
 * Specifies mechanism for Termination currency to be selected by the Non-defaulting Party/party which is not the Affected Party.
 */
export interface PartyOptionTerminationCurrency {
    /** Specifies termination currency where there are two Affected Parties and they cannot agree on the termination currency. */
    bothAffectedTermCurrencyOption?: string,
    /** Specifies alternative conditions for selection of the termination currency. */
    terminationCurrencySpecifiedCondition?: string,
    /** Specifies the enumerated conditions for selection of the termination currency. */
    terminationCurrencyCondition: string,
}


/**
 * Specifies the termination currency to be used by a party when it is the Non-Defaulting Party or the Party which is not the Affected Party.
 */
export interface PartyTerminationCurrencySelection {
    /** The elective party. */
    party: Party,
    /** Specifies termination Currency. */
    statedPartyCurrency: string,
}


/**
 * A provision that allows each party to specify its Specified Entities for certain Events of Default and Termination Events.
 */
export interface SpecifiedEntities {
    /** The Event of Default or Termination event for which Specified Entities terms are being defined. */
    specifiedEntityClause: string,
    /** The party specific election of Specified Entities for the Event of Default or Termination Event specified. */
    specifiedEntity: SpecifiedEntity[],
}


/**
 * The party specific election of Specified Entities for the Event of Default or Termination Event specified.
 */
export interface SpecifiedEntity {
    /** The elective party. */
    party: Party,
    /** The specified entity terms for the Event of Default or Termination Event specified. */
    specifiedEntityTerms: string,
    /** The specified entities for the Event of Default or Termination Event specified. */
    specifiedEntity?: LegalEntity[],
    /** The meaning of Material Subsidiary for the Event of Default or Termination Event specified. */
    materialSubsidiaryTerms?: string,
    /** The non standard terms for the Event of Default or Termination Event specified. */
    otherSpecifiedEntityTerms?: string,
}


/**
 * The set of elections that define a GMSLA.
 */
export interface GlobalMasterSecuritiesLendingAgreement extends MasterAgreementBase {
    /** The definitions regarding collateral relating to the GMSLA. */
    collateral: CollateralSpecification,
    /** The definitions regarding base currency determinations relating to the GMSLA. */
    baseCurrency: BaseCurrency,
    /** The definitions regarding any different events of default relating to the GMSLA. */
    eventsOfDefault: EventsOfDefault,
}


/**
 * The definitions regarding collateral relating to the GMSLA.
 */
export interface CollateralSpecification {
    /** Is the standard definition of 'Market Value' used in the GMSLA? */
    standardDefinitionOfMarketValueUsed: boolean,
    /** If the definition of 'Market Value' is non standard, it can be specified here. */
    definitionOfMarketValueNonStandard?: string,
    /** How does paragraph 5.4 (aggregation) apply to the GMSLA? */
    aggregation: Aggregation,
    /** Does paragraph 5.6 (netting of obligations to deliver Collateral and redeliver Equivalent Collateral) apply to the GMSLA? */
    nettingObligationsApply: string,
    /** Does paragraph 6.4 (indemnity for failure to redeliver Equivalent Non-Cash Collateral) apply to the GMSLA? */
    failureToDevliverIndemnityApplies: string,
    /** What is the notification time of the GMSLA? */
    notificationTime: BusinessCenterTime,
}


/**
 * The definitions regarding base currency determinations relating to the GMSLA.
 */
export interface BaseCurrency {
    /** The base currency as defined in the GMSLA. */
    baseCurrency: string,
    /** The base currency that is defined must be freely convertible. */
    baseCurrencyMustBeFreelyConvertible?: boolean,
    /** If the base currency is not freely convertible, the fallback currency can be defined here. */
    baseCurrencyIfOriginalNotFreelyConvertible?: string,
}


/**
 * The specific party elections for events of default.
 */
export interface EventsOfDefaultElection {
    /** The specified party. */
    party: string,
    /** Does automatic early termination apply to the specified party? */
    automaticEarlyTerminationApplies: string,
    /** Are existing loans covered in the event of default (Only relevant to 2010 GMSLAs)? */
    existingLoans?: string,
    /** Are third party vendors allowed to automate the processing of loans in the event of default (Only relevant to 2010 GMSLAs)? */
    automation?: string,
    /** In the case where party is acting as an agent, is automatic early termination specified on a per principle basis? */
    automaticEarlyTerminationSpecifiedSeparatelyForEachPrinciple: boolean,
    /** Automatic early termination only applies if required due to system of law, otherwise it is not applicable. */
    automaticEarlyTerminationRequiredDueToSystemOfLaw: boolean,
    /** Automatic early termination applies in a modified form only. */
    automaticEarlyTerminationInModifiedForm: boolean,
    /** Optional string to capture any further information regarding the automatic early termination even of default. */
    automaticEarlyTerminationOther?: string,
}


/**
 * The definitions regarding any different events of default relating to the GMSLA.
 */
export interface EventsOfDefault {
    /** The specified events of default elections for the respective parties signing the GMSLA. */
    partyElection: EventsOfDefaultElection[],
}


/**
 * The definitions for whether aggregation applies to the GMSLA, and if so how it applies.
 */
export interface Aggregation {
    /** How is the mark to market value determined? */
    aggregationType: string,
    /** The mark to market can be defined separately for different groups of loans. */
    loansCanBeGrouped: boolean,
    /** Any specfic elections that are not captured by the above options, such as grouping information. */
    otherDetails?: string,
}


/**
 * Additional specification for the extraordinary events that may affect a trade and the related contractual rights and obligation of the parties when this happens. Such terms are typically required to extend the economics terms, for the purpose of producing the final legal contractual form of the Transaction.
 */
export interface TransactionAdditionalTerms {
    equityAdditionalTerms?: EquityAdditionalTerms,
    foreignExchangeAdditionalTerms?: FxAdditionalTerms,
    commoditiesAdditionalTerms?: string,
    creditAdditionalTerms?: string,
    interestRateAdditionalTerms?: string,
    digitalAssetAdditionalTerms?: string,
}


/**
 * Transaction AdditionalTerms that apply to Equity asset class.
 */
export interface EquityAdditionalTerms {
    extraordinaryEvents?: ExtraordinaryEvents,
    determinationTerms: DeterminationRolesAndTerms[],
    substitutionProvision?: UnderlierSubstitutionProvision,
}


/**
 * Where parties describe any substitution terms.
 */
export interface UnderlierSubstitutionProvision {
    /** Designates which Counterparty to the transaction who has the right to trigger a substitution or to provide related determination e.g. for instance to qualify the effectiveness of an Event which may be a trigger for substitution, determine the replacement Share to substitute, etc. ; cardinality of this object is 2, in case parties jointly have this role. */
    whoMaySubstitute: string,
    /** Where parties describe any substitution terms e.g. for instance the election criteria for an Asset to be eligible as the Substitute Asset to the prior Affected Asset in terms of sector of activity, currency, market capitalisation, liquidity, volatility, or any additional features that parties would agree to take into considerations, etc. */
    substitutionBeSpokeTerms?: Clause[],
    /** Where the parties may optionnally explictly specify the list of Events to be considered as a trigger for a Substitution. */
    substitutionTriggerEvents?: ExtraordinaryEvents[],
    /** Where the party who is not granted with the substitution role at least has a right to dispute the determination given by the counterparty with such role. As an example, a given PartyA is the unique Counterparty with the Role of WhoMaySubstitute, yet PartyB could be Disputing Party in regard of such Role. */
    disputingParty?: string,
}


/**
 * Where the underlying is shares, defines market events affecting the issuer of those shares that may require the terms of the transaction to be adjusted.
 */
export interface ExtraordinaryEvents {
    /** Where parties may optionnaly describe any extra bespoke agreements, in regards of the standardized Extraordinary Events. */
    additionalBespokeTerms?: Clause[],
    mergerEvents?: EquityCorporateEvents,
    tenderOfferEvents?: EquityCorporateEvents,
    compositionOfCombinedConsideration?: boolean,
    indexAdjustmentEvents?: IndexAdjustmentEvents,
    additionalDisruptionEvents?: AdditionalDisruptionEvents,
    /** If true, failure to deliver is applicable. */
    failureToDeliver?: boolean,
    representations?: Representations,
    nationalizationOrInsolvency?: string,
    delisting?: string,
}


/**
 * A type for defining the Additional Disruption Events.
 */
export interface AdditionalDisruptionEvents {
    changeInLaw?: boolean,
    /** If true, failure to deliver is applicable. */
    failureToDeliver?: boolean,
    insolvencyFiling?: boolean,
    hedgingDisruption?: boolean,
    increasedCostOfHedging?: boolean,
    foreignOwnershipEvent?: boolean,
    lossOfStockBorrow?: boolean,
    /** Specifies the maximum stock loan rate for Loss of Stock Borrow. A percentage of 5% is represented as 0.05. */
    maximumStockLoanRate?: string,
    increasedCostOfStockBorrow?: boolean,
    /** Specifies the initial stock loan per ISDA Def. A percentage of 5% is represented as 0.05. */
    initialStockLoanRate?: string,
    /** Specifies the party which determines additional disruption events. */
    determiningParty?: string,
    /** Where parties may optionnaly describe any extra bespoke agreements, in regards of the standardized Extraordinary Events. */
    additionalBespokeTerms?: Clause[],
}


/**
 * A type for documenting additional clause that cannot yet be represented with the model and yet needed for a digital representation of the agreement.
 */
export interface Clause {
    /** The  name or identifier associated to this clause. */
    identifier?: string,
    /** Content of this bespoke clause. */
    terms?: string,
    /** Additional hierarchical components of the clause if relevant. */
    subcomponents?: Clause[],
}


/**
 * Legal agreement specification for General Terms and Elections that are applicable across multiple confirmations and are referenced by these confirmations.
 */
export interface MasterConfirmationBase {
}


/**
 * Identifies the market sector in which the trade has been arranged.
 */
export interface BrokerConfirmation {
    /** The type of broker confirmation executed between the parties. */
    brokerConfirmationType: string,
}


/**
 * Specification for the General Terms and Relationship Supplement Elections as provided in the 2018 ISDA CDM Equity Confirmation for Security Equity Swap.
 */
export interface EquitySwapMasterConfirmation2018 extends EquityMasterConfirmation {
    /** Per Part 1 Section 4, 'Dividend Obligations', of the 2018 ISDA CDM Equity Confirmation, Para 4.2 'Dividend Returns'. */
    typeOfSwapElection: string,
    /** Per Part 1 Section 5, 'Pricing', of the 2018 ISDA CDM Equity Confirmation, Para 5.1. */
    pricingMethodElection: PriceReturnTerms,
    /** Per Part 1 Section 3, 'Floating Obligations', of the 2018 ISDA CDM Equity Confirmation. Para 3.3. */
    linearInterpolationElection: string,
    /** Per Part 1 Section 8, 'Settlement', of the 2018 ISDA CDM Equity Confirmation for Security Equity Swap. */
    settlementTerms: SettlementTerms,
    /** The parameters used to generate the 'Equity Valuation Dates' schedule, including the Effective Date and Termination Date for the Swap. */
    valuationDates: ValuationDates,
    /** The parameters used to generate the payment date schedule, relative to the equityCalculationPeriod. Per Part 1 Section 12, 'Definitions', of the 2018 ISDA CDM Equity Confirmation. Para 73. */
    equityCashSettlementDates: PaymentDates,
}


/**
 * Specification for General Terms and Elections of an Equity Master Confirmation that is applicable across multiple Equity confirmations and is referenced by each of these confirmations, an example of which being the 2018 ISDA CDM Equity Confirmation for Security Equity Swap.
 */
export interface EquityMasterConfirmation extends MasterConfirmationBase {
}


/**
 * TransactionAdditionalTerms which apply to the CurrencyPair asset class.
 */
export interface FxAdditionalTerms {
    disruptionEvents?: FxDisruptionEvents,
    determinationTerms?: DeterminationRolesAndTerms,
    noFaultTermination?: boolean,
}



export interface Representations {
    /** If true, then non reliance is applicable. */
    nonReliance: boolean,
    /** If true, then agreements regarding hedging are applicable. */
    agreementsRegardingHedging: boolean,
    /** If present and true, then index disclaimer is applicable. */
    indexDisclaimer?: boolean,
    /** If true, then additional acknowledgements are applicable. */
    additionalAcknowledgements?: boolean,
    /** Any additional terms that might be specified applicable. */
    additionalBespokeTerms?: Clause[],
}


/**
 * Defines the specification of the consequences of Index Events
 */
export interface IndexAdjustmentEvents {
    /** Consequence of index modification. */
    indexModification: string,
    /** Consequence of index cancellation. */
    indexCancellation: string,
    /** Consequence of index disruption. */
    indexDisruption: string,
}


/**
 * Defines the roles and related terms which document the agreement of parties about any determination requirements ; mostly about Extraordinary Events, without being necessarily restricted to such scope, as further specified in the particular product at stake e.g. for instance when Calculation Agent is mentioned as the Price Determination Method enumarated value, etc.
 */
export interface DeterminationRolesAndTerms {
    /** Defines the enumerated values to specify the determination roles to the transaction. Such roles mostly address any determination that would be required when some Extraordinary Events would happen, for instance qualifying the effectiveness of such event, or when a calculation is required, etc. else any other kind of determination as need be. Yet for clarity, other kinds of determination may be covered by such roles, which are not necessarily restricted to the scope of Extarordinary Events, depending on particular product specifications e.g. for instance when Calculation Agent is mentioned as the Price Determination Method enumarated value, etc. */
    determinationRole: string,
    /** Designates which Counterparty to the transaction is granted with the particular role ; cardinality of this object is 2, in case parties are both having the same role e.g. jointly CalculationAgent, jointly Determining Party, etc.  */
    whoToDetermine: string,
    /** Where the party who is not granted with the determination role at least has a right to dispute the determination given by the counterparty with such role. As an example, a given PartyA is the unique Counterparty with the Role of CalculationAgent, yet Party B could be Disputing Party in regard of such Role. */
    disputingParty?: string,
    /** Where parties may optionnaly describe any extra bespoke agreements about fallback procedure attached to a particular determination role. This fallback bespoke paragraph would typically address topics such as : delay for the Disputing Party to notify a disagreement when receiving a determination from the Calculation Agent ; Independent Third Party Dealers election terms e.g. number of Dealers to be jointly elected by the counterparties ; delay for joint election of Dealers by the counterparties ; further fallback election procedure in case of disagreement between parties to jointly elect all the Dealers e.g. typically each party designates 1 Dealer, each a Party Representant, which thus result in 2 Dealers in charge of joint election of 3 other Dealers who will be the final ones to provide the response, each a Final Dealer ; delay for joint election of the Final Dealers by the Party Representants ; fees repartition e.g. typically cost and fees incurred by the election of Dealers are equally shared by the parties ; etc. */
    fallbackLanguageBespokeTerms?: Clause,
}


/**
 * A class for defining the merger events and their treatment.
 */
export interface EquityCorporateEvents {
    /** Shall occur if a Merger Event occurs and the consideration for the relevant Security consists solely of Combined Consideration. */
    shareForShare: string,
    /** Shall occur if a Merger Event occurs and the consideration for the relevant Security consists solely of Other Consideration. */
    shareForOther: string,
    /** Shall occur if a Merger Event occurs and the consideration for the relevant Security consists solely of Combined Consideration. */
    shareForCombined: string,
}


/**
 * Additional Provisions for a Confirmation of an FX or Currency Option Transaction
 */
export interface FxDisruptionEvents {
    illegalityOrImpossibility?: string,
    forceMajeureOrActOfSState?: string,
    /** Additional Provisions for a Confirmation of an FX or Currency Option Transaction. */
    eventCurrency?: EventCurrency,
    priceSourceDisruption?: FxPriceSourceDisruption,
    benchmarkObligationDefault?: FxBenchmarkObligationDefault,
    dualExchangeRate?: FxDualExchangeRate,
    inconvertibilityOrNonTransferability?: FxInconvertibilityOrNonTransferability,
    governmentalAuthorityDefault?: GovernmentalAuthorityDefault,
    illiquidity?: Illiquidity,
    materialChangeInCircumstance?: MaterialChangeInCircumstance,
    nationalization?: Nationalization,
    priceMateriality?: PriceMateriality,
    /** Where parties may optionaly describe any extra bespoke agreements about fallback procedure attached to a particular determination role. */
    fallbackLanguageBespokeTerms?: Clause,
}



export interface GovernmentalAuthorityDefault {
    governmentalAuthorityDefaultIsApplicable: boolean,
    escrowArrangement?: EscrowArrangement,
    localSubstituteProvisionType?: string,
    specifiedValue?: string,
    maximumDaysOfDisruption?: ValuationPostponement,
}



export interface Illiquidity {
    illiquidityIsApplicable: boolean,
    escrowArrangement?: EscrowArrangement,
    minimumAmount?: Quantity,
    /** The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlement rate option. */
    fallbackReferencePrice?: SettlementRateOption,
    illiquidityValuationDate?: AdjustedRelativeDateOffset,
}



export interface MaterialChangeInCircumstance {
    materialChangeInCircumstanceIsApplicable: boolean,
    escrowArrangement?: EscrowArrangement,
    maximumDaysOfDisruption?: ValuationPostponement,
}



export interface Nationalization {
    nationalizationIsApplicable: boolean,
    relevantAffiliate?: AncillaryEntity[],
    escrowArrangement?: EscrowArrangement,
    assignmentOfClaim?: boolean,
    maximumDaysOfDisruption?: ValuationPostponement,
}



export interface PriceMateriality {
    priceMaterialityIsApplicable: boolean,
    escrowArrangement?: EscrowArrangement,
    /** The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlement rate option. */
    fallbackReferencePrice?: SettlementRateOption,
}



export interface FxPriceSourceDisruption {
    priceSourceDisruptionIsApplicable: boolean,
    /** The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlement rate option. */
    fallbackReferencePrice?: SettlementRateOption,
    escrowArrangement?: EscrowArrangement,
}



export interface FxBenchmarkObligationDefault {
    benchmarkObligationDefaultIsApplicable: boolean,
    localSubstituteProvisionType?: string,
    specifiedValue?: string,
    escrowArrangement?: EscrowArrangement,
}



export interface FxDualExchangeRate {
    dualExchangeRateIsApplicable: boolean,
    /** The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlement rate option. */
    fallbackReferencePrice?: FallbackReferencePrice,
    escrowArrangement?: EscrowArrangement,
}



export interface FxInconvertibilityOrNonTransferability {
    generalInconvertibility?: GeneralInconvertibility,
    generalNonTransferability?: GeneralNonTransferability,
    specificInconvertibility?: SpecificInconvertibility,
    specificNonTransferability?: SpecificNonTransferability,
}



export interface GeneralInconvertibility {
    generalInconvertibilityIsApplicable: boolean,
    localSubstituteProvisionType?: string,
    /** Specifies how long to wait to get a quote from a settlement rate option upon a price source disruption. */
    maximumDaysOfDisruption?: ValuationPostponement,
    nonDeliverableSubstitute?: NonDeliverableSubstitute,
    /** Refers to the segregation deposit procedure parties may want to commmit into during the time a Disruption Event is effective */
    escrowArrangement?: EscrowArrangement,
}



export interface GeneralNonTransferability {
    generalNonTransferabilityIsApplicable: boolean,
    localSubstituteProvisionType?: string,
    /** Specifies how long to wait to get a quote from a settlement rate option upon a price source disruption. */
    maximumDaysOfDisruption?: ValuationPostponement,
    nonDeliverableSubstitute?: NonDeliverableSubstitute,
    /** Refers to the segregation deposit procedure parties may want to commmit into during the time a Disruption Event is effective */
    escrowArrangement?: EscrowArrangement,
}



export interface SpecificInconvertibility {
    specificInconvertibilityIsApplicable: boolean,
    minimumAmount?: Quantity,
    relevantClass?: string[],
    localSubstituteProvisionType?: string,
    /** Specifies how long to wait to get a quote from a settlement rate option upon a price source disruption. */
    maximumDaysOfDisruption?: ValuationPostponement,
    nonDeliverableSubstitute?: NonDeliverableSubstitute,
    /** Refers to the segregation deposit procedure parties may want to commmit into during the time a Disruption Event is effective */
    escrowArrangement?: EscrowArrangement,
}



export interface SpecificNonTransferability {
    specificNonTransferabilityIsApplicable: boolean,
    relevantClass?: string[],
    localSubstituteProvisionType?: string,
    maximumDaysOfDisruption?: ValuationPostponement,
    nonDeliverableSubstitute?: NonDeliverableSubstitute,
    escrowArrangement?: EscrowArrangement,
}


/**
 * Refers to the segregation deposit procedure parties may want to commmit into during the time a Disruption Event is effective
 */
export interface EscrowArrangement {
    escrowArrangementIsApplicable: boolean,
    maximumDaysOfDisruption?: ValuationPostponement,
    bespokeEscrowArrangements?: Clause,
}


/**
 * Defines the currency referenced as such in any terms related to FX Disruption Events
 */
export interface EventCurrency {
    /** Defines the currency referenced as such in any terms related to FX Disruption Events */
    eventCurrency: string,
    /** Defines the Event Currency Amount to be exchanged on the Settlement Date. */
    eventCurrentAmount?: Quantity,
    /** Unless otherwised specified, the Buyer is the party to which the Event Currency Amount is owed on the Settlement Date and the Seller is the party which owes the Event Currency Amount on the Settlement Date. */
    eventCurrencyBuyerSeller?: BuyerSeller[],
}



export interface NonDeliverableSubstitute {
    nonDeliverableSubstituteIsApplicable: boolean,
    /** Details of the days on which calculations are to be made to determine the Delivery Amount and/or Return Amount. */
    valuationDate?: AdjustedRelativeDateOffset,
    /** The date for settlement of the transfer. */
    settlementDate?: AdjustedRelativeDateOffset,
    settlementRateOption?: SettlementRateOption,
}



export interface StandardizedSchedule {
    assetClass: string,
    productClass: string,
    notional: string,
    notionalCurrency: string,
    durationInYears?: string,
}



export interface StandardizedScheduleTradeInfo {
    assetClass?: string,
    productClass?: string,
    grossInitialMargin?: Money,
    markToMarketValue?: Money,
}



export interface StandardizedScheduleInitialMargin {
    tradeInfo?: StandardizedScheduleTradeInfo[],
    netInitialMargin: Money,
}


/**
 * Identifies an index by referencing an identifier.
 */
export interface IndexBase extends AssetBase {
    /** A description of the Index. */
    name?: string,
    /** The organisation that creates or maintains the Index. */
    provider?: LegalEntity,
    /** The Asset Class of the Index. */
    assetClass?: string,
}


/**
 * Specification of an interest rate index which can change over time, e.g. the SONIA (Sterling Overnight Index Average) in the UK.
 */
export interface FloatingRateIndex extends IndexBase {
    /** The reference index that is used to specify the floating interest rate. */
    floatingRateIndex: string,
    /** The ISDA Designated Maturity, i.e. the floating rate tenor. */
    indexTenor?: Period,
}


/**
 * Specification of a rate based on the exchange of a pair of cash assets in specific currencies, e.g. USD versus GBP.
 */
export interface ForeignExchangeRateIndex extends IndexBase {
    /** Describes the composition of a rate that has been quoted or is to be quoted. */
    quotedCurrencyPair: QuotedCurrencyPair,
    /** Specifies the primary source from which a rate should be observed. */
    primaryFxSpotRateSource: InformationSource,
    /** Specifies an alternative, or secondary, source from which a rate should be observed. */
    secondaryFxSpotRateSource?: InformationSource,
}


/**
 * Specification of an index that measures inflation in a specific market, e.g. the US Consumer Price Index.
 */
export interface InflationIndex extends IndexBase {
    /** The reference index that is used to specify the inflation interest rate. */
    inflationRateIndex: string,
    /** The ISDA Designated Maturity, i.e. the floating rate tenor. */
    indexTenor?: Period,
}


/**
 * Specification of an index based on credit risk, typically composed using corporate debt instruments in a region or industry sector, e.g. the iTraxx indices.
 */
export interface CreditIndex extends IndexBase {
    /** A CDS index series identifier, e.g. 1, 2, 3 etc. */
    indexSeries?: number,
    /** A CDS index series version identifier, e.g. 1, 2, 3 etc. */
    indexAnnexVersion?: number,
    /** A CDS index series annex date. */
    indexAnnexDate?: date,
    /** A CDS index series annex source. */
    indexAnnexSource?: string,
    /** Excluded reference entity. */
    excludedReferenceEntity?: ReferenceInformation[],
    /** This element contains CDS tranche terms. */
    tranche?: Tranche,
    /** Used to specify the Relevant Settled Entity Matrix when there are settled entities at the time of the trade. */
    settledEntityMatrix?: SettledEntityMatrix,
    /** Index Factor is the index version factor or percent, expressed as an absolute decimal value between 0 and 1, that multiplied by the original notional amount yields the notional amount covered by the seller of protection. */
    indexFactor?: string,
    /** Seniority of debt instruments comprising the index. */
    seniority?: string,
}


/**
 * Specification of an index based on equity securities, e.g. the S&P 500.
 */
export interface EquityIndex extends IndexBase {
}


/**
 * Specification of a user-defined index that does not meet the criteria of other Index data types.
 */
export interface OtherIndex extends IndexBase {
    /** A description that defines the OtherIndex. */
    description?: string,
}


/**
 * Defines a settlement as an exchange between two parties of a specified quantity of an asset (the quantity) against a specified quantity of another asset (the price). The settlement is optional and can be either cash or physical. The quantity can additionally be specified in terms of one or more currency amounts. In the case of non-cash products, the settlement of the price/quantity would not be specified here and instead would be delegated to the product mechanics, as parameterised by the price/quantity values.
 */
export interface PriceQuantity {
    /** Specifies a price to be used for trade amounts and other purposes. */
    price?: PriceSchedule[],
    /** Specifies a quantity to be associated with an event, for example a trade amount. */
    quantity?: NonNegativeQuantitySchedule[],
    /** Specifies the object to be observed for a price, it could be an asset or an index. The cardinality is optional as some quantity / price cases have no observable (e.g. a fixed rate in a given currency). */
    observable?: Observable,
    /** Specifies the date at which the price and quantity become effective. This day may be subject to adjustment in accordance with a business day convention, or could be specified as relative to a trade date, for instance. Optional cardinality, as the effective date is usually specified in the product definition, so it may only need to be specified as part of the PriceQuantity in an increase/decrease scenario for an existing trade. */
    effectiveDate?: AdjustableOrRelativeDate,
}


/**
 * Defines the inputs required to calculate a price as a simple composite of 2 other values. The inputs consist of 2 numbers and a simple arithmetic operator. This generic data type applies to a variety of use cases where a price is obtained by simple composition, e.g. dirty = clean + accrued (Bond), forward rate = spot rate + forward point (FX) etc.
 */
export interface PriceComposite {
    /** The 1st value in the arithmetic operation, which may be non-commutative in some cases: Subtract, Divide). This 1st operand is called 'baseValue' as it refers to the price anchor in the arithmetic operation: e.g. the clean price (Bond) or the spot rate (FX). */
    baseValue: string,
    /** The 2nd value in the arithmetic operation, which may be non-commutative in some cases: Subtract, Divide). The 2nd operand is called 'operand' to distinguish it from the 1st one which is the price anchor. */
    operand: string,
    /** Specifies the arithmetic operator via an enumeration. */
    arithmeticOperator: string,
    /** Optionally qualifies the type of operand: e.g. accrued or forward point. */
    operandType?: string,
}


/**
 * Specifies the price of a financial instrument in a trade as a schedule of measures. A price generically expresses the value of an exchange as a ratio: it measures the amount of one thing needed to be exchanged for 1 unit of another thing (e.g. cash in a specific currency in exchange for a bond or share). This generic representation can be used to support any type of financial price beyond just cash price: e.g. an interest rate, a foreign exchange rate, etc. This data type is generically based on a schedule and can also be used to represent a price as a single value.
 */
export interface PriceSchedule extends MeasureSchedule {
    /** Provides an attribute to define the unit of the thing being priced. For example, {amount, unitOfAmount, PerUnitOfAmount} = [10, EUR, Shares] = (10.00 EUR/SHARE) * (300,000 SHARES) = EUR 3,000,000.00 (Shares cancel out in the calculation). */
    perUnitOf?: UnitType,
    /** Specifies the price type as an enumeration: interest rate, exchange rate, asset price etc. This attribute is mandatory so that prices can always be clasiffied according to their type. The price type implies some constraints on the price's units. */
    priceType: string,
    /** Allows further classification of the chosen price type. */
    priceSubType?: string,
    /** (Optionally) Specifies whether the price is expressed in absolute or percentage terms. */
    priceExpression?: string,
    /** (Optionally) Specifies the underlying price components if the price can be expressed as a composite: e.g. dirty price = clean price + accrued. */
    composite?: PriceComposite,
    /** (Optionally) When the price is to be understood as an operator to apply to an observable, i.e. a spread, multiplier or min/max. */
    arithmeticOperator?: string,
    /** (Optionally) Additional attribute that can further define any premium to the price. */
    premiumType?: string,
}


/**
 * Specifies a price as a single value to be associated to a financial product. This data type extends PriceSchedule and requires that only the amount value exists.
 */
export interface Price extends PriceSchedule {
}


/**
 * Defines a custom basket by referencing an identifier and its constituents.
 */
export interface Basket extends AssetBase {
    /** Identifies the constituents of the basket */
    basketConstituent: BasketConstituent[],
}


/**
 * Identifies the constituents of the basket
 */
export interface BasketConstituent extends Observable {
    /** Specifies a quantity schedule to be associated to an individual underlier that is a basket constituent. The multiple cardinality is aligned to the one of the PriceQuantity->quantity that this quantity is referencing. */
    quantity?: NonNegativeQuantitySchedule[],
    /** Specifies an initial price schedule to be associated to an individual underlier that is a basket constituent. The multiple cardinality is aligned to the one of the PriceQuantity->price that this price is referencing. */
    initialValuationPrice?: PriceSchedule[],
    /** Specifies an interim price schedule to be associated to an individual underlier that is a basket constituent. The multiple cardinality is aligned to the one of the PriceQuantity->price that this price is referencing. */
    interimValuationPrice?: PriceSchedule[],
    /** Specifies a final price schedule to be associated to an individual underlier that is a basket constituent. The multiple cardinality is aligned to the one of the PriceQuantity->price that this price is referencing. */
    finalValuationPrice?: PriceSchedule[],
}


/**
 * A class defining the source for a piece of information (e.g. a rate fix or an FX fixing). The attribute names have been adjusted from FpML to address the fact that the information is not limited to rates.
 */
export interface InformationSource {
    /** An information source for obtaining a market data point. For example Bloomberg, Reuters, Telerate, etc. */
    sourceProvider: string,
    /** A specific page for the source for obtaining a market data point. In FpML, this is specified as a scheme, rateSourcePageScheme, for which no coding Scheme or URI is specified. */
    sourcePage?: string,
    /** The heading for the source on a given source page. */
    sourcePageHeading?: string,
}


/**
 * Defines a monetary amount in a specified currency.
 */
export interface Money extends Quantity {
}


/**
 * A class that describes the composition of a rate that has been quoted or is to be quoted. This includes the two currencies and the quotation relationship between the two currencies and is used as a building block throughout the FX specification.
 */
export interface QuotedCurrencyPair {
    /** The first currency specified when a pair of currencies is to be evaluated. */
    currency1: string,
    /** The second currency specified when a pair of currencies is to be evaluated. */
    currency2: string,
    /** The method by which the exchange rate is quoted. */
    quoteBasis: string,
}



export interface Curve {
    interestRateCurve?: InterestRateCurve,
    commodityCurve?: string,
}



export interface InterestRateCurve {
    /** The reference index that is used to specify the floating interest rate. */
    floatingRateIndex: string,
    tenor: Period,
}


/**
 *  A class to represent the transacted price attributes that are positioned as part of the FpML FeeLeg.
 */
export interface TransactedPrice {
    /** An optional element that only has meaning in a credit index trade. This element contains the credit spread ('fair value') at which the trade was executed. Unlike the fixedRate of an index, the marketFixedRate varies over the life of the index depending on market conditions. The marketFixedRate is the price of the index as quoted by trading desks. */
    marketFixedRate?: string,
    /** An optional element that contains the up-front points expressed as a percentage of the notional. An initialPoints value of 5% would be represented as 0.05. The initialPoints element is an alternative to marketFixedRate in quoting the traded level of a trade. When initialPoints is used, the traded level is the sum of fixedRate and initialPoints. The initialPoints is one of the items that are factored into the initialPayment calculation and is payable by the Buyer to the Seller. Note that initialPoints and marketFixedRate may both be present in the same document when both implied values are desired. */
    initialPoints?: string,
    /** An optional element that only has meaning in a credit index trade. This element contains the price at which the trade was executed and is used instead of marketFixedRate on credit trades on certain indicies which are quoted using a price rather than a spread. */
    marketPrice?: string,
    /** An optional element that contains the up-front points expressed as a percentage of the notional. An initialPoints value of 5% would be represented as 0.05. The initialPoints element is an alternative to marketFixedRate in quoting the traded level of a trade. When initialPoints is used, the traded level is the sum of fixedRate and initialPoints. The initialPoints is one of the items that are factored into the initialPayment calculation and is payable by the Buyer to the Seller. Note that initialPoints and marketFixedRate may both be present in the same document when both implied values are desired. */
    quotationStyle?: string,
}


/**
 * A complex type used to specify the option and convertible bond option strike when expressed in reference to a swap curve.
 */
export interface ReferenceSwapCurve {
    swapUnwindValue: SwapCurveValuation,
    /** Amount to be paid by the buyer of the option if the option is exercised prior to the Early Call Date. (The market practice in the convertible bond option space being that the buyer should be penalised if he/she exercises the option early on.) */
    makeWholeAmount?: MakeWholeAmount,
}


/**
 * A class to specify the amount to be paid by the buyer of the option if the option is exercised prior to the Early Call Date (typically applicable to the convertible bond options).
 */
export interface MakeWholeAmount extends SwapCurveValuation {
    /** The type of interpolation method that the calculation agent reserves the right to use. */
    interpolationMethod?: string,
    /** Date prior to which the option buyer will have to pay a Make Whole Amount to the option seller if he/she exercises the option. */
    earlyCallDate: date,
}


/**
 * A class to specify a valuation swap curve, which is used as part of the strike construct for the bond and convertible bond options.
 */
export interface SwapCurveValuation {
    /** The reference index that is used to specify the floating interest rate. */
    floatingRateIndex: string,
    /** The ISDA Designated Maturity, i.e. the tenor of the floating rate. */
    indexTenor?: Period,
    /** Spread in basis points over the floating rate index. */
    spread: string,
    /** The side (bid/mid/ask) of the measure. */
    side?: string,
}


/**
 * Defines how and when a performance type option or performance type swap is to be valued, including initial, interim and final valuation dates.
 */
export interface ValuationDates {
    /** Specifies the initial valuation dates of the underlyer. */
    initialValuationDate?: PerformanceValuationDates,
    /** Specifies the interim valuation dates of the underlyer. */
    interimValuationDate?: PerformanceValuationDates,
    /** Specifies the final valuation dates of the underlyer. */
    finalValuationDate: PerformanceValuationDates,
}


/**
 * Defines how and when a performance type option or performance type swap is to be valued.
 */
export interface PerformanceValuationDates {
    /** Specifies the method according to which an amount or a date is determined. */
    determinationMethod: string,
    /** 2018 ISDA CDM Equity Confirmation for Security Equity Swap: Pricing Date */
    valuationDates?: AdjustableRelativeOrPeriodicDates,
    /** 2018 ISDA CDM Equity Confirmation for Security Equity Swap: Pricing Date */
    valuationDate?: AdjustableOrRelativeDate,
    /** The specific time of day at which the calculation agent values the underlying. The SpecificTime is the only case when the valuationTime (time + business center location  e.g. 10:00:00 USNY) should be provided. You should be able to provide just the valuationTime without valuationTimeType, which infer that this is a specific time. */
    valuationTime?: BusinessCenterTime,
    /** The time of day at which the calculation agent values the underlying, for example the official closing time of the exchange. */
    valuationTimeType?: string,
}


/**
 * Describes a rate source to be fixed and the date the fixing occurs
 */
export interface FxRateSourceFixing {
    settlementRateSource: FxSettlementRateSource,
    /** The date on which the fixing is scheduled to occur. */
    fixingDate: AdjustableDate,
}


/**
 * Information source specific to Foreign Exchange products.
 */
export interface FxInformationSource extends InformationSource {
    /** The time that the fixing will be taken along with a business center to define the time zone. */
    fixingTime?: BusinessCenterTime,
}


/**
 * The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlement rate option.
 */
export interface FallbackReferencePrice {
    /** Specifies how long to wait to get a quote from a settlement rate option upon a price source disruption. */
    valuationPostponement?: ValuationPostponement,
    /** This settlement rate option will be used in its place. */
    fallBackSettlementRateOption?: string,
    /** Request rate quotes from the market. This element is set as type Empty in FpML. When present, the FpML synonym is mapped to a value True in the CDM. */
    fallbackSurveyValuationPostponement?: boolean,
    /** The calculation agent will decide the rate. */
    calculationAgentDetermination?: CalculationAgent,
}


/**
 * Specifies how long to wait to get a quote from a settlement rate option upon a price source disruption.
 */
export interface ValuationPostponement {
    /** The maximum number of days to wait for a quote from the disrupted settlement rate option before proceeding to the next method. */
    maximumDaysOfPostponement: number,
}


/**
 * A class describing the rate of a currency conversion: pair of currency, quotation mode and exchange rate.
 */
export interface FxRate {
    /** Defines the two currencies for an FX trade and the quotation relationship between the two currencies. */
    quotedCurrencyPair: QuotedCurrencyPair,
    /** The rate of exchange between the two currencies of the leg of a deal. Must be specified with a quote basis. */
    rate?: string,
}


/**
 * A class defining the rate source and fixing time for an FX rate.
 */
export interface FxSpotRateSource {
    /** The primary source for where the rate observation will occur. Will typically be either a page or a reference bank published rate. */
    primarySource: InformationSource,
    /** An alternative, or secondary, source for where the rate observation will occur. Will typically be either a page or a reference bank published rate. */
    secondarySource?: InformationSource,
}


/**
 * A class defining parameters associated with an individual observation or fixing. This class forms part of the cashflow representation of a stream.
 */
export interface RateObservation {
    /** The reset date. */
    resetDate?: date,
    /** The adjusted fixing date, i.e. the actual date the rate is observed. The date should already be adjusted for any applicable business day convention. */
    adjustedFixingDate?: date,
    /** The actual observed rate before any required rate treatment is applied, e.g. before converting a rate quoted on a discount basis to an equivalent yield. An observed rate of 5% would be represented as 0.05. */
    observedRate?: string,
    /** The observed rate after any required rate treatment is applied. A treated rate of 5% would be represented as 0.05. */
    treatedRate?: string,
    /** The number of days weighting to be associated with the rate observation, i.e. the number of days such rate is in effect. This is applicable in the case of a weighted average method of calculation where more than one reset date is established for a single calculation period. */
    observationWeight?: number,
    /** A pointer style reference to a floating rate component defined as part of a stub calculation period amount component. It is only required when it is necessary to distinguish two rate observations for the same fixing date which could occur when linear interpolation of two different rates occurs for a stub calculation period. */
    rateReference?: RateObservation,
    /** The value representing the forecast rate used to calculate the forecast future value of the accrual period.A value of 1% should be represented as 0.01. */
    forecastRate?: string,
    /** The value representing the forecast rate after applying rate treatment rules. A value of 1% should be represented as 0.01. */
    treatedForecastRate?: string,
}


/**
 * Represents a class to specify the credit notation as the combination of agency, notation, scale and debt type qualifications.
 */
export interface CreditNotation {
    /** Specifies The credit agency to which the other variables (notation, scale, debt type) refer to. */
    agency: string,
    /** Specifies The credit rating notation. As it varies among credit rating agencies, FpML doesn't specify a default scheme. */
    notation: string,
    /** Specifies the credit rating scale, with a typical distinction between short term, long term. FpML doesn't specify a default scheme, which is hence not specified as an enumeration as part of the CDM. */
    scale?: string,
    /** Specifies the credit rating debt type (e.g. long term, high yield, deposits, ...) associated with the credit rating notation and scale. */
    debt?: CreditRatingDebt,
    /** Assesses the potential direction of a long-term credit rating over the intermediate term, which is generally up to two years for investment grade and generally up to one year for speculative grade. */
    outlook?: string,
    /** Indicates the potential direction of a short-term or long-term rating. It focuses on identifiable events and short-term trends that cause ratings to be placed under special surveillance. */
    creditWatch?: string,
}


/**
 * Represents the credit rating notation higher level construct, which provides the ability to specify multiple rating notations.
 */
export interface CreditNotations {
    /** Specifies only one credit notation is determined. */
    creditNotation?: CreditNotation,
    /** Specifies if several credit notations exist, alongside an 'any' or 'all' or all condition. */
    creditNotations?: MultipleCreditNotations,
}


/**
 * Specifies the credit rating debt type(s) associated with the credit rating notation and scale. When several debt types are specified, they must be qualified through an 'any' or 'all'.
 */
export interface CreditRatingDebt {
    /** Specifies when there is only one debt type. FpML doesn't specify values in relation to the associated scheme, which is hence not specified as an enumeration as part of the CDM. */
    debtType?: string,
    /** Specifies if there are several debt types, alongside an 'any' or 'all' or all condition. As an example, Baa1 rating is required for any long term debt and deposit. */
    debtTypes?: MultipleDebtTypes,
}


/**
 * Represetns a class to specify multiple credit notations alongside a conditional 'any' or 'all' qualifier.
 */
export interface MultipleCreditNotations {
    /** An enumerated element, to qualify whether All or Any credit notation applies. */
    condition: string,
    /** At least two credit notations much be specified. */
    creditNotation: CreditNotation[],
    mismatchResolution?: string,
    referenceAgency?: string,
}


/**
 * Represents a class to specify multiple credit debt types alongside a conditional 'any' or 'all' qualifier.
 */
export interface MultipleDebtTypes {
    /** An enumerated attribute, to qualify whether All or Any debt type applies. */
    condition: string,
    /** The type of debt, e.g. long term debt, deposit, ... FpML doesn't specific a scheme value, hence no enumeration is specified as part of the CDM. At least two debt types much be specified. */
    debtType: string[],
}


/**
 * The source of the Foreign Exchange settlement rate.
 */
export interface FxSettlementRateSource {
    /** Indicates that an officially defined rate settlement rate option will be the used for the fixing. */
    settlementRateOption?: string,
    /** Indicates that a non-standard rate source will be used for the fixing. */
    nonstandardSettlementRate?: FxInformationSource,
}


/**
 * A class defining the ISDA calculation agent responsible for performing duties as defined in the applicable product definitions.
 */
export interface CalculationAgent {
    /** Specifies the party which is the ISDA Calculation Agent for the trade. If more than one party is referenced then the parties are assumed to be co-calculation agents, i.e. they have joint responsibility. */
    calculationAgentParty?: string,
    /** Specifies the ISDA calculation agent responsible for performing duties as defined in the applicable product definitions. For example, the Calculation Agent may be defined as being the Non-exercising Party. */
    calculationAgentPartyEnum?: string,
    /** The city in which the office through which ISDA Calculation Agent is acting for purposes of the transaction is located The short-form confirm for a trade that is executed under a Sovereign or Asia Pacific Master Confirmation Agreement ( MCA ), does not need to specify the Calculation Agent. However, the confirm does need to specify the Calculation Agent City. This is due to the fact that the MCA sets the value for Calculation Agent but does not set the value for Calculation Agent City. */
    calculationAgentBusinessCenter?: string,
}


/**
 * Defines the settlement rate option to use for fixing in case of cash settlement. Currently only applicable to foreign exchange fixing in case of cross-currency settlement.
 */
export interface SettlementRateOption {
    /** The rate source for the conversion to the settlement currency. This source is specified through a scheme that reflects the terms of the Annex A to the 1998 FX and Currency Option Definitions. */
    settlementRateOption: string,
    /** An attribute defining the parameters to get a new quote when a settlement rate option is disrupted. */
    priceSourceDisruption?: PriceSourceDisruption,
}



export interface MultipleValuationDates extends SingleValuationDate {
    /** The number of business days between successive valuation dates when multiple valuation dates are applicable for cash settlement. ISDA 2003 Term: Business Days thereafter. */
    businessDaysThereafter?: number,
    /** Where multiple valuation dates are specified as being applicable for cash settlement, this element specifies (a) the number of applicable valuation dates, and (b) the number of business days after satisfaction of all conditions to settlement when the first such valuation date occurs, and (c) the number of business days thereafter of each successive valuation date. ISDA 2003 Term: Multiple Valuation Dates. */
    numberValuationDates?: number,
}


/**
 * A class to specify the number of business days after satisfaction of all conditions to settlement.
 */
export interface SingleValuationDate {
    /** A number of business days. Its precise meaning is dependant on the context in which this element is used. ISDA 2003 Term: Business Day. */
    businessDays?: number,
}


/**
 * A class describing the method for obtaining a settlement rate, specified through either an information source (page), a settlement rate option (fixing) or by using quotes from reference banks.
 */
export interface ValuationSource {
    /** Defines the two currencies for an FX trade and the quotation relationship between the two currencies.  This attribute was formerly part of 'fxSettlementTerms', which is now being harmonised into a common 'CashSettlementTerms' that includes a 'ValuationDate'. */
    quotedCurrencyPair?: QuotedCurrencyPair,
    /** The information source where a published or displayed market rate will be obtained, e.g. Telerate Page 3750. */
    informationSource?: FxSpotRateSource,
    /** The rate option to use for the fixing. Currently only applicable to foreign exchange fixing in case of cross-currency settlement. */
    settlementRateOption?: SettlementRateOption,
    /** A container for a set of reference institutions that may be called upon to provide rate quotations as part of the method to determine the applicable cash settlement amount. If institutions are not specified, it is assumed that reference institutions will be agreed between the parties on the exercise date, or in the case of swap transaction to which mandatory early termination is applicable, the cash settlement valuation date. */
    referenceBanks?: ReferenceBanks,
    /** Holds an identifier for the reference entity that is agreed by both parties as a basis for cash settlement calculations. This could be a dealer from whom quotations are obtained by the calculation agent on the reference obligation for purposes of cash settlement in a credit event. ISDA 2003 Term: Dealer. This could be the clearing organization (CCP, DCO) to which the trade should be cleared, as applicable for cash-settled swaptions. */
    dealerOrCCP?: AncillaryEntity,
}


/**
 * A data defining:  the parameters used to get a price quote to replace the settlement rate option that is disrupted.
 */
export interface PriceSourceDisruption {
    /** The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlement rate option. */
    fallbackReferencePrice: FallbackReferencePrice,
}


/**
 * Specifies the parameters required to obtain a valuation, including the source, quotation method (bid, mid etc.) and any applicable quotation amount.
 */
export interface ValuationMethod {
    /** The source for obtaining a valuation. This may come from some information source (e.g. Reuters), from a rate option fixing (e.g. FX fixing for cross-currency settlement), or from a set of reference banks. This is a mandatory attribute as the valuation method relies on one of those sources to be specified. */
    valuationSource: ValuationSource,
    /** The type of price quotations to be requested from dealers when determining the market value of the reference obligation for purposes of cash settlement, or which rate quote is to be observed for a fixing. For example, Bid, Offer, Mid-market or Exercising Party Pays. ISDA 2003 Term: Quotation Method. The meaning of Exercising Party Pays is defined in the 2000 ISDA Definitions, Section 17.2. Certain Definitions Relating to Cash Settlement, paragraph (j). */
    quotationMethod?: string,
    /** The ISDA defined methodology for determining the final price of the reference obligation for purposes of cash settlement. (ISDA 2003 Term: Valuation Method). For example, Market, Highest etc. */
    valuationMethod?: string,
    /** In the determination of a cash settlement amount, if weighted average quotations are to be obtained, the quotation amount specifies an upper limit to the outstanding principal balance of the reference obligation for which the quote should be obtained. If not specified, the ISDA definitions provide for a fallback amount equal to the floating rate payer calculation amount. ISDA 2003 Term: Quotation Amount. */
    quotationAmount?: Money,
    /** In the determination of a cash settlement amount, if weighted average quotations are to be obtained, the minimum quotation amount specifies a minimum intended threshold amount of outstanding principal balance of the reference obligation for which the quote should be obtained. If not specified, the ISDA definitions provide for a fallback amount of the lower of either USD 1,000,000 (or its equivalent in the relevant obligation currency) or the quotation amount. ISDA 2003 Term: Minimum Quotation Amount. */
    minimumQuotationAmount?: Money,
    /** Specifies the parameters representing several mid-market valuation and replacement value methods. */
    cashCollateralValuationMethod?: CashCollateralValuationMethod,
}


/**
 * This type is a generic structure that can represent the parameters of several mid-market valuation and replacement value methods described in the 2021 ISDA Definitions.
 */
export interface CashCollateralValuationMethod {
    /** This may be used to specify what type of CSA (credit support annex/agreement) is to be used for cash settlement purposes. */
    applicableCsa?: string,
    /** This may be used to indicate the currency of cash collateral for cash settlement purposes. */
    cashCollateralCurrency?: string,
    /** This may be used to indicate the interest rate to be used for cash collateral for cash settlement purposes. */
    cashCollateralInterestRate?: string,
    /** This may be used to indicate the discount rate to be used for cash collateral for cash settlement purposes. */
    agreedDiscountRate?: string,
    /** This may be used to specify which party is protected (e.g. under Replacement Value cash settlement methods). */
    protectedParty?: string,
    /** This may be used to indicate that 'prescribed documentation adjustment' is applicable. */
    prescribedDocumentationAdjustment?: boolean,
}


/**
 * The parameters which define whether dividends are applicable
 */
export interface DividendApplicability {
    /** If present and true, then options exchange dividends are applicable. */
    optionsExchangeDividends?: boolean,
    /** If present and true, then additional dividends are applicable. */
    additionalDividends?: boolean,
    /** Represents the European Master Confirmation value of 'All Dividends' which, when applicable, signifies that, for a given Ex-Date, the daily observed Share Price for that day is adjusted (reduced) by the cash dividend and/or the cash value of any non cash dividend per Share (including Extraordinary Dividends) declared by the Issuer. All Dividends in accordance with the ISDA 2002 Equity Derivatives Definitions. */
    allDividends?: boolean,
}


/**
 * An Index is an Observable which is computed based on the prices, rates or valuations of a number of assets that are tracked in a standardized way.  Examples include equity market indices as well as indices on interest rates, inflation and credit instruments.
 */
export interface Index {
}


/**
 * An index based in interest rates or inflation rates in a certain market.
 */
export interface InterestRateIndex {
}


/**
 * Specifies the object to be observed for a price, it could be an asset or a reference.
 */
export interface Observable {
}


/**
 * Defines the structures needed to represent the calculation parameters for daily averaged and compounded modular rates as defined in the 2021 ISDA Definitions in Section 7. This type is used to represent modular computed rates in interestRatePayouts.
 */
export interface FloatingRateCalculationParameters {
    /** calculation type (averaging or compounding). */
    calculationMethod: string,
    /** any obervation shift parameters if applicable. */
    observationShiftCalculation?: ObservationShiftCalculation,
    /** any lookback  parameters if applicable. */
    lookbackCalculation?: OffsetCalculation,
    /** any lockout  parameters if applicable. */
    lockoutCalculation?: OffsetCalculation,
    /** the business days that are applicable for the calculation. */
    applicableBusinessDays?: BusinessCenters,
    /**  any applicable observation parameters, such as daily caps or floors. */
    observationParameters?: ObservationParameters,
}


/**
 * Defines the structure needed to represent fallback rate parameters. This type is used to represent modular computed rates in interestRatePayouts.
 */
export interface FallbackRateParameters {
    /** The floating rate index that is used as the basis of the fallback rate. */
    floatingRateIndex: string,
    /** The date the fallback rate takes effect. */
    effectiveDate?: date,
    /** Support for modular calculated rates, such such as lockout compound calculations. */
    calculationParameters?: FloatingRateCalculationParameters,
    /** The economic spread applied to the underlying fallback rate to replicate the original risky rate. */
    spreadAdjustment?: string,
}


/**
 * Defines business day shifts for daily componded or averaged rates.  This type is used for lookback and lockout rates. This type is used to represent modular computed rates in interestRatePayouts.
 */
export interface OffsetCalculation {
    /** The number of business days offset. */
    offsetDays?: number,
}


/**
 * Parameters to describe the observation shift for a daily compounded or averaged floating rate. This type is used to represent modular computed rates in interestRatePayouts.
 */
export interface ObservationShiftCalculation {
    /** The number of days of observation shift. */
    offsetDays?: number,
    /** Whether the rate is calculated in advance, in arrears, or relative to a reset date. */
    calculationBase?: string,
    /** Any additional business days that be applicable. */
    additionalBusinessDays?: BusinessCenters,
}


/**
 * Parameters on daily observed computed rates, specifically daily caps and floors. This type is used to represent modular computed rates in interestRatePayouts.
 */
export interface ObservationParameters {
    /** A daily observation cap rate. */
    observationCapRate?: string,
    /** A daily observation floor rate. */
    observationFloorRate?: string,
}


/**
 * Type for reporting details of calculated rates, including the observations that went into the final reported rate.
 */
export interface CalculatedRateDetails {
    /** The observation dates and weights for each observation date. */
    observations?: CalculatedRateObservations,
    /** The weighted value of each observation. */
    weightedRates?: string[],
    /** The daily growth factors, showing the weighted rates divided by the day count basis plus one, giving how much the value grows for each step in the calculation. */
    growthFactor?: string[],
    /** The compounding curve, showing how the initial value grew during the calculation period. */
    compoundedGrowth?: string[],
    /** The total sum or product of all the individual terms that went into the calculated rate. */
    aggregateValue?: string,
    /** The total weight of all the terms that went into the calculated rate. */
    aggregateWeight?: string,
    /** The resulting calculated weight. */
    calculatedRate?: string,
}


/**
 * Type for reporting observations that went into the final reported rate.
 */
export interface CalculatedRateObservations {
    /** The observation date upon which the rate is observed. */
    observationDates?: date[],
    /** The corresponding weight for each date. */
    weights?: string[],
    /** The value observed for that date */
    observedRates?: string[],
    /** The value after any processing, such as application of caps or floors. */
    processedRates?: string[],
}


/**
 * Type for reporting the observations dates and the corresponding weights going into a daily calculated rate
 */
export interface CalculatedRateObservationDatesAndWeights {
    /** The observation date upon which the rate is observed. */
    observationDates?: date[],
    /** The corresponding weight for each date. */
    weights?: string[],
}



export interface FloatingRateIndexDefinition {
    /** The underlying FRO name and designated maturity. */
    fro: FloatingRateIndexIdentification,
    /** Any calculation default values. */
    calculationDefaults?: FloatingRateIndexCalculationDefaults,
    /** The definition version or versions supported by the FRO. */
    supportedDefinition?: ContractualDefinition[],
    /** The source of an FRO, particularly if not a Contractual Definition (e.g. the broker rates matrix). */
    definitionalSource?: string,
    designatedMaturityApplicable?: boolean,
    /** Any mappings to other FRos. */
    mappings?: FloatingRateIndexMappings,
    /** Any mappings to other codes for this index. */
    externalMappings?: FloatingRateIndexExternalMappings,
    /** YES / NO to flag FROs identified by the FpML Syndicated Loan WG as having underlying benchmark that may also be referenced in syndicated loans. */
    inLoan?: boolean,
    /** FRO History */
    history?: FroHistory,
    /** Deprecation and Code Descriptions */
    deprecationReason?: string,
    /** FpML Description */
    fpmlDescription?: string,
}



export interface FloatingRateIndexIdentification {
    /** The reference index that is used to specify the floating interest rate. The FpML standard maintains the list of such indices, which are positioned as enumeration values as part of the CDM. */
    floatingRateIndex?: string,
    /** FRO currency - 3 character ISO currrency code */
    currency?: string,
    /** FRO type (e.g. OIS) */
    froType?: string,
}


/**
 * This holds the rate calculation defaults applicable for a floating rate index.
 */
export interface FloatingRateIndexCalculationDefaults {
    /** The ISDA FRO category (e.g. screen rate or calculated rate). */
    category?: string,
    /** The ISDA FRO style (e.g. term rate, swap rate, etc). */
    indexStyle?: string,
    /** The ISDA FRO calculation method (e.g. OIS Compounding). */
    method?: string,
    /** The default fixing details. */
    fixing?: FloatingRateIndexFixingDetails[],
    /** The default day count fraction. */
    dayCountFraction?: string,
    /** The default applicable business days. */
    applicableBusinessDays?: BusinessCenters,
    /** Publication Calendar (e.g. EUR-ICESWAP) */
    publicationCalendar?: string,
}



export interface ContractualDefinition {
    /** Corresponds to the unique identifier of the Contractual Definition in which the code is published */
    identifier?: Identifier,
    /** Contractual Definition Identifier in which the code is published. Includes Document Type and Document Version */
    contractualDefinitionIdentifier?: ContractualDefinitionIdentifier,
    /** 2021-06-11 */
    publicationDate?: date,
}



export interface ContractualDefinitionIdentifier {
    /** e.g. ISDA2021Definitions */
    contractualDefinitionType: string,
    /** e.g. V1 */
    contractualDefinitionVersion?: string,
}


/**
 * A map for a single FRO to or from an equivalent or similar FRO in a different contractual definitions version.
 */
export interface FloatingRateIndexMap {
    /**  The FRO name that is being mapped to/from. */
    index_?: string,
    /** Contractual Definition to which the map applies. Includes Document Type and Document Version */
    contractualDefinitionIdentifier?: ContractualDefinitionIdentifier,
    /** Corresponds to the unique identifier of the Contractual Definition to which the map applies */
    identifier?: Identifier,
}


/**
 * This type defines mappings between FROs in different definitional versions.
 */
export interface FloatingRateIndexMappings {
    /** The successor FRO that this index maps to. */
    mapsTo?: FloatingRateIndexMap,
    /** The predecessor FRO(s) that this index maps to. */
    mapsFrom?: FloatingRateIndexMap[],
}


/**
 * A map for a single FRO to or from an equivalent or similar codes in a different standard such as ISO.
 */
export interface FloatingRateIndexExternalMap {
    /**  The FRO name that is being mapped to/from. */
    externalId: string,
    /** The standard/version to which the map applies. */
    externalStandard?: string,
}


/**
 * Represents the mappings of FRO codes to other.
 */
export interface FloatingRateIndexExternalMappings {
    isoCode?: FloatingRateIndexExternalMap,
}


/**
 * FRO History
 */
export interface FroHistory {
    /** The date the Floating Rate Option was added to the 2006 Definitions or 2021 Floating Rate Matrix. (e.g. 2017/04/06) */
    startDate?: date,
    /** The supplement or version the FRO was first added to the 2006 Definitions or 2021 Floating Rate Matrix. (e.g. S52) */
    firstDefinedIn?: ContractualDefinition,
    /** The date the Floating Rate Option was last updated in the 2006 Definitions or 2021 Floating Rate Matrix. (e.g. 2021/06/11) */
    updateDate?: date,
    /** The supplement or version the FRO was last updated in the 2006 Definitions or 2021 Floating Rate Matrix. (e.g. FRO-M-V1) */
    lastUpdatedIn?: ContractualDefinition,
    /** The date the Floating Rate Option was removed from the 2006 Definitions or 2021 Floating Rate Matrix. (e.g. 2014/01/01) */
    endDate?: date,
}


/**
 * This type holds parameters defining the fixingt time and offset for a floating rate index.
 */
export interface FloatingRateIndexFixingDetails {
    /** Parameters defining the normal fixing time (can vary by index tenor / designated maturity). */
    fixingTime?: FloatingRateIndexFixingTime,
    /** Parameters defining the normal fixing offset (can vary by index tenor / designated maturity). */
    fixingOffset?: BusinessDayOffset,
}


/**
 * This type holds parameters defining the normal fixing time for a floating rate index.
 */
export interface FloatingRateIndexFixingTime extends BusinessCenterTime {
    /** Allows a designed maturity to be specified for the fixing time. */
    designatedMaturity?: string,
    /** Legal text that underlies the Fixing Time. ISDA Fixing Time Definition. (e.g. 09:30, Sydney time). */
    fixingTimeDefinition?: string,
    /** Fixing Reason */
    fixingReason?: string,
}


/**
 * This type holds parameters defining the normal fixing offset for a floating rate index.
 */
export interface FloatingRateIndexFixingOffset extends BusinessDayOffset {
    /** Allows a reason to be specified for using the alternative fixing offset. */
    designatedMaturity?: string,
}


/**
 * This allows an offset to be specified as, for instance, N business days, with a business centers specified as included.
 */
export interface BusinessDayOffset extends Period {
    /** The business centers for the offset. */
    businessCenters?: BusinessCenters,
    /** Legal text that underlies the Fixing Offset. ISDA Fixing Offset Definition. (e.g. One day that is either a Sydney Business Day or a Melbourne Business Day following the Reset Date) */
    fixingOffsetDefinition?: string,
    /** Fixing Offset Reason */
    fixingOffsetReason?: string,
}


/**
 * Trigger point at which feature is effective.
 */
export interface Trigger {
    /** The trigger level. */
    level?: PriceSchedule[],
    creditEvents?: CreditEvents,
    creditEventsReference?: CreditEvents,
    /** The Triggering condition. */
    triggerType?: string,
    /** The valuation time type of knock condition. */
    triggerTimeType?: string,
}


/**
 * Observation point for trigger.
 */
export interface TriggerEvent {
    /** A derivative schedule. */
    schedule?: AveragingSchedule[],
    /** The trigger Dates. */
    triggerDates?: DateList,
    /** The trigger level */
    trigger: Trigger,
    /** The feature payment, i.e. the payment made following trigger occurrence. */
    featurePayment?: FeaturePayment,
}


/**
 * A class to specify the applicable Credit Events that would trigger a settlement, as specified in the related Confirmation and defined in the ISDA 2014 Credit Definition article IV section 4.1.
 */
export interface CreditEvents {
    /** A credit event. The reference entity has been dissolved or has become insolvent. It also covers events that may be a precursor to insolvency such as instigation of bankruptcy or insolvency proceedings. Sovereign trades are not subject to Bankruptcy as 'technically' a Sovereign cannot become bankrupt. ISDA 2003 Term: Bankruptcy. */
    bankruptcy?: boolean,
    /** A credit event. This credit event triggers, after the expiration of any applicable grace period, if the reference entity fails to make due payments in an aggregate amount of not less than the payment requirement on one or more obligations (e.g. a missed coupon payment). ISDA 2003 Term: Failure to Pay. */
    failureToPay?: FailureToPay,
    /** A credit event. Corresponds to the failure by the Reference Entity to pay an expected principal amount or the payment of an actual principal amount that is less than the expected principal amount. ISDA 2003 Term: Failure to Pay Principal. */
    failureToPayPrincipal?: boolean,
    /** A credit event. Corresponds to the failure by the Reference Entity to pay an expected interest amount or the payment of an actual interest amount that is less than the expected interest amount. ISDA 2003 Term: Failure to Pay Interest. */
    failureToPayInterest?: boolean,
    /** A credit event. One or more of the obligations have become capable of being declared due and payable before they would otherwise have been due and payable as a result of, or on the basis of, the occurrence of a default, event of default or other similar condition or event other than failure to pay. ISDA 2003 Term: Obligation Default. */
    obligationDefault?: boolean,
    /** A credit event. One or more of the obligations have been declared due and payable before they would otherwise have been due and payable as a result of, or on the basis of, the occurrence of a default, event of default or other similar condition or event other than failure to pay (preferred by the market over Obligation Default, because more definitive and encompasses the definition of Obligation Default - this is more favorable to the Seller). Subject to the default requirement amount. ISDA 2003 Term: Obligation Acceleration. */
    obligationAcceleration?: boolean,
    /** A credit event. The reference entity, or a governmental authority, either refuses to recognise or challenges the validity of one or more obligations of the reference entity, or imposes a moratorium thereby postponing payments on one or more of the obligations of the reference entity. Subject to the default requirement amount. ISDA 2003 Term: Repudiation/Moratorium. */
    repudiationMoratorium?: boolean,
    /** A credit event. A restructuring is an event that materially impacts the reference entity's obligations, such as an interest rate reduction, principal reduction, deferral of interest or principal, change in priority ranking, or change in currency or composition of payment. ISDA 2003 Term: Restructuring. */
    restructuring?: Restructuring,
    /** A credit event. A governmental intervention is an event resulting from an action by a governmental authority that materially impacts the reference entity's obligations, such as an interest rate reduction, principal reduction, deferral of interest or principal, change in priority ranking, or change in currency or composition of payment. ISDA 2014 Term: Governmental Intervention. */
    governmentalIntervention?: boolean,
    /** A credit event. Results from the fact that the rating of the reference obligation is down-graded to a distressed rating level. From a usage standpoint, this credit event is typically not applicable in case of RMBS trades. */
    distressedRatingsDowngrade?: boolean,
    /** A credit event. Results from the fact that the underlier fails to make principal payments as expected. */
    maturityExtension?: boolean,
    /** A credit event. Results from the fact that the underlier writes down its outstanding principal amount. */
    writedown?: boolean,
    /** A credit event. Results from the fact that losses occur to the underlying instruments that do not result in reductions of the outstanding principal of the reference obligation. */
    impliedWritedown?: boolean,
    /** In relation to certain credit events, serves as a threshold for Obligation Acceleration, Obligation Default, Repudiation/Moratorium and Restructuring. Market standard is USD 10,000,000 (JPY 1,000,000,000 for all Japanese Yen trades). This is applied on an aggregate or total basis across all Obligations of the Reference Entity. Used to prevent technical/operational errors from triggering credit events. ISDA 2003 Term: Default Requirement. */
    defaultRequirement?: Money,
    /** A specified condition to settlement. An irrevocable written or verbal notice that describes a credit event that has occurred. The notice is sent from the notifying party (either the buyer or the seller) to the counterparty. It provides information relevant to determining that a credit event has occurred. This is typically accompanied by Publicly Available Information. ISDA 2003 Term: Credit Event Notice. */
    creditEventNotice?: CreditEventNotice,
}



export interface CreditEventNotice {
    /** The notifying party is the party that notifies the other party when a credit event has occurred by means of a credit event notice. If more than one party is referenced as being the notifying party then either party may notify the other of a credit event occurring. ISDA 2003 Term: Notifying Party. */
    notifyingParty: string,
    /** Inclusion of this business center element implies that Greenwich Mean Time in Section 3.3 of the 2003 ISDA Credit Derivatives Definitions is replaced by the local time of the city indicated by the businessCenter element value. */
    businessCenter?: string,
    /** A specified condition to settlement. Publicly available information means information that reasonably confirms any of the facts relevant to determining that a credit event or potential repudiation/moratorium, as applicable, has occurred. The ISDA defined list (2003) is the market standard and is considered comprehensive, and a minimum of two differing public sources must have published the relevant information, to declare a Credit Event. ISDA 2003 Term: Notice of Publicly Available Information Applicable. */
    publiclyAvailableInformation?: PubliclyAvailableInformation,
}



export interface FailureToPay {
    /** Indicates whether the failure to pay provision is applicable. */
    applicable: boolean,
    /** If this element is specified, indicates whether or not a grace period extension is applicable. ISDA 2003 Term: Grace Period Extension Applicable. */
    gracePeriodExtension?: GracePeriodExtension,
    /** Specifies a threshold for the failure to pay credit event. Market standard is USD 1,000,000 (JPY 100,000,000 for Japanese Yen trades) or its equivalent in the relevant obligation currency. This is applied on an aggregate basis across all Obligations of the Reference Entity. Intended to prevent technical/operational errors from triggering credit events. ISDA 2003 Term: Payment Requirement */
    paymentRequirement?: Money,
}



export interface GracePeriodExtension {
    /** Indicates whether the grace period extension provision is applicable. */
    applicable: boolean,
    /** The number of calendar or business days after any due date that the reference entity has to fulfil its obligations before a failure to pay credit event is deemed to have occurred. ISDA 2003 Term: Grace Period. */
    gracePeriod?: Offset,
}



export interface PubliclyAvailableInformation {
    /** If this element is specified and set to 'true', indicates that ISDA defined Standard Public Sources are applicable. */
    standardPublicSources?: boolean,
    /** A public information source, e.g. a particular newspaper or electronic news service, that may publish relevant information used in the determination of whether or not a credit event has occurred. ISDA 2003 Term: Public Source. */
    publicSource?: string[],
    /** The minimum number of the specified public information sources that must publish information that reasonably confirms that a credit event has occurred. The market convention is two. ISDA 2003 Term: Specified Number. */
    specifiedNumber?: number,
}



export interface Restructuring {
    /** Indicates whether the restructuring provision is applicable. */
    applicable: boolean,
    /** Specifies the type of restructuring that is applicable. */
    restructuringType?: string,
    /** In relation to a restructuring credit event, unless multiple holder obligation is not specified restructurings are limited to multiple holder obligations. A multiple holder obligation means an obligation that is held by more than three holders that are not affiliates of each other and where at least two thirds of the holders must agree to the event that constitutes the restructuring credit event. ISDA 2003 Term: Multiple Holder Obligation. */
    multipleHolderObligation?: boolean,
    /** Presence of this element and value set to 'true' indicates that Section 3.9 of the 2003 Credit Derivatives Definitions shall apply. Absence of this element indicates that Section 3.9 shall not apply. NOTE: Not allowed under ISDA Credit 1999. */
    multipleCreditEventNotices?: boolean,
}


/**
 * Payment made following trigger occurrence.
 */
export interface FeaturePayment {
    /** This attribute doesn't exist as part of the FpML construct, which makes use of the PayerReceiver.model group. */
    payerReceiver: PartyReferencePayerReceiver,
    /** The trigger level percentage. */
    levelPercentage?: string,
    /** The monetary quantity in currency units. */
    amount?: string,
    /** The feature payment time. */
    time?: string,
    /** The currency in which an amount is denominated. */
    currency?: string,
    /** The feature payment date. */
    paymentDate?: AdjustableOrRelativeDate,
}


/**
 * Defines a single, numerical value that was observed in the marketplace. Observations of market data are made independently to business events or trade life-cycle events, so data instances of Observation can be created independently of any other model type, hence it is annotated as a root type. Observations will be broadly reused in many situations, so references to Observation are supported via the 'key' annotation.
 */
export interface Observation {
    /** Specifies the observed value as a number. */
    observedValue: Price,
    /** Represents the observation was made i.e. how to uniquely identify the observed value among the population of all available market data. */
    observationIdentifier: ObservationIdentifier,
}


/**
 * Defines the parameters needed to uniquely identify a piece of data among the population of all available market data.
 */
export interface ObservationIdentifier {
    /** Represents the asset or rate to which the observation relates. */
    observable: Observable,
    /** Specifies the date value to use when resolving the market data. */
    observationDate: date,
    /** Represents the time and time-zone. */
    observationTime?: TimeZone,
    /** Represents where the market data published and should be observed. */
    informationSource?: InformationSource,
    /** Specifies the method according to which an amount or a date is determined. */
    determinationMethodology?: DeterminationMethodology,
}


/**
 * Specifies the method according to which an amount or a date is determined.
 */
export interface DeterminationMethodology {
    /** Represents a more granular dimention of observation. Typically relevent for resolving a unique equity price, which can be expressed as trade-weighted or volume-weighted averages. */
    determinationMethod?: string,
    /** Specifies enumerations for the type of averaging calculation. */
    averagingMethod?: string,
}


/**
 * Payout based on the averaged price of a referenced underlier. (e.g. Commodities). Can represent both average (average of many) & bullet (average of 1) pricing
 */
export interface CommodityPayout extends PayoutBase {
    /** Indicates if the averaging calculation, when applicable, is weighted or unweighted. */
    averagingFeature?: AveragingCalculation,
    /** Defines parameters in which the commodity price is assessed. */
    commodityPriceReturnTerms?: CommodityPriceReturnTerms,
    /** Specifies specific dates or parametric rules for the dates on which the price will be determined. */
    pricingDates: PricingDates,
    /** Allows the full representation of a payout by defining a set of schedule periods. It supports standard schedule customization by expressing all the dates, quantities, and pricing data in a non-parametric way. */
    schedule?: CalculationSchedule,
    /** Defines the calculation period dates schedule. */
    calculationPeriodDates?: CalculationPeriodDates,
    /** Defines the payment date schedule, as defined by the parameters that are needed to specify it, either in a parametric way or by reference to another schedule of dates (e.g. the valuation dates). */
    paymentDates: PaymentDates,
    /** Identifies the underlying product that is referenced for pricing of the applicable leg in a swap. Referenced in the '2018 ISDA CDM Equity Confirmation for Security Equity Swap' as Security. */
    underlier: Underlier,
    /** Defines quanto or composite FX features that are included in the swap leg. */
    fxFeature?: FxFeature,
    /** Contains the information relative to the delivery of the asset. */
    delivery?: AssetDeliveryInformation,
}


/**
 * Contains the information relative to the delivery of the asset.
 */
export interface AssetDeliveryInformation {
    /** Defines the periods of delivery, including the delivery profile. */
    periods?: AssetDeliveryPeriods,
    /** Defines the location of the delivery of the commodity. */
    location?: LocationIdentifier[],
    /** The number of units included in the transaction for each delivery interval */
    deliveryCapacity?: Quantity,
    /** Grade of product being delivered (e.g., grade of oil). */
    commodityGrade?: string,
}


/**
 * Defines the periods of delivery, including the delivery profile.
 */
export interface AssetDeliveryPeriods {
    /** Defines the delivery profile of the asset, including the load type and the delivery intervals. */
    profile?: AssetDeliveryProfile[],
    /** Delivery start date */
    startDate?: date,
    /** Delivery end date */
    endDate?: date,
}


/**
 * Period and time profile over which the delivery takes place.
 */
export interface CalculationScheduleDeliveryPeriods extends AssetDeliveryPeriods {
    /** The number of units included in the transaction for each delivery interval */
    deliveryCapacity?: Quantity,
    /** Price per quantity per delivery time interval. */
    priceTimeIntervalQuantity?: Price,
}


/**
 * Defines the delivery profile of the asset, including the load type and the delivery intervals.
 */
export interface AssetDeliveryProfile {
    /** Identification of the delivery profile. */
    loadType?: string,
    /** Defines a delivery profile block, including start and end time, days of the week, duration, delivery capacity and price time interval quantity. */
    block?: AssetDeliveryProfileBlock[],
    /** Specifies whether the dates defined include holidays or not. */
    bankHolidaysTreatment?: string,
}


/**
 * Defines a delivery profile block, including start and end time, days of the week, duration, delivery capacity and price time interval quantity.
 */
export interface AssetDeliveryProfileBlock {
    /** The start time of the delivery interval for each block or shape. */
    startTime?: string,
    /** The end time of the delivery interval for each block or shape. */
    endTime?: string,
    /** The days of the week of the delivery. */
    dayOfWeek?: string,
    /** The number of units included in the transaction for each delivery interval */
    deliveryCapacity?: Quantity,
    /** Price per quantity per delivery time interval. */
    priceTimeIntervalQuantity?: Price,
}


/**
 *  The credit default payout specification provides the details necessary for determining when a credit payout will be triggered as well as the parameters for calculating the payout and the settlement terms. The associated globalKey denotes the ability to associate a hash value to the CreditDefaultPayout instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface CreditDefaultPayout extends PayoutBase {
    /** The specification of the non-monetary terms for the Credit Derivative Transaction, including the buyer and seller and selected items from the ISDA 2014 Credit Definition article II, such as the reference obligation and related terms. */
    generalTerms: GeneralTerms,
    /** Specifies the terms for calculating a payout to protect the buyer of the swap in the case of a qualified credit event. These terms include the applicable credit events, the reference obligation, and in the case of a CDS on mortgage-backed securities, the floatingAmountEvents. */
    protectionTerms?: ProtectionTerms[],
    /** The qualification of the price at which the contract has been transacted, in terms of market fixed rate, initial points, market price and/or quotation style. In FpML, those attributes are positioned as part of the fee leg. */
    transactedPrice?: TransactedPrice,
}


/**
 *  A class specifying a set of non-monetary terms for the Credit Derivative Transaction, including the buyer and seller and selected items from the ISDA 2014 Credit Definition article II, such as the reference obligation and related terms. The CDM GeneralTerms class corresponds to the FpML GeneralTerms complex type, except that the effectiveDate and scheduledTerminationDate have been positioned as part of the InterestRatePayout class in the CDM instead of in GeneralTerms.
 */
export interface GeneralTerms {
    /** This attribute contains all the terms relevant to defining the reference entity and reference obligation(s). */
    referenceInformation?: ReferenceInformation,
    /** This attribute contains all the terms relevant to the underlying Index. */
    indexReferenceInformation?: CreditIndex,
    /** This attribute contains all the terms relevant to defining the Credit Default Swap Basket. */
    basketReferenceInformation?: BasketReferenceInformation,
    /** This attribute is used for representing information contained in the Additional Terms field of the 2003 Master Credit Derivatives confirm. */
    additionalTerm?: string[],
    /** Value of this attribute set to 'true' indicates that substitution is applicable. */
    substitution?: boolean,
    /** Value of this attribute set to 'true' indicates that modified equity delivery is applicable. */
    modifiedEquityDelivery?: boolean,
}


/**
 *  A class to specify all of the terms necessary to define and calculate a cash flow based on a fixed, a floating or an inflation index rate. The interest rate payout can be applied to interest rate swaps and FRA (which both have two associated interest rate payouts), credit default swaps (to represent the fee leg when subject to periodic payments) and equity swaps (to represent the funding leg). The associated globalKey denotes the ability to associate a hash value to the InterestRatePayout instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface InterestRatePayout extends PayoutBase {
    /** The specification of the rate value(s) applicable to the contract using either a floating rate calculation, a single fixed rate, a fixed rate schedule, or an inflation rate calculation. */
    rateSpecification?: RateSpecification,
    /** The day count fraction. The cardinality has been relaxed when compared with the FpML interest rate swap for the purpose of accommodating standardized credit default swaps which DCF is not explicitly stated as part of the economic terms. The data rule InterestRatePayout_dayCountFraction requires that the DCF be stated for interest rate products. */
    dayCountFraction?: string,
    /** The parameters used to generate the calculation period dates schedule, including the specification of any initial or final stub calculation periods. */
    calculationPeriodDates?: CalculationPeriodDates,
    /** The payment date schedule, as defined by the parameters that are needed to specify it, either in a parametric way or by reference to another schedule of dates (e.g. the reset dates). */
    paymentDates?: PaymentDates,
    /** The payment date, where only one date is specified, as for the FRA product. */
    paymentDate?: AdjustableDate,
    /** Applicable to CDS on MBS to specify whether payment delays are applicable to the fixed Amount. RMBS typically have a payment delay of 5 days between the coupon date of the reference obligation and the payment date of the synthetic swap. CMBS do not, on the other hand, with both payment dates being on the 25th of each month. */
    paymentDelay?: boolean,
    /** The reset dates schedule, i.e. the dates on which the new observed index value is applied for each period and the interest rate hence begins to accrue. */
    resetDates?: ResetDates,
    /** The parameters specifying any discounting conventions that may apply. This element must only be included if discounting applies. */
    discountingMethod?: DiscountingMethod,
    /** If one or more calculation period contributes to a single payment amount this element specifies whether compounding is applicable and, if so, what compounding method is to be used. This element must only be included when more than one calculation period contributes to a single payment amount. */
    compoundingMethod?: string,
    /** The cashflow representation of the swap stream. */
    cashflowRepresentation?: CashflowRepresentation,
    /** The stub calculation period amount parameters. This element must only be included if there is an initial or final stub calculation period. Even then, it must only be included if either the stub references a different floating rate tenor to the regular calculation periods, or if the stub is calculated as a linear interpolation of two different floating rate tenors, or if a specific stub rate or stub amount has been negotiated. */
    stubPeriod?: StubPeriod,
    /** Reference to a bond underlier to represent an asset swap or Condition Precedent Bond. */
    bondReference?: BondReference,
    /** Fixed Amount Calculation */
    fixedAmount?: string,
    /** Floating Amount Calculation */
    floatingAmount?: string,
    /** Method by which spread is calculated. For example on an asset swap: 'ParPar' or 'Proceeds' may be the method indicated. */
    spreadCalculationMethod?: string,
}


/**
 * A class describing the dividend payout ratio associated with an equity underlier. In certain cases the actual ratio is not known on trade inception, and only general conditions are then specified.
 */
export interface DividendPayoutRatio {
    /** Specifies the total actual dividend payout ratio associated with the equity underlier. A ratio of 90% should be expressed at 0.90. */
    totalRatio: string,
    /** Specifies the cash actual dividend payout ratio associated with the equity underlier. A ratio of 90% should be expressed at 0.90. */
    cashRatio?: string,
    /** Specifies the non cash actual dividend payout ratio associated with the equity underlier. A ratio of 90% should be expressed at 0.90. */
    nonCashRatio?: string,
    /** In the case of a basket underlier, specifies to which component of the basket this particular set of dividend payout ratios correspond. */
    basketConstituent?: BasketConstituent,
}


/**
 * A class to specify the events that will give rise to the payment additional fixed payments.
 */
export interface AdditionalFixedPayments {
    /** An additional Fixed Payment Event. Corresponds to the payment by or on behalf of the Issuer of an actual interest amount in respect to the reference obligation that is greater than the expected interest amount. ISDA 2003 Term: Interest Shortfall Reimbursement. */
    interestShortfallReimbursement?: boolean,
    /** An additional Fixed Payment Event. Corresponds to the payment by or on behalf of the Issuer of an actual principal amount in respect to the reference obligation that is greater than the expected principal amount. ISDA 2003 Term: Principal Shortfall Reimbursement. */
    principalShortfallReimbursement?: boolean,
    /** An Additional Fixed Payment. Corresponds to the payment by or on behalf of the issuer of an amount in respect to the reference obligation in reduction of the prior writedowns. ISDA 2003 Term: Writedown Reimbursement. */
    writedownReimbursement?: boolean,
}


/**
 * CDS Basket Reference Information.
 */
export interface BasketReferenceInformation {
    /** The name of the basket expressed as a free format string. FpML does not define usage rules for this element. */
    basketName?: string,
    /** A CDS basket identifier. */
    basketId?: string[],
    /** This element contains all the reference pool items to define the reference entity and reference obligation(s) in the basket. */
    referencePool: ReferencePool,
    /** N th reference obligation to default triggers payout. */
    nthToDefault?: number,
    /** M th reference obligation to default to allow representation of N th to M th defaults. */
    mthToDefault?: number,
    /** This element contains CDS tranche terms. */
    tranche?: Tranche,
}


/**
 * A class to specify the ISDA terms relating to the floating rate payment events and the implied additional fixed payments, applicable to the credit derivatives transactions on mortgage-backed securities with pay-as-you-go or physical settlement.
 */
export interface FloatingAmountEvents {
    /** A floating rate payment event. Corresponds to the failure by the Reference Entity to pay an expected principal amount or the payment of an actual principal amount that is less than the expected principal amount. ISDA 2003 Term: Failure to Pay Principal. */
    failureToPayPrincipal?: boolean,
    /** A floating rate payment event. With respect to any Reference Obligation Payment Date, either (a) the non-payment of an Expected Interest Amount or (b) the payment of an Actual Interest Amount that is less than the Expected Interest Amount. ISDA 2003 Term: Interest Shortfall. */
    interestShortfall?: InterestShortFall,
    /** A floating rate payment event. Results from the fact that the underlier writes down its outstanding principal amount. ISDA 2003 Term: Writedown. */
    writedown?: boolean,
    /** A floating rate payment event. Results from the fact that losses occur to the underlying instruments that do not result in reductions of the outstanding principal of the reference obligation. */
    impliedWritedown?: boolean,
    /** Specifies the floating amount provisions associated with the floatingAmountEvents. */
    floatingAmountProvisions?: FloatingAmountProvisions,
    /** Specifies the events that will give rise to the payment additional fixed payments. */
    additionalFixedPayments?: AdditionalFixedPayments,
}



export interface FloatingAmountProvisions {
    /** As specified by the ISDA Supplement for use with trades on mortgage-backed securities, 'WAC Cap' means a weighted average coupon or weighted average rate cap provision (however defined in the Underlying Instruments) of the Underlying Instruments that limits, increases or decreases the interest rate or interest entitlement, as set out in the Underlying Instruments on the Effective Date without regard to any subsequent amendment The presence of the element with value set to 'true' signifies that the provision is applicable. From a usage standpoint, this provision is typically applicable in the case of CMBS and not applicable in case of RMBS trades. */
    wacCapInterestProvision?: boolean,
    /** As specified by the ISDA Standard Terms Supplement for use with trades on mortgage-backed securities. The presence of the element with value set to 'true' signifies that the provision is applicable. If applicable, the applicable step-up terms are specified as part of that ISDA Standard Terms Supplement. From a usage standpoint, this provision is typically applicable in the case of RMBS and not applicable in case of CMBS trades. */
    stepUpProvision?: boolean,
}


/**
 * A class to specify the interest shortfall floating rate payment event.
 */
export interface InterestShortFall {
    /** Specifies the nature of the interest Shortfall cap (i.e. Fixed Cap or Variable Cap) in the case where it is applicable. ISDA 2003 Term: Interest Shortfall Cap. */
    interestShortfallCap: string,
    compounding: boolean,
    /** The rate source in the case of a variable cap. */
    rateSource?: string,
}


/**
 * A class to specify the terms for calculating a payout to protect the buyer of the swap in the case of a qualified credit event. These terms include the applicable credit events, the reference obligation, and in the case of a CDS on mortgage-backed securities, the floatingAmountEvents.
 */
export interface ProtectionTerms {
    /** Specifies the applicable Credit Events that would trigger a settlement, as specified in the related Confirmation and defined in the ISDA 2014 Credit Definition article IV section 4.1. */
    creditEvents?: CreditEvents,
    /** The underlying obligations of the reference entity on which you are buying or selling protection. The credit events Failure to Pay, Obligation Acceleration, Obligation Default, Restructuring, Repudiation/Moratorium are defined with respect to these obligations. */
    obligations?: Obligations,
    /** This element contains the ISDA terms relating to the floating rate payment events and the implied additional fixed payments, applicable to the credit derivatives transactions on mortgage-backed securities with pay-as-you-go or physical settlement. */
    floatingAmountEvents?: FloatingAmountEvents,
}


/**
 * A class specifying the Credit Default Swap Reference Information.
 */
export interface ReferenceInformation {
    /** The corporate or sovereign entity which is subject to the swap transaction and any successor that assumes all or substantially all of its contractual and other obligations. Reference Entities cannot be senior or subordinated. It is the obligations of the Reference Entities that can be senior or subordinated. ISDA 2014 Credit definitions article II section 2.1: `Reference Entity` means the entity specified as such in the related Confirmation. */
    referenceEntity: LegalEntity,
    /** The Reference Obligation is a financial instrument that is either issued or guaranteed by the reference entity. It serves to clarify the precise reference entity protection is being offered upon, and its legal position with regard to other related firms (parents/subsidiaries). Furthermore the Reference Obligation is ALWAYS deliverable and establishes the Pari Passu ranking (as the deliverable bonds must rank equal to the reference obligation). ISDA 2003 Term: Reference Obligation. */
    referenceObligation?: ReferenceObligation[],
    /** Used to indicate that there is no Reference Obligation associated with this Credit Default Swap and that there will never be one. */
    noReferenceObligation?: boolean,
    /** Used to indicate that the Reference obligation associated with the Credit Default Swap is currently not known. This is not valid for Legal Confirmation purposes, but is valid for earlier stages in the trade life cycle (e.g. Broker Confirmation). */
    unknownReferenceObligation?: boolean,
    /** Indicates whether an obligation of the Reference Entity, guaranteed by the Reference Entity on behalf of a non-Affiliate, is to be considered an Obligation for the purpose of the transaction. It will be considered an obligation if allGuarantees is applicable (true) and not if allGuarantees is inapplicable (false). ISDA 2003 Term: All Guarantees. */
    allGuarantees?: boolean,
    /** Used to determine (a) for physically settled trades, the Physical Settlement Amount, which equals the Floating Rate Payer Calculation Amount times the Reference Price and (b) for cash settled trades, the Cash Settlement Amount, which equals the greater of (i) the difference between the Reference Price and the Final Price and (ii) zero. ISDA 2003 Term: Reference Price. */
    referencePrice?: Price,
    /** Applicable to the transactions on mortgage-backed security, which can make use of a reference policy. Presence of the element with value set to 'true' indicates that the reference policy is applicable; absence implies that it is not. */
    referencePolicy?: boolean,
    /** With respect to any day, the list of Syndicated Secured Obligations of the Designated Priority of the Reference Entity published by Markit Group Limited or any successor thereto appointed by the Specified Dealers (the 'Secured List Publisher') on or most recently before such day, which list is currently available at [http://www.markit.com]. ISDA 2003 Term: Relevant Secured List. */
    securedList?: boolean,
}


/**
 * A class to specify the reference obligation that is associated with a credit derivative instrument.
 */
export interface ReferenceObligation {
    /** Identifies the underlying asset when it is a security, such as a bond or convertible bond. The security data type requires one or more productIdentifiers, specificaiton of the security type (e.g. debt), and includes optional attributes to specify a debt class, such as asset-backed, as well as seniority. */
    security?: Security,
    /** Identifies the underlying asset when it is a loan. */
    loan?: Loan,
    /** The entity primarily responsible for repaying debt to a creditor as a result of borrowing or issuing bonds. ISDA 2003 Term: Primary Obligor. */
    primaryObligor?: LegalEntity,
    /** A pointer style reference to a reference entity defined elsewhere in the document. Used when the reference entity is the primary obligor. */
    primaryObligorReference?: LegalEntity,
    /** The party that guarantees by way of a contractual arrangement to pay the debts of an obligor if the obligor is unable to make the required payments itself. ISDA 2003 Term: Guarantor. */
    guarantor?: LegalEntity,
    /** A pointer style reference to a reference entity defined elsewhere in the document. Used when the reference entity is the guarantor. */
    guarantorReference?: string,
    /** Indicates if the reference obligation is a Standard Reference Obligation. ISDA 2014 Term: Standard Reference Obligation. */
    standardReferenceObligation?: boolean,
}



export interface ReferencePair {
    /** The corporate or sovereign entity on which you are buying or selling protection and any successor that assumes all or substantially all of its contractual and other obligations. It is vital to use the correct legal name of the entity and to be careful not to choose a subsidiary if you really want to trade protection on a parent company. Please note, Reference Entities cannot be senior or subordinated. It is the obligations of the Reference Entities that can be senior or subordinated. ISDA 2003 Term: Reference Entity. */
    referenceEntity: LegalEntity,
    /** The Reference Obligation is a financial instrument that is either issued or guaranteed by the reference entity. It serves to clarify the precise reference entity protection is being offered upon, and its legal position with regard to other related firms (parents/subsidiaries). Furthermore the Reference Obligation is ALWAYS deliverable and establishes the Pari Passu ranking (as the deliverable bonds must rank equal to the reference obligation). ISDA 2003 Term: Reference Obligation. */
    referenceObligation?: ReferenceObligation,
    /** Used to indicate that there is no Reference Obligation associated with this Credit Default Swap and that there will never be one. */
    noReferenceObligation?: boolean,
    /** Defines the reference entity types corresponding to a list of types in the ISDA First to Default documentation. */
    entityType: string,
}


/**
 * This type contains all the reference pool items to define the reference entity and reference obligation(s) in the basket.
 */
export interface ReferencePool {
    /** This type contains all the constituent weight and reference information. */
    referencePoolItem: ReferencePoolItem[],
}


/**
 * This type contains all the constituent weight and reference information.
 */
export interface ReferencePoolItem {
    /** Describes the weight of each of the constituents within the basket. If not provided, it is assumed to be equal weighted. */
    constituentWeight?: ConstituentWeight,
    referencePair: ReferencePair,
    /** Reference to the documentation terms applicable to this item. */
    protectionTermsReference?: ProtectionTerms,
    /** Reference to the cash settlement terms applicable to this item. */
    cashSettlementTermsReference?: CashSettlementTerms,
    /** Reference to the physical settlement terms applicable to this item. */
    physicalSettlementTermsReference?: PhysicalSettlementTerms,
}


/**
 * A class to specify the Relevant Settled Entity Matrix.
 */
export interface SettledEntityMatrix {
    /** Relevant settled entity matrix source. */
    matrixSource: string,
    /** Specifies the publication date of the applicable version of the matrix. When this element is omitted, the Standard Terms Supplement defines rules for which version of the matrix is applicable. */
    publicationDate?: date,
}


/**
 * The class to represent a CDS Tranche.
 */
export interface Tranche {
    /** Lower bound percentage of the loss that the Tranche can endure, expressed as a decimal. An attachment point of 5% would be represented as 0.05. The difference between Attachment and Exhaustion points is called the width of the Tranche. */
    attachmentPoint: string,
    /** Upper bound percentage of the loss that the Tranche can endure, expressed as a decimal. An exhaustion point of 5% would be represented as 0.05. The difference between Attachment and Exhaustion points is call the width of the Tranche. */
    exhaustionPoint: string,
    /** Outstanding Swap Notional Amount is defined at any time on any day, as the greater of: (a) Zero; If Incurred Recovery Amount Applicable: (b) The Original Swap Notional Amount minus the sum of all Incurred Loss Amounts and all Incurred Recovery Amounts (if any) determined under this Confirmation at or prior to such time.Incurred Recovery Amount not populated: (b) The Original Swap Notional Amount minus the sum of all Incurred Loss Amounts determined under this Confirmation at or prior to such time. */
    incurredRecoveryApplicable?: boolean,
}


/**
 * A class to specify the currency in which the dividends will be denominated, i.e. either in the dividend currency or in a currency specified as part of the contract.
 */
export interface DividendCurrency {
    /** The currency in which the dividend is denominated. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
    /** Specifies the method according to which the dividend is determined, e.g. the dividend currency. */
    determinationMethod?: string,
    /** Reference to a currency specified elsewhere in the document */
    currencyReference?: string,
}


/**
 * A class to specify the dividend date by reference to another date, with the ability to apply and offset. This class doesn't exist in FpML and is meant to simplify the choice constraint associated with the DividendPaymentDate class.
 */
export interface DividendDateReference {
    /** Specification of the dividend date using an enumeration, with values such as the pay date, the ex-date or the record date. */
    dateReference: string,
    /** Only to be used when SharePayment has been specified in the dividendDateReference element. The number of Currency Business Days following the day on which the Issuer of the Shares pays the relevant dividend to holders of record of the Shares. */
    paymentDateOffset?: Offset,
}


/**
 * A class describing the date on which the dividend will be paid/received. This class is also used to specify the date on which the FX rate will be determined, when applicable.
 */
export interface DividendPaymentDate {
    dividendDateReference?: DividendDateReference,
    dividendDate?: AdjustableOrRelativeDate,
}


/**
 * A class describing the conditions governing the payment of dividends to the receiver of the equity return, with the exception of the dividend payout ratio, which is defined for each of the underlying components.
 */
export interface DividendReturnTerms {
    /** Specifies the dividend payout ratio associated with each underlier. In FpML 5.10 the payout is positioned at the underlier level, although there is an intent to reconsider this approach and position it at the leg level. This is approach adopted by the CDM. */
    dividendPayoutRatio?: DividendPayoutRatio[],
    /** Boolean element that defines whether the dividend will be reinvested or not. */
    dividendReinvestment?: boolean,
    /** Defines the date on which the receiver of the equity return is entitled to the dividend. */
    dividendEntitlement?: string,
    /** Specifies whether the dividend is paid with respect to the Dividend Period. */
    dividendAmountType?: string,
    /** Performance calculation, in accordance with Part 1 Section 12 of the 2018 ISDA CDM Equity Confirmation for Security Equity Swap, Para 75. 'Equity Performance'. Cumulative performance is used as a notional multiplier factor on both legs of an Equity Swap. */
    performance?: string,
    /** 2002 ISDA Equity Derivatives Definitions: Dividend Period as either the First Period or the Second Period. |  */
    firstOrSecondPeriod?: string,
    /** Specifies the party which determines if dividends are extraordinary in relation to normal levels. */
    extraordinaryDividendsParty?: string,
    /** Determination of Gross Cash Dividend per Share. */
    excessDividendAmount?: string,
    /** Specifies the currency in which the dividend will be denominated, e.g. the dividend currency, or a specified currency. This class is not specified as such in FpML, which makes use of the CurrencyAndDeterminationMethod.model to specify such terms. */
    dividendCurrency?: DividendCurrency,
    /** Specifies the treatment of Non-Cash Dividends. */
    nonCashDividendTreatment?: string,
    /** Specifies how the composition of Dividends is to be determined. */
    dividendComposition?: string,
    /** Specifies the method according to which special dividends are determined. */
    specialDividends?: boolean,
    /** If present and true, then material non cash dividends are applicable. */
    materialDividend?: boolean,
    /** One to many time bounded dividend payment periods, each with a dividend payment date per period. */
    dividendPeriod?: DividendPeriod[],
}


/**
 * Time bounded dividend payment periods, each with a dividend payment date per period.
 */
export interface DividendPeriod {
    /** Dividend period start date. */
    startDate?: DividendPaymentDate,
    /** Dividend period end date. */
    endDate?: DividendPaymentDate,
    /** Date adjustments for all unadjusted dates in this dividend period. */
    dateAdjustments: BusinessDayAdjustments,
    /** For basket underliers, reference to the basket component which is paying dividends in the specified period. */
    basketConstituent?: BasketConstituent,
    /** Specifies when the dividend will be paid to the receiver of the equity return. Has the meaning as defined in the ISDA 2002 Equity Derivatives Definitions. Is not applicable in the case of a dividend reinvestment election. */
    dividendPaymentDate: DividendPaymentDate,
    /** Specifies the dividend valuation dates of the swap. */
    dividendValuationDate?: AdjustableOrRelativeDate,
}



export interface PriceReturnTerms {
    /** The type of return associated with the equity swap. */
    returnType: string,
    /** Defines the conversion applied if the quantity unit on contract is different from unit on referenced underlier. */
    conversionFactor?: string,
    /** Performance calculation, in accordance with Part 1 Section 12 of the 2018 ISDA CDM Equity Confirmation for Security Equity Swap, Para 75. 'Equity Performance'. Cumulative performance is used as a notional multiplier factor on both legs of an Equity Swap. */
    performance?: string,
}


/**
 * Contains all common elements in variance, volatility and correlation return Terms.
 */
export interface ReturnTermsBase {
    /** Contains all non-date valuation information. */
    valuationTerms: ValuationTerms,
    /** This specifies the numerator of an annualization factor. Frequently this number is equal to the number of observations of prices in a year e.g. 252. */
    annualizationFactor?: number,
    /** The parameters which define whether dividends are applicable */
    dividendApplicability?: DividendApplicability,
    /** Contains Equity Underlyer provisions regarding jurisdiction and fallbacks. */
    equityUnderlierProvisions?: EquityUnderlierProvisions,
    /** Indicates whether the price of shares is adjusted for dividends or not. */
    sharePriceDividendAdjustment?: boolean,
    /** Expected number of trading days. */
    expectedN: number,
    /** Contract will strike off this initial level. Providing just the initialLevel without initialLevelSource, infers that this is AgreedInitialPrice - a specified Initial Index Level. */
    initialLevel?: string,
    /** In this context, this is AgreedInitialPrice - a specified Initial Index Level. */
    initialLevelSource?: string,
    /** Specifies whether Mean Adjustment is applicable or not in the calculation of the Realized Volatility, Variance or Correlation */
    meanAdjustment?: boolean,
    /** Performance calculation, in accordance with Part 1 Section 12 of the 2018 ISDA CDM Equity Confirmation for Security Equity Swap, Para 75. 'Equity Performance'. Cumulative performance is used as a notional multiplier factor on both legs of an Equity Swap. */
    performance?: string,
}



export interface VarianceReturnTerms extends ReturnTermsBase {
    /** Variance Strike Price in accordance with the ISDA 2011 Equity Derivatives Definitions. */
    varianceStrikePrice?: Price,
    /** Volatility Strike Price in accordance with the ISDA 2011 Equity Derivatives Definitions. */
    volatilityStrikePrice?: Price,
    /** Contains possible barriers for variance products, both variance-based and underlier price based */
    varianceCapFloor?: VarianceCapFloor,
    /** Contains containing volatility-based barriers */
    volatilityCapFloor?: VolatilityCapFloor,
    /** Vega Notional represents the approximate gain/loss at maturity for a 1% difference between RVol (realised vol) and KVol (strike vol). It does not necessarily represent the Vega Risk of the trade. */
    vegaNotionalAmount?: NonNegativeQuantitySchedule,
    /** Specification of the exchange traded contract nearest. */
    exchangeTradedContractNearest?: Observable,
}



export interface VolatilityReturnTerms extends ReturnTermsBase {
    /** Volatility Strike Price in accordance with the ISDA 2011 Equity Derivatives Definitions. */
    volatilityStrikePrice: Price,
    /** Contains volatility-based barriers */
    volatilityCapFloor?: VolatilityCapFloor,
    /** Specification of the exchange traded contract nearest. */
    exchangeTradedContractNearest?: ListedDerivative,
}



export interface CorrelationReturnTerms extends ReturnTermsBase {
    /** Correlation Strike Price in accordance with the ISDA 2011 Equity Derivatives Definitions. */
    correlationStrikePrice: Price,
    /** Describes correlation bounds, which form a cap and a floor on the realized correlation. */
    boundedCorrelation?: NumberRange,
    /** Number of data series, normal market practice is that correlation data sets are drawn from geographic market areas, such as America, Europe and Asia Pacific, each of these geographic areas will have its own data series to avoid contagion. */
    numberOfDataSeries?: number,
}



export interface ValuationTerms {
    /** The official settlement price as announced by the related exchange is applicable, in accordance with the ISDA 2002 definitions. */
    futuresPriceValuation?: boolean,
    /** The official settlement price as announced by the related exchange is applicable, in accordance with the ISDA 2002 definitions */
    optionsPriceValuation?: boolean,
    /** The number of valuation dates between valuation start date and valuation end date. */
    numberOfValuationDates?: number,
    /** Specifies the dividend valuation dates of the swap. */
    dividendValuationDates?: AdjustableRelativeOrPeriodicDates,
    /** Specifies the fallback provisions for Hedging Party in the determination of the Final Price. */
    fPVFinalPriceElectionFallback?: string,
    /** For an index option transaction, a flag to indicate whether a relevant Multiple Exchange Index Annex is applicable to the transaction. This annex defines additional provisions which are applicable where an index is comprised of component securities that are traded on multiple exchanges. */
    multipleExchangeIndexAnnexFallback?: boolean,
    /** For an index option transaction, a flag to indicate whether a relevant Component Security Index Annex is applicable to the transaction. */
    componentSecurityIndexAnnexFallback?: boolean,
}



export interface EquityUnderlierProvisions {
    /** For an index option or swap transaction, a flag to indicate whether a relevant Multiple Exchange Index Annex is applicable to the transaction. This annex defines additional provisions which are applicable where an index is comprised of component securities that are traded on multiple exchanges. */
    multipleExchangeIndexAnnexFallback?: boolean,
    /** For an index option or swap transaction, a flag to indicate whether a relevant Component Security Index Annex is applicable to the transaction. */
    componentSecurityIndexAnnexFallback?: boolean,
    /** The ISO 3166 standard code for the country within which the postal address is located. */
    localJurisdiction?: string,
    /** The ISO 3166 standard code for the country within which the postal address is located. */
    relevantJurisdiction?: string,
}



export interface VarianceCapFloor {
    /** If present and true, then variance cap is applicable. */
    varianceCap: boolean,
    /** For use when varianceCap is applicable. Contains the scaling factor of the Variance Cap that can differ on a trade-by-trade basis in the European market. For example, a Variance Cap of 2.5^2 x Variance Strike Price has an unadjustedVarianceCap of 2.5. */
    unadjustedVarianceCap?: string,
    /** Conditions which bound variance. The contract specifies one or more boundary levels. These levels are expressed as prices for confirmation purposes Underlyer price must be equal to or higher than Lower Barrier is known as Up Conditional Swap Underlyer price must be equal to or lower than Upper Barrier is known as Down Conditional Swap Underlyer price must be equal to or higher than Lower Barrier and must be equal to or lower than Upper Barrier is known as Barrier Conditional Swap. */
    boundedVariance?: BoundedVariance,
}



export interface BoundedVariance {
    /** The contract specifies which price must satisfy the boundary condition. */
    realisedVarianceMethod: string,
    /** The contract specifies whether the notional should be scaled by the Number of Days in Range divided by the Expected N. The number of Days in Ranges refers to the number of returns that contribute to the realized volatility. */
    daysInRangeAdjustment: boolean,
    /** All observations above this price level will be excluded from the variance calculation. */
    upperBarrier?: string,
    /** All observations below this price level will be excluded from the variance calculation. */
    lowerBarrier?: string,
}


/**
 * Contains volatility-based barriers. Volatility Cap needs to be specified in accordance with the ISDA 2011 Equity Derivatives Definitions.
 */
export interface VolatilityCapFloor {
    /** Indicates whether the volatility cap is applicable in accordance with the ISDA 2011 Equity Derivatives Definitions. Setting the element 'applicable' to 'False' - means No Volatility Cap and no 'totalVolatilityCap' or 'volatilityCapFactor' should be provided. Setting the element 'applicable' to 'True' - means Volatility Cap election, then 'totalVolatilityCap' or 'volatilityCapFactor' should be provided, otherwise it defaults to volatilityCapFactor=2.5. */
    applicable: boolean,
    /** Volatility Cap Amount in accordance with the ISDA 2011 Equity Derivatives Definitions. This means the Volatility Cap Amount election is a number. */
    totalVolatilityCap?: string,
    /** Volatility Cap Amount in accordance with the ISDA 2011 Equity Derivatives Definitions. The Calculated VolCapAmt can be optionally provided. */
    volatilityCapFactor?: string,
}


/**
 * Describes correlation bounds, which form a cap and a floor on the realized correlation.
 */
export interface BoundedCorrelation {
    /** Minimum Boundary as a percentage of the Strike Price. */
    minimumBoundaryPercent?: string,
    /** Maximum Boundary as a percentage of the Strike Price. */
    maximumBoundaryPercent?: string,
}


/**
 * Reference to a bond underlier to represent an asset swap or Condition Precedent Bond.
 */
export interface BondReference {
    /** Reference to a bond underlier. */
    bond: Security,
    /** To indicate whether the Condition Precedent Bond is applicable. The swap contract is only valid if the bond is issued and if there is any dispute over the terms of fixed stream then the bond terms would be used. */
    conditionPrecedentBond: boolean,
    /** To indicate whether the Discrepancy Clause is applicable. */
    discrepancyClause?: boolean,
    /** Specifies the coupon rate (expressed in percentage) of a fixed income security or convertible bond. */
    couponRate?: FixedRateSpecification,
}


/**
 * A data defining:  the cashflow representation of a swap trade.
 */
export interface CashflowRepresentation {
    /** A true/false flag to indicate whether the cashflows match the parametric definition of the stream, i.e. whether the cashflows could be regenerated from the parameters without loss of information. */
    cashflowsMatchParameters: boolean,
    /** The adjusted payment date and associated calculation period parameters required to calculate the actual or projected payment amount. A list of payment calculation period elements may be ordered in the document by ascending adjusted payment date. An FpML document containing an unordered list of payment calculation periods is still regarded as a conformant document. */
    paymentCalculationPeriod?: PaymentCalculationPeriod[],
}


/**
 * A data defining:  discounting information. The 2000 ISDA definitions, section 8.4. discounting (related to the calculation of a discounted fixed amount or floating amount) apply. This type must only be included if discounting applies.
 */
export interface DiscountingMethod {
    /** The discounting method that is applicable. */
    discountingType: string,
    /** A discount rate, expressed as a decimal, to be used in the calculation of a discounted amount. A discount amount of 5% would be represented as 0.05. */
    discountRate?: string,
    /** A discount day count fraction to be used in the calculation of a discounted amount. */
    discountRateDayCountFraction?: string,
}


/**
 * A data defining:  parameters associated with a floating rate reset. This data forms:  part of the cashflows representation of a stream.
 */
export interface FloatingRateDefinition {
    /** The final calculated rate for a calculation period after any required averaging of rates A calculated rate of 5% would be represented as 0.05. */
    calculatedRate?: string,
    /** The details of a particular rate observation, including the fixing date and observed rate. A list of rate observation elements may be ordered in the document by ascending adjusted fixing date. An FpML document containing an unordered list of rate observations is still regarded as a conformant document. */
    rateObservation?: RateObservation[],
    /** A rate multiplier to apply to the floating rate. The multiplier can be a positive or negative decimal. This element should only be included if the multiplier is not equal to 1 (one). */
    floatingRateMultiplier?: string,
    /** The ISDA Spread, if any, which applies for the calculation period. The spread is a per annum rate, expressed as a decimal. For purposes of determining a calculation period amount, if positive the spread will be added to the floating rate and if negative the spread will be subtracted from the floating rate. A positive 10 basis point (0.1%) spread would be represented as 0.001. */
    spread?: string,
    /** The cap rate, if any, which applies to the floating rate for the calculation period. The cap rate (strike) is only required where the floating rate on a swap stream is capped at a certain strike level. The cap rate is assumed to be exclusive of any spread and is a per annum rate, expressed as a decimal. A cap rate of 5% would be represented as 0.05. */
    capRate?: Strike[],
    /** The floor rate, if any, which applies to the floating rate for the calculation period. The floor rate (strike) is only required where the floating rate on a swap stream is floored at a certain strike level. The floor rate is assumed to be exclusive of any spread and is a per annum rate, expressed as a decimal. The floor rate of 5% would be represented as 0.05. */
    floorRate?: Strike[],
}


/**
 * A data to:  specify the inflation rate.
 */
export interface InflationRateSpecification extends FloatingRateSpecification {
    /** An off-setting period from the payment date which determines the reference period for which the inflation index is observed. */
    inflationLag: Offset,
    /** The reference source such as Reuters or Bloomberg. FpML specifies indexSource to be of type rateSourcePageScheme, but without specifying actual values. */
    indexSource: string,
    /** The current main publication source such as relevant web site or a government body. FpML specifies mainPublication to be of type mainPublicationSource, but without specifying actual values. */
    mainPublication: string,
    /** The method used when calculating the Inflation Index Level from multiple points. The most common is Linear. */
    interpolationMethod: string,
    /** Initial known index level for the first calculation period. */
    initialIndexLevel?: string,
    /** The applicability of a fallback bond as defined in the 2006 ISDA Inflation Derivatives Definitions, sections 1.3 and 1.8. */
    fallbackBondApplicable: boolean,
    /** Indicates how to use the inflation index to calculate the payment (e.g. Ratio, Return, Spread). Added for Inflation Asset Swap */
    calculationMethod?: string,
    /** Indicates the style of how the inflation index calculates the payment (e.g. YearOnYear, ZeroCoupon). */
    calculationStyle?: string,
    /** To be specified only for products that embed a redemption payment. */
    finalPrincipalExchangeCalculation?: string,
}


/**
 * A class defining a floating interest rate through the specification of the floating rate index, the tenor, the multiplier schedule, the spread, the qualification of whether a specific rate treatment and/or a cap or floor apply.
 */
export interface FloatingRateBase {
    rateOption?: InterestRateIndex,
    /** The ISDA Spread or a Spread schedule expressed as explicit spreads and dates. In the case of a schedule, the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. The spread is a per annum rate, expressed as a decimal. For purposes of determining a calculation period amount, if positive the spread will be added to the floating rate and if negative the spread will be subtracted from the floating rate. A positive 10 basis point (0.1%) spread would be represented as 0.001. */
    spreadSchedule?: SpreadSchedule,
    /** The cap rate or cap rate schedule, if any, which applies to the floating rate. The cap rate (strike) is only required where the floating rate on a swap stream is capped at a certain level. A cap rate schedule is expressed as explicit cap rates and dates and the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. The cap rate is assumed to be exclusive of any spread and is a per annum rate, expressed as a decimal. A cap rate of 5% would be represented as 0.05. */
    capRateSchedule?: StrikeSchedule,
    /** The floor rate or floor rate schedule, if any, which applies to the floating rate. The floor rate (strike) is only required where the floating rate on a swap stream is floored at a certain strike level. A floor rate schedule is expressed as explicit floor rates and dates and the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. The floor rate is assumed to be exclusive of any spread and is a per annum rate, expressed as a decimal. A floor rate of 5% would be represented as 0.05. */
    floorRateSchedule?: StrikeSchedule,
}



export interface FloatingRate extends FloatingRateBase {
    /** A rate multiplier or multiplier schedule to apply to the floating rate. A multiplier schedule is expressed as explicit multipliers and dates. In the case of a schedule, the step dates may be subject to adjustment in accordance with any adjustments specified in the calculationPeriodDatesAdjustments. The multiplier can be a positive or negative decimal. This element should only be included if the multiplier is not equal to 1 (one) for the term of the stream. */
    floatingRateMultiplierSchedule?: RateSchedule,
    /** The specification of any rate conversion which needs to be applied to the observed rate before being used in any calculations. The two common conversions are for securities quoted on a bank discount basis which will need to be converted to either a Money Market Yield or Bond Equivalent Yield. See the Annex to the 2000 ISDA Definitions, Section 7.3. Certain General Definitions Relating to Floating Rate Options, paragraphs (g) and (h) for definitions of these terms. */
    rateTreatment?: string,
    /** Support for modular calculated rates, such such as lockout compound calculations. */
    calculationParameters?: FloatingRateCalculationParameters,
    /** Definition of any fallback rate that may be applicable. */
    fallbackRate?: FallbackRateParameters,
}


/**
 * A class to specify the floating interest rate by extending the floating rate definition with a set of attributes that specify such rate: the initial value specified as part of the trade, the rounding convention, the averaging method and the negative interest rate treatment.
 */
export interface FloatingRateSpecification extends FloatingRate {
    /** The initial floating rate reset agreed between the principal parties involved in the trade. This is assumed to be the first required reset rate for the first regular calculation period. It should only be included when the rate is not equal to the rate published on the source implied by the floating rate index. An initial rate of 5% would be represented as 0.05. */
    initialRate?: Price,
    /** The rounding convention to apply to the final rate used in determination of a calculation period amount. */
    finalRateRounding?: Rounding,
    /** If averaging is applicable, this component specifies whether a weighted or unweighted average method of calculation is to be used. The component must only be included when averaging applies. */
    averagingMethod?: string,
    /** The specification of any provisions for calculating payment obligations when a floating rate is negative (either due to a quoted negative floating rate or by operation of a spread that is subtracted from the floating rate). */
    negativeInterestRateTreatment?: string,
}


/**
 * Type defining the specification for a fixed rate.
 */
export interface FixedRateSpecification {
    /** The fixed rate or fixed rate schedule expressed as explicit fixed rates and dates. In the case of a schedule, the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. */
    rateSchedule?: RateSchedule,
}


/**
 * A class defining a currency and a future value date.
 */
export interface FutureValueAmount {
    /** Specifies quantity amount returned if not the full amount from the TradeState, e.g. partial return */
    quantity?: NonNegativeQuantitySchedule,
    /** The currency in which the an amount is denominated. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency: string,
    /** The number of days from the adjusted calculation period start date to the adjusted value date, calculated in accordance with the applicable day count fraction. */
    calculationPeriodNumberOfDays: number,
    /** Adjusted value date of the future value amount. */
    valueDate: date,
}


/**
 * Adds an optional spread type element to the Schedule to identify a long or short spread value.
 */
export interface SpreadSchedule extends RateSchedule {
    /** An element which purpose is to identify a long or short spread value. */
    spreadScheduleType?: string,
}


/**
 * A class defining a floating rate.
 */
export interface StubFloatingRate {
    /** The floating rate index. */
    floatingRateIndex: string,
    /** The ISDA Designated Maturity, i.e. the tenor of the floating rate. */
    indexTenor?: Period,
    /** A rate multiplier or multiplier schedule to apply to the floating rate. A multiplier schedule is expressed as explicit multipliers and dates. In the case of a schedule, the step dates may be subject to adjustment in accordance with any adjustments specified in the calculationPeriodDatesAdjustments. The multiplier can be a positive or negative decimal. This element should only be included if the multiplier is not equal to 1 (one) for the term of the stream. */
    floatingRateMultiplierSchedule?: Schedule,
    /** The ISDA Spread or a Spread schedule expressed as explicit spreads and dates. In the case of a schedule, the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. The spread is a per annum rate, expressed as a decimal. For purposes of determining a calculation period amount, if positive the spread will be added to the floating rate and if negative the spread will be subtracted from the floating rate. A positive 10 basis point (0.1%) spread would be represented as 0.001. */
    spreadSchedule?: SpreadSchedule[],
    /** The specification of any rate conversion which needs to be applied to the observed rate before being used in any calculations. The two common conversions are for securities quoted on a bank discount basis which will need to be converted to either a Money Market Yield or Bond Equivalent Yield. See the Annex to the 2000 ISDA Definitions, Section 7.3. Certain General Definitions Relating to Floating Rate Options, paragraphs (g) and (h) for definitions of these terms. */
    rateTreatment?: string,
    /** The cap rate or cap rate schedule, if any, which applies to the floating rate. The cap rate (strike) is only required where the floating rate on a swap stream is capped at a certain level. A cap rate schedule is expressed as explicit cap rates and dates and the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. The cap rate is assumed to be exclusive of any spread and is a per annum rate, expressed as a decimal. A cap rate of 5% would be represented as 0.05. */
    capRateSchedule?: StrikeSchedule[],
    /** The floor rate or floor rate schedule, if any, which applies to the floating rate. The floor rate (strike) is only required where the floating rate on a swap stream is floored at a certain strike level. A floor rate schedule is expressed as explicit floor rates and dates and the step dates may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. The floor rate is assumed to be exclusive of any spread and is a per annum rate, expressed as a decimal. A floor rate of 5% would be represented as 0.05. */
    floorRateSchedule?: StrikeSchedule[],
}


/**
 * A type defining how a stub calculation period amount is calculated. A single floating rate tenor different to that used for the regular part of the calculation periods schedule may be specified, or two floating rate tenors many be specified. If two floating rate tenors are specified then Linear Interpolation (in accordance with the 2000 ISDA Definitions, Section 8.3 Interpolation) is assumed to apply. Alternatively, an actual known stub rate or stub amount may be specified.
 */
export interface StubValue {
    /** The rates to be applied to the initial or final stub may be the linear interpolation of two different rates. While the majority of the time, the rate indices will be the same as that specified in the stream and only the tenor itself will be different, it is possible to specift two different rates. For example, a 2 month stub period may use the linear interpolation of a 1 month and 3 month rate. The different rates would be specified in this component. Note that a maximum of two rates can be specified. If a stub period uses the same floating rate index, including tenor, as the regular calculation periods then this should not be specified again within this component, i.e. the stub calculation period amount component may not need to be specified even if there is an initial or final stub period. If a stub period uses a different floating rate index compared to the regular calculation periods then this should be specified within this component. If specified here, they are likely to have id attributes, allowing them to be referenced from within the cashflows component. */
    floatingRate?: StubFloatingRate[],
    /** An actual rate to apply for the initial or final stub period may have been agreed between the principal parties (in a similar way to how an initial rate may have been agreed for the first regular period). If an actual stub rate has been agreed then it would be included in this component. It will be a per annum rate, expressed as a decimal. A stub rate of 5% would be represented as 0.05. */
    stubRate?: string,
    /** An actual amount to apply for the initial or final stub period may have been agreed between the two parties. If an actual stub amount has been agreed then it would be included in this component. */
    stubAmount?: Money,
}


/**
 * Type for reporting the detailed results of calculating a cash flow for a calculation period.  This is enhanced relative to the FpML-based cashflows structure to allow more information to be returned about daily compounded rates.
 */
export interface FixedAmountCalculationDetails {
    /** The calculation period for which the floating calculation was performed. */
    calculationPeriod: CalculationPeriodBase,
    /** The notional in effect during the calculation period. */
    calculationPeriodNotionalAmount: Money,
    /** The value of the fixed rate that was used. */
    fixedRate: string,
    /** The fraction of a year that this calculation represents, according to the day count fraction method. */
    yearFraction: string,
    /** The amount of the cash flow that was computed, including any spreads and other processing. */
    calculatedAmount: string,
}


/**
 *  A data type to specify the fixed interest rate, floating interest rate or inflation rate.
 */
export interface RateSpecification {
}



export interface CalculationPeriodData {
    /** The first date of a date range. */
    startDate: date,
    /** The last date of a date range. */
    endDate: date,
    daysInPeriod: number,
    daysInLeapYearPeriod: number,
    isFirstPeriod: boolean,
    isLastPeriod: boolean,
}


/**
 *  A class defining how the initial or final stub calculation period amounts is calculated. For example, the rate to be applied to the initial or final stub calculation period may be the linear interpolation of two different tenors for the floating rate index specified in the calculation period amount component, e.g. A two month stub period may used the linear interpolation of a one month and three month floating rate. The different rate tenors would be specified in this component. Note that a maximum of two rate tenors can be specified. If a stub period uses a single index tenor and this is the same as that specified in the calculation period amount component then the initial stub or final stub component, as the case may be, must not be included.
 */
export interface StubPeriod {
    /** A pointer style reference to the associated calculation period dates component defined elsewhere in the document. */
    calculationPeriodDatesReference: CalculationPeriodDates,
    /** Specifies how the initial stub amount is calculated. A single floating rate tenor different to that used for the regular part of the calculation periods schedule may be specified, or two floating tenors may be specified. If two floating rate tenors are specified then Linear Interpolation (in accordance with the 2000 ISDA Definitions, Section 8.3. Interpolation) is assumed to apply. Alternatively, an actual known stub rate or stub amount may be specified. */
    initialStub?: StubValue,
    /** Specifies how the final stub amount is calculated. A single floating rate tenor different to that used for the regular part of the calculation periods schedule may be specified, or two floating tenors may be specified. If two floating rate tenors are specified then Linear Interpolation (in accordance with the 2000 ISDA Definitions, Section 8.3. Interpolation) is assumed to apply. Alternatively, an actual known stub rate or stub amount may be specified. */
    finalStub?: StubValue,
}


/**
 * The calculation period adjusted start and end dates, which are the baseline arguments needed to compute an interest accrual calculation.
 */
export interface CalculationPeriodBase {
    /** The calculation period start date, adjusted according to any relevant business day convention. */
    adjustedStartDate?: date,
    /** The calculation period end date, adjusted according to any relevant business day convention. */
    adjustedEndDate?: date,
}


/**
 * A data defining:  the parameters used in the calculation of a fixed or floating rate calculation period amount. This data forms:  part of cashflows representation of a swap stream.
 */
export interface CalculationPeriod extends CalculationPeriodBase {
    /** The calculation start date, unadjusted. */
    unadjustedStartDate?: date,
    /** The calculation end date, unadjusted. */
    unadjustedEndDate?: date,
    /** The number of days from the adjusted effective / start date to the adjusted termination / end date calculated in accordance with the applicable day count fraction. */
    calculationPeriodNumberOfDays?: number,
    /** The amount that a cashflow will accrue interest on. */
    notionalAmount?: string,
    /** The amount that a cashflow will accrue interest on. This is the calculated amount of the FX linked - i.e. the other currency notional amount multiplied by the appropriate FX spot rate. */
    fxLinkedNotionalAmount?: FxLinkedNotionalAmount,
    /** The floating rate reset information for the calculation period. */
    floatingRateDefinition?: FloatingRateDefinition,
    /** The calculation period fixed rate. A per annum rate, expressed as a decimal. A fixed rate of 5% would be represented as 0.05. */
    fixedRate?: string,
    /** The year fraction value of the calculation period, result of applying the ISDA rules for day count fraction defined in the ISDA Annex. */
    dayCountYearFraction?: string,
    /** The amount representing the forecast of the accrued value of the calculation period. An intermediate value used to generate the forecastPaymentAmount in the PaymentCalculationPeriod. */
    forecastAmount?: Money,
    /** A value representing the forecast rate used to calculate the forecast future value of the accrual period. This is a calculated rate determined based on averaging the rates in the rateObservation elements, and incorporates all of the rate treatment and averaging rules. A value of 1% should be represented as 0.01. */
    forecastRate?: string,
}


/**
 * A data for:  defining the parameters used to generate the calculation period dates schedule, including the specification of any initial or final stub calculation periods. A calculation period schedule consists of an optional initial stub calculation period, one or more regular calculation periods and an optional final stub calculation period. In the absence of any initial or final stub calculation periods, the regular part of the calculation period schedule is assumed to be between the effective date and the termination date. No implicit stubs are allowed, i.e. stubs must be explicitly specified using an appropriate combination of firstPeriodStartDate, firstRegularPeriodStartDate and lastRegularPeriodEndDate.
 */
export interface CalculationPeriodDates {
    /** The first day of the terms of the trade. This day may be subject to adjustment in accordance with a business day convention. */
    effectiveDate?: AdjustableOrRelativeDate,
    /** The last day of the terms of the trade. This date may be subject to adjustments in accordance with the business day convention. It can also be specified in relation to another scheduled date (e.g. the last payment date). */
    terminationDate?: AdjustableOrRelativeDate,
    /** The specification of the business day convention and financial business centers used for adjusting any calculation period date if it would otherwise fall on a day that is not a business day in the specified business center. */
    calculationPeriodDatesAdjustments?: BusinessDayAdjustments,
    /** The start date of the calculation period. FpML specifies that for interest rate swaps this date must only be specified if it is not equal to the effective date. It is always specified in the case of equity swaps and credit default swaps with periodic payments. This date may be subject to adjustment in accordance with a business day convention. */
    firstPeriodStartDate?: AdjustableOrRelativeDate,
    /** The start date of the regular part of the calculation period schedule. It must only be specified if there is an initial stub calculation period. This day may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. */
    firstRegularPeriodStartDate?: date,
    /** The end date of the initial compounding period when compounding is applicable. It must only be specified when the compoundingMethod element is present and not equal to a value of None. This date may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. */
    firstCompoundingPeriodEndDate?: date,
    /** The end date of the regular part of the calculation period schedule. It must only be specified if there is a final stub calculation period. This day may be subject to adjustment in accordance with any adjustments specified in calculationPeriodDatesAdjustments. */
    lastRegularPeriodEndDate?: date,
    /** Method to allocate any irregular period remaining after regular periods have been allocated between the effective and termination date. The multiplicity of 2 allows the possibility of expressing the type for both an initial and final stub at the same time. */
    stubPeriodType?: string,
    /** The frequency at which calculation period end dates occur with the regular part of the calculation period schedule and their roll date convention. */
    calculationPeriodFrequency?: CalculationPeriodFrequency,
}


/**
 * A data to:  provide the ability to point to multiple payment nodes in the document through the unbounded paymentDatesReference.
 */
export interface DateRelativeToCalculationPeriodDates {
    /** A set of href pointers to calculation period dates defined somewhere else in the document. */
    calculationPeriodDatesReference: CalculationPeriodDates[],
}


/**
 * A data to:  provide the ability to point to multiple payment nodes in the document through the unbounded paymentDatesReference.
 */
export interface DateRelativeToValuationDates {
    /** A set of href pointers to valuation period dates defined somewhere else in the document. */
    valuationDatesReference: PerformanceValuationDates[],
}


/**
 * A data to:  provide the ability to point to multiple payment nodes in the document through the unbounded paymentDatesReference.
 */
export interface DateRelativeToPaymentDates {
    /** A set of href pointers to payment dates defined somewhere else in the document. */
    paymentDatesReference: PaymentDates[],
}


/**
 * A data to:  describe the cashflow representation for FX linked notionals.
 */
export interface FxLinkedNotionalAmount {
    /** The reset date. */
    resetDate?: date,
    /** The date on which the FX spot rate is observed. This date should already be adjusted for any applicable business day convention. */
    adjustedFxSpotFixingDate?: date,
    /** The actual observed FX spot rate. */
    observedFxSpotRate?: string,
    /** The calculation period notional amount. */
    notionalAmount?: string,
}


/**
 * A data to:  describe a notional schedule where each notional that applies to a calculation period is calculated with reference to a notional amount or notional amount schedule in a different currency by means of a spot currency exchange rate which is normally observed at the beginning of each period.
 */
export interface FxLinkedNotionalSchedule {
    /** The currency of the varying notional amount, i.e. the notional amount being determined periodically based on observation of a spot currency exchange rate. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    varyingNotionalCurrency: string,
    /** The dates on which spot currency exchange rates are observed for purposes of determining the varying notional currency amount that will apply to a calculation period. */
    varyingNotionalFixingDates: RelativeDateOffset,
    /** The information source and time at which the spot currency exchange rate will be observed. */
    fxSpotRateSource: FxSpotRateSource,
    /** The time at which the spot currency exchange rate will be observed. It is specified as a time in a business day calendar location, e.g. 11:00am London time. */
    fixingTime?: BusinessCenterTime,
    /** The dates on which interim exchanges of notional are paid. Interim exchanges will arise as a result of changes in the spot currency exchange amount or changes in the constant notional schedule (e.g. amortisation). */
    varyingNotionalInterimExchangePaymentDates: RelativeDateOffset,
}


/**
 * A data defining:  the adjusted payment date and associated calculation period parameters required to calculate the actual or projected payment amount. This data forms:  part of the cashflow representation of a swap stream.
 */
export interface PaymentCalculationPeriod {
    /** The unadjusted payment date. */
    unadjustedPaymentDate?: date,
    /** The adjusted payment date. This date should already be adjusted for any applicable business day convention. This component is not intended for use in trade confirmation but may be specified to allow the fee structure to also serve as a cashflow type component. */
    adjustedPaymentDate?: date,
    /** The parameters used in the calculation of a fixed or floating rate calculation period amount. A list of calculation period elements may be ordered in the document by ascending start date. An FpML document which contains an unordered list of calculation periods is still regarded as a conformant document. */
    calculationPeriod: CalculationPeriod[],
    /** A known fixed payment amount. */
    fixedPaymentAmount?: Money,
    /** A decimal value representing the discount factor used to calculate the present value of cash flow. */
    discountFactor?: string,
    /** A monetary amount representing the forecast of the future value of the payment. */
    forecastPaymentAmount?: Money,
    /** A monetary amount representing the present value of the forecast payment. */
    presentValueAmount?: Money,
}


/**
 * A class defining a schedule of rates or amounts in terms of an initial value and then a series of step date and value pairs. On each step date the rate or amount changes to the new step value. The series of step date and value pairs are optional. If not specified, this implies that the initial value remains unchanged over time.
 */
export interface RateSchedule {
    /** The initial rate. An initial rate of 5% would be represented as 0.05. */
    price: PriceSchedule,
}


/**
 * Describes date details for a set of observation dates in parametric or non-parametric form.
 */
export interface ObservationDates {
    /** Specifies a schedule of dates (non-parametric) on which market observations take place, and allows for the optional definition of weights where applicable.  When no weight is specified, then weight of each date is assumed to be 1.0 */
    observationSchedule?: ObservationSchedule,
    /** Specifies the date range and frequency on which market observations take place.  Weights can be assigned to dates in the schedule by assigning the weight and corresponding observationReference in the observationSchedule. */
    periodicSchedule?: PeriodicDates,
    /** Specifies parametric terms to determine which days within a given calculation period the price would be observed. Typically associated with Commodities.  */
    parametricDates?: ParametricDates,
}


/**
 * Specifies a single date on which market observations take place and specifies optional associated weighting.
 */
export interface ObservationSchedule {
    /** Specifies an adjusted or unadjusted date for a market observation. */
    observationDate?: ObservationDate[],
    /** The business day convention and financial business centers used for adjusting the date if it would otherwise fall on a day that is not a business date in the specified business centers. */
    dateAdjustments?: BusinessDayAdjustments,
}


/**
 * Specifies a single date on which market observations take place and specifies optional associated weighting.
 */
export interface ObservationDate {
    /** A date subject to adjustment. */
    unadjustedDate?: date,
    /** The date once the adjustment has been performed. (Note that this date may change if the business center holidays change). */
    adjustedDate?: date,
    /** Specifies the degree of importance of the observation. */
    weight?: string,
    /** Specifies an identification key for the market observation. This attribute can be used as a reference to assign weights to a series of dates defined in a parametricSchedule. */
    observationReference?: string,
}


/**
 * Class containing terms that are associated with observing a price/benchmark/index across either single or multiple observations. 
 */
export interface ObservationTerms {
    /** Defines time in respect to a business calendar location that the price/benchmark/index is observed */
    observationTime?: BusinessCenterTime,
    /** The enumerated values to specify points in the day when option exercise and valuation can occur. */
    observationTimeType?: string,
    /** The information source where a published or displayed market rate will be obtained, e.g. Telerate Page 3750. */
    informationSource?: FxSpotRateSource,
    /** Defines rounding rules and precision to be used in the rounding of observations. */
    precision?: Rounding,
    /** Defines parameters used to generate the calculation period dates schedule, including the specification of any initial or final stub calculation periods. A calculation period schedule consists of an optional initial stub calculation period, one or more regular calculation periods and an optional final stub calculation period. In the absence of any initial or final stub calculation periods, the regular part of the calculation period schedule is assumed to be between the effective date and the termination date. No implicit stubs are allowed, i.e. stubs must be explicitly specified using an appropriate combination of firstPeriodStartDate, firstRegularPeriodStartDate and lastRegularPeriodEndDate. */
    calculationPeriodDates?: CalculationPeriodDates,
    /** Describes date details for a set of observation dates in parametric or non-parametric form. */
    observationDates: ObservationDates,
    /** The number of observation dates between observation start date and observation end date. */
    numberOfObservationDates?: number,
}


/**
 * Defines rules for the dates on which the price will be determined.
 */
export interface ParametricDates {
    /** Denotes the enumerated values to specify the day type classification used in counting the number of days between two dates. */
    dayType: string,
    /** Denotes the method by which the pricing days are distributed across the pricing period. */
    dayDistribution?: string,
    /** Indicates the days of the week on which the price will be determined. */
    dayOfWeek?: string,
    /** Defines the occurrence of the dayOfWeek within the pricing period on which pricing will take place, e.g. the 3rd Friday within each Calculation Period. If omitted, every dayOfWeek will be a pricing day. */
    dayFrequency?: string,
    /** The pricing period per calculation period if the pricing days do not wholly fall within the respective calculation period. */
    lag?: Lag,
    /** The enumerated values to specify the business centers. */
    businessCenters: BusinessCenters,
}


/**
 * The pricing period per calculation period if the pricing days do not wholly fall within the respective calculation period.
 */
export interface Lag {
    /** Defines the offset of the series of pricing dates relative to the calculation period. */
    lagDuration: Offset,
    /** Defines the offset of the series of pricing dates relative to the calculation period. */
    firstObservationDateOffset?: Offset,
}


/**
 * A data defining:  the parameters used to generate the reset dates schedule and associated fixing dates. The reset dates are the dates on which the new index value (which is observed on the fixing date) is applied for each period and on which the interest rate hence begins to accrue.
 */
export interface ResetDates {
    /** A pointer style reference to the associated calculation period dates component defined elsewhere in the document. */
    calculationPeriodDatesReference?: CalculationPeriodDates,
    /** Specifies whether the reset dates are determined with respect to each adjusted calculation period start date or adjusted calculation period end date. If the reset frequency is specified as daily this element must not be included. */
    resetRelativeTo?: string,
    /** The initial fixing date. */
    initialFixingDate?: InitialFixingDate,
    /** The fixing dates are the dates on which the index values are observed. The fixing dates are specified by reference to the reset date through business days offset and an associated set of financial business centers. Normally these offset calculation rules will be those specified in the ISDA definition for the relevant floating rate index (ISDA's Floating Rate Option). However, non-standard offset calculation rules may apply for a trade if mutually agreed by the principal parties to the transaction. */
    fixingDates?: RelativeDateOffset,
    /** This attribute is not part of the FpML ResetDate, and has been added as part of the CDM to support the credit derivatives final fixing date. */
    finalFixingDate?: AdjustableDate,
    /** Specifies the number of business days before the period end date when the rate cut-off date is assumed to apply. The financial business centers associated with determining the rate cut-off date are those specified in the reset dates adjustments. The rate cut-off number of days must be a negative integer (a value of zero would imply no rate cut off applies in which case the rateCutOffDaysOffset element should not be included). The relevant rate for each reset date in the period from, and including, a rate cut-off date to, but excluding, the next applicable period end date (or, in the case of the last calculation period, the termination date) will (solely for purposes of calculating the floating amount payable on the next applicable payment date) be deemed to be the relevant rate in effect on that rate cut-off date. For example, if rate cut-off days for a daily averaging deal is -2 business days, then the refix rate applied on (period end date - 2 days) will also be applied as the reset on (period end date - 1 day), i.e. the actual number of reset dates remains the same but from the rate cut-off date until the period end date, the same refix rate is applied. Note that in the case of several calculation periods contributing to a single payment, the rate cut-off is assumed only to apply to the final calculation period contributing to that payment. The day type associated with the offset must imply a business days offset. */
    rateCutOffDaysOffset?: Offset,
    /** The frequency at which the reset dates occur. In the case of a weekly reset frequency, also specifies the day of the week that the reset occurs. If the reset frequency is greater than the calculation period frequency then this implies that more than one reset is established for each calculation period and some form of rate averaging is applicable. */
    resetFrequency?: ResetFrequency,
    /** The definition of the business day convention and financial business centers used for adjusting the reset date if it would otherwise fall on a day that is not a business day in the specified business center. */
    resetDatesAdjustments?: BusinessDayAdjustments,
}


/**
 * A data defining:  how the initial or final stub calculation period amounts is calculated. For example, the rate to be applied to the initial or final stub calculation period may be the linear interpolation of two different tenors for the floating rate index specified in the calculation period amount component, e.g. A two month stub period may used the linear interpolation of a one month and three month floating rate. The different rate tenors would be specified in this component. Note that a maximum of two rate tenors can be specified. If a stub period uses a single index tenor and this is the same as that specified in the calculation period amount component then the initial stub or final stub component, as the case may be, must not be included.
 */
export interface StubCalculationPeriodAmount {
    /** A pointer style reference to the associated calculation period dates component defined elsewhere in the document. */
    calculationPeriodDatesReference: CalculationPeriodDates,
    /** Specifies how the initial stub amount is calculated. A single floating rate tenor different to that used for the regular part of the calculation periods schedule may be specified, or two floating tenors may be specified. If two floating rate tenors are specified then Linear Interpolation (in accordance with the 2000 ISDA Definitions, Section 8.3. Interpolation) is assumed to apply. Alternatively, an actual known stub rate or stub amount may be specified. */
    initialStub?: StubValue,
    /** Specifies how the final stub amount is calculated. A single floating rate tenor different to that used for the regular part of the calculation periods schedule may be specified, or two floating tenors may be specified. If two floating rate tenors are specified then Linear Interpolation (in accordance with the 2000 ISDA Definitions, Section 8.3. Interpolation) is assumed to apply. Alternatively, an actual known stub rate or stub amount may be specified. */
    finalStub?: StubValue,
}


/**
 * An unordered list of weighted averaging observations.
 */
export interface AveragingObservationList {
    /** A single weighted averaging observation. */
    averagingObservation: WeightedAveragingObservation[],
}


/**
 * Period over which an average value is taken.
 */
export interface AveragingPeriod {
    /** A schedule for generating averaging observation dates. */
    schedule?: AveragingSchedule[],
    /** An unweighted list of averaging observation date and times. */
    averagingDateTimes?: DateTimeList,
    /** A weighted list of averaging observation date and times. */
    averagingObservations?: AveragingObservationList,
    /** The market disruption event as defined by ISDA 2002 Definitions. */
    marketDisruption?: string,
}


/**
 * A single weighted averaging observation.
 */
export interface WeightedAveragingObservation {
    /** Observation date time, which should be used when literal observation dates are required. The CDM specifies that the zoned date time is to be expressed in accordance with ISO 8601, either as UTC as an offset to UTC. */
    dateTime?: string,
    /** Observation number, which should be unique, within a series generated by a date schedule. */
    observationNumber?: number,
    /** Observation weight, which is used as a multiplier for the observation value. */
    weight: string,
}


/**
 * A class to specify a currency amount or a currency amount schedule.
 */
export interface AmountSchedule extends Schedule {
    /** The currency in which the amount schedule is denominated. The currency is specified outside of the actual schedule in order to be applied uniformly to it. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency: string[],
}


/**
 * A CDM class which purpose is to specify the initial fixing date either alongside the FpML interest rate specification as an offset of another date, or alongside the credit derivative specification as an unadjusted date.
 */
export interface InitialFixingDate {
    relativeDateOffset?: RelativeDateOffset,
    /** The initial fixing date. */
    initialFixingDate?: date,
}


/**
 * Specifies the parameters to generate the payment date schedule, either through a parametric representation or by reference to specified dates.
 */
export interface PaymentDates {
    /** The frequency at which regular payment dates occur. If the payment frequency is equal to the frequency defined in the calculation period dates component then one calculation period contributes to each payment amount. If the payment frequency is less frequent than the frequency defined in the calculation period dates component then more than one calculation period will contribute to the payment amount. A payment frequency more frequent than the calculation period frequency or one that is not a multiple of the calculation period frequency is invalid. If the payment frequency is of value T (term), the period is defined by the effectiveDate and the terminationDate. */
    paymentFrequency?: Frequency,
    /** The first unadjusted payment date. This day may be subject to adjustment in accordance with any business day convention specified in paymentDatesAdjustments. This element must only be included if there is an initial stub. This date will normally correspond to an unadjusted calculation period start or end date. This is true even if early or delayed payment is specified to be applicable since the actual first payment date will be the specified number of days before or after the applicable adjusted calculation period start or end date with the resulting payment date then being adjusted in accordance with any business day convention specified in paymentDatesAdjustments. */
    firstPaymentDate?: date,
    /** The last regular payment date when specified as a date, as in the FpML interest rate construct. FpML specifies that this date may be subject to adjustment in accordance with any business day convention specified in the paymentDatesAdjustments attribute. */
    lastRegularPaymentDate?: date,
    /** The payment dates when specified as relative to a set of dates specified somewhere else in the instance document/transaction, e.g. the valuation dates as typically the case for equity swaps, or when specified as a calculation period schedule. */
    paymentDateSchedule?: PaymentDateSchedule,
    /** Specifies whether the payments occur relative to each adjusted calculation period start date or end date, each reset date, valuation date or the last pricing date. Calculation period start date means relative to the start of the first calculation period contributing to a given payment. Similarly, calculation period end date means the end of the last calculation period contributing to a given payment. The valuation date is applicable for Brazilian-CDI and equity swaps. */
    payRelativeTo?: string,
    /** If early payment or delayed payment is required, specifies the number of days offset that the payment occurs relative to what would otherwise be the unadjusted payment date. The offset can be specified in terms of either calendar or business days. Even in the case of a calendar days offset, the resulting payment date, adjusted for the specified calendar days offset, will still be adjusted in accordance with the specified payment dates adjustments. This element should only be included if early or delayed payment is applicable, i.e. if the periodMultiplier element value is not equal to zero. An early payment would be indicated by a negative periodMultiplier element value and a delayed payment (or payment lag) would be indicated by a positive periodMultiplier element value. */
    paymentDaysOffset?: Offset,
    /** The definition of the business day convention and financial business centers used for adjusting the payment date if it would otherwise fall on a day that is not a business day in the specified business center. */
    paymentDatesAdjustments?: BusinessDayAdjustments,
}


/**
 * The payment dates when specified as relative to a set of dates specified somewhere else in the instance document/transaction, e.g. the valuation dates as typically the case for equity swaps, or when specified as a calculation period schedule.
 */
export interface PaymentDateSchedule {
    interimPaymentDates?: AdjustableRelativeOrPeriodicDates[],
    /** The last payment when specified as an adjustable or relative date, as in the FpML total return construct. */
    finalPaymentDate?: AdjustableOrRelativeDate,
}


/**
 * A class defining the reset frequency. In the case of a weekly reset, also specifies the day of the week that the reset occurs. If the reset frequency is greater than the calculation period frequency the this implies that more or more reset dates is established for each calculation period and some form of rate averaging is applicable. The specific averaging method of calculation is specified in FloatingRateCalculation. In case the reset frequency is of value T (term), the period is defined by the swap/swapStream/calculationPerioDates/effectiveDate and the swap/swapStream/calculationPerioDates/terminationDate.
 */
export interface ResetFrequency extends Frequency {
    /** The day of the week on which a weekly reset date occurs. This element must be included if the reset frequency is defined as weekly and not otherwise. */
    weeklyRollConvention?: string,
}


/**
 * A data to:  define business date convention adjustment to final payment period per leg.
 */
export interface FinalCalculationPeriodDateAdjustment {
    /** Reference to the unadjusted cancellation effective dates. */
    relevantUnderlyingDateReference: AdjustableOrRelativeDates,
    /** Reference to the leg, where date adjustments may apply. */
    swapStreamReference: InterestRatePayout,
    /** Override business date convention. This takes precedence over leg level information. */
    businessDayConvention: string,
}


/**
 * Type for reporting the detailed results of calculating a cash flow for a calculation period.  This is enhanced relative to the FpML-based cashflows structure to allow more information to be returned about daily compounded rates.
 */
export interface FloatingAmountCalculationDetails {
    /** The calculation period for which the floating calculation was performed. */
    calculationPeriod: CalculationPeriodBase,
    /** The notional in effect during the calculation period. */
    calculationPeriodNotionalAmount: Money,
    /** The details of the floating rate setting.  (If it is a calculated rate, details of that calculation will be inside that. */
    floatingRate?: FloatingRateSettingDetails,
    /** Details fo the floating rate treatment after the rate is observed or calculated.  This will include details of things like multipliers, spreads, caps and floors, and the raw and treated rates. */
    processingDetails?: FloatingRateProcessingDetails,
    /** The rate that was actually applied, after all calculations and treatments. */
    appliedRate: string,
    /** The fraction of a year that this calculation represents, according to the day count fraction method. */
    yearFraction: string,
    /** The amount of the cash flow that was computed, including any spreads and other processing. */
    calculatedAmount: string,
    /** The amount of the cash flow excluding any spread, for subsequent processing. */
    spreadExclusiveCalculatedAMount: string,
}


/**
 * Type for reporting the raw (untreated) observed or calculated rate for a calculation period.  If this is a calculated rate, it allows details of the observations and the resulting rate to be returned.
 */
export interface FloatingRateSettingDetails {
    /** Calculated rate details (observation dates, values, and weights). */
    calculationDetails?: CalculatedRateDetails,
    /** The day upon which the rate was observed (for term rates). */
    observationDate?: date,
    /** The day for which the rate is needed (e.g. period beginning or end date). */
    resetDate?: date,
    /** The resulting rate that was observed or calculated. */
    floatingRate: string,
}


/**
 * Type for reporting the details of the rate treatment.  This could potentially be replaced by the existing FloatingRateDefinition type , but this is slightly more detailed.
 */
export interface FloatingRateProcessingDetails {
    /** The raw or untreated rate, prior to any of the rate treatments. */
    rawRate: string,
    processingParameters?: FloatingRateProcessingParameters,
    /** The value of the rate after processing. */
    processedRate: string,
    /** The value of the processed rate without the spread applied, for subsequent compounding, etc. */
    spreadExclusiveRate: string,
}


/**
 * Type to hold the processing parameters that should be or were used to calculate a floating amount.  These parameters can vary over a schedule so this type holds the acutal values applicable to this calculation.
 */
export interface FloatingRateProcessingParameters {
    /** The rate to be applied for the initial period. */
    initialRate?: Price,
    /** floating rate multiplier. */
    multiplier?: string,
    /** spread to be added to the floating rate. */
    spread?: string,
    /** US rate treatment (Bond Equivalent Yield or Money Market Yield, if applicable. */
    treatment?: string,
    /** capt to be applied to the floating rate. */
    capRate?: string,
    /** floor to be applied to the floating rate. */
    floorRate?: string,
    /** THe final rate rounding to be applied. */
    rounding?: Rounding,
    /** How to handle negative interest rates. */
    negativeTreatment?: string,
}


/**
 * Query to check against an EligibleCollateralSpecification
 */
export interface EligibilityQuery {
    /** Maturity in years */
    maturity: string,
    /** The asset product type. */
    collateralAssetType: AssetType,
    /** The asset country of origin. */
    assetCountryOfOrigin: string,
    /** The underlying asset denominated currency. */
    denominatedCurrency: string,
    /** The agency rating based on default risk and creditors claim in event of default associated with specific instrument. */
    agencyRating: AgencyRatingCriteria,
    /** Represents a filter based on the type of entity issuing the asset. */
    issuerType: CollateralIssuerType,
    /** Specifies the issuing entity name or LEI. */
    issuerName: LegalEntity,
}


/**
 * Result for the CheckEligibilityByDetails and CheckEligibilityForProduct functions
 */
export interface CheckEligibilityResult {
    /** a simple boolean which is set to true if the asset described in the EligibilityQuery input is eligible */
    isEligible: boolean,
    /** if there was a match, this will be the one or more criteria that were supplied in the EligbilityCollateralSpecification which matched with the query input */
    matchingEligibleCriteria?: EligibleCollateralCriteria[],
    /** a copy of the input query that was checked against the eligible collateral specification */
    eligibilityQuery: EligibilityQuery,
    /** a copy of the input EligbilityCollateralSpecification that was checked against the query */
    specification: EligibleCollateralSpecification,
}


/**
 * A type for defining the obligations of the counterparty subject to credit support requirements.
 */
export interface Collateral {
    /** Independent Amount is an amount that usually less creditworthy counterparties are asked to provide. It can either be a fixed amount or a percentage of the Transaction's value. The Independent Amount can be: (i) transferred before any trading between the parties occurs (as a deposit at a third party's account or with the counterparty) or (ii) callable after trading has occurred (typically because a downgrade has occurred). In situation (i), the Independent Amount is not included in the calculation of Exposure, but in situation (ii), it is included in the calculation of Exposure. Thus, for situation (ii), the Independent Amount may be transferred along with any collateral call. Independent Amount is a defined term in the ISDA Credit Support Annex. ('with respect to a party, the amount specified as such for that party in Paragraph 13; if no amount is specified, zero'). */
    independentAmount?: IndependentAmount,
    /** A list of identifiers pointing to the collateral portfolios which contain the collateral which covers a trade. */
    portfolioIdentifier?: Identifier[],
    /** The collateral portfolios which contain the collateral which covers a trade. (NB: this can be provided by reference to a global key for each CollateralPortfolio object) */
    collateralPortfolio?: CollateralPortfolio[],
    /** specifies the collateral provisions of the product. */
    collateralProvisions?: CollateralProvisions,
}


/**
 * Contains collateral attributes which can also inherit information from a GMRA
 */
export interface CollateralProvisions {
    /** Enumerates the collateral types which are accepted by the Seller. */
    collateralType: string,
    /** The eligible collateral as specified in relation to the transaction. */
    eligibleCollateral?: EligibleCollateralCriteria[],
    /** The provisions for collateral substitutions such as how many and when they are allowed. */
    substitutionProvisions?: SubstitutionProvisions,
}


/**
 * Defines collateral substitution provisions such as how many and with how much notice are substitutions allowed.
 */
export interface SubstitutionProvisions {
    /** Specifies if 1 or more substitutions are allowed. */
    numberOfSubstitutionsAllowed?: number,
    /** Defines the min period for notify of a substitution. */
    noticeDeadlinePeriod?: Period,
    /** A specific date and time for the notice deadline */
    noticeDeadlineDateTime?: string,
}


/**
 * A class specifying the Independent Amount as the combination of a payer/receiver, a payment amount, a payment date and an associated payment calculation rule.
 */
export interface IndependentAmount extends PartyReferencePayerReceiver {
    /** An attribute that specifies a payment as the combination of a payment amount, a payment date and an associated payment calculation rule. */
    paymentDetail: PaymentDetail[],
}


/**
 * Represents a class to specify a credit notation.
 */
export interface AgencyRatingCriteria {
    /** Indicates the agency rating criteria specified for the asset or issuer. */
    creditNotation: CreditNotation,
    /** Indicator for options to be used if several agency ratings (>1) are specified and its necessary to identify specific charateristics. i.e (lowest or highest). */
    mismatchResolution?: string,
    /** identifies the dominant reference agency if there is a missmatch and several reference agencies exsist. */
    referenceAgency?: string,
    /** Indicates the boundary of a credit agency rating i.e minimum or maximum. */
    boundary?: string,
}


/**
 * Specification of the valuation treatment for the specified collateral.
 */
export interface CollateralValuationTreatment {
    /** Specifies a haircut percentage to be applied to the value of asset and used as a discount factor to the value of the collateral asset,expressed as a percentage in decimal terms. As an example a 0.5% haircut would be represented as a decimal number 0.005. */
    haircutPercentage?: string,
    /** Specifies a percentage value of transaction needing to be posted as collateral expressed as a valuation. As an example a 104% requirement would be represented as a decimal number 1.04. */
    marginPercentage?: string,
    /** Specifies an FX haircut applied to a specific asset which is agreed between the parties (for example, if pledgor eligible collateral is not denominated in the termination currency or under other specified cases in collateral support documents both for initial margin and variation margin).The percentage value is expressed as the discount haircut to the value of the collateral- as an example an 8% FX haircut would be expressed as 0.08. */
    fxHaircutPercentage?: string,
    /** Specifies a percentage value of any additional haircut to be applied to a collateral asset,the percentage value is expressed as the discount haircut to the value of the collateral- as an example a 5% haircut would be expressed as 0.05.  */
    additionalHaircutPercentage?: string,
}


/**
 * Represents a set of criteria used to specify eligible collateral.
 */
export interface EligibleCollateralSpecification {
    /** Specifies the identifier(s) to uniquely identify eligible collateral or a set of eligible collateral, such as a schedule or equivalant for an identity issuer. */
    identifier?: Identifier[],
    /** The parties associated with the specification. */
    party?: Party[],
    /** Specification of the roles of the counterparties to the specification. */
    counterparty?: Counterparty[],
    /** Represents a set of criteria used to specify eligible collateral. */
    criteria: EligibleCollateralCriteria[],
    /** Specifies the role(s) that each of the party(s) is playing in the context of the specification, eg Payor or Receiver. */
    partyRole?: PartyRole[],
}


/**
 * Represents a set of criteria used to specify and describe collateral.
 */
export interface CollateralCriteriaBase {
    /** The specific criteria that applies. It can be created using AND, OR and NOT logic, and both asset and issuer characteristics. */
    collateralCriteria?: CollateralCriteria,
    /** Specifies which of the two counterparties the criteria applies to (either one or both counterparties). This attribute is optional, in case the applicable party is already specified elsewhere within a party election. */
    appliesTo?: string,
    /** Restrict the criteria to only apply to a specific type of margin, ie IM or VM. */
    restrictTo?: string,
    /** Denotes which Criteria has priority if more than one agency rating applies. */
    ratingPriorityResolution?: string,
}


/**
 * Represents a set of criteria used to specify eligible collateral.
 */
export interface EligibleCollateralCriteria extends CollateralCriteriaBase {
    /** Identifies the treatment of specified collateral, e.g., haircuts,holding limits or exclusions. */
    treatment: CollateralTreatment,
}


/**
 * Specifies the treatment terms for the eligible collateral criteria specified.
 */
export interface CollateralTreatment {
    /** Specification of the valuation treatment for the specified collateral. */
    valuationTreatment?: CollateralValuationTreatment,
    /** Specification of concentration limits applicable to the collateral criteria. */
    concentrationLimit?: ConcentrationLimit[],
    /** A boolean attribute to specify whether collateral critieria are inclusion (True) or exclusion (False) criteria. */
    isIncluded: boolean,
}


/**
 * Represents a class to describe concentration limits that may be applicable to eligible collateral criteria.
 */
export interface ConcentrationLimit {
    /** Specifies a set of criteria to describe the assets that the concentration limits apply to. */
    concentrationLimitCriteria?: ConcentrationLimitCriteria,
    /** Specifies the value of collateral limit represented as a range. */
    valueLimit?: MoneyRange,
    /** Specifies the perecentage of collateral limit represented as a decimal number - example 25% is 0.25. */
    percentageLimit?: NumberRange,
}


/**
 * Respresents a class to describe a set of criteria to describe specific assets that the concentration limits apply to.
 */
export interface ConcentrationLimitCriteria extends CollateralCriteriaBase {
    /** Specifies the type of concentration limit to be applied. */
    concentrationLimitType?: string,
    /** Specifies an average trading volume on an exchange in relation to Equity products. */
    averageTradingVolume?: AverageTradingVolume,
}


/**
 * Represents the average trading volume of an Equity product upon an exchange or set of exchanges.
 */
export interface AverageTradingVolume {
    /** Represents the period of the equities average trading volume on the exchange/s. */
    period: Period,
    /** Indicates the type of equity average trading volume being stated (single) the highest amount on one exchange, or (consolidated) volumes across multiple exchanges. */
    methodology: string,
}



export interface IssuerCountryOfOrigin {
    /** Represents a filter on the issuing entity country of origin based on the ISO Standard 3166, which is the same as filtering by eligible Sovereigns. */
    issuerCountryOfOrigin: string,
}



export interface AssetCountryOfOrigin {
    /** Represents a filter on the asset country of origin based on the ISO Standard 3166. */
    assetCountryOfOrigin: string,
}



export interface IssuerName {
    /** Specifies the issuing entity name or LEI. */
    issuerName: LegalEntity,
}



export interface IssuerAgencyRating {
    /** Represents an agency rating based on default risk and creditors claim in event of default associated with asset issuer. */
    issuerAgencyRating: AgencyRatingCriteria,
}



export interface SovereignAgencyRating {
    /** Represents an agency rating based on default risk of the country of the issuer. */
    sovereignAgencyRating: AgencyRatingCriteria,
}



export interface AssetAgencyRating {
    /** Represents an agency rating based on default risk and creditors claim in event of default associated with specific instrument. */
    assetAgencyRating: AgencyRatingCriteria,
}



export interface AssetMaturity {
    /** Specifies whether the maturity range is the remaining or original maturity. */
    maturityType: string,
    /** Represents a filter based on the underlying asset maturity. */
    maturityRange: PeriodRange,
}


/**
 * Specifies a filter based on a stock exchange.
 */
export interface ListingExchange {
    /** Represents a filter based on the Primary Stock Exchange facilitating the listing of companies, exchange of Stocks, Exchange traded Derivatives, Bonds, and other Securities expressed in ISO standard 10383. */
    exchange: string[],
}


/**
 * Specifies a filter based on an industry sector.
 */
export interface ListingSector {
    /** Represents a filter based on an industry sector defined under a system for classifying industry types such as Global Industry Classification Standard (GICS) and North American Industry Classification System (NAICS) */
    sector: string[],
}



export interface DomesticCurrencyIssued {
    /** Identifies that the Security must be denominated in the domestic currency of the issuer. */
    domesticCurrencyIssued: boolean,
}



export interface CounterpartyOwnIssuePermitted {
    /** Represents a filter based on whether it is permitted for the underlying asset to be issued by the posting entity or part of their corporate family. */
    counterpartyOwnIssuePermitted: boolean,
}


/**
 * Used to combine two or more Collateral Criteria using AND logic.
 */
export interface AllCriteria {
    allCriteria: CollateralCriteria[],
}


/**
 * Used to combine two or more Collateral Criteria using OR logic.
 */
export interface AnyCriteria {
    anyCriteria: CollateralCriteria[],
}


/**
 * Used to apply a NOT logic condition to a single Collateral Criteria.
 */
export interface NegativeCriteria {
    negativeCriteria: CollateralCriteria,
}


/**
 * A class to specify the Distributions and Interest Payment provisions applicable to the collateral agreement.
 */
export interface DistributionAndInterestPayment {
    /** Represents the interest parameters for the various currencies, margin types, posting parties. */
    interestParameters?: CollateralInterestParameters[],
}


/**
 * Represents the floating interest calculation and distribution parameters for a single currency.
 */
export interface CollateralInterestParameters {
    /** Represents the party to which these parameters apply (the applicable party).  In other words, if the parameters are different depending on which party is posting/holding the collateral, for which party to the Collateral Agreement (Party 1 or Party 2) that is posting the collateral do these parameters apply? */
    postingParty?: string,
    /** Specifies the type of margin for which interest is being calculated, if the parameters are different depending on type of margin (initial or variation). */
    marginType?: string,
    /** Specifies the currency for which the parameters are captured. */
    currency?: string,
    /** Represents the basic interest calculation parameters. */
    interestCalculationParameters?: CollateralInterestCalculationParameters,
    /** Represents how often and when interest is calculated. */
    interestCalculationFrequency?: CalculationFrequency,
    /** Represents the parameters describing how and when interest transfer occurs. */
    interestHandlingParameters?: CollateralInterestHandlingParameters,
}


/**
 * Represents parameters for calculating the amount the floating interest calculation, e.g.  for a single currency or defaults for all currencies.
 */
export interface CollateralInterestCalculationParameters {
    /** Specifies the applicable fixed rate  if used. */
    fixedRate?: string,
    /** Specifies the floating interest rate to be used. */
    floatingRate?: CollateralAgreementFloatingRate,
    /** If True, specifies that the interest transfers should be converted to base currency equivalent, or if False specifies that the transfer should be in the currency of the collateral. */
    inBaseCurrency: boolean,
    /** Specifies the type of compounding to be applied (None, Business, Calendar). */
    compoundingType?: string,
    /** Specifies the applicable business centers for compounding. */
    compoundingBusinessCenter?: string[],
    /** Specifies the day count fraction to use for that currency. */
    dayCountFraction: string,
    /** Specifies the rounding rules for settling in that currency. */
    rounding?: Rounding,
    /** Specifies when/how often is rounding applied? */
    roundingFrequency?: string,
    /** Specifies the withholding tax rate if a withholding tax is applicable. */
    withholdingTaxRate?: string,
}


/**
 * Represents the parameters needed to calculate the floating rate paid on collateral holdings.
 */
export interface CollateralAgreementFloatingRate extends FloatingRateBase {
    /** Specifies how negative rates should be applied.  If rates go negative, should the payment be reversed (true) or zeroed out (false)? */
    negativeInterest: boolean,
    /** Specifies how spreads should be applied in a low/negative rate environment.  If true, spread is applied only if rate is positive. */
    compressibleSpread: boolean,
}


/**
 * Represents parameters that describe how calculated interest amounts are handled, i.e. are they transferred/distributed, or is the collateral balance adjusted, is netting done, and any other special handling.
 */
export interface CollateralInterestHandlingParameters {
    /** Specifies how the collateral interest is to be handled. */
    interestPaymentHandling: string,
    /** Specifies applicable business centers for payments. */
    paymentBusinessCenter?: string[],
    /** Indicates whether to net Held and Posted Interest Payments (i.e. whether interest payable for a period can be netted with interest receivable). */
    netPostedAndHeldInterest: boolean,
    /** Indicates whether the interest amount may be offset against any margin call deliver or return amounts?   (aka 'net payments' indicator). */
    netInterestWithMarginCalls: boolean,
    /** Indicates whether or not to include the open interest accrual in the margin calculation. */
    includeAccrualInMarginCalc: boolean,
    /** Indicates whether interest accruing on unsettled interest amount is included (continues to be accrued) in the following period. */
    accrueInterestOnUnsettledInterest?: boolean,
    /** Indicates the option that accrued interest should be calculated and distributed when a full return of collateral occurs. */
    onFullReturn: boolean,
    /** Indicates the option that accrued interest should be calculated and distributed when a partial return collateral occurs. */
    onPartialReturn: boolean,
    /** The application of Interest Amount with respect to the Delivery Amount and the Return Amount. */
    interestAmountApplication?: InterestAmountApplication,
    /** Specifies the level below which the interest will be rolled over. */
    interestRolloverLimit?: NumberBound,
    /** Specifies the level below which the interest will be written off; if omitted write-off is not applicable. */
    writeoffLimit?: NumberBound,
    /** Specifies the alternative to interest amounts. */
    alternativeToInterestAmount?: string,
    /** Specifies an alternative to interest amount, when the alternative provision clause is specified. */
    alternativeProvision?: string,
    /** Specifies the time of day that interest needs to be confirmed by. */
    cutoffTime?: string,
    /** Specifies the terms describing notification requirements. */
    notification?: CollateralInterestNotification,
}


/**
 * Represents the parameters describing when notifications should be made for required collateral interest transfers.
 */
export interface CollateralInterestNotification {
    /** Specifies what triggers notification (should be enum) Interest Statement Frequency, Period End Date. */
    trigger: string,
    /** Specifies the number of days before (negative) or after (positive) the trigger event. */
    offset: string,
    /** Specifies the time of day that the notification should occur. */
    notificationTime: string,
    /** The type of days on which notification should occur. */
    notificationDayType: string,
}


/**
 * A class to specify the application of Interest Amount with respect to the Delivery Amount and the Return Amount.
 */
export interface InterestAmountApplication {
    /** The application of Interest Amount with respect the Return Amount. */
    returnAmount: ReturnAmount,
    /** The application of Interest Amount with respect the Delivery Amount. */
    deliveryAmount: DeliveryAmount,
}


/**
 * A class to specify the application of Interest Amount with respect the Return Amount.
 */
export interface ReturnAmount {
    /** Default language is included when True, and excluded when False. */
    includesDefaultLanguage?: boolean,
    /** Custom election that might be specified by the parties to the agreement. */
    customElection?: string,
}


/**
 * A class to specify the application of Interest Amount with respect the Delivery Amount.
 */
export interface DeliveryAmount {
    /** The standard election as specified by an enumeration. */
    standardElection?: string,
    /** The custom election that might be specified by the parties to the agreement. */
    customElection?: string,
}


/**
 * A single, specifically identified Asset chosen from the Asset data type
 */
export interface SpecificAsset extends Asset {
}


/**
 * Specification of an index.
 */
export interface IndexType {
    /** An enumerated list for equity indices. */
    equityIndex?: string,
}


/**
 * The possible different terms that can be combined, using AND, OR and NOT logic, to define the issuers and/or assets that meet a given criteria for collateral.
 */
export interface CollateralCriteria {
}


/**
 * A class to specify the outcome of a computed amount, for testing purposes.
 */
export interface ComputedAmount {
    callFunction: string,
    /** Current value of the outstanding contract */
    amount: string,
    /** The currency in which the computed amount is denominated. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    currency?: string,
}


/**
 * Defines the basic parameters of an asset transfer, e.g. a cashflow: what (the asset), how much (the quantity) and when (the settlement date).
 */
export interface AssetFlowBase {
    /** Represents the amount of the asset to be transferred. The cashflow amount is always a positive number, as the cashflow direction is implied by the payer/receiver attribute. */
    quantity: NonNegativeQuantity,
    /** Represents the object that is subject to the transfer, it could be an asset or a reference. */
    asset: Asset,
    /** Represents the date on which the transfer to due. */
    settlementDate: AdjustableOrAdjustedOrRelativeDate,
    /** FpML specifies the FpML PaymentDiscounting.model group for representing the discounting elements that can be associated with a payment. */
    paymentDiscounting?: PaymentDiscounting,
}


/**
 * Specifies an asset flow, i.e. the outcome of either of computation (e.g. interest accrual) or an assessment of some sort. The asset flow can then be turned into an asset transfer artefact to be used as the input to a payment system or the outcome of it. The associated globalKey denotes the ability to associate a hash value to the AssetFlow instantiations for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface AssetFlow extends AssetFlowBase {
    /** Specifies who pays / receives the asset flow, through a normalised Party1 / Party2 enumerator. */
    payerReceiver: PayerReceiver,
    /** Type of asset flow corresponding to a scheduled event. */
    assetFlowType?: string,
}


/**
 *  Class to specify a mechanism for a quantity to be set as a multiplier to another (reference) quantity, based on a price observation. At the moment this class only supports FX or Equity-linked notional and re-uses existing building blocks for those 2 cases, until such time when component can be made more generic. This captures the case of resetting cross-currency swaps and resetting equity swaps.
 */
export interface QuantityMultiplier {
    /** Multiplier specified as an FX-linked schedule, e.g. for a resetting cross-currency swap.. */
    fxLinkedNotionalSchedule?: FxLinkedNotionalSchedule,
    multiplierValue?: string,
}


/**
 * Generic class to specify the quantity for different payout legs in a contractual product, when that quantity can vary across payout legs or across time. A resolvable quantity can always be resolved into a single quantity from the quantity notation which has a corresponding asset identifier. In addition to the base case, where quantity is directly specified as a number as part of the quantity notation, the other use cases are: (i) quantity based on some pre-defined schedule (eg amortising notional), (ii) quantity based on some pre-defined events (eg resetting cross-currency notional), or quantity set as reference to another quantity (eg equity notional as no. securities x price).
 */
export interface ResolvablePriceQuantity {
    /** A product's quantity as a single, non-negative amount.  When specified as part of a product definition, this quantity attribute would not be set.  Instead it is specified on the quantity notation along with an asset identifier matching this payout's asset identifier.  This allows the quantity to be resolved for a payout leg, which can then be specified here for convenience during data processing.  There needs to be at least one resolvable quantity across payout legs of a product to define an anchor that other payout quantities can refer to.  This attribute is ignored when mapping existing FpML messages. */
    resolvedQuantity?: Quantity,
    /** A payout's quantity specified as a schedule, which may also contain a single value if that quantity is constant. There can only be a single quantity schedule applicable to a payout: e.g. the notional for an interest rate leg. The quantity must be specified outside of the payout in a PriceQuantity object and only referenced inside the payout using an address. */
    quantitySchedule?: NonNegativeQuantitySchedule,
    /** Reference quantity when resolvable quantity is defined as relative to another (resolvable) quantity. A resolvable quantity needs to contain either an absolute quantity or a reference to another (resolvable) quantity. This requirement is captured by a choice rule on the class. */
    quantityReference?: ResolvablePriceQuantity,
    /** Quantity multiplier is specified on top of a reference quantity and is used as a multiplying factor when resolving the quantity. A quantity multiplier can only exist when the resolvable quantity specifies a reference quantity. */
    quantityMultiplier?: QuantityMultiplier,
    /** Whether the quantity is resettable */
    reset?: boolean,
    /** The future value notional is specific to BRL CDI swaps, and is specified alongside the notional amount. The value is calculated as follows: Future Value Notional = Notional Amount * (1 + Fixed Rate) ^ (Fixed Rate Day Count Fraction). The currency should always match that expressed in the notional schedule. The value date should match the adjusted termination date. */
    futureValueNotional?: FutureValueAmount,
    /** A payout's price specified as a schedule, which may also contain a single value if that price is constant. There may be multiple prices specified for a single payout: e.g. a floating interest rate leg may specify a spread, a cap and/or floor and a multiplier. The price must be specified outside of the payout in a PriceQuantity object and only referenced inside the payout using an address. */
    priceSchedule?: PriceSchedule[],
}


/**
 * A data type that contains the common attributes (e.g. payer and receiver parties) and validation conditions that apply across all payout types
 */
export interface PayoutBase {
    /** Canonical representation of the payer and receiver parties applicable to each payout leg. */
    payerReceiver: PayerReceiver,
    /** Each payout leg must implement the quantity concept as a 'resolvable' type, which allows for different payout legs to be linked to each other (e.g. in the case of cross-curreny products). */
    priceQuantity?: ResolvablePriceQuantity,
    /** The specification of the principal exchange. Optional as only applicable in the case of cross-currency or zero-coupon swaps with a final payment. */
    principalPayment?: PrincipalPayments,
    /** Each payout leg must specifies its settlement terms, including the delivery type (i.e. cash vs physical, and their respective terms), the transfer type (DvP etc.) and settlement date, if any. */
    settlementTerms?: SettlementTerms,
}


/**
 * Defines parameters in which the commodity price is assessed.
 */
export interface CommodityPriceReturnTerms {
    /** Defines rounding rules and precision to be used in the rounding of a number. */
    rounding?: Rounding,
    /** Defines a spread value for one or more defined dates. */
    spread?: SpreadSchedule,
    /** Used in conjunction with an exchange-based pricing source. Identifies a way in which the futures contracts referenced will roll between periods.  */
    rollFeature?: RollFeature,
    /** Defines the conversion applied if the quantity unit on contract is different from unit on referenced underlier. */
    conversionFactor?: string,
}


/**
 * Used in conjunction with an exchange-based pricing source. Identifies a way in which the futures contracts referenced will roll between periods. 
 */
export interface RollFeature {
    /** Used in conjunction with an exchange-based pricing source. Identifies a date source calendar from which the pricing dates and thus roll to the next contract will be based off (e.g. pricing is based on the NYMEX WTI First Nearby Futures Contract, if Future is chosen, the pricing will roll to the next futures contract on expiration, if ListedOption is chosen, the pricing will roll to the next futures contract on the Option expiration date which is three business days before the expiration of the NYMEX WTI futures contract.) Omitting this element will result in the default behavior expected with the pricing source described within the commodity element. */
    rollSourceCalendar?: string,
    /** Specifies, for a Commodity Transaction that references a delivery date for a listed future, the day on which the specified future will roll to the next nearby month prior to the expiration of the referenced future. If the future will not roll at all - i.e. the price will be taken from the expiring contract, 0 should be specified here. If the future will roll to the next nearby on the last trading day - i.e. the price will be taken from the next nearby on the last trading day, then 1 should be specified and so on. */
    deliveryDateRollConvention?: Offset,
}


/**
 * Specifies specific dates or parametric rules for the dates on which the price will be determined
 */
export interface PricingDates {
    /** Defines specified dates on which the price will be determined. */
    specifiedDates?: AdjustableDates[],
    /** Defines rules for the dates on which the price will be determined. */
    parametricDates?: ParametricDates,
}


/**
 * A single object that represents the different methods to specify a valuation date, as used for cash settlement. The Single / Multiple ValuationDate is used for the determination of recovery in a credit event, the RelativeDateOffset is used for cash-settled option, and FxFixingDate is used for cross-currency settlement.
 */
export interface ValuationDate {
    /** Where single valuation date is specified as being applicable for cash settlement, this element specifies the number of business days after satisfaction of all conditions to settlement when such valuation date occurs. ISDA 2003 Term: Single Valuation Date. */
    singleValuationDate?: SingleValuationDate,
    /** Where multiple valuation dates are specified as being applicable for cash settlement, this element specifies (a) the number of applicable valuation dates, and (b) the number of business days after satisfaction of all conditions to settlement when the first such valuation date occurs, and (c) the number of business days thereafter of each successive valuation date. ISDA 2003 Term: Multiple Valuation Dates. */
    multipleValuationDates?: MultipleValuationDates,
    /** The date on which the cash settlement amount will be determined according to the cash settlement method if the parties have not otherwise been able to agree the cash settlement amount. This attribute was formerly part of 'OptionCashSettlement', which is now being harmonised into a common 'CashSettlementTerms' that includes a 'ValuationDate'. */
    valuationDate?: RelativeDateOffset,
    /** The date on which the currency rate will be determined for the purpose of specifying the amount in deliverable currency. This attribute was formerly part of 'NonDeliverableSettlement', which is now being harmonised into a common 'CashSettlementTerms' that includes a 'ValuationDate'. */
    fxFixingDate?: FxFixingDate,
    /** The date, when expressed as a schedule of date(s), on which the currency rate will be determined for the purpose of specifying the amount in deliverable currency. This attribute was formerly part of 'NonDeliverableSettlement', which is now being harmonised into a common 'CashSettlementTerms' that includes a 'ValuationDate'. */
    fxFixingSchedule?: AdjustableDates,
}


/**
 * Defines the terms required to compute and settle a cash settlement amount according to a fixing value, including the fixing source, fixing method and fixing date. In FpML, PhysicalSettlementTerms and CashSettlementTerms extend SettlementTerms. In the CDM, this extension paradigm has not been used because SettlementTerms class has been used for purposes related to securities transactions, while it is not used as such in the FpML standard (i.e. only as an abstract construct.
 */
export interface CashSettlementTerms {
    /** Specifies the type of cash settlement method: cash price, yield curve etc. */
    cashSettlementMethod?: string,
    /** Specifies the parameters required to obtain a valuation, including the source, quotation method (bid, mid etc.) and any applicable quotation amount. */
    valuationMethod?: ValuationMethod,
    /** Defines the different methods to specify a valuation date, as used for cash settlement. The Single / Multiple ValuationDate is used for the determination of recovery in a credit event, the RelativeDateOffset is used for cash-settled option, and FxFixingDate is used for cross-currency settlement. */
    valuationDate?: ValuationDate,
    /** The time of the cash settlement valuation date when the cash settlement amount will be determined according to the cash settlement method, if the parties have not otherwise been able to agree the cash settlement amount. When using quations, this is the time of day in the specified business center when the calculation agent seeks quotations for an amount of the reference obligation for purposes of cash settlement. ISDA 2003 Term: Valuation Time. */
    valuationTime?: BusinessCenterTime,
    /** The amount paid by the seller to the buyer for cash settlement on the cash settlement date. If not otherwise specified, would typically be calculated as 100 (or the Reference Price) minus the price of the Reference Obligation (all expressed as a percentage) times Floating Rate Payer Calculation Amount. ISDA 2003 Term: Cash Settlement Amount. */
    cashSettlementAmount?: Money,
    /** Used for fixed recovery, specifies the recovery level, determined at contract formation, to be applied on a default. Used to calculate the amount paid by the seller to the buyer for cash settlement on the cash settlement date. Amount calculation is (1 minus the Recovery Factor) multiplied by the Floating Rate Payer Calculation Amount. The currency will be derived from the Floating Rate Payer Calculation Amount. */
    recoveryFactor?: string,
    /** Used for Recovery Lock, to indicate whether fixed Settlement is Applicable or Not Applicable. If Buyer fails to deliver an effective Notice of Physical Settlement on or before the Buyer NOPS Cut-off Date, and if Seller fails to deliver an effective Seller NOPS on or before the Seller NOPS Cut-off Date, then either: (a) if Fixed Settlement is specified in the related Confirmation as not applicable, then the Seller NOPS Cut-off Date shall be the Termination Date; or (b) if Fixed Settlement is specified in the related Confirmation as applicable, then: (i) if the Fixed Settlement Amount is a positive number, Seller shall, subject to Section 3.1 (except for the requirement of satisfaction of the Notice of Physical Settlement Condition to Settlement), pay the Fixed Settlement Amount to Buyer on the Fixed Settlement Payment Date; and (ii) if the Fixed Settlement Amount is a negative number, Buyer shall, subject to Section 3.1 (except for the requirement of satisfaction of the Notice of Physical Settlement Condition to Settlement), pay the absolute value of the Fixed Settlement Amount to Seller on the Fixed Settlement Payment Date. */
    fixedSettlement?: boolean,
    /** Indicates whether accrued interest is included (true) or not (false). For cash settlement this specifies whether quotations should be obtained inclusive or not of accrued interest. For physical settlement this specifies whether the buyer should deliver the obligation with an outstanding principal balance that includes or excludes accrued interest. ISDA 2003 Term: Include/Exclude Accrued Interest. */
    accruedInterest?: boolean,
}


/**
 * A class to specify all the ISDA terms relevant to defining the deliverable obligations.
 */
export interface DeliverableObligations {
    /** Indicates whether accrued interest is included (true) or not (false). For cash settlement this specifies whether quotations should be obtained inclusive or not of accrued interest. For physical settlement this specifies whether the buyer should deliver the obligation with an outstanding principal balance that includes or excludes accrued interest. ISDA 2003 Term: Include/Exclude Accrued Interest. */
    accruedInterest?: boolean,
    /** Used in both obligations and deliverable obligations to represent a class or type of securities which apply. ISDA 2003 Term: Obligation Category/Deliverable Obligation Category. */
    category?: string,
    /** An obligation and deliverable obligation characteristic. An obligation that ranks at least equal with the most senior Reference Obligation in priority of payment or, if no Reference Obligation is specified in the related Confirmation, the obligations of the Reference Entity that are senior. ISDA 2003 Term: Not Subordinated. */
    notSubordinated?: boolean,
    /** An obligation and deliverable obligation characteristic. The currency or currencies in which an obligation or deliverable obligation must be payable. ISDA 2003 Term: Specified Currency. */
    specifiedCurrency?: SpecifiedCurrency,
    /** An obligation and deliverable obligation characteristic. Any obligation that is not primarily (majority) owed to a Sovereign or Supranational Organisation. ISDA 2003 Term: Not Sovereign Lender. */
    notSovereignLender?: boolean,
    /** An obligation and deliverable obligation characteristic. Any obligation that is payable in any currency other than the domestic currency. Domestic currency is either the currency so specified or, if no currency is specified, the currency of (a) the reference entity, if the reference entity is a sovereign, or (b) the jurisdiction in which the relevant reference entity is organised, if the reference entity is not a sovereign. ISDA 2003 Term: Not Domestic Currency. */
    notDomesticCurrency?: NotDomesticCurrency,
    /** An obligation and deliverable obligation characteristic. If the reference entity is a Sovereign, this means any obligation that is not subject to the laws of the reference entity. If the reference entity is not a sovereign, this means any obligation that is not subject to the laws of the jurisdiction of the reference entity. ISDA 2003 Term: Not Domestic Law. */
    notDomesticLaw?: boolean,
    /** An obligation and deliverable obligation characteristic. Indicates whether or not the obligation is quoted, listed or ordinarily purchased and sold on an exchange. ISDA 2003 Term: Listed. */
    listed?: boolean,
    /** A deliverable obligation characteristic. In essence Not Contingent means the repayment of principal cannot be dependant on a formula/index, i.e. to prevent the risk of being delivered an instrument that may never pay any element of principal, and to ensure that the obligation is interest bearing (on a regular schedule). ISDA 2003 Term: Not Contingent. */
    notContingent?: boolean,
    /** An obligation and deliverable obligation characteristic. Any obligation other than an obligation that was intended to be offered for sale primarily in the domestic market of the relevant Reference Entity. This specifies that the obligation must be an internationally recognised bond. ISDA 2003 Term: Not Domestic Issuance. */
    notDomesticIssuance?: boolean,
    /** A deliverable obligation characteristic. A loan that is freely assignable to a bank or financial institution without the consent of the Reference Entity or the guarantor, if any, of the loan (or the consent of the applicable borrower if a Reference Entity is guaranteeing the loan) or any agent. ISDA 2003 Term: Assignable Loan. */
    assignableLoan?: PCDeliverableObligationCharac,
    /** A deliverable obligation characteristic. A loan that is capable of being assigned with the consent of the Reference Entity or the guarantor, if any, of the loan or any agent. ISDA 2003 Term: Consent Required Loan. */
    consentRequiredLoan?: PCDeliverableObligationCharac,
    /** A deliverable obligation characteristic. A loan with a participation agreement whereby the buyer is capable of creating, or procuring the creation of, a contractual right in favour of the seller that provides the seller with recourse to the participation seller for a specified share in any payments due under the relevant loan which are received by the participation seller. ISDA 2003 Term: Direct Loan Participation. */
    directLoanParticipation?: LoanParticipation,
    /** A deliverable obligation characteristic. An obligation that is transferable to institutional investors without any contractual, statutory or regulatory restrictions. ISDA 2003 Term: Transferable. */
    transferable?: boolean,
    /** A deliverable obligation characteristic. An obligation that has a remaining maturity from the Physical Settlement Date of not greater than the period specified. ISDA 2003 Term: Maximum Maturity. */
    maximumMaturity?: Period,
    /** A deliverable obligation characteristic. An obligation at time of default is due to mature and due to be repaid, or as a result of downgrade/bankruptcy is due to be repaid as a result of an acceleration clause. ISDA 2003 Term: Accelerated or Matured. */
    acceleratedOrMatured?: boolean,
    /** A deliverable obligation characteristic. Any obligation that is not a bearer instrument. This applies to Bonds only and is meant to avoid tax, fraud and security/delivery provisions that can potentially be associated with Bearer Bonds. ISDA 2003 Term: Not Bearer. */
    notBearer?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published additional provisions for U.S. Municipal as Reference Entity. ISDA 2003 Term: Full Faith and Credit Obligation Liability. */
    fullFaithAndCreditObLiability?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published additional provisions for U.S. Municipal as Reference Entity. ISDA 2003 Term: General Fund Obligation Liability. */
    generalFundObligationLiability?: boolean,
    /** An obligation and deliverable obligation characteristic. Defined in the ISDA published additional provisions for U.S. Municipal as Reference Entity. ISDA 2003 Term: Revenue Obligation Liability. */
    revenueObligationLiability?: boolean,
    /** ISDA 1999 Term: Indirect Loan Participation. NOTE: Only applicable as a deliverable obligation under ISDA Credit 1999. */
    indirectLoanParticipation?: LoanParticipation,
    /** A free format string to specify any excluded obligations or deliverable obligations, as the case may be, of the reference entity or excluded types of obligations or deliverable obligations. ISDA 2003 Term: Excluded Obligations/Excluded Deliverable Obligations. */
    excluded?: string,
    /** This element is used to specify any other obligations of a reference entity in both obligations and deliverable obligations. The obligations can be specified free-form. ISDA 2003 Term: Other Obligations of a Reference Entity. */
    othReferenceEntityObligations?: string,
}


/**
 * A class to specify loan with a participation agreement whereby the buyer is capable of creating, or procuring the creation of, a contractual right in favour of the seller that provides the seller with recourse to the participation seller for a specified share in any payments due under the relevant loan which are received by the participation seller. ISDA 2003 Term: Direct Loan Participation.
 */
export interface LoanParticipation extends PCDeliverableObligationCharac {
    /** If Direct Loan Participation is specified as a deliverable obligation characteristic, this specifies any requirements for the Qualifying Participation Seller. The requirements may be listed free-form. ISDA 2003 Term: Qualifying Participation Seller. */
    qualifyingParticipationSeller?: string,
}


/**
 * A class to specify the Partial Cash Deliverable Obligation Characteristic.
 */
export interface PCDeliverableObligationCharac {
    /** Indicates whether the provision is applicable. */
    applicable: boolean,
    /** Specifies whether either 'Partial Cash Settlement of Assignable Loans', 'Partial Cash Settlement of Consent Required Loans' or 'Partial Cash Settlement of Participations' is applicable. If this element is specified and Assignable Loan is a Deliverable Obligation Characteristic, any Assignable Loan that is deliverable, but where a non-receipt of Consent by the Physical Settlement Date has occurred, the Loan can be cash settled rather than physically delivered. If this element is specified and Consent Required Loan is a Deliverable Obligation Characteristic, any Consent Required Loan that is deliverable, but where a non-receipt of Consent by the Physical Settlement Date has occurred, the Loan can be cash settled rather than physically delivered. If this element is specified and Direct Loan Participation is a Deliverable Obligation Characteristic, any Participation that is deliverable, but where this participation has not been effected (has not come into effect) by the Physical Settlement Date, the participation can be cash settled rather than physically delivered. */
    partialCashSettlement?: boolean,
}



export interface PhysicalSettlementPeriod {
    /** An explicit indication that a number of business days are not specified and therefore ISDA fallback provisions should apply. */
    businessDaysNotSpecified?: boolean,
    /** A number of business days. Its precise meaning is dependant on the context in which this element is used. ISDA 2003 Term: Business Day. */
    businessDays?: number,
    /** A maximum number of business days. Its precise meaning is dependant on the context in which this element is used. Intended to be used to limit a particular ISDA fallback provision. */
    maximumBusinessDays?: number,
}


/**
 * Specifies Physical Settlement Terms characteristics for the settlement of a Credit Default Swap or Option.
 */
export interface PhysicalSettlementTerms {
    /** Specifies whether the swap resulting from physical settlement of the swaption transaction will clear through a clearing house. The meaning of Cleared Physical Settlement is defined in the 2006 ISDA Definitions, Section 15.2 (published in Supplement number 28). */
    clearedPhysicalSettlement?: boolean,
    /** Specifies the clearing organization (CCP, DCO) to which the trade should be cleared. */
    predeterminedClearingOrganizationParty?: string,
    /** The number of business days used in the determination of the physical settlement date. The physical settlement date is this number of business days after all applicable conditions to settlement are satisfied. If a number of business days is not specified fallback provisions apply for determining the number of business days. If Section 8.5/8.6 of the 1999/2003 ISDA Definitions are to apply the businessDaysNotSpecified element should be included. If a specified number of business days are to apply these should be specified in the businessDays element. If Section 8.5/8.6 of the 1999/2003 ISDA Definitions are to apply but capped at a maximum number of business days then the maximum number should be specified in the maximumBusinessDays element. ISDA 2003 Term: Physical Settlement Period. */
    physicalSettlementPeriod?: PhysicalSettlementPeriod,
    /** This element contains all the ISDA terms relevant to defining the deliverable obligations. */
    deliverableObligations?: DeliverableObligations,
    /** If this element is specified and set to 'true', indicates that physical settlement must take place through the use of an escrow agent. (For Canadian counterparties this is always 'Not Applicable'. ISDA 2003 Term: Escrow. */
    escrow?: boolean,
    /** If this element is specified and set to 'true', for a transaction documented under the 2003 ISDA Credit Derivatives Definitions, has the effect of incorporating the language set forth below into the confirmation. The section references are to the 2003 ISDA Credit Derivatives Definitions. Notwithstanding Section 1.7 or any provisions of Sections 9.9 or 9.10 to the contrary, but without prejudice to Section 9.3 and (where applicable) Sections 9.4, 9.5 and 9.6, if the Termination Date has not occurred on or prior to the date that is 60 Business Days following the Physical Settlement Date, such 60th Business Day shall be deemed to be the Termination Date with respect to this Transaction except in relation to any portion of the Transaction (an 'Affected Portion') in respect of which: (1) a valid notice of Buy-in Price has been delivered that is effective fewer than three Business Days prior to such 60th Business Day, in which case the Termination Date for that Affected Portion shall be the third Business Day following the date on which such notice is effective; or (2) Buyer has purchased but not Delivered Deliverable Obligations validly specified by Seller pursuant to Section 9.10(b), in which case the Termination Date for that Affected Portion shall be the tenth Business Day following the date on which Seller validly specified such Deliverable Obligations to Buyer. */
    sixtyBusinessDaySettlementCap?: boolean,
}


/**
 * A class defining a content model for a calculation rule defined as percentage of the notional amount.
 */
export interface PercentageRule {
    /** A percentage of the notional amount. */
    paymentPercent: string,
    /** A reference to the notional amount. */
    notionalAmountReference: Money,
}


/**
 * A data defining the settlement date(s) for cash or physical settlement as either a set of explicit dates, together with applicable adjustments, or as a date relative to some other (anchor) date, or as any date in a range of contiguous business days. This data type provides a level of abstraction on top of the different legacy methods used to specify a settlement / payment date, which vary across product types, asset classes and delivery types.
 */
export interface SettlementDate {
    /** A single settlement date subject to adjustment or specified as relative to another date (e.g. the trade date). This attribute was formerly part of 'SettlementTerms', which is now being harmonised to include a common 'SettlementDate', as inherited from 'SettlementBase'. */
    adjustableOrRelativeDate?: AdjustableOrAdjustedOrRelativeDate,
    /** The settlement date for a forward settling product. For Foreign Exchange contracts, this represents a common settlement date between both currency legs. To specify different settlement dates for each currency leg, see the ForeignExchange class. This attribute was formerly part of 'SettlementTerms', which is now being harmonised to include a common 'SettlementDate', as inherited from 'SettlementBase'. */
    valueDate?: date,
    /** A series of dates that shall be subject to adjustment if they would otherwise fall on a day that is not a business day in the specified business centers, together with the convention for adjusting the date. This attributes was formerly part of 'CashSettlementPaymentDate' as included into 'OptionCashSettlement' (which is now merged into a unique 'CashSettlementTerms' data type. */
    adjustableDates?: AdjustableDates,
    /** A range of contiguous business days. This attribute is meant to be merged with the 'settlementDate' at some future point once we refactor 'Date' to use a single complex type across the model. This attributes was formerly part of 'CashSettlementPaymentDate', as included into 'OptionCashSettlement' (which is now merged into a unique 'CashSettlementTerms' data type. */
    businessDateRange?: BusinessDateRange,
    /** The number of business days used in the determination of the cash settlement payment date. If a cash settlement amount is specified, the cash settlement payment date will be this number of business days following the calculation of the final price. If a cash settlement amount is not specified, the cash settlement payment date will be this number of business days after all conditions to settlement are satisfied. ISDA 2003 Term: Cash Settlement Date. This attribute was formerly part of 'CashSettlementTerms' as used for credit event settlement, which now includes a common 'SettlementDate' attribute. */
    cashSettlementBusinessDays?: number,
    /** Applicable to CDS on MBS to specify whether payment delays are applicable to the fixed Amount. RMBS typically have a payment delay of 5 days between the coupon date of the reference obligation and the payment date of the synthetic swap. CMBS do not, on the other hand, with both payment dates being on the 25th of each month. */
    paymentDelay?: boolean,
}


/**
 * Extends the Offset structure to specify an FX fixing date as an offset to dates specified somewhere else in the document.
 */
export interface FxFixingDate extends Offset {
    /** The convention for adjusting a date if it would otherwise fall on a day that is not a business day, as specified by an ISDA convention (e.g. Following, Precedent). */
    businessDayConvention?: string,
    /** The business center(s), specified either explicitly or by reference to those specified somewhere else in the instance document. */
    businessCenters?: BusinessCenters,
    /** A reference to a set of financial business centers defined elsewhere in the document. This set of business centers is used to determine whether a particular day is a business day or not. */
    businessCentersReference?: BusinessCenters,
    /** The payment date references on which settlements in non-deliverable currency are due and will then have to be converted according to the terms specified through the other parts of the nonDeliverableSettlement structure. */
    dateRelativeToPaymentDates?: DateRelativeToPaymentDates,
    /** The calculation period references on which settlements in non-deliverable currency are due and will then have to be converted according to the terms specified through the other parts of the nonDeliverableSettlement structure. Implemented for Brazilian-CDI swaps where it will refer to the termination date of the appropriate leg. */
    dateRelativeToCalculationPeriodDates?: DateRelativeToCalculationPeriodDates,
    /** The calculation period references on which settlements in non-deliverable currency are due and will then have to be converted according to the terms specified through the other parts of the nonDeliverableSettlement structure. Implemented for Brazilian-CDI swaps where it will refer to the termination date of the appropriate leg. */
    dateRelativeToValuationDates?: DateRelativeToValuationDates,
    /** Describes the specific date when a non-deliverable forward or cash-settled option will 'fix' against a particular rate, which will be used to compute the ultimate cash settlement. This element should be omitted where a single, discrete fixing date cannot be identified e.g. on an american option, where fixing may occur at any date on a continuous range.  This attribute was formerly part of 'fxSettlementTerms', which is now being harmonised into a common 'CashSettlementTerms' that includes a 'ValuationDate'. */
    fxFixingDate?: AdjustableOrRelativeDate,
}


/**
 * Specifies the settlement terms, which can either be cash, physical, or fx-based cash-settlement. This class can be used for the settlement of options and forwards, cash transactions (e.g. securities or foreign exchange), or in case of credit event.
 */
export interface SettlementTerms extends SettlementBase {
    /** Specifies the parameters associated with the cash settlement procedure. */
    cashSettlementTerms?: CashSettlementTerms[],
    /** Specifies the physical settlement terms which apply to the transaction. */
    physicalSettlementTerms?: PhysicalSettlementTerms,
}


/**
 * A predefined price accorded by the counterparties.
 */
export interface FixedPrice {
    /** Fixed price step schedule, including an initial price specified as an absolute number. */
    price?: PriceSchedule,
}



export interface PaymentDetail {
    /** The feature payment date. */
    paymentDate?: AdjustableOrRelativeDate,
    /** The calculation rule. */
    paymentRule: PaymentRule,
    /** A fixed payment amount. */
    paymentAmount?: Money,
}


/**
 * This class corresponds to the FpML PaymentDiscounting.model group for representing the discounting elements that can be associated with a payment.
 */
export interface PaymentDiscounting {
    /** The value representing the discount factor used to calculate the present value of the cash flow. */
    discountFactor?: string,
    /** The amount representing the present value of the forecast payment. */
    presentValueAmount?: Money,
}


/**
 * A class defining the payment calculation rule. As of FpML 5.10, percentage rule is the only calculation rule that has been specified as part of the standard.
 */
export interface PaymentRule {
    /** This attribute is not present as part of the FpML construct, as the payment rule is specialised by means of runtime type extension through the xsi:type. */
    percentageRule?: PercentageRule,
}


/**
 * A class defining which principal exchanges occur for the stream.
 */
export interface PrincipalPayments {
    /** A true/false flag to indicate whether there is an initial exchange of principal on the effective date. */
    initialPayment: boolean,
    /** A true/false flag to indicate whether there is a final exchange of principal on the termination date. */
    finalPayment: boolean,
    /** A true/false flag to indicate whether there are intermediate or interim exchanges of principal during the term of the swap. */
    intermediatePayment: boolean,
    /** Indicate the Payout legs which nominal amount may vary in regards of FX Fixing dates as determined in the product terms. */
    varyingLegNotionalCurrency?: string[],
    /** Describe dates schedules for Principal Exchanges and related role of the parties when known. */
    principalPaymentSchedule?: PrincipalPaymentSchedule,
}


/**
 * Describe dates schedules for Principal Exchanges and related role of the parties when known.
 */
export interface PrincipalPaymentSchedule {
    /** Principal Payment made at Trade inception. */
    initialPrincipalPayment?: PrincipalPayment,
    /** Principal Payment as part of the Trade lifecycle e.g. as part of notional reset adjustements in a Cross Currency Swap with a varying notional leg. */
    intermediatePrincipalPayment?: AdjustableRelativeOrPeriodicDates,
    /** Principal Payment at Trade maturity */
    finalPrincipalPayment?: PrincipalPayment,
}


/**
 * Any kind of principal payments when the amount is known and thus fixed.
 */
export interface PrincipalPayment {
    /** The date where the PrincipalPayment shall be settled. */
    principalPaymentDate?: AdjustableDate,
    /** Specifies the parties responsible for making and receiving payments defined by this structure. */
    payerReceiver?: PayerReceiver,
    /** When known at the time the transaction is made, the cash amount to be paid. */
    principalAmount?: Money,
    /** The value representing the discount factor used to calculate the present value of the principal payment amount. */
    discountFactor?: string,
    /** The amount representing the present value of the principal payment. */
    presentValuePrincipalAmount?: Money,
}


/**
 * A base class to be extended by the SettlementTerms class.
 */
export interface SettlementBase {
    /** Whether the settlement will be cash, physical, by election, ... */
    settlementType: string,
    /** The qualification as to how the transfer will settle, e.g. a DvP settlement. */
    transferSettlementType?: string,
    /** The settlement currency is to be specified when the Settlement Amount cannot be known in advance. The list of valid currencies is not presently positioned as an enumeration as part of the CDM because that scope is limited to the values specified by ISDA and FpML. As a result, implementers have to make reference to the relevant standard, such as the ISO 4217 standard for currency codes. */
    settlementCurrency?: string,
    /** The date on which the settlement amount will be paid, subject to adjustment in accordance with any applicable business day convention. This component would not be present for a mandatory early termination provision where the cash settlement payment date is the mandatory early termination date. */
    settlementDate?: SettlementDate,
    /** Optional settlement centre as an enumerated list: Euroclear, Clearstream. */
    settlementCentre?: string,
    /** Optionally defines the parameters that regulate a settlement. */
    settlementProvision?: SettlementProvision,
    /** Settlement Style. */
    standardSettlementStyle?: string,
}


/**
 * Defines parameters that regulate a settlement, for instance whether this settlement should be netted with other ones or broken-down into smaller amounts.
 */
export interface SettlementProvision {
    /** Defines the parameters that are necessary to 'shape' a settlement, i.e. break it down into smaller amounts. */
    shapingProvisions?: ShapingProvision,
}


/**
 * Defines the applicable settlement limits that may require a settlement to be 'shaped', i.e. broken-down into smaller amounts.
 */
export interface ShapingProvision {
    /** Defines applicable settlement limits in each currency. */
    shapeSchedule: Money[],
}


/**
 * A TransferableProduct is a type of financial product which can be held or transferred, represented as an Asset with the addition of specific EconomicTerms.
 */
export interface TransferableProduct extends Asset {
    /** The price forming features, including payouts and provisions. */
    economicTerms: EconomicTerms,
}


/**
 *  This class represents the full set of price-forming features associated with a contractual product: the payout component, the notional/quantity, the effective and termination date and the date adjustment provisions when applying uniformily across the payout components. This class also includes the legal provisions which have valuation implications: cancelable provision, extendible provision, early termination provision and extraordinary events specification.
 */
export interface EconomicTerms {
    /** The first day of the terms of the trade. This day may be subject to adjustment in accordance with a business day convention. */
    effectiveDate?: AdjustableOrRelativeDate,
    /** The time and zone of the first day of the terms of the trade. It can also be specified in relation to another time. */
    effectiveTime?: DirectOrRelativeTime,
    /** The last day of the terms of the trade. This date may be subject to adjustments in accordance with the business day convention. It can also be specified in relation to another scheduled date (e.g. the last payment date). */
    terminationDate?: AdjustableOrRelativeDate,
    /** The time and zone of the last day of the terms of the trade. It can also be specified in relation to another time. */
    terminationTime?: DirectOrRelativeTime,
    /** The business day adjustment convention when it applies across all the payout components. This specification of the business day convention and financial business centers is used for adjusting any calculation period date if it would otherwise fall on a day that is not a business day in the specified business center. */
    dateAdjustments?: BusinessDayAdjustments,
    /** The payout specifies the future cashflow computation methodology which characterizes a financial product. */
    payout: Payout[],
    /** Contains optional provisions pertaining to the termination characteristics of a contract. */
    terminationProvision?: TerminationProvision,
    /** The ISDA calculation agent responsible for performing duties as defined in the applicable product definitions. */
    calculationAgent?: CalculationAgent,
    /** Specifies, when boolean value is True, that additional economic terms exist that have not been included in the product representation. */
    nonStandardisedTerms?: boolean,
    /** Represents the collateral obligations of a party. */
    collateral?: Collateral,
}


/**
 *  The option payout specification terms. The associated globalKey denotes the ability to associate a hash value to the respective OptionPayout instantiation for the purpose of model cross-referencing, in support of functionality such as the event effect and the lineage.
 */
export interface OptionPayout extends PayoutBase {
    buyerSeller: BuyerSeller,
    /** The option feature, such as quanto, Asian, barrier, knock. */
    feature?: OptionFeature,
    /** Class containing terms that are associated with observing a price/benchmark/index across either single or multple observations. To be used for option contracts that reference a benchmark price. */
    observationTerms?: ObservationTerms,
    /** Allows the full representation of a payout by defining a set of schedule periods. It supports standard schedule customization by expressing all the dates, quantities, and pricing data in a non-parametric way. */
    schedule?: CalculationSchedule,
    /** Contains the information relative to the delivery of the asset. */
    delivery?: AssetDeliveryInformation,
    /** The financial product underlying the option, which can be of any type including an Asset, Basket, Index or a NonTransferableProduct. */
    underlier: Underlier,
    /** The type of option transaction. From a usage standpoint, put/call is the default option type, while payer/receiver indicator is used for options on index credit default swaps, consistently with the industry practice. Straddle is used for the case of straddle strategy, that combine a call and a put with the same strike. */
    optionType?: string,
    /** The terms for exercising the option, which include the option style (e.g. American style option), the exercise procedure (e.g. manual exercise) and the settlement terms (e.g. physical vs. cash). */
    exerciseTerms: ExerciseTerms,
    /** Specifies the strike of the option */
    strike?: OptionStrike,
}


/**
 * Specifies the type of return of a performance payout.
 */
export interface ReturnTerms {
    /** Return terms based upon the underlier's observed price. */
    priceReturnTerms?: PriceReturnTerms,
    /** Return terms based upon dividend payments associated to the underlier. */
    dividendReturnTerms?: DividendReturnTerms,
    /** Return terms based upon the observed variance of the underlier's price. */
    varianceReturnTerms?: VarianceReturnTerms,
    /** Return terms based upon the observed volatility of the underlier's price. */
    volatilityReturnTerms?: VolatilityReturnTerms,
    /** Return terms based upon the observed correlation between the components of the underlying basket. */
    correlationReturnTerms?: CorrelationReturnTerms,
}


/**
 * Contains the necessary specifications for all performance payouts, encompassing equity return, dividend, variance, volatility and correlation products.
 */
export interface PerformancePayout extends PayoutBase {
    /** Defines how and when a performance type option or performance type swap is to be observed. */
    observationTerms?: ObservationTerms,
    /** Defines how and when a performance type option or performance type swap is to be valued, including both interim and final valuation. */
    valuationDates: ValuationDates,
    /** Defines the payment date schedule, as defined by the parameters that are needed to specify it, either in a parametric way or by reference to another schedule of dates (e.g. the valuation dates). */
    paymentDates: PaymentDates,
    /** Identifies the underlying product that is referenced for pricing of the applicable leg in a swap.  Referenced in the '2018 ISDA CDM Equity Confirmation for Security Equity Swap' as Security. */
    underlier?: Underlier,
    /** Defines quanto or composite FX features that are included in the swap leg. */
    fxFeature?: FxFeature[],
    /** Specifies the type of return of a performance payout. */
    returnTerms?: ReturnTerms,
    /** Specifies an individual type of return of a Performance Payout, when such individual return is part of an aggregation of multiple similar returns, at Performance Payout level */
    portfolioReturnTerms?: PortfolioReturnTerms[],
    /** Specifies the net initial valuation price(s) of the underlier at Performance Payout level. This price can be expressed either as an actual amount/currency, as a determination method, or by reference to another value specified in the swap document. */
    initialValuationPrice?: PriceSchedule[],
    /** Specifies the net initial valuation price(s) of the underlier at Performance Payout level. This price can be expressed either as an actual amount/currency, as a determination method, or by reference to another value specified in the swap document. */
    interimValuationPrice?: PriceSchedule[],
    /** Specifies the net final valuation price(s) of the underlier at Performance Payout level. This price can be expressed either as an actual amount/currency, as a determination method, or by reference to another value specified in the swap document. */
    finalValuationPrice?: PriceSchedule[],
}


/**
 * Specifies an individual type of return of a Performance Payout, when such individual return is part of an aggregation of multiple similar returns, at Performance Payout level.
 */
export interface PortfolioReturnTerms extends ReturnTerms {
    /** Canonical representation of the payer and receiver parties applicable to each individual return leg. */
    payerReceiver: PayerReceiver,
    /** Defines the product that is the subject of a tradable product definition, an underlying product definition, a physical exercise, a position, or other purposes. */
    underlier: Observable,
    /** Specifies a quantity schedule for the underlier, which applies to each individual return leg. */
    quantity?: NonNegativeQuantitySchedule,
    /** Specifies the initial valuation price(s) of the underlier. This price can be expressed either as an actual amount/currency, as a determination method, or by reference to another value specified in the swap document. */
    initialValuationPrice?: PriceSchedule[],
    /** Specifies the initial valuation price(s) of the underlier. This price can be expressed either as an actual amount/currency, as a determination method, or by reference to another value specified in the swap document. */
    interimValuationPrice?: PriceSchedule[],
    /** 2018 ISDA CDM Equity Confirmation for Security Equity Swap: Final Price | Specifies the final valuation price of the underlier. This price can be expressed either as an actual amount/currency, as a determination method, or by reference to another value specified in the swap document. */
    finalValuationPrice?: PriceSchedule[],
}


/**
 * A data type to specify the financial product's economic terms, alongside the product identification and product taxonomy. The non-transferable product data type represents a product that can be traded (as part of a TradableProduct) but cannot be transferred to others.  It is meant to be used across the pre-execution, execution and (as part of the Contract) post-execution lifecycle contexts.
 */
export interface NonTransferableProduct {
    /** Comprises a identifier and a source to uniquely identify the nonTransferableProduct.  */
    identifier?: ProductIdentifier[],
    /** Specifies the product taxonomy, which is composed of a taxonomy value and a taxonomy source. */
    taxonomy?: ProductTaxonomy[],
    /** The price forming features, including payouts and provisions. */
    economicTerms: EconomicTerms,
}


/**
 * Specifies the price and quantity of a trade lot, where the same product could be traded multiple times with the same counterparty but in different lots (at a different date, in a different quantity and at a different price). One trade lot combined with a product definition specifies the entire economics of a trade. The lifecycle mechanics of each such trade lot (e.g. cashflow payments) is independent of the other lots.
 */
export interface TradeLot {
    /** Specifies one or more identifiers for the lot, if any. */
    lotIdentifier?: Identifier[],
    /** Specifies the settlement characteristics of a trade lot: price, quantity, observable (optionally) and the settlement terms. This attribute has a multiple cardinality to allow to specify the price, quantity and observable of different legs in a single, composite product (e.g. a Swap). */
    priceQuantity: PriceQuantity[],
}


/**
 * Definition of a product as ready to be traded, i.e. included in an execution or contract, by associating a specific price and quantity to this product plus an (optional) mechanism for any potential future quantity adjustment.
 */
export interface TradableProduct {
    /** The underlying product to be included in a contract or execution. */
    product: NonTransferableProduct,
    /** Specifies the price, quantity and effective date of each trade lot, when the same product may be traded multiple times in different lots with the same counterparty. In a trade increase, a new trade lot is added to the list, with the corresponding effective date. In a trade decrease, the existing trade lot(s) are decreased of the corresponding quantity (and an unwind fee may have to be settled). The multiple cardinality and the ability to increase existing trades is used for Equity Swaps in particular. */
    tradeLot: TradeLot[],
    /** Specifies the parties which are the two counterparties to the transaction.  The product is agnostic to the actual parties to the transaction, with the party references abstracted away from the product definition and replaced by the counterparty enum (e.g. CounterpartyEnum values Party1 or Party2). The counterparty enum can then be positioned in the product (e.g. to specify which counterparty is the payer, receiver etc) and this counterparties attribute, which is positioned outside of the product definition, allows the counterparty enum to be associated with an actual party reference. */
    counterparty: Counterparty[],
    /** Specifies the parties with ancillary roles in the transaction. The product is agnostic to the actual parties involved in the transaction, with the party references abstracted away from the product definition and replaced by the AncillaryRoleEnum. The AncillaryRoleEnum can then be positioned in the product and this AncillaryParty type, which is positioned outside of the product definition, allows the AncillaryRoleEnum to be associated with an actual party reference. */
    ancillaryParty?: AncillaryParty[],
    /** Specifies the conditions that govern the adjustment to the quantity of a product being traded: e.g. execution, portfolio rebalancing etc. It is typically used in the context of Equity Swaps. */
    adjustment?: string,
}


/**
 * A class describing the weight of each of the underlier constituent within the basket, either in absolute or relative terms.
 */
export interface ConstituentWeight {
    /** The number of units (index or securities) that constitute the underlier of the swap. In the case of a basket swap, this element is used to reference both the number of basket units, and the number of each asset components of the basket when these are expressed in absolute terms. */
    openUnits?: string,
    /** The relative weight of each respective basket constituent, expressed in percentage. A basket percentage of 5% would be represented as 0.05. */
    basketPercentage?: string,
}


/**
 * Represents a forward settling payout. The underlier attribute captures the underlying payout, which is settled according to the settlementTerms attribute (which is part of PayoutBase). Both FX Spot and FX Forward should use this component.
 */
export interface SettlementPayout extends PayoutBase {
    /** The underlying financial product that will be physically or cash settled, which can be of any type, eg an asset such as cash or a security, or the cash settlement of an index rate. */
    underlier: Underlier,
    /** Also called contract month or delivery month. However, it's not always a month. It is usually expressed using a code, e.g. Z23 would be the Dec 2023 contract, (Z = December). For crude oil, the corresponding contract might be called CLZ23. */
    deliveryTerm?: string,
    /** Contains the information relative to the delivery of the asset. */
    delivery?: AssetDeliveryInformation,
    /** Allows the full representation of a payout by defining a set of schedule periods. It supports standard schedule customization by expressing all the dates, quantities, and pricing data in a non-parametric way. */
    schedule?: CalculationSchedule,
}


/**
 * A class that allows the full representation of a payout by defining a set of schedule periods. It supports standard schedule customization by expressing all the dates, quantities, and pricing data in a non-parametric way.
 */
export interface CalculationSchedule {
    /** Defines a period of a calculation schedule structure. */
    schedulePeriod: SchedulePeriod[],
}


/**
 * A class that defines the period of a schedule. The period contains a set of start and end dates, quantities, fixing, and pricing data.
 */
export interface SchedulePeriod {
    /** Period for which the payment is generated. */
    calculationPeriod: DateRange,
    /** Adjusted payment date. */
    paymentDate: date,
    /** Period over which the underlying price is observed. */
    fixingPeriod: DateRange,
    /** Period and time profile over which the delivery takes place. */
    deliveryPeriod?: CalculationScheduleDeliveryPeriods,
}


/**
 * Represents a fixed price payout. There is no underlier associated with this payout type and is based on fixed pricing per a given unit (e.g. in commodities price per barrel)
 */
export interface FixedPricePayout extends PayoutBase {
    /** Specifies the parameters to generate the payment date schedule, either through a parametric representation or by reference to specified dates. */
    paymentDates: PaymentDates,
    /** Specifies the fixed price on which fixed forward payments are based. */
    fixedPrice: FixedPrice,
    /** Allows the full representation of a payout by defining a set of schedule periods. It supports standard schedule customization by expressing all the dates, quantities, and pricing data in a non-parametric way. */
    schedule?: CalculationSchedule,
}


/**
 * A data defining:  the right of a party to cancel a swap transaction on the specified exercise dates. The provision is for 'walk-away' cancellation (i.e. the fair value of the swap is not paid). A fee payable on exercise can be specified. As a difference from the FpML construct, the canonical model extends the BuyerSeller class.
 */
export interface CancelableProvision extends BuyerSeller {
    /** Definition of the party to whom notice of exercise should be given. */
    exerciseNotice?: ExerciseNotice,
    /** A flag to indicate whether follow-up confirmation of exercise (written or electronic) is required following telephonic notice by the buyer to the seller or seller's agent. */
    followUpConfirmation: boolean,
    /** The adjusted dates associated with a cancelable provision. These dates have been adjusted for any applicable business day convention. */
    cancelableProvisionAdjustedDates?: CancelableProvisionAdjustedDates,
    /** Business date convention adjustment to final payment period per leg (swapStream) upon exercise event. The adjustments can be made in-line with leg level BDC's or they can be specified separately. */
    finalCalculationPeriodDateAdjustment?: FinalCalculationPeriodDateAdjustment[],
    /** An initial fee for the cancelable option. */
    initialFee?: Transfer,
    /** The party with right to exercise a cancellation. Allows for buyer, seller or either. */
    callingParty?: string,
    /** The first day when cancelation is permitted to take effect. A party may give notice prior to this date and taken together with the effective period would be necessary to cancel on this date. */
    earliestDate?: AdjustableOrRelativeDate,
    /** The last day within the term of the contract that cancelation is allowed. */
    expirationDate?: AdjustableOrRelativeDate,
    /** The effective date if cancelation is invoked otherwise the cancellation period defines the cancellation date. */
    effectiveDate?: AdjustableOrRelativeDates,
    /** Effective period for cancelation when notice is given. This is the period after notice is given that cancellation becomes effecticve. */
    effectivePeriod?: Period,
    /** The earliest time in a business day that notice of cancelation can be given. */
    earliestCancellationTime?: BusinessCenterTime,
    /** The latest time at which notice of cancelation can be given. */
    latestCancelationTime?: BusinessCenterTime,
    /** The exercise terms associated with the cancelable provision, including details such as exercise style, exercise fees, and any other relevant conditions or terms governing the cancellation of the swap transaction. */
    exerciseTerms: ExerciseTerms,
}


/**
 * A data to:  define the adjusted dates for a cancelable provision on a swap transaction.
 */
export interface CancelableProvisionAdjustedDates {
    /** The adjusted dates for an individual cancellation date. */
    cancellationEvent: CancellationEvent[],
}


/**
 * The adjusted dates for a specific cancellation date, including the adjusted exercise date and adjusted termination date.
 */
export interface CancellationEvent {
    /** The date on which option exercise takes place. This date should already be adjusted for any applicable business day convention. */
    adjustedExerciseDate: date,
    /** The early termination date that is applicable if an early termination provision is exercised. This date should already be adjusted for any applicable business day convention. */
    adjustedEarlyTerminationDate: date,
}


/**
 * A data to:  define the adjusted dates associated with an early termination provision.
 */
export interface EarlyTerminationEvent {
    /** The date on which option exercise takes place. This date should already be adjusted for any applicable business day convention. */
    adjustedExerciseDate: date,
    /** The early termination date that is applicable if an early termination provision is exercised. This date should already be adjusted for any applicable business day convention. */
    adjustedEarlyTerminationDate: date,
    /** The date by which the cash settlement amount must be agreed. This date should already be adjusted for any applicable business day convention. */
    adjustedCashSettlementValuationDate: date,
    /** The date on which the cash settlement amount is paid. This date should already be adjusted for any applicable business date convention. */
    adjustedCashSettlementPaymentDate: date,
    /** The date on which the exercise fee amount is paid. This date should already be adjusted for any applicable business day convention. */
    adjustedExerciseFeePaymentDate?: date,
}


/**
 * A data defining:  an early termination provision for a swap. This early termination is at fair value, i.e. on termination the fair value of the product must be settled between the parties.
 */
export interface EarlyTerminationProvision {
    /** A mandatory early termination provision to terminate the swap at fair value. */
    mandatoryEarlyTermination?: MandatoryEarlyTermination,
    /** Period after trade date of the mandatory early termination date. */
    mandatoryEarlyTerminationDateTenor?: Period,
    /** An option for either or both parties to terminate the swap at fair value. */
    optionalEarlyTermination?: OptionalEarlyTermination,
    /** Definition of the first early termination date and the frequency of the termination dates subsequent to that. American exercise is defined by having a frequency of one day. */
    optionalEarlyTerminationParameters?: ExercisePeriod,
}


/**
 * This defines the time interval to the start of the exercise period, i.e. the earliest exercise date, and the frequency of subsequent exercise dates (if any).
 */
export interface ExercisePeriod {
    /** The time interval to the first (and possibly only) exercise date in the exercise period. */
    earliestExerciseDateTenor: Period,
    /** The frequency of subsequent exercise dates in the exercise period following the earliest exercise date. An interval of 1 day should be used to indicate an American style exercise period. */
    exerciseFrequency?: Period,
}


/**
 * A data defining:  an option to extend an existing swap transaction on the specified exercise dates for a term ending on the specified new termination date. As a difference from FpML, it extends the BuyerSeller class, which represents the BuyerSeller.model.
 */
export interface ExtendibleProvision extends BuyerSeller {
    /** Definition of the party to whom notice of exercise should be given. */
    exerciseNotice?: ExerciseNotice,
    /** A flag to indicate whether follow-up confirmation of exercise (written or electronic) is required following telephonic notice by the buyer to the seller or seller's agent. */
    followUpConfirmation?: boolean,
    /** The adjusted dates associated with an extendible provision. These dates have been adjusted for any applicable business day convention. */
    extendibleProvisionAdjustedDates?: ExtendibleProvisionAdjustedDates,
    /** The party with right to exercise a cancellation. Allows for buyer, seller or either. */
    callingParty?: string,
    /** If the ability to extend the contract is not available to both parties then this component specifies the buyer and seller of the option. */
    singlePartyOption?: PartyRole,
    /** Defines the minimum period before a contract is scheduled to terminate that notice can be given that it will terminate beyond the scheduled termination date. */
    noticeDeadlinePeriod?: RelativeDateOffset,
    /** A specific date and time for the notice deadline */
    noticeDeadlineDateTime?: string,
    /** The length of each extension period relative to the effective date of the preceding contract. */
    extensionTerm?: RelativeDateOffset,
    /** The period within which notice can be given that the contract will be extended. */
    extensionPeriod?: AdjustableRelativeOrPeriodicDates,
    /** The exercise terms associated with the extendible provision, including details such as exercise style, exercise fees, and any other relevant conditions or terms governing the extension of the swap transaction. */
    exerciseTerms: ExerciseTerms,
}


/**
 * A data defining:  the adjusted dates associated with a provision to extend a swap.
 */
export interface ExtendibleProvisionAdjustedDates {
    /** The adjusted dates associated with a single extendible exercise date. */
    extensionEvent: ExtensionEvent[],
}


/**
 * A data to:  define the adjusted dates associated with an individual extension event.
 */
export interface ExtensionEvent {
    /** The date on which option exercise takes place. This date should already be adjusted for any applicable business day convention. */
    adjustedExerciseDate: date,
    /** The termination date if an extendible provision is exercised. This date should already be adjusted for any applicable business day convention. */
    adjustedExtendedTerminationDate: date,
}


/**
 * A data to:  define an early termination provision for which exercise is mandatory.
 */
export interface MandatoryEarlyTermination {
    /** The early termination date associated with a mandatory early termination of a swap. */
    mandatoryEarlyTerminationDate: AdjustableDate,
    /** The ISDA Calculation Agent responsible for performing duties associated with an optional early termination. */
    calculationAgent: CalculationAgent,
    /** If specified, this means that cash settlement is applicable to the transaction and defines the parameters associated with the cash settlement procedure. If not specified, then physical settlement is applicable. */
    cashSettlement: SettlementTerms,
    /** The adjusted dates associated with a mandatory early termination provision. These dates have been adjusted for any applicable business day convention. */
    mandatoryEarlyTerminationAdjustedDates?: MandatoryEarlyTerminationAdjustedDates,
}


/**
 * A data defining:  the adjusted dates associated with a mandatory early termination provision.
 */
export interface MandatoryEarlyTerminationAdjustedDates {
    /** The early termination date that is applicable if an early termination provision is exercised. This date should already be adjusted for any applicable business day convention. */
    adjustedEarlyTerminationDate: date,
    /** The date by which the cash settlement amount must be agreed. This date should already be adjusted for any applicable business day convention. */
    adjustedCashSettlementValuationDate: date,
    /** The date on which the cash settlement amount is paid. This date should already be adjusted for any applicable business date convention. */
    adjustedCashSettlementPaymentDate: date,
}


/**
 * A data defining:  an early termination provision where either or both parties have the right to exercise.
 */
export interface OptionalEarlyTermination {
    /** If optional early termination is not available to both parties then this component specifies the buyer and seller of the option. In FpML, this attribute is of type SinglePsrtyOption, which actually consists of the BuyerSeller.model. */
    singlePartyOption?: BuyerSeller,
    /** Used for specifying whether the Mutual Early Termination Right that is detailed in the Master Confirmation will apply. */
    mutualEarlyTermination?: boolean,
    /** Definition of the party to whom notice of exercise should be given. */
    exerciseNotice?: ExerciseNotice[],
    /** A flag to indicate whether follow-up confirmation of exercise (written or electronic) is required following telephonic notice by the buyer to the seller or seller's agent. */
    followUpConfirmation?: boolean,
    /** The ISDA Calculation Agent responsible for performing duties associated with an optional early termination. */
    calculationAgent?: CalculationAgent,
    /** If specified, this means that cash settlement is applicable to the transaction and defines the parameters associated with the cash settlement procedure. If not specified, then physical settlement is applicable. */
    cashSettlement?: SettlementTerms,
    /** An early termination provision to terminate the trade at fair value where one or both parties have the right to decide on termination. */
    optionalEarlyTerminationAdjustedDates?: OptionalEarlyTerminationAdjustedDates,
    /** The exercise terms associated with the optional early termination, including details such as exercise style, exercise fees, and any other relevant conditions or terms. */
    exerciseTerms: ExerciseTerms,
}


/**
 * A data defining:  the adjusted dates associated with an optional early termination provision.
 */
export interface OptionalEarlyTerminationAdjustedDates {
    /** The adjusted dates associated with an individual early termination date. */
    earlyTerminationEvent: EarlyTerminationEvent[],
}


/**
 * As per ISDA 2002 Definitions.
 */
export interface AveragingFeature {
    /** Defines parameters for use in cases when a valuation or other term is based on an average of market observations. */
    averagingCalculation: AveragingCalculation,
    averagingInOut: string,
    /** The factor of strike. */
    strikeFactor?: string,
    /** The averaging in period. */
    averagingPeriodIn?: AveragingPeriod,
    /** The averaging out period. */
    averagingPeriodOut?: AveragingPeriod,
}


/**
 * Defines parameters for use in cases when a valuation or other term is based on an average of market observations.
 */
export interface AveragingCalculation {
    /** Specifies enumerations for the type of averaging calculation. */
    averagingMethod: AveragingCalculationMethod,
    /** Rounding applied to the average calculation.  */
    precision: Rounding,
}


/**
 * Defines the terms required to calculate the average observations associated with an averaging strike.
 */
export interface AveragingStrikeFeature {
    /** Defines parameters for use in cases when a valuation or other term is based on an average of market observations. */
    averagingCalculation: AveragingCalculation,
    /** Class containing terms that are associated with observing a price/benchmark/index across either single or multple observations.  */
    observationTerms: ObservationTerms,
}


/**
 * As per ISDA 2002 Definitions.
 */
export interface Barrier {
    /** The option only becomes active when a predetermined trigger level is breached. */
    knockIn?: TriggerEvent[],
    /** The option becomes null and void when a predetermined trigger level is breached. */
    knockOut?: TriggerEvent[],
}


/**
 * A type for defining a calendar spread feature.
 */
export interface CalendarSpread {
    expirationDateTwo: AdjustableOrRelativeDate,
}


/**
 * Specifies the conditions to be applied for converting into a reference currency when the actual currency rate is not determined upfront.
 */
export interface Composite {
    /** Specifies the method according to which an amount or a date is determined. */
    determinationMethod?: string,
    /** A date specified as some offset to another date (the anchor date). */
    relativeDate?: RelativeDateOffset,
    /** Specifies the methodology (reference source and, optionally, fixing time) to be used for determining a currency conversion rate. */
    fxSpotRateSource?: FxSpotRateSource,
    /** The time at which the spot currency exchange rate will be observed. It is specified as a time in a business day calendar location, e.g. 11:00am London time. */
    fixingTime?: BusinessCenterTime,
}


/**
 * A type for defining FX Features.
 */
export interface FxFeature {
    /** Specifies the reference currency of the trade. */
    referenceCurrency: string,
    /** If 'Composite' is specified as the Settlement Type in the relevant Transaction Supplement, an amount in the Settlement Currency, determined by the Calculation Agent as being equal to the number of Options exercised or deemed exercised, multiplied by: (Settlement Price – Strike Price) / (Strike Price – Settlement Price) x Multiplier provided that if the above is equal to a negative amount the Option Cash Settlement Amount shall be deemed to be zero. */
    composite?: Composite,
    /** If 'Quanto' is specified as the Settlement Type in the relevant Transaction Supplement, an amount, as determined by the Calculation Agent in accordance with the Section 8.2 of the Equity Definitions. */
    quanto?: Quanto,
    /** If 'Cross-Currency' is specified as the Settlement Type in the relevant Transaction Supplement, an amount in the Settlement Currency, determined by the Calculation Agent as being equal to the number of Options exercised or deemed exercised, multiplied by: (Settlement Price – Strike Price) / (Strike Price – Settlement Price) x Multiplier x one unit of the Reference Currency converted into an amount in the Settlement Currency using the rate of exchange of the Settlement Currency as quoted on the Reference Price Source on the Valuation Date, provided that if the above is equal to a negative amount the Option Cash Settlement Amount shall be deemed to be zero. */
    crossCurrency?: Composite,
}


/**
 * Defines additional optional features that can be included in an option contract.
 */
export interface OptionFeature {
    /** Describes a quanto or composite FX feature. */
    fxFeature?: FxFeature[],
    /** Defines a simple strategy feature. */
    strategyFeature?: StrategyFeature,
    /** Defines an option feature in which an average market observation price is determined on valuation and compared to the strike to determine a settlement amount. */
    averagingFeature?: AveragingFeature,
    /** Specifies a barrier feature. */
    barrier?: Barrier,
    /** Specifies the rules for pass-through payments from the underlier, such as dividends. */
    passThrough?: PassThrough,
}


/**
 * A class for defining option provisions.
 */
export interface TerminationProvision {
    /** A provision that allows the specification of an embedded option within a swap giving the buyer of the option the right to terminate the swap, in whole or in part, on the early termination date. */
    cancelableProvision?: CancelableProvision,
    /** Parameters specifying provisions relating to the optional and mandatory early termination of a swap transaction. */
    earlyTerminationProvision?: EarlyTerminationProvision,
    /** A data defining: the right of a party to exercise an Evergreen option */
    evergreenProvision?: EvergreenProvision,
    /** A provision that allows the specification of an embedded option with a swap giving the buyer of the option the right to extend the swap, in whole or in part, to the extended termination date. */
    extendibleProvision?: ExtendibleProvision,
    /** A provision defining criteria controlling the ability for a lender to recall securities it has lent out. */
    recallProvision?: RecallProvision,
}


/**
 * Defines the additional features that allow securities to be recalled by the lender before the term of the loan. A recall is not the same as an early termination, but may result in the early termination of a loan.
 */
export interface RecallProvision {
    /** Whether the securities on loan can be recalled by the lender. */
    isRecallable: boolean,
    /** The operational deadline on T+0 before which a recall must be requested. For the UK, best practice recommends that this would be 30 minutes before market close i.e. 16:00:00 GMT. */
    issuanceCutoffTime?: TimeZone,
    /** The time on the day of intended settlement by which the borrower must notify the lender that they will be returning the requested security. For the UK, best practice recommends that this would be 30 minutes before the CREST DVP cut-off time i.e. 15:15:00 GMT */
    notificationTime?: TimeZone,
    /** The intended settlement date of the recall, expressed as an offset against a relative date e.g. T+1. The best practice is that the borrower must make their best efforts to settle on T+1. */
    intendedSettlementDate?: RelativeDateOffset,
}


/**
 * Defines the strike price of an option.
 */
export interface OptionStrike {
    /** Defines the strike of an option in the form of a price that could be a cash price, interestRate, or other types. */
    strikePrice?: Price,
    /** Defines the strike of an option in reference to the spread of the underlying swap (typical practice in the case of an option on a credit single name swaps). */
    strikeReference?: FixedRateSpecification,
    /** Defines the strike of an option when expressed by reference to a swap curve (Typically the case for a convertible bond option). */
    referenceSwapCurve?: ReferenceSwapCurve,
    /** Defines an  option strike that is calculated from an average of observed market prices. */
    averagingStrikeFeature?: AveragingStrikeFeature,
}


/**
 * Type which contains pass through payments.
 */
export interface PassThrough {
    /** One to many pass through payment items. */
    passThroughItem: PassThroughItem[],
}


/**
 * Class to represent a single pass through payment.
 */
export interface PassThroughItem {
    /** This attribute doesn't exists in the FpML construct, which makes use of the PayerReceiver.model group. */
    payerReceiver: PayerReceiver,
    /** Percentage of payments from the underlier which are passed through. */
    passThroughPercentage: string,
}


/**
 * Determines the currency rate that the seller of the equity amounts will apply at each valuation date for converting the respective amounts into a currency that is different from the currency denomination of the underlier.
 */
export interface Quanto {
    /** Specifies a currency conversion rate. */
    fxRate?: FxRate[],
    /** Specifies the methodology (reference source and, optionally, fixing time) to be used for determining a currency conversion rate. */
    fxSpotRateSource?: FxSpotRateSource,
    /** The time at which the spot currency exchange rate will be observed. It is specified as a time in a business day calendar location, e.g. 11:00am London time. */
    fixingTime?: BusinessCenterTime,
}


/**
 * A class for defining option strategy features.
 */
export interface StrategyFeature {
    /** Definition of the upper strike in a strike spread. */
    strikeSpread?: StrikeSpread,
    /** Definition of the later expiration date in a calendar spread. */
    calendarSpread?: CalendarSpread,
}


/**
 * A class for defining a strike spread feature.
 */
export interface StrikeSpread {
    /** Upper strike in a strike spread. */
    upperStrike: OptionStrike,
    /** Number of options at the upper strike price in a strike spread. */
    upperStrikeNumberOfOptions: string,
}


/**
 * A class defining the exercise period for an option together with any rules governing the notional amount of the underlying which can be exercised on any given exercise date and any associated exercise fees.
 */
export interface ExerciseTerms {
    /** Whether the option has a single exercise (european), multiple exercise dates (bermuda), or a continuous range of exercise (american). */
    style?: string,
    /** The first day of the exercise period for an American style option. */
    commencementDate?: AdjustableOrRelativeDate,
    /** The dates that define the Bermuda option exercise dates and the expiration date. The last specified date is assumed to be the expiration date. The dates can either be specified as a series of explicit dates and associated adjustments or as a series of dates defined relative to another schedule of dates, for example, the calculation period start dates. Where a relative series of dates are defined the first and last possible exercise dates can be separately specified. */
    exerciseDates?: AdjustableOrRelativeDates,
    /** The last day within an exercise period for an American style option. For a European style option it is the only day within the exercise period. */
    expirationDate?: AdjustableOrRelativeDate[],
    /** The effective date on the underlying product if the option is exercised.  For example, for a swaption it is the swap effective date, for an option on an FX spot or forward it is the value date for settlement, and in an extendible/cancelable provision it is the swap termination date, which is the date on which the termination is effective.' */
    relevantUnderlyingDate?: AdjustableOrRelativeDates,
    /** The earliest time at which notice of exercise can be given by the buyer to the seller (or seller's agent) to, and including, the expiration date. */
    earliestExerciseTime?: BusinessCenterTime,
    /** For a Bermuda or American style option, the latest time on an exercise business day (excluding the expiration date) within the exercise period that notice can be given by the buyer to the seller or seller's agent. Notice of exercise given after this time will be deemed to have been given on the next exercise business day. */
    latestExerciseTime?: BusinessCenterTime,
    /** The latest time for exercise on expirationDate. It is made mandatory given that for all option styles, this field is required. */
    expirationTime?: BusinessCenterTime,
    /** The time of day at which the equity option expires, for example the official closing time of the exchange. */
    expirationTimeType: string,
    /** As defined in the 2000 ISDA Definitions, Section 12.4. Multiple Exercise, the buyer of the option has the right to exercise all or less than all the unexercised notional amount of the underlying swap on one or more days in the exercise period, but on any such day may not exercise less than the minimum notional amount or more that the maximum notional amount, and if an integral multiple amount is specified, the notional amount exercised must be equal to, or be an integral multiple of, the integral multiple amount. */
    multipleExercise?: MultipleExercise,
    /** The fees associated with an exercise date. The fees are conditional on the exercise occurring. The fees can be specified as actual currency amounts or as percentages of the notional amount being exercised. */
    exerciseFeeSchedule?: ExerciseFeeSchedule,
    /** The set of parameters defining the procedure associated with the exercise, e.g. manual exercise. */
    exerciseProcedure?: ExerciseProcedure,
    /** A fee to be paid on exercise. This could be represented as an amount or a rate and notional reference on which to apply the rate. */
    exerciseFee?: ExerciseFee,
    /** As defined in the 2000 ISDA Definitions, Section 12.3. Partial Exercise, the buyer of the option has the right to exercise all or less than all the notional amount of the underlying swap on the expiration date, but may not exercise less than the minimum notional amount, and if an integral multiple amount is specified, the notional amount exercised must be equal to, or be an integral multiple of, the integral multiple amount. */
    partialExercise?: PartialExercise,
}


/**
 * A type to define automatic exercise of a swaption. With automatic exercise the option is deemed to have exercised if it is in the money by more than the threshold amount on the exercise date.
 */
export interface AutomaticExercise {
    /** A threshold rate. The threshold of 0.10% would be represented as 0.001 */
    thresholdRate?: string,
    /** Boolean that indicates if it has an automaticExercise */
    isApplicable?: boolean,
}


/**
 * A class defining the fee payable on exercise of an option. This fee may be defined as an amount or a percentage of the notional exercised. As a difference with FpML, it extends the BuyerSeller class.
 */
export interface ExerciseFee extends PayerReceiver {
    /** A pointer style reference to the associated notional schedule defined elsewhere in the document. */
    notionalReference: Money,
    /** The amount of fee to be paid on exercise. The fee currency is that of the referenced notional. */
    feeAmount?: string,
    /** A fee represented as a percentage of some referenced notional. A percentage of 5% would be represented as 0.05. */
    feeRate?: string,
    /** The date on which exercise fee(s) will be paid. It is specified as a relative date. */
    feePaymentDate: RelativeDateOffset,
}


/**
 * A class to define a fee or schedule of fees to be payable on the exercise of an option. This fee may be defined as an amount or a percentage of the notional exercised. As a difference with FpML, it extends the BuyerSeller class.
 */
export interface ExerciseFeeSchedule extends PayerReceiver {
    /** A pointer style reference to the associated notional schedule defined elsewhere in the document. */
    notionalReference: Money,
    /** The exercise fee amount schedule. The fees are expressed as currency amounts. The currency of the fee is assumed to be that of the notional schedule referenced. */
    feeAmountSchedule?: AmountSchedule,
    /** The exercise free rate schedule. The fees are expressed as percentage rates of the notional being exercised. The currency of the fee is assumed to be that of the notional schedule referenced. */
    feeRateSchedule?: Schedule,
    /** The date on which exercise fee(s) will be paid. It is specified as a relative date. */
    feePaymentDate: RelativeDateOffset,
}


/**
 * Defines to whom and where notice of execution should be given. The exerciseNoticeGiver refers to one or both of the principal parties of the trade. If present the exerciseNoticeReceiver refers to a party, other than the principal party, to whom notice should be given.
 */
export interface ExerciseNotice {
    /** Specifies the principal party of the trade that has the right to exercise. */
    exerciseNoticeGiver: string,
    /** Specifies the party to which notice of exercise should be given, e.g. by the buyer of the option. Although in many cases it is the buyer of the option who sends the exercise notice to the seller of the option, this component is reused, e.g. in case of OptionEarlyTermination, either or both parties have the right to exercise. */
    exerciseNoticeReceiver?: string,
    /** Specifies the location where the exercise must be reported, e.g. where the exercise notice receiver is based. */
    businessCenter: string,
}


/**
 * A class describing how notice of exercise should be given. This can be either manual or automatic.
 */
export interface ExerciseProcedure {
    /** Specifies that the notice of exercise must be given by the buyer to the seller or seller's agent. */
    manualExercise?: ManualExercise,
    /** If automatic is specified, then the notional amount of the underlying swap not previously exercised under the swaption will be automatically exercised at the expiration time on the expiration date if at such time the buyer is in-the-money, provided that the difference between the settlement rate and the fixed rate under the relevant underlying swap is not less than the specified threshold rate. The term in-the-money is assumed to have the meaning defining in the 2000 ISDA Definitions, Section 17.4 In-the-money. */
    automaticExercise?: AutomaticExercise,
    /** A flag to indicate whether follow-up confirmation of exercise (written or electronic) is required following telephonic notice by the buyer to the seller or seller's agent. */
    followUpConfirmation: boolean,
    /** Has the meaning defined as part of the 1997 ISDA Government Bond Option Definitions, section 4.5 Limited Right to Confirm Exercise. If present, (i) the Seller may request the Buyer to confirm its intent if not done on or before the expiration time on the Expiration date (ii) specific rules will apply in relation to the settlement mode. */
    limitedRightToConfirm?: boolean,
    /** Typically applicable to the physical settlement of bond and convertible bond options. If present, means that the party required to deliver the bonds will divide those to be delivered as notifying party desires to facilitate delivery obligations. */
    splitTicket?: boolean,
}


/**
 * A class defining manual exercise, i.e. that the option buyer counterparty must give notice to the option seller of exercise.
 */
export interface ManualExercise {
    /** Definition of the party to whom notice of exercise should be given. */
    exerciseNotice?: ExerciseNotice,
    /** If fallback exercise is specified then the notional amount of the underlying swap, not previously exercised under the swaption, will be automatically exercised at the expiration time on the expiration date if at such time the buyer is in-the-money, provided that the difference between the settlement rate and the fixed rate under the relevant underlying swap is not less than one tenth of a percentage point (0.10% or 0.001). The term in-the-money is assumed to have the meaning defined in the 2000 ISDA Definitions, Section 17.4. In-the-money. */
    fallbackExercise?: boolean,
}


/**
 * A class defining multiple exercises. As defined in the 2000 ISDA Definitions, Section 12.4. Multiple Exercise, the buyer of the option has the right to exercise all or less than all the unexercised notional amount of the underlying swap on one or more days in the exercise period, but on any such day may not exercise less than the minimum notional amount or more than the maximum notional amount, and if an integral multiple amount is specified, the notional exercised must be equal to or, be an integral multiple of, the integral multiple amount. In FpML, MultipleExercise is built upon the PartialExercise.model.
 */
export interface MultipleExercise extends PartialExercise {
    /** The maximum notional amount that can be exercised on a given exercise date. */
    maximumNotionalAmount?: string,
    /** The maximum number of options that can be exercised on a given exercise date. If the number is not specified, it means that the maximum number of options corresponds to the remaining unexercised options. */
    maximumNumberOfOptions?: number,
}


/**
 * A class defining partial exercise. As defined in the 2000 ISDA Definitions, Section 12.3 Partial Exercise, the buyer of the option may exercise all or less than all the notional amount of the underlying swap but may not be less than the minimum notional amount (if specified) and must be an integral multiple of the integral multiple amount if specified.
 */
export interface PartialExercise {
    /** A pointer style reference to the associated notional schedule defined elsewhere in the document. This element has been made optional as part of its integration in the OptionBaseExtended, because not required for the options on securities. */
    notionalReference?: Money,
    /** A notional amount which restricts the amount of notional that can be exercised when partial exercise or multiple exercise is applicable. The integral multiple amount defines a lower limit of notional that can be exercised and also defines a unit multiple of notional that can be exercised, i.e. only integer multiples of this amount can be exercised. */
    integralMultipleAmount?: string,
    /** The minimum notional amount that can be exercised on a given exercise date. See multipleExercise. */
    minimumNotionalAmount?: string,
    /** The minimum number of options that can be exercised on a given exercise date. */
    minimumNumberOfOptions?: number,
}


/**
 * A class describing a single cap or floor rate.
 */
export interface Strike {
    /** The rate for a cap or floor. */
    strikeRate: string,
    /** The buyer of the option. */
    buyer?: string,
    /** The party that has sold. */
    seller?: string,
}


/**
 * A class describing a schedule of cap or floor rates.
 */
export interface StrikeSchedule extends RateSchedule {
    /** The buyer of the option. */
    buyer?: string,
    /** The party that has sold. */
    seller?: string,
}


/**
 * Security finance payout specification in case the product payout involves some form of security collateral, as in a securities financing transaction. Plus additional description for ICMA.
 */
export interface AssetPayout extends PayoutBase {
    /** Defines each asset movement as a buy/sell at different dates, typically 1 near leg and 1 far leg in a securities financing transaction. */
    assetLeg: AssetLeg[],
    /** Specifies the Purchased Asset, usually a Security. */
    underlier: Asset,
    /** A contractual minimum amount which the borrower will pay, regardless of the duration of the loan. A mechanism for making sure that a trade generates enough income. */
    minimumFee?: Money,
    /** Specifies the terms under which dividends received by the borrower are passed through to the lender. */
    dividendTerms?: DividendTerms,
    /** The trade type, eg repurchase transaction or buy/sell-back. */
    tradeType?: string,
}


/**
 * Information related to dividends and payments.
 */
export interface DividendTerms {
    /** Specifies the proportion of the value of the dividend on the borrowed shares that the borrower is legally obligated to return to the lender. */
    manufacturedIncomeRequirement: DividendPayoutRatio,
    /** Defines the date on which the receiver of the equity return is entitled to the dividend. */
    dividendEntitlement?: string,
    /** daily fee increments accrue until a threshold is crossed, at which point payment becomes due) */
    minimumBillingAmount?: Money,
}


/**
 * Defines each asset movement of an asset payout.
 */
export interface AssetLeg {
    /** Specifies the settlement date of securities.  In a repo transaction the purchase date would always be the effective date as specified under Economic Terms, the repurchase date would always be the termination date as specified under Economic Terms. */
    settlementDate: AdjustableOrRelativeDate,
    /** Specifies a delivery method for the security transaction. */
    deliveryMethod: string,
}


/**
 * Specifies a transaction which automatically extends for a specified timeframe until the exercise of an embedded option.
 */
export interface EvergreenProvision {
    /** If evergreen termination is not available to both parties then this component specifies the buyer and seller of the option. */
    singlePartyOption?: PartyRole,
    /** The length of each evergreen extension period relative to the effective date of the preceding contract. */
    noticePeriod: RelativeDateOffset,
    /** Defines the minimum period before an evergreen is scheduled to terminate that notice can be given that it will terminate beyond the scheduled termination date. */
    noticeDeadlinePeriod?: RelativeDateOffset,
    /** A specific date and time for the notice deadline */
    noticeDeadlineDateTime?: string,
    /** The frequency with which the evergreen contract will be extended if notice is not given. */
    extensionFrequency: AdjustableRelativeOrPeriodicDates,
    /** An optional adjustment to the rate for the last period of the evergreen i.e. the period from when notice is given to stop rolling the contract through to the termination date. */
    finalPeriodFeeAdjustment?: Price,
}


/**
 * Represents the set of future cashflow methodologies in the form of specific payout data type(s) which result from the financial product.  Examples: a trade in a cash asset will use only a settlement payout; for derivatives, two interest rate payouts can be combined to specify an interest rate swap; one interest rate payout can be combined with a credit default payout to specify a credit default swap.
 */
export interface Payout {
}


/**
 * The underlying financial product that will be physically or cash settled, which can be of any type, eg an asset such as cash or a security, a product, or the cash settlement of an index rate.  Conditions are usually applied when used in a data type, such as a payout, to ensure this aligns with the use case.
 */
export interface Underlier {
}


/**
 * Enables either a TransferableProduct or a NonTransferableProduct to be used in an underlier.
 */
export interface Product {
}



export interface Document {
    finInstrmRptgTxRpt: FinInstrmRptgTxRpt,
}



export interface FinInstrmRptgTxRpt {
    tx: Tx,
}



export interface Tx {
    newTx: New,
    tradDt: string,
    tradgCpcty: string,
    qty: Qty,
    pric: Pric,
    tradVn: string,
    ctryOfBrnch: string,
}



export interface New {
    txId: string,
    exctgPty: string,
    invstmtPtyInd: string,
    submitgPty: string,
    buyr: Buyr,
    sellr: Sellr,
    ordrTrnsmssn: OrdrTrnsmssn,
    tx: Tx,
    finInstrm: FinInstrm,
    invstmtDcsnPrsn: InvstmtDcsnPrsn,
    exctgPrsn: ExctgPrsn,
    addtlAttrbts: AddtlAttrbts,
}



export interface Sellr {
    acctOwnr: AcctOwnr,
}



export interface Sngl {
    isin: string,
    indx: Indx,
}



export interface OrdrTrnsmssn {
    trnsmssnInd: string,
}



export interface DerivInstrmAttrbts {
    xpryDt: string,
    pricMltplr: string,
    undrlygInstrm: UndrlygInstrm,
    dlvryTp: string,
}



export interface UndrlygInstrm {
    swp: Swp,
}



export interface AcctOwnr {
    id: Id,
}



export interface InvstmtDcsnPrsn {
    prsn: Prsn,
}



export interface Buyr {
    acctOwnr: AcctOwnr,
}



export interface Qty {
    /** Qualifies the unit by which the amount is measured. Optional because a measure may be unit-less (e.g. when representing a ratio between amounts in the same unit). */
    unit: string,
}



export interface FinInstrm {
    othr: Othr,
}



export interface Pric {
    pric: Pric,
    bsisPts: string,
}



export interface Prsn {
    ctryOfBrnch: string,
    othr: Othr,
}



export interface FinInstrmGnlAttrbts {
    fullNm: string,
    clssfctnTp: string,
    ntnlCcy: string,
}



export interface Nm {
    refRate: RefRate,
    term: Term,
}



export interface SwpOut {
    sngl: Sngl,
}



export interface Othr {
    finInstrmGnlAttrbts: FinInstrmGnlAttrbts,
    derivInstrmAttrbts: DerivInstrmAttrbts,
    id: string,
    schmeNm: SchmeNm,
}



export interface ExctgPrsn {
    prsn: Prsn,
}



export interface Term {
    /** Qualifies the unit by which the amount is measured. Optional because a measure may be unit-less (e.g. when representing a ratio between amounts in the same unit). */
    unit: string,
    val: string,
}



export interface Swp {
    swpIn: SwpIn,
    swpOut: SwpOut,
}



export interface SchmeNm {
    prtry: string,
}



export interface RefRate {
    indx: string,
    nm: string,
}



export interface AddtlAttrbts {
    rskRdcgTx: string,
    sctiesFincgTxInd: string,
}



export interface SwpIn {
    sngl: Sngl,
}



export interface Indx {
    nm: Nm,
}



export interface Id {
    lei: string,
}




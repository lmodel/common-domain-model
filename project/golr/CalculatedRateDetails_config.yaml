# metamodel_version: 1.11.0id: CalculatedRateDetails
schema_generating: true
description: Type for reporting details of calculated rates, including the observations
  that went into the final reported rate.
display_name: CalculatedRateDetails
document_category: CalculatedRateDetails
weight: 20
fields:
- id: observations
  description: The observation dates and weights for each observation date.
  display_name: CalculatedRateDetails_observations
  cardinality: multi
- id: weightedRates
  description: The weighted value of each observation.
  display_name: weightedRates
  cardinality: multi
- id: growthFactor
  description: The daily growth factors, showing the weighted rates divided by the
    day count basis plus one, giving how much the value grows for each step in the
    calculation.
  display_name: growthFactor
  cardinality: multi
- id: compoundedGrowth
  description: The compounding curve, showing how the initial value grew during the
    calculation period.
  display_name: compoundedGrowth
  cardinality: multi
- id: aggregateValue
  description: The total sum or product of all the individual terms that went into
    the calculated rate.
  display_name: CalculatedRateDetails_aggregateValue
- id: aggregateWeight
  description: The total weight of all the terms that went into the calculated rate.
  display_name: aggregateWeight
- id: calculatedRate
  description: The resulting calculated weight.
  display_name: calculatedRate

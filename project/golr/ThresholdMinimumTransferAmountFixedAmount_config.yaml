# metamodel_version: 1.11.0id: ThresholdMinimumTransferAmountFixedAmount
schema_generating: true
description: Defines the Threshold or the Minimum Transfer Amount (MTA) as a fixed
  amount.
display_name: ThresholdMinimumTransferAmountFixedAmount
document_category: ThresholdMinimumTransferAmountFixedAmount
weight: 20
fields:
- id: zeroEvent
  description: Indicates whether a trigger applies for the Threshold or Minimum Transfer
    Amount to fall to zero. If 'True' then such a trigger exists.
  display_name: zeroEvent
- id: event
  description: Specifies the relevant trigger for the Threshold or Minimum Transfer
    Amount to fall to zero.
  display_name: event
  cardinality: multi
- id: amount
  description: Specifies the monetary value of the Threshold or the Minimum Transfer
    Amount (MTA) applicable to collateral transfer obligations.
  display_name: ThresholdMinimumTransferAmountFixedAmount_amount

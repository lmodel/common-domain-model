# metamodel_version: 1.11.0id: CalculateTransferInstruction
schema_generating: true
description: Defines the tradeState or payout on which to create a Transfer along
  with all necessary resets.
display_name: CalculateTransferInstruction
document_category: CalculateTransferInstruction
weight: 20
fields:
- id: tradeState
  display_name: tradeState
- id: payout
  display_name: CalculateTransferInstruction_payout
  cardinality: multi
- id: resets
  display_name: resets
  cardinality: multi
- id: payerReceiver
  display_name: payerReceiver
- id: quantity
  description: Specifies quantity amount returned if not the full amount from the
    TradeState, e.g. partial return
  display_name: quantity
- id: date
  display_name: CalculateTransferInstruction_date
  cardinality: multi

# metamodel_version: 1.11.0id: AdjustableOrRelativeDate
schema_generating: true
description: A class giving the choice between defining a date as an explicit date
  together with applicable adjustments or as relative to some other (anchor) date.
display_name: AdjustableOrRelativeDate
document_category: AdjustableOrRelativeDate
weight: 20
fields:
- id: adjustableDate
  description: A date that shall be subject to adjustment if it would otherwise fall
    on a day that is not a business day in the specified business centers, together
    with the convention for adjusting the date.
  display_name: adjustableDate
- id: relativeDate
  description: A date specified as some offset to another date (the anchor date).
  display_name: AdjustableOrRelativeDate_relativeDate

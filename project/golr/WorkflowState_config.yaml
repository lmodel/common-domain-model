# metamodel_version: 1.11.0id: WorkflowState
schema_generating: true
description: A class to specify workflow information, which is conceptually applicable
  to all lifecycle events.
display_name: WorkflowState
document_category: WorkflowState
weight: 20
fields:
- id: workflowStatus
  description: The workflow status indicator, e.g. Accepted, Rejected, ...
  display_name: workflowStatus
- id: comment
  description: A comment field to be associated with the workflow, e.g. to specify
    why a transaction event was rejected by a party.
  display_name: WorkflowState_comment
- id: partyCustomisedWorkflow
  description: Workflow data that is specific to certain market participants and is
    expressed as part of the CDM in a very generic manner, which can be party-specific.
    The initial use cases have been derived from the CME clearing and the DTCC TIW
    submissions.
  display_name: partyCustomisedWorkflow
  cardinality: multi
- id: warehouseIdentity
  description: The identity of the warehouse, if any, that is executing that workflow
    step.
  display_name: warehouseIdentity

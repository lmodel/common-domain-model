# metamodel_version: 1.11.0id: OptionStrike
schema_generating: true
description: Defines the strike price of an option.
display_name: OptionStrike
document_category: OptionStrike
weight: 20
fields:
- id: strikePrice
  description: Defines the strike of an option in the form of a price that could be
    a cash price, interestRate, or other types.
  display_name: strikePrice
- id: strikeReference
  description: Defines the strike of an option in reference to the spread of the underlying
    swap (typical practice in the case of an option on a credit single name swaps).
  display_name: strikeReference
- id: referenceSwapCurve
  description: Defines the strike of an option when expressed by reference to a swap
    curve (Typically the case for a convertible bond option).
  display_name: referenceSwapCurve
- id: averagingStrikeFeature
  description: Defines an  option strike that is calculated from an average of observed
    market prices.
  display_name: averagingStrikeFeature

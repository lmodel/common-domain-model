# metamodel_version: 1.11.0id: Rounding
schema_generating: true
description: Defines rounding rules and precision to be used in the rounding of a
  number.
display_name: Rounding
document_category: Rounding
weight: 20
fields:
- id: roundingDirection
  description: Specifies the rounding rounding rule as up, down, or nearest.
  display_name: Rounding_roundingDirection
- id: precision
  description: Specifies the rounding precision in terms of a number of decimal places
    when the number is evaluated in decimal form (not percentage), e.g. 0.09876543
    rounded to the nearest 5 decimal places is  0.0987654.
  display_name: precision

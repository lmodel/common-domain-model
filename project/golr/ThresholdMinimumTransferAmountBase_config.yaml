# metamodel_version: 1.11.0id: ThresholdMinimumTransferAmountBase
schema_generating: true
description: A logical container designed to hold a set of related data. In this case,
  designed to hold a set of information required to specify conditions under which
  Threshold or Minimum Transfer Amount can fall to zero.
display_name: ThresholdMinimumTransferAmountBase
document_category: ThresholdMinimumTransferAmountBase
weight: 20
fields:
- id: zeroEvent
  description: Indicates whether a trigger applies for the Threshold or Minimum Transfer
    Amount to fall to zero. If 'True' then such a trigger exists.
  display_name: zeroEvent
- id: event
  description: Specifies the relevant trigger for the Threshold or Minimum Transfer
    Amount to fall to zero.
  display_name: event
  cardinality: multi

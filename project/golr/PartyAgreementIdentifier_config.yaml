# metamodel_version: 1.11.0id: PartyAgreementIdentifier
schema_generating: true
description: A class defining a legal agreement identifier issued by the indicated
  party.
display_name: PartyAgreementIdentifier
document_category: PartyAgreementIdentifier
weight: 20
fields:
- id: partyReference
  description: Party that issued the document identifier.
  display_name: PartyAgreementIdentifier_partyReference
- id: documentIdentifier
  description: While FpML specifies the document identifier with a value and an associated
    scheme, the CDM makes use of the Identifier, which has an explicit issuer. The
    issuer of this identifier is not necessarily the same as the party reference.
  display_name: documentIdentifier
  cardinality: multi

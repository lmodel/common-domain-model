# metamodel_version: 1.11.0id: PeriodBound
schema_generating: true
description: Indicator to specify if the period bound is defined as a period and whether
  the bound is inclusive.
display_name: PeriodBound
document_category: PeriodBound
weight: 20
fields:
- id: period
  description: Specifies the period is to be used as the bound, e.g. 5Y.
  display_name: PeriodBound_period
- id: inclusive
  description: Specifies whether the period bound is inclusive, e.g. for a lower bound,
    false would indicate greater than, whereas true would indicate greater than or
    equal to.
  display_name: inclusive

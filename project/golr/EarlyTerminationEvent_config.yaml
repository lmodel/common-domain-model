# metamodel_version: 1.11.0id: EarlyTerminationEvent
schema_generating: true
description: 'A data to:  define the adjusted dates associated with an early termination
  provision.'
display_name: EarlyTerminationEvent
document_category: EarlyTerminationEvent
weight: 20
fields:
- id: adjustedExerciseDate
  description: The date on which option exercise takes place. This date should already
    be adjusted for any applicable business day convention.
  display_name: EarlyTerminationEvent_adjustedExerciseDate
- id: adjustedEarlyTerminationDate
  description: The early termination date that is applicable if an early termination
    provision is exercised. This date should already be adjusted for any applicable
    business day convention.
  display_name: adjustedEarlyTerminationDate
- id: adjustedCashSettlementValuationDate
  description: The date by which the cash settlement amount must be agreed. This date
    should already be adjusted for any applicable business day convention.
  display_name: EarlyTerminationEvent_adjustedCashSettlementValuationDate
- id: adjustedCashSettlementPaymentDate
  description: The date on which the cash settlement amount is paid. This date should
    already be adjusted for any applicable business date convention.
  display_name: EarlyTerminationEvent_adjustedCashSettlementPaymentDate
- id: adjustedExerciseFeePaymentDate
  description: The date on which the exercise fee amount is paid. This date should
    already be adjusted for any applicable business day convention.
  display_name: adjustedExerciseFeePaymentDate
